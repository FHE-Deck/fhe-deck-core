#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_calculator_boolean(FHEContext& ctx, uint8_t op = 0, uint16_t x = 0, uint16_t y = 0)
{
    Ciphertext ct_op0 = ctx.encrypt_public((op >> 0) & 1);
    Ciphertext ct_op1 = ctx.encrypt_public((op >> 1) & 1);
    Ciphertext ct_op2 = ctx.encrypt_public((op >> 2) & 1);
    Ciphertext ct_op3 = ctx.encrypt_public((op >> 3) & 1);
    Ciphertext ct_op4 = ctx.encrypt_public((op >> 4) & 1);
    Ciphertext ct_op5 = ctx.encrypt_public((op >> 5) & 1);
    Ciphertext ct_op6 = ctx.encrypt_public((op >> 6) & 1);
    Ciphertext ct_op7 = ctx.encrypt_public((op >> 7) & 1);
    Ciphertext ct_x0 = ctx.encrypt_public((x >> 0) & 1);
    Ciphertext ct_x1 = ctx.encrypt_public((x >> 1) & 1);
    Ciphertext ct_x2 = ctx.encrypt_public((x >> 2) & 1);
    Ciphertext ct_x3 = ctx.encrypt_public((x >> 3) & 1);
    Ciphertext ct_x4 = ctx.encrypt_public((x >> 4) & 1);
    Ciphertext ct_x5 = ctx.encrypt_public((x >> 5) & 1);
    Ciphertext ct_x6 = ctx.encrypt_public((x >> 6) & 1);
    Ciphertext ct_x7 = ctx.encrypt_public((x >> 7) & 1);
    Ciphertext ct_x8 = ctx.encrypt_public((x >> 8) & 1);
    Ciphertext ct_x9 = ctx.encrypt_public((x >> 9) & 1);
    Ciphertext ct_x10 = ctx.encrypt_public((x >> 10) & 1);
    Ciphertext ct_x11 = ctx.encrypt_public((x >> 11) & 1);
    Ciphertext ct_x12 = ctx.encrypt_public((x >> 12) & 1);
    Ciphertext ct_x13 = ctx.encrypt_public((x >> 13) & 1);
    Ciphertext ct_x14 = ctx.encrypt_public((x >> 14) & 1);
    Ciphertext ct_x15 = ctx.encrypt_public((x >> 15) & 1);
    Ciphertext ct_y0 = ctx.encrypt_public((y >> 0) & 1);
    Ciphertext ct_y1 = ctx.encrypt_public((y >> 1) & 1);
    Ciphertext ct_y2 = ctx.encrypt_public((y >> 2) & 1);
    Ciphertext ct_y3 = ctx.encrypt_public((y >> 3) & 1);
    Ciphertext ct_y4 = ctx.encrypt_public((y >> 4) & 1);
    Ciphertext ct_y5 = ctx.encrypt_public((y >> 5) & 1);
    Ciphertext ct_y6 = ctx.encrypt_public((y >> 6) & 1);
    Ciphertext ct_y7 = ctx.encrypt_public((y >> 7) & 1);
    Ciphertext ct_y8 = ctx.encrypt_public((y >> 8) & 1);
    Ciphertext ct_y9 = ctx.encrypt_public((y >> 9) & 1);
    Ciphertext ct_y10 = ctx.encrypt_public((y >> 10) & 1);
    Ciphertext ct_y11 = ctx.encrypt_public((y >> 11) & 1);
    Ciphertext ct_y12 = ctx.encrypt_public((y >> 12) & 1);
    Ciphertext ct_y13 = ctx.encrypt_public((y >> 13) & 1);
    Ciphertext ct_y14 = ctx.encrypt_public((y >> 14) & 1);
    Ciphertext ct_y15 = ctx.encrypt_public((y >> 15) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_y2 + ct_x0;
    Ciphertext gout0 = ctx.eval_lut(&gin0, and2);
    std::cerr << "\rINV1   ";
    Ciphertext gout1 = 1 - ct_y14;

    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_y2 + ct_x5;
    Ciphertext gout2 = ctx.eval_lut(&gin2, and2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_y2 + ct_x2;
    Ciphertext gout3 = ctx.eval_lut(&gin3, and2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_y7 + ct_x0;
    Ciphertext gout4 = ctx.eval_lut(&gin4, and2);
    std::cerr << "\rINV5   ";
    Ciphertext gout5 = 1 - ct_y1;

    std::cerr << "\rINV6   ";
    Ciphertext gout6 = 1 - ct_x8;

    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_y8 + ct_x4;
    Ciphertext gout7 = ctx.eval_lut(&gin7, and2);
    std::cerr << "\rINV8   ";
    Ciphertext gout8 = 1 - ct_y5;

    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_y8 + ct_x5;
    Ciphertext gout9 = ctx.eval_lut(&gin9, and2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_y11 + ct_x2;
    Ciphertext gout10 = ctx.eval_lut(&gin10, and2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_y2 + ct_x3;
    Ciphertext gout11 = ctx.eval_lut(&gin11, and2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_y3 + ct_x2;
    Ciphertext gout12 = ctx.eval_lut(&gin12, and2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_y3 + ct_x6;
    Ciphertext gout13 = ctx.eval_lut(&gin13, and2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_y8 + ct_x0;
    Ciphertext gout14 = ctx.eval_lut(&gin14, and2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_y0 + ct_x2;
    Ciphertext gout15 = ctx.eval_lut(&gin15, and2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_y0 + ct_x5;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nand2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_y1 + ct_x7;
    Ciphertext gout17 = ctx.eval_lut(&gin17, and2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_y3 + ct_x9;
    Ciphertext gout18 = ctx.eval_lut(&gin18, and2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * (1 - ct_op2) + ct_op1;
    Ciphertext gout19 = ctx.eval_lut(&gin19, and2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_y13 + ct_x1;
    Ciphertext gout20 = ctx.eval_lut(&gin20, and2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_y4 + ct_x1;
    Ciphertext gout21 = ctx.eval_lut(&gin21, and2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_y6 + ct_x5;
    Ciphertext gout22 = ctx.eval_lut(&gin22, and2);
    std::cerr << "\rINV23   ";
    Ciphertext gout23 = 1 - ct_x4;

    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_y11 + ct_x0;
    Ciphertext gout24 = ctx.eval_lut(&gin24, and2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_y2 + ct_x8;
    Ciphertext gout25 = ctx.eval_lut(&gin25, and2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_y0 + ct_x4;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nand2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_y3 + ct_x12;
    Ciphertext gout27 = ctx.eval_lut(&gin27, and2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_y9 + ct_x6;
    Ciphertext gout28 = ctx.eval_lut(&gin28, and2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_y6 + ct_x9;
    Ciphertext gout29 = ctx.eval_lut(&gin29, and2);
    std::cerr << "\rINV30   ";
    Ciphertext gout30 = 1 - ct_x13;

    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_y0 + ct_x10;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nand2);
    std::cerr << "\rINV32   ";
    Ciphertext gout32 = 1 - ct_x12;

    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_y4 + ct_x6;
    Ciphertext gout33 = ctx.eval_lut(&gin33, and2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * ct_y0 + ct_x13;
    Ciphertext gout34 = ctx.eval_lut(&gin34, and2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * ct_y0 + ct_x8;
    Ciphertext gout35 = ctx.eval_lut(&gin35, nand2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * ct_y10 + ct_x5;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * ct_y12 + ct_x0;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * ct_y5 + ct_x1;
    Ciphertext gout38 = ctx.eval_lut(&gin38, and2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * ct_y10 + ct_x1;
    Ciphertext gout39 = ctx.eval_lut(&gin39, and2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * ct_y3 + ct_x11;
    Ciphertext gout40 = ctx.eval_lut(&gin40, nand2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_y1 + ct_x5;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * ct_y1 + ct_x3;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rINV43   ";
    Ciphertext gout43 = 1 - ct_x9;

    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * ct_y3 + ct_x5;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rINV45   ";
    Ciphertext gout45 = 1 - ct_y6;

    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * ct_y2 + ct_x7;
    Ciphertext gout46 = ctx.eval_lut(&gin46, and2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * ct_y0 + ct_x14;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * ct_y13 + ct_x2;
    Ciphertext gout48 = ctx.eval_lut(&gin48, and2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * (1 - ct_x0) + ct_y15;
    Ciphertext gout49 = ctx.eval_lut(&gin49, and2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_y5 + ct_x6;
    Ciphertext gout50 = ctx.eval_lut(&gin50, and2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * ct_y0 + ct_x9;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rINV52   ";
    Ciphertext gout52 = 1 - ct_y2;

    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * ct_y2 + ct_x4;
    Ciphertext gout53 = ctx.eval_lut(&gin53, and2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * ct_y4 + ct_x4;
    Ciphertext gout54 = ctx.eval_lut(&gin54, and2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_y2 + ct_x6;
    Ciphertext gout55 = ctx.eval_lut(&gin55, and2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * ct_y4 + ct_x9;
    Ciphertext gout56 = ctx.eval_lut(&gin56, and2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * ct_op6 + ct_op7;
    Ciphertext gout57 = ctx.eval_lut(&gin57, nor2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_y0 + ct_x15;
    Ciphertext gout58 = ctx.eval_lut(&gin58, and2);
    std::cerr << "\rINV59   ";
    Ciphertext gout59 = 1 - ct_op0;

    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * ct_y3 + ct_x3;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * ct_y5 + ct_x5;
    Ciphertext gout61 = ctx.eval_lut(&gin61, and2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * ct_y8 + ct_x6;
    Ciphertext gout62 = ctx.eval_lut(&gin62, and2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * ct_y6 + ct_x1;
    Ciphertext gout63 = ctx.eval_lut(&gin63, nand2);
    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * ct_y5 + ct_x7;
    Ciphertext gout64 = ctx.eval_lut(&gin64, and2);
    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * ct_y9 + ct_x3;
    Ciphertext gout65 = ctx.eval_lut(&gin65, and2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_y1 + ct_x1;
    Ciphertext gout66 = ctx.eval_lut(&gin66, nand2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_y1 + ct_x11;
    Ciphertext gout67 = ctx.eval_lut(&gin67, and2);
    std::cerr << "\rINV68   ";
    Ciphertext gout68 = 1 - ct_x14;

    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * ct_y1 + ct_x13;
    Ciphertext gout69 = ctx.eval_lut(&gin69, and2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_y0 + ct_x12;
    Ciphertext gout70 = ctx.eval_lut(&gin70, and2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_y0 + ct_x7;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_y10 + ct_x2;
    Ciphertext gout72 = ctx.eval_lut(&gin72, and2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * ct_y8 + ct_x3;
    Ciphertext gout73 = ctx.eval_lut(&gin73, and2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * ct_y5 + ct_x9;
    Ciphertext gout74 = ctx.eval_lut(&gin74, and2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * ct_y0 + ct_x1;
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * ct_y1 + ct_x4;
    Ciphertext gout76 = ctx.eval_lut(&gin76, and2);
    std::cerr << "\rINV77   ";
    Ciphertext gout77 = 1 - ct_y8;

    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * ct_y0 + ct_x0;
    Ciphertext gout78 = ctx.eval_lut(&gin78, xnor2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * ct_y1 + ct_x6;
    Ciphertext gout79 = ctx.eval_lut(&gin79, and2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * ct_y2 + ct_x11;
    Ciphertext gout80 = ctx.eval_lut(&gin80, and2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * ct_y3 + ct_x0;
    Ciphertext gout81 = ctx.eval_lut(&gin81, nand2);
    std::cerr << "\rINV82   ";
    Ciphertext gout82 = 1 - ct_y7;

    std::cerr << "\rINV83   ";
    Ciphertext gout83 = 1 - ct_x1;

    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * ct_y9 + ct_x4;
    Ciphertext gout84 = ctx.eval_lut(&gin84, and2);
    std::cerr << "\rLUT85   ";
    Ciphertext gin85 = 2 * ct_y5 + ct_x3;
    Ciphertext gout85 = ctx.eval_lut(&gin85, and2);
    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * ct_y1 + ct_x8;
    Ciphertext gout86 = ctx.eval_lut(&gin86, and2);
    std::cerr << "\rINV87   ";
    Ciphertext gout87 = 1 - ct_x10;

    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * ct_y9 + ct_x0;
    Ciphertext gout88 = ctx.eval_lut(&gin88, and2);
    std::cerr << "\rINV89   ";
    Ciphertext gout89 = 1 - ct_y10;

    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * ct_y1 + ct_x0;
    Ciphertext gout90 = ctx.eval_lut(&gin90, and2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * ct_y8 + ct_x2;
    Ciphertext gout91 = ctx.eval_lut(&gin91, and2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * ct_y5 + ct_x0;
    Ciphertext gout92 = ctx.eval_lut(&gin92, and2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * ct_y10 + ct_x4;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * ct_y2 + ct_x1;
    Ciphertext gout94 = ctx.eval_lut(&gin94, and2);
    std::cerr << "\rINV95   ";
    Ciphertext gout95 = 1 - ct_x7;

    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * ct_y7 + ct_x3;
    Ciphertext gout96 = ctx.eval_lut(&gin96, and2);
    std::cerr << "\rINV97   ";
    Ciphertext gout97 = 1 - ct_x6;

    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * ct_y4 + ct_x11;
    Ciphertext gout98 = ctx.eval_lut(&gin98, and2);
    std::cerr << "\rINV99   ";
    Ciphertext gout99 = 1 - ct_y9;

    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * ct_y6 + ct_x4;
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * ct_y0 + ct_x6;
    Ciphertext gout101 = ctx.eval_lut(&gin101, nand2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * ct_y6 + ct_x7;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * ct_y13 + ct_x0;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * ct_y1 + ct_x10;
    Ciphertext gout104 = ctx.eval_lut(&gin104, and2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * (1 - ct_op4) + ct_op5;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * ct_y1 + ct_x9;
    Ciphertext gout106 = ctx.eval_lut(&gin106, and2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * ct_y9 + ct_x2;
    Ciphertext gout107 = ctx.eval_lut(&gin107, and2);
    std::cerr << "\rLUT108   ";
    Ciphertext gin108 = 2 * (1 - ct_op1) + ct_op0;
    Ciphertext gout108 = ctx.eval_lut(&gin108, and2);
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * ct_y6 + ct_x0;
    Ciphertext gout109 = ctx.eval_lut(&gin109, and2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * ct_y6 + ct_x2;
    Ciphertext gout110 = ctx.eval_lut(&gin110, and2);
    std::cerr << "\rINV111   ";
    Ciphertext gout111 = 1 - ct_y4;

    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * ct_y2 + ct_x13;
    Ciphertext gout112 = ctx.eval_lut(&gin112, and2);
    std::cerr << "\rINV113   ";
    Ciphertext gout113 = 1 - ct_x5;

    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * ct_y1 + ct_x12;
    Ciphertext gout114 = ctx.eval_lut(&gin114, and2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * ct_y5 + ct_x10;
    Ciphertext gout115 = ctx.eval_lut(&gin115, and2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * ct_y15 + ct_x15;
    Ciphertext gout116 = ctx.eval_lut(&gin116, xor2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * ct_y7 + ct_x4;
    Ciphertext gout117 = ctx.eval_lut(&gin117, and2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * ct_y4 + ct_x2;
    Ciphertext gout118 = ctx.eval_lut(&gin118, and2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * ct_y5 + ct_x8;
    Ciphertext gout119 = ctx.eval_lut(&gin119, and2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * ct_op2 + ct_op3;
    Ciphertext gout120 = ctx.eval_lut(&gin120, and2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * ct_y7 + ct_x7;
    Ciphertext gout121 = ctx.eval_lut(&gin121, nand2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * ct_y9 + ct_x5;
    Ciphertext gout122 = ctx.eval_lut(&gin122, and2);
    std::cerr << "\rINV123   ";
    Ciphertext gout123 = 1 - ct_x2;

    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * ct_y1 + ct_x2;
    Ciphertext gout124 = ctx.eval_lut(&gin124, and2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * ct_y5 + ct_x2;
    Ciphertext gout125 = ctx.eval_lut(&gin125, and2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * ct_y10 + ct_x0;
    Ciphertext gout126 = ctx.eval_lut(&gin126, and2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * ct_y4 + ct_x5;
    Ciphertext gout127 = ctx.eval_lut(&gin127, and2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * ct_y2 + ct_x10;
    Ciphertext gout128 = ctx.eval_lut(&gin128, and2);
    std::cerr << "\rINV129   ";
    Ciphertext gout129 = 1 - ct_y3;

    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * ct_y7 + ct_x2;
    Ciphertext gout130 = ctx.eval_lut(&gin130, and2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * ct_y9 + ct_x1;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rINV132   ";
    Ciphertext gout132 = 1 - ct_x11;

    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * ct_y4 + ct_x3;
    Ciphertext gout133 = ctx.eval_lut(&gin133, and2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * ct_y14 + ct_x0;
    Ciphertext gout134 = ctx.eval_lut(&gin134, and2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * ct_y5 + ct_x4;
    Ciphertext gout135 = ctx.eval_lut(&gin135, and2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * ct_y12 + ct_x3;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_y14 + ct_x1;
    Ciphertext gout137 = ctx.eval_lut(&gin137, and2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * ct_y4 + ct_x7;
    Ciphertext gout138 = ctx.eval_lut(&gin138, and2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_y3 + ct_x7;
    Ciphertext gout139 = ctx.eval_lut(&gin139, and2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * ct_y4 + ct_x0;
    Ciphertext gout140 = ctx.eval_lut(&gin140, and2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * ct_y3 + ct_x4;
    Ciphertext gout141 = ctx.eval_lut(&gin141, and2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_y3 + ct_x1;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * ct_y6 + ct_x6;
    Ciphertext gout143 = ctx.eval_lut(&gin143, and2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * ct_y0 + ct_x11;
    Ciphertext gout144 = ctx.eval_lut(&gin144, nand2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * ct_y12 + ct_x2;
    Ciphertext gout145 = ctx.eval_lut(&gin145, nand2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * ct_y0 + ct_x0;
    Ciphertext gout146 = ctx.eval_lut(&gin146, and2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_y6 + ct_x3;
    Ciphertext gout147 = ctx.eval_lut(&gin147, and2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * ct_y2 + ct_x9;
    Ciphertext gout148 = ctx.eval_lut(&gin148, and2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * ct_y7 + ct_x1;
    Ciphertext gout149 = ctx.eval_lut(&gin149, and2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * ct_y1 + ct_x14;
    Ciphertext gout150 = ctx.eval_lut(&gin150, and2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_y10 + ct_x3;
    Ciphertext gout151 = ctx.eval_lut(&gin151, and2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_y7 + ct_x6;
    Ciphertext gout152 = ctx.eval_lut(&gin152, and2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * ct_y8 + ct_x7;
    Ciphertext gout153 = ctx.eval_lut(&gin153, and2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * ct_y6 + ct_x8;
    Ciphertext gout154 = ctx.eval_lut(&gin154, and2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * ct_y11 + ct_x3;
    Ciphertext gout155 = ctx.eval_lut(&gin155, and2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * ct_y8 + ct_x1;
    Ciphertext gout156 = ctx.eval_lut(&gin156, and2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * ct_y2 + ct_x12;
    Ciphertext gout157 = ctx.eval_lut(&gin157, and2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * ct_y3 + ct_x8;
    Ciphertext gout158 = ctx.eval_lut(&gin158, and2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * ct_y3 + ct_x10;
    Ciphertext gout159 = ctx.eval_lut(&gin159, and2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * ct_y7 + ct_x5;
    Ciphertext gout160 = ctx.eval_lut(&gin160, and2);
    std::cerr << "\rINV161   ";
    Ciphertext gout161 = 1 - ct_x3;

    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * ct_y4 + ct_x10;
    Ciphertext gout162 = ctx.eval_lut(&gin162, and2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_y7 + ct_x8;
    Ciphertext gout163 = ctx.eval_lut(&gin163, and2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_y12 + ct_x1;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * ct_y11 + ct_x4;
    Ciphertext gout165 = ctx.eval_lut(&gin165, and2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * ct_y0 + ct_x3;
    Ciphertext gout166 = ctx.eval_lut(&gin166, and2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_y4 + ct_x8;
    Ciphertext gout167 = ctx.eval_lut(&gin167, and2);
    std::cerr << "\rINV168   ";
    Ciphertext gout168 = 1 - ct_y15;

    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * ct_y11 + ct_x1;
    Ciphertext gout169 = ctx.eval_lut(&gin169, and2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * ct_y1 + ct_y0;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nor2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout12 + gout11;
    Ciphertext gout171 = ctx.eval_lut(&gin171, xor2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout13 + gout46;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * gout16 + (1 - gout56);
    Ciphertext gout173 = ctx.eval_lut(&gin173, or2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout56 + gout34;
    Ciphertext gout174 = ctx.eval_lut(&gin174, nand2);
    std::cerr << "\rINV175   ";
    Ciphertext gout175 = 1 - gout37;

    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * gout12 + gout11;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * ct_op3 + gout19;
    Ciphertext gout177 = ctx.eval_lut(&gin177, and2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * gout13 + gout46;
    Ciphertext gout178 = ctx.eval_lut(&gin178, xor2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * gout44 + gout55;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout66 + gout0;
    Ciphertext gout180 = ctx.eval_lut(&gin180, xnor2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * gout88 + gout39;
    Ciphertext gout181 = ctx.eval_lut(&gin181, and2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * gout37 + gout20;
    Ciphertext gout182 = ctx.eval_lut(&gin182, and2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * gout94 + gout81;
    Ciphertext gout183 = ctx.eval_lut(&gin183, xnor2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout39 + gout107;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * gout115 + gout29;
    Ciphertext gout185 = ctx.eval_lut(&gin185, xnor2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * gout96 + gout100;
    Ciphertext gout186 = ctx.eval_lut(&gin186, xor2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * gout117 + gout22;
    Ciphertext gout187 = ctx.eval_lut(&gin187, xor2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout84 + gout36;
    Ciphertext gout188 = ctx.eval_lut(&gin188, nand2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * gout16 + gout21;
    Ciphertext gout189 = ctx.eval_lut(&gin189, xnor2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout72 + gout65;
    Ciphertext gout190 = ctx.eval_lut(&gin190, nand2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout72 + gout65;
    Ciphertext gout191 = ctx.eval_lut(&gin191, xor2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * gout15 + gout33;
    Ciphertext gout192 = ctx.eval_lut(&gin192, nand2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout76 + gout171;
    Ciphertext gout193 = ctx.eval_lut(&gin193, xor2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * gout80 + gout27;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * gout76 + gout171;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout96 + gout100;
    Ciphertext gout196 = ctx.eval_lut(&gin196, nand2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout26 + gout140;
    Ciphertext gout197 = ctx.eval_lut(&gin197, xnor2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * gout18 + gout128;
    Ciphertext gout198 = ctx.eval_lut(&gin198, nand2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout146 + gout54;
    Ciphertext gout199 = ctx.eval_lut(&gin199, and2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout109 + gout149;
    Ciphertext gout200 = ctx.eval_lut(&gin200, and2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout71 + (1 - gout98);
    Ciphertext gout201 = ctx.eval_lut(&gin201, or2);
    std::cerr << "\rINV202   ";
    Ciphertext gout202 = 1 - gout15;

    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout149 + gout110;
    Ciphertext gout203 = ctx.eval_lut(&gin203, xor2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout60 + gout53;
    Ciphertext gout204 = ctx.eval_lut(&gin204, nand2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout94 + gout12;
    Ciphertext gout205 = ctx.eval_lut(&gin205, nand2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout105 + gout57;
    Ciphertext gout206 = ctx.eval_lut(&gin206, and2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout60 + gout53;
    Ciphertext gout207 = ctx.eval_lut(&gin207, xor2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout52 + gout170;
    Ciphertext gout208 = ctx.eval_lut(&gin208, and2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout63 + gout4;
    Ciphertext gout209 = ctx.eval_lut(&gin209, xnor2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout90 + gout94;
    Ciphertext gout210 = ctx.eval_lut(&gin210, and2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout33 + gout31;
    Ciphertext gout211 = ctx.eval_lut(&gin211, xnor2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout160 + gout143;
    Ciphertext gout212 = ctx.eval_lut(&gin212, xor2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout121 + gout154;
    Ciphertext gout213 = ctx.eval_lut(&gin213, xnor2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout124 + gout183;
    Ciphertext gout214 = ctx.eval_lut(&gin214, xor2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout54 + gout35;
    Ciphertext gout215 = ctx.eval_lut(&gin215, xnor2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout124 + gout183;
    Ciphertext gout216 = ctx.eval_lut(&gin216, nand2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout139 + gout25;
    Ciphertext gout217 = ctx.eval_lut(&gin217, xor2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout18 + gout128;
    Ciphertext gout218 = ctx.eval_lut(&gin218, xor2);
    std::cerr << "\rINV219   ";
    Ciphertext gout219 = 1 - gout166;

    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout149 + gout110;
    Ciphertext gout220 = ctx.eval_lut(&gin220, nand2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout117 + gout22;
    Ciphertext gout221 = ctx.eval_lut(&gin221, nand2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout75 + gout127;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout127 + gout51;
    Ciphertext gout223 = ctx.eval_lut(&gin223, xnor2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout152 + gout102;
    Ciphertext gout224 = ctx.eval_lut(&gin224, xor2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout56 + gout34;
    Ciphertext gout225 = ctx.eval_lut(&gin225, xor2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout159 + gout80;
    Ciphertext gout226 = ctx.eval_lut(&gin226, nand2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout151 + gout84;
    Ciphertext gout227 = ctx.eval_lut(&gin227, xor2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout102 + gout163;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout90 + gout75;
    Ciphertext gout229 = ctx.eval_lut(&gin229, xor2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout20 + gout145;
    Ciphertext gout230 = ctx.eval_lut(&gin230, xnor2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout166 + gout138;
    Ciphertext gout231 = ctx.eval_lut(&gin231, nand2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout44 + gout55;
    Ciphertext gout232 = ctx.eval_lut(&gin232, xor2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout169 + gout191;
    Ciphertext gout233 = ctx.eval_lut(&gin233, xor2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout158 + gout148;
    Ciphertext gout234 = ctx.eval_lut(&gin234, xor2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout158 + gout148;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout131 + gout126;
    Ciphertext gout236 = ctx.eval_lut(&gin236, xnor2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout160 + gout143;
    Ciphertext gout237 = ctx.eval_lut(&gin237, nand2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout167 + gout70;
    Ciphertext gout238 = ctx.eval_lut(&gin238, nand2);
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout167 + gout70;
    Ciphertext gout239 = ctx.eval_lut(&gin239, xor2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout163 + gout185;
    Ciphertext gout240 = ctx.eval_lut(&gin240, xnor2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout39 + gout107;
    Ciphertext gout241 = ctx.eval_lut(&gin241, xor2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout101 + gout118;
    Ciphertext gout242 = ctx.eval_lut(&gin242, xnor2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * gout146 + (1 - gout66);
    Ciphertext gout243 = ctx.eval_lut(&gin243, and2);
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout141 + gout2;
    Ciphertext gout244 = ctx.eval_lut(&gin244, nand2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * gout152 + gout102;
    Ciphertext gout245 = ctx.eval_lut(&gin245, nand2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout164 + (1 - gout48);
    Ciphertext gout246 = ctx.eval_lut(&gin246, or2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout139 + gout25;
    Ciphertext gout247 = ctx.eval_lut(&gin247, nand2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout71 + gout133;
    Ciphertext gout248 = ctx.eval_lut(&gin248, xnor2);
    std::cerr << "\rINV249   ";
    Ciphertext gout249 = 1 - gout170;

    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout159 + gout80;
    Ciphertext gout250 = ctx.eval_lut(&gin250, xor2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout141 + gout2;
    Ciphertext gout251 = ctx.eval_lut(&gin251, xor2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout164 + gout103;
    Ciphertext gout252 = ctx.eval_lut(&gin252, xnor2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout162 + gout47;
    Ciphertext gout253 = ctx.eval_lut(&gin253, xor2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout151 + gout84;
    Ciphertext gout254 = ctx.eval_lut(&gin254, nand2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout169 + gout191;
    Ciphertext gout255 = ctx.eval_lut(&gin255, nand2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout49 + gout137;
    Ciphertext gout256 = ctx.eval_lut(&gin256, xnor2);
    std::cerr << "\rINV257   ";
    Ciphertext gout257 = 1 - gout146;

    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout168 + gout27;
    Ciphertext gout258 = ctx.eval_lut(&gin258, xnor2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout150 + gout58;
    Ciphertext gout259 = ctx.eval_lut(&gin259, xnor2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * gout101 + (1 - gout162);
    Ciphertext gout260 = ctx.eval_lut(&gin260, or2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout93 + gout122;
    Ciphertext gout261 = ctx.eval_lut(&gin261, xnor2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout26 + (1 - gout167);
    Ciphertext gout262 = ctx.eval_lut(&gin262, or2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * (1 - gout0) + gout142;
    Ciphertext gout263 = ctx.eval_lut(&gin263, or2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout142 + gout3;
    Ciphertext gout264 = ctx.eval_lut(&gin264, xnor2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout153 + gout28;
    Ciphertext gout265 = ctx.eval_lut(&gin265, xnor2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout108 + gout120;
    Ciphertext gout266 = ctx.eval_lut(&gin266, and2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout130 + gout147;
    Ciphertext gout267 = ctx.eval_lut(&gin267, xor2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout130 + gout147;
    Ciphertext gout268 = ctx.eval_lut(&gin268, nand2);
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout162 + gout47;
    Ciphertext gout269 = ctx.eval_lut(&gin269, nand2);
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout165 + gout136;
    Ciphertext gout270 = ctx.eval_lut(&gin270, xnor2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout138 + gout144;
    Ciphertext gout271 = ctx.eval_lut(&gin271, xnor2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout40 + gout157;
    Ciphertext gout272 = ctx.eval_lut(&gin272, xnor2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout86 + gout178;
    Ciphertext gout273 = ctx.eval_lut(&gin273, xor2);
    std::cerr << "\rINV274   ";
    Ciphertext gout274 = 1 - gout181;

    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout91 + gout186;
    Ciphertext gout275 = ctx.eval_lut(&gin275, xor2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * gout202 + gout180;
    Ciphertext gout276 = ctx.eval_lut(&gin276, xnor2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout14 + gout203;
    Ciphertext gout277 = ctx.eval_lut(&gin277, nand2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout15 + gout180;
    Ciphertext gout278 = ctx.eval_lut(&gin278, and2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout14 + gout203;
    Ciphertext gout279 = ctx.eval_lut(&gin279, xor2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout92 + gout189;
    Ciphertext gout280 = ctx.eval_lut(&gin280, nand2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout91 + gout186;
    Ciphertext gout281 = ctx.eval_lut(&gin281, nand2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout92 + gout189;
    Ciphertext gout282 = ctx.eval_lut(&gin282, xor2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout73 + gout187;
    Ciphertext gout283 = ctx.eval_lut(&gin283, nand2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout41 + gout207;
    Ciphertext gout284 = ctx.eval_lut(&gin284, nand2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout61 + gout211;
    Ciphertext gout285 = ctx.eval_lut(&gin285, nand2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout62 + gout213;
    Ciphertext gout286 = ctx.eval_lut(&gin286, xor2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout61 + gout211;
    Ciphertext gout287 = ctx.eval_lut(&gin287, xor2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout220 + gout277;
    Ciphertext gout288 = ctx.eval_lut(&gin288, nand2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout85 + gout215;
    Ciphertext gout289 = ctx.eval_lut(&gin289, xor2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout210 + gout214;
    Ciphertext gout290 = ctx.eval_lut(&gin290, xor2);
    std::cerr << "\rINV291   ";
    Ciphertext gout291 = 1 - gout199;

    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout210 + gout214;
    Ciphertext gout292 = ctx.eval_lut(&gin292, nand2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout106 + gout217;
    Ciphertext gout293 = ctx.eval_lut(&gin293, xor2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout67 + gout218;
    Ciphertext gout294 = ctx.eval_lut(&gin294, xor2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout135 + gout223;
    Ciphertext gout295 = ctx.eval_lut(&gin295, xor2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout9 + gout224;
    Ciphertext gout296 = ctx.eval_lut(&gin296, xor2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout10 + gout227;
    Ciphertext gout297 = ctx.eval_lut(&gin297, xor2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout129 + gout208;
    Ciphertext gout298 = ctx.eval_lut(&gin298, and2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout222 + gout280;
    Ciphertext gout299 = ctx.eval_lut(&gin299, nand2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout62 + gout213;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout86 + gout178;
    Ciphertext gout301 = ctx.eval_lut(&gin301, nand2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout64 + gout239;
    Ciphertext gout302 = ctx.eval_lut(&gin302, xor2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout17 + gout232;
    Ciphertext gout303 = ctx.eval_lut(&gin303, xor2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout24 + gout241;
    Ciphertext gout304 = ctx.eval_lut(&gin304, xor2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout119 + gout225;
    Ciphertext gout305 = ctx.eval_lut(&gin305, nand2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout104 + gout234;
    Ciphertext gout306 = ctx.eval_lut(&gin306, xor2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout74 + gout253;
    Ciphertext gout307 = ctx.eval_lut(&gin307, xor2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout104 + gout234;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout85 + gout215;
    Ciphertext gout309 = ctx.eval_lut(&gin309, nand2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout243 + gout276;
    Ciphertext gout310 = ctx.eval_lut(&gin310, xnor2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout176 + gout195;
    Ciphertext gout311 = ctx.eval_lut(&gin311, nand2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout135 + gout223;
    Ciphertext gout312 = ctx.eval_lut(&gin312, nand2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout7 + gout212;
    Ciphertext gout313 = ctx.eval_lut(&gin313, nand2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout73 + gout187;
    Ciphertext gout314 = ctx.eval_lut(&gin314, xor2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout134 + gout230;
    Ciphertext gout315 = ctx.eval_lut(&gin315, nand2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout106 + gout217;
    Ciphertext gout316 = ctx.eval_lut(&gin316, nand2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout9 + gout224;
    Ciphertext gout317 = ctx.eval_lut(&gin317, nand2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout221 + gout283;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout114 + gout250;
    Ciphertext gout319 = ctx.eval_lut(&gin319, xor2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout79 + gout251;
    Ciphertext gout320 = ctx.eval_lut(&gin320, xor2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout134 + gout230;
    Ciphertext gout321 = ctx.eval_lut(&gin321, xor2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout79 + gout251;
    Ciphertext gout322 = ctx.eval_lut(&gin322, nand2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout10 + gout227;
    Ciphertext gout323 = ctx.eval_lut(&gin323, nand2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout125 + gout248;
    Ciphertext gout324 = ctx.eval_lut(&gin324, nand2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout155 + gout261;
    Ciphertext gout325 = ctx.eval_lut(&gin325, xor2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout64 + gout239;
    Ciphertext gout326 = ctx.eval_lut(&gin326, nand2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout17 + gout232;
    Ciphertext gout327 = ctx.eval_lut(&gin327, nand2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout125 + gout248;
    Ciphertext gout328 = ctx.eval_lut(&gin328, xor2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout260 + gout285;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout119 + gout225;
    Ciphertext gout330 = ctx.eval_lut(&gin330, xor2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout243 + gout276;
    Ciphertext gout331 = ctx.eval_lut(&gin331, and2);
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout206 + gout266;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout263 + gout216;
    Ciphertext gout333 = ctx.eval_lut(&gin333, nand2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout74 + gout253;
    Ciphertext gout334 = ctx.eval_lut(&gin334, nand2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout38 + gout242;
    Ciphertext gout335 = ctx.eval_lut(&gin335, xor2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout42 + gout264;
    Ciphertext gout336 = ctx.eval_lut(&gin336, nand2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout38 + gout242;
    Ciphertext gout337 = ctx.eval_lut(&gin337, nand2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout265 + gout270;
    Ciphertext gout338 = ctx.eval_lut(&gin338, xnor2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout50 + gout271;
    Ciphertext gout339 = ctx.eval_lut(&gin339, xor2);
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout156 + gout267;
    Ciphertext gout340 = ctx.eval_lut(&gin340, xor2);
    std::cerr << "\rLUT341   ";
    Ciphertext gin341 = 2 * gout42 + gout264;
    Ciphertext gout341 = ctx.eval_lut(&gin341, xor2);
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout156 + gout267;
    Ciphertext gout342 = ctx.eval_lut(&gin342, nand2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout69 + gout272;
    Ciphertext gout343 = ctx.eval_lut(&gin343, xor2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout114 + gout250;
    Ciphertext gout344 = ctx.eval_lut(&gin344, nand2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout69 + gout272;
    Ciphertext gout345 = ctx.eval_lut(&gin345, nand2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout112 + gout258;
    Ciphertext gout346 = ctx.eval_lut(&gin346, xnor2);
    std::cerr << "\rLUT347   ";
    Ciphertext gin347 = 2 * gout206 + gout177;
    Ciphertext gout347 = ctx.eval_lut(&gin347, and2);
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout41 + gout207;
    Ciphertext gout348 = ctx.eval_lut(&gin348, xor2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout24 + gout241;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * gout155 + gout261;
    Ciphertext gout350 = ctx.eval_lut(&gin350, nand2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout262 + gout309;
    Ciphertext gout351 = ctx.eval_lut(&gin351, nand2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout7 + gout212;
    Ciphertext gout352 = ctx.eval_lut(&gin352, xor2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout67 + gout218;
    Ciphertext gout353 = ctx.eval_lut(&gin353, nand2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout190 + gout255;
    Ciphertext gout354 = ctx.eval_lut(&gin354, nand2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout50 + gout271;
    Ciphertext gout355 = ctx.eval_lut(&gin355, nand2);
    std::cerr << "\rLUT356   ";
    Ciphertext gin356 = 2 * gout196 + gout281;
    Ciphertext gout356 = ctx.eval_lut(&gin356, nand2);
    std::cerr << "\rINV357   ";
    Ciphertext gout357 = 1 - gout278;

    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout204 + gout284;
    Ciphertext gout358 = ctx.eval_lut(&gin358, nand2);
    std::cerr << "\rLUT359   ";
    Ciphertext gin359 = 2 * gout111 + gout298;
    Ciphertext gout359 = ctx.eval_lut(&gin359, and2);
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout299 + gout109;
    Ciphertext gout360 = ctx.eval_lut(&gin360, xor2);
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout166 + gout290;
    Ciphertext gout361 = ctx.eval_lut(&gin361, nand2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout219 + gout290;
    Ciphertext gout362 = ctx.eval_lut(&gin362, xnor2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout274 + gout304;
    Ciphertext gout363 = ctx.eval_lut(&gin363, xnor2);
    std::cerr << "\rLUT364   ";
    Ciphertext gin364 = 2 * gout173 + gout312;
    Ciphertext gout364 = ctx.eval_lut(&gin364, nand2);
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout244 + gout322;
    Ciphertext gout365 = ctx.eval_lut(&gin365, nand2);
    std::cerr << "\rLUT366   ";
    Ciphertext gin366 = 2 * gout245 + gout317;
    Ciphertext gout366 = ctx.eval_lut(&gin366, nand2);
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout329 + gout314;
    Ciphertext gout367 = ctx.eval_lut(&gin367, xor2);
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout172 + gout301;
    Ciphertext gout368 = ctx.eval_lut(&gin368, nand2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout228 + gout300;
    Ciphertext gout369 = ctx.eval_lut(&gin369, and2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * ct_y14 + gout332;
    Ciphertext gout370 = ctx.eval_lut(&gin370, and2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout174 + gout305;
    Ciphertext gout371 = ctx.eval_lut(&gin371, nand2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout192 + gout337;
    Ciphertext gout372 = ctx.eval_lut(&gin372, nand2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout358 + gout320;
    Ciphertext gout373 = ctx.eval_lut(&gin373, xnor2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout358 + gout320;
    Ciphertext gout374 = ctx.eval_lut(&gin374, and2);
    std::cerr << "\rLUT375   ";
    Ciphertext gin375 = 2 * ct_y13 + gout332;
    Ciphertext gout375 = ctx.eval_lut(&gin375, and2);
    std::cerr << "\rLUT376   ";
    Ciphertext gin376 = 2 * ct_y0 + gout332;
    Ciphertext gout376 = ctx.eval_lut(&gin376, and2);
    std::cerr << "\rLUT377   ";
    Ciphertext gin377 = 2 * (1 - gout359) + gout332;
    Ciphertext gout377 = ctx.eval_lut(&gin377, and2);
    std::cerr << "\rLUT378   ";
    Ciphertext gin378 = 2 * (1 - gout298) + gout332;
    Ciphertext gout378 = ctx.eval_lut(&gin378, and2);
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout235 + gout308;
    Ciphertext gout379 = ctx.eval_lut(&gin379, nand2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout246 + gout315;
    Ciphertext gout380 = ctx.eval_lut(&gin380, nand2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout333 + gout341;
    Ciphertext gout381 = ctx.eval_lut(&gin381, and2);
    std::cerr << "\rLUT382   ";
    Ciphertext gin382 = 2 * gout181 + gout304;
    Ciphertext gout382 = ctx.eval_lut(&gin382, and2);
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * gout179 + gout327;
    Ciphertext gout383 = ctx.eval_lut(&gin383, nand2);
    std::cerr << "\rLUT384   ";
    Ciphertext gin384 = 2 * gout268 + gout342;
    Ciphertext gout384 = ctx.eval_lut(&gin384, nand2);
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout238 + gout326;
    Ciphertext gout385 = ctx.eval_lut(&gin385, nand2);
    std::cerr << "\rLUT386   ";
    Ciphertext gin386 = 2 * gout237 + gout313;
    Ciphertext gout386 = ctx.eval_lut(&gin386, nand2);
    std::cerr << "\rLUT387   ";
    Ciphertext gin387 = 2 * gout231 + gout324;
    Ciphertext gout387 = ctx.eval_lut(&gin387, nand2);
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * gout351 + gout340;
    Ciphertext gout388 = ctx.eval_lut(&gin388, xor2);
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout259 + gout346;
    Ciphertext gout389 = ctx.eval_lut(&gin389, xnor2);
    std::cerr << "\rLUT390   ";
    Ciphertext gin390 = 2 * gout247 + gout316;
    Ciphertext gout390 = ctx.eval_lut(&gin390, nand2);
    std::cerr << "\rLUT391   ";
    Ciphertext gin391 = 2 * gout59 + gout347;
    Ciphertext gout391 = ctx.eval_lut(&gin391, and2);
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * gout188 + gout350;
    Ciphertext gout392 = ctx.eval_lut(&gin392, and2);
    std::cerr << "\rLUT393   ";
    Ciphertext gin393 = 2 * gout351 + gout340;
    Ciphertext gout393 = ctx.eval_lut(&gin393, nand2);
    std::cerr << "\rLUT394   ";
    Ciphertext gin394 = 2 * gout329 + gout314;
    Ciphertext gout394 = ctx.eval_lut(&gin394, nand2);
    std::cerr << "\rLUT395   ";
    Ciphertext gin395 = 2 * gout254 + gout323;
    Ciphertext gout395 = ctx.eval_lut(&gin395, nand2);
    std::cerr << "\rLUT396   ";
    Ciphertext gin396 = 2 * gout354 + gout297;
    Ciphertext gout396 = ctx.eval_lut(&gin396, xnor2);
    std::cerr << "\rLUT397   ";
    Ciphertext gin397 = 2 * gout269 + gout334;
    Ciphertext gout397 = ctx.eval_lut(&gin397, and2);
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout226 + gout344;
    Ciphertext gout398 = ctx.eval_lut(&gin398, nand2);
    std::cerr << "\rLUT399   ";
    Ciphertext gin399 = 2 * ct_y11 + gout332;
    Ciphertext gout399 = ctx.eval_lut(&gin399, nand2);
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout299 + gout109;
    Ciphertext gout400 = ctx.eval_lut(&gin400, and2);
    std::cerr << "\rLUT401   ";
    Ciphertext gin401 = 2 * gout311 + gout348;
    Ciphertext gout401 = ctx.eval_lut(&gin401, xnor2);
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * gout311 + gout348;
    Ciphertext gout402 = ctx.eval_lut(&gin402, and2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout184 + gout349;
    Ciphertext gout403 = ctx.eval_lut(&gin403, nand2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * gout332 + gout249;
    Ciphertext gout404 = ctx.eval_lut(&gin404, and2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * (1 - gout208) + gout332;
    Ciphertext gout405 = ctx.eval_lut(&gin405, and2);
    std::cerr << "\rLUT406   ";
    Ciphertext gin406 = 2 * gout205 + gout336;
    Ciphertext gout406 = ctx.eval_lut(&gin406, nand2);
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout347 + gout332;
    Ciphertext gout407 = ctx.eval_lut(&gin407, or2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout201 + gout355;
    Ciphertext gout408 = ctx.eval_lut(&gin408, nand2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * ct_y12 + gout332;
    Ciphertext gout409 = ctx.eval_lut(&gin409, nand2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout36 + gout338;
    Ciphertext gout410 = ctx.eval_lut(&gin410, xnor2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout333 + gout341;
    Ciphertext gout411 = ctx.eval_lut(&gin411, xnor2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout194 + gout345;
    Ciphertext gout412 = ctx.eval_lut(&gin412, and2);
    std::cerr << "\rLUT413   ";
    Ciphertext gin413 = 2 * gout198 + gout353;
    Ciphertext gout413 = ctx.eval_lut(&gin413, nand2);
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout354 + gout297;
    Ciphertext gout414 = ctx.eval_lut(&gin414, and2);
    std::cerr << "\rLUT415   ";
    Ciphertext gin415 = 2 * gout8 + gout359;
    Ciphertext gout415 = ctx.eval_lut(&gin415, and2);
    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout356 + gout367;
    Ciphertext gout416 = ctx.eval_lut(&gin416, nand2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout368 + gout293;
    Ciphertext gout417 = ctx.eval_lut(&gin417, xnor2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout278 + gout362;
    Ciphertext gout418 = ctx.eval_lut(&gin418, and2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout5 + gout376;
    Ciphertext gout419 = ctx.eval_lut(&gin419, xnor2);
    std::cerr << "\rLUT420   ";
    Ciphertext gin420 = 2 * gout371 + gout286;
    Ciphertext gout420 = ctx.eval_lut(&gin420, nand2);
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * (1 - gout415) + gout332;
    Ciphertext gout421 = ctx.eval_lut(&gin421, and2);
    std::cerr << "\rLUT422   ";
    Ciphertext gin422 = 2 * gout292 + gout361;
    Ciphertext gout422 = ctx.eval_lut(&gin422, nand2);
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * (1 - gout373) + gout328;
    Ciphertext gout423 = ctx.eval_lut(&gin423, and2);
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * gout8 + gout377;
    Ciphertext gout424 = ctx.eval_lut(&gin424, xnor2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * gout365 + gout303;
    Ciphertext gout425 = ctx.eval_lut(&gin425, xnor2);
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout371 + gout286;
    Ciphertext gout426 = ctx.eval_lut(&gin426, xor2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout365 + gout303;
    Ciphertext gout427 = ctx.eval_lut(&gin427, and2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout383 + gout273;
    Ciphertext gout428 = ctx.eval_lut(&gin428, xnor2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout373 + gout328;
    Ciphertext gout429 = ctx.eval_lut(&gin429, xnor2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout390 + gout306;
    Ciphertext gout430 = ctx.eval_lut(&gin430, xnor2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout356 + gout367;
    Ciphertext gout431 = ctx.eval_lut(&gin431, xor2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout387 + gout279;
    Ciphertext gout432 = ctx.eval_lut(&gin432, xor2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout379 + gout294;
    Ciphertext gout433 = ctx.eval_lut(&gin433, and2);
    std::cerr << "\rLUT434   ";
    Ciphertext gin434 = 2 * gout379 + gout294;
    Ciphertext gout434 = ctx.eval_lut(&gin434, xnor2);
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout364 + gout275;
    Ciphertext gout435 = ctx.eval_lut(&gin435, nand2);
    std::cerr << "\rLUT436   ";
    Ciphertext gin436 = 2 * gout391 + gout78;
    Ciphertext gout436 = ctx.eval_lut(&gin436, or2);
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout391 + gout146;
    Ciphertext gout437 = ctx.eval_lut(&gin437, nand2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout364 + gout275;
    Ciphertext gout438 = ctx.eval_lut(&gin438, xor2);
    std::cerr << "\rLUT439   ";
    Ciphertext gin439 = 2 * gout368 + gout293;
    Ciphertext gout439 = ctx.eval_lut(&gin439, and2);
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout357 + gout362;
    Ciphertext gout440 = ctx.eval_lut(&gin440, xnor2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout380 + gout256;
    Ciphertext gout441 = ctx.eval_lut(&gin441, xnor2);
    std::cerr << "\rLUT442   ";
    Ciphertext gin442 = 2 * gout398 + gout343;
    Ciphertext gout442 = ctx.eval_lut(&gin442, xnor2);
    std::cerr << "\rLUT443   ";
    Ciphertext gin443 = 2 * gout288 + gout388;
    Ciphertext gout443 = ctx.eval_lut(&gin443, xor2);
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout372 + gout209;
    Ciphertext gout444 = ctx.eval_lut(&gin444, and2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout403 + gout233;
    Ciphertext gout445 = ctx.eval_lut(&gin445, nand2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout288 + gout388;
    Ciphertext gout446 = ctx.eval_lut(&gin446, nand2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout407 + gout436;
    Ciphertext gout447 = ctx.eval_lut(&gin447, and2);
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * gout372 + gout209;
    Ciphertext gout448 = ctx.eval_lut(&gin448, xor2);
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout395 + gout325;
    Ciphertext gout449 = ctx.eval_lut(&gin449, nand2);
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout383 + gout273;
    Ciphertext gout450 = ctx.eval_lut(&gin450, and2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout111 + gout378;
    Ciphertext gout451 = ctx.eval_lut(&gin451, xnor2);
    std::cerr << "\rLUT452   ";
    Ciphertext gin452 = 2 * gout129 + gout405;
    Ciphertext gout452 = ctx.eval_lut(&gin452, xnor2);
    std::cerr << "\rLUT453   ";
    Ciphertext gin453 = 2 * gout411 + gout197;
    Ciphertext gout453 = ctx.eval_lut(&gin453, xnor2);
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout387 + gout279;
    Ciphertext gout454 = ctx.eval_lut(&gin454, nand2);
    std::cerr << "\rLUT455   ";
    Ciphertext gin455 = 2 * gout395 + gout325;
    Ciphertext gout455 = ctx.eval_lut(&gin455, xnor2);
    std::cerr << "\rLUT456   ";
    Ciphertext gin456 = 2 * gout397 + gout240;
    Ciphertext gout456 = ctx.eval_lut(&gin456, xnor2);
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout394 + gout416;
    Ciphertext gout457 = ctx.eval_lut(&gin457, nand2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * (1 - gout411) + gout197;
    Ciphertext gout458 = ctx.eval_lut(&gin458, and2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout408 + gout352;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout401 + gout335;
    Ciphertext gout460 = ctx.eval_lut(&gin460, xnor2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout391 + gout229;
    Ciphertext gout461 = ctx.eval_lut(&gin461, nand2);
    std::cerr << "\rLUT462   ";
    Ciphertext gin462 = 2 * (1 - gout401) + gout335;
    Ciphertext gout462 = ctx.eval_lut(&gin462, and2);
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout408 + gout352;
    Ciphertext gout463 = ctx.eval_lut(&gin463, xor2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout396 + gout252;
    Ciphertext gout464 = ctx.eval_lut(&gin464, xnor2);
    std::cerr << "\rLUT465   ";
    Ciphertext gin465 = 2 * gout48 + gout392;
    Ciphertext gout465 = ctx.eval_lut(&gin465, xnor2);
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * (1 - gout396) + gout252;
    Ciphertext gout466 = ctx.eval_lut(&gin466, and2);
    std::cerr << "\rLUT467   ";
    Ciphertext gin467 = 2 * gout385 + gout296;
    Ciphertext gout467 = ctx.eval_lut(&gin467, xor2);
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout385 + gout296;
    Ciphertext gout468 = ctx.eval_lut(&gin468, nand2);
    std::cerr << "\rLUT469   ";
    Ciphertext gin469 = 2 * gout398 + gout343;
    Ciphertext gout469 = ctx.eval_lut(&gin469, nand2);
    std::cerr << "\rLUT470   ";
    Ciphertext gin470 = 2 * gout390 + gout306;
    Ciphertext gout470 = ctx.eval_lut(&gin470, and2);
    std::cerr << "\rINV471   ";
    Ciphertext gout471 = 1 - gout391;

    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout413 + gout319;
    Ciphertext gout472 = ctx.eval_lut(&gin472, xnor2);
    std::cerr << "\rLUT473   ";
    Ciphertext gin473 = 2 * gout52 + gout404;
    Ciphertext gout473 = ctx.eval_lut(&gin473, xnor2);
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout403 + gout233;
    Ciphertext gout474 = ctx.eval_lut(&gin474, xor2);
    std::cerr << "\rLUT475   ";
    Ciphertext gin475 = 2 * gout406 + gout193;
    Ciphertext gout475 = ctx.eval_lut(&gin475, xnor2);
    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout406 + gout193;
    Ciphertext gout476 = ctx.eval_lut(&gin476, and2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout413 + gout319;
    Ciphertext gout477 = ctx.eval_lut(&gin477, and2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout98 + gout412;
    Ciphertext gout478 = ctx.eval_lut(&gin478, xnor2);
    std::cerr << "\rLUT479   ";
    Ciphertext gin479 = 2 * gout45 + gout415;
    Ciphertext gout479 = ctx.eval_lut(&gin479, and2);
    std::cerr << "\rLUT480   ";
    Ciphertext gin480 = 2 * (1 - gout417) + gout287;
    Ciphertext gout480 = ctx.eval_lut(&gin480, and2);
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout417 + gout287;
    Ciphertext gout481 = ctx.eval_lut(&gin481, xnor2);
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout430 + gout339;
    Ciphertext gout482 = ctx.eval_lut(&gin482, xnor2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout83 + gout419;
    Ciphertext gout483 = ctx.eval_lut(&gin483, xnor2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout374 + gout423;
    Ciphertext gout484 = ctx.eval_lut(&gin484, or2);
    std::cerr << "\rINV485   ";
    Ciphertext gout485 = 1 - gout418;

    std::cerr << "\rLUT486   ";
    Ciphertext gin486 = 2 * gout434 + gout302;
    Ciphertext gout486 = ctx.eval_lut(&gin486, xnor2);
    std::cerr << "\rLUT487   ";
    Ciphertext gin487 = 2 * gout113 + gout424;
    Ciphertext gout487 = ctx.eval_lut(&gin487, xnor2);
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * gout366 + gout426;
    Ciphertext gout488 = ctx.eval_lut(&gin488, nand2);
    std::cerr << "\rLUT489   ";
    Ciphertext gin489 = 2 * gout366 + gout426;
    Ciphertext gout489 = ctx.eval_lut(&gin489, xor2);
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout442 + gout307;
    Ciphertext gout490 = ctx.eval_lut(&gin490, xnor2);
    std::cerr << "\rLUT491   ";
    Ciphertext gin491 = 2 * (1 - gout428) + gout295;
    Ciphertext gout491 = ctx.eval_lut(&gin491, and2);
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout384 + gout438;
    Ciphertext gout492 = ctx.eval_lut(&gin492, nand2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout428 + gout295;
    Ciphertext gout493 = ctx.eval_lut(&gin493, xnor2);
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * gout161 + gout452;
    Ciphertext gout494 = ctx.eval_lut(&gin494, xnor2);
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * (1 - gout455) + gout321;
    Ciphertext gout495 = ctx.eval_lut(&gin495, and2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout200 + gout432;
    Ciphertext gout496 = ctx.eval_lut(&gin496, xor2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * ct_x1 + gout419;
    Ciphertext gout497 = ctx.eval_lut(&gin497, nand2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout384 + gout438;
    Ciphertext gout498 = ctx.eval_lut(&gin498, xor2);
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * (1 - gout430) + gout339;
    Ciphertext gout499 = ctx.eval_lut(&gin499, and2);
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * gout455 + gout321;
    Ciphertext gout500 = ctx.eval_lut(&gin500, xnor2);
    std::cerr << "\rLUT501   ";
    Ciphertext gin501 = 2 * gout439 + gout480;
    Ciphertext gout501 = ctx.eval_lut(&gin501, or2);
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout437 + gout447;
    Ciphertext gout502 = ctx.eval_lut(&gin502, nand2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout23 + gout451;
    Ciphertext gout503 = ctx.eval_lut(&gin503, xnor2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * gout45 + gout421;
    Ciphertext gout504 = ctx.eval_lut(&gin504, xnor2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * (1 - gout434) + gout302;
    Ciphertext gout505 = ctx.eval_lut(&gin505, and2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * (1 - gout425) + gout289;
    Ciphertext gout506 = ctx.eval_lut(&gin506, and2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * ct_x5 + gout424;
    Ciphertext gout507 = ctx.eval_lut(&gin507, nand2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout381 + gout458;
    Ciphertext gout508 = ctx.eval_lut(&gin508, or2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout402 + gout462;
    Ciphertext gout509 = ctx.eval_lut(&gin509, or2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout425 + gout289;
    Ciphertext gout510 = ctx.eval_lut(&gin510, xnor2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * gout393 + gout446;
    Ciphertext gout511 = ctx.eval_lut(&gin511, nand2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout200 + gout432;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * (1 - gout442) + gout307;
    Ciphertext gout513 = ctx.eval_lut(&gin513, and2);
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * gout422 + gout453;
    Ciphertext gout514 = ctx.eval_lut(&gin514, and2);
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout318 + gout463;
    Ciphertext gout515 = ctx.eval_lut(&gin515, nand2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * gout331 + gout440;
    Ciphertext gout516 = ctx.eval_lut(&gin516, xnor2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout410 + gout465;
    Ciphertext gout517 = ctx.eval_lut(&gin517, xnor2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout331 + gout440;
    Ciphertext gout518 = ctx.eval_lut(&gin518, nand2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout386 + gout467;
    Ciphertext gout519 = ctx.eval_lut(&gin519, xor2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * ct_x4 + gout451;
    Ciphertext gout520 = ctx.eval_lut(&gin520, nand2);
    std::cerr << "\rLUT521   ";
    Ciphertext gin521 = 2 * gout472 + gout330;
    Ciphertext gout521 = ctx.eval_lut(&gin521, xnor2);
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * (1 - gout472) + gout330;
    Ciphertext gout522 = ctx.eval_lut(&gin522, and2);
    std::cerr << "\rLUT523   ";
    Ciphertext gin523 = 2 * ct_x3 + gout452;
    Ciphertext gout523 = ctx.eval_lut(&gin523, nand2);
    std::cerr << "\rLUT524   ";
    Ciphertext gin524 = 2 * gout175 + gout474;
    Ciphertext gout524 = ctx.eval_lut(&gin524, xnor2);
    std::cerr << "\rLUT525   ";
    Ciphertext gin525 = 2 * gout478 + gout456;
    Ciphertext gout525 = ctx.eval_lut(&gin525, xnor2);
    std::cerr << "\rLUT526   ";
    Ciphertext gin526 = 2 * gout422 + gout453;
    Ciphertext gout526 = ctx.eval_lut(&gin526, xor2);
    std::cerr << "\rLUT527   ";
    Ciphertext gin527 = 2 * (1 - gout475) + gout282;
    Ciphertext gout527 = ctx.eval_lut(&gin527, and2);
    std::cerr << "\rLUT528   ";
    Ciphertext gin528 = 2 * gout475 + gout282;
    Ciphertext gout528 = ctx.eval_lut(&gin528, xnor2);
    std::cerr << "\rLUT529   ";
    Ciphertext gin529 = 2 * gout318 + gout463;
    Ciphertext gout529 = ctx.eval_lut(&gin529, xor2);
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * gout407 + gout461;
    Ciphertext gout530 = ctx.eval_lut(&gin530, and2);
    std::cerr << "\rLUT531   ";
    Ciphertext gin531 = 2 * gout386 + gout467;
    Ciphertext gout531 = ctx.eval_lut(&gin531, nand2);
    std::cerr << "\rLUT532   ";
    Ciphertext gin532 = 2 * ct_x2 + gout473;
    Ciphertext gout532 = ctx.eval_lut(&gin532, nand2);
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout414 + gout466;
    Ciphertext gout533 = ctx.eval_lut(&gin533, or2);
    std::cerr << "\rLUT534   ";
    Ciphertext gin534 = 2 * gout470 + gout499;
    Ciphertext gout534 = ctx.eval_lut(&gin534, or2);
    std::cerr << "\rLUT535   ";
    Ciphertext gin535 = 2 * gout37 + gout474;
    Ciphertext gout535 = ctx.eval_lut(&gin535, nand2);
    std::cerr << "\rLUT536   ";
    Ciphertext gin536 = 2 * gout123 + gout473;
    Ciphertext gout536 = ctx.eval_lut(&gin536, xnor2);
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * (1 - gout479) + gout332;
    Ciphertext gout537 = ctx.eval_lut(&gin537, and2);
    std::cerr << "\rLUT538   ";
    Ciphertext gin538 = 2 * gout82 + gout479;
    Ciphertext gout538 = ctx.eval_lut(&gin538, and2);
    std::cerr << "\rLUT539   ";
    Ciphertext gin539 = 2 * gout146 + gout483;
    Ciphertext gout539 = ctx.eval_lut(&gin539, nand2);
    std::cerr << "\rLUT540   ";
    Ciphertext gin540 = 2 * gout497 + gout539;
    Ciphertext gout540 = ctx.eval_lut(&gin540, nand2);
    std::cerr << "\rLUT541   ";
    Ciphertext gin541 = 2 * gout257 + gout483;
    Ciphertext gout541 = ctx.eval_lut(&gin541, xnor2);
    std::cerr << "\rLUT542   ";
    Ciphertext gin542 = 2 * gout449 + (1 - gout495);
    Ciphertext gout542 = ctx.eval_lut(&gin542, and2);
    std::cerr << "\rLUT543   ";
    Ciphertext gin543 = 2 * gout501 + gout482;
    Ciphertext gout543 = ctx.eval_lut(&gin543, and2);
    std::cerr << "\rLUT544   ";
    Ciphertext gin544 = 2 * gout433 + gout505;
    Ciphertext gout544 = ctx.eval_lut(&gin544, or2);
    std::cerr << "\rLUT545   ";
    Ciphertext gin545 = 2 * gout420 + gout488;
    Ciphertext gout545 = ctx.eval_lut(&gin545, and2);
    std::cerr << "\rLUT546   ";
    Ciphertext gin546 = 2 * gout509 + gout429;
    Ciphertext gout546 = ctx.eval_lut(&gin546, xnor2);
    std::cerr << "\rLUT547   ";
    Ciphertext gin547 = 2 * ct_x6 + gout504;
    Ciphertext gout547 = ctx.eval_lut(&gin547, nand2);
    std::cerr << "\rLUT548   ";
    Ciphertext gin548 = 2 * gout97 + gout504;
    Ciphertext gout548 = ctx.eval_lut(&gin548, xnor2);
    std::cerr << "\rLUT549   ";
    Ciphertext gin549 = 2 * gout427 + gout506;
    Ciphertext gout549 = ctx.eval_lut(&gin549, or2);
    std::cerr << "\rLUT550   ";
    Ciphertext gin550 = 2 * gout511 + gout236;
    Ciphertext gout550 = ctx.eval_lut(&gin550, xor2);
    std::cerr << "\rLUT551   ";
    Ciphertext gin551 = 2 * gout484 + gout510;
    Ciphertext gout551 = ctx.eval_lut(&gin551, and2);
    std::cerr << "\rLUT552   ";
    Ciphertext gin552 = 2 * gout511 + gout236;
    Ciphertext gout552 = ctx.eval_lut(&gin552, and2);
    std::cerr << "\rLUT553   ";
    Ciphertext gin553 = 2 * gout454 + gout512;
    Ciphertext gout553 = ctx.eval_lut(&gin553, nand2);
    std::cerr << "\rLUT554   ";
    Ciphertext gin554 = 2 * gout459 + gout515;
    Ciphertext gout554 = ctx.eval_lut(&gin554, nand2);
    std::cerr << "\rLUT555   ";
    Ciphertext gin555 = 2 * gout471 + gout541;
    Ciphertext gout555 = ctx.eval_lut(&gin555, nand2);
    std::cerr << "\rLUT556   ";
    Ciphertext gin556 = 2 * gout484 + gout510;
    Ciphertext gout556 = ctx.eval_lut(&gin556, xnor2);
    std::cerr << "\rLUT557   ";
    Ciphertext gin557 = 2 * gout485 + gout518;
    Ciphertext gout557 = ctx.eval_lut(&gin557, nand2);
    std::cerr << "\rLUT558   ";
    Ciphertext gin558 = 2 * gout545 + gout517;
    Ciphertext gout558 = ctx.eval_lut(&gin558, xnor2);
    std::cerr << "\rLUT559   ";
    Ciphertext gin559 = 2 * gout435 + gout492;
    Ciphertext gout559 = ctx.eval_lut(&gin559, nand2);
    std::cerr << "\rLUT560   ";
    Ciphertext gin560 = 2 * gout450 + gout491;
    Ciphertext gout560 = ctx.eval_lut(&gin560, or2);
    std::cerr << "\rLUT561   ";
    Ciphertext gin561 = 2 * gout501 + gout482;
    Ciphertext gout561 = ctx.eval_lut(&gin561, xnor2);
    std::cerr << "\rLUT562   ";
    Ciphertext gin562 = 2 * gout509 + gout429;
    Ciphertext gout562 = ctx.eval_lut(&gin562, and2);
    std::cerr << "\rLUT563   ";
    Ciphertext gin563 = 2 * gout526 + gout557;
    Ciphertext gout563 = ctx.eval_lut(&gin563, xnor2);
    std::cerr << "\rLUT564   ";
    Ciphertext gin564 = 2 * gout476 + gout527;
    Ciphertext gout564 = ctx.eval_lut(&gin564, or2);
    std::cerr << "\rLUT565   ";
    Ciphertext gin565 = 2 * gout389 + gout525;
    Ciphertext gout565 = ctx.eval_lut(&gin565, xnor2);
    std::cerr << "\rLUT566   ";
    Ciphertext gin566 = 2 * gout457 + gout524;
    Ciphertext gout566 = ctx.eval_lut(&gin566, xor2);
    std::cerr << "\rLUT567   ";
    Ciphertext gin567 = 2 * gout544 + gout521;
    Ciphertext gout567 = ctx.eval_lut(&gin567, and2);
    std::cerr << "\rLUT568   ";
    Ciphertext gin568 = 2 * (1 - gout518) + gout526;
    Ciphertext gout568 = ctx.eval_lut(&gin568, and2);
    std::cerr << "\rLUT569   ";
    Ciphertext gin569 = 2 * gout469 + (1 - gout513);
    Ciphertext gout569 = ctx.eval_lut(&gin569, and2);
    std::cerr << "\rLUT570   ";
    Ciphertext gin570 = 2 * gout508 + gout528;
    Ciphertext gout570 = ctx.eval_lut(&gin570, xor2);
    std::cerr << "\rLUT571   ";
    Ciphertext gin571 = 2 * gout468 + gout531;
    Ciphertext gout571 = ctx.eval_lut(&gin571, nand2);
    std::cerr << "\rLUT572   ";
    Ciphertext gin572 = 2 * gout477 + gout522;
    Ciphertext gout572 = ctx.eval_lut(&gin572, or2);
    std::cerr << "\rLUT573   ";
    Ciphertext gin573 = 2 * gout534 + gout486;
    Ciphertext gout573 = ctx.eval_lut(&gin573, and2);
    std::cerr << "\rLUT574   ";
    Ciphertext gin574 = 2 * gout457 + gout524;
    Ciphertext gout574 = ctx.eval_lut(&gin574, nand2);
    std::cerr << "\rLUT575   ";
    Ciphertext gin575 = 2 * gout544 + gout521;
    Ciphertext gout575 = ctx.eval_lut(&gin575, xnor2);
    std::cerr << "\rLUT576   ";
    Ciphertext gin576 = 2 * gout445 + gout535;
    Ciphertext gout576 = ctx.eval_lut(&gin576, nand2);
    std::cerr << "\rLUT577   ";
    Ciphertext gin577 = 2 * gout418 + gout526;
    Ciphertext gout577 = ctx.eval_lut(&gin577, and2);
    std::cerr << "\rLUT578   ";
    Ciphertext gin578 = 2 * gout555 + gout530;
    Ciphertext gout578 = ctx.eval_lut(&gin578, nand2);
    std::cerr << "\rLUT579   ";
    Ciphertext gin579 = 2 * gout508 + gout528;
    Ciphertext gout579 = ctx.eval_lut(&gin579, nand2);
    std::cerr << "\rLUT580   ";
    Ciphertext gin580 = 2 * gout540 + gout536;
    Ciphertext gout580 = ctx.eval_lut(&gin580, xnor2);
    std::cerr << "\rLUT581   ";
    Ciphertext gin581 = 2 * gout540 + gout536;
    Ciphertext gout581 = ctx.eval_lut(&gin581, nand2);
    std::cerr << "\rLUT582   ";
    Ciphertext gin582 = 2 * gout534 + gout486;
    Ciphertext gout582 = ctx.eval_lut(&gin582, xnor2);
    std::cerr << "\rLUT583   ";
    Ciphertext gin583 = 2 * gout82 + gout537;
    Ciphertext gout583 = ctx.eval_lut(&gin583, xnor2);
    std::cerr << "\rLUT584   ";
    Ciphertext gin584 = 2 * (1 - gout538) + gout332;
    Ciphertext gout584 = ctx.eval_lut(&gin584, and2);
    std::cerr << "\rLUT585   ";
    Ciphertext gin585 = 2 * gout77 + gout538;
    Ciphertext gout585 = ctx.eval_lut(&gin585, and2);
    std::cerr << "\rLUT586   ";
    Ciphertext gin586 = 2 * gout560 + gout481;
    Ciphertext gout586 = ctx.eval_lut(&gin586, and2);
    std::cerr << "\rLUT587   ";
    Ciphertext gin587 = 2 * gout560 + gout481;
    Ciphertext gout587 = ctx.eval_lut(&gin587, xnor2);
    std::cerr << "\rLUT588   ";
    Ciphertext gin588 = 2 * gout559 + gout363;
    Ciphertext gout588 = ctx.eval_lut(&gin588, xor2);
    std::cerr << "\rLUT589   ";
    Ciphertext gin589 = 2 * gout559 + gout363;
    Ciphertext gout589 = ctx.eval_lut(&gin589, and2);
    std::cerr << "\rLUT590   ";
    Ciphertext gin590 = 2 * gout553 + gout88;
    Ciphertext gout590 = ctx.eval_lut(&gin590, xor2);
    std::cerr << "\rLUT591   ";
    Ciphertext gin591 = 2 * gout553 + gout88;
    Ciphertext gout591 = ctx.eval_lut(&gin591, and2);
    std::cerr << "\rLUT592   ";
    Ciphertext gin592 = 2 * gout549 + gout493;
    Ciphertext gout592 = ctx.eval_lut(&gin592, xnor2);
    std::cerr << "\rLUT593   ";
    Ciphertext gin593 = 2 * gout554 + gout464;
    Ciphertext gout593 = ctx.eval_lut(&gin593, xor2);
    std::cerr << "\rLUT594   ";
    Ciphertext gin594 = 2 * gout549 + gout493;
    Ciphertext gout594 = ctx.eval_lut(&gin594, and2);
    std::cerr << "\rLUT595   ";
    Ciphertext gin595 = 2 * gout556 + gout496;
    Ciphertext gout595 = ctx.eval_lut(&gin595, xnor2);
    std::cerr << "\rLUT596   ";
    Ciphertext gin596 = 2 * (1 - gout556) + gout496;
    Ciphertext gout596 = ctx.eval_lut(&gin596, and2);
    std::cerr << "\rLUT597   ";
    Ciphertext gin597 = 2 * (1 - gout546) + gout448;
    Ciphertext gout597 = ctx.eval_lut(&gin597, and2);
    std::cerr << "\rLUT598   ";
    Ciphertext gin598 = 2 * gout546 + gout448;
    Ciphertext gout598 = ctx.eval_lut(&gin598, xnor2);
    std::cerr << "\rLUT599   ";
    Ciphertext gin599 = 2 * gout558 + gout542;
    Ciphertext gout599 = ctx.eval_lut(&gin599, xnor2);
    std::cerr << "\rLUT600   ";
    Ciphertext gin600 = 2 * (1 - gout561) + gout431;
    Ciphertext gout600 = ctx.eval_lut(&gin600, and2);
    std::cerr << "\rLUT601   ";
    Ciphertext gin601 = 2 * gout382 + gout566;
    Ciphertext gout601 = ctx.eval_lut(&gin601, nand2);
    std::cerr << "\rLUT602   ";
    Ciphertext gin602 = 2 * gout564 + gout460;
    Ciphertext gout602 = ctx.eval_lut(&gin602, xnor2);
    std::cerr << "\rLUT603   ";
    Ciphertext gin603 = 2 * gout564 + gout460;
    Ciphertext gout603 = ctx.eval_lut(&gin603, and2);
    std::cerr << "\rLUT604   ";
    Ciphertext gin604 = 2 * gout554 + gout464;
    Ciphertext gout604 = ctx.eval_lut(&gin604, nand2);
    std::cerr << "\rLUT605   ";
    Ciphertext gin605 = 2 * gout571 + gout500;
    Ciphertext gout605 = ctx.eval_lut(&gin605, xor2);
    std::cerr << "\rLUT606   ";
    Ciphertext gin606 = 2 * gout576 + gout593;
    Ciphertext gout606 = ctx.eval_lut(&gin606, nand2);
    std::cerr << "\rLUT607   ";
    Ciphertext gin607 = 2 * (1 - gout575) + gout519;
    Ciphertext gout607 = ctx.eval_lut(&gin607, and2);
    std::cerr << "\rLUT608   ";
    Ciphertext gin608 = 2 * gout576 + gout593;
    Ciphertext gout608 = ctx.eval_lut(&gin608, xor2);
    std::cerr << "\rLUT609   ";
    Ciphertext gin609 = 2 * gout514 + gout577;
    Ciphertext gout609 = ctx.eval_lut(&gin609, or2);
    std::cerr << "\rLUT610   ";
    Ciphertext gin610 = 2 * gout382 + gout566;
    Ciphertext gout610 = ctx.eval_lut(&gin610, xor2);
    std::cerr << "\rLUT611   ";
    Ciphertext gin611 = 2 * gout572 + gout490;
    Ciphertext gout611 = ctx.eval_lut(&gin611, xnor2);
    std::cerr << "\rLUT612   ";
    Ciphertext gin612 = 2 * gout572 + gout490;
    Ciphertext gout612 = ctx.eval_lut(&gin612, and2);
    std::cerr << "\rLUT613   ";
    Ciphertext gin613 = 2 * gout569 + gout369;
    Ciphertext gout613 = ctx.eval_lut(&gin613, xnor2);
    std::cerr << "\rLUT614   ";
    Ciphertext gin614 = 2 * gout574 + gout601;
    Ciphertext gout614 = ctx.eval_lut(&gin614, nand2);
    std::cerr << "\rLUT615   ";
    Ciphertext gin615 = 2 * gout199 + gout570;
    Ciphertext gout615 = ctx.eval_lut(&gin615, nand2);
    std::cerr << "\rLUT616   ";
    Ciphertext gin616 = 2 * gout291 + gout570;
    Ciphertext gout616 = ctx.eval_lut(&gin616, xnor2);
    std::cerr << "\rLUT617   ";
    Ciphertext gin617 = 2 * gout571 + gout500;
    Ciphertext gout617 = ctx.eval_lut(&gin617, nand2);
    std::cerr << "\rLUT618   ";
    Ciphertext gin618 = 2 * gout575 + gout519;
    Ciphertext gout618 = ctx.eval_lut(&gin618, xnor2);
    std::cerr << "\rLUT619   ";
    Ciphertext gin619 = 2 * gout561 + gout431;
    Ciphertext gout619 = ctx.eval_lut(&gin619, xnor2);
    std::cerr << "\rIMUX620  ";
    Ciphertext gin620A = 2 * gout310 + gout391;
    Ciphertext gout620A = ctx.eval_lut(&gin620A, and2);
    Ciphertext gin620B = 2 * gout580 + (1 - gout391);
    Ciphertext gout620B = ctx.eval_lut(&gin620B, and2);
    Ciphertext gout620 = gout620A + gout620B;
    std::cerr << "\rLUT621   ";
    Ciphertext gin621 = 2 * (1 - gout582) + gout529;
    Ciphertext gout621 = ctx.eval_lut(&gin621, and2);
    std::cerr << "\rLUT622   ";
    Ciphertext gin622 = 2 * gout582 + gout529;
    Ciphertext gout622 = ctx.eval_lut(&gin622, xnor2);
    std::cerr << "\rLUT623   ";
    Ciphertext gin623 = 2 * gout532 + gout581;
    Ciphertext gout623 = ctx.eval_lut(&gin623, nand2);
    std::cerr << "\rLUT624   ";
    Ciphertext gin624 = 2 * ct_x7 + gout583;
    Ciphertext gout624 = ctx.eval_lut(&gin624, nand2);
    std::cerr << "\rLUT625   ";
    Ciphertext gin625 = 2 * gout95 + gout583;
    Ciphertext gout625 = ctx.eval_lut(&gin625, xnor2);
    std::cerr << "\rLUT626   ";
    Ciphertext gin626 = 2 * gout587 + gout498;
    Ciphertext gout626 = ctx.eval_lut(&gin626, xnor2);
    std::cerr << "\rLUT627   ";
    Ciphertext gin627 = 2 * gout592 + gout443;
    Ciphertext gout627 = ctx.eval_lut(&gin627, xnor2);
    std::cerr << "\rLUT628   ";
    Ciphertext gin628 = 2 * gout77 + gout584;
    Ciphertext gout628 = ctx.eval_lut(&gin628, xnor2);
    std::cerr << "\rLUT629   ";
    Ciphertext gin629 = 2 * gout551 + gout596;
    Ciphertext gout629 = ctx.eval_lut(&gin629, or2);
    std::cerr << "\rLUT630   ";
    Ciphertext gin630 = 2 * gout99 + gout585;
    Ciphertext gout630 = ctx.eval_lut(&gin630, and2);
    std::cerr << "\rLUT631   ";
    Ciphertext gin631 = 2 * (1 - gout587) + gout498;
    Ciphertext gout631 = ctx.eval_lut(&gin631, and2);
    std::cerr << "\rLUT632   ";
    Ciphertext gin632 = 2 * (1 - gout585) + gout332;
    Ciphertext gout632 = ctx.eval_lut(&gin632, and2);
    std::cerr << "\rLUT633   ";
    Ciphertext gin633 = 2 * gout543 + gout600;
    Ciphertext gout633 = ctx.eval_lut(&gin633, or2);
    std::cerr << "\rLUT634   ";
    Ciphertext gin634 = 2 * (1 - gout602) + gout360;
    Ciphertext gout634 = ctx.eval_lut(&gin634, and2);
    std::cerr << "\rLUT635   ";
    Ciphertext gin635 = 2 * gout533 + gout605;
    Ciphertext gout635 = ctx.eval_lut(&gin635, nand2);
    std::cerr << "\rLUT636   ";
    Ciphertext gin636 = 2 * gout562 + gout597;
    Ciphertext gout636 = ctx.eval_lut(&gin636, or2);
    std::cerr << "\rLUT637   ";
    Ciphertext gin637 = 2 * gout604 + gout606;
    Ciphertext gout637 = ctx.eval_lut(&gin637, nand2);
    std::cerr << "\rLUT638   ";
    Ciphertext gin638 = 2 * (1 - gout592) + gout443;
    Ciphertext gout638 = ctx.eval_lut(&gin638, and2);
    std::cerr << "\rLUT639   ";
    Ciphertext gin639 = 2 * gout533 + gout605;
    Ciphertext gout639 = ctx.eval_lut(&gin639, xor2);
    std::cerr << "\rLUT640   ";
    Ciphertext gin640 = 2 * gout602 + gout360;
    Ciphertext gout640 = ctx.eval_lut(&gin640, xnor2);
    std::cerr << "\rLUT641   ";
    Ciphertext gin641 = 2 * gout617 + gout635;
    Ciphertext gout641 = ctx.eval_lut(&gin641, nand2);
    std::cerr << "\rLUT642   ";
    Ciphertext gin642 = 2 * gout613 + gout565;
    Ciphertext gout642 = ctx.eval_lut(&gin642, xnor2);
    std::cerr << "\rLUT643   ";
    Ciphertext gin643 = 2 * gout611 + gout489;
    Ciphertext gout643 = ctx.eval_lut(&gin643, xnor2);
    std::cerr << "\rLUT644   ";
    Ciphertext gin644 = 2 * gout567 + gout607;
    Ciphertext gout644 = ctx.eval_lut(&gin644, or2);
    std::cerr << "\rLUT645   ";
    Ciphertext gin645 = 2 * gout514 + gout616;
    Ciphertext gout645 = ctx.eval_lut(&gin645, and2);
    std::cerr << "\rLUT646   ";
    Ciphertext gin646 = 2 * gout577 + gout616;
    Ciphertext gout646 = ctx.eval_lut(&gin646, and2);
    std::cerr << "\rLUT647   ";
    Ciphertext gin647 = 2 * gout616 + gout609;
    Ciphertext gout647 = ctx.eval_lut(&gin647, xor2);
    std::cerr << "\rLUT648   ";
    Ciphertext gin648 = 2 * gout407 + gout620;
    Ciphertext gout648 = ctx.eval_lut(&gin648, nand2);
    std::cerr << "\rLUT649   ";
    Ciphertext gin649 = 2 * (1 - gout611) + gout489;
    Ciphertext gout649 = ctx.eval_lut(&gin649, and2);
    std::cerr << "\rLUT650   ";
    Ciphertext gin650 = 2 * gout573 + gout621;
    Ciphertext gout650 = ctx.eval_lut(&gin650, or2);
    std::cerr << "\rLUT651   ";
    Ciphertext gin651 = 2 * gout623 + gout494;
    Ciphertext gout651 = ctx.eval_lut(&gin651, xnor2);
    std::cerr << "\rLUT652   ";
    Ciphertext gin652 = 2 * gout633 + gout622;
    Ciphertext gout652 = ctx.eval_lut(&gin652, xnor2);
    std::cerr << "\rLUT653   ";
    Ciphertext gin653 = 2 * gout633 + gout622;
    Ciphertext gout653 = ctx.eval_lut(&gin653, and2);
    std::cerr << "\rLUT654   ";
    Ciphertext gin654 = 2 * gout579 + gout615;
    Ciphertext gout654 = ctx.eval_lut(&gin654, nand2);
    std::cerr << "\rLUT655   ";
    Ciphertext gin655 = 2 * gout623 + gout494;
    Ciphertext gout655 = ctx.eval_lut(&gin655, nand2);
    std::cerr << "\rLUT656   ";
    Ciphertext gin656 = 2 * gout629 + gout627;
    Ciphertext gout656 = ctx.eval_lut(&gin656, and2);
    std::cerr << "\rLUT657   ";
    Ciphertext gin657 = 2 * gout6 + gout628;
    Ciphertext gout657 = ctx.eval_lut(&gin657, xnor2);
    std::cerr << "\rLUT658   ";
    Ciphertext gin658 = 2 * (1 - gout630) + gout332;
    Ciphertext gout658 = ctx.eval_lut(&gin658, and2);
    std::cerr << "\rLUT659   ";
    Ciphertext gin659 = 2 * gout636 + gout595;
    Ciphertext gout659 = ctx.eval_lut(&gin659, xor2);
    std::cerr << "\rLUT660   ";
    Ciphertext gin660 = 2 * ct_x8 + gout628;
    Ciphertext gout660 = ctx.eval_lut(&gin660, nand2);
    std::cerr << "\rLUT661   ";
    Ciphertext gin661 = 2 * gout629 + gout627;
    Ciphertext gout661 = ctx.eval_lut(&gin661, xnor2);
    std::cerr << "\rLUT662   ";
    Ciphertext gin662 = 2 * gout594 + gout638;
    Ciphertext gout662 = ctx.eval_lut(&gin662, or2);
    std::cerr << "\rLUT663   ";
    Ciphertext gin663 = 2 * gout99 + gout632;
    Ciphertext gout663 = ctx.eval_lut(&gin663, xnor2);
    std::cerr << "\rLUT664   ";
    Ciphertext gin664 = 2 * gout89 + gout630;
    Ciphertext gout664 = ctx.eval_lut(&gin664, nand2);
    std::cerr << "\rLUT665   ";
    Ciphertext gin665 = 2 * gout636 + gout595;
    Ciphertext gout665 = ctx.eval_lut(&gin665, nand2);
    std::cerr << "\rLUT666   ";
    Ciphertext gin666 = 2 * gout586 + gout631;
    Ciphertext gout666 = ctx.eval_lut(&gin666, or2);
    std::cerr << "\rLUT667   ";
    Ciphertext gin667 = 2 * gout603 + gout634;
    Ciphertext gout667 = ctx.eval_lut(&gin667, or2);
    std::cerr << "\rINV668   ";
    Ciphertext gout668 = 1 - gout640;

    std::cerr << "\rLUT669   ";
    Ciphertext gin669 = 2 * gout568 + gout647;
    Ciphertext gout669 = ctx.eval_lut(&gin669, xnor2);
    std::cerr << "\rLUT670   ";
    Ciphertext gin670 = 2 * gout182 + gout637;
    Ciphertext gout670 = ctx.eval_lut(&gin670, xor2);
    std::cerr << "\rLUT671   ";
    Ciphertext gin671 = 2 * gout644 + gout643;
    Ciphertext gout671 = ctx.eval_lut(&gin671, xnor2);
    std::cerr << "\rLUT672   ";
    Ciphertext gin672 = 2 * gout666 + gout619;
    Ciphertext gout672 = ctx.eval_lut(&gin672, xnor2);
    std::cerr << "\rLUT673   ";
    Ciphertext gin673 = 2 * gout182 + gout637;
    Ciphertext gout673 = ctx.eval_lut(&gin673, and2);
    std::cerr << "\rLUT674   ";
    Ciphertext gin674 = 2 * gout641 + gout441;
    Ciphertext gout674 = ctx.eval_lut(&gin674, xnor2);
    std::cerr << "\rINV675   ";
    Ciphertext gout675 = 1 - gout645;

    std::cerr << "\rLUT676   ";
    Ciphertext gin676 = 2 * gout612 + gout649;
    Ciphertext gout676 = ctx.eval_lut(&gin676, or2);
    std::cerr << "\rLUT677   ";
    Ciphertext gin677 = 2 * gout599 + gout642;
    Ciphertext gout677 = ctx.eval_lut(&gin677, xnor2);
    std::cerr << "\rINV678   ";
    Ciphertext gout678 = 1 - gout646;

    std::cerr << "\rLUT679   ";
    Ciphertext gin679 = 2 * gout666 + gout619;
    Ciphertext gout679 = ctx.eval_lut(&gin679, and2);
    std::cerr << "\rLUT680   ";
    Ciphertext gin680 = 2 * gout644 + gout643;
    Ciphertext gout680 = ctx.eval_lut(&gin680, and2);
    std::cerr << "\rLUT681   ";
    Ciphertext gin681 = 2 * gout568 + gout647;
    Ciphertext gout681 = ctx.eval_lut(&gin681, nand2);
    std::cerr << "\rIMUX682  ";
    Ciphertext gin682A = 2 * gout516 + gout391;
    Ciphertext gout682A = ctx.eval_lut(&gin682A, and2);
    Ciphertext gin682B = 2 * gout651 + (1 - gout391);
    Ciphertext gout682B = ctx.eval_lut(&gin682B, and2);
    Ciphertext gout682 = gout682A + gout682B;
    std::cerr << "\rLUT683   ";
    Ciphertext gin683 = 2 * gout650 + gout618;
    Ciphertext gout683 = ctx.eval_lut(&gin683, xnor2);
    std::cerr << "\rLUT684   ";
    Ciphertext gin684 = 2 * gout650 + gout618;
    Ciphertext gout684 = ctx.eval_lut(&gin684, and2);
    std::cerr << "\rLUT685   ";
    Ciphertext gin685 = 2 * gout654 + gout640;
    Ciphertext gout685 = ctx.eval_lut(&gin685, and2);
    std::cerr << "\rLUT686   ";
    Ciphertext gin686 = 2 * gout654 + gout668;
    Ciphertext gout686 = ctx.eval_lut(&gin686, xnor2);
    std::cerr << "\rLUT687   ";
    Ciphertext gin687 = 2 * gout652 + gout610;
    Ciphertext gout687 = ctx.eval_lut(&gin687, xnor2);
    std::cerr << "\rLUT688   ";
    Ciphertext gin688 = 2 * (1 - gout652) + gout610;
    Ciphertext gout688 = ctx.eval_lut(&gin688, and2);
    std::cerr << "\rLUT689   ";
    Ciphertext gin689 = 2 * gout523 + gout655;
    Ciphertext gout689 = ctx.eval_lut(&gin689, nand2);
    std::cerr << "\rLUT690   ";
    Ciphertext gin690 = 2 * (1 - gout661) + gout590;
    Ciphertext gout690 = ctx.eval_lut(&gin690, and2);
    std::cerr << "\rLUT691   ";
    Ciphertext gin691 = 2 * gout444 + gout659;
    Ciphertext gout691 = ctx.eval_lut(&gin691, xor2);
    std::cerr << "\rLUT692   ";
    Ciphertext gin692 = 2 * gout661 + gout590;
    Ciphertext gout692 = ctx.eval_lut(&gin692, xnor2);
    std::cerr << "\rLUT693   ";
    Ciphertext gin693 = 2 * ct_x9 + gout663;
    Ciphertext gout693 = ctx.eval_lut(&gin693, nand2);
    std::cerr << "\rLUT694   ";
    Ciphertext gin694 = 2 * gout89 + gout658;
    Ciphertext gout694 = ctx.eval_lut(&gin694, xnor2);
    std::cerr << "\rLUT695   ";
    Ciphertext gin695 = 2 * ct_x9 + gout663;
    Ciphertext gout695 = ctx.eval_lut(&gin695, or2);
    std::cerr << "\rLUT696   ";
    Ciphertext gin696 = 2 * gout662 + gout626;
    Ciphertext gout696 = ctx.eval_lut(&gin696, and2);
    std::cerr << "\rLUT697   ";
    Ciphertext gin697 = 2 * gout667 + gout598;
    Ciphertext gout697 = ctx.eval_lut(&gin697, nand2);
    std::cerr << "\rLUT698   ";
    Ciphertext gin698 = 2 * gout671 + gout639;
    Ciphertext gout698 = ctx.eval_lut(&gin698, xnor2);
    std::cerr << "\rLUT699   ";
    Ciphertext gin699 = 2 * gout444 + gout659;
    Ciphertext gout699 = ctx.eval_lut(&gin699, nand2);
    std::cerr << "\rLUT700   ";
    Ciphertext gin700 = 2 * gout667 + gout598;
    Ciphertext gout700 = ctx.eval_lut(&gin700, xor2);
    std::cerr << "\rLUT701   ";
    Ciphertext gin701 = 2 * (1 - gout671) + gout639;
    Ciphertext gout701 = ctx.eval_lut(&gin701, and2);
    std::cerr << "\rLUT702   ";
    Ciphertext gin702 = 2 * gout672 + gout588;
    Ciphertext gout702 = ctx.eval_lut(&gin702, xnor2);
    std::cerr << "\rLUT703   ";
    Ciphertext gin703 = 2 * gout43 + gout663;
    Ciphertext gout703 = ctx.eval_lut(&gin703, xnor2);
    std::cerr << "\rLUT704   ";
    Ciphertext gin704 = 2 * (1 - gout672) + gout588;
    Ciphertext gout704 = ctx.eval_lut(&gin704, and2);
    std::cerr << "\rLUT705   ";
    Ciphertext gin705 = 2 * gout662 + gout626;
    Ciphertext gout705 = ctx.eval_lut(&gin705, xnor2);
    std::cerr << "\rLUT706   ";
    Ciphertext gin706 = 2 * gout332 + gout664;
    Ciphertext gout706 = ctx.eval_lut(&gin706, and2);
    std::cerr << "\rLUT707   ";
    Ciphertext gin707 = 2 * gout680 + gout701;
    Ciphertext gout707 = ctx.eval_lut(&gin707, or2);
    std::cerr << "\rINV708   ";
    Ciphertext gout708 = 1 - gout681;

    std::cerr << "\rLUT709   ";
    Ciphertext gin709 = 2 * gout679 + gout704;
    Ciphertext gout709 = ctx.eval_lut(&gin709, or2);
    std::cerr << "\rINV710   ";
    Ciphertext gout710 = 1 - gout685;

    std::cerr << "\rLUT711   ";
    Ciphertext gin711 = 2 * gout675 + gout686;
    Ciphertext gout711 = ctx.eval_lut(&gin711, xnor2);
    std::cerr << "\rLUT712   ";
    Ciphertext gin712 = 2 * gout676 + gout677;
    Ciphertext gout712 = ctx.eval_lut(&gin712, xnor2);
    std::cerr << "\rLUT713   ";
    Ciphertext gin713 = 2 * gout645 + gout686;
    Ciphertext gout713 = ctx.eval_lut(&gin713, and2);
    std::cerr << "\rLUT714   ";
    Ciphertext gin714 = 2 * gout407 + gout682;
    Ciphertext gout714 = ctx.eval_lut(&gin714, nand2);
    std::cerr << "\rLUT715   ";
    Ciphertext gin715 = 2 * (1 - gout683) + gout608;
    Ciphertext gout715 = ctx.eval_lut(&gin715, and2);
    std::cerr << "\rLUT716   ";
    Ciphertext gin716 = 2 * gout683 + gout608;
    Ciphertext gout716 = ctx.eval_lut(&gin716, xnor2);
    std::cerr << "\rLUT717   ";
    Ciphertext gin717 = 2 * gout689 + gout503;
    Ciphertext gout717 = ctx.eval_lut(&gin717, xnor2);
    std::cerr << "\rLUT718   ";
    Ciphertext gin718 = 2 * gout653 + gout688;
    Ciphertext gout718 = ctx.eval_lut(&gin718, or2);
    std::cerr << "\rLUT719   ";
    Ciphertext gin719 = 2 * gout689 + gout503;
    Ciphertext gout719 = ctx.eval_lut(&gin719, nand2);
    std::cerr << "\rLUT720   ";
    Ciphertext gin720 = 2 * gout656 + gout690;
    Ciphertext gout720 = ctx.eval_lut(&gin720, or2);
    std::cerr << "\rLUT721   ";
    Ciphertext gin721 = 2 * gout660 + gout693;
    Ciphertext gout721 = ctx.eval_lut(&gin721, nand2);
    std::cerr << "\rLUT722   ";
    Ciphertext gin722 = 2 * gout87 + gout694;
    Ciphertext gout722 = ctx.eval_lut(&gin722, xnor2);
    std::cerr << "\rLUT723   ";
    Ciphertext gin723 = 2 * gout400 + gout700;
    Ciphertext gout723 = ctx.eval_lut(&gin723, nand2);
    std::cerr << "\rLUT724   ";
    Ciphertext gin724 = 2 * (1 - gout705) + gout550;
    Ciphertext gout724 = ctx.eval_lut(&gin724, and2);
    std::cerr << "\rLUT725   ";
    Ciphertext gin725 = 2 * gout400 + gout700;
    Ciphertext gout725 = ctx.eval_lut(&gin725, xor2);
    std::cerr << "\rLUT726   ";
    Ciphertext gin726 = 2 * ct_x10 + gout694;
    Ciphertext gout726 = ctx.eval_lut(&gin726, nand2);
    std::cerr << "\rLUT727   ";
    Ciphertext gin727 = 2 * (1 - gout706) + gout399;
    Ciphertext gout727 = ctx.eval_lut(&gin727, and2);
    std::cerr << "\rLUT728   ";
    Ciphertext gin728 = 2 * ct_y11 + gout706;
    Ciphertext gout728 = ctx.eval_lut(&gin728, xnor2);
    std::cerr << "\rLUT729   ";
    Ciphertext gin729 = 2 * gout665 + gout699;
    Ciphertext gout729 = ctx.eval_lut(&gin729, nand2);
    std::cerr << "\rINV730   ";
    Ciphertext gout730 = 1 - gout703;

    std::cerr << "\rLUT731   ";
    Ciphertext gin731 = 2 * gout705 + gout550;
    Ciphertext gout731 = ctx.eval_lut(&gin731, xnor2);
    std::cerr << "\rLUT732   ";
    Ciphertext gin732 = 2 * gout709 + gout687;
    Ciphertext gout732 = ctx.eval_lut(&gin732, xor2);
    std::cerr << "\rLUT733   ";
    Ciphertext gin733 = 2 * gout674 + gout712;
    Ciphertext gout733 = ctx.eval_lut(&gin733, xnor2);
    std::cerr << "\rLUT734   ";
    Ciphertext gin734 = 2 * gout710 + gout725;
    Ciphertext gout734 = ctx.eval_lut(&gin734, xnor2);
    std::cerr << "\rLUT735   ";
    Ciphertext gin735 = 2 * gout685 + gout725;
    Ciphertext gout735 = ctx.eval_lut(&gin735, and2);
    std::cerr << "\rLUT736   ";
    Ciphertext gin736 = 2 * gout678 + gout711;
    Ciphertext gout736 = ctx.eval_lut(&gin736, xnor2);
    std::cerr << "\rLUT737   ";
    Ciphertext gin737 = 2 * gout684 + gout715;
    Ciphertext gout737 = ctx.eval_lut(&gin737, or2);
    std::cerr << "\rLUT738   ";
    Ciphertext gin738 = 2 * gout709 + gout687;
    Ciphertext gout738 = ctx.eval_lut(&gin738, nand2);
    std::cerr << "\rLUT739   ";
    Ciphertext gin739 = 2 * gout646 + gout711;
    Ciphertext gout739 = ctx.eval_lut(&gin739, nand2);
    std::cerr << "\rLUT740   ";
    Ciphertext gin740 = 2 * gout718 + gout716;
    Ciphertext gout740 = ctx.eval_lut(&gin740, xor2);
    std::cerr << "\rINV741   ";
    Ciphertext gout741 = 1 - gout713;

    std::cerr << "\rIMUX742  ";
    Ciphertext gin742A = 2 * gout717 + gout471;
    Ciphertext gout742A = ctx.eval_lut(&gin742A, and2);
    Ciphertext gin742B = 2 * gout563 + (1 - gout471);
    Ciphertext gout742B = ctx.eval_lut(&gin742B, and2);
    Ciphertext gout742 = gout742A + gout742B;
    std::cerr << "\rLUT743   ";
    Ciphertext gin743 = 2 * gout520 + gout719;
    Ciphertext gout743 = ctx.eval_lut(&gin743, nand2);
    std::cerr << "\rLUT744   ";
    Ciphertext gin744 = 2 * gout718 + gout716;
    Ciphertext gout744 = ctx.eval_lut(&gin744, nand2);
    std::cerr << "\rLUT745   ";
    Ciphertext gin745 = 2 * gout695 + gout721;
    Ciphertext gout745 = ctx.eval_lut(&gin745, nand2);
    std::cerr << "\rLUT746   ";
    Ciphertext gin746 = 2 * gout720 + gout731;
    Ciphertext gout746 = ctx.eval_lut(&gin746, nand2);
    std::cerr << "\rLUT747   ";
    Ciphertext gin747 = 2 * gout729 + gout692;
    Ciphertext gout747 = ctx.eval_lut(&gin747, xor2);
    std::cerr << "\rLUT748   ";
    Ciphertext gin748 = 2 * gout696 + gout724;
    Ciphertext gout748 = ctx.eval_lut(&gin748, or2);
    std::cerr << "\rLUT749   ";
    Ciphertext gin749 = 2 * gout589 + gout732;
    Ciphertext gout749 = ctx.eval_lut(&gin749, nand2);
    std::cerr << "\rLUT750   ";
    Ciphertext gin750 = 2 * gout132 + gout728;
    Ciphertext gout750 = ctx.eval_lut(&gin750, nand2);
    std::cerr << "\rLUT751   ";
    Ciphertext gin751 = 2 * gout132 + gout728;
    Ciphertext gout751 = ctx.eval_lut(&gin751, or2);
    std::cerr << "\rLUT752   ";
    Ciphertext gin752 = 2 * ct_x11 + gout728;
    Ciphertext gout752 = ctx.eval_lut(&gin752, xnor2);
    std::cerr << "\rLUT753   ";
    Ciphertext gin753 = 2 * gout727 + gout409;
    Ciphertext gout753 = ctx.eval_lut(&gin753, nand2);
    std::cerr << "\rLUT754   ";
    Ciphertext gin754 = 2 * gout720 + gout731;
    Ciphertext gout754 = ctx.eval_lut(&gin754, xor2);
    std::cerr << "\rLUT755   ";
    Ciphertext gin755 = 2 * gout697 + gout723;
    Ciphertext gout755 = ctx.eval_lut(&gin755, nand2);
    std::cerr << "\rLUT756   ";
    Ciphertext gin756 = 2 * gout729 + gout692;
    Ciphertext gout756 = ctx.eval_lut(&gin756, and2);
    std::cerr << "\rLUT757   ";
    Ciphertext gin757 = 2 * ct_y12 + gout727;
    Ciphertext gout757 = ctx.eval_lut(&gin757, xnor2);
    std::cerr << "\rLUT758   ";
    Ciphertext gin758 = 2 * gout707 + gout733;
    Ciphertext gout758 = ctx.eval_lut(&gin758, xnor2);
    std::cerr << "\rLUT759   ";
    Ciphertext gin759 = 2 * gout737 + gout698;
    Ciphertext gout759 = ctx.eval_lut(&gin759, xnor2);
    std::cerr << "\rLUT760   ";
    Ciphertext gin760 = 2 * gout713 + gout734;
    Ciphertext gout760 = ctx.eval_lut(&gin760, nand2);
    std::cerr << "\rLUT761   ";
    Ciphertext gin761 = 2 * gout737 + gout698;
    Ciphertext gout761 = ctx.eval_lut(&gin761, nand2);
    std::cerr << "\rLUT762   ";
    Ciphertext gin762 = 2 * gout708 + gout736;
    Ciphertext gout762 = ctx.eval_lut(&gin762, nand2);
    std::cerr << "\rLUT763   ";
    Ciphertext gin763 = 2 * gout589 + gout732;
    Ciphertext gout763 = ctx.eval_lut(&gin763, xor2);
    std::cerr << "\rLUT764   ";
    Ciphertext gin764 = 2 * gout614 + gout740;
    Ciphertext gout764 = ctx.eval_lut(&gin764, xor2);
    std::cerr << "\rLUT765   ";
    Ciphertext gin765 = 2 * gout614 + gout740;
    Ciphertext gout765 = ctx.eval_lut(&gin765, nand2);
    std::cerr << "\rLUT766   ";
    Ciphertext gin766 = 2 * gout708 + gout736;
    Ciphertext gout766 = ctx.eval_lut(&gin766, xnor2);
    std::cerr << "\rINV767   ";
    Ciphertext gout767 = 1 - gout735;

    std::cerr << "\rLUT768   ";
    Ciphertext gin768 = 2 * gout739 + gout762;
    Ciphertext gout768 = ctx.eval_lut(&gin768, nand2);
    std::cerr << "\rLUT769   ";
    Ciphertext gin769 = 2 * gout738 + gout749;
    Ciphertext gout769 = ctx.eval_lut(&gin769, nand2);
    std::cerr << "\rLUT770   ";
    Ciphertext gin770 = 2 * gout407 + gout742;
    Ciphertext gout770 = ctx.eval_lut(&gin770, nand2);
    std::cerr << "\rLUT771   ";
    Ciphertext gin771 = 2 * gout743 + gout487;
    Ciphertext gout771 = ctx.eval_lut(&gin771, xnor2);
    std::cerr << "\rLUT772   ";
    Ciphertext gin772 = 2 * gout741 + gout734;
    Ciphertext gout772 = ctx.eval_lut(&gin772, xnor2);
    std::cerr << "\rLUT773   ";
    Ciphertext gin773 = 2 * gout744 + gout765;
    Ciphertext gout773 = ctx.eval_lut(&gin773, nand2);
    std::cerr << "\rLUT774   ";
    Ciphertext gin774 = 2 * gout743 + gout487;
    Ciphertext gout774 = ctx.eval_lut(&gin774, nand2);
    std::cerr << "\rLUT775   ";
    Ciphertext gin775 = 2 * gout726 + gout751;
    Ciphertext gout775 = ctx.eval_lut(&gin775, nand2);
    std::cerr << "\rLUT776   ";
    Ciphertext gin776 = 2 * gout591 + gout754;
    Ciphertext gout776 = ctx.eval_lut(&gin776, nand2);
    std::cerr << "\rLUT777   ";
    Ciphertext gin777 = 2 * gout748 + gout702;
    Ciphertext gout777 = ctx.eval_lut(&gin777, nand2);
    std::cerr << "\rLUT778   ";
    Ciphertext gin778 = 2 * gout755 + gout691;
    Ciphertext gout778 = ctx.eval_lut(&gin778, and2);
    std::cerr << "\rLUT779   ";
    Ciphertext gin779 = 2 * gout591 + gout754;
    Ciphertext gout779 = ctx.eval_lut(&gin779, xor2);
    std::cerr << "\rLUT780   ";
    Ciphertext gin780 = 2 * gout32 + gout757;
    Ciphertext gout780 = ctx.eval_lut(&gin780, xnor2);
    std::cerr << "\rLUT781   ";
    Ciphertext gin781 = 2 * gout748 + gout702;
    Ciphertext gout781 = ctx.eval_lut(&gin781, xor2);
    std::cerr << "\rLUT782   ";
    Ciphertext gin782 = 2 * gout759 + gout670;
    Ciphertext gout782 = ctx.eval_lut(&gin782, xnor2);
    std::cerr << "\rLUT783   ";
    Ciphertext gin783 = 2 * (1 - gout759) + gout670;
    Ciphertext gout783 = ctx.eval_lut(&gin783, and2);
    std::cerr << "\rINV784   ";
    Ciphertext gout784 = 1 - gout756;

    std::cerr << "\rINV785   ";
    Ciphertext gout785 = 1 - gout752;

    std::cerr << "\rLUT786   ";
    Ciphertext gin786 = 2 * gout755 + gout691;
    Ciphertext gout786 = ctx.eval_lut(&gin786, xor2);
    std::cerr << "\rLUT787   ";
    Ciphertext gin787 = 2 * ct_y13 + gout753;
    Ciphertext gout787 = ctx.eval_lut(&gin787, xnor2);
    std::cerr << "\rLUT788   ";
    Ciphertext gin788 = 2 * gout753 + gout375;
    Ciphertext gout788 = ctx.eval_lut(&gin788, or2);
    std::cerr << "\rLUT789   ";
    Ciphertext gin789 = 2 * ct_x12 + gout757;
    Ciphertext gout789 = ctx.eval_lut(&gin789, nand2);
    std::cerr << "\rLUT790   ";
    Ciphertext gin790 = 2 * gout769 + gout764;
    Ciphertext gout790 = ctx.eval_lut(&gin790, xor2);
    std::cerr << "\rLUT791   ";
    Ciphertext gin791 = 2 * gout767 + gout760;
    Ciphertext gout791 = ctx.eval_lut(&gin791, and2);
    std::cerr << "\rLUT792   ";
    Ciphertext gin792 = 2 * gout769 + gout764;
    Ciphertext gout792 = ctx.eval_lut(&gin792, and2);
    std::cerr << "\rIMUX793  ";
    Ciphertext gin793A = 2 * gout669 + gout391;
    Ciphertext gout793A = ctx.eval_lut(&gin793A, and2);
    Ciphertext gin793B = 2 * gout771 + (1 - gout391);
    Ciphertext gout793B = ctx.eval_lut(&gin793B, and2);
    Ciphertext gout793 = gout793A + gout793B;
    std::cerr << "\rLUT794   ";
    Ciphertext gin794 = 2 * gout772 + gout768;
    Ciphertext gout794 = ctx.eval_lut(&gin794, xnor2);
    std::cerr << "\rLUT795   ";
    Ciphertext gin795 = 2 * gout507 + gout774;
    Ciphertext gout795 = ctx.eval_lut(&gin795, nand2);
    std::cerr << "\rLUT796   ";
    Ciphertext gin796 = 2 * gout772 + gout768;
    Ciphertext gout796 = ctx.eval_lut(&gin796, and2);
    std::cerr << "\rLUT797   ";
    Ciphertext gin797 = 2 * gout773 + gout782;
    Ciphertext gout797 = ctx.eval_lut(&gin797, xor2);
    std::cerr << "\rLUT798   ";
    Ciphertext gin798 = 2 * gout773 + gout782;
    Ciphertext gout798 = ctx.eval_lut(&gin798, and2);
    std::cerr << "\rLUT799   ";
    Ciphertext gin799 = 2 * gout750 + gout775;
    Ciphertext gout799 = ctx.eval_lut(&gin799, nand2);
    std::cerr << "\rLUT800   ";
    Ciphertext gin800 = 2 * gout778 + gout747;
    Ciphertext gout800 = ctx.eval_lut(&gin800, xor2);
    std::cerr << "\rLUT801   ";
    Ciphertext gin801 = 2 * gout756 + gout779;
    Ciphertext gout801 = ctx.eval_lut(&gin801, and2);
    std::cerr << "\rLUT802   ";
    Ciphertext gin802 = 2 * gout778 + gout747;
    Ciphertext gout802 = ctx.eval_lut(&gin802, and2);
    std::cerr << "\rLUT803   ";
    Ciphertext gin803 = 2 * gout552 + gout781;
    Ciphertext gout803 = ctx.eval_lut(&gin803, nand2);
    std::cerr << "\rLUT804   ";
    Ciphertext gin804 = 2 * gout761 + (1 - gout783);
    Ciphertext gout804 = ctx.eval_lut(&gin804, and2);
    std::cerr << "\rLUT805   ";
    Ciphertext gin805 = 2 * gout746 + gout776;
    Ciphertext gout805 = ctx.eval_lut(&gin805, nand2);
    std::cerr << "\rLUT806   ";
    Ciphertext gin806 = 2 * gout552 + gout781;
    Ciphertext gout806 = ctx.eval_lut(&gin806, xor2);
    std::cerr << "\rLUT807   ";
    Ciphertext gin807 = 2 * gout735 + gout786;
    Ciphertext gout807 = ctx.eval_lut(&gin807, and2);
    std::cerr << "\rLUT808   ";
    Ciphertext gin808 = 2 * gout1 + gout788;
    Ciphertext gout808 = ctx.eval_lut(&gin808, xnor2);
    std::cerr << "\rLUT809   ";
    Ciphertext gin809 = 2 * gout788 + gout370;
    Ciphertext gout809 = ctx.eval_lut(&gin809, or2);
    std::cerr << "\rLUT810   ";
    Ciphertext gin810 = 2 * gout784 + gout779;
    Ciphertext gout810 = ctx.eval_lut(&gin810, xnor2);
    std::cerr << "\rLUT811   ";
    Ciphertext gin811 = 2 * gout30 + gout787;
    Ciphertext gout811 = ctx.eval_lut(&gin811, nand2);
    std::cerr << "\rLUT812   ";
    Ciphertext gin812 = 2 * gout30 + gout787;
    Ciphertext gout812 = ctx.eval_lut(&gin812, or2);
    std::cerr << "\rLUT813   ";
    Ciphertext gin813 = 2 * ct_x13 + gout787;
    Ciphertext gout813 = ctx.eval_lut(&gin813, xnor2);
    std::cerr << "\rLUT814   ";
    Ciphertext gin814 = 2 * gout760 + (1 - gout786);
    Ciphertext gout814 = ctx.eval_lut(&gin814, or2);
    std::cerr << "\rLUT815   ";
    Ciphertext gin815 = 2 * gout786 + gout791;
    Ciphertext gout815 = ctx.eval_lut(&gin815, xnor2);
    std::cerr << "\rLUT816   ";
    Ciphertext gin816 = 2 * gout407 + gout793;
    Ciphertext gout816 = ctx.eval_lut(&gin816, nand2);
    std::cerr << "\rLUT817   ";
    Ciphertext gin817 = 2 * gout795 + gout548;
    Ciphertext gout817 = ctx.eval_lut(&gin817, xnor2);
    std::cerr << "\rLUT818   ";
    Ciphertext gin818 = 2 * gout796 + gout815;
    Ciphertext gout818 = ctx.eval_lut(&gin818, nand2);
    std::cerr << "\rLUT819   ";
    Ciphertext gin819 = 2 * gout796 + gout815;
    Ciphertext gout819 = ctx.eval_lut(&gin819, xnor2);
    std::cerr << "\rLUT820   ";
    Ciphertext gin820 = 2 * gout795 + gout548;
    Ciphertext gout820 = ctx.eval_lut(&gin820, nand2);
    std::cerr << "\rLUT821   ";
    Ciphertext gin821 = 2 * gout792 + gout797;
    Ciphertext gout821 = ctx.eval_lut(&gin821, xor2);
    std::cerr << "\rLUT822   ";
    Ciphertext gin822 = 2 * gout792 + gout797;
    Ciphertext gout822 = ctx.eval_lut(&gin822, nand2);
    std::cerr << "\rLUT823   ";
    Ciphertext gin823 = 2 * gout673 + gout798;
    Ciphertext gout823 = ctx.eval_lut(&gin823, xnor2);
    std::cerr << "\rLUT824   ";
    Ciphertext gin824 = 2 * gout777 + gout803;
    Ciphertext gout824 = ctx.eval_lut(&gin824, nand2);
    std::cerr << "\rINV825   ";
    Ciphertext gout825 = 1 - gout801;

    std::cerr << "\rLUT826   ";
    Ciphertext gin826 = 2 * gout805 + gout806;
    Ciphertext gout826 = ctx.eval_lut(&gin826, xor2);
    std::cerr << "\rLUT827   ";
    Ciphertext gin827 = 2 * gout807 + gout800;
    Ciphertext gout827 = ctx.eval_lut(&gin827, xnor2);
    std::cerr << "\rLUT828   ";
    Ciphertext gin828 = 2 * gout805 + gout806;
    Ciphertext gout828 = ctx.eval_lut(&gin828, and2);
    std::cerr << "\rLUT829   ";
    Ciphertext gin829 = 2 * gout807 + gout800;
    Ciphertext gout829 = ctx.eval_lut(&gin829, nand2);
    std::cerr << "\rLUT830   ";
    Ciphertext gin830 = 2 * gout68 + gout808;
    Ciphertext gout830 = ctx.eval_lut(&gin830, xnor2);
    std::cerr << "\rLUT831   ";
    Ciphertext gin831 = 2 * gout804 + gout758;
    Ciphertext gout831 = ctx.eval_lut(&gin831, xnor2);
    std::cerr << "\rLUT832   ";
    Ciphertext gin832 = 2 * gout809 + gout116;
    Ciphertext gout832 = ctx.eval_lut(&gin832, xnor2);
    std::cerr << "\rLUT833   ";
    Ciphertext gin833 = 2 * ct_x14 + gout808;
    Ciphertext gout833 = ctx.eval_lut(&gin833, nand2);
    std::cerr << "\rLUT834   ";
    Ciphertext gin834 = 2 * gout789 + gout812;
    Ciphertext gout834 = ctx.eval_lut(&gin834, nand2);
    std::cerr << "\rLUT835   ";
    Ciphertext gin835 = 2 * gout802 + gout810;
    Ciphertext gout835 = ctx.eval_lut(&gin835, xor2);
    std::cerr << "\rLUT836   ";
    Ciphertext gin836 = 2 * gout802 + gout810;
    Ciphertext gout836 = ctx.eval_lut(&gin836, nand2);
    std::cerr << "\rINV837   ";
    Ciphertext gout837 = 1 - gout813;

    std::cerr << "\rIMUX838  ";
    Ciphertext gin838A = 2 * gout766 + gout391;
    Ciphertext gout838A = ctx.eval_lut(&gin838A, and2);
    Ciphertext gin838B = 2 * gout817 + (1 - gout391);
    Ciphertext gout838B = ctx.eval_lut(&gin838B, and2);
    Ciphertext gout838 = gout838A + gout838B;
    std::cerr << "\rLUT839   ";
    Ciphertext gin839 = 2 * gout818 + gout814;
    Ciphertext gout839 = ctx.eval_lut(&gin839, and2);
    std::cerr << "\rLUT840   ";
    Ciphertext gin840 = 2 * gout547 + gout820;
    Ciphertext gout840 = ctx.eval_lut(&gin840, nand2);
    std::cerr << "\rLUT841   ";
    Ciphertext gin841 = 2 * gout831 + gout823;
    Ciphertext gout841 = ctx.eval_lut(&gin841, xnor2);
    std::cerr << "\rLUT842   ";
    Ciphertext gin842 = 2 * gout825 + gout826;
    Ciphertext gout842 = ctx.eval_lut(&gin842, xnor2);
    std::cerr << "\rLUT843   ";
    Ciphertext gin843 = 2 * gout824 + gout763;
    Ciphertext gout843 = ctx.eval_lut(&gin843, and2);
    std::cerr << "\rLUT844   ";
    Ciphertext gin844 = 2 * gout824 + gout763;
    Ciphertext gout844 = ctx.eval_lut(&gin844, xor2);
    std::cerr << "\rLUT845   ";
    Ciphertext gin845 = 2 * gout801 + gout826;
    Ciphertext gout845 = ctx.eval_lut(&gin845, nand2);
    std::cerr << "\rLUT846   ";
    Ciphertext gin846 = 2 * gout811 + gout834;
    Ciphertext gout846 = ctx.eval_lut(&gin846, nand2);
    std::cerr << "\rLUT847   ";
    Ciphertext gin847 = 2 * gout407 + gout838;
    Ciphertext gout847 = ctx.eval_lut(&gin847, nand2);
    std::cerr << "\rLUT848   ";
    Ciphertext gin848 = 2 * gout839 + gout827;
    Ciphertext gout848 = ctx.eval_lut(&gin848, or2);
    std::cerr << "\rLUT849   ";
    Ciphertext gin849 = 2 * gout839 + gout827;
    Ciphertext gout849 = ctx.eval_lut(&gin849, nand2);
    std::cerr << "\rLUT850   ";
    Ciphertext gin850 = 2 * gout840 + gout625;
    Ciphertext gout850 = ctx.eval_lut(&gin850, nand2);
    std::cerr << "\rLUT851   ";
    Ciphertext gin851 = 2 * gout840 + gout625;
    Ciphertext gout851 = ctx.eval_lut(&gin851, xnor2);
    std::cerr << "\rLUT852   ";
    Ciphertext gin852 = 2 * gout843 + gout790;
    Ciphertext gout852 = ctx.eval_lut(&gin852, xor2);
    std::cerr << "\rLUT853   ";
    Ciphertext gin853 = 2 * gout843 + gout790;
    Ciphertext gout853 = ctx.eval_lut(&gin853, nand2);
    std::cerr << "\rLUT854   ";
    Ciphertext gin854 = 2 * gout828 + gout844;
    Ciphertext gout854 = ctx.eval_lut(&gin854, nand2);
    std::cerr << "\rLUT855   ";
    Ciphertext gin855 = 2 * gout828 + gout844;
    Ciphertext gout855 = ctx.eval_lut(&gin855, xor2);
    std::cerr << "\rLUT856   ";
    Ciphertext gin856 = 2 * gout391 + gout848;
    Ciphertext gout856 = ctx.eval_lut(&gin856, and2);
    std::cerr << "\rLUT857   ";
    Ciphertext gin857 = 2 * gout829 + gout848;
    Ciphertext gout857 = ctx.eval_lut(&gin857, nand2);
    std::cerr << "\rIMUX858  ";
    Ciphertext gin858A = 2 * gout794 + gout391;
    Ciphertext gout858A = ctx.eval_lut(&gin858A, and2);
    Ciphertext gin858B = 2 * gout851 + (1 - gout391);
    Ciphertext gout858B = ctx.eval_lut(&gin858B, and2);
    Ciphertext gout858 = gout858A + gout858B;
    std::cerr << "\rLUT859   ";
    Ciphertext gin859 = 2 * gout624 + gout850;
    Ciphertext gout859 = ctx.eval_lut(&gin859, nand2);
    std::cerr << "\rLUT860   ";
    Ciphertext gin860 = 2 * gout857 + gout835;
    Ciphertext gout860 = ctx.eval_lut(&gin860, xnor2);
    std::cerr << "\rLUT861   ";
    Ciphertext gin861 = 2 * gout857 + gout835;
    Ciphertext gout861 = ctx.eval_lut(&gin861, nand2);
    std::cerr << "\rLUT862   ";
    Ciphertext gin862 = 2 * gout859 + gout657;
    Ciphertext gout862 = ctx.eval_lut(&gin862, nand2);
    std::cerr << "\rLUT863   ";
    Ciphertext gin863 = 2 * gout859 + gout657;
    Ciphertext gout863 = ctx.eval_lut(&gin863, xnor2);
    std::cerr << "\rLUT864   ";
    Ciphertext gin864 = 2 * gout849 + gout856;
    Ciphertext gout864 = ctx.eval_lut(&gin864, nand2);
    std::cerr << "\rLUT865   ";
    Ciphertext gin865 = 2 * gout407 + gout858;
    Ciphertext gout865 = ctx.eval_lut(&gin865, nand2);
    std::cerr << "\rLUT866   ";
    Ciphertext gin866 = 2 * gout660 + gout862;
    Ciphertext gout866 = ctx.eval_lut(&gin866, nand2);
    std::cerr << "\rLUT867   ";
    Ciphertext gin867 = 2 * gout836 + gout861;
    Ciphertext gout867 = ctx.eval_lut(&gin867, nand2);
    std::cerr << "\rIMUX868  ";
    Ciphertext gin868A = 2 * gout819 + gout391;
    Ciphertext gout868A = ctx.eval_lut(&gin868A, and2);
    Ciphertext gin868B = 2 * gout863 + (1 - gout391);
    Ciphertext gout868B = ctx.eval_lut(&gin868B, and2);
    Ciphertext gout868 = gout868A + gout868B;
    std::cerr << "\rLUT869   ";
    Ciphertext gin869 = 2 * gout862 + gout730;
    Ciphertext gout869 = ctx.eval_lut(&gin869, or2);
    std::cerr << "\rLUT870   ";
    Ciphertext gin870 = 2 * gout407 + gout864;
    Ciphertext gout870 = ctx.eval_lut(&gin870, and2);
    std::cerr << "\rLUT871   ";
    Ciphertext gin871 = 2 * gout866 + gout703;
    Ciphertext gout871 = ctx.eval_lut(&gin871, or2);
    std::cerr << "\rLUT872   ";
    Ciphertext gin872 = 2 * gout866 + gout703;
    Ciphertext gout872 = ctx.eval_lut(&gin872, nand2);
    std::cerr << "\rLUT873   ";
    Ciphertext gin873 = 2 * gout867 + gout842;
    Ciphertext gout873 = ctx.eval_lut(&gin873, nand2);
    std::cerr << "\rLUT874   ";
    Ciphertext gin874 = 2 * gout867 + gout842;
    Ciphertext gout874 = ctx.eval_lut(&gin874, or2);
    std::cerr << "\rLUT875   ";
    Ciphertext gin875 = 2 * gout407 + gout868;
    Ciphertext gout875 = ctx.eval_lut(&gin875, nand2);
    std::cerr << "\rLUT876   ";
    Ciphertext gin876 = 2 * gout869 + gout745;
    Ciphertext gout876 = ctx.eval_lut(&gin876, nand2);
    std::cerr << "\rLUT877   ";
    Ciphertext gin877 = 2 * gout471 + gout871;
    Ciphertext gout877 = ctx.eval_lut(&gin877, and2);
    std::cerr << "\rLUT878   ";
    Ciphertext gin878 = 2 * gout391 + gout874;
    Ciphertext gout878 = ctx.eval_lut(&gin878, and2);
    std::cerr << "\rLUT879   ";
    Ciphertext gin879 = 2 * gout845 + gout873;
    Ciphertext gout879 = ctx.eval_lut(&gin879, nand2);
    std::cerr << "\rLUT880   ";
    Ciphertext gin880 = 2 * gout876 + gout722;
    Ciphertext gout880 = ctx.eval_lut(&gin880, xnor2);
    std::cerr << "\rLUT881   ";
    Ciphertext gin881 = 2 * gout876 + gout722;
    Ciphertext gout881 = ctx.eval_lut(&gin881, nand2);
    std::cerr << "\rLUT882   ";
    Ciphertext gin882 = 2 * gout872 + gout877;
    Ciphertext gout882 = ctx.eval_lut(&gin882, nand2);
    std::cerr << "\rLUT883   ";
    Ciphertext gin883 = 2 * gout882 + gout870;
    Ciphertext gout883 = ctx.eval_lut(&gin883, nand2);
    std::cerr << "\rLUT884   ";
    Ciphertext gin884 = 2 * gout879 + gout855;
    Ciphertext gout884 = ctx.eval_lut(&gin884, nand2);
    std::cerr << "\rLUT885   ";
    Ciphertext gin885 = 2 * gout879 + gout855;
    Ciphertext gout885 = ctx.eval_lut(&gin885, xnor2);
    std::cerr << "\rLUT886   ";
    Ciphertext gin886 = 2 * gout881 + gout785;
    Ciphertext gout886 = ctx.eval_lut(&gin886, or2);
    std::cerr << "\rLUT887   ";
    Ciphertext gin887 = 2 * gout873 + gout878;
    Ciphertext gout887 = ctx.eval_lut(&gin887, nand2);
    std::cerr << "\rLUT888   ";
    Ciphertext gin888 = 2 * gout726 + gout881;
    Ciphertext gout888 = ctx.eval_lut(&gin888, nand2);
    std::cerr << "\rIMUX889  ";
    Ciphertext gin889A = 2 * gout860 + gout391;
    Ciphertext gout889A = ctx.eval_lut(&gin889A, and2);
    Ciphertext gin889B = 2 * gout880 + (1 - gout391);
    Ciphertext gout889B = ctx.eval_lut(&gin889B, and2);
    Ciphertext gout889 = gout889A + gout889B;
    std::cerr << "\rLUT890   ";
    Ciphertext gin890 = 2 * gout854 + gout884;
    Ciphertext gout890 = ctx.eval_lut(&gin890, nand2);
    std::cerr << "\rLUT891   ";
    Ciphertext gin891 = 2 * gout407 + gout887;
    Ciphertext gout891 = ctx.eval_lut(&gin891, and2);
    std::cerr << "\rLUT892   ";
    Ciphertext gin892 = 2 * gout888 + gout752;
    Ciphertext gout892 = ctx.eval_lut(&gin892, nand2);
    std::cerr << "\rLUT893   ";
    Ciphertext gin893 = 2 * gout886 + gout799;
    Ciphertext gout893 = ctx.eval_lut(&gin893, nand2);
    std::cerr << "\rLUT894   ";
    Ciphertext gin894 = 2 * gout888 + gout752;
    Ciphertext gout894 = ctx.eval_lut(&gin894, or2);
    std::cerr << "\rLUT895   ";
    Ciphertext gin895 = 2 * gout407 + gout889;
    Ciphertext gout895 = ctx.eval_lut(&gin895, nand2);
    std::cerr << "\rLUT896   ";
    Ciphertext gin896 = 2 * gout890 + gout852;
    Ciphertext gout896 = ctx.eval_lut(&gin896, or2);
    std::cerr << "\rLUT897   ";
    Ciphertext gin897 = 2 * gout890 + gout852;
    Ciphertext gout897 = ctx.eval_lut(&gin897, nand2);
    std::cerr << "\rLUT898   ";
    Ciphertext gin898 = 2 * gout893 + gout780;
    Ciphertext gout898 = ctx.eval_lut(&gin898, xnor2);
    std::cerr << "\rLUT899   ";
    Ciphertext gin899 = 2 * gout471 + gout894;
    Ciphertext gout899 = ctx.eval_lut(&gin899, and2);
    std::cerr << "\rLUT900   ";
    Ciphertext gin900 = 2 * gout893 + gout780;
    Ciphertext gout900 = ctx.eval_lut(&gin900, nand2);
    std::cerr << "\rLUT901   ";
    Ciphertext gin901 = 2 * gout853 + gout897;
    Ciphertext gout901 = ctx.eval_lut(&gin901, nand2);
    std::cerr << "\rIMUX902  ";
    Ciphertext gin902A = 2 * gout885 + gout391;
    Ciphertext gout902A = ctx.eval_lut(&gin902A, and2);
    Ciphertext gin902B = 2 * gout898 + (1 - gout391);
    Ciphertext gout902B = ctx.eval_lut(&gin902B, and2);
    Ciphertext gout902 = gout902A + gout902B;
    std::cerr << "\rLUT903   ";
    Ciphertext gin903 = 2 * gout391 + gout896;
    Ciphertext gout903 = ctx.eval_lut(&gin903, and2);
    std::cerr << "\rLUT904   ";
    Ciphertext gin904 = 2 * gout892 + gout899;
    Ciphertext gout904 = ctx.eval_lut(&gin904, nand2);
    std::cerr << "\rLUT905   ";
    Ciphertext gin905 = 2 * gout900 + gout837;
    Ciphertext gout905 = ctx.eval_lut(&gin905, or2);
    std::cerr << "\rLUT906   ";
    Ciphertext gin906 = 2 * gout789 + gout900;
    Ciphertext gout906 = ctx.eval_lut(&gin906, nand2);
    std::cerr << "\rLUT907   ";
    Ciphertext gin907 = 2 * gout901 + gout821;
    Ciphertext gout907 = ctx.eval_lut(&gin907, nand2);
    std::cerr << "\rLUT908   ";
    Ciphertext gin908 = 2 * gout901 + gout821;
    Ciphertext gout908 = ctx.eval_lut(&gin908, xnor2);
    std::cerr << "\rLUT909   ";
    Ciphertext gin909 = 2 * gout897 + gout903;
    Ciphertext gout909 = ctx.eval_lut(&gin909, nand2);
    std::cerr << "\rLUT910   ";
    Ciphertext gin910 = 2 * gout904 + gout891;
    Ciphertext gout910 = ctx.eval_lut(&gin910, nand2);
    std::cerr << "\rLUT911   ";
    Ciphertext gin911 = 2 * gout407 + gout902;
    Ciphertext gout911 = ctx.eval_lut(&gin911, nand2);
    std::cerr << "\rLUT912   ";
    Ciphertext gin912 = 2 * gout905 + gout846;
    Ciphertext gout912 = ctx.eval_lut(&gin912, nand2);
    std::cerr << "\rLUT913   ";
    Ciphertext gin913 = 2 * gout906 + gout813;
    Ciphertext gout913 = ctx.eval_lut(&gin913, nand2);
    std::cerr << "\rLUT914   ";
    Ciphertext gin914 = 2 * gout906 + gout813;
    Ciphertext gout914 = ctx.eval_lut(&gin914, or2);
    std::cerr << "\rLUT915   ";
    Ciphertext gin915 = 2 * gout822 + gout907;
    Ciphertext gout915 = ctx.eval_lut(&gin915, and2);
    std::cerr << "\rLUT916   ";
    Ciphertext gin916 = 2 * gout407 + gout909;
    Ciphertext gout916 = ctx.eval_lut(&gin916, and2);
    std::cerr << "\rLUT917   ";
    Ciphertext gin917 = 2 * gout912 + gout830;
    Ciphertext gout917 = ctx.eval_lut(&gin917, nand2);
    std::cerr << "\rLUT918   ";
    Ciphertext gin918 = 2 * gout912 + gout830;
    Ciphertext gout918 = ctx.eval_lut(&gin918, xnor2);
    std::cerr << "\rLUT919   ";
    Ciphertext gin919 = 2 * gout471 + gout914;
    Ciphertext gout919 = ctx.eval_lut(&gin919, and2);
    std::cerr << "\rLUT920   ";
    Ciphertext gin920 = 2 * gout915 + gout841;
    Ciphertext gout920 = ctx.eval_lut(&gin920, or2);
    std::cerr << "\rLUT921   ";
    Ciphertext gin921 = 2 * gout915 + gout841;
    Ciphertext gout921 = ctx.eval_lut(&gin921, nand2);
    std::cerr << "\rIMUX922  ";
    Ciphertext gin922A = 2 * gout908 + gout391;
    Ciphertext gout922A = ctx.eval_lut(&gin922A, and2);
    Ciphertext gin922B = 2 * gout918 + (1 - gout391);
    Ciphertext gout922B = ctx.eval_lut(&gin922B, and2);
    Ciphertext gout922 = gout922A + gout922B;
    std::cerr << "\rLUT923   ";
    Ciphertext gin923 = 2 * gout833 + gout917;
    Ciphertext gout923 = ctx.eval_lut(&gin923, and2);
    std::cerr << "\rLUT924   ";
    Ciphertext gin924 = 2 * gout913 + gout919;
    Ciphertext gout924 = ctx.eval_lut(&gin924, nand2);
    std::cerr << "\rLUT925   ";
    Ciphertext gin925 = 2 * gout391 + gout920;
    Ciphertext gout925 = ctx.eval_lut(&gin925, and2);
    std::cerr << "\rLUT926   ";
    Ciphertext gin926 = 2 * gout923 + gout832;
    Ciphertext gout926 = ctx.eval_lut(&gin926, nand2);
    std::cerr << "\rLUT927   ";
    Ciphertext gin927 = 2 * gout407 + gout922;
    Ciphertext gout927 = ctx.eval_lut(&gin927, nand2);
    std::cerr << "\rLUT928   ";
    Ciphertext gin928 = 2 * gout923 + gout832;
    Ciphertext gout928 = ctx.eval_lut(&gin928, or2);
    std::cerr << "\rLUT929   ";
    Ciphertext gin929 = 2 * gout924 + gout916;
    Ciphertext gout929 = ctx.eval_lut(&gin929, nand2);
    std::cerr << "\rLUT930   ";
    Ciphertext gin930 = 2 * gout921 + gout925;
    Ciphertext gout930 = ctx.eval_lut(&gin930, nand2);
    std::cerr << "\rLUT931   ";
    Ciphertext gin931 = 2 * gout471 + gout928;
    Ciphertext gout931 = ctx.eval_lut(&gin931, and2);
    std::cerr << "\rLUT932   ";
    Ciphertext gin932 = 2 * gout926 + gout931;
    Ciphertext gout932 = ctx.eval_lut(&gin932, nand2);
    std::cerr << "\rLUT933   ";
    Ciphertext gin933 = 2 * gout407 + gout932;
    Ciphertext gout933 = ctx.eval_lut(&gin933, and2);
    std::cerr << "\rLUT934   ";
    Ciphertext gin934 = 2 * gout930 + gout933;
    Ciphertext gout934 = ctx.eval_lut(&gin934, nand2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout502)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout578)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout648)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout714)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout770)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout816)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout847)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout865)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout875)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout883)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout895)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout910)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout911)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout929)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout927)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout934)); /* out15 */
    return test_out;
}

static void
BM_calculator_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_calculator_boolean(ctx);
    }
}

BENCHMARK(BM_calculator_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
