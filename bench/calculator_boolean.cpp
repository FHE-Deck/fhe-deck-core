#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_calculator_boolean(FHEContext& ctx, uint8_t op = 0, uint16_t x = 0, uint16_t y = 0)
{
    Ciphertext ct_op0 = ctx.encrypt_public((op >> 0) & 1);
    Ciphertext ct_op1 = ctx.encrypt_public((op >> 1) & 1);
    Ciphertext ct_op2 = ctx.encrypt_public((op >> 2) & 1);
    Ciphertext ct_op3 = ctx.encrypt_public((op >> 3) & 1);
    Ciphertext ct_op4 = ctx.encrypt_public((op >> 4) & 1);
    Ciphertext ct_op5 = ctx.encrypt_public((op >> 5) & 1);
    Ciphertext ct_op6 = ctx.encrypt_public((op >> 6) & 1);
    Ciphertext ct_op7 = ctx.encrypt_public((op >> 7) & 1);
    Ciphertext ct_x0 = ctx.encrypt_public((x >> 0) & 1);
    Ciphertext ct_x1 = ctx.encrypt_public((x >> 1) & 1);
    Ciphertext ct_x2 = ctx.encrypt_public((x >> 2) & 1);
    Ciphertext ct_x3 = ctx.encrypt_public((x >> 3) & 1);
    Ciphertext ct_x4 = ctx.encrypt_public((x >> 4) & 1);
    Ciphertext ct_x5 = ctx.encrypt_public((x >> 5) & 1);
    Ciphertext ct_x6 = ctx.encrypt_public((x >> 6) & 1);
    Ciphertext ct_x7 = ctx.encrypt_public((x >> 7) & 1);
    Ciphertext ct_x8 = ctx.encrypt_public((x >> 8) & 1);
    Ciphertext ct_x9 = ctx.encrypt_public((x >> 9) & 1);
    Ciphertext ct_x10 = ctx.encrypt_public((x >> 10) & 1);
    Ciphertext ct_x11 = ctx.encrypt_public((x >> 11) & 1);
    Ciphertext ct_x12 = ctx.encrypt_public((x >> 12) & 1);
    Ciphertext ct_x13 = ctx.encrypt_public((x >> 13) & 1);
    Ciphertext ct_x14 = ctx.encrypt_public((x >> 14) & 1);
    Ciphertext ct_x15 = ctx.encrypt_public((x >> 15) & 1);
    Ciphertext ct_y0 = ctx.encrypt_public((y >> 0) & 1);
    Ciphertext ct_y1 = ctx.encrypt_public((y >> 1) & 1);
    Ciphertext ct_y2 = ctx.encrypt_public((y >> 2) & 1);
    Ciphertext ct_y3 = ctx.encrypt_public((y >> 3) & 1);
    Ciphertext ct_y4 = ctx.encrypt_public((y >> 4) & 1);
    Ciphertext ct_y5 = ctx.encrypt_public((y >> 5) & 1);
    Ciphertext ct_y6 = ctx.encrypt_public((y >> 6) & 1);
    Ciphertext ct_y7 = ctx.encrypt_public((y >> 7) & 1);
    Ciphertext ct_y8 = ctx.encrypt_public((y >> 8) & 1);
    Ciphertext ct_y9 = ctx.encrypt_public((y >> 9) & 1);
    Ciphertext ct_y10 = ctx.encrypt_public((y >> 10) & 1);
    Ciphertext ct_y11 = ctx.encrypt_public((y >> 11) & 1);
    Ciphertext ct_y12 = ctx.encrypt_public((y >> 12) & 1);
    Ciphertext ct_y13 = ctx.encrypt_public((y >> 13) & 1);
    Ciphertext ct_y14 = ctx.encrypt_public((y >> 14) & 1);
    Ciphertext ct_y15 = ctx.encrypt_public((y >> 15) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_y0 + ct_x11;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nand2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_y12 + ct_x2;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nand2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * (1 - ct_op2) + ct_op1;
    Ciphertext gout2 = ctx.eval_lut(&gin2, and2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_y8 + ct_x5;
    Ciphertext gout3 = ctx.eval_lut(&gin3, and2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_y6 + ct_x3;
    Ciphertext gout4 = ctx.eval_lut(&gin4, and2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_y2 + ct_x3;
    Ciphertext gout5 = ctx.eval_lut(&gin5, and2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_y2 + ct_x9;
    Ciphertext gout6 = ctx.eval_lut(&gin6, and2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_y8 + ct_x0;
    Ciphertext gout7 = ctx.eval_lut(&gin7, and2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_y10 + ct_x3;
    Ciphertext gout8 = ctx.eval_lut(&gin8, and2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_y0 + ct_x4;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nand2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_y8 + ct_x7;
    Ciphertext gout10 = ctx.eval_lut(&gin10, and2);
    std::cerr << "\rINV11   ";
    Ciphertext gout11 = 1 - ct_y4;

    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_y6 + ct_x8;
    Ciphertext gout12 = ctx.eval_lut(&gin12, and2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_y11 + ct_x3;
    Ciphertext gout13 = ctx.eval_lut(&gin13, and2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_y8 + ct_x1;
    Ciphertext gout14 = ctx.eval_lut(&gin14, and2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_y2 + ct_x12;
    Ciphertext gout15 = ctx.eval_lut(&gin15, and2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_y3 + ct_x8;
    Ciphertext gout16 = ctx.eval_lut(&gin16, and2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_y3 + ct_x10;
    Ciphertext gout17 = ctx.eval_lut(&gin17, and2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_y7 + ct_x5;
    Ciphertext gout18 = ctx.eval_lut(&gin18, and2);
    std::cerr << "\rINV19   ";
    Ciphertext gout19 = 1 - ct_x3;

    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_y4 + ct_x10;
    Ciphertext gout20 = ctx.eval_lut(&gin20, and2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_y1 + ct_x3;
    Ciphertext gout21 = ctx.eval_lut(&gin21, and2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_y7 + ct_x8;
    Ciphertext gout22 = ctx.eval_lut(&gin22, and2);
    std::cerr << "\rINV23   ";
    Ciphertext gout23 = 1 - ct_y7;

    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_y11 + ct_x4;
    Ciphertext gout24 = ctx.eval_lut(&gin24, and2);
    std::cerr << "\rINV25   ";
    Ciphertext gout25 = 1 - ct_x12;

    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_y4 + ct_x8;
    Ciphertext gout26 = ctx.eval_lut(&gin26, and2);
    std::cerr << "\rINV27   ";
    Ciphertext gout27 = 1 - ct_y15;

    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_y11 + ct_x1;
    Ciphertext gout28 = ctx.eval_lut(&gin28, and2);
    std::cerr << "\rINV29   ";
    Ciphertext gout29 = 1 - ct_y2;

    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_y2 + ct_x5;
    Ciphertext gout30 = ctx.eval_lut(&gin30, and2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_y1 + ct_x8;
    Ciphertext gout31 = ctx.eval_lut(&gin31, and2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * ct_y12 + ct_x0;
    Ciphertext gout32 = ctx.eval_lut(&gin32, and2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_y3 + ct_x11;
    Ciphertext gout33 = ctx.eval_lut(&gin33, nand2);
    std::cerr << "\rINV34   ";
    Ciphertext gout34 = 1 - ct_x9;

    std::cerr << "\rINV35   ";
    Ciphertext gout35 = 1 - ct_y6;

    std::cerr << "\rINV36   ";
    Ciphertext gout36 = 1 - ct_x8;

    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * ct_y8 + ct_x4;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rINV38   ";
    Ciphertext gout38 = 1 - ct_y5;

    std::cerr << "\rINV39   ";
    Ciphertext gout39 = 1 - ct_y14;

    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * ct_y11 + ct_x2;
    Ciphertext gout40 = ctx.eval_lut(&gin40, and2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_y3 + ct_x2;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * ct_y3 + ct_x1;
    Ciphertext gout42 = ctx.eval_lut(&gin42, nand2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * ct_y3 + ct_x6;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * ct_y0 + ct_x2;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * ct_y1 + ct_x1;
    Ciphertext gout45 = ctx.eval_lut(&gin45, nand2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * ct_y0 + ct_x5;
    Ciphertext gout46 = ctx.eval_lut(&gin46, nand2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * ct_y0 + ct_x0;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * ct_y1 + ct_x7;
    Ciphertext gout48 = ctx.eval_lut(&gin48, and2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * ct_y2 + ct_x6;
    Ciphertext gout49 = ctx.eval_lut(&gin49, and2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_y4 + ct_x9;
    Ciphertext gout50 = ctx.eval_lut(&gin50, and2);
    std::cerr << "\rINV51   ";
    Ciphertext gout51 = 1 - ct_x14;

    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * ct_y0 + ct_x15;
    Ciphertext gout52 = ctx.eval_lut(&gin52, and2);
    std::cerr << "\rINV53   ";
    Ciphertext gout53 = 1 - ct_x5;

    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * ct_y13 + ct_x1;
    Ciphertext gout54 = ctx.eval_lut(&gin54, and2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_y8 + ct_x6;
    Ciphertext gout55 = ctx.eval_lut(&gin55, and2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * ct_y4 + ct_x1;
    Ciphertext gout56 = ctx.eval_lut(&gin56, and2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * ct_y3 + ct_x9;
    Ciphertext gout57 = ctx.eval_lut(&gin57, and2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_y6 + ct_x1;
    Ciphertext gout58 = ctx.eval_lut(&gin58, nand2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * ct_y6 + ct_x5;
    Ciphertext gout59 = ctx.eval_lut(&gin59, and2);
    std::cerr << "\rINV60   ";
    Ciphertext gout60 = 1 - ct_x4;

    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * ct_y2 + ct_x8;
    Ciphertext gout61 = ctx.eval_lut(&gin61, and2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * ct_y8 + ct_x3;
    Ciphertext gout62 = ctx.eval_lut(&gin62, and2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * ct_y6 + ct_x9;
    Ciphertext gout63 = ctx.eval_lut(&gin63, and2);
    std::cerr << "\rINV64   ";
    Ciphertext gout64 = 1 - ct_x13;

    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * ct_y0 + ct_x10;
    Ciphertext gout65 = ctx.eval_lut(&gin65, nand2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_y4 + ct_x6;
    Ciphertext gout66 = ctx.eval_lut(&gin66, and2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_y3 + ct_x12;
    Ciphertext gout67 = ctx.eval_lut(&gin67, and2);
    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * ct_y0 + ct_x13;
    Ciphertext gout68 = ctx.eval_lut(&gin68, and2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * ct_y0 + ct_x8;
    Ciphertext gout69 = ctx.eval_lut(&gin69, nand2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_y10 + ct_x5;
    Ciphertext gout70 = ctx.eval_lut(&gin70, and2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_y0 + ct_x7;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_y5 + ct_x1;
    Ciphertext gout72 = ctx.eval_lut(&gin72, and2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * ct_y10 + ct_x1;
    Ciphertext gout73 = ctx.eval_lut(&gin73, and2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * ct_y1 + ct_x4;
    Ciphertext gout74 = ctx.eval_lut(&gin74, and2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * ct_y1 + ct_x5;
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * ct_y3 + ct_x5;
    Ciphertext gout76 = ctx.eval_lut(&gin76, and2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * ct_y2 + ct_x7;
    Ciphertext gout77 = ctx.eval_lut(&gin77, and2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * ct_y13 + ct_x2;
    Ciphertext gout78 = ctx.eval_lut(&gin78, and2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * ct_y10 + ct_x4;
    Ciphertext gout79 = ctx.eval_lut(&gin79, nand2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * (1 - ct_x0) + ct_y15;
    Ciphertext gout80 = ctx.eval_lut(&gin80, and2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * ct_y4 + ct_x7;
    Ciphertext gout81 = ctx.eval_lut(&gin81, and2);
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * ct_y0 + ct_x9;
    Ciphertext gout82 = ctx.eval_lut(&gin82, nand2);
    std::cerr << "\rLUT83   ";
    Ciphertext gin83 = 2 * ct_y7 + ct_x0;
    Ciphertext gout83 = ctx.eval_lut(&gin83, and2);
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * ct_op6 + ct_op7;
    Ciphertext gout84 = ctx.eval_lut(&gin84, nor2);
    std::cerr << "\rLUT85   ";
    Ciphertext gin85 = 2 * ct_y4 + ct_x4;
    Ciphertext gout85 = ctx.eval_lut(&gin85, and2);
    std::cerr << "\rINV86   ";
    Ciphertext gout86 = 1 - ct_op0;

    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * ct_y3 + ct_x3;
    Ciphertext gout87 = ctx.eval_lut(&gin87, and2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * ct_y5 + ct_x5;
    Ciphertext gout88 = ctx.eval_lut(&gin88, and2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * ct_y5 + ct_x7;
    Ciphertext gout89 = ctx.eval_lut(&gin89, and2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * ct_y4 + ct_x11;
    Ciphertext gout90 = ctx.eval_lut(&gin90, and2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * ct_y9 + ct_x2;
    Ciphertext gout91 = ctx.eval_lut(&gin91, and2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * ct_y6 + ct_x0;
    Ciphertext gout92 = ctx.eval_lut(&gin92, and2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * ct_y1 + ct_x13;
    Ciphertext gout93 = ctx.eval_lut(&gin93, and2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * ct_y0 + ct_x12;
    Ciphertext gout94 = ctx.eval_lut(&gin94, and2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * ct_y0 + ct_x3;
    Ciphertext gout95 = ctx.eval_lut(&gin95, and2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * ct_y7 + ct_x4;
    Ciphertext gout96 = ctx.eval_lut(&gin96, and2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * ct_y9 + ct_x6;
    Ciphertext gout97 = ctx.eval_lut(&gin97, and2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * ct_y10 + ct_x2;
    Ciphertext gout98 = ctx.eval_lut(&gin98, and2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * ct_y1 + ct_x11;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * ct_y5 + ct_x9;
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * ct_y0 + ct_x1;
    Ciphertext gout101 = ctx.eval_lut(&gin101, and2);
    std::cerr << "\rINV102   ";
    Ciphertext gout102 = 1 - ct_y8;

    std::cerr << "\rINV103   ";
    Ciphertext gout103 = 1 - ct_x2;

    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * ct_y1 + ct_x2;
    Ciphertext gout104 = ctx.eval_lut(&gin104, and2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * ct_y1 + ct_x6;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * ct_y2 + ct_x11;
    Ciphertext gout106 = ctx.eval_lut(&gin106, and2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * ct_y3 + ct_x0;
    Ciphertext gout107 = ctx.eval_lut(&gin107, nand2);
    std::cerr << "\rINV108   ";
    Ciphertext gout108 = 1 - ct_x1;

    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * ct_y9 + ct_x4;
    Ciphertext gout109 = ctx.eval_lut(&gin109, and2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * ct_y9 + ct_x1;
    Ciphertext gout110 = ctx.eval_lut(&gin110, nand2);
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * ct_y5 + ct_x3;
    Ciphertext gout111 = ctx.eval_lut(&gin111, and2);
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * ct_y0 + ct_x0;
    Ciphertext gout112 = ctx.eval_lut(&gin112, xnor2);
    std::cerr << "\rINV113   ";
    Ciphertext gout113 = 1 - ct_x10;

    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * ct_y0 + ct_x14;
    Ciphertext gout114 = ctx.eval_lut(&gin114, and2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * ct_y9 + ct_x0;
    Ciphertext gout115 = ctx.eval_lut(&gin115, and2);
    std::cerr << "\rINV116   ";
    Ciphertext gout116 = 1 - ct_y10;

    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * ct_y12 + ct_x3;
    Ciphertext gout117 = ctx.eval_lut(&gin117, and2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * ct_y1 + ct_x0;
    Ciphertext gout118 = ctx.eval_lut(&gin118, and2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * ct_y8 + ct_x2;
    Ciphertext gout119 = ctx.eval_lut(&gin119, and2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * ct_y5 + ct_x0;
    Ciphertext gout120 = ctx.eval_lut(&gin120, and2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * ct_y2 + ct_x1;
    Ciphertext gout121 = ctx.eval_lut(&gin121, and2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * ct_y2 + ct_x4;
    Ciphertext gout122 = ctx.eval_lut(&gin122, and2);
    std::cerr << "\rINV123   ";
    Ciphertext gout123 = 1 - ct_x7;

    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * ct_y7 + ct_x3;
    Ciphertext gout124 = ctx.eval_lut(&gin124, and2);
    std::cerr << "\rINV125   ";
    Ciphertext gout125 = 1 - ct_y9;

    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * ct_y6 + ct_x6;
    Ciphertext gout126 = ctx.eval_lut(&gin126, and2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * ct_y0 + ct_x6;
    Ciphertext gout127 = ctx.eval_lut(&gin127, nand2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * ct_y11 + ct_x0;
    Ciphertext gout128 = ctx.eval_lut(&gin128, and2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * ct_y6 + ct_x7;
    Ciphertext gout129 = ctx.eval_lut(&gin129, and2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * ct_y1 + ct_x10;
    Ciphertext gout130 = ctx.eval_lut(&gin130, and2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * (1 - ct_op4) + ct_op5;
    Ciphertext gout131 = ctx.eval_lut(&gin131, and2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * ct_y1 + ct_x9;
    Ciphertext gout132 = ctx.eval_lut(&gin132, and2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * ct_y13 + ct_x0;
    Ciphertext gout133 = ctx.eval_lut(&gin133, and2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * (1 - ct_op1) + ct_op0;
    Ciphertext gout134 = ctx.eval_lut(&gin134, and2);
    std::cerr << "\rINV135   ";
    Ciphertext gout135 = 1 - ct_x6;

    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * ct_y2 + ct_x13;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_y6 + ct_x2;
    Ciphertext gout137 = ctx.eval_lut(&gin137, and2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * ct_y9 + ct_x3;
    Ciphertext gout138 = ctx.eval_lut(&gin138, and2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_y6 + ct_x4;
    Ciphertext gout139 = ctx.eval_lut(&gin139, and2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * ct_y7 + ct_x1;
    Ciphertext gout140 = ctx.eval_lut(&gin140, and2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * ct_y1 + ct_x12;
    Ciphertext gout141 = ctx.eval_lut(&gin141, and2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_y1 + ct_x14;
    Ciphertext gout142 = ctx.eval_lut(&gin142, and2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * ct_y5 + ct_x10;
    Ciphertext gout143 = ctx.eval_lut(&gin143, and2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * ct_y4 + ct_x0;
    Ciphertext gout144 = ctx.eval_lut(&gin144, and2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * ct_y15 + ct_x15;
    Ciphertext gout145 = ctx.eval_lut(&gin145, xor2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * ct_y7 + ct_x6;
    Ciphertext gout146 = ctx.eval_lut(&gin146, and2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_y5 + ct_x8;
    Ciphertext gout147 = ctx.eval_lut(&gin147, and2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * ct_op2 + ct_op3;
    Ciphertext gout148 = ctx.eval_lut(&gin148, and2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * ct_y7 + ct_x7;
    Ciphertext gout149 = ctx.eval_lut(&gin149, nand2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * ct_y9 + ct_x5;
    Ciphertext gout150 = ctx.eval_lut(&gin150, and2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_y5 + ct_x2;
    Ciphertext gout151 = ctx.eval_lut(&gin151, and2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_y10 + ct_x0;
    Ciphertext gout152 = ctx.eval_lut(&gin152, and2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * ct_y12 + ct_x1;
    Ciphertext gout153 = ctx.eval_lut(&gin153, nand2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * ct_y4 + ct_x5;
    Ciphertext gout154 = ctx.eval_lut(&gin154, and2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * ct_y2 + ct_x10;
    Ciphertext gout155 = ctx.eval_lut(&gin155, and2);
    std::cerr << "\rINV156   ";
    Ciphertext gout156 = 1 - ct_y3;

    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * ct_y7 + ct_x2;
    Ciphertext gout157 = ctx.eval_lut(&gin157, and2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * ct_y4 + ct_x2;
    Ciphertext gout158 = ctx.eval_lut(&gin158, and2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * ct_y1 + ct_y0;
    Ciphertext gout159 = ctx.eval_lut(&gin159, nor2);
    std::cerr << "\rINV160   ";
    Ciphertext gout160 = 1 - ct_x11;

    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * ct_y4 + ct_x3;
    Ciphertext gout161 = ctx.eval_lut(&gin161, and2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * ct_y14 + ct_x0;
    Ciphertext gout162 = ctx.eval_lut(&gin162, and2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_y5 + ct_x4;
    Ciphertext gout163 = ctx.eval_lut(&gin163, and2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_y14 + ct_x1;
    Ciphertext gout164 = ctx.eval_lut(&gin164, and2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * ct_y5 + ct_x6;
    Ciphertext gout165 = ctx.eval_lut(&gin165, and2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * ct_y2 + ct_x0;
    Ciphertext gout166 = ctx.eval_lut(&gin166, and2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_y3 + ct_x7;
    Ciphertext gout167 = ctx.eval_lut(&gin167, and2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * ct_y2 + ct_x2;
    Ciphertext gout168 = ctx.eval_lut(&gin168, and2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * ct_y3 + ct_x4;
    Ciphertext gout169 = ctx.eval_lut(&gin169, and2);
    std::cerr << "\rINV170   ";
    Ciphertext gout170 = 1 - ct_y1;

    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout46 + (1 - gout50);
    Ciphertext gout171 = ctx.eval_lut(&gin171, or2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout47 + (1 - gout45);
    Ciphertext gout172 = ctx.eval_lut(&gin172, and2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * gout54 + gout1;
    Ciphertext gout173 = ctx.eval_lut(&gin173, xnor2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout41 + gout5;
    Ciphertext gout174 = ctx.eval_lut(&gin174, xor2);
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * gout43 + gout77;
    Ciphertext gout175 = ctx.eval_lut(&gin175, nand2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * gout76 + gout49;
    Ciphertext gout176 = ctx.eval_lut(&gin176, xor2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * gout43 + gout77;
    Ciphertext gout177 = ctx.eval_lut(&gin177, xor2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * gout16 + gout6;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * gout16 + gout6;
    Ciphertext gout179 = ctx.eval_lut(&gin179, xor2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout32 + gout54;
    Ciphertext gout180 = ctx.eval_lut(&gin180, and2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * gout46 + gout56;
    Ciphertext gout181 = ctx.eval_lut(&gin181, xnor2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * gout73 + gout91;
    Ciphertext gout182 = ctx.eval_lut(&gin182, xor2);
    std::cerr << "\rINV183   ";
    Ciphertext gout183 = 1 - gout32;

    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout41 + gout5;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * ct_op3 + gout2;
    Ciphertext gout185 = ctx.eval_lut(&gin185, and2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * gout8 + gout109;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rINV187   ";
    Ciphertext gout187 = 1 - gout47;

    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout27 + gout67;
    Ciphertext gout188 = ctx.eval_lut(&gin188, xnor2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * gout76 + gout49;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout9 + (1 - gout26);
    Ciphertext gout190 = ctx.eval_lut(&gin190, or2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout121 + gout107;
    Ciphertext gout191 = ctx.eval_lut(&gin191, xnor2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * gout124 + gout139;
    Ciphertext gout192 = ctx.eval_lut(&gin192, xor2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout143 + gout63;
    Ciphertext gout193 = ctx.eval_lut(&gin193, xnor2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * gout9 + gout144;
    Ciphertext gout194 = ctx.eval_lut(&gin194, xnor2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * gout73 + gout91;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout96 + gout59;
    Ciphertext gout196 = ctx.eval_lut(&gin196, xor2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout47 + gout85;
    Ciphertext gout197 = ctx.eval_lut(&gin197, and2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * gout92 + gout140;
    Ciphertext gout198 = ctx.eval_lut(&gin198, and2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout146 + gout129;
    Ciphertext gout199 = ctx.eval_lut(&gin199, nand2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout24 + gout117;
    Ciphertext gout200 = ctx.eval_lut(&gin200, xnor2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout81 + gout0;
    Ciphertext gout201 = ctx.eval_lut(&gin201, xnor2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout98 + gout138;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout109 + gout70;
    Ciphertext gout203 = ctx.eval_lut(&gin203, nand2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout33 + gout15;
    Ciphertext gout204 = ctx.eval_lut(&gin204, xnor2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout98 + gout138;
    Ciphertext gout205 = ctx.eval_lut(&gin205, xor2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout87 + gout122;
    Ciphertext gout206 = ctx.eval_lut(&gin206, nand2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout121 + gout41;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout66 + gout65;
    Ciphertext gout208 = ctx.eval_lut(&gin208, xnor2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout44 + gout66;
    Ciphertext gout209 = ctx.eval_lut(&gin209, nand2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout142 + gout52;
    Ciphertext gout210 = ctx.eval_lut(&gin210, xnor2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout85 + gout69;
    Ciphertext gout211 = ctx.eval_lut(&gin211, xnor2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout58 + gout83;
    Ciphertext gout212 = ctx.eval_lut(&gin212, xnor2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout130 + gout179;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout106 + gout67;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout74 + gout174;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout18 + gout126;
    Ciphertext gout216 = ctx.eval_lut(&gin216, xor2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout124 + gout139;
    Ciphertext gout217 = ctx.eval_lut(&gin217, nand2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout57 + gout155;
    Ciphertext gout218 = ctx.eval_lut(&gin218, nand2);
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout130 + gout179;
    Ciphertext gout219 = ctx.eval_lut(&gin219, xor2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout74 + gout174;
    Ciphertext gout220 = ctx.eval_lut(&gin220, xor2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout167 + gout61;
    Ciphertext gout221 = ctx.eval_lut(&gin221, xor2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout129 + gout22;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rINV223   ";
    Ciphertext gout223 = 1 - gout44;

    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout140 + gout137;
    Ciphertext gout224 = ctx.eval_lut(&gin224, nand2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout140 + gout137;
    Ciphertext gout225 = ctx.eval_lut(&gin225, xor2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout101 + gout154;
    Ciphertext gout226 = ctx.eval_lut(&gin226, nand2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout71 + (1 - gout90);
    Ciphertext gout227 = ctx.eval_lut(&gin227, or2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout127 + (1 - gout20);
    Ciphertext gout228 = ctx.eval_lut(&gin228, or2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout162 + gout173;
    Ciphertext gout229 = ctx.eval_lut(&gin229, xor2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout87 + gout122;
    Ciphertext gout230 = ctx.eval_lut(&gin230, xor2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout162 + gout173;
    Ciphertext gout231 = ctx.eval_lut(&gin231, nand2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout136 + gout188;
    Ciphertext gout232 = ctx.eval_lut(&gin232, xnor2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout131 + gout84;
    Ciphertext gout233 = ctx.eval_lut(&gin233, and2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout118 + gout121;
    Ciphertext gout234 = ctx.eval_lut(&gin234, and2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout149 + gout12;
    Ciphertext gout235 = ctx.eval_lut(&gin235, xnor2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout128 + gout182;
    Ciphertext gout236 = ctx.eval_lut(&gin236, xor2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout29 + gout159;
    Ciphertext gout237 = ctx.eval_lut(&gin237, and2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout57 + gout155;
    Ciphertext gout238 = ctx.eval_lut(&gin238, xor2);
    std::cerr << "\rINV239   ";
    Ciphertext gout239 = 1 - gout95;

    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout96 + gout59;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout50 + gout68;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout154 + gout82;
    Ciphertext gout242 = ctx.eval_lut(&gin242, xnor2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * gout146 + gout129;
    Ciphertext gout243 = ctx.eval_lut(&gin243, xor2);
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout50 + gout68;
    Ciphertext gout244 = ctx.eval_lut(&gin244, xor2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * gout17 + gout106;
    Ciphertext gout245 = ctx.eval_lut(&gin245, nand2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout165 + gout201;
    Ciphertext gout246 = ctx.eval_lut(&gin246, xor2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout153 + (1 - gout78);
    Ciphertext gout247 = ctx.eval_lut(&gin247, or2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout118 + gout101;
    Ciphertext gout248 = ctx.eval_lut(&gin248, xor2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout71 + gout161;
    Ciphertext gout249 = ctx.eval_lut(&gin249, xnor2);
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout17 + gout106;
    Ciphertext gout250 = ctx.eval_lut(&gin250, xor2);
    std::cerr << "\rINV251   ";
    Ciphertext gout251 = 1 - gout159;

    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout8 + gout109;
    Ciphertext gout252 = ctx.eval_lut(&gin252, xor2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout115 + gout73;
    Ciphertext gout253 = ctx.eval_lut(&gin253, and2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout95 + gout81;
    Ciphertext gout254 = ctx.eval_lut(&gin254, nand2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout128 + gout182;
    Ciphertext gout255 = ctx.eval_lut(&gin255, nand2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout79 + gout150;
    Ciphertext gout256 = ctx.eval_lut(&gin256, xnor2);
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * gout42 + gout168;
    Ciphertext gout257 = ctx.eval_lut(&gin257, xnor2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout26 + gout94;
    Ciphertext gout258 = ctx.eval_lut(&gin258, nand2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout18 + gout126;
    Ciphertext gout259 = ctx.eval_lut(&gin259, nand2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * gout110 + gout152;
    Ciphertext gout260 = ctx.eval_lut(&gin260, xnor2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout26 + gout94;
    Ciphertext gout261 = ctx.eval_lut(&gin261, xor2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout10 + gout97;
    Ciphertext gout262 = ctx.eval_lut(&gin262, xnor2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout127 + gout158;
    Ciphertext gout263 = ctx.eval_lut(&gin263, xnor2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout167 + gout61;
    Ciphertext gout264 = ctx.eval_lut(&gin264, nand2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout120 + gout181;
    Ciphertext gout265 = ctx.eval_lut(&gin265, xor2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout120 + gout181;
    Ciphertext gout266 = ctx.eval_lut(&gin266, nand2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout169 + gout30;
    Ciphertext gout267 = ctx.eval_lut(&gin267, xor2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout169 + gout30;
    Ciphertext gout268 = ctx.eval_lut(&gin268, nand2);
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout20 + gout114;
    Ciphertext gout269 = ctx.eval_lut(&gin269, nand2);
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout20 + gout114;
    Ciphertext gout270 = ctx.eval_lut(&gin270, xor2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout165 + gout201;
    Ciphertext gout271 = ctx.eval_lut(&gin271, nand2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout153 + gout133;
    Ciphertext gout272 = ctx.eval_lut(&gin272, xnor2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout80 + gout164;
    Ciphertext gout273 = ctx.eval_lut(&gin273, xnor2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout45 + gout166;
    Ciphertext gout274 = ctx.eval_lut(&gin274, xnor2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * (1 - gout166) + gout42;
    Ciphertext gout275 = ctx.eval_lut(&gin275, or2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * gout134 + gout148;
    Ciphertext gout276 = ctx.eval_lut(&gin276, and2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout157 + gout4;
    Ciphertext gout277 = ctx.eval_lut(&gin277, xor2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout157 + gout4;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout31 + gout177;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout48 + gout176;
    Ciphertext gout280 = ctx.eval_lut(&gin280, xor2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout31 + gout177;
    Ciphertext gout281 = ctx.eval_lut(&gin281, xor2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout48 + gout176;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout22 + gout193;
    Ciphertext gout283 = ctx.eval_lut(&gin283, xnor2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout104 + gout191;
    Ciphertext gout284 = ctx.eval_lut(&gin284, nand2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout119 + gout192;
    Ciphertext gout285 = ctx.eval_lut(&gin285, xor2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout62 + gout196;
    Ciphertext gout286 = ctx.eval_lut(&gin286, xor2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout93 + gout204;
    Ciphertext gout287 = ctx.eval_lut(&gin287, xor2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout62 + gout196;
    Ciphertext gout288 = ctx.eval_lut(&gin288, nand2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout28 + gout205;
    Ciphertext gout289 = ctx.eval_lut(&gin289, nand2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout28 + gout205;
    Ciphertext gout290 = ctx.eval_lut(&gin290, xor2);
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout7 + gout225;
    Ciphertext gout291 = ctx.eval_lut(&gin291, nand2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout7 + gout225;
    Ciphertext gout292 = ctx.eval_lut(&gin292, xor2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout37 + gout216;
    Ciphertext gout293 = ctx.eval_lut(&gin293, nand2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout119 + gout192;
    Ciphertext gout294 = ctx.eval_lut(&gin294, nand2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout37 + gout216;
    Ciphertext gout295 = ctx.eval_lut(&gin295, xor2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout132 + gout221;
    Ciphertext gout296 = ctx.eval_lut(&gin296, xor2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout93 + gout204;
    Ciphertext gout297 = ctx.eval_lut(&gin297, nand2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout210 + gout232;
    Ciphertext gout298 = ctx.eval_lut(&gin298, xnor2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout75 + gout230;
    Ciphertext gout299 = ctx.eval_lut(&gin299, xor2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout55 + gout235;
    Ciphertext gout300 = ctx.eval_lut(&gin300, xor2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout88 + gout208;
    Ciphertext gout301 = ctx.eval_lut(&gin301, xor2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout55 + gout235;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout88 + gout208;
    Ciphertext gout303 = ctx.eval_lut(&gin303, nand2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout147 + gout244;
    Ciphertext gout304 = ctx.eval_lut(&gin304, xor2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout111 + gout211;
    Ciphertext gout305 = ctx.eval_lut(&gin305, xor2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout111 + gout211;
    Ciphertext gout306 = ctx.eval_lut(&gin306, nand2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout104 + gout191;
    Ciphertext gout307 = ctx.eval_lut(&gin307, xor2);
    std::cerr << "\rINV308   ";
    Ciphertext gout308 = 1 - gout197;

    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout247 + gout231;
    Ciphertext gout309 = ctx.eval_lut(&gin309, nand2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout163 + gout242;
    Ciphertext gout310 = ctx.eval_lut(&gin310, nand2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout253 + gout236;
    Ciphertext gout311 = ctx.eval_lut(&gin311, and2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout3 + gout243;
    Ciphertext gout312 = ctx.eval_lut(&gin312, xor2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout21 + gout257;
    Ciphertext gout313 = ctx.eval_lut(&gin313, nand2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout21 + gout257;
    Ciphertext gout314 = ctx.eval_lut(&gin314, xor2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout259 + gout293;
    Ciphertext gout315 = ctx.eval_lut(&gin315, nand2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout141 + gout250;
    Ciphertext gout316 = ctx.eval_lut(&gin316, nand2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout156 + gout237;
    Ciphertext gout317 = ctx.eval_lut(&gin317, and2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout141 + gout250;
    Ciphertext gout318 = ctx.eval_lut(&gin318, xor2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout226 + gout266;
    Ciphertext gout319 = ctx.eval_lut(&gin319, nand2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout40 + gout252;
    Ciphertext gout320 = ctx.eval_lut(&gin320, xor2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout75 + gout230;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout89 + gout261;
    Ciphertext gout322 = ctx.eval_lut(&gin322, xor2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout178 + gout213;
    Ciphertext gout323 = ctx.eval_lut(&gin323, nand2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout13 + gout256;
    Ciphertext gout324 = ctx.eval_lut(&gin324, xor2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout147 + gout244;
    Ciphertext gout325 = ctx.eval_lut(&gin325, nand2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout195 + gout255;
    Ciphertext gout326 = ctx.eval_lut(&gin326, nand2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout184 + gout215;
    Ciphertext gout327 = ctx.eval_lut(&gin327, nand2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout72 + gout263;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout99 + gout238;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout240 + gout288;
    Ciphertext gout330 = ctx.eval_lut(&gin330, nand2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout163 + gout242;
    Ciphertext gout331 = ctx.eval_lut(&gin331, xor2);
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout262 + gout200;
    Ciphertext gout332 = ctx.eval_lut(&gin332, xnor2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout132 + gout221;
    Ciphertext gout333 = ctx.eval_lut(&gin333, nand2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout105 + gout267;
    Ciphertext gout334 = ctx.eval_lut(&gin334, nand2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout3 + gout243;
    Ciphertext gout335 = ctx.eval_lut(&gin335, nand2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout40 + gout252;
    Ciphertext gout336 = ctx.eval_lut(&gin336, nand2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout151 + gout249;
    Ciphertext gout337 = ctx.eval_lut(&gin337, nand2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout13 + gout256;
    Ciphertext gout338 = ctx.eval_lut(&gin338, nand2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout151 + gout249;
    Ciphertext gout339 = ctx.eval_lut(&gin339, xor2);
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout223 + gout274;
    Ciphertext gout340 = ctx.eval_lut(&gin340, xnor2);
    std::cerr << "\rLUT341   ";
    Ciphertext gin341 = 2 * gout89 + gout261;
    Ciphertext gout341 = ctx.eval_lut(&gin341, nand2);
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout233 + gout276;
    Ciphertext gout342 = ctx.eval_lut(&gin342, and2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout275 + gout284;
    Ciphertext gout343 = ctx.eval_lut(&gin343, nand2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout100 + gout270;
    Ciphertext gout344 = ctx.eval_lut(&gin344, xor2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout100 + gout270;
    Ciphertext gout345 = ctx.eval_lut(&gin345, nand2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout72 + gout263;
    Ciphertext gout346 = ctx.eval_lut(&gin346, xor2);
    std::cerr << "\rLUT347   ";
    Ciphertext gin347 = 2 * gout99 + gout238;
    Ciphertext gout347 = ctx.eval_lut(&gin347, xor2);
    std::cerr << "\rINV348   ";
    Ciphertext gout348 = 1 - gout253;

    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout227 + gout271;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * gout14 + gout277;
    Ciphertext gout350 = ctx.eval_lut(&gin350, nand2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout233 + gout185;
    Ciphertext gout351 = ctx.eval_lut(&gin351, and2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout105 + gout267;
    Ciphertext gout352 = ctx.eval_lut(&gin352, xor2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout309 + gout273;
    Ciphertext gout353 = ctx.eval_lut(&gin353, xnor2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout44 + gout274;
    Ciphertext gout354 = ctx.eval_lut(&gin354, and2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout14 + gout277;
    Ciphertext gout355 = ctx.eval_lut(&gin355, xor2);
    std::cerr << "\rLUT356   ";
    Ciphertext gin356 = 2 * gout189 + gout282;
    Ciphertext gout356 = ctx.eval_lut(&gin356, nand2);
    std::cerr << "\rLUT357   ";
    Ciphertext gin357 = 2 * gout217 + gout294;
    Ciphertext gout357 = ctx.eval_lut(&gin357, nand2);
    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout175 + gout279;
    Ciphertext gout358 = ctx.eval_lut(&gin358, nand2);
    std::cerr << "\rLUT359   ";
    Ciphertext gin359 = 2 * gout190 + gout306;
    Ciphertext gout359 = ctx.eval_lut(&gin359, nand2);
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout234 + gout307;
    Ciphertext gout360 = ctx.eval_lut(&gin360, xor2);
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout206 + gout321;
    Ciphertext gout361 = ctx.eval_lut(&gin361, nand2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout171 + gout310;
    Ciphertext gout362 = ctx.eval_lut(&gin362, nand2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout11 + gout317;
    Ciphertext gout363 = ctx.eval_lut(&gin363, and2);
    std::cerr << "\rLUT364   ";
    Ciphertext gin364 = 2 * gout214 + gout297;
    Ciphertext gout364 = ctx.eval_lut(&gin364, and2);
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout245 + gout316;
    Ciphertext gout365 = ctx.eval_lut(&gin365, nand2);
    std::cerr << "\rLUT366   ";
    Ciphertext gin366 = 2 * gout319 + gout92;
    Ciphertext gout366 = ctx.eval_lut(&gin366, and2);
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout327 + gout299;
    Ciphertext gout367 = ctx.eval_lut(&gin367, xnor2);
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout326 + gout290;
    Ciphertext gout368 = ctx.eval_lut(&gin368, xor2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout326 + gout290;
    Ciphertext gout369 = ctx.eval_lut(&gin369, nand2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * gout327 + gout299;
    Ciphertext gout370 = ctx.eval_lut(&gin370, and2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout319 + gout92;
    Ciphertext gout371 = ctx.eval_lut(&gin371, xor2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout228 + gout303;
    Ciphertext gout372 = ctx.eval_lut(&gin372, nand2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout234 + gout307;
    Ciphertext gout373 = ctx.eval_lut(&gin373, nand2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout207 + gout313;
    Ciphertext gout374 = ctx.eval_lut(&gin374, nand2);
    std::cerr << "\rLUT375   ";
    Ciphertext gin375 = 2 * gout202 + gout289;
    Ciphertext gout375 = ctx.eval_lut(&gin375, nand2);
    std::cerr << "\rLUT376   ";
    Ciphertext gin376 = 2 * gout70 + gout332;
    Ciphertext gout376 = ctx.eval_lut(&gin376, xnor2);
    std::cerr << "\rLUT377   ";
    Ciphertext gin377 = 2 * gout203 + gout338;
    Ciphertext gout377 = ctx.eval_lut(&gin377, and2);
    std::cerr << "\rLUT378   ";
    Ciphertext gin378 = 2 * gout241 + gout325;
    Ciphertext gout378 = ctx.eval_lut(&gin378, nand2);
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout209 + gout328;
    Ciphertext gout379 = ctx.eval_lut(&gin379, nand2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout199 + gout335;
    Ciphertext gout380 = ctx.eval_lut(&gin380, nand2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout343 + gout314;
    Ciphertext gout381 = ctx.eval_lut(&gin381, xnor2);
    std::cerr << "\rLUT382   ";
    Ciphertext gin382 = 2 * gout222 + gout302;
    Ciphertext gout382 = ctx.eval_lut(&gin382, and2);
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * ct_y13 + gout342;
    Ciphertext gout383 = ctx.eval_lut(&gin383, and2);
    std::cerr << "\rLUT384   ";
    Ciphertext gin384 = 2 * ct_y11 + gout342;
    Ciphertext gout384 = ctx.eval_lut(&gin384, nand2);
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout218 + gout329;
    Ciphertext gout385 = ctx.eval_lut(&gin385, nand2);
    std::cerr << "\rLUT386   ";
    Ciphertext gin386 = 2 * gout224 + gout291;
    Ciphertext gout386 = ctx.eval_lut(&gin386, nand2);
    std::cerr << "\rLUT387   ";
    Ciphertext gin387 = 2 * (1 - gout237) + gout342;
    Ciphertext gout387 = ctx.eval_lut(&gin387, and2);
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * ct_y12 + gout342;
    Ciphertext gout388 = ctx.eval_lut(&gin388, nand2);
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout343 + gout314;
    Ciphertext gout389 = ctx.eval_lut(&gin389, and2);
    std::cerr << "\rLUT390   ";
    Ciphertext gin390 = 2 * gout342 + gout251;
    Ciphertext gout390 = ctx.eval_lut(&gin390, and2);
    std::cerr << "\rLUT391   ";
    Ciphertext gin391 = 2 * ct_y14 + gout342;
    Ciphertext gout391 = ctx.eval_lut(&gin391, and2);
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * gout278 + gout350;
    Ciphertext gout392 = ctx.eval_lut(&gin392, nand2);
    std::cerr << "\rLUT393   ";
    Ciphertext gin393 = 2 * gout258 + gout341;
    Ciphertext gout393 = ctx.eval_lut(&gin393, nand2);
    std::cerr << "\rLUT394   ";
    Ciphertext gin394 = 2 * gout323 + gout347;
    Ciphertext gout394 = ctx.eval_lut(&gin394, xnor2);
    std::cerr << "\rLUT395   ";
    Ciphertext gin395 = 2 * gout254 + gout337;
    Ciphertext gout395 = ctx.eval_lut(&gin395, nand2);
    std::cerr << "\rLUT396   ";
    Ciphertext gin396 = 2 * gout268 + gout334;
    Ciphertext gout396 = ctx.eval_lut(&gin396, nand2);
    std::cerr << "\rLUT397   ";
    Ciphertext gin397 = 2 * gout264 + gout333;
    Ciphertext gout397 = ctx.eval_lut(&gin397, nand2);
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout186 + gout336;
    Ciphertext gout398 = ctx.eval_lut(&gin398, nand2);
    std::cerr << "\rLUT399   ";
    Ciphertext gin399 = 2 * gout269 + gout345;
    Ciphertext gout399 = ctx.eval_lut(&gin399, and2);
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * (1 - gout317) + gout342;
    Ciphertext gout400 = ctx.eval_lut(&gin400, and2);
    std::cerr << "\rLUT401   ";
    Ciphertext gin401 = 2 * gout361 + gout352;
    Ciphertext gout401 = ctx.eval_lut(&gin401, xnor2);
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * ct_y0 + gout342;
    Ciphertext gout402 = ctx.eval_lut(&gin402, and2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout172 + gout340;
    Ciphertext gout403 = ctx.eval_lut(&gin403, and2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * (1 - gout363) + gout342;
    Ciphertext gout404 = ctx.eval_lut(&gin404, and2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout351 + gout342;
    Ciphertext gout405 = ctx.eval_lut(&gin405, or2);
    std::cerr << "\rLUT406   ";
    Ciphertext gin406 = 2 * gout172 + gout340;
    Ciphertext gout406 = ctx.eval_lut(&gin406, xnor2);
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * (1 - gout367) + gout346;
    Ciphertext gout407 = ctx.eval_lut(&gin407, and2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout349 + gout295;
    Ciphertext gout408 = ctx.eval_lut(&gin408, xor2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout349 + gout295;
    Ciphertext gout409 = ctx.eval_lut(&gin409, nand2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout323 + gout347;
    Ciphertext gout410 = ctx.eval_lut(&gin410, and2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout367 + gout346;
    Ciphertext gout411 = ctx.eval_lut(&gin411, xnor2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout359 + gout355;
    Ciphertext gout412 = ctx.eval_lut(&gin412, xor2);
    std::cerr << "\rLUT413   ";
    Ciphertext gin413 = 2 * gout86 + gout351;
    Ciphertext gout413 = ctx.eval_lut(&gin413, and2);
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout361 + gout352;
    Ciphertext gout414 = ctx.eval_lut(&gin414, and2);
    std::cerr << "\rINV415   ";
    Ciphertext gout415 = 1 - gout354;

    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout348 + gout236;
    Ciphertext gout416 = ctx.eval_lut(&gin416, xnor2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout359 + gout355;
    Ciphertext gout417 = ctx.eval_lut(&gin417, nand2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout356 + gout281;
    Ciphertext gout418 = ctx.eval_lut(&gin418, xnor2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout356 + gout281;
    Ciphertext gout419 = ctx.eval_lut(&gin419, and2);
    std::cerr << "\rLUT420   ";
    Ciphertext gin420 = 2 * gout358 + gout296;
    Ciphertext gout420 = ctx.eval_lut(&gin420, and2);
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout358 + gout296;
    Ciphertext gout421 = ctx.eval_lut(&gin421, xnor2);
    std::cerr << "\rLUT422   ";
    Ciphertext gin422 = 2 * gout239 + gout360;
    Ciphertext gout422 = ctx.eval_lut(&gin422, xnor2);
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * gout362 + gout285;
    Ciphertext gout423 = ctx.eval_lut(&gin423, xor2);
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * gout418 + gout331;
    Ciphertext gout424 = ctx.eval_lut(&gin424, xnor2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * (1 - gout418) + gout331;
    Ciphertext gout425 = ctx.eval_lut(&gin425, and2);
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout365 + gout287;
    Ciphertext gout426 = ctx.eval_lut(&gin426, xnor2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout372 + gout286;
    Ciphertext gout427 = ctx.eval_lut(&gin427, xor2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout372 + gout286;
    Ciphertext gout428 = ctx.eval_lut(&gin428, nand2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout378 + gout300;
    Ciphertext gout429 = ctx.eval_lut(&gin429, nand2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout32 + gout368;
    Ciphertext gout430 = ctx.eval_lut(&gin430, nand2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout95 + gout360;
    Ciphertext gout431 = ctx.eval_lut(&gin431, nand2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout78 + gout377;
    Ciphertext gout432 = ctx.eval_lut(&gin432, xnor2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout362 + gout285;
    Ciphertext gout433 = ctx.eval_lut(&gin433, nand2);
    std::cerr << "\rLUT434   ";
    Ciphertext gin434 = 2 * gout90 + gout364;
    Ciphertext gout434 = ctx.eval_lut(&gin434, xnor2);
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout38 + gout363;
    Ciphertext gout435 = ctx.eval_lut(&gin435, and2);
    std::cerr << "\rLUT436   ";
    Ciphertext gin436 = 2 * gout365 + gout287;
    Ciphertext gout436 = ctx.eval_lut(&gin436, nand2);
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout375 + gout320;
    Ciphertext gout437 = ctx.eval_lut(&gin437, xnor2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout375 + gout320;
    Ciphertext gout438 = ctx.eval_lut(&gin438, and2);
    std::cerr << "\rLUT439   ";
    Ciphertext gin439 = 2 * gout394 + gout322;
    Ciphertext gout439 = ctx.eval_lut(&gin439, xnor2);
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout396 + gout280;
    Ciphertext gout440 = ctx.eval_lut(&gin440, xnor2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout396 + gout280;
    Ciphertext gout441 = ctx.eval_lut(&gin441, and2);
    std::cerr << "\rLUT442   ";
    Ciphertext gin442 = 2 * gout378 + gout300;
    Ciphertext gout442 = ctx.eval_lut(&gin442, xor2);
    std::cerr << "\rLUT443   ";
    Ciphertext gin443 = 2 * gout183 + gout368;
    Ciphertext gout443 = ctx.eval_lut(&gin443, xnor2);
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout156 + gout387;
    Ciphertext gout444 = ctx.eval_lut(&gin444, xnor2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout397 + gout219;
    Ciphertext gout445 = ctx.eval_lut(&gin445, xnor2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout381 + gout194;
    Ciphertext gout446 = ctx.eval_lut(&gin446, xnor2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout395 + gout292;
    Ciphertext gout447 = ctx.eval_lut(&gin447, xor2);
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * (1 - gout381) + gout194;
    Ciphertext gout448 = ctx.eval_lut(&gin448, and2);
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout374 + gout220;
    Ciphertext gout449 = ctx.eval_lut(&gin449, and2);
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout426 + gout344;
    Ciphertext gout450 = ctx.eval_lut(&gin450, xnor2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * (1 - gout426) + gout344;
    Ciphertext gout451 = ctx.eval_lut(&gin451, and2);
    std::cerr << "\rLUT452   ";
    Ciphertext gin452 = 2 * gout374 + gout220;
    Ciphertext gout452 = ctx.eval_lut(&gin452, xnor2);
    std::cerr << "\rLUT453   ";
    Ciphertext gin453 = 2 * gout385 + gout318;
    Ciphertext gout453 = ctx.eval_lut(&gin453, xnor2);
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout385 + gout318;
    Ciphertext gout454 = ctx.eval_lut(&gin454, and2);
    std::cerr << "\rLUT455   ";
    Ciphertext gin455 = 2 * gout29 + gout390;
    Ciphertext gout455 = ctx.eval_lut(&gin455, xnor2);
    std::cerr << "\rLUT456   ";
    Ciphertext gin456 = 2 * gout392 + gout423;
    Ciphertext gout456 = ctx.eval_lut(&gin456, nand2);
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout379 + gout212;
    Ciphertext gout457 = ctx.eval_lut(&gin457, xor2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout398 + gout324;
    Ciphertext gout458 = ctx.eval_lut(&gin458, nand2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout395 + gout292;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout38 + gout404;
    Ciphertext gout460 = ctx.eval_lut(&gin460, xnor2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout398 + gout324;
    Ciphertext gout461 = ctx.eval_lut(&gin461, xnor2);
    std::cerr << "\rLUT462   ";
    Ciphertext gin462 = 2 * (1 - gout401) + gout339;
    Ciphertext gout462 = ctx.eval_lut(&gin462, and2);
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout370 + gout407;
    Ciphertext gout463 = ctx.eval_lut(&gin463, or2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * (1 - gout394) + gout322;
    Ciphertext gout464 = ctx.eval_lut(&gin464, and2);
    std::cerr << "\rLUT465   ";
    Ciphertext gin465 = 2 * gout11 + gout400;
    Ciphertext gout465 = ctx.eval_lut(&gin465, xnor2);
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout354 + gout422;
    Ciphertext gout466 = ctx.eval_lut(&gin466, and2);
    std::cerr << "\rLUT467   ";
    Ciphertext gin467 = 2 * gout392 + gout423;
    Ciphertext gout467 = ctx.eval_lut(&gin467, xor2);
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout170 + gout402;
    Ciphertext gout468 = ctx.eval_lut(&gin468, xnor2);
    std::cerr << "\rLUT469   ";
    Ciphertext gin469 = 2 * gout330 + gout408;
    Ciphertext gout469 = ctx.eval_lut(&gin469, xor2);
    std::cerr << "\rLUT470   ";
    Ciphertext gin470 = 2 * gout393 + gout312;
    Ciphertext gout470 = ctx.eval_lut(&gin470, xor2);
    std::cerr << "\rLUT471   ";
    Ciphertext gin471 = 2 * gout414 + gout462;
    Ciphertext gout471 = ctx.eval_lut(&gin471, or2);
    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout330 + gout408;
    Ciphertext gout472 = ctx.eval_lut(&gin472, nand2);
    std::cerr << "\rLUT473   ";
    Ciphertext gin473 = 2 * gout393 + gout312;
    Ciphertext gout473 = ctx.eval_lut(&gin473, nand2);
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout397 + gout219;
    Ciphertext gout474 = ctx.eval_lut(&gin474, and2);
    std::cerr << "\rINV475   ";
    Ciphertext gout475 = 1 - gout413;

    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout379 + gout212;
    Ciphertext gout476 = ctx.eval_lut(&gin476, and2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout386 + gout412;
    Ciphertext gout477 = ctx.eval_lut(&gin477, xor2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout401 + gout339;
    Ciphertext gout478 = ctx.eval_lut(&gin478, xnor2);
    std::cerr << "\rLUT479   ";
    Ciphertext gin479 = 2 * gout399 + gout283;
    Ciphertext gout479 = ctx.eval_lut(&gin479, xnor2);
    std::cerr << "\rLUT480   ";
    Ciphertext gin480 = 2 * gout413 + gout112;
    Ciphertext gout480 = ctx.eval_lut(&gin480, or2);
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout413 + gout47;
    Ciphertext gout481 = ctx.eval_lut(&gin481, nand2);
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout413 + gout248;
    Ciphertext gout482 = ctx.eval_lut(&gin482, nand2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout415 + gout422;
    Ciphertext gout483 = ctx.eval_lut(&gin483, xnor2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout386 + gout412;
    Ciphertext gout484 = ctx.eval_lut(&gin484, nand2);
    std::cerr << "\rLUT485   ";
    Ciphertext gin485 = 2 * gout421 + gout301;
    Ciphertext gout485 = ctx.eval_lut(&gin485, xnor2);
    std::cerr << "\rLUT486   ";
    Ciphertext gin486 = 2 * (1 - gout421) + gout301;
    Ciphertext gout486 = ctx.eval_lut(&gin486, and2);
    std::cerr << "\rLUT487   ";
    Ciphertext gin487 = 2 * gout357 + gout427;
    Ciphertext gout487 = ctx.eval_lut(&gin487, xor2);
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * gout373 + gout431;
    Ciphertext gout488 = ctx.eval_lut(&gin488, nand2);
    std::cerr << "\rLUT489   ";
    Ciphertext gin489 = 2 * gout357 + gout427;
    Ciphertext gout489 = ctx.eval_lut(&gin489, nand2);
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout369 + gout430;
    Ciphertext gout490 = ctx.eval_lut(&gin490, nand2);
    std::cerr << "\rLUT491   ";
    Ciphertext gin491 = 2 * gout445 + gout246;
    Ciphertext gout491 = ctx.eval_lut(&gin491, xnor2);
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout376 + gout432;
    Ciphertext gout492 = ctx.eval_lut(&gin492, xnor2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout419 + gout425;
    Ciphertext gout493 = ctx.eval_lut(&gin493, or2);
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * (1 - gout435) + gout342;
    Ciphertext gout494 = ctx.eval_lut(&gin494, and2);
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * gout35 + gout435;
    Ciphertext gout495 = ctx.eval_lut(&gin495, and2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * ct_x3 + gout444;
    Ciphertext gout496 = ctx.eval_lut(&gin496, nand2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout389 + gout448;
    Ciphertext gout497 = ctx.eval_lut(&gin497, or2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout380 + gout442;
    Ciphertext gout498 = ctx.eval_lut(&gin498, xor2);
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * gout488 + gout446;
    Ciphertext gout499 = ctx.eval_lut(&gin499, xor2);
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * gout198 + gout447;
    Ciphertext gout500 = ctx.eval_lut(&gin500, nand2);
    std::cerr << "\rLUT501   ";
    Ciphertext gin501 = 2 * gout488 + gout446;
    Ciphertext gout501 = ctx.eval_lut(&gin501, and2);
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * (1 - gout437) + gout272;
    Ciphertext gout502 = ctx.eval_lut(&gin502, and2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout452 + gout265;
    Ciphertext gout503 = ctx.eval_lut(&gin503, xnor2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * (1 - gout445) + gout246;
    Ciphertext gout504 = ctx.eval_lut(&gin504, and2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * (1 - gout452) + gout265;
    Ciphertext gout505 = ctx.eval_lut(&gin505, and2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * gout436 + (1 - gout451);
    Ciphertext gout506 = ctx.eval_lut(&gin506, and2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout19 + gout444;
    Ciphertext gout507 = ctx.eval_lut(&gin507, xnor2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout437 + gout272;
    Ciphertext gout508 = ctx.eval_lut(&gin508, xnor2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout198 + gout447;
    Ciphertext gout509 = ctx.eval_lut(&gin509, xor2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * ct_x2 + gout455;
    Ciphertext gout510 = ctx.eval_lut(&gin510, nand2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * (1 - gout453) + gout304;
    Ciphertext gout511 = ctx.eval_lut(&gin511, and2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout474 + gout504;
    Ciphertext gout512 = ctx.eval_lut(&gin512, or2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * gout60 + gout465;
    Ciphertext gout513 = ctx.eval_lut(&gin513, xnor2);
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * gout453 + gout304;
    Ciphertext gout514 = ctx.eval_lut(&gin514, xnor2);
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout440 + gout305;
    Ciphertext gout515 = ctx.eval_lut(&gin515, xnor2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * (1 - gout440) + gout305;
    Ciphertext gout516 = ctx.eval_lut(&gin516, and2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout380 + gout442;
    Ciphertext gout517 = ctx.eval_lut(&gin517, nand2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout103 + gout455;
    Ciphertext gout518 = ctx.eval_lut(&gin518, xnor2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout459 + gout500;
    Ciphertext gout519 = ctx.eval_lut(&gin519, nand2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * gout409 + gout472;
    Ciphertext gout520 = ctx.eval_lut(&gin520, nand2);
    std::cerr << "\rLUT521   ";
    Ciphertext gin521 = 2 * ct_x1 + gout468;
    Ciphertext gout521 = ctx.eval_lut(&gin521, nand2);
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * gout461 + gout229;
    Ciphertext gout522 = ctx.eval_lut(&gin522, xnor2);
    std::cerr << "\rINV523   ";
    Ciphertext gout523 = 1 - gout466;

    std::cerr << "\rLUT524   ";
    Ciphertext gin524 = 2 * gout466 + gout499;
    Ciphertext gout524 = ctx.eval_lut(&gin524, and2);
    std::cerr << "\rLUT525   ";
    Ciphertext gin525 = 2 * (1 - gout461) + gout229;
    Ciphertext gout525 = ctx.eval_lut(&gin525, and2);
    std::cerr << "\rLUT526   ";
    Ciphertext gin526 = 2 * gout315 + gout470;
    Ciphertext gout526 = ctx.eval_lut(&gin526, xor2);
    std::cerr << "\rLUT527   ";
    Ciphertext gin527 = 2 * gout410 + gout464;
    Ciphertext gout527 = ctx.eval_lut(&gin527, or2);
    std::cerr << "\rLUT528   ";
    Ciphertext gin528 = 2 * gout433 + gout456;
    Ciphertext gout528 = ctx.eval_lut(&gin528, nand2);
    std::cerr << "\rLUT529   ";
    Ciphertext gin529 = 2 * gout315 + gout470;
    Ciphertext gout529 = ctx.eval_lut(&gin529, nand2);
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * ct_x5 + gout460;
    Ciphertext gout530 = ctx.eval_lut(&gin530, nand2);
    std::cerr << "\rLUT531   ";
    Ciphertext gin531 = 2 * ct_x4 + gout465;
    Ciphertext gout531 = ctx.eval_lut(&gin531, nand2);
    std::cerr << "\rLUT532   ";
    Ciphertext gin532 = 2 * gout53 + gout460;
    Ciphertext gout532 = ctx.eval_lut(&gin532, xnor2);
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout434 + gout479;
    Ciphertext gout533 = ctx.eval_lut(&gin533, xnor2);
    std::cerr << "\rLUT534   ";
    Ciphertext gin534 = 2 * gout405 + gout482;
    Ciphertext gout534 = ctx.eval_lut(&gin534, and2);
    std::cerr << "\rLUT535   ";
    Ciphertext gin535 = 2 * gout108 + gout468;
    Ciphertext gout535 = ctx.eval_lut(&gin535, xnor2);
    std::cerr << "\rLUT536   ";
    Ciphertext gin536 = 2 * gout403 + gout483;
    Ciphertext gout536 = ctx.eval_lut(&gin536, nand2);
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * gout463 + gout478;
    Ciphertext gout537 = ctx.eval_lut(&gin537, xnor2);
    std::cerr << "\rLUT538   ";
    Ciphertext gin538 = 2 * gout463 + gout478;
    Ciphertext gout538 = ctx.eval_lut(&gin538, and2);
    std::cerr << "\rLUT539   ";
    Ciphertext gin539 = 2 * gout405 + gout480;
    Ciphertext gout539 = ctx.eval_lut(&gin539, and2);
    std::cerr << "\rLUT540   ";
    Ciphertext gin540 = 2 * gout417 + gout484;
    Ciphertext gout540 = ctx.eval_lut(&gin540, nand2);
    std::cerr << "\rLUT541   ";
    Ciphertext gin541 = 2 * gout403 + gout483;
    Ciphertext gout541 = ctx.eval_lut(&gin541, xnor2);
    std::cerr << "\rLUT542   ";
    Ciphertext gin542 = 2 * gout420 + gout486;
    Ciphertext gout542 = ctx.eval_lut(&gin542, or2);
    std::cerr << "\rLUT543   ";
    Ciphertext gin543 = 2 * gout493 + gout485;
    Ciphertext gout543 = ctx.eval_lut(&gin543, xnor2);
    std::cerr << "\rLUT544   ";
    Ciphertext gin544 = 2 * gout493 + gout485;
    Ciphertext gout544 = ctx.eval_lut(&gin544, and2);
    std::cerr << "\rLUT545   ";
    Ciphertext gin545 = 2 * gout23 + gout495;
    Ciphertext gout545 = ctx.eval_lut(&gin545, and2);
    std::cerr << "\rLUT546   ";
    Ciphertext gin546 = 2 * gout449 + gout505;
    Ciphertext gout546 = ctx.eval_lut(&gin546, or2);
    std::cerr << "\rLUT547   ";
    Ciphertext gin547 = 2 * gout35 + gout494;
    Ciphertext gout547 = ctx.eval_lut(&gin547, xnor2);
    std::cerr << "\rLUT548   ";
    Ciphertext gin548 = 2 * gout428 + gout489;
    Ciphertext gout548 = ctx.eval_lut(&gin548, nand2);
    std::cerr << "\rLUT549   ";
    Ciphertext gin549 = 2 * (1 - gout495) + gout342;
    Ciphertext gout549 = ctx.eval_lut(&gin549, and2);
    std::cerr << "\rLUT550   ";
    Ciphertext gin550 = 2 * gout543 + gout467;
    Ciphertext gout550 = ctx.eval_lut(&gin550, xnor2);
    std::cerr << "\rLUT551   ";
    Ciphertext gin551 = 2 * gout506 + gout382;
    Ciphertext gout551 = ctx.eval_lut(&gin551, xnor2);
    std::cerr << "\rLUT552   ";
    Ciphertext gin552 = 2 * gout542 + gout491;
    Ciphertext gout552 = ctx.eval_lut(&gin552, and2);
    std::cerr << "\rLUT553   ";
    Ciphertext gin553 = 2 * gout497 + gout503;
    Ciphertext gout553 = ctx.eval_lut(&gin553, nand2);
    std::cerr << "\rLUT554   ";
    Ciphertext gin554 = 2 * gout438 + gout502;
    Ciphertext gout554 = ctx.eval_lut(&gin554, or2);
    std::cerr << "\rLUT555   ";
    Ciphertext gin555 = 2 * gout429 + gout517;
    Ciphertext gout555 = ctx.eval_lut(&gin555, and2);
    std::cerr << "\rLUT556   ";
    Ciphertext gin556 = 2 * gout542 + gout491;
    Ciphertext gout556 = ctx.eval_lut(&gin556, xnor2);
    std::cerr << "\rLUT557   ";
    Ciphertext gin557 = 2 * gout501 + gout524;
    Ciphertext gout557 = ctx.eval_lut(&gin557, or2);
    std::cerr << "\rLUT558   ";
    Ciphertext gin558 = 2 * gout527 + gout514;
    Ciphertext gout558 = ctx.eval_lut(&gin558, xnor2);
    std::cerr << "\rLUT559   ";
    Ciphertext gin559 = 2 * gout512 + gout439;
    Ciphertext gout559 = ctx.eval_lut(&gin559, and2);
    std::cerr << "\rLUT560   ";
    Ciphertext gin560 = 2 * gout471 + gout515;
    Ciphertext gout560 = ctx.eval_lut(&gin560, xnor2);
    std::cerr << "\rLUT561   ";
    Ciphertext gin561 = 2 * gout471 + gout515;
    Ciphertext gout561 = ctx.eval_lut(&gin561, and2);
    std::cerr << "\rLUT562   ";
    Ciphertext gin562 = 2 * gout519 + gout115;
    Ciphertext gout562 = ctx.eval_lut(&gin562, and2);
    std::cerr << "\rLUT563   ";
    Ciphertext gin563 = 2 * gout497 + gout503;
    Ciphertext gout563 = ctx.eval_lut(&gin563, xor2);
    std::cerr << "\rLUT564   ";
    Ciphertext gin564 = 2 * gout528 + gout416;
    Ciphertext gout564 = ctx.eval_lut(&gin564, and2);
    std::cerr << "\rLUT565   ";
    Ciphertext gin565 = 2 * gout473 + gout529;
    Ciphertext gout565 = ctx.eval_lut(&gin565, nand2);
    std::cerr << "\rLUT566   ";
    Ciphertext gin566 = 2 * (1 - gout543) + gout467;
    Ciphertext gout566 = ctx.eval_lut(&gin566, and2);
    std::cerr << "\rLUT567   ";
    Ciphertext gin567 = 2 * gout454 + gout511;
    Ciphertext gout567 = ctx.eval_lut(&gin567, or2);
    std::cerr << "\rLUT568   ";
    Ciphertext gin568 = 2 * gout512 + gout439;
    Ciphertext gout568 = ctx.eval_lut(&gin568, xnor2);
    std::cerr << "\rLUT569   ";
    Ciphertext gin569 = 2 * gout298 + gout533;
    Ciphertext gout569 = ctx.eval_lut(&gin569, xnor2);
    std::cerr << "\rLUT570   ";
    Ciphertext gin570 = 2 * gout441 + gout516;
    Ciphertext gout570 = ctx.eval_lut(&gin570, or2);
    std::cerr << "\rLUT571   ";
    Ciphertext gin571 = 2 * gout527 + gout514;
    Ciphertext gout571 = ctx.eval_lut(&gin571, and2);
    std::cerr << "\rLUT572   ";
    Ciphertext gin572 = 2 * gout523 + gout536;
    Ciphertext gout572 = ctx.eval_lut(&gin572, nand2);
    std::cerr << "\rLUT573   ";
    Ciphertext gin573 = 2 * gout187 + gout535;
    Ciphertext gout573 = ctx.eval_lut(&gin573, xnor2);
    std::cerr << "\rLUT574   ";
    Ciphertext gin574 = 2 * gout528 + gout416;
    Ciphertext gout574 = ctx.eval_lut(&gin574, xor2);
    std::cerr << "\rLUT575   ";
    Ciphertext gin575 = 2 * gout519 + gout115;
    Ciphertext gout575 = ctx.eval_lut(&gin575, xor2);
    std::cerr << "\rLUT576   ";
    Ciphertext gin576 = 2 * (1 - gout536) + gout499;
    Ciphertext gout576 = ctx.eval_lut(&gin576, and2);
    std::cerr << "\rLUT577   ";
    Ciphertext gin577 = 2 * gout520 + gout508;
    Ciphertext gout577 = ctx.eval_lut(&gin577, nand2);
    std::cerr << "\rLUT578   ";
    Ciphertext gin578 = 2 * gout458 + (1 - gout525);
    Ciphertext gout578 = ctx.eval_lut(&gin578, and2);
    std::cerr << "\rLUT579   ";
    Ciphertext gin579 = 2 * gout520 + gout508;
    Ciphertext gout579 = ctx.eval_lut(&gin579, xor2);
    std::cerr << "\rLUT580   ";
    Ciphertext gin580 = 2 * gout540 + gout260;
    Ciphertext gout580 = ctx.eval_lut(&gin580, and2);
    std::cerr << "\rLUT581   ";
    Ciphertext gin581 = 2 * gout47 + gout535;
    Ciphertext gout581 = ctx.eval_lut(&gin581, nand2);
    std::cerr << "\rLUT582   ";
    Ciphertext gin582 = 2 * (1 - gout537) + gout457;
    Ciphertext gout582 = ctx.eval_lut(&gin582, and2);
    std::cerr << "\rLUT583   ";
    Ciphertext gin583 = 2 * gout481 + gout539;
    Ciphertext gout583 = ctx.eval_lut(&gin583, nand2);
    std::cerr << "\rLUT584   ";
    Ciphertext gin584 = 2 * gout540 + gout260;
    Ciphertext gout584 = ctx.eval_lut(&gin584, xor2);
    std::cerr << "\rLUT585   ";
    Ciphertext gin585 = 2 * gout537 + gout457;
    Ciphertext gout585 = ctx.eval_lut(&gin585, xnor2);
    std::cerr << "\rLUT586   ";
    Ciphertext gin586 = 2 * (1 - gout545) + gout342;
    Ciphertext gout586 = ctx.eval_lut(&gin586, and2);
    std::cerr << "\rLUT587   ";
    Ciphertext gin587 = 2 * gout135 + gout547;
    Ciphertext gout587 = ctx.eval_lut(&gin587, xnor2);
    std::cerr << "\rLUT588   ";
    Ciphertext gin588 = 2 * gout548 + gout443;
    Ciphertext gout588 = ctx.eval_lut(&gin588, nand2);
    std::cerr << "\rLUT589   ";
    Ciphertext gin589 = 2 * (1 - gout556) + gout487;
    Ciphertext gout589 = ctx.eval_lut(&gin589, and2);
    std::cerr << "\rLUT590   ";
    Ciphertext gin590 = 2 * gout556 + gout487;
    Ciphertext gout590 = ctx.eval_lut(&gin590, xnor2);
    std::cerr << "\rLUT591   ";
    Ciphertext gin591 = 2 * gout546 + gout411;
    Ciphertext gout591 = ctx.eval_lut(&gin591, and2);
    std::cerr << "\rLUT592   ";
    Ciphertext gin592 = 2 * gout102 + gout545;
    Ciphertext gout592 = ctx.eval_lut(&gin592, and2);
    std::cerr << "\rLUT593   ";
    Ciphertext gin593 = 2 * gout555 + gout492;
    Ciphertext gout593 = ctx.eval_lut(&gin593, xnor2);
    std::cerr << "\rLUT594   ";
    Ciphertext gin594 = 2 * gout197 + gout563;
    Ciphertext gout594 = ctx.eval_lut(&gin594, nand2);
    std::cerr << "\rLUT595   ";
    Ciphertext gin595 = 2 * ct_x6 + gout547;
    Ciphertext gout595 = ctx.eval_lut(&gin595, nand2);
    std::cerr << "\rLUT596   ";
    Ciphertext gin596 = 2 * gout560 + gout509;
    Ciphertext gout596 = ctx.eval_lut(&gin596, xnor2);
    std::cerr << "\rLUT597   ";
    Ciphertext gin597 = 2 * (1 - gout558) + gout526;
    Ciphertext gout597 = ctx.eval_lut(&gin597, and2);
    std::cerr << "\rLUT598   ";
    Ciphertext gin598 = 2 * gout308 + gout563;
    Ciphertext gout598 = ctx.eval_lut(&gin598, xnor2);
    std::cerr << "\rLUT599   ";
    Ciphertext gin599 = 2 * gout546 + gout411;
    Ciphertext gout599 = ctx.eval_lut(&gin599, xnor2);
    std::cerr << "\rLUT600   ";
    Ciphertext gin600 = 2 * gout558 + gout526;
    Ciphertext gout600 = ctx.eval_lut(&gin600, xnor2);
    std::cerr << "\rLUT601   ";
    Ciphertext gin601 = 2 * gout548 + gout443;
    Ciphertext gout601 = ctx.eval_lut(&gin601, xor2);
    std::cerr << "\rLUT602   ";
    Ciphertext gin602 = 2 * gout23 + gout549;
    Ciphertext gout602 = ctx.eval_lut(&gin602, xnor2);
    std::cerr << "\rLUT603   ";
    Ciphertext gin603 = 2 * gout567 + gout450;
    Ciphertext gout603 = ctx.eval_lut(&gin603, and2);
    std::cerr << "\rLUT604   ";
    Ciphertext gin604 = 2 * gout567 + gout450;
    Ciphertext gout604 = ctx.eval_lut(&gin604, xnor2);
    std::cerr << "\rLUT605   ";
    Ciphertext gin605 = 2 * gout570 + gout424;
    Ciphertext gout605 = ctx.eval_lut(&gin605, and2);
    std::cerr << "\rLUT606   ";
    Ciphertext gin606 = 2 * gout571 + gout597;
    Ciphertext gout606 = ctx.eval_lut(&gin606, or2);
    std::cerr << "\rLUT607   ";
    Ciphertext gin607 = 2 * (1 - gout560) + gout509;
    Ciphertext gout607 = ctx.eval_lut(&gin607, and2);
    std::cerr << "\rLUT608   ";
    Ciphertext gin608 = 2 * gout565 + gout522;
    Ciphertext gout608 = ctx.eval_lut(&gin608, nand2);
    std::cerr << "\rLUT609   ";
    Ciphertext gin609 = 2 * gout544 + gout566;
    Ciphertext gout609 = ctx.eval_lut(&gin609, or2);
    std::cerr << "\rLUT610   ";
    Ciphertext gin610 = 2 * gout593 + gout578;
    Ciphertext gout610 = ctx.eval_lut(&gin610, xnor2);
    std::cerr << "\rLUT611   ";
    Ciphertext gin611 = 2 * gout551 + gout569;
    Ciphertext gout611 = ctx.eval_lut(&gin611, xnor2);
    std::cerr << "\rLUT612   ";
    Ciphertext gin612 = 2 * gout475 + gout573;
    Ciphertext gout612 = ctx.eval_lut(&gin612, nand2);
    std::cerr << "\rLUT613   ";
    Ciphertext gin613 = 2 * gout568 + gout469;
    Ciphertext gout613 = ctx.eval_lut(&gin613, xnor2);
    std::cerr << "\rLUT614   ";
    Ciphertext gin614 = 2 * gout565 + gout522;
    Ciphertext gout614 = ctx.eval_lut(&gin614, xor2);
    std::cerr << "\rLUT615   ";
    Ciphertext gin615 = 2 * (1 - gout568) + gout469;
    Ciphertext gout615 = ctx.eval_lut(&gin615, and2);
    std::cerr << "\rLUT616   ";
    Ciphertext gin616 = 2 * gout490 + gout579;
    Ciphertext gout616 = ctx.eval_lut(&gin616, nand2);
    std::cerr << "\rLUT617   ";
    Ciphertext gin617 = 2 * gout570 + gout424;
    Ciphertext gout617 = ctx.eval_lut(&gin617, xnor2);
    std::cerr << "\rLUT618   ";
    Ciphertext gin618 = 2 * gout490 + gout579;
    Ciphertext gout618 = ctx.eval_lut(&gin618, xor2);
    std::cerr << "\rLUT619   ";
    Ciphertext gin619 = 2 * gout499 + gout572;
    Ciphertext gout619 = ctx.eval_lut(&gin619, xnor2);
    std::cerr << "\rLUT620   ";
    Ciphertext gin620 = 2 * gout521 + gout581;
    Ciphertext gout620 = ctx.eval_lut(&gin620, nand2);
    std::cerr << "\rLUT621   ";
    Ciphertext gin621 = 2 * gout538 + gout582;
    Ciphertext gout621 = ctx.eval_lut(&gin621, or2);
    std::cerr << "\rLUT622   ";
    Ciphertext gin622 = 2 * gout102 + gout586;
    Ciphertext gout622 = ctx.eval_lut(&gin622, xnor2);
    std::cerr << "\rLUT623   ";
    Ciphertext gin623 = 2 * gout552 + gout589;
    Ciphertext gout623 = ctx.eval_lut(&gin623, or2);
    std::cerr << "\rLUT624   ";
    Ciphertext gin624 = 2 * gout311 + gout601;
    Ciphertext gout624 = ctx.eval_lut(&gin624, nand2);
    std::cerr << "\rLUT625   ";
    Ciphertext gin625 = 2 * gout598 + gout557;
    Ciphertext gout625 = ctx.eval_lut(&gin625, xor2);
    std::cerr << "\rLUT626   ";
    Ciphertext gin626 = 2 * gout501 + gout598;
    Ciphertext gout626 = ctx.eval_lut(&gin626, and2);
    std::cerr << "\rLUT627   ";
    Ciphertext gin627 = 2 * gout553 + gout594;
    Ciphertext gout627 = ctx.eval_lut(&gin627, nand2);
    std::cerr << "\rLUT628   ";
    Ciphertext gin628 = 2 * gout599 + gout371;
    Ciphertext gout628 = ctx.eval_lut(&gin628, xnor2);
    std::cerr << "\rLUT629   ";
    Ciphertext gin629 = 2 * ct_x7 + gout602;
    Ciphertext gout629 = ctx.eval_lut(&gin629, nand2);
    std::cerr << "\rLUT630   ";
    Ciphertext gin630 = 2 * gout561 + gout607;
    Ciphertext gout630 = ctx.eval_lut(&gin630, or2);
    std::cerr << "\rLUT631   ";
    Ciphertext gin631 = 2 * (1 - gout604) + gout498;
    Ciphertext gout631 = ctx.eval_lut(&gin631, and2);
    std::cerr << "\rLUT632   ";
    Ciphertext gin632 = 2 * gout576 + gout625;
    Ciphertext gout632 = ctx.eval_lut(&gin632, xnor2);
    std::cerr << "\rLUT633   ";
    Ciphertext gin633 = 2 * gout125 + gout592;
    Ciphertext gout633 = ctx.eval_lut(&gin633, and2);
    std::cerr << "\rLUT634   ";
    Ciphertext gin634 = 2 * gout576 + gout625;
    Ciphertext gout634 = ctx.eval_lut(&gin634, nand2);
    std::cerr << "\rLUT635   ";
    Ciphertext gin635 = 2 * (1 - gout592) + gout342;
    Ciphertext gout635 = ctx.eval_lut(&gin635, and2);
    std::cerr << "\rLUT636   ";
    Ciphertext gin636 = 2 * (1 - gout599) + gout371;
    Ciphertext gout636 = ctx.eval_lut(&gin636, and2);
    std::cerr << "\rLUT637   ";
    Ciphertext gin637 = 2 * gout554 + gout614;
    Ciphertext gout637 = ctx.eval_lut(&gin637, nand2);
    std::cerr << "\rLUT638   ";
    Ciphertext gin638 = 2 * gout524 + gout598;
    Ciphertext gout638 = ctx.eval_lut(&gin638, and2);
    std::cerr << "\rLUT639   ";
    Ciphertext gin639 = 2 * gout123 + gout602;
    Ciphertext gout639 = ctx.eval_lut(&gin639, xnor2);
    std::cerr << "\rLUT640   ";
    Ciphertext gin640 = 2 * gout311 + gout601;
    Ciphertext gout640 = ctx.eval_lut(&gin640, xor2);
    std::cerr << "\rLUT641   ";
    Ciphertext gin641 = 2 * gout609 + gout590;
    Ciphertext gout641 = ctx.eval_lut(&gin641, and2);
    std::cerr << "\rLUT642   ";
    Ciphertext gin642 = 2 * gout577 + gout616;
    Ciphertext gout642 = ctx.eval_lut(&gin642, nand2);
    std::cerr << "\rLUT643   ";
    Ciphertext gin643 = 2 * gout604 + gout498;
    Ciphertext gout643 = ctx.eval_lut(&gin643, xnor2);
    std::cerr << "\rLUT644   ";
    Ciphertext gin644 = 2 * gout612 + gout534;
    Ciphertext gout644 = ctx.eval_lut(&gin644, nand2);
    std::cerr << "\rLUT645   ";
    Ciphertext gin645 = 2 * gout609 + gout590;
    Ciphertext gout645 = ctx.eval_lut(&gin645, xnor2);
    std::cerr << "\rLUT646   ";
    Ciphertext gin646 = 2 * gout623 + gout613;
    Ciphertext gout646 = ctx.eval_lut(&gin646, xnor2);
    std::cerr << "\rLUT647   ";
    Ciphertext gin647 = 2 * (1 - gout617) + gout477;
    Ciphertext gout647 = ctx.eval_lut(&gin647, and2);
    std::cerr << "\rLUT648   ";
    Ciphertext gin648 = 2 * gout623 + gout613;
    Ciphertext gout648 = ctx.eval_lut(&gin648, and2);
    std::cerr << "\rLUT649   ";
    Ciphertext gin649 = 2 * gout610 + gout611;
    Ciphertext gout649 = ctx.eval_lut(&gin649, xnor2);
    std::cerr << "\rLUT650   ";
    Ciphertext gin650 = 2 * gout620 + gout518;
    Ciphertext gout650 = ctx.eval_lut(&gin650, xnor2);
    std::cerr << "\rLUT651   ";
    Ciphertext gin651 = 2 * gout559 + gout615;
    Ciphertext gout651 = ctx.eval_lut(&gin651, or2);
    std::cerr << "\rLUT652   ";
    Ciphertext gin652 = 2 * gout617 + gout477;
    Ciphertext gout652 = ctx.eval_lut(&gin652, xnor2);
    std::cerr << "\rLUT653   ";
    Ciphertext gin653 = 2 * gout621 + gout596;
    Ciphertext gout653 = ctx.eval_lut(&gin653, nand2);
    std::cerr << "\rLUT654   ";
    Ciphertext gin654 = 2 * gout554 + gout614;
    Ciphertext gout654 = ctx.eval_lut(&gin654, xor2);
    std::cerr << "\rLUT655   ";
    Ciphertext gin655 = 2 * gout621 + gout596;
    Ciphertext gout655 = ctx.eval_lut(&gin655, xor2);
    std::cerr << "\rLUT656   ";
    Ciphertext gin656 = 2 * gout620 + gout518;
    Ciphertext gout656 = ctx.eval_lut(&gin656, nand2);
    std::cerr << "\rLUT657   ";
    Ciphertext gin657 = 2 * ct_x8 + gout622;
    Ciphertext gout657 = ctx.eval_lut(&gin657, nand2);
    std::cerr << "\rLUT658   ";
    Ciphertext gin658 = 2 * gout36 + gout622;
    Ciphertext gout658 = ctx.eval_lut(&gin658, xnor2);
    std::cerr << "\rLUT659   ";
    Ciphertext gin659 = 2 * gout588 + gout624;
    Ciphertext gout659 = ctx.eval_lut(&gin659, nand2);
    std::cerr << "\rLUT660   ";
    Ciphertext gin660 = 2 * gout627 + gout628;
    Ciphertext gout660 = ctx.eval_lut(&gin660, and2);
    std::cerr << "\rLUT661   ";
    Ciphertext gin661 = 2 * gout603 + gout631;
    Ciphertext gout661 = ctx.eval_lut(&gin661, or2);
    std::cerr << "\rINV662   ";
    Ciphertext gout662 = 1 - gout628;

    std::cerr << "\rINV663   ";
    Ciphertext gout663 = 1 - gout634;

    std::cerr << "\rLUT664   ";
    Ciphertext gin664 = 2 * gout591 + gout636;
    Ciphertext gout664 = ctx.eval_lut(&gin664, or2);
    std::cerr << "\rINV665   ";
    Ciphertext gout665 = 1 - gout626;

    std::cerr << "\rINV666   ";
    Ciphertext gout666 = 1 - gout638;

    std::cerr << "\rLUT667   ";
    Ciphertext gin667 = 2 * gout606 + gout643;
    Ciphertext gout667 = ctx.eval_lut(&gin667, and2);
    std::cerr << "\rLUT668   ";
    Ciphertext gin668 = 2 * gout646 + gout640;
    Ciphertext gout668 = ctx.eval_lut(&gin668, xnor2);
    std::cerr << "\rLUT669   ";
    Ciphertext gin669 = 2 * (1 - gout646) + gout640;
    Ciphertext gout669 = ctx.eval_lut(&gin669, and2);
    std::cerr << "\rLUT670   ";
    Ciphertext gin670 = 2 * (1 - gout633) + gout342;
    Ciphertext gout670 = ctx.eval_lut(&gin670, and2);
    std::cerr << "\rLUT671   ";
    Ciphertext gin671 = 2 * gout606 + gout643;
    Ciphertext gout671 = ctx.eval_lut(&gin671, xnor2);
    std::cerr << "\rLUT672   ";
    Ciphertext gin672 = 2 * (1 - gout645) + gout574;
    Ciphertext gout672 = ctx.eval_lut(&gin672, and2);
    std::cerr << "\rLUT673   ";
    Ciphertext gin673 = 2 * gout116 + gout633;
    Ciphertext gout673 = ctx.eval_lut(&gin673, nand2);
    std::cerr << "\rLUT674   ";
    Ciphertext gin674 = 2 * gout125 + gout635;
    Ciphertext gout674 = ctx.eval_lut(&gin674, xnor2);
    std::cerr << "\rLUT675   ";
    Ciphertext gin675 = 2 * gout608 + gout637;
    Ciphertext gout675 = ctx.eval_lut(&gin675, nand2);
    std::cerr << "\rLUT676   ";
    Ciphertext gin676 = 2 * gout651 + gout600;
    Ciphertext gout676 = ctx.eval_lut(&gin676, and2);
    std::cerr << "\rLUT677   ";
    Ciphertext gin677 = 2 * gout664 + gout585;
    Ciphertext gout677 = ctx.eval_lut(&gin677, xor2);
    std::cerr << "\rLUT678   ";
    Ciphertext gin678 = 2 * gout664 + gout585;
    Ciphertext gout678 = ctx.eval_lut(&gin678, nand2);
    std::cerr << "\rLUT679   ";
    Ciphertext gin679 = 2 * gout651 + gout600;
    Ciphertext gout679 = ctx.eval_lut(&gin679, xnor2);
    std::cerr << "\rIMUX680  ";
    Ciphertext gin680A = 2 * gout406 + gout413;
    Ciphertext gout680A = ctx.eval_lut(&gin680A, and2);
    Ciphertext gin680B = 2 * gout650 + (1 - gout413);
    Ciphertext gout680B = ctx.eval_lut(&gin680B, and2);
    Ciphertext gout680 = gout680A + gout680B;
    std::cerr << "\rLUT681   ";
    Ciphertext gin681 = 2 * gout645 + gout574;
    Ciphertext gout681 = ctx.eval_lut(&gin681, xnor2);
    std::cerr << "\rLUT682   ";
    Ciphertext gin682 = 2 * gout180 + gout642;
    Ciphertext gout682 = ctx.eval_lut(&gin682, and2);
    std::cerr << "\rLUT683   ";
    Ciphertext gin683 = 2 * gout605 + gout647;
    Ciphertext gout683 = ctx.eval_lut(&gin683, or2);
    std::cerr << "\rLUT684   ";
    Ciphertext gin684 = 2 * gout180 + gout642;
    Ciphertext gout684 = ctx.eval_lut(&gin684, xor2);
    std::cerr << "\rLUT685   ";
    Ciphertext gin685 = 2 * gout630 + gout652;
    Ciphertext gout685 = ctx.eval_lut(&gin685, xnor2);
    std::cerr << "\rLUT686   ";
    Ciphertext gin686 = 2 * gout671 + gout654;
    Ciphertext gout686 = ctx.eval_lut(&gin686, xnor2);
    std::cerr << "\rLUT687   ";
    Ciphertext gin687 = 2 * (1 - gout671) + gout654;
    Ciphertext gout687 = ctx.eval_lut(&gin687, and2);
    std::cerr << "\rLUT688   ";
    Ciphertext gin688 = 2 * gout661 + gout649;
    Ciphertext gout688 = ctx.eval_lut(&gin688, xnor2);
    std::cerr << "\rLUT689   ";
    Ciphertext gin689 = 2 * gout630 + gout652;
    Ciphertext gout689 = ctx.eval_lut(&gin689, and2);
    std::cerr << "\rLUT690   ";
    Ciphertext gin690 = 2 * gout510 + gout656;
    Ciphertext gout690 = ctx.eval_lut(&gin690, nand2);
    std::cerr << "\rLUT691   ";
    Ciphertext gin691 = 2 * gout476 + gout655;
    Ciphertext gout691 = ctx.eval_lut(&gin691, xor2);
    std::cerr << "\rLUT692   ";
    Ciphertext gin692 = 2 * gout476 + gout655;
    Ciphertext gout692 = ctx.eval_lut(&gin692, nand2);
    std::cerr << "\rINV693   ";
    Ciphertext gout693 = 1 - gout660;

    std::cerr << "\rLUT694   ";
    Ciphertext gin694 = 2 * gout627 + gout662;
    Ciphertext gout694 = ctx.eval_lut(&gin694, xnor2);
    std::cerr << "\rLUT695   ";
    Ciphertext gin695 = 2 * gout675 + gout353;
    Ciphertext gout695 = ctx.eval_lut(&gin695, xnor2);
    std::cerr << "\rLUT696   ";
    Ciphertext gin696 = 2 * ct_x9 + gout674;
    Ciphertext gout696 = ctx.eval_lut(&gin696, nand2);
    std::cerr << "\rLUT697   ";
    Ciphertext gin697 = 2 * gout116 + gout670;
    Ciphertext gout697 = ctx.eval_lut(&gin697, xnor2);
    std::cerr << "\rLUT698   ";
    Ciphertext gin698 = 2 * gout648 + gout669;
    Ciphertext gout698 = ctx.eval_lut(&gin698, or2);
    std::cerr << "\rLUT699   ";
    Ciphertext gin699 = 2 * gout641 + gout672;
    Ciphertext gout699 = ctx.eval_lut(&gin699, or2);
    std::cerr << "\rLUT700   ";
    Ciphertext gin700 = 2 * gout683 + gout550;
    Ciphertext gout700 = ctx.eval_lut(&gin700, and2);
    std::cerr << "\rLUT701   ";
    Ciphertext gin701 = 2 * gout342 + gout673;
    Ciphertext gout701 = ctx.eval_lut(&gin701, and2);
    std::cerr << "\rLUT702   ";
    Ciphertext gin702 = 2 * (1 - gout679) + gout618;
    Ciphertext gout702 = ctx.eval_lut(&gin702, and2);
    std::cerr << "\rLUT703   ";
    Ciphertext gin703 = 2 * gout366 + gout677;
    Ciphertext gout703 = ctx.eval_lut(&gin703, xor2);
    std::cerr << "\rLUT704   ";
    Ciphertext gin704 = 2 * gout366 + gout677;
    Ciphertext gout704 = ctx.eval_lut(&gin704, nand2);
    std::cerr << "\rLUT705   ";
    Ciphertext gin705 = 2 * gout405 + gout680;
    Ciphertext gout705 = ctx.eval_lut(&gin705, nand2);
    std::cerr << "\rLUT706   ";
    Ciphertext gin706 = 2 * gout34 + gout674;
    Ciphertext gout706 = ctx.eval_lut(&gin706, xnor2);
    std::cerr << "\rLUT707   ";
    Ciphertext gin707 = 2 * ct_x9 + gout674;
    Ciphertext gout707 = ctx.eval_lut(&gin707, or2);
    std::cerr << "\rLUT708   ";
    Ciphertext gin708 = 2 * gout679 + gout618;
    Ciphertext gout708 = ctx.eval_lut(&gin708, xnor2);
    std::cerr << "\rLUT709   ";
    Ciphertext gin709 = 2 * gout667 + gout687;
    Ciphertext gout709 = ctx.eval_lut(&gin709, or2);
    std::cerr << "\rLUT710   ";
    Ciphertext gin710 = 2 * gout685 + gout575;
    Ciphertext gout710 = ctx.eval_lut(&gin710, xnor2);
    std::cerr << "\rLUT711   ";
    Ciphertext gin711 = 2 * (1 - gout685) + gout575;
    Ciphertext gout711 = ctx.eval_lut(&gin711, and2);
    std::cerr << "\rLUT712   ";
    Ciphertext gin712 = 2 * gout683 + gout550;
    Ciphertext gout712 = ctx.eval_lut(&gin712, xnor2);
    std::cerr << "\rLUT713   ";
    Ciphertext gin713 = 2 * gout695 + gout688;
    Ciphertext gout713 = ctx.eval_lut(&gin713, xnor2);
    std::cerr << "\rLUT714   ";
    Ciphertext gin714 = 2 * gout690 + gout507;
    Ciphertext gout714 = ctx.eval_lut(&gin714, nand2);
    std::cerr << "\rLUT715   ";
    Ciphertext gin715 = 2 * gout653 + gout692;
    Ciphertext gout715 = ctx.eval_lut(&gin715, nand2);
    std::cerr << "\rLUT716   ";
    Ciphertext gin716 = 2 * gout690 + gout507;
    Ciphertext gout716 = ctx.eval_lut(&gin716, xnor2);
    std::cerr << "\rLUT717   ";
    Ciphertext gin717 = 2 * gout665 + gout694;
    Ciphertext gout717 = ctx.eval_lut(&gin717, xnor2);
    std::cerr << "\rLUT718   ";
    Ciphertext gin718 = 2 * gout657 + gout696;
    Ciphertext gout718 = ctx.eval_lut(&gin718, nand2);
    std::cerr << "\rLUT719   ";
    Ciphertext gin719 = 2 * gout113 + gout697;
    Ciphertext gout719 = ctx.eval_lut(&gin719, xnor2);
    std::cerr << "\rLUT720   ";
    Ciphertext gin720 = 2 * gout676 + gout702;
    Ciphertext gout720 = ctx.eval_lut(&gin720, or2);
    std::cerr << "\rLUT721   ";
    Ciphertext gin721 = 2 * ct_y11 + gout701;
    Ciphertext gout721 = ctx.eval_lut(&gin721, xnor2);
    std::cerr << "\rLUT722   ";
    Ciphertext gin722 = 2 * gout626 + gout694;
    Ciphertext gout722 = ctx.eval_lut(&gin722, and2);
    std::cerr << "\rLUT723   ";
    Ciphertext gin723 = 2 * gout660 + gout703;
    Ciphertext gout723 = ctx.eval_lut(&gin723, and2);
    std::cerr << "\rLUT724   ";
    Ciphertext gin724 = 2 * gout699 + gout668;
    Ciphertext gout724 = ctx.eval_lut(&gin724, xor2);
    std::cerr << "\rLUT725   ";
    Ciphertext gin725 = 2 * gout699 + gout668;
    Ciphertext gout725 = ctx.eval_lut(&gin725, nand2);
    std::cerr << "\rLUT726   ";
    Ciphertext gin726 = 2 * ct_x10 + gout697;
    Ciphertext gout726 = ctx.eval_lut(&gin726, nand2);
    std::cerr << "\rINV727   ";
    Ciphertext gout727 = 1 - gout706;

    std::cerr << "\rLUT728   ";
    Ciphertext gin728 = 2 * (1 - gout701) + gout384;
    Ciphertext gout728 = ctx.eval_lut(&gin728, and2);
    std::cerr << "\rLUT729   ";
    Ciphertext gin729 = 2 * gout712 + gout584;
    Ciphertext gout729 = ctx.eval_lut(&gin729, xnor2);
    std::cerr << "\rLUT730   ";
    Ciphertext gin730 = 2 * gout707 + gout718;
    Ciphertext gout730 = ctx.eval_lut(&gin730, nand2);
    std::cerr << "\rLUT731   ";
    Ciphertext gin731 = 2 * gout678 + gout704;
    Ciphertext gout731 = ctx.eval_lut(&gin731, nand2);
    std::cerr << "\rLUT732   ";
    Ciphertext gin732 = 2 * gout693 + gout703;
    Ciphertext gout732 = ctx.eval_lut(&gin732, xnor2);
    std::cerr << "\rLUT733   ";
    Ciphertext gin733 = 2 * gout698 + gout708;
    Ciphertext gout733 = ctx.eval_lut(&gin733, xor2);
    std::cerr << "\rLUT734   ";
    Ciphertext gin734 = 2 * gout698 + gout708;
    Ciphertext gout734 = ctx.eval_lut(&gin734, nand2);
    std::cerr << "\rLUT735   ";
    Ciphertext gin735 = 2 * gout731 + gout691;
    Ciphertext gout735 = ctx.eval_lut(&gin735, and2);
    std::cerr << "\rLUT736   ";
    Ciphertext gin736 = 2 * (1 - gout712) + gout584;
    Ciphertext gout736 = ctx.eval_lut(&gin736, and2);
    std::cerr << "\rLUT737   ";
    Ciphertext gin737 = 2 * gout689 + gout711;
    Ciphertext gout737 = ctx.eval_lut(&gin737, or2);
    std::cerr << "\rLUT738   ";
    Ciphertext gin738 = 2 * gout496 + gout714;
    Ciphertext gout738 = ctx.eval_lut(&gin738, nand2);
    std::cerr << "\rLUT739   ";
    Ciphertext gin739 = 2 * gout709 + gout713;
    Ciphertext gout739 = ctx.eval_lut(&gin739, xnor2);
    std::cerr << "\rLUT740   ";
    Ciphertext gin740 = 2 * gout731 + gout691;
    Ciphertext gout740 = ctx.eval_lut(&gin740, xor2);
    std::cerr << "\rLUT741   ";
    Ciphertext gin741 = 2 * gout715 + gout710;
    Ciphertext gout741 = ctx.eval_lut(&gin741, xor2);
    std::cerr << "\rLUT742   ";
    Ciphertext gin742 = 2 * gout715 + gout710;
    Ciphertext gout742 = ctx.eval_lut(&gin742, and2);
    std::cerr << "\rIMUX743  ";
    Ciphertext gin743A = 2 * gout541 + gout413;
    Ciphertext gout743A = ctx.eval_lut(&gin743A, and2);
    Ciphertext gin743B = 2 * gout716 + (1 - gout413);
    Ciphertext gout743B = ctx.eval_lut(&gin743B, and2);
    Ciphertext gout743 = gout743A + gout743B;
    std::cerr << "\rLUT744   ";
    Ciphertext gin744 = 2 * ct_x11 + gout721;
    Ciphertext gout744 = ctx.eval_lut(&gin744, xnor2);
    std::cerr << "\rLUT745   ";
    Ciphertext gin745 = 2 * gout720 + gout686;
    Ciphertext gout745 = ctx.eval_lut(&gin745, nand2);
    std::cerr << "\rLUT746   ";
    Ciphertext gin746 = 2 * gout666 + gout717;
    Ciphertext gout746 = ctx.eval_lut(&gin746, xnor2);
    std::cerr << "\rLUT747   ";
    Ciphertext gin747 = 2 * gout160 + gout721;
    Ciphertext gout747 = ctx.eval_lut(&gin747, nand2);
    std::cerr << "\rLUT748   ";
    Ciphertext gin748 = 2 * gout720 + gout686;
    Ciphertext gout748 = ctx.eval_lut(&gin748, xnor2);
    std::cerr << "\rLUT749   ";
    Ciphertext gin749 = 2 * gout638 + gout717;
    Ciphertext gout749 = ctx.eval_lut(&gin749, nand2);
    std::cerr << "\rINV750   ";
    Ciphertext gout750 = 1 - gout722;

    std::cerr << "\rLUT751   ";
    Ciphertext gin751 = 2 * gout564 + gout724;
    Ciphertext gout751 = ctx.eval_lut(&gin751, xor2);
    std::cerr << "\rLUT752   ";
    Ciphertext gin752 = 2 * gout160 + gout721;
    Ciphertext gout752 = ctx.eval_lut(&gin752, or2);
    std::cerr << "\rINV753   ";
    Ciphertext gout753 = 1 - gout723;

    std::cerr << "\rLUT754   ";
    Ciphertext gin754 = 2 * gout750 + gout732;
    Ciphertext gout754 = ctx.eval_lut(&gin754, xnor2);
    std::cerr << "\rLUT755   ";
    Ciphertext gin755 = 2 * gout564 + gout724;
    Ciphertext gout755 = ctx.eval_lut(&gin755, nand2);
    std::cerr << "\rLUT756   ";
    Ciphertext gin756 = 2 * ct_y12 + gout728;
    Ciphertext gout756 = ctx.eval_lut(&gin756, xnor2);
    std::cerr << "\rLUT757   ";
    Ciphertext gin757 = 2 * gout659 + gout733;
    Ciphertext gout757 = ctx.eval_lut(&gin757, nand2);
    std::cerr << "\rLUT758   ";
    Ciphertext gin758 = 2 * gout728 + gout388;
    Ciphertext gout758 = ctx.eval_lut(&gin758, nand2);
    std::cerr << "\rLUT759   ";
    Ciphertext gin759 = 2 * gout722 + gout732;
    Ciphertext gout759 = ctx.eval_lut(&gin759, nand2);
    std::cerr << "\rLUT760   ";
    Ciphertext gin760 = 2 * gout737 + gout729;
    Ciphertext gout760 = ctx.eval_lut(&gin760, nand2);
    std::cerr << "\rLUT761   ";
    Ciphertext gin761 = 2 * gout738 + gout513;
    Ciphertext gout761 = ctx.eval_lut(&gin761, xnor2);
    std::cerr << "\rLUT762   ";
    Ciphertext gin762 = 2 * gout700 + gout736;
    Ciphertext gout762 = ctx.eval_lut(&gin762, or2);
    std::cerr << "\rLUT763   ";
    Ciphertext gin763 = 2 * gout659 + gout733;
    Ciphertext gout763 = ctx.eval_lut(&gin763, xor2);
    std::cerr << "\rLUT764   ";
    Ciphertext gin764 = 2 * gout723 + gout740;
    Ciphertext gout764 = ctx.eval_lut(&gin764, and2);
    std::cerr << "\rLUT765   ";
    Ciphertext gin765 = 2 * gout737 + gout729;
    Ciphertext gout765 = ctx.eval_lut(&gin765, xor2);
    std::cerr << "\rLUT766   ";
    Ciphertext gin766 = 2 * gout735 + gout741;
    Ciphertext gout766 = ctx.eval_lut(&gin766, xor2);
    std::cerr << "\rINV767   ";
    Ciphertext gout767 = 1 - gout742;

    std::cerr << "\rLUT768   ";
    Ciphertext gin768 = 2 * gout735 + gout741;
    Ciphertext gout768 = ctx.eval_lut(&gin768, and2);
    std::cerr << "\rLUT769   ";
    Ciphertext gin769 = 2 * gout738 + gout513;
    Ciphertext gout769 = ctx.eval_lut(&gin769, nand2);
    std::cerr << "\rLUT770   ";
    Ciphertext gin770 = 2 * gout759 + (1 - gout740);
    Ciphertext gout770 = ctx.eval_lut(&gin770, or2);
    std::cerr << "\rLUT771   ";
    Ciphertext gin771 = 2 * gout405 + gout743;
    Ciphertext gout771 = ctx.eval_lut(&gin771, nand2);
    std::cerr << "\rLUT772   ";
    Ciphertext gin772 = 2 * gout663 + gout746;
    Ciphertext gout772 = ctx.eval_lut(&gin772, xnor2);
    std::cerr << "\rLUT773   ";
    Ciphertext gin773 = 2 * (1 - gout748) + gout684;
    Ciphertext gout773 = ctx.eval_lut(&gin773, and2);
    std::cerr << "\rINV774   ";
    Ciphertext gout774 = 1 - gout744;

    std::cerr << "\rLUT775   ";
    Ciphertext gin775 = 2 * gout663 + gout746;
    Ciphertext gout775 = ctx.eval_lut(&gin775, nand2);
    std::cerr << "\rLUT776   ";
    Ciphertext gin776 = 2 * gout726 + gout752;
    Ciphertext gout776 = ctx.eval_lut(&gin776, nand2);
    std::cerr << "\rLUT777   ";
    Ciphertext gin777 = 2 * gout748 + gout684;
    Ciphertext gout777 = ctx.eval_lut(&gin777, xnor2);
    std::cerr << "\rLUT778   ";
    Ciphertext gin778 = 2 * gout734 + gout757;
    Ciphertext gout778 = ctx.eval_lut(&gin778, nand2);
    std::cerr << "\rLUT779   ";
    Ciphertext gin779 = 2 * gout758 + gout383;
    Ciphertext gout779 = ctx.eval_lut(&gin779, or2);
    std::cerr << "\rLUT780   ";
    Ciphertext gin780 = 2 * ct_y13 + gout758;
    Ciphertext gout780 = ctx.eval_lut(&gin780, xnor2);
    std::cerr << "\rLUT781   ";
    Ciphertext gin781 = 2 * gout753 + gout759;
    Ciphertext gout781 = ctx.eval_lut(&gin781, and2);
    std::cerr << "\rIMUX782  ";
    Ciphertext gin782A = 2 * gout761 + gout475;
    Ciphertext gout782A = ctx.eval_lut(&gin782A, and2);
    Ciphertext gin782B = 2 * gout619 + (1 - gout475);
    Ciphertext gout782B = ctx.eval_lut(&gin782B, and2);
    Ciphertext gout782 = gout782A + gout782B;
    std::cerr << "\rLUT783   ";
    Ciphertext gin783 = 2 * gout725 + gout755;
    Ciphertext gout783 = ctx.eval_lut(&gin783, nand2);
    std::cerr << "\rLUT784   ";
    Ciphertext gin784 = 2 * gout25 + gout756;
    Ciphertext gout784 = ctx.eval_lut(&gin784, xnor2);
    std::cerr << "\rLUT785   ";
    Ciphertext gin785 = 2 * gout762 + gout681;
    Ciphertext gout785 = ctx.eval_lut(&gin785, xor2);
    std::cerr << "\rLUT786   ";
    Ciphertext gin786 = 2 * gout762 + gout681;
    Ciphertext gout786 = ctx.eval_lut(&gin786, nand2);
    std::cerr << "\rLUT787   ";
    Ciphertext gin787 = 2 * ct_x12 + gout756;
    Ciphertext gout787 = ctx.eval_lut(&gin787, nand2);
    std::cerr << "\rLUT788   ";
    Ciphertext gin788 = 2 * gout764 + gout766;
    Ciphertext gout788 = ctx.eval_lut(&gin788, nand2);
    std::cerr << "\rLUT789   ";
    Ciphertext gin789 = 2 * gout531 + gout769;
    Ciphertext gout789 = ctx.eval_lut(&gin789, nand2);
    std::cerr << "\rLUT790   ";
    Ciphertext gin790 = 2 * gout562 + gout765;
    Ciphertext gout790 = ctx.eval_lut(&gin790, xor2);
    std::cerr << "\rLUT791   ";
    Ciphertext gin791 = 2 * gout562 + gout765;
    Ciphertext gout791 = ctx.eval_lut(&gin791, nand2);
    std::cerr << "\rLUT792   ";
    Ciphertext gin792 = 2 * gout764 + gout766;
    Ciphertext gout792 = ctx.eval_lut(&gin792, xnor2);
    std::cerr << "\rLUT793   ";
    Ciphertext gin793 = 2 * gout745 + (1 - gout773);
    Ciphertext gout793 = ctx.eval_lut(&gin793, and2);
    std::cerr << "\rLUT794   ";
    Ciphertext gin794 = 2 * gout749 + gout775;
    Ciphertext gout794 = ctx.eval_lut(&gin794, nand2);
    std::cerr << "\rLUT795   ";
    Ciphertext gin795 = 2 * gout747 + gout776;
    Ciphertext gout795 = ctx.eval_lut(&gin795, nand2);
    std::cerr << "\rLUT796   ";
    Ciphertext gin796 = 2 * gout793 + gout739;
    Ciphertext gout796 = ctx.eval_lut(&gin796, xnor2);
    std::cerr << "\rLUT797   ";
    Ciphertext gin797 = 2 * gout778 + gout777;
    Ciphertext gout797 = ctx.eval_lut(&gin797, xor2);
    std::cerr << "\rLUT798   ";
    Ciphertext gin798 = 2 * gout778 + gout777;
    Ciphertext gout798 = ctx.eval_lut(&gin798, and2);
    std::cerr << "\rLUT799   ";
    Ciphertext gin799 = 2 * gout405 + gout782;
    Ciphertext gout799 = ctx.eval_lut(&gin799, nand2);
    std::cerr << "\rLUT800   ";
    Ciphertext gin800 = 2 * gout64 + gout780;
    Ciphertext gout800 = ctx.eval_lut(&gin800, nand2);
    std::cerr << "\rLUT801   ";
    Ciphertext gin801 = 2 * gout740 + gout781;
    Ciphertext gout801 = ctx.eval_lut(&gin801, xnor2);
    std::cerr << "\rLUT802   ";
    Ciphertext gin802 = 2 * gout783 + gout763;
    Ciphertext gout802 = ctx.eval_lut(&gin802, and2);
    std::cerr << "\rLUT803   ";
    Ciphertext gin803 = 2 * gout779 + gout391;
    Ciphertext gout803 = ctx.eval_lut(&gin803, or2);
    std::cerr << "\rLUT804   ";
    Ciphertext gin804 = 2 * gout580 + gout785;
    Ciphertext gout804 = ctx.eval_lut(&gin804, nand2);
    std::cerr << "\rLUT805   ";
    Ciphertext gin805 = 2 * gout64 + gout780;
    Ciphertext gout805 = ctx.eval_lut(&gin805, or2);
    std::cerr << "\rLUT806   ";
    Ciphertext gin806 = 2 * gout580 + gout785;
    Ciphertext gout806 = ctx.eval_lut(&gin806, xor2);
    std::cerr << "\rLUT807   ";
    Ciphertext gin807 = 2 * ct_x13 + gout780;
    Ciphertext gout807 = ctx.eval_lut(&gin807, xnor2);
    std::cerr << "\rLUT808   ";
    Ciphertext gin808 = 2 * gout789 + gout532;
    Ciphertext gout808 = ctx.eval_lut(&gin808, xnor2);
    std::cerr << "\rLUT809   ";
    Ciphertext gin809 = 2 * gout783 + gout763;
    Ciphertext gout809 = ctx.eval_lut(&gin809, xor2);
    std::cerr << "\rLUT810   ";
    Ciphertext gin810 = 2 * gout39 + gout779;
    Ciphertext gout810 = ctx.eval_lut(&gin810, xnor2);
    std::cerr << "\rLUT811   ";
    Ciphertext gin811 = 2 * gout742 + gout790;
    Ciphertext gout811 = ctx.eval_lut(&gin811, and2);
    std::cerr << "\rLUT812   ";
    Ciphertext gin812 = 2 * gout789 + gout532;
    Ciphertext gout812 = ctx.eval_lut(&gin812, nand2);
    std::cerr << "\rLUT813   ";
    Ciphertext gin813 = 2 * gout767 + gout790;
    Ciphertext gout813 = ctx.eval_lut(&gin813, xnor2);
    std::cerr << "\rLUT814   ";
    Ciphertext gin814 = 2 * gout760 + gout791;
    Ciphertext gout814 = ctx.eval_lut(&gin814, nand2);
    std::cerr << "\rLUT815   ";
    Ciphertext gin815 = 2 * gout754 + gout794;
    Ciphertext gout815 = ctx.eval_lut(&gin815, xnor2);
    std::cerr << "\rLUT816   ";
    Ciphertext gin816 = 2 * gout754 + gout794;
    Ciphertext gout816 = ctx.eval_lut(&gin816, and2);
    std::cerr << "\rLUT817   ";
    Ciphertext gin817 = 2 * gout802 + gout797;
    Ciphertext gout817 = ctx.eval_lut(&gin817, xor2);
    std::cerr << "\rLUT818   ";
    Ciphertext gin818 = 2 * gout802 + gout797;
    Ciphertext gout818 = ctx.eval_lut(&gin818, nand2);
    std::cerr << "\rLUT819   ";
    Ciphertext gin819 = 2 * gout682 + gout798;
    Ciphertext gout819 = ctx.eval_lut(&gin819, xnor2);
    std::cerr << "\rLUT820   ";
    Ciphertext gin820 = 2 * gout803 + gout145;
    Ciphertext gout820 = ctx.eval_lut(&gin820, xnor2);
    std::cerr << "\rLUT821   ";
    Ciphertext gin821 = 2 * gout787 + gout805;
    Ciphertext gout821 = ctx.eval_lut(&gin821, nand2);
    std::cerr << "\rLUT822   ";
    Ciphertext gin822 = 2 * gout786 + gout804;
    Ciphertext gout822 = ctx.eval_lut(&gin822, nand2);
    std::cerr << "\rLUT823   ";
    Ciphertext gin823 = 2 * gout51 + gout810;
    Ciphertext gout823 = ctx.eval_lut(&gin823, xnor2);
    std::cerr << "\rIMUX824  ";
    Ciphertext gin824A = 2 * gout632 + gout413;
    Ciphertext gout824A = ctx.eval_lut(&gin824A, and2);
    Ciphertext gin824B = 2 * gout808 + (1 - gout413);
    Ciphertext gout824B = ctx.eval_lut(&gin824B, and2);
    Ciphertext gout824 = gout824A + gout824B;
    std::cerr << "\rINV825   ";
    Ciphertext gout825 = 1 - gout807;

    std::cerr << "\rLUT826   ";
    Ciphertext gin826 = 2 * ct_x14 + gout810;
    Ciphertext gout826 = ctx.eval_lut(&gin826, nand2);
    std::cerr << "\rLUT827   ";
    Ciphertext gin827 = 2 * gout530 + gout812;
    Ciphertext gout827 = ctx.eval_lut(&gin827, nand2);
    std::cerr << "\rLUT828   ";
    Ciphertext gin828 = 2 * gout768 + gout813;
    Ciphertext gout828 = ctx.eval_lut(&gin828, xor2);
    std::cerr << "\rINV829   ";
    Ciphertext gout829 = 1 - gout811;

    std::cerr << "\rLUT830   ";
    Ciphertext gin830 = 2 * gout768 + gout813;
    Ciphertext gout830 = ctx.eval_lut(&gin830, nand2);
    std::cerr << "\rLUT831   ";
    Ciphertext gin831 = 2 * gout814 + gout806;
    Ciphertext gout831 = ctx.eval_lut(&gin831, and2);
    std::cerr << "\rLUT832   ";
    Ciphertext gin832 = 2 * gout814 + gout806;
    Ciphertext gout832 = ctx.eval_lut(&gin832, xor2);
    std::cerr << "\rLUT833   ";
    Ciphertext gin833 = 2 * gout816 + gout801;
    Ciphertext gout833 = ctx.eval_lut(&gin833, xnor2);
    std::cerr << "\rLUT834   ";
    Ciphertext gin834 = 2 * gout816 + gout801;
    Ciphertext gout834 = ctx.eval_lut(&gin834, nand2);
    std::cerr << "\rLUT835   ";
    Ciphertext gin835 = 2 * gout796 + gout819;
    Ciphertext gout835 = ctx.eval_lut(&gin835, xnor2);
    std::cerr << "\rLUT836   ";
    Ciphertext gin836 = 2 * gout800 + gout821;
    Ciphertext gout836 = ctx.eval_lut(&gin836, nand2);
    std::cerr << "\rLUT837   ";
    Ciphertext gin837 = 2 * gout822 + gout751;
    Ciphertext gout837 = ctx.eval_lut(&gin837, and2);
    std::cerr << "\rLUT838   ";
    Ciphertext gin838 = 2 * gout405 + gout824;
    Ciphertext gout838 = ctx.eval_lut(&gin838, nand2);
    std::cerr << "\rLUT839   ";
    Ciphertext gin839 = 2 * gout822 + gout751;
    Ciphertext gout839 = ctx.eval_lut(&gin839, xor2);
    std::cerr << "\rLUT840   ";
    Ciphertext gin840 = 2 * gout827 + gout587;
    Ciphertext gout840 = ctx.eval_lut(&gin840, nand2);
    std::cerr << "\rLUT841   ";
    Ciphertext gin841 = 2 * gout827 + gout587;
    Ciphertext gout841 = ctx.eval_lut(&gin841, xnor2);
    std::cerr << "\rLUT842   ";
    Ciphertext gin842 = 2 * gout831 + gout839;
    Ciphertext gout842 = ctx.eval_lut(&gin842, xor2);
    std::cerr << "\rLUT843   ";
    Ciphertext gin843 = 2 * gout811 + gout832;
    Ciphertext gout843 = ctx.eval_lut(&gin843, nand2);
    std::cerr << "\rLUT844   ";
    Ciphertext gin844 = 2 * gout831 + gout839;
    Ciphertext gout844 = ctx.eval_lut(&gin844, nand2);
    std::cerr << "\rLUT845   ";
    Ciphertext gin845 = 2 * gout829 + gout832;
    Ciphertext gout845 = ctx.eval_lut(&gin845, xnor2);
    std::cerr << "\rLUT846   ";
    Ciphertext gin846 = 2 * gout834 + gout770;
    Ciphertext gout846 = ctx.eval_lut(&gin846, and2);
    std::cerr << "\rLUT847   ";
    Ciphertext gin847 = 2 * gout837 + gout809;
    Ciphertext gout847 = ctx.eval_lut(&gin847, nand2);
    std::cerr << "\rLUT848   ";
    Ciphertext gin848 = 2 * gout837 + gout809;
    Ciphertext gout848 = ctx.eval_lut(&gin848, xor2);
    std::cerr << "\rLUT849   ";
    Ciphertext gin849 = 2 * gout595 + gout840;
    Ciphertext gout849 = ctx.eval_lut(&gin849, nand2);
    std::cerr << "\rIMUX850  ";
    Ciphertext gin850A = 2 * gout772 + gout413;
    Ciphertext gout850A = ctx.eval_lut(&gin850A, and2);
    Ciphertext gin850B = 2 * gout841 + (1 - gout413);
    Ciphertext gout850B = ctx.eval_lut(&gin850B, and2);
    Ciphertext gout850 = gout850A + gout850B;
    std::cerr << "\rLUT851   ";
    Ciphertext gin851 = 2 * gout846 + gout792;
    Ciphertext gout851 = ctx.eval_lut(&gin851, nand2);
    std::cerr << "\rLUT852   ";
    Ciphertext gin852 = 2 * gout846 + gout792;
    Ciphertext gout852 = ctx.eval_lut(&gin852, or2);
    std::cerr << "\rLUT853   ";
    Ciphertext gin853 = 2 * gout849 + gout639;
    Ciphertext gout853 = ctx.eval_lut(&gin853, xnor2);
    std::cerr << "\rLUT854   ";
    Ciphertext gin854 = 2 * gout405 + gout850;
    Ciphertext gout854 = ctx.eval_lut(&gin854, nand2);
    std::cerr << "\rLUT855   ";
    Ciphertext gin855 = 2 * gout849 + gout639;
    Ciphertext gout855 = ctx.eval_lut(&gin855, nand2);
    std::cerr << "\rLUT856   ";
    Ciphertext gin856 = 2 * gout788 + gout852;
    Ciphertext gout856 = ctx.eval_lut(&gin856, nand2);
    std::cerr << "\rLUT857   ";
    Ciphertext gin857 = 2 * gout413 + gout852;
    Ciphertext gout857 = ctx.eval_lut(&gin857, and2);
    std::cerr << "\rIMUX858  ";
    Ciphertext gin858A = 2 * gout815 + gout413;
    Ciphertext gout858A = ctx.eval_lut(&gin858A, and2);
    Ciphertext gin858B = 2 * gout853 + (1 - gout413);
    Ciphertext gout858B = ctx.eval_lut(&gin858B, and2);
    Ciphertext gout858 = gout858A + gout858B;
    std::cerr << "\rLUT859   ";
    Ciphertext gin859 = 2 * gout629 + gout855;
    Ciphertext gout859 = ctx.eval_lut(&gin859, nand2);
    std::cerr << "\rLUT860   ";
    Ciphertext gin860 = 2 * gout856 + gout828;
    Ciphertext gout860 = ctx.eval_lut(&gin860, xnor2);
    std::cerr << "\rLUT861   ";
    Ciphertext gin861 = 2 * gout856 + gout828;
    Ciphertext gout861 = ctx.eval_lut(&gin861, nand2);
    std::cerr << "\rLUT862   ";
    Ciphertext gin862 = 2 * gout851 + gout857;
    Ciphertext gout862 = ctx.eval_lut(&gin862, nand2);
    std::cerr << "\rLUT863   ";
    Ciphertext gin863 = 2 * gout859 + gout658;
    Ciphertext gout863 = ctx.eval_lut(&gin863, xnor2);
    std::cerr << "\rLUT864   ";
    Ciphertext gin864 = 2 * gout859 + gout658;
    Ciphertext gout864 = ctx.eval_lut(&gin864, nand2);
    std::cerr << "\rLUT865   ";
    Ciphertext gin865 = 2 * gout405 + gout858;
    Ciphertext gout865 = ctx.eval_lut(&gin865, nand2);
    std::cerr << "\rLUT866   ";
    Ciphertext gin866 = 2 * gout830 + gout861;
    Ciphertext gout866 = ctx.eval_lut(&gin866, nand2);
    std::cerr << "\rLUT867   ";
    Ciphertext gin867 = 2 * gout866 + gout845;
    Ciphertext gout867 = ctx.eval_lut(&gin867, nand2);
    std::cerr << "\rLUT868   ";
    Ciphertext gin868 = 2 * gout405 + gout862;
    Ciphertext gout868 = ctx.eval_lut(&gin868, and2);
    std::cerr << "\rLUT869   ";
    Ciphertext gin869 = 2 * gout866 + gout845;
    Ciphertext gout869 = ctx.eval_lut(&gin869, or2);
    std::cerr << "\rLUT870   ";
    Ciphertext gin870 = 2 * gout864 + gout727;
    Ciphertext gout870 = ctx.eval_lut(&gin870, or2);
    std::cerr << "\rIMUX871  ";
    Ciphertext gin871A = 2 * gout833 + gout413;
    Ciphertext gout871A = ctx.eval_lut(&gin871A, and2);
    Ciphertext gin871B = 2 * gout863 + (1 - gout413);
    Ciphertext gout871B = ctx.eval_lut(&gin871B, and2);
    Ciphertext gout871 = gout871A + gout871B;
    std::cerr << "\rLUT872   ";
    Ciphertext gin872 = 2 * gout657 + gout864;
    Ciphertext gout872 = ctx.eval_lut(&gin872, nand2);
    std::cerr << "\rLUT873   ";
    Ciphertext gin873 = 2 * gout843 + gout867;
    Ciphertext gout873 = ctx.eval_lut(&gin873, nand2);
    std::cerr << "\rLUT874   ";
    Ciphertext gin874 = 2 * gout413 + gout869;
    Ciphertext gout874 = ctx.eval_lut(&gin874, and2);
    std::cerr << "\rLUT875   ";
    Ciphertext gin875 = 2 * gout405 + gout871;
    Ciphertext gout875 = ctx.eval_lut(&gin875, nand2);
    std::cerr << "\rLUT876   ";
    Ciphertext gin876 = 2 * gout872 + gout706;
    Ciphertext gout876 = ctx.eval_lut(&gin876, nand2);
    std::cerr << "\rLUT877   ";
    Ciphertext gin877 = 2 * gout872 + gout706;
    Ciphertext gout877 = ctx.eval_lut(&gin877, or2);
    std::cerr << "\rLUT878   ";
    Ciphertext gin878 = 2 * gout870 + gout730;
    Ciphertext gout878 = ctx.eval_lut(&gin878, nand2);
    std::cerr << "\rLUT879   ";
    Ciphertext gin879 = 2 * gout873 + gout842;
    Ciphertext gout879 = ctx.eval_lut(&gin879, xnor2);
    std::cerr << "\rLUT880   ";
    Ciphertext gin880 = 2 * gout873 + gout842;
    Ciphertext gout880 = ctx.eval_lut(&gin880, nand2);
    std::cerr << "\rLUT881   ";
    Ciphertext gin881 = 2 * gout867 + gout874;
    Ciphertext gout881 = ctx.eval_lut(&gin881, nand2);
    std::cerr << "\rLUT882   ";
    Ciphertext gin882 = 2 * gout878 + gout719;
    Ciphertext gout882 = ctx.eval_lut(&gin882, nand2);
    std::cerr << "\rLUT883   ";
    Ciphertext gin883 = 2 * gout475 + gout877;
    Ciphertext gout883 = ctx.eval_lut(&gin883, and2);
    std::cerr << "\rLUT884   ";
    Ciphertext gin884 = 2 * gout878 + gout719;
    Ciphertext gout884 = ctx.eval_lut(&gin884, xnor2);
    std::cerr << "\rLUT885   ";
    Ciphertext gin885 = 2 * gout876 + gout883;
    Ciphertext gout885 = ctx.eval_lut(&gin885, nand2);
    std::cerr << "\rLUT886   ";
    Ciphertext gin886 = 2 * gout405 + gout881;
    Ciphertext gout886 = ctx.eval_lut(&gin886, and2);
    std::cerr << "\rLUT887   ";
    Ciphertext gin887 = 2 * gout882 + gout774;
    Ciphertext gout887 = ctx.eval_lut(&gin887, or2);
    std::cerr << "\rLUT888   ";
    Ciphertext gin888 = 2 * gout844 + gout880;
    Ciphertext gout888 = ctx.eval_lut(&gin888, nand2);
    std::cerr << "\rLUT889   ";
    Ciphertext gin889 = 2 * gout726 + gout882;
    Ciphertext gout889 = ctx.eval_lut(&gin889, nand2);
    std::cerr << "\rIMUX890  ";
    Ciphertext gin890A = 2 * gout860 + gout413;
    Ciphertext gout890A = ctx.eval_lut(&gin890A, and2);
    Ciphertext gin890B = 2 * gout884 + (1 - gout413);
    Ciphertext gout890B = ctx.eval_lut(&gin890B, and2);
    Ciphertext gout890 = gout890A + gout890B;
    std::cerr << "\rLUT891   ";
    Ciphertext gin891 = 2 * gout887 + gout795;
    Ciphertext gout891 = ctx.eval_lut(&gin891, nand2);
    std::cerr << "\rLUT892   ";
    Ciphertext gin892 = 2 * gout885 + gout868;
    Ciphertext gout892 = ctx.eval_lut(&gin892, nand2);
    std::cerr << "\rLUT893   ";
    Ciphertext gin893 = 2 * gout888 + gout848;
    Ciphertext gout893 = ctx.eval_lut(&gin893, nand2);
    std::cerr << "\rLUT894   ";
    Ciphertext gin894 = 2 * gout405 + gout890;
    Ciphertext gout894 = ctx.eval_lut(&gin894, nand2);
    std::cerr << "\rLUT895   ";
    Ciphertext gin895 = 2 * gout889 + gout744;
    Ciphertext gout895 = ctx.eval_lut(&gin895, or2);
    std::cerr << "\rLUT896   ";
    Ciphertext gin896 = 2 * gout888 + gout848;
    Ciphertext gout896 = ctx.eval_lut(&gin896, or2);
    std::cerr << "\rLUT897   ";
    Ciphertext gin897 = 2 * gout889 + gout744;
    Ciphertext gout897 = ctx.eval_lut(&gin897, nand2);
    std::cerr << "\rLUT898   ";
    Ciphertext gin898 = 2 * gout413 + gout896;
    Ciphertext gout898 = ctx.eval_lut(&gin898, and2);
    std::cerr << "\rLUT899   ";
    Ciphertext gin899 = 2 * gout891 + gout784;
    Ciphertext gout899 = ctx.eval_lut(&gin899, nand2);
    std::cerr << "\rLUT900   ";
    Ciphertext gin900 = 2 * gout891 + gout784;
    Ciphertext gout900 = ctx.eval_lut(&gin900, xnor2);
    std::cerr << "\rLUT901   ";
    Ciphertext gin901 = 2 * gout475 + gout895;
    Ciphertext gout901 = ctx.eval_lut(&gin901, and2);
    std::cerr << "\rLUT902   ";
    Ciphertext gin902 = 2 * gout847 + gout893;
    Ciphertext gout902 = ctx.eval_lut(&gin902, nand2);
    std::cerr << "\rLUT903   ";
    Ciphertext gin903 = 2 * gout893 + gout898;
    Ciphertext gout903 = ctx.eval_lut(&gin903, nand2);
    std::cerr << "\rLUT904   ";
    Ciphertext gin904 = 2 * gout899 + gout825;
    Ciphertext gout904 = ctx.eval_lut(&gin904, or2);
    std::cerr << "\rLUT905   ";
    Ciphertext gin905 = 2 * gout902 + gout817;
    Ciphertext gout905 = ctx.eval_lut(&gin905, xnor2);
    std::cerr << "\rLUT906   ";
    Ciphertext gin906 = 2 * gout897 + gout901;
    Ciphertext gout906 = ctx.eval_lut(&gin906, nand2);
    std::cerr << "\rLUT907   ";
    Ciphertext gin907 = 2 * gout902 + gout817;
    Ciphertext gout907 = ctx.eval_lut(&gin907, nand2);
    std::cerr << "\rIMUX908  ";
    Ciphertext gin908A = 2 * gout879 + gout413;
    Ciphertext gout908A = ctx.eval_lut(&gin908A, and2);
    Ciphertext gin908B = 2 * gout900 + (1 - gout413);
    Ciphertext gout908B = ctx.eval_lut(&gin908B, and2);
    Ciphertext gout908 = gout908A + gout908B;
    std::cerr << "\rLUT909   ";
    Ciphertext gin909 = 2 * gout787 + gout899;
    Ciphertext gout909 = ctx.eval_lut(&gin909, nand2);
    std::cerr << "\rLUT910   ";
    Ciphertext gin910 = 2 * gout405 + gout903;
    Ciphertext gout910 = ctx.eval_lut(&gin910, and2);
    std::cerr << "\rLUT911   ";
    Ciphertext gin911 = 2 * gout904 + gout836;
    Ciphertext gout911 = ctx.eval_lut(&gin911, nand2);
    std::cerr << "\rLUT912   ";
    Ciphertext gin912 = 2 * gout818 + gout907;
    Ciphertext gout912 = ctx.eval_lut(&gin912, and2);
    std::cerr << "\rLUT913   ";
    Ciphertext gin913 = 2 * gout405 + gout908;
    Ciphertext gout913 = ctx.eval_lut(&gin913, nand2);
    std::cerr << "\rLUT914   ";
    Ciphertext gin914 = 2 * gout906 + gout886;
    Ciphertext gout914 = ctx.eval_lut(&gin914, nand2);
    std::cerr << "\rLUT915   ";
    Ciphertext gin915 = 2 * gout909 + gout807;
    Ciphertext gout915 = ctx.eval_lut(&gin915, or2);
    std::cerr << "\rLUT916   ";
    Ciphertext gin916 = 2 * gout909 + gout807;
    Ciphertext gout916 = ctx.eval_lut(&gin916, nand2);
    std::cerr << "\rLUT917   ";
    Ciphertext gin917 = 2 * gout912 + gout835;
    Ciphertext gout917 = ctx.eval_lut(&gin917, or2);
    std::cerr << "\rLUT918   ";
    Ciphertext gin918 = 2 * gout911 + gout823;
    Ciphertext gout918 = ctx.eval_lut(&gin918, nand2);
    std::cerr << "\rLUT919   ";
    Ciphertext gin919 = 2 * gout911 + gout823;
    Ciphertext gout919 = ctx.eval_lut(&gin919, xnor2);
    std::cerr << "\rLUT920   ";
    Ciphertext gin920 = 2 * gout912 + gout835;
    Ciphertext gout920 = ctx.eval_lut(&gin920, nand2);
    std::cerr << "\rLUT921   ";
    Ciphertext gin921 = 2 * gout475 + gout915;
    Ciphertext gout921 = ctx.eval_lut(&gin921, and2);
    std::cerr << "\rIMUX922  ";
    Ciphertext gin922A = 2 * gout905 + gout413;
    Ciphertext gout922A = ctx.eval_lut(&gin922A, and2);
    Ciphertext gin922B = 2 * gout919 + (1 - gout413);
    Ciphertext gout922B = ctx.eval_lut(&gin922B, and2);
    Ciphertext gout922 = gout922A + gout922B;
    std::cerr << "\rLUT923   ";
    Ciphertext gin923 = 2 * gout413 + gout917;
    Ciphertext gout923 = ctx.eval_lut(&gin923, and2);
    std::cerr << "\rLUT924   ";
    Ciphertext gin924 = 2 * gout826 + gout918;
    Ciphertext gout924 = ctx.eval_lut(&gin924, and2);
    std::cerr << "\rLUT925   ";
    Ciphertext gin925 = 2 * gout916 + gout921;
    Ciphertext gout925 = ctx.eval_lut(&gin925, nand2);
    std::cerr << "\rLUT926   ";
    Ciphertext gin926 = 2 * gout405 + gout922;
    Ciphertext gout926 = ctx.eval_lut(&gin926, nand2);
    std::cerr << "\rLUT927   ";
    Ciphertext gin927 = 2 * gout924 + gout820;
    Ciphertext gout927 = ctx.eval_lut(&gin927, nand2);
    std::cerr << "\rLUT928   ";
    Ciphertext gin928 = 2 * gout924 + gout820;
    Ciphertext gout928 = ctx.eval_lut(&gin928, or2);
    std::cerr << "\rLUT929   ";
    Ciphertext gin929 = 2 * gout925 + gout910;
    Ciphertext gout929 = ctx.eval_lut(&gin929, nand2);
    std::cerr << "\rLUT930   ";
    Ciphertext gin930 = 2 * gout920 + gout923;
    Ciphertext gout930 = ctx.eval_lut(&gin930, nand2);
    std::cerr << "\rLUT931   ";
    Ciphertext gin931 = 2 * gout475 + gout928;
    Ciphertext gout931 = ctx.eval_lut(&gin931, and2);
    std::cerr << "\rLUT932   ";
    Ciphertext gin932 = 2 * gout927 + gout931;
    Ciphertext gout932 = ctx.eval_lut(&gin932, nand2);
    std::cerr << "\rLUT933   ";
    Ciphertext gin933 = 2 * gout405 + gout932;
    Ciphertext gout933 = ctx.eval_lut(&gin933, and2);
    std::cerr << "\rLUT934   ";
    Ciphertext gin934 = 2 * gout930 + gout933;
    Ciphertext gout934 = ctx.eval_lut(&gin934, nand2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout583)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout644)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout705)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout771)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout799)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout838)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout854)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout865)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout875)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout892)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout894)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout914)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout913)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout929)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout926)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout934)); /* out15 */
    return test_out;
}

static void
BM_calculator_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_calculator_boolean(ctx);
    }
}

BENCHMARK(BM_calculator_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
