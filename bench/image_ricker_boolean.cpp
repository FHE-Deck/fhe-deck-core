#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_image_ricker_boolean(FHEContext& ctx, std::vector<uint64_t> window = { 0 })
{
    Ciphertext ct_window0 = ctx.encrypt_public((window[0] >> 0) & 1);
    Ciphertext ct_window1 = ctx.encrypt_public((window[0] >> 1) & 1);
    Ciphertext ct_window2 = ctx.encrypt_public((window[0] >> 2) & 1);
    Ciphertext ct_window3 = ctx.encrypt_public((window[0] >> 3) & 1);
    Ciphertext ct_window4 = ctx.encrypt_public((window[0] >> 4) & 1);
    Ciphertext ct_window5 = ctx.encrypt_public((window[0] >> 5) & 1);
    Ciphertext ct_window6 = ctx.encrypt_public((window[0] >> 6) & 1);
    Ciphertext ct_window7 = ctx.encrypt_public((window[0] >> 7) & 1);
    Ciphertext ct_window8 = ctx.encrypt_public((window[0] >> 8) & 1);
    Ciphertext ct_window9 = ctx.encrypt_public((window[0] >> 9) & 1);
    Ciphertext ct_window10 = ctx.encrypt_public((window[0] >> 10) & 1);
    Ciphertext ct_window11 = ctx.encrypt_public((window[0] >> 11) & 1);
    Ciphertext ct_window12 = ctx.encrypt_public((window[0] >> 12) & 1);
    Ciphertext ct_window13 = ctx.encrypt_public((window[0] >> 13) & 1);
    Ciphertext ct_window14 = ctx.encrypt_public((window[0] >> 14) & 1);
    Ciphertext ct_window15 = ctx.encrypt_public((window[0] >> 15) & 1);
    Ciphertext ct_window16 = ctx.encrypt_public((window[0] >> 16) & 1);
    Ciphertext ct_window17 = ctx.encrypt_public((window[0] >> 17) & 1);
    Ciphertext ct_window18 = ctx.encrypt_public((window[0] >> 18) & 1);
    Ciphertext ct_window19 = ctx.encrypt_public((window[0] >> 19) & 1);
    Ciphertext ct_window20 = ctx.encrypt_public((window[0] >> 20) & 1);
    Ciphertext ct_window21 = ctx.encrypt_public((window[0] >> 21) & 1);
    Ciphertext ct_window22 = ctx.encrypt_public((window[0] >> 22) & 1);
    Ciphertext ct_window23 = ctx.encrypt_public((window[0] >> 23) & 1);
    Ciphertext ct_window24 = ctx.encrypt_public((window[0] >> 24) & 1);
    Ciphertext ct_window25 = ctx.encrypt_public((window[0] >> 25) & 1);
    Ciphertext ct_window26 = ctx.encrypt_public((window[0] >> 26) & 1);
    Ciphertext ct_window27 = ctx.encrypt_public((window[0] >> 27) & 1);
    Ciphertext ct_window28 = ctx.encrypt_public((window[0] >> 28) & 1);
    Ciphertext ct_window29 = ctx.encrypt_public((window[0] >> 29) & 1);
    Ciphertext ct_window30 = ctx.encrypt_public((window[0] >> 30) & 1);
    Ciphertext ct_window31 = ctx.encrypt_public((window[0] >> 31) & 1);
    Ciphertext ct_window32 = ctx.encrypt_public((window[0] >> 32) & 1);
    Ciphertext ct_window33 = ctx.encrypt_public((window[0] >> 33) & 1);
    Ciphertext ct_window34 = ctx.encrypt_public((window[0] >> 34) & 1);
    Ciphertext ct_window35 = ctx.encrypt_public((window[0] >> 35) & 1);
    Ciphertext ct_window36 = ctx.encrypt_public((window[0] >> 36) & 1);
    Ciphertext ct_window37 = ctx.encrypt_public((window[0] >> 37) & 1);
    Ciphertext ct_window38 = ctx.encrypt_public((window[0] >> 38) & 1);
    Ciphertext ct_window39 = ctx.encrypt_public((window[0] >> 39) & 1);
    Ciphertext ct_window40 = ctx.encrypt_public((window[0] >> 40) & 1);
    Ciphertext ct_window41 = ctx.encrypt_public((window[0] >> 41) & 1);
    Ciphertext ct_window42 = ctx.encrypt_public((window[0] >> 42) & 1);
    Ciphertext ct_window43 = ctx.encrypt_public((window[0] >> 43) & 1);
    Ciphertext ct_window44 = ctx.encrypt_public((window[0] >> 44) & 1);
    Ciphertext ct_window45 = ctx.encrypt_public((window[0] >> 45) & 1);
    Ciphertext ct_window46 = ctx.encrypt_public((window[0] >> 46) & 1);
    Ciphertext ct_window47 = ctx.encrypt_public((window[0] >> 47) & 1);
    Ciphertext ct_window48 = ctx.encrypt_public((window[0] >> 48) & 1);
    Ciphertext ct_window49 = ctx.encrypt_public((window[0] >> 49) & 1);
    Ciphertext ct_window50 = ctx.encrypt_public((window[0] >> 50) & 1);
    Ciphertext ct_window51 = ctx.encrypt_public((window[0] >> 51) & 1);
    Ciphertext ct_window52 = ctx.encrypt_public((window[0] >> 52) & 1);
    Ciphertext ct_window53 = ctx.encrypt_public((window[0] >> 53) & 1);
    Ciphertext ct_window54 = ctx.encrypt_public((window[0] >> 54) & 1);
    Ciphertext ct_window55 = ctx.encrypt_public((window[0] >> 55) & 1);
    Ciphertext ct_window56 = ctx.encrypt_public((window[0] >> 56) & 1);
    Ciphertext ct_window57 = ctx.encrypt_public((window[0] >> 57) & 1);
    Ciphertext ct_window58 = ctx.encrypt_public((window[0] >> 58) & 1);
    Ciphertext ct_window59 = ctx.encrypt_public((window[0] >> 59) & 1);
    Ciphertext ct_window60 = ctx.encrypt_public((window[0] >> 60) & 1);
    Ciphertext ct_window61 = ctx.encrypt_public((window[0] >> 61) & 1);
    Ciphertext ct_window62 = ctx.encrypt_public((window[0] >> 62) & 1);
    Ciphertext ct_window63 = ctx.encrypt_public((window[0] >> 63) & 1);
    Ciphertext ct_window64 = ctx.encrypt_public((window[1] >> 0) & 1);
    Ciphertext ct_window65 = ctx.encrypt_public((window[1] >> 1) & 1);
    Ciphertext ct_window66 = ctx.encrypt_public((window[1] >> 2) & 1);
    Ciphertext ct_window67 = ctx.encrypt_public((window[1] >> 3) & 1);
    Ciphertext ct_window68 = ctx.encrypt_public((window[1] >> 4) & 1);
    Ciphertext ct_window69 = ctx.encrypt_public((window[1] >> 5) & 1);
    Ciphertext ct_window70 = ctx.encrypt_public((window[1] >> 6) & 1);
    Ciphertext ct_window71 = ctx.encrypt_public((window[1] >> 7) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_window60 + ct_window68;
    Ciphertext gout0 = ctx.eval_lut(&gin0, xor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_window21 + ct_window29;
    Ciphertext gout1 = ctx.eval_lut(&gin1, xor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_window42 + ct_window50;
    Ciphertext gout2 = ctx.eval_lut(&gin2, xor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_window45 + ct_window53;
    Ciphertext gout3 = ctx.eval_lut(&gin3, nand2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_window45 + ct_window53;
    Ciphertext gout4 = ctx.eval_lut(&gin4, xor2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_window62 + ct_window70;
    Ciphertext gout5 = ctx.eval_lut(&gin5, xor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_window59 + ct_window67;
    Ciphertext gout6 = ctx.eval_lut(&gin6, xor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_window19 + ct_window27;
    Ciphertext gout7 = ctx.eval_lut(&gin7, xor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_window62 + ct_window70;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nand2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_window56 + ct_window64;
    Ciphertext gout9 = ctx.eval_lut(&gin9, and2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_window16 + ct_window24;
    Ciphertext gout10 = ctx.eval_lut(&gin10, and2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_window57 + ct_window65;
    Ciphertext gout11 = ctx.eval_lut(&gin11, xor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_window19 + ct_window27;
    Ciphertext gout12 = ctx.eval_lut(&gin12, nand2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_window59 + ct_window67;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nand2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_window57 + ct_window65;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nand2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_window56 + ct_window64;
    Ciphertext gout15 = ctx.eval_lut(&gin15, xor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_window4 + ct_window12;
    Ciphertext gout16 = ctx.eval_lut(&gin16, xor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_window16 + ct_window24;
    Ciphertext gout17 = ctx.eval_lut(&gin17, xor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_window63 + ct_window71;
    Ciphertext gout18 = ctx.eval_lut(&gin18, nand2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_window21 + ct_window29;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nand2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_window18 + ct_window26;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nand2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_window18 + ct_window26;
    Ciphertext gout21 = ctx.eval_lut(&gin21, xor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_window58 + ct_window66;
    Ciphertext gout22 = ctx.eval_lut(&gin22, xor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_window58 + ct_window66;
    Ciphertext gout23 = ctx.eval_lut(&gin23, nand2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_window23 + ct_window31;
    Ciphertext gout24 = ctx.eval_lut(&gin24, xor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_window4 + ct_window12;
    Ciphertext gout25 = ctx.eval_lut(&gin25, nand2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_window61 + ct_window69;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nand2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_window20 + ct_window28;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nand2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_window5 + ct_window13;
    Ciphertext gout28 = ctx.eval_lut(&gin28, xor2);
    std::cerr << "\rINV29   ";
    Ciphertext gout29 = 1 - ct_window37;

    std::cerr << "\rINV30   ";
    Ciphertext gout30 = 1 - ct_window34;

    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_window47 + ct_window55;
    Ciphertext gout31 = ctx.eval_lut(&gin31, or2);
    std::cerr << "\rINV32   ";
    Ciphertext gout32 = 1 - gout28;

    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * gout9 + gout11;
    Ciphertext gout33 = ctx.eval_lut(&gin33, xor2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * ct_window47 + ct_window55;
    Ciphertext gout34 = ctx.eval_lut(&gin34, nand2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * ct_window47 + ct_window55;
    Ciphertext gout35 = ctx.eval_lut(&gin35, xor2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * ct_window0 + ct_window8;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * ct_window1 + ct_window9;
    Ciphertext gout37 = ctx.eval_lut(&gin37, xor2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * ct_window61 + ct_window69;
    Ciphertext gout38 = ctx.eval_lut(&gin38, xor2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * ct_window7 + ct_window15;
    Ciphertext gout39 = ctx.eval_lut(&gin39, xor2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * ct_window1 + ct_window9;
    Ciphertext gout40 = ctx.eval_lut(&gin40, nand2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_window5 + ct_window13;
    Ciphertext gout41 = ctx.eval_lut(&gin41, nand2);
    std::cerr << "\rINV42   ";
    Ciphertext gout42 = 1 - ct_window39;

    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * ct_window3 + ct_window11;
    Ciphertext gout43 = ctx.eval_lut(&gin43, nand2);
    std::cerr << "\rINV44   ";
    Ciphertext gout44 = 1 - ct_window33;

    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * ct_window6 + ct_window14;
    Ciphertext gout45 = ctx.eval_lut(&gin45, xor2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * ct_window6 + ct_window14;
    Ciphertext gout46 = ctx.eval_lut(&gin46, nand2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * ct_window44 + ct_window52;
    Ciphertext gout47 = ctx.eval_lut(&gin47, nand2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * ct_window46 + ct_window54;
    Ciphertext gout48 = ctx.eval_lut(&gin48, xor2);
    std::cerr << "\rINV49   ";
    Ciphertext gout49 = 1 - ct_window36;

    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_window2 + ct_window10;
    Ciphertext gout50 = ctx.eval_lut(&gin50, xor2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * ct_window43 + ct_window51;
    Ciphertext gout51 = ctx.eval_lut(&gin51, xor2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * ct_window3 + ct_window11;
    Ciphertext gout52 = ctx.eval_lut(&gin52, xor2);
    std::cerr << "\rINV53   ";
    Ciphertext gout53 = 1 - ct_window35;

    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * ct_window23 + ct_window31;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_window43 + ct_window51;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * ct_window0 + ct_window8;
    Ciphertext gout56 = ctx.eval_lut(&gin56, xor2);
    std::cerr << "\rINV57   ";
    Ciphertext gout57 = 1 - ct_window38;

    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_window63 + ct_window71;
    Ciphertext gout58 = ctx.eval_lut(&gin58, xor2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * ct_window20 + ct_window28;
    Ciphertext gout59 = ctx.eval_lut(&gin59, xor2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * ct_window60 + ct_window68;
    Ciphertext gout60 = ctx.eval_lut(&gin60, nand2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * ct_window17 + ct_window25;
    Ciphertext gout61 = ctx.eval_lut(&gin61, xor2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * gout9 + gout11;
    Ciphertext gout62 = ctx.eval_lut(&gin62, nand2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * gout10 + gout61;
    Ciphertext gout63 = ctx.eval_lut(&gin63, nand2);
    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * gout36 + gout37;
    Ciphertext gout64 = ctx.eval_lut(&gin64, xnor2);
    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * gout10 + gout61;
    Ciphertext gout65 = ctx.eval_lut(&gin65, xor2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_window7 + ct_window15;
    Ciphertext gout66 = ctx.eval_lut(&gin66, nand2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_window44 + ct_window52;
    Ciphertext gout67 = ctx.eval_lut(&gin67, xor2);
    std::cerr << "\rINV68   ";
    Ciphertext gout68 = 1 - ct_window32;

    std::cerr << "\rINV69   ";
    Ciphertext gout69 = 1 - gout45;

    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * gout14 + gout62;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_window2 + ct_window10;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_window46 + ct_window54;
    Ciphertext gout72 = ctx.eval_lut(&gin72, nand2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * ct_window40 + ct_window48;
    Ciphertext gout73 = ctx.eval_lut(&gin73, and2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * ct_window41 + ct_window49;
    Ciphertext gout74 = ctx.eval_lut(&gin74, nand2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * ct_window22 + ct_window30;
    Ciphertext gout75 = ctx.eval_lut(&gin75, xor2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * ct_window41 + ct_window49;
    Ciphertext gout76 = ctx.eval_lut(&gin76, xor2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * ct_window22 + ct_window30;
    Ciphertext gout77 = ctx.eval_lut(&gin77, nand2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * ct_window42 + ct_window50;
    Ciphertext gout78 = ctx.eval_lut(&gin78, nand2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * ct_window40 + ct_window48;
    Ciphertext gout79 = ctx.eval_lut(&gin79, xor2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * ct_window17 + ct_window25;
    Ciphertext gout80 = ctx.eval_lut(&gin80, nand2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * gout56 + gout17;
    Ciphertext gout81 = ctx.eval_lut(&gin81, and2);
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * gout79 + gout15;
    Ciphertext gout82 = ctx.eval_lut(&gin82, xor2);
    std::cerr << "\rLUT83   ";
    Ciphertext gin83 = 2 * gout36 + gout37;
    Ciphertext gout83 = ctx.eval_lut(&gin83, nand2);
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * (1 - gout64) + gout65;
    Ciphertext gout84 = ctx.eval_lut(&gin84, and2);
    std::cerr << "\rINV85   ";
    Ciphertext gout85 = 1 - gout39;

    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * gout70 + gout22;
    Ciphertext gout86 = ctx.eval_lut(&gin86, xor2);
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * gout73 + gout76;
    Ciphertext gout87 = ctx.eval_lut(&gin87, nand2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout56 + gout17;
    Ciphertext gout88 = ctx.eval_lut(&gin88, xor2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * gout64 + gout65;
    Ciphertext gout89 = ctx.eval_lut(&gin89, xnor2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout80 + gout63;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout40 + gout83;
    Ciphertext gout91 = ctx.eval_lut(&gin91, nand2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout79 + gout15;
    Ciphertext gout92 = ctx.eval_lut(&gin92, and2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout74 + gout87;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout82 + gout88;
    Ciphertext gout94 = ctx.eval_lut(&gin94, and2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout73 + gout76;
    Ciphertext gout95 = ctx.eval_lut(&gin95, xnor2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout70 + gout22;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * gout81 + gout89;
    Ciphertext gout97 = ctx.eval_lut(&gin97, and2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout95 + gout33;
    Ciphertext gout98 = ctx.eval_lut(&gin98, xnor2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout93 + gout2;
    Ciphertext gout99 = ctx.eval_lut(&gin99, xnor2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout91 + gout50;
    Ciphertext gout100 = ctx.eval_lut(&gin100, xnor2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout84 + gout97;
    Ciphertext gout101 = ctx.eval_lut(&gin101, or2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * (1 - gout99) + gout86;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * (1 - gout95) + gout33;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout99 + gout86;
    Ciphertext gout104 = ctx.eval_lut(&gin104, xnor2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * gout81 + gout89;
    Ciphertext gout105 = ctx.eval_lut(&gin105, xnor2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * gout90 + gout21;
    Ciphertext gout106 = ctx.eval_lut(&gin106, xor2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * gout90 + gout21;
    Ciphertext gout107 = ctx.eval_lut(&gin107, nand2);
    std::cerr << "\rLUT108   ";
    Ciphertext gin108 = 2 * gout23 + gout96;
    Ciphertext gout108 = ctx.eval_lut(&gin108, nand2);
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * gout91 + gout50;
    Ciphertext gout109 = ctx.eval_lut(&gin109, nand2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * gout93 + gout2;
    Ciphertext gout110 = ctx.eval_lut(&gin110, nand2);
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * gout20 + gout107;
    Ciphertext gout111 = ctx.eval_lut(&gin111, nand2);
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * gout92 + gout98;
    Ciphertext gout112 = ctx.eval_lut(&gin112, xor2);
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * gout98 + gout94;
    Ciphertext gout113 = ctx.eval_lut(&gin113, nand2);
    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * gout78 + gout110;
    Ciphertext gout114 = ctx.eval_lut(&gin114, nand2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * gout94 + gout112;
    Ciphertext gout115 = ctx.eval_lut(&gin115, or2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * gout92 + gout98;
    Ciphertext gout116 = ctx.eval_lut(&gin116, and2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * (1 - gout100) + gout106;
    Ciphertext gout117 = ctx.eval_lut(&gin117, and2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout108 + gout6;
    Ciphertext gout118 = ctx.eval_lut(&gin118, nand2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * gout108 + gout6;
    Ciphertext gout119 = ctx.eval_lut(&gin119, xor2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * gout13 + gout118;
    Ciphertext gout120 = ctx.eval_lut(&gin120, nand2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * gout111 + gout7;
    Ciphertext gout121 = ctx.eval_lut(&gin121, xor2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * gout103 + gout116;
    Ciphertext gout122 = ctx.eval_lut(&gin122, or2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * gout114 + gout51;
    Ciphertext gout123 = ctx.eval_lut(&gin123, xnor2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * gout114 + gout51;
    Ciphertext gout124 = ctx.eval_lut(&gin124, nand2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * gout71 + gout109;
    Ciphertext gout125 = ctx.eval_lut(&gin125, nand2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * (1 - gout123) + gout119;
    Ciphertext gout126 = ctx.eval_lut(&gin126, and2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * gout100 + gout106;
    Ciphertext gout127 = ctx.eval_lut(&gin127, xnor2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * gout122 + gout104;
    Ciphertext gout128 = ctx.eval_lut(&gin128, xnor2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * gout123 + gout119;
    Ciphertext gout129 = ctx.eval_lut(&gin129, xnor2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * gout55 + gout124;
    Ciphertext gout130 = ctx.eval_lut(&gin130, nand2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * gout105 + gout113;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * gout111 + gout7;
    Ciphertext gout132 = ctx.eval_lut(&gin132, nand2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * gout122 + gout104;
    Ciphertext gout133 = ctx.eval_lut(&gin133, and2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * gout12 + gout132;
    Ciphertext gout134 = ctx.eval_lut(&gin134, nand2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * gout134 + gout59;
    Ciphertext gout135 = ctx.eval_lut(&gin135, xor2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * gout101 + gout127;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * gout130 + gout67;
    Ciphertext gout137 = ctx.eval_lut(&gin137, xnor2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * gout130 + gout67;
    Ciphertext gout138 = ctx.eval_lut(&gin138, nand2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * gout101 + gout127;
    Ciphertext gout139 = ctx.eval_lut(&gin139, xnor2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * gout120 + gout0;
    Ciphertext gout140 = ctx.eval_lut(&gin140, xor2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * gout120 + gout0;
    Ciphertext gout141 = ctx.eval_lut(&gin141, nand2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * gout134 + gout59;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * gout125 + gout52;
    Ciphertext gout143 = ctx.eval_lut(&gin143, nand2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * gout125 + gout52;
    Ciphertext gout144 = ctx.eval_lut(&gin144, xnor2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * gout131 + gout115;
    Ciphertext gout145 = ctx.eval_lut(&gin145, nand2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * gout47 + gout138;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * gout144 + gout121;
    Ciphertext gout147 = ctx.eval_lut(&gin147, xnor2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * gout139 + gout128;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * gout137 + gout140;
    Ciphertext gout149 = ctx.eval_lut(&gin149, xnor2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * gout60 + gout141;
    Ciphertext gout150 = ctx.eval_lut(&gin150, nand2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * gout139 + gout128;
    Ciphertext gout151 = ctx.eval_lut(&gin151, or2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * (1 - gout137) + gout140;
    Ciphertext gout152 = ctx.eval_lut(&gin152, and2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * gout27 + gout142;
    Ciphertext gout153 = ctx.eval_lut(&gin153, nand2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * gout102 + gout133;
    Ciphertext gout154 = ctx.eval_lut(&gin154, or2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * gout117 + gout136;
    Ciphertext gout155 = ctx.eval_lut(&gin155, or2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * gout146 + gout4;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * gout43 + gout143;
    Ciphertext gout157 = ctx.eval_lut(&gin157, nand2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * gout154 + gout129;
    Ciphertext gout158 = ctx.eval_lut(&gin158, and2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * gout3 + gout156;
    Ciphertext gout159 = ctx.eval_lut(&gin159, nand2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * (1 - gout144) + gout121;
    Ciphertext gout160 = ctx.eval_lut(&gin160, and2);
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * gout159 + gout48;
    Ciphertext gout161 = ctx.eval_lut(&gin161, xnor2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * gout154 + gout129;
    Ciphertext gout162 = ctx.eval_lut(&gin162, xor2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * gout155 + gout147;
    Ciphertext gout163 = ctx.eval_lut(&gin163, and2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * gout150 + gout38;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * gout155 + gout147;
    Ciphertext gout165 = ctx.eval_lut(&gin165, xor2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * gout160 + gout163;
    Ciphertext gout166 = ctx.eval_lut(&gin166, or2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * gout153 + gout1;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * gout150 + gout38;
    Ciphertext gout168 = ctx.eval_lut(&gin168, xor2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * gout26 + gout164;
    Ciphertext gout169 = ctx.eval_lut(&gin169, nand2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * gout151 + gout145;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout19 + gout167;
    Ciphertext gout171 = ctx.eval_lut(&gin171, nand2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout157 + gout16;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * gout146 + gout4;
    Ciphertext gout173 = ctx.eval_lut(&gin173, xnor2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout157 + gout16;
    Ciphertext gout174 = ctx.eval_lut(&gin174, xnor2);
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * gout173 + gout168;
    Ciphertext gout175 = ctx.eval_lut(&gin175, xnor2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * (1 - gout173) + gout168;
    Ciphertext gout176 = ctx.eval_lut(&gin176, and2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * gout165 + gout162;
    Ciphertext gout177 = ctx.eval_lut(&gin177, xor2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * gout165 + gout162;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * gout153 + gout1;
    Ciphertext gout179 = ctx.eval_lut(&gin179, xnor2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout169 + gout5;
    Ciphertext gout180 = ctx.eval_lut(&gin180, nand2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * gout171 + gout75;
    Ciphertext gout181 = ctx.eval_lut(&gin181, xor2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * gout126 + gout158;
    Ciphertext gout182 = ctx.eval_lut(&gin182, or2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * gout171 + gout75;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout25 + gout172;
    Ciphertext gout184 = ctx.eval_lut(&gin184, and2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * gout77 + gout183;
    Ciphertext gout185 = ctx.eval_lut(&gin185, nand2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * gout8 + gout180;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * gout159 + gout48;
    Ciphertext gout187 = ctx.eval_lut(&gin187, nand2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * (1 - gout174) + gout135;
    Ciphertext gout188 = ctx.eval_lut(&gin188, and2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * gout182 + gout149;
    Ciphertext gout189 = ctx.eval_lut(&gin189, xor2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout174 + gout135;
    Ciphertext gout190 = ctx.eval_lut(&gin190, xnor2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout182 + gout149;
    Ciphertext gout191 = ctx.eval_lut(&gin191, and2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * gout169 + gout5;
    Ciphertext gout192 = ctx.eval_lut(&gin192, xor2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout148 + gout170;
    Ciphertext gout193 = ctx.eval_lut(&gin193, and2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * gout161 + gout192;
    Ciphertext gout194 = ctx.eval_lut(&gin194, xnor2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * gout185 + gout24;
    Ciphertext gout195 = ctx.eval_lut(&gin195, xor2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout152 + gout191;
    Ciphertext gout196 = ctx.eval_lut(&gin196, or2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout185 + gout24;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * gout32 + gout184;
    Ciphertext gout198 = ctx.eval_lut(&gin198, or2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout177 + gout193;
    Ciphertext gout199 = ctx.eval_lut(&gin199, xor2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout166 + gout190;
    Ciphertext gout200 = ctx.eval_lut(&gin200, and2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout177 + gout193;
    Ciphertext gout201 = ctx.eval_lut(&gin201, nand2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout166 + gout190;
    Ciphertext gout202 = ctx.eval_lut(&gin202, xnor2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout28 + gout184;
    Ciphertext gout203 = ctx.eval_lut(&gin203, xnor2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * (1 - gout179) + gout203;
    Ciphertext gout204 = ctx.eval_lut(&gin204, or2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout186 + gout58;
    Ciphertext gout205 = ctx.eval_lut(&gin205, xnor2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout54 + gout197;
    Ciphertext gout206 = ctx.eval_lut(&gin206, nand2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout72 + gout187;
    Ciphertext gout207 = ctx.eval_lut(&gin207, and2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout186 + gout58;
    Ciphertext gout208 = ctx.eval_lut(&gin208, nand2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout188 + gout200;
    Ciphertext gout209 = ctx.eval_lut(&gin209, or2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout68 + gout199;
    Ciphertext gout210 = ctx.eval_lut(&gin210, xnor2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout68 + gout199;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout178 + gout201;
    Ciphertext gout212 = ctx.eval_lut(&gin212, nand2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout41 + gout198;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout196 + gout175;
    Ciphertext gout214 = ctx.eval_lut(&gin214, xor2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout196 + gout175;
    Ciphertext gout215 = ctx.eval_lut(&gin215, and2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout202 + gout189;
    Ciphertext gout216 = ctx.eval_lut(&gin216, xnor2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * (1 - gout161) + gout192;
    Ciphertext gout217 = ctx.eval_lut(&gin217, and2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout179 + gout203;
    Ciphertext gout218 = ctx.eval_lut(&gin218, xnor2);
    std::cerr << "\rINV219   ";
    Ciphertext gout219 = 1 - gout211;

    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout213 + gout45;
    Ciphertext gout220 = ctx.eval_lut(&gin220, nand2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout207 + gout34;
    Ciphertext gout221 = ctx.eval_lut(&gin221, nand2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout212 + gout216;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout212 + gout216;
    Ciphertext gout223 = ctx.eval_lut(&gin223, xnor2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout207 + gout35;
    Ciphertext gout224 = ctx.eval_lut(&gin224, xnor2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout179 + (1 - gout203);
    Ciphertext gout225 = ctx.eval_lut(&gin225, or2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout209 + gout218;
    Ciphertext gout226 = ctx.eval_lut(&gin226, xnor2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * (1 - gout202) + gout189;
    Ciphertext gout227 = ctx.eval_lut(&gin227, and2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout18 + gout208;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout46 + gout220;
    Ciphertext gout229 = ctx.eval_lut(&gin229, nand2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout209 + gout204;
    Ciphertext gout230 = ctx.eval_lut(&gin230, nand2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout176 + gout215;
    Ciphertext gout231 = ctx.eval_lut(&gin231, or2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * ct_window33 + gout223;
    Ciphertext gout232 = ctx.eval_lut(&gin232, nand2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * (1 - gout214) + gout226;
    Ciphertext gout233 = ctx.eval_lut(&gin233, or2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout205 + gout224;
    Ciphertext gout234 = ctx.eval_lut(&gin234, xnor2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout213 + gout69;
    Ciphertext gout235 = ctx.eval_lut(&gin235, xnor2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout214 + gout226;
    Ciphertext gout236 = ctx.eval_lut(&gin236, xnor2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout229 + gout39;
    Ciphertext gout237 = ctx.eval_lut(&gin237, nand2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * (1 - gout227) + gout222;
    Ciphertext gout238 = ctx.eval_lut(&gin238, and2);
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout31 + gout221;
    Ciphertext gout239 = ctx.eval_lut(&gin239, and2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout205 + (1 - gout224);
    Ciphertext gout240 = ctx.eval_lut(&gin240, or2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout44 + gout223;
    Ciphertext gout241 = ctx.eval_lut(&gin241, xnor2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout235 + gout181;
    Ciphertext gout242 = ctx.eval_lut(&gin242, nand2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * (1 - gout236) + gout238;
    Ciphertext gout243 = ctx.eval_lut(&gin243, or2);
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout66 + gout237;
    Ciphertext gout244 = ctx.eval_lut(&gin244, nand2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * gout231 + gout194;
    Ciphertext gout245 = ctx.eval_lut(&gin245, and2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout235 + gout181;
    Ciphertext gout246 = ctx.eval_lut(&gin246, xor2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout236 + gout238;
    Ciphertext gout247 = ctx.eval_lut(&gin247, xnor2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout233 + gout243;
    Ciphertext gout248 = ctx.eval_lut(&gin248, nand2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout231 + gout194;
    Ciphertext gout249 = ctx.eval_lut(&gin249, xor2);
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout228 + gout239;
    Ciphertext gout250 = ctx.eval_lut(&gin250, nand2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout225 + gout230;
    Ciphertext gout251 = ctx.eval_lut(&gin251, and2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout229 + gout85;
    Ciphertext gout252 = ctx.eval_lut(&gin252, xnor2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout219 + gout241;
    Ciphertext gout253 = ctx.eval_lut(&gin253, xnor2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout228 + gout239;
    Ciphertext gout254 = ctx.eval_lut(&gin254, xor2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout244 + gout206;
    Ciphertext gout255 = ctx.eval_lut(&gin255, xor2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout30 + gout247;
    Ciphertext gout256 = ctx.eval_lut(&gin256, or2);
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * ct_window34 + gout247;
    Ciphertext gout257 = ctx.eval_lut(&gin257, xnor2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout252 + gout195;
    Ciphertext gout258 = ctx.eval_lut(&gin258, nand2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout252 + gout195;
    Ciphertext gout259 = ctx.eval_lut(&gin259, xor2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * gout217 + gout245;
    Ciphertext gout260 = ctx.eval_lut(&gin260, or2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout244 + gout206;
    Ciphertext gout261 = ctx.eval_lut(&gin261, nand2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout211 + gout241;
    Ciphertext gout262 = ctx.eval_lut(&gin262, nand2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout234 + gout260;
    Ciphertext gout263 = ctx.eval_lut(&gin263, xor2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout234 + gout260;
    Ciphertext gout264 = ctx.eval_lut(&gin264, nand2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout240 + gout264;
    Ciphertext gout265 = ctx.eval_lut(&gin265, nand2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout210 + (1 - gout241);
    Ciphertext gout266 = ctx.eval_lut(&gin266, or2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout246 + gout251;
    Ciphertext gout267 = ctx.eval_lut(&gin267, xnor2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout265 + gout254;
    Ciphertext gout268 = ctx.eval_lut(&gin268, xor2);
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout232 + gout262;
    Ciphertext gout269 = ctx.eval_lut(&gin269, nand2);
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout267 + gout249;
    Ciphertext gout270 = ctx.eval_lut(&gin270, xor2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * (1 - gout246) + gout251;
    Ciphertext gout271 = ctx.eval_lut(&gin271, or2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout257 + gout269;
    Ciphertext gout272 = ctx.eval_lut(&gin272, nand2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout267 + gout249;
    Ciphertext gout273 = ctx.eval_lut(&gin273, nand2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * (1 - gout266) + gout257;
    Ciphertext gout274 = ctx.eval_lut(&gin274, and2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout210 + gout253;
    Ciphertext gout275 = ctx.eval_lut(&gin275, nand2);
    std::cerr << "\rINV276   ";
    Ciphertext gout276 = 1 - gout274;

    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout257 + gout269;
    Ciphertext gout277 = ctx.eval_lut(&gin277, xnor2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout248 + gout270;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout265 + gout254;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout248 + gout270;
    Ciphertext gout280 = ctx.eval_lut(&gin280, xnor2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout242 + gout271;
    Ciphertext gout281 = ctx.eval_lut(&gin281, and2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout256 + gout272;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout273 + gout278;
    Ciphertext gout283 = ctx.eval_lut(&gin283, nand2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * (1 - gout259) + gout281;
    Ciphertext gout284 = ctx.eval_lut(&gin284, or2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * ct_window35 + gout280;
    Ciphertext gout285 = ctx.eval_lut(&gin285, nand2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout250 + gout279;
    Ciphertext gout286 = ctx.eval_lut(&gin286, nand2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout258 + gout284;
    Ciphertext gout287 = ctx.eval_lut(&gin287, nand2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout53 + gout280;
    Ciphertext gout288 = ctx.eval_lut(&gin288, xnor2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout259 + gout281;
    Ciphertext gout289 = ctx.eval_lut(&gin289, xnor2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout289 + gout263;
    Ciphertext gout290 = ctx.eval_lut(&gin290, xor2);
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout282 + gout288;
    Ciphertext gout291 = ctx.eval_lut(&gin291, nand2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout287 + gout255;
    Ciphertext gout292 = ctx.eval_lut(&gin292, xnor2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout287 + gout255;
    Ciphertext gout293 = ctx.eval_lut(&gin293, nand2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout283 + gout290;
    Ciphertext gout294 = ctx.eval_lut(&gin294, xor2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout283 + gout290;
    Ciphertext gout295 = ctx.eval_lut(&gin295, nand2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout285 + gout291;
    Ciphertext gout296 = ctx.eval_lut(&gin296, nand2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout261 + gout293;
    Ciphertext gout297 = ctx.eval_lut(&gin297, nand2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout289 + gout263;
    Ciphertext gout298 = ctx.eval_lut(&gin298, nand2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout282 + gout288;
    Ciphertext gout299 = ctx.eval_lut(&gin299, xnor2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * (1 - gout292) + gout268;
    Ciphertext gout300 = ctx.eval_lut(&gin300, and2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout297 + gout286;
    Ciphertext gout301 = ctx.eval_lut(&gin301, xor2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout297 + gout286;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout298 + gout295;
    Ciphertext gout303 = ctx.eval_lut(&gin303, nand2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout292 + gout268;
    Ciphertext gout304 = ctx.eval_lut(&gin304, xnor2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * ct_window36 + gout294;
    Ciphertext gout305 = ctx.eval_lut(&gin305, xnor2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout49 + gout294;
    Ciphertext gout306 = ctx.eval_lut(&gin306, or2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout303 + gout304;
    Ciphertext gout307 = ctx.eval_lut(&gin307, and2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout274 + gout299;
    Ciphertext gout308 = ctx.eval_lut(&gin308, and2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout303 + gout304;
    Ciphertext gout309 = ctx.eval_lut(&gin309, xor2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout305 + gout296;
    Ciphertext gout310 = ctx.eval_lut(&gin310, xnor2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout308 + gout310;
    Ciphertext gout311 = ctx.eval_lut(&gin311, and2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout305 + gout296;
    Ciphertext gout312 = ctx.eval_lut(&gin312, nand2);
    std::cerr << "\rINV313   ";
    Ciphertext gout313 = 1 - gout308;

    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout300 + gout307;
    Ciphertext gout314 = ctx.eval_lut(&gin314, or2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout29 + gout309;
    Ciphertext gout315 = ctx.eval_lut(&gin315, or2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * ct_window37 + gout309;
    Ciphertext gout316 = ctx.eval_lut(&gin316, xnor2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout306 + gout312;
    Ciphertext gout317 = ctx.eval_lut(&gin317, nand2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout314 + gout301;
    Ciphertext gout318 = ctx.eval_lut(&gin318, xnor2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout57 + gout318;
    Ciphertext gout319 = ctx.eval_lut(&gin319, xnor2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout314 + gout301;
    Ciphertext gout320 = ctx.eval_lut(&gin320, nand2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout316 + gout317;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout316 + gout317;
    Ciphertext gout322 = ctx.eval_lut(&gin322, xnor2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout302 + gout320;
    Ciphertext gout323 = ctx.eval_lut(&gin323, and2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * ct_window39 + gout323;
    Ciphertext gout324 = ctx.eval_lut(&gin324, nand2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * ct_window38 + gout318;
    Ciphertext gout325 = ctx.eval_lut(&gin325, nand2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout311 + gout322;
    Ciphertext gout326 = ctx.eval_lut(&gin326, and2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout315 + gout321;
    Ciphertext gout327 = ctx.eval_lut(&gin327, nand2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout42 + gout323;
    Ciphertext gout328 = ctx.eval_lut(&gin328, xnor2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout327 + gout319;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout327 + gout319;
    Ciphertext gout330 = ctx.eval_lut(&gin330, xnor2);
    std::cerr << "\rINV331   ";
    Ciphertext gout331 = 1 - gout328;

    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout325 + gout329;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout326 + gout330;
    Ciphertext gout333 = ctx.eval_lut(&gin333, and2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout332 + gout328;
    Ciphertext gout334 = ctx.eval_lut(&gin334, xnor2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout332 + gout331;
    Ciphertext gout335 = ctx.eval_lut(&gin335, and2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * (1 - gout333) + gout334;
    Ciphertext gout336 = ctx.eval_lut(&gin336, or2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout332 + gout331;
    Ciphertext gout337 = ctx.eval_lut(&gin337, or2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout324 + gout337;
    Ciphertext gout338 = ctx.eval_lut(&gin338, nand2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout326 + gout338;
    Ciphertext gout339 = ctx.eval_lut(&gin339, or2);
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout336 + gout338;
    Ciphertext gout340 = ctx.eval_lut(&gin340, nand2);
    std::cerr << "\rLUT341   ";
    Ciphertext gin341 = 2 * gout266 + gout340;
    Ciphertext gout341 = ctx.eval_lut(&gin341, and2);
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout276 + gout340;
    Ciphertext gout342 = ctx.eval_lut(&gin342, and2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout313 + gout340;
    Ciphertext gout343 = ctx.eval_lut(&gin343, and2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout311 + gout338;
    Ciphertext gout344 = ctx.eval_lut(&gin344, or2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout341 + gout277;
    Ciphertext gout345 = ctx.eval_lut(&gin345, xnor2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout333 + gout338;
    Ciphertext gout346 = ctx.eval_lut(&gin346, or2);
    std::cerr << "\rLUT347   ";
    Ciphertext gin347 = 2 * gout310 + gout343;
    Ciphertext gout347 = ctx.eval_lut(&gin347, xnor2);
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout330 + gout339;
    Ciphertext gout348 = ctx.eval_lut(&gin348, xor2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout322 + gout344;
    Ciphertext gout349 = ctx.eval_lut(&gin349, xor2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * (1 - gout253) + gout340;
    Ciphertext gout350 = ctx.eval_lut(&gin350, or2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout299 + gout342;
    Ciphertext gout351 = ctx.eval_lut(&gin351, xnor2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout275 + gout341;
    Ciphertext gout352 = ctx.eval_lut(&gin352, nand2);
    std::cerr << "\rIMUX353  ";
    Ciphertext gin353A = 2 * gout334 + gout346;
    Ciphertext gout353A = ctx.eval_lut(&gin353A, and2);
    Ciphertext gin353B = 2 * gout335 + (1 - gout346);
    Ciphertext gout353B = ctx.eval_lut(&gin353B, and2);
    Ciphertext gout353 = gout353A + gout353B;
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout352 + gout350;
    Ciphertext gout354 = ctx.eval_lut(&gin354, nand2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout210)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout354)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout345)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout351)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout347)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout349)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout348)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout353)); /* out7 */
    return test_out;
}

static void
BM_image_ricker_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(rlwe_hom_acc_scheme_C_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_image_ricker_boolean(ctx);
    }
}

BENCHMARK(BM_image_ricker_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
