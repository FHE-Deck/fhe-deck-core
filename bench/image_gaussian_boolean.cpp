#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_image_gaussian_boolean(FHEContext& ctx, std::vector<uint64_t> window = { 0 })
{
    Ciphertext ct_window0 = ctx.encrypt_public((window[0] >> 0) & 1);
    Ciphertext ct_window1 = ctx.encrypt_public((window[0] >> 1) & 1);
    Ciphertext ct_window2 = ctx.encrypt_public((window[0] >> 2) & 1);
    Ciphertext ct_window3 = ctx.encrypt_public((window[0] >> 3) & 1);
    Ciphertext ct_window4 = ctx.encrypt_public((window[0] >> 4) & 1);
    Ciphertext ct_window5 = ctx.encrypt_public((window[0] >> 5) & 1);
    Ciphertext ct_window6 = ctx.encrypt_public((window[0] >> 6) & 1);
    Ciphertext ct_window7 = ctx.encrypt_public((window[0] >> 7) & 1);
    Ciphertext ct_window8 = ctx.encrypt_public((window[0] >> 8) & 1);
    Ciphertext ct_window9 = ctx.encrypt_public((window[0] >> 9) & 1);
    Ciphertext ct_window10 = ctx.encrypt_public((window[0] >> 10) & 1);
    Ciphertext ct_window11 = ctx.encrypt_public((window[0] >> 11) & 1);
    Ciphertext ct_window12 = ctx.encrypt_public((window[0] >> 12) & 1);
    Ciphertext ct_window13 = ctx.encrypt_public((window[0] >> 13) & 1);
    Ciphertext ct_window14 = ctx.encrypt_public((window[0] >> 14) & 1);
    Ciphertext ct_window15 = ctx.encrypt_public((window[0] >> 15) & 1);
    Ciphertext ct_window16 = ctx.encrypt_public((window[0] >> 16) & 1);
    Ciphertext ct_window17 = ctx.encrypt_public((window[0] >> 17) & 1);
    Ciphertext ct_window18 = ctx.encrypt_public((window[0] >> 18) & 1);
    Ciphertext ct_window19 = ctx.encrypt_public((window[0] >> 19) & 1);
    Ciphertext ct_window20 = ctx.encrypt_public((window[0] >> 20) & 1);
    Ciphertext ct_window21 = ctx.encrypt_public((window[0] >> 21) & 1);
    Ciphertext ct_window22 = ctx.encrypt_public((window[0] >> 22) & 1);
    Ciphertext ct_window23 = ctx.encrypt_public((window[0] >> 23) & 1);
    Ciphertext ct_window24 = ctx.encrypt_public((window[0] >> 24) & 1);
    Ciphertext ct_window25 = ctx.encrypt_public((window[0] >> 25) & 1);
    Ciphertext ct_window26 = ctx.encrypt_public((window[0] >> 26) & 1);
    Ciphertext ct_window27 = ctx.encrypt_public((window[0] >> 27) & 1);
    Ciphertext ct_window28 = ctx.encrypt_public((window[0] >> 28) & 1);
    Ciphertext ct_window29 = ctx.encrypt_public((window[0] >> 29) & 1);
    Ciphertext ct_window30 = ctx.encrypt_public((window[0] >> 30) & 1);
    Ciphertext ct_window31 = ctx.encrypt_public((window[0] >> 31) & 1);
    Ciphertext ct_window32 = ctx.encrypt_public((window[0] >> 32) & 1);
    Ciphertext ct_window33 = ctx.encrypt_public((window[0] >> 33) & 1);
    Ciphertext ct_window34 = ctx.encrypt_public((window[0] >> 34) & 1);
    Ciphertext ct_window35 = ctx.encrypt_public((window[0] >> 35) & 1);
    Ciphertext ct_window36 = ctx.encrypt_public((window[0] >> 36) & 1);
    Ciphertext ct_window37 = ctx.encrypt_public((window[0] >> 37) & 1);
    Ciphertext ct_window38 = ctx.encrypt_public((window[0] >> 38) & 1);
    Ciphertext ct_window39 = ctx.encrypt_public((window[0] >> 39) & 1);
    Ciphertext ct_window40 = ctx.encrypt_public((window[0] >> 40) & 1);
    Ciphertext ct_window41 = ctx.encrypt_public((window[0] >> 41) & 1);
    Ciphertext ct_window42 = ctx.encrypt_public((window[0] >> 42) & 1);
    Ciphertext ct_window43 = ctx.encrypt_public((window[0] >> 43) & 1);
    Ciphertext ct_window44 = ctx.encrypt_public((window[0] >> 44) & 1);
    Ciphertext ct_window45 = ctx.encrypt_public((window[0] >> 45) & 1);
    Ciphertext ct_window46 = ctx.encrypt_public((window[0] >> 46) & 1);
    Ciphertext ct_window47 = ctx.encrypt_public((window[0] >> 47) & 1);
    Ciphertext ct_window48 = ctx.encrypt_public((window[0] >> 48) & 1);
    Ciphertext ct_window49 = ctx.encrypt_public((window[0] >> 49) & 1);
    Ciphertext ct_window50 = ctx.encrypt_public((window[0] >> 50) & 1);
    Ciphertext ct_window51 = ctx.encrypt_public((window[0] >> 51) & 1);
    Ciphertext ct_window52 = ctx.encrypt_public((window[0] >> 52) & 1);
    Ciphertext ct_window53 = ctx.encrypt_public((window[0] >> 53) & 1);
    Ciphertext ct_window54 = ctx.encrypt_public((window[0] >> 54) & 1);
    Ciphertext ct_window55 = ctx.encrypt_public((window[0] >> 55) & 1);
    Ciphertext ct_window56 = ctx.encrypt_public((window[0] >> 56) & 1);
    Ciphertext ct_window57 = ctx.encrypt_public((window[0] >> 57) & 1);
    Ciphertext ct_window58 = ctx.encrypt_public((window[0] >> 58) & 1);
    Ciphertext ct_window59 = ctx.encrypt_public((window[0] >> 59) & 1);
    Ciphertext ct_window60 = ctx.encrypt_public((window[0] >> 60) & 1);
    Ciphertext ct_window61 = ctx.encrypt_public((window[0] >> 61) & 1);
    Ciphertext ct_window62 = ctx.encrypt_public((window[0] >> 62) & 1);
    Ciphertext ct_window63 = ctx.encrypt_public((window[0] >> 63) & 1);
    Ciphertext ct_window64 = ctx.encrypt_public((window[1] >> 0) & 1);
    Ciphertext ct_window65 = ctx.encrypt_public((window[1] >> 1) & 1);
    Ciphertext ct_window66 = ctx.encrypt_public((window[1] >> 2) & 1);
    Ciphertext ct_window67 = ctx.encrypt_public((window[1] >> 3) & 1);
    Ciphertext ct_window68 = ctx.encrypt_public((window[1] >> 4) & 1);
    Ciphertext ct_window69 = ctx.encrypt_public((window[1] >> 5) & 1);
    Ciphertext ct_window70 = ctx.encrypt_public((window[1] >> 6) & 1);
    Ciphertext ct_window71 = ctx.encrypt_public((window[1] >> 7) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rINV0   ";
    Ciphertext gout0 = 1 - ct_window18;

    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_window69 + ct_window60;
    Ciphertext gout1 = ctx.eval_lut(&gin1, xor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_window54 + ct_window45;
    Ciphertext gout2 = ctx.eval_lut(&gin2, nand2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_window54 + ct_window45;
    Ciphertext gout3 = ctx.eval_lut(&gin3, xor2);
    std::cerr << "\rINV4   ";
    Ciphertext gout4 = 1 - ct_window17;

    std::cerr << "\rINV5   ";
    Ciphertext gout5 = 1 - ct_window15;

    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_window1 + ct_window8;
    Ciphertext gout6 = ctx.eval_lut(&gin6, xor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_window6 + ct_window13;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nand2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_window5 + ct_window12;
    Ciphertext gout8 = ctx.eval_lut(&gin8, xor2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_window52 + ct_window43;
    Ciphertext gout9 = ctx.eval_lut(&gin9, xor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_window26 + ct_window33;
    Ciphertext gout10 = ctx.eval_lut(&gin10, xor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_window55 + ct_window46;
    Ciphertext gout11 = ctx.eval_lut(&gin11, xor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_window16 + ct_window0;
    Ciphertext gout12 = ctx.eval_lut(&gin12, and2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_window52 + ct_window43;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nand2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_window26 + ct_window33;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nand2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_window55 + ct_window46;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nand2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_window27 + ct_window34;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nand2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_window66 + ct_window57;
    Ciphertext gout17 = ctx.eval_lut(&gin17, xor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_window25 + ct_window32;
    Ciphertext gout18 = ctx.eval_lut(&gin18, and2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_window25 + ct_window32;
    Ciphertext gout19 = ctx.eval_lut(&gin19, xor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_window66 + ct_window57;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nand2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_window5 + ct_window12;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nand2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_window70 + ct_window61;
    Ciphertext gout22 = ctx.eval_lut(&gin22, or2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_window49 + ct_window40;
    Ciphertext gout23 = ctx.eval_lut(&gin23, xor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_window70 + ct_window61;
    Ciphertext gout24 = ctx.eval_lut(&gin24, nand2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_window67 + ct_window58;
    Ciphertext gout25 = ctx.eval_lut(&gin25, xor2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_window30 + ct_window37;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nand2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_window27 + ct_window34;
    Ciphertext gout27 = ctx.eval_lut(&gin27, xor2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_window3 + ct_window10;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nand2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_window3 + ct_window10;
    Ciphertext gout29 = ctx.eval_lut(&gin29, xor2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_window65 + ct_window56;
    Ciphertext gout30 = ctx.eval_lut(&gin30, and2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_window30 + ct_window37;
    Ciphertext gout31 = ctx.eval_lut(&gin31, xor2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * ct_window68 + ct_window59;
    Ciphertext gout32 = ctx.eval_lut(&gin32, xor2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_window2 + ct_window9;
    Ciphertext gout33 = ctx.eval_lut(&gin33, xor2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * ct_window71 + ct_window62;
    Ciphertext gout34 = ctx.eval_lut(&gin34, xor2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * ct_window29 + ct_window36;
    Ciphertext gout35 = ctx.eval_lut(&gin35, xor2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * ct_window67 + ct_window58;
    Ciphertext gout36 = ctx.eval_lut(&gin36, nand2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * ct_window70 + ct_window61;
    Ciphertext gout37 = ctx.eval_lut(&gin37, xor2);
    std::cerr << "\rINV38   ";
    Ciphertext gout38 = 1 - ct_window23;

    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * ct_window2 + ct_window9;
    Ciphertext gout39 = ctx.eval_lut(&gin39, nand2);
    std::cerr << "\rINV40   ";
    Ciphertext gout40 = 1 - ct_window63;

    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_window4 + ct_window11;
    Ciphertext gout41 = ctx.eval_lut(&gin41, xor2);
    std::cerr << "\rINV42   ";
    Ciphertext gout42 = 1 - ct_window39;

    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * ct_window4 + ct_window11;
    Ciphertext gout43 = ctx.eval_lut(&gin43, nand2);
    std::cerr << "\rINV44   ";
    Ciphertext gout44 = 1 - ct_window19;

    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * ct_window28 + ct_window35;
    Ciphertext gout45 = ctx.eval_lut(&gin45, nand2);
    std::cerr << "\rINV46   ";
    Ciphertext gout46 = 1 - ct_window22;

    std::cerr << "\rINV47   ";
    Ciphertext gout47 = 1 - ct_window20;

    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * ct_window51 + ct_window42;
    Ciphertext gout48 = ctx.eval_lut(&gin48, xor2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * ct_window53 + ct_window44;
    Ciphertext gout49 = ctx.eval_lut(&gin49, xor2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_window53 + ct_window44;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * ct_window68 + ct_window59;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * ct_window71 + ct_window62;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * ct_window65 + ct_window56;
    Ciphertext gout53 = ctx.eval_lut(&gin53, xor2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * ct_window31 + ct_window38;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_window1 + ct_window8;
    Ciphertext gout55 = ctx.eval_lut(&gin55, and2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * ct_window49 + ct_window40;
    Ciphertext gout56 = ctx.eval_lut(&gin56, and2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * ct_window48 + ct_window64;
    Ciphertext gout57 = ctx.eval_lut(&gin57, xor2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_window50 + ct_window41;
    Ciphertext gout58 = ctx.eval_lut(&gin58, nand2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * ct_window31 + ct_window38;
    Ciphertext gout59 = ctx.eval_lut(&gin59, xor2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * ct_window48 + ct_window64;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * ct_window7 + ct_window14;
    Ciphertext gout61 = ctx.eval_lut(&gin61, xor2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * ct_window29 + ct_window36;
    Ciphertext gout62 = ctx.eval_lut(&gin62, nand2);
    std::cerr << "\rINV63   ";
    Ciphertext gout63 = 1 - ct_window47;

    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * ct_window50 + ct_window41;
    Ciphertext gout64 = ctx.eval_lut(&gin64, xor2);
    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * ct_window16 + ct_window0;
    Ciphertext gout65 = ctx.eval_lut(&gin65, xor2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_window28 + ct_window35;
    Ciphertext gout66 = ctx.eval_lut(&gin66, xor2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_window7 + ct_window14;
    Ciphertext gout67 = ctx.eval_lut(&gin67, nand2);
    std::cerr << "\rINV68   ";
    Ciphertext gout68 = 1 - ct_window21;

    std::cerr << "\rINV69   ";
    Ciphertext gout69 = 1 - ct_window24;

    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_window69 + ct_window60;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_window51 + ct_window42;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_window6 + ct_window13;
    Ciphertext gout72 = ctx.eval_lut(&gin72, xor2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * gout30 + gout17;
    Ciphertext gout73 = ctx.eval_lut(&gin73, xor2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * gout18 + gout10;
    Ciphertext gout74 = ctx.eval_lut(&gin74, nand2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * gout30 + gout17;
    Ciphertext gout75 = ctx.eval_lut(&gin75, nand2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * gout18 + gout10;
    Ciphertext gout76 = ctx.eval_lut(&gin76, xor2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * ct_window17 + gout6;
    Ciphertext gout77 = ctx.eval_lut(&gin77, nand2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * gout4 + gout6;
    Ciphertext gout78 = ctx.eval_lut(&gin78, xnor2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * gout23 + gout53;
    Ciphertext gout79 = ctx.eval_lut(&gin79, nand2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * gout23 + gout53;
    Ciphertext gout80 = ctx.eval_lut(&gin80, xor2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * gout55 + gout33;
    Ciphertext gout81 = ctx.eval_lut(&gin81, nand2);
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * gout57 + gout65;
    Ciphertext gout82 = ctx.eval_lut(&gin82, and2);
    std::cerr << "\rLUT83   ";
    Ciphertext gin83 = 2 * gout55 + gout33;
    Ciphertext gout83 = ctx.eval_lut(&gin83, xor2);
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * gout56 + gout64;
    Ciphertext gout84 = ctx.eval_lut(&gin84, nand2);
    std::cerr << "\rLUT85   ";
    Ciphertext gin85 = 2 * gout56 + gout64;
    Ciphertext gout85 = ctx.eval_lut(&gin85, xor2);
    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * gout14 + gout74;
    Ciphertext gout86 = ctx.eval_lut(&gin86, nand2);
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * gout20 + gout75;
    Ciphertext gout87 = ctx.eval_lut(&gin87, nand2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout60 + gout80;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * gout60 + gout80;
    Ciphertext gout89 = ctx.eval_lut(&gin89, xnor2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout80 + gout82;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout12 + gout78;
    Ciphertext gout91 = ctx.eval_lut(&gin91, nand2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout0 + gout83;
    Ciphertext gout92 = ctx.eval_lut(&gin92, xnor2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout12 + gout78;
    Ciphertext gout93 = ctx.eval_lut(&gin93, xor2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout39 + gout81;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout85 + gout73;
    Ciphertext gout95 = ctx.eval_lut(&gin95, xor2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout58 + gout84;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * ct_window18 + gout83;
    Ciphertext gout97 = ctx.eval_lut(&gin97, nand2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout85 + gout73;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout79 + gout88;
    Ciphertext gout99 = ctx.eval_lut(&gin99, nand2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout87 + gout25;
    Ciphertext gout100 = ctx.eval_lut(&gin100, xor2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout86 + gout27;
    Ciphertext gout101 = ctx.eval_lut(&gin101, nand2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout86 + gout27;
    Ciphertext gout102 = ctx.eval_lut(&gin102, xor2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout87 + gout25;
    Ciphertext gout103 = ctx.eval_lut(&gin103, nand2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout77 + gout91;
    Ciphertext gout104 = ctx.eval_lut(&gin104, nand2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * ct_window24 + gout93;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * gout82 + (1 - gout89);
    Ciphertext gout106 = ctx.eval_lut(&gin106, or2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * gout69 + gout93;
    Ciphertext gout107 = ctx.eval_lut(&gin107, xnor2);
    std::cerr << "\rLUT108   ";
    Ciphertext gin108 = 2 * gout94 + gout29;
    Ciphertext gout108 = ctx.eval_lut(&gin108, nand2);
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * gout94 + gout29;
    Ciphertext gout109 = ctx.eval_lut(&gin109, xor2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * gout99 + gout95;
    Ciphertext gout110 = ctx.eval_lut(&gin110, nand2);
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * gout96 + gout48;
    Ciphertext gout111 = ctx.eval_lut(&gin111, xnor2);
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * gout96 + gout48;
    Ciphertext gout112 = ctx.eval_lut(&gin112, nand2);
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * gout99 + gout95;
    Ciphertext gout113 = ctx.eval_lut(&gin113, xor2);
    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * gout16 + gout101;
    Ciphertext gout114 = ctx.eval_lut(&gin114, nand2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * gout104 + gout92;
    Ciphertext gout115 = ctx.eval_lut(&gin115, xnor2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * gout36 + gout103;
    Ciphertext gout116 = ctx.eval_lut(&gin116, nand2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * gout104 + gout92;
    Ciphertext gout117 = ctx.eval_lut(&gin117, nand2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout106 + gout107;
    Ciphertext gout118 = ctx.eval_lut(&gin118, nand2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * gout97 + gout117;
    Ciphertext gout119 = ctx.eval_lut(&gin119, nand2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * gout98 + gout110;
    Ciphertext gout120 = ctx.eval_lut(&gin120, nand2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * gout111 + gout100;
    Ciphertext gout121 = ctx.eval_lut(&gin121, xnor2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * gout44 + gout109;
    Ciphertext gout122 = ctx.eval_lut(&gin122, xnor2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * gout71 + gout112;
    Ciphertext gout123 = ctx.eval_lut(&gin123, nand2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * gout28 + gout108;
    Ciphertext gout124 = ctx.eval_lut(&gin124, nand2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * ct_window19 + gout109;
    Ciphertext gout125 = ctx.eval_lut(&gin125, nand2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * gout111 + (1 - gout100);
    Ciphertext gout126 = ctx.eval_lut(&gin126, or2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * gout114 + gout66;
    Ciphertext gout127 = ctx.eval_lut(&gin127, nand2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * (1 - gout115) + gout19;
    Ciphertext gout128 = ctx.eval_lut(&gin128, and2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * gout114 + gout66;
    Ciphertext gout129 = ctx.eval_lut(&gin129, xor2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * gout115 + gout19;
    Ciphertext gout130 = ctx.eval_lut(&gin130, xnor2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * gout116 + gout32;
    Ciphertext gout131 = ctx.eval_lut(&gin131, xor2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * gout90 + gout118;
    Ciphertext gout132 = ctx.eval_lut(&gin132, nand2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * gout123 + gout9;
    Ciphertext gout133 = ctx.eval_lut(&gin133, nand2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * gout123 + gout9;
    Ciphertext gout134 = ctx.eval_lut(&gin134, xnor2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * gout116 + gout32;
    Ciphertext gout135 = ctx.eval_lut(&gin135, nand2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * gout119 + gout122;
    Ciphertext gout136 = ctx.eval_lut(&gin136, xor2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * gout119 + gout122;
    Ciphertext gout137 = ctx.eval_lut(&gin137, nand2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * gout120 + gout121;
    Ciphertext gout138 = ctx.eval_lut(&gin138, xnor2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * gout120 + gout121;
    Ciphertext gout139 = ctx.eval_lut(&gin139, nand2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * gout124 + gout41;
    Ciphertext gout140 = ctx.eval_lut(&gin140, xor2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * gout125 + gout137;
    Ciphertext gout141 = ctx.eval_lut(&gin141, nand2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * gout124 + gout41;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * gout45 + gout127;
    Ciphertext gout143 = ctx.eval_lut(&gin143, nand2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * gout105 + gout130;
    Ciphertext gout144 = ctx.eval_lut(&gin144, and2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * gout134 + gout131;
    Ciphertext gout145 = ctx.eval_lut(&gin145, xnor2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * gout13 + gout133;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * gout132 + gout113;
    Ciphertext gout147 = ctx.eval_lut(&gin147, nor2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * gout132 + gout113;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * gout105 + gout130;
    Ciphertext gout149 = ctx.eval_lut(&gin149, xnor2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * gout134 + (1 - gout131);
    Ciphertext gout150 = ctx.eval_lut(&gin150, or2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * gout126 + gout139;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * gout76 + gout136;
    Ciphertext gout152 = ctx.eval_lut(&gin152, nand2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * gout76 + gout136;
    Ciphertext gout153 = ctx.eval_lut(&gin153, xor2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * gout51 + gout135;
    Ciphertext gout154 = ctx.eval_lut(&gin154, nand2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * gout47 + gout140;
    Ciphertext gout155 = ctx.eval_lut(&gin155, xnor2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * ct_window20 + gout140;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * gout43 + gout142;
    Ciphertext gout157 = ctx.eval_lut(&gin157, nand2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * gout143 + gout35;
    Ciphertext gout158 = ctx.eval_lut(&gin158, xor2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * gout143 + gout35;
    Ciphertext gout159 = ctx.eval_lut(&gin159, nand2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * gout128 + gout144;
    Ciphertext gout160 = ctx.eval_lut(&gin160, or2);
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * gout146 + gout49;
    Ciphertext gout161 = ctx.eval_lut(&gin161, nand2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * gout147 + gout149;
    Ciphertext gout162 = ctx.eval_lut(&gin162, or2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * gout146 + gout49;
    Ciphertext gout163 = ctx.eval_lut(&gin163, xnor2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * gout151 + gout145;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * gout160 + gout153;
    Ciphertext gout165 = ctx.eval_lut(&gin165, xnor2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * gout160 + gout153;
    Ciphertext gout166 = ctx.eval_lut(&gin166, nand2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * gout151 + gout145;
    Ciphertext gout167 = ctx.eval_lut(&gin167, xor2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * gout154 + gout1;
    Ciphertext gout168 = ctx.eval_lut(&gin168, xor2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * gout141 + gout155;
    Ciphertext gout169 = ctx.eval_lut(&gin169, nand2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * gout154 + gout1;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout141 + gout155;
    Ciphertext gout171 = ctx.eval_lut(&gin171, xnor2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout157 + gout8;
    Ciphertext gout172 = ctx.eval_lut(&gin172, xor2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * gout157 + gout8;
    Ciphertext gout173 = ctx.eval_lut(&gin173, nand2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout62 + gout159;
    Ciphertext gout174 = ctx.eval_lut(&gin174, nand2);
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * gout148 + gout162;
    Ciphertext gout175 = ctx.eval_lut(&gin175, and2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * gout50 + gout161;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * gout138 + gout165;
    Ciphertext gout177 = ctx.eval_lut(&gin177, or2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * gout138 + gout165;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * gout150 + gout164;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout171 + gout102;
    Ciphertext gout180 = ctx.eval_lut(&gin180, xnor2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * gout152 + gout166;
    Ciphertext gout181 = ctx.eval_lut(&gin181, nand2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * gout68 + gout172;
    Ciphertext gout182 = ctx.eval_lut(&gin182, xnor2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * (1 - gout171) + gout102;
    Ciphertext gout183 = ctx.eval_lut(&gin183, and2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout21 + gout173;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * gout163 + gout168;
    Ciphertext gout185 = ctx.eval_lut(&gin185, xnor2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * gout156 + gout169;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * gout163 + (1 - gout168);
    Ciphertext gout187 = ctx.eval_lut(&gin187, or2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout70 + gout170;
    Ciphertext gout188 = ctx.eval_lut(&gin188, and2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * ct_window21 + gout172;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout174 + gout31;
    Ciphertext gout190 = ctx.eval_lut(&gin190, xor2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout174 + gout31;
    Ciphertext gout191 = ctx.eval_lut(&gin191, nand2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * gout176 + gout3;
    Ciphertext gout192 = ctx.eval_lut(&gin192, xnor2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout176 + gout3;
    Ciphertext gout193 = ctx.eval_lut(&gin193, nand2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * gout177 + gout175;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * gout179 + gout185;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout181 + gout180;
    Ciphertext gout196 = ctx.eval_lut(&gin196, xor2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout188 + gout24;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * gout181 + gout180;
    Ciphertext gout198 = ctx.eval_lut(&gin198, and2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout184 + gout72;
    Ciphertext gout199 = ctx.eval_lut(&gin199, nand2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout179 + gout185;
    Ciphertext gout200 = ctx.eval_lut(&gin200, xor2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout188 + gout37;
    Ciphertext gout201 = ctx.eval_lut(&gin201, xnor2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout186 + gout182;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout186 + gout182;
    Ciphertext gout203 = ctx.eval_lut(&gin203, xor2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout184 + gout72;
    Ciphertext gout204 = ctx.eval_lut(&gin204, xor2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout2 + gout193;
    Ciphertext gout205 = ctx.eval_lut(&gin205, nand2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout26 + gout191;
    Ciphertext gout206 = ctx.eval_lut(&gin206, nand2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout178 + gout194;
    Ciphertext gout207 = ctx.eval_lut(&gin207, and2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout167 + gout196;
    Ciphertext gout208 = ctx.eval_lut(&gin208, xor2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout167 + gout196;
    Ciphertext gout209 = ctx.eval_lut(&gin209, nand2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout183 + gout198;
    Ciphertext gout210 = ctx.eval_lut(&gin210, or2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout7 + gout199;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout22 + gout197;
    Ciphertext gout212 = ctx.eval_lut(&gin212, and2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout187 + gout195;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout189 + gout202;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * ct_window22 + gout204;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout129 + gout203;
    Ciphertext gout216 = ctx.eval_lut(&gin216, xor2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout192 + gout201;
    Ciphertext gout217 = ctx.eval_lut(&gin217, xnor2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout192 + (1 - gout201);
    Ciphertext gout218 = ctx.eval_lut(&gin218, or2);
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout129 + gout203;
    Ciphertext gout219 = ctx.eval_lut(&gin219, nand2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout46 + gout204;
    Ciphertext gout220 = ctx.eval_lut(&gin220, xnor2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout205 + gout11;
    Ciphertext gout221 = ctx.eval_lut(&gin221, nand2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout206 + gout59;
    Ciphertext gout222 = ctx.eval_lut(&gin222, xor2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout205 + gout11;
    Ciphertext gout223 = ctx.eval_lut(&gin223, xnor2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout206 + gout59;
    Ciphertext gout224 = ctx.eval_lut(&gin224, nand2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout208 + gout207;
    Ciphertext gout225 = ctx.eval_lut(&gin225, xor2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout208 + gout207;
    Ciphertext gout226 = ctx.eval_lut(&gin226, nand2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout34 + gout212;
    Ciphertext gout227 = ctx.eval_lut(&gin227, nand2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout211 + gout61;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout211 + gout61;
    Ciphertext gout229 = ctx.eval_lut(&gin229, xor2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout34 + gout212;
    Ciphertext gout230 = ctx.eval_lut(&gin230, xor2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout210 + gout216;
    Ciphertext gout231 = ctx.eval_lut(&gin231, xnor2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout213 + gout217;
    Ciphertext gout232 = ctx.eval_lut(&gin232, xor2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout213 + gout217;
    Ciphertext gout233 = ctx.eval_lut(&gin233, nand2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout210 + gout216;
    Ciphertext gout234 = ctx.eval_lut(&gin234, nand2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout214 + gout220;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout219 + gout234;
    Ciphertext gout236 = ctx.eval_lut(&gin236, nand2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout214 + gout220;
    Ciphertext gout237 = ctx.eval_lut(&gin237, xnor2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout15 + gout221;
    Ciphertext gout238 = ctx.eval_lut(&gin238, nand2);
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout54 + gout224;
    Ciphertext gout239 = ctx.eval_lut(&gin239, nand2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout209 + gout226;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * ct_window23 + gout229;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout38 + gout229;
    Ciphertext gout242 = ctx.eval_lut(&gin242, xnor2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * gout67 + gout228;
    Ciphertext gout243 = ctx.eval_lut(&gin243, nand2);
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout231 + gout200;
    Ciphertext gout244 = ctx.eval_lut(&gin244, xnor2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * (1 - gout231) + gout200;
    Ciphertext gout245 = ctx.eval_lut(&gin245, and2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout223 + (1 - gout230);
    Ciphertext gout246 = ctx.eval_lut(&gin246, or2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout52 + gout227;
    Ciphertext gout247 = ctx.eval_lut(&gin247, nand2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout223 + gout230;
    Ciphertext gout248 = ctx.eval_lut(&gin248, xnor2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout218 + gout233;
    Ciphertext gout249 = ctx.eval_lut(&gin249, nand2);
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * (1 - gout237) + gout158;
    Ciphertext gout250 = ctx.eval_lut(&gin250, and2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout237 + gout158;
    Ciphertext gout251 = ctx.eval_lut(&gin251, xnor2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout215 + gout235;
    Ciphertext gout252 = ctx.eval_lut(&gin252, nand2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * ct_window47 + gout238;
    Ciphertext gout253 = ctx.eval_lut(&gin253, and2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout63 + gout238;
    Ciphertext gout254 = ctx.eval_lut(&gin254, xnor2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout42 + gout239;
    Ciphertext gout255 = ctx.eval_lut(&gin255, xnor2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * ct_window39 + gout239;
    Ciphertext gout256 = ctx.eval_lut(&gin256, nand2);
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * ct_window15 + gout243;
    Ciphertext gout257 = ctx.eval_lut(&gin257, nand2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout240 + gout244;
    Ciphertext gout258 = ctx.eval_lut(&gin258, and2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout240 + gout244;
    Ciphertext gout259 = ctx.eval_lut(&gin259, xor2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * ct_window63 + gout247;
    Ciphertext gout260 = ctx.eval_lut(&gin260, and2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout5 + gout243;
    Ciphertext gout261 = ctx.eval_lut(&gin261, xnor2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout40 + gout247;
    Ciphertext gout262 = ctx.eval_lut(&gin262, xnor2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout249 + gout248;
    Ciphertext gout263 = ctx.eval_lut(&gin263, xor2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout249 + gout248;
    Ciphertext gout264 = ctx.eval_lut(&gin264, nand2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout236 + gout251;
    Ciphertext gout265 = ctx.eval_lut(&gin265, and2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout252 + gout242;
    Ciphertext gout266 = ctx.eval_lut(&gin266, nand2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout236 + gout251;
    Ciphertext gout267 = ctx.eval_lut(&gin267, xor2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout252 + gout242;
    Ciphertext gout268 = ctx.eval_lut(&gin268, xnor2);
    std::cerr << "\rINV269   ";
    Ciphertext gout269 = 1 - gout256;

    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout253 + gout260;
    Ciphertext gout270 = ctx.eval_lut(&gin270, nand2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout254 + gout262;
    Ciphertext gout271 = ctx.eval_lut(&gin271, xor2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout245 + gout258;
    Ciphertext gout272 = ctx.eval_lut(&gin272, or2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout254 + gout262;
    Ciphertext gout273 = ctx.eval_lut(&gin273, nand2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout253 + gout260;
    Ciphertext gout274 = ctx.eval_lut(&gin274, xor2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout246 + gout264;
    Ciphertext gout275 = ctx.eval_lut(&gin275, nand2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * (1 - gout268) + gout190;
    Ciphertext gout276 = ctx.eval_lut(&gin276, and2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout241 + gout266;
    Ciphertext gout277 = ctx.eval_lut(&gin277, nand2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout250 + gout265;
    Ciphertext gout278 = ctx.eval_lut(&gin278, or2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout232 + gout267;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout268 + gout190;
    Ciphertext gout280 = ctx.eval_lut(&gin280, xnor2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout232 + gout267;
    Ciphertext gout281 = ctx.eval_lut(&gin281, xor2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout275 + gout271;
    Ciphertext gout282 = ctx.eval_lut(&gin282, xor2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout275 + gout271;
    Ciphertext gout283 = ctx.eval_lut(&gin283, nand2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout277 + gout261;
    Ciphertext gout284 = ctx.eval_lut(&gin284, nand2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout278 + gout280;
    Ciphertext gout285 = ctx.eval_lut(&gin285, and2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout277 + gout261;
    Ciphertext gout286 = ctx.eval_lut(&gin286, xnor2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout272 + gout281;
    Ciphertext gout287 = ctx.eval_lut(&gin287, nand2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout278 + gout280;
    Ciphertext gout288 = ctx.eval_lut(&gin288, xnor2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout272 + gout281;
    Ciphertext gout289 = ctx.eval_lut(&gin289, xor2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout273 + gout283;
    Ciphertext gout290 = ctx.eval_lut(&gin290, nand2);
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout257 + gout284;
    Ciphertext gout291 = ctx.eval_lut(&gin291, nand2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout276 + gout285;
    Ciphertext gout292 = ctx.eval_lut(&gin292, or2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout288 + gout263;
    Ciphertext gout293 = ctx.eval_lut(&gin293, xnor2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * (1 - gout286) + gout222;
    Ciphertext gout294 = ctx.eval_lut(&gin294, and2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * (1 - gout288) + gout263;
    Ciphertext gout295 = ctx.eval_lut(&gin295, and2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout286 + gout222;
    Ciphertext gout296 = ctx.eval_lut(&gin296, xnor2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout279 + gout287;
    Ciphertext gout297 = ctx.eval_lut(&gin297, nand2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout290 + gout274;
    Ciphertext gout298 = ctx.eval_lut(&gin298, xor2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout290 + gout274;
    Ciphertext gout299 = ctx.eval_lut(&gin299, nand2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout291 + gout255;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout291 + gout255;
    Ciphertext gout301 = ctx.eval_lut(&gin301, xor2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout292 + gout296;
    Ciphertext gout302 = ctx.eval_lut(&gin302, xor2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout297 + gout293;
    Ciphertext gout303 = ctx.eval_lut(&gin303, and2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout292 + gout296;
    Ciphertext gout304 = ctx.eval_lut(&gin304, and2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout297 + gout293;
    Ciphertext gout305 = ctx.eval_lut(&gin305, xor2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout270 + gout299;
    Ciphertext gout306 = ctx.eval_lut(&gin306, nand2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout256 + gout300;
    Ciphertext gout307 = ctx.eval_lut(&gin307, nand2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout282 + gout302;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout295 + gout303;
    Ciphertext gout309 = ctx.eval_lut(&gin309, or2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout294 + gout304;
    Ciphertext gout310 = ctx.eval_lut(&gin310, or2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout282 + gout302;
    Ciphertext gout311 = ctx.eval_lut(&gin311, xor2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout310 + gout301;
    Ciphertext gout312 = ctx.eval_lut(&gin312, and2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout309 + gout311;
    Ciphertext gout313 = ctx.eval_lut(&gin313, xor2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout309 + gout311;
    Ciphertext gout314 = ctx.eval_lut(&gin314, nand2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout310 + gout301;
    Ciphertext gout315 = ctx.eval_lut(&gin315, xor2);
    std::cerr << "\rIMUX316  ";
    Ciphertext gin316A = 2 * gout256 + gout312;
    Ciphertext gout316A = ctx.eval_lut(&gin316A, and2);
    Ciphertext gin316B = 2 * gout307 + (1 - gout312);
    Ciphertext gout316B = ctx.eval_lut(&gin316B, and2);
    Ciphertext gout316 = gout316A + gout316B;
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout269 + gout312;
    Ciphertext gout317 = ctx.eval_lut(&gin317, nand2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout308 + gout314;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout315 + gout298;
    Ciphertext gout319 = ctx.eval_lut(&gin319, nand2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout315 + gout298;
    Ciphertext gout320 = ctx.eval_lut(&gin320, xor2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout316 + gout306;
    Ciphertext gout321 = ctx.eval_lut(&gin321, xor2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout316 + gout306;
    Ciphertext gout322 = ctx.eval_lut(&gin322, nand2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout318 + gout320;
    Ciphertext gout323 = ctx.eval_lut(&gin323, nand2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout318 + gout320;
    Ciphertext gout324 = ctx.eval_lut(&gin324, xor2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout317 + gout322;
    Ciphertext gout325 = ctx.eval_lut(&gin325, and2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout319 + gout323;
    Ciphertext gout326 = ctx.eval_lut(&gin326, nand2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout326 + gout321;
    Ciphertext gout327 = ctx.eval_lut(&gin327, xor2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout326 + gout321;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout328 + gout325;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout225)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout259)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout289)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout305)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout313)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout324)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout327)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout329)); /* out7 */
    return test_out;
}

static void
BM_image_gaussian_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_image_gaussian_boolean(ctx);
    }
}

BENCHMARK(BM_image_gaussian_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
