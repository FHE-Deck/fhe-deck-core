#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_image_gaussian_boolean(FHEContext& ctx, std::vector<uint64_t> window = { 0 })
{
    Ciphertext ct_window0 = ctx.encrypt_public((window[0] >> 0) & 1);
    Ciphertext ct_window1 = ctx.encrypt_public((window[0] >> 1) & 1);
    Ciphertext ct_window2 = ctx.encrypt_public((window[0] >> 2) & 1);
    Ciphertext ct_window3 = ctx.encrypt_public((window[0] >> 3) & 1);
    Ciphertext ct_window4 = ctx.encrypt_public((window[0] >> 4) & 1);
    Ciphertext ct_window5 = ctx.encrypt_public((window[0] >> 5) & 1);
    Ciphertext ct_window6 = ctx.encrypt_public((window[0] >> 6) & 1);
    Ciphertext ct_window7 = ctx.encrypt_public((window[0] >> 7) & 1);
    Ciphertext ct_window8 = ctx.encrypt_public((window[0] >> 8) & 1);
    Ciphertext ct_window9 = ctx.encrypt_public((window[0] >> 9) & 1);
    Ciphertext ct_window10 = ctx.encrypt_public((window[0] >> 10) & 1);
    Ciphertext ct_window11 = ctx.encrypt_public((window[0] >> 11) & 1);
    Ciphertext ct_window12 = ctx.encrypt_public((window[0] >> 12) & 1);
    Ciphertext ct_window13 = ctx.encrypt_public((window[0] >> 13) & 1);
    Ciphertext ct_window14 = ctx.encrypt_public((window[0] >> 14) & 1);
    Ciphertext ct_window15 = ctx.encrypt_public((window[0] >> 15) & 1);
    Ciphertext ct_window16 = ctx.encrypt_public((window[0] >> 16) & 1);
    Ciphertext ct_window17 = ctx.encrypt_public((window[0] >> 17) & 1);
    Ciphertext ct_window18 = ctx.encrypt_public((window[0] >> 18) & 1);
    Ciphertext ct_window19 = ctx.encrypt_public((window[0] >> 19) & 1);
    Ciphertext ct_window20 = ctx.encrypt_public((window[0] >> 20) & 1);
    Ciphertext ct_window21 = ctx.encrypt_public((window[0] >> 21) & 1);
    Ciphertext ct_window22 = ctx.encrypt_public((window[0] >> 22) & 1);
    Ciphertext ct_window23 = ctx.encrypt_public((window[0] >> 23) & 1);
    Ciphertext ct_window24 = ctx.encrypt_public((window[0] >> 24) & 1);
    Ciphertext ct_window25 = ctx.encrypt_public((window[0] >> 25) & 1);
    Ciphertext ct_window26 = ctx.encrypt_public((window[0] >> 26) & 1);
    Ciphertext ct_window27 = ctx.encrypt_public((window[0] >> 27) & 1);
    Ciphertext ct_window28 = ctx.encrypt_public((window[0] >> 28) & 1);
    Ciphertext ct_window29 = ctx.encrypt_public((window[0] >> 29) & 1);
    Ciphertext ct_window30 = ctx.encrypt_public((window[0] >> 30) & 1);
    Ciphertext ct_window31 = ctx.encrypt_public((window[0] >> 31) & 1);
    Ciphertext ct_window32 = ctx.encrypt_public((window[0] >> 32) & 1);
    Ciphertext ct_window33 = ctx.encrypt_public((window[0] >> 33) & 1);
    Ciphertext ct_window34 = ctx.encrypt_public((window[0] >> 34) & 1);
    Ciphertext ct_window35 = ctx.encrypt_public((window[0] >> 35) & 1);
    Ciphertext ct_window36 = ctx.encrypt_public((window[0] >> 36) & 1);
    Ciphertext ct_window37 = ctx.encrypt_public((window[0] >> 37) & 1);
    Ciphertext ct_window38 = ctx.encrypt_public((window[0] >> 38) & 1);
    Ciphertext ct_window39 = ctx.encrypt_public((window[0] >> 39) & 1);
    Ciphertext ct_window40 = ctx.encrypt_public((window[0] >> 40) & 1);
    Ciphertext ct_window41 = ctx.encrypt_public((window[0] >> 41) & 1);
    Ciphertext ct_window42 = ctx.encrypt_public((window[0] >> 42) & 1);
    Ciphertext ct_window43 = ctx.encrypt_public((window[0] >> 43) & 1);
    Ciphertext ct_window44 = ctx.encrypt_public((window[0] >> 44) & 1);
    Ciphertext ct_window45 = ctx.encrypt_public((window[0] >> 45) & 1);
    Ciphertext ct_window46 = ctx.encrypt_public((window[0] >> 46) & 1);
    Ciphertext ct_window47 = ctx.encrypt_public((window[0] >> 47) & 1);
    Ciphertext ct_window48 = ctx.encrypt_public((window[0] >> 48) & 1);
    Ciphertext ct_window49 = ctx.encrypt_public((window[0] >> 49) & 1);
    Ciphertext ct_window50 = ctx.encrypt_public((window[0] >> 50) & 1);
    Ciphertext ct_window51 = ctx.encrypt_public((window[0] >> 51) & 1);
    Ciphertext ct_window52 = ctx.encrypt_public((window[0] >> 52) & 1);
    Ciphertext ct_window53 = ctx.encrypt_public((window[0] >> 53) & 1);
    Ciphertext ct_window54 = ctx.encrypt_public((window[0] >> 54) & 1);
    Ciphertext ct_window55 = ctx.encrypt_public((window[0] >> 55) & 1);
    Ciphertext ct_window56 = ctx.encrypt_public((window[0] >> 56) & 1);
    Ciphertext ct_window57 = ctx.encrypt_public((window[0] >> 57) & 1);
    Ciphertext ct_window58 = ctx.encrypt_public((window[0] >> 58) & 1);
    Ciphertext ct_window59 = ctx.encrypt_public((window[0] >> 59) & 1);
    Ciphertext ct_window60 = ctx.encrypt_public((window[0] >> 60) & 1);
    Ciphertext ct_window61 = ctx.encrypt_public((window[0] >> 61) & 1);
    Ciphertext ct_window62 = ctx.encrypt_public((window[0] >> 62) & 1);
    Ciphertext ct_window63 = ctx.encrypt_public((window[0] >> 63) & 1);
    Ciphertext ct_window64 = ctx.encrypt_public((window[1] >> 0) & 1);
    Ciphertext ct_window65 = ctx.encrypt_public((window[1] >> 1) & 1);
    Ciphertext ct_window66 = ctx.encrypt_public((window[1] >> 2) & 1);
    Ciphertext ct_window67 = ctx.encrypt_public((window[1] >> 3) & 1);
    Ciphertext ct_window68 = ctx.encrypt_public((window[1] >> 4) & 1);
    Ciphertext ct_window69 = ctx.encrypt_public((window[1] >> 5) & 1);
    Ciphertext ct_window70 = ctx.encrypt_public((window[1] >> 6) & 1);
    Ciphertext ct_window71 = ctx.encrypt_public((window[1] >> 7) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_window26 + ct_window33;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nand2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_window26 + ct_window33;
    Ciphertext gout1 = ctx.eval_lut(&gin1, xor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_window66 + ct_window57;
    Ciphertext gout2 = ctx.eval_lut(&gin2, xor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_window27 + ct_window34;
    Ciphertext gout3 = ctx.eval_lut(&gin3, xor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_window3 + ct_window10;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nand2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_window25 + ct_window32;
    Ciphertext gout5 = ctx.eval_lut(&gin5, xor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_window5 + ct_window12;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nand2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_window27 + ct_window34;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nand2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_window70 + ct_window61;
    Ciphertext gout8 = ctx.eval_lut(&gin8, or2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_window66 + ct_window57;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nand2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_window49 + ct_window40;
    Ciphertext gout10 = ctx.eval_lut(&gin10, xor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_window70 + ct_window61;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nand2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_window70 + ct_window61;
    Ciphertext gout12 = ctx.eval_lut(&gin12, xor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_window30 + ct_window37;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nand2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_window68 + ct_window59;
    Ciphertext gout14 = ctx.eval_lut(&gin14, xor2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_window30 + ct_window37;
    Ciphertext gout15 = ctx.eval_lut(&gin15, xor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_window3 + ct_window10;
    Ciphertext gout16 = ctx.eval_lut(&gin16, xor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_window67 + ct_window58;
    Ciphertext gout17 = ctx.eval_lut(&gin17, xor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_window53 + ct_window44;
    Ciphertext gout18 = ctx.eval_lut(&gin18, xor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_window53 + ct_window44;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nand2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_window67 + ct_window58;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nand2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_window68 + ct_window59;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nand2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_window65 + ct_window56;
    Ciphertext gout22 = ctx.eval_lut(&gin22, xor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_window2 + ct_window9;
    Ciphertext gout23 = ctx.eval_lut(&gin23, xor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_window71 + ct_window62;
    Ciphertext gout24 = ctx.eval_lut(&gin24, xor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_window31 + ct_window38;
    Ciphertext gout25 = ctx.eval_lut(&gin25, nand2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_window31 + ct_window38;
    Ciphertext gout26 = ctx.eval_lut(&gin26, xor2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_window1 + ct_window8;
    Ciphertext gout27 = ctx.eval_lut(&gin27, and2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_window71 + ct_window62;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nand2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_window2 + ct_window9;
    Ciphertext gout29 = ctx.eval_lut(&gin29, nand2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_window4 + ct_window11;
    Ciphertext gout30 = ctx.eval_lut(&gin30, xor2);
    std::cerr << "\rINV31   ";
    Ciphertext gout31 = 1 - ct_window24;

    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * ct_window29 + ct_window36;
    Ciphertext gout32 = ctx.eval_lut(&gin32, xor2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_window4 + ct_window11;
    Ciphertext gout33 = ctx.eval_lut(&gin33, nand2);
    std::cerr << "\rINV34   ";
    Ciphertext gout34 = 1 - ct_window39;

    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * ct_window49 + ct_window40;
    Ciphertext gout35 = ctx.eval_lut(&gin35, and2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * ct_window50 + ct_window41;
    Ciphertext gout36 = ctx.eval_lut(&gin36, xor2);
    std::cerr << "\rINV37   ";
    Ciphertext gout37 = 1 - ct_window23;

    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * ct_window50 + ct_window41;
    Ciphertext gout38 = ctx.eval_lut(&gin38, nand2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * ct_window16 + ct_window0;
    Ciphertext gout39 = ctx.eval_lut(&gin39, xor2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * ct_window48 + ct_window64;
    Ciphertext gout40 = ctx.eval_lut(&gin40, xor2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_window7 + ct_window14;
    Ciphertext gout41 = ctx.eval_lut(&gin41, xor2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * ct_window48 + ct_window64;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rINV43   ";
    Ciphertext gout43 = 1 - ct_window63;

    std::cerr << "\rINV44   ";
    Ciphertext gout44 = 1 - ct_window20;

    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * ct_window7 + ct_window14;
    Ciphertext gout45 = ctx.eval_lut(&gin45, nand2);
    std::cerr << "\rINV46   ";
    Ciphertext gout46 = 1 - ct_window47;

    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * ct_window29 + ct_window36;
    Ciphertext gout47 = ctx.eval_lut(&gin47, nand2);
    std::cerr << "\rINV48   ";
    Ciphertext gout48 = 1 - ct_window19;

    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * ct_window28 + ct_window35;
    Ciphertext gout49 = ctx.eval_lut(&gin49, nand2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_window28 + ct_window35;
    Ciphertext gout50 = ctx.eval_lut(&gin50, xor2);
    std::cerr << "\rINV51   ";
    Ciphertext gout51 = 1 - ct_window22;

    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * ct_window69 + ct_window60;
    Ciphertext gout52 = ctx.eval_lut(&gin52, xor2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * ct_window51 + ct_window42;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rINV54   ";
    Ciphertext gout54 = 1 - ct_window21;

    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_window69 + ct_window60;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rINV56   ";
    Ciphertext gout56 = 1 - ct_window15;

    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * ct_window54 + ct_window45;
    Ciphertext gout57 = ctx.eval_lut(&gin57, nand2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_window1 + ct_window8;
    Ciphertext gout58 = ctx.eval_lut(&gin58, xor2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * ct_window51 + ct_window42;
    Ciphertext gout59 = ctx.eval_lut(&gin59, xor2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * ct_window6 + ct_window13;
    Ciphertext gout60 = ctx.eval_lut(&gin60, xor2);
    std::cerr << "\rINV61   ";
    Ciphertext gout61 = 1 - ct_window18;

    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * ct_window54 + ct_window45;
    Ciphertext gout62 = ctx.eval_lut(&gin62, xor2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * ct_window6 + ct_window13;
    Ciphertext gout63 = ctx.eval_lut(&gin63, nand2);
    std::cerr << "\rINV64   ";
    Ciphertext gout64 = 1 - ct_window17;

    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * ct_window52 + ct_window43;
    Ciphertext gout65 = ctx.eval_lut(&gin65, xor2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_window52 + ct_window43;
    Ciphertext gout66 = ctx.eval_lut(&gin66, nand2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_window16 + ct_window0;
    Ciphertext gout67 = ctx.eval_lut(&gin67, and2);
    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * ct_window55 + ct_window46;
    Ciphertext gout68 = ctx.eval_lut(&gin68, nand2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * ct_window25 + ct_window32;
    Ciphertext gout69 = ctx.eval_lut(&gin69, and2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_window65 + ct_window56;
    Ciphertext gout70 = ctx.eval_lut(&gin70, and2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_window5 + ct_window12;
    Ciphertext gout71 = ctx.eval_lut(&gin71, xor2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_window55 + ct_window46;
    Ciphertext gout72 = ctx.eval_lut(&gin72, xor2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * gout64 + gout58;
    Ciphertext gout73 = ctx.eval_lut(&gin73, xnor2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * gout10 + gout22;
    Ciphertext gout74 = ctx.eval_lut(&gin74, nand2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * ct_window17 + gout58;
    Ciphertext gout75 = ctx.eval_lut(&gin75, nand2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * gout70 + gout2;
    Ciphertext gout76 = ctx.eval_lut(&gin76, xor2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * gout27 + gout23;
    Ciphertext gout77 = ctx.eval_lut(&gin77, xor2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * gout10 + gout22;
    Ciphertext gout78 = ctx.eval_lut(&gin78, xor2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * gout27 + gout23;
    Ciphertext gout79 = ctx.eval_lut(&gin79, nand2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * gout35 + gout36;
    Ciphertext gout80 = ctx.eval_lut(&gin80, nand2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * gout70 + gout2;
    Ciphertext gout81 = ctx.eval_lut(&gin81, nand2);
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * gout69 + gout1;
    Ciphertext gout82 = ctx.eval_lut(&gin82, nand2);
    std::cerr << "\rLUT83   ";
    Ciphertext gin83 = 2 * gout69 + gout1;
    Ciphertext gout83 = ctx.eval_lut(&gin83, xor2);
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * gout40 + gout39;
    Ciphertext gout84 = ctx.eval_lut(&gin84, and2);
    std::cerr << "\rLUT85   ";
    Ciphertext gin85 = 2 * gout35 + gout36;
    Ciphertext gout85 = ctx.eval_lut(&gin85, xor2);
    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * gout67 + gout73;
    Ciphertext gout86 = ctx.eval_lut(&gin86, xor2);
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * gout42 + gout78;
    Ciphertext gout87 = ctx.eval_lut(&gin87, nand2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout67 + gout73;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * gout0 + gout82;
    Ciphertext gout89 = ctx.eval_lut(&gin89, nand2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout29 + gout79;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout61 + gout77;
    Ciphertext gout91 = ctx.eval_lut(&gin91, xnor2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout42 + gout78;
    Ciphertext gout92 = ctx.eval_lut(&gin92, xnor2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * ct_window18 + gout77;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout78 + gout84;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout9 + gout81;
    Ciphertext gout95 = ctx.eval_lut(&gin95, nand2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout85 + gout76;
    Ciphertext gout96 = ctx.eval_lut(&gin96, xor2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * gout85 + gout76;
    Ciphertext gout97 = ctx.eval_lut(&gin97, nand2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout38 + gout80;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * ct_window24 + gout86;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout31 + gout86;
    Ciphertext gout100 = ctx.eval_lut(&gin100, xnor2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout74 + gout87;
    Ciphertext gout101 = ctx.eval_lut(&gin101, nand2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout75 + gout88;
    Ciphertext gout102 = ctx.eval_lut(&gin102, nand2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout102 + gout91;
    Ciphertext gout103 = ctx.eval_lut(&gin103, nand2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout84 + (1 - gout92);
    Ciphertext gout104 = ctx.eval_lut(&gin104, or2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * gout90 + gout16;
    Ciphertext gout105 = ctx.eval_lut(&gin105, nand2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * gout102 + gout91;
    Ciphertext gout106 = ctx.eval_lut(&gin106, xnor2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * gout89 + gout3;
    Ciphertext gout107 = ctx.eval_lut(&gin107, nand2);
    std::cerr << "\rLUT108   ";
    Ciphertext gin108 = 2 * gout89 + gout3;
    Ciphertext gout108 = ctx.eval_lut(&gin108, xor2);
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * gout95 + gout17;
    Ciphertext gout109 = ctx.eval_lut(&gin109, nand2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * gout90 + gout16;
    Ciphertext gout110 = ctx.eval_lut(&gin110, xor2);
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * gout95 + gout17;
    Ciphertext gout111 = ctx.eval_lut(&gin111, xor2);
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * gout101 + gout96;
    Ciphertext gout112 = ctx.eval_lut(&gin112, nand2);
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * gout101 + gout96;
    Ciphertext gout113 = ctx.eval_lut(&gin113, xor2);
    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * gout98 + gout59;
    Ciphertext gout114 = ctx.eval_lut(&gin114, xnor2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * gout98 + gout59;
    Ciphertext gout115 = ctx.eval_lut(&gin115, nand2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * (1 - gout106) + gout5;
    Ciphertext gout116 = ctx.eval_lut(&gin116, and2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * gout93 + gout103;
    Ciphertext gout117 = ctx.eval_lut(&gin117, nand2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout7 + gout107;
    Ciphertext gout118 = ctx.eval_lut(&gin118, nand2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * gout20 + gout109;
    Ciphertext gout119 = ctx.eval_lut(&gin119, nand2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * gout106 + gout5;
    Ciphertext gout120 = ctx.eval_lut(&gin120, xnor2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * ct_window19 + gout110;
    Ciphertext gout121 = ctx.eval_lut(&gin121, nand2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * gout4 + gout105;
    Ciphertext gout122 = ctx.eval_lut(&gin122, nand2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * gout104 + gout100;
    Ciphertext gout123 = ctx.eval_lut(&gin123, nand2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * gout48 + gout110;
    Ciphertext gout124 = ctx.eval_lut(&gin124, xnor2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * gout97 + gout112;
    Ciphertext gout125 = ctx.eval_lut(&gin125, nand2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * gout53 + gout115;
    Ciphertext gout126 = ctx.eval_lut(&gin126, nand2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * gout114 + (1 - gout111);
    Ciphertext gout127 = ctx.eval_lut(&gin127, or2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * gout114 + gout111;
    Ciphertext gout128 = ctx.eval_lut(&gin128, xnor2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * gout99 + gout120;
    Ciphertext gout129 = ctx.eval_lut(&gin129, xnor2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * gout118 + gout50;
    Ciphertext gout130 = ctx.eval_lut(&gin130, xor2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * gout118 + gout50;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * gout119 + gout14;
    Ciphertext gout132 = ctx.eval_lut(&gin132, xor2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * gout94 + gout123;
    Ciphertext gout133 = ctx.eval_lut(&gin133, nand2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * gout99 + gout120;
    Ciphertext gout134 = ctx.eval_lut(&gin134, and2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * gout119 + gout14;
    Ciphertext gout135 = ctx.eval_lut(&gin135, nand2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * gout122 + gout30;
    Ciphertext gout136 = ctx.eval_lut(&gin136, xor2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * gout117 + gout124;
    Ciphertext gout137 = ctx.eval_lut(&gin137, nand2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * gout117 + gout124;
    Ciphertext gout138 = ctx.eval_lut(&gin138, xor2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * gout122 + gout30;
    Ciphertext gout139 = ctx.eval_lut(&gin139, nand2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * gout126 + gout65;
    Ciphertext gout140 = ctx.eval_lut(&gin140, nand2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * gout126 + gout65;
    Ciphertext gout141 = ctx.eval_lut(&gin141, xnor2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * gout125 + gout128;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * gout125 + gout128;
    Ciphertext gout143 = ctx.eval_lut(&gin143, xnor2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * gout49 + gout131;
    Ciphertext gout144 = ctx.eval_lut(&gin144, nand2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * gout133 + gout113;
    Ciphertext gout145 = ctx.eval_lut(&gin145, nand2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * gout33 + gout139;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * gout116 + gout134;
    Ciphertext gout147 = ctx.eval_lut(&gin147, or2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * gout21 + gout135;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * gout83 + gout138;
    Ciphertext gout149 = ctx.eval_lut(&gin149, xor2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * gout133 + gout113;
    Ciphertext gout150 = ctx.eval_lut(&gin150, nor2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * gout121 + gout137;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_window20 + gout136;
    Ciphertext gout152 = ctx.eval_lut(&gin152, nand2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * gout44 + gout136;
    Ciphertext gout153 = ctx.eval_lut(&gin153, xnor2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * gout83 + gout138;
    Ciphertext gout154 = ctx.eval_lut(&gin154, nand2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * gout66 + gout140;
    Ciphertext gout155 = ctx.eval_lut(&gin155, nand2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * gout141 + gout132;
    Ciphertext gout156 = ctx.eval_lut(&gin156, xnor2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * gout127 + gout142;
    Ciphertext gout157 = ctx.eval_lut(&gin157, nand2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * gout141 + (1 - gout132);
    Ciphertext gout158 = ctx.eval_lut(&gin158, or2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * gout144 + gout32;
    Ciphertext gout159 = ctx.eval_lut(&gin159, xor2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * gout144 + gout32;
    Ciphertext gout160 = ctx.eval_lut(&gin160, nand2);
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * gout146 + gout71;
    Ciphertext gout161 = ctx.eval_lut(&gin161, xor2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * gout147 + gout149;
    Ciphertext gout162 = ctx.eval_lut(&gin162, xnor2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * gout147 + gout149;
    Ciphertext gout163 = ctx.eval_lut(&gin163, nand2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * gout154 + gout163;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * gout146 + gout71;
    Ciphertext gout165 = ctx.eval_lut(&gin165, nand2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * gout150 + gout129;
    Ciphertext gout166 = ctx.eval_lut(&gin166, or2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * gout148 + gout52;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * gout148 + gout52;
    Ciphertext gout168 = ctx.eval_lut(&gin168, xor2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * gout151 + gout153;
    Ciphertext gout169 = ctx.eval_lut(&gin169, xnor2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * gout151 + gout153;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout155 + gout18;
    Ciphertext gout171 = ctx.eval_lut(&gin171, xnor2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout155 + gout18;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * gout143 + gout162;
    Ciphertext gout173 = ctx.eval_lut(&gin173, nand2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout143 + gout162;
    Ciphertext gout174 = ctx.eval_lut(&gin174, or2);
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * gout157 + gout156;
    Ciphertext gout175 = ctx.eval_lut(&gin175, xor2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * gout157 + gout156;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * gout55 + gout167;
    Ciphertext gout177 = ctx.eval_lut(&gin177, and2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * gout47 + gout160;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * ct_window21 + gout161;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout54 + gout161;
    Ciphertext gout180 = ctx.eval_lut(&gin180, xnor2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * gout6 + gout165;
    Ciphertext gout181 = ctx.eval_lut(&gin181, nand2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * gout152 + gout170;
    Ciphertext gout182 = ctx.eval_lut(&gin182, nand2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * (1 - gout169) + gout108;
    Ciphertext gout183 = ctx.eval_lut(&gin183, and2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout145 + gout166;
    Ciphertext gout184 = ctx.eval_lut(&gin184, and2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * gout171 + (1 - gout168);
    Ciphertext gout185 = ctx.eval_lut(&gin185, or2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * gout171 + gout168;
    Ciphertext gout186 = ctx.eval_lut(&gin186, xnor2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * gout169 + gout108;
    Ciphertext gout187 = ctx.eval_lut(&gin187, xnor2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout19 + gout172;
    Ciphertext gout188 = ctx.eval_lut(&gin188, nand2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * gout174 + gout184;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout158 + gout176;
    Ciphertext gout190 = ctx.eval_lut(&gin190, nand2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout177 + gout11;
    Ciphertext gout191 = ctx.eval_lut(&gin191, nand2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * gout177 + gout12;
    Ciphertext gout192 = ctx.eval_lut(&gin192, xnor2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout178 + gout15;
    Ciphertext gout193 = ctx.eval_lut(&gin193, xor2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * gout178 + gout15;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * gout182 + gout180;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout181 + gout60;
    Ciphertext gout196 = ctx.eval_lut(&gin196, xor2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout181 + gout60;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * gout182 + gout180;
    Ciphertext gout198 = ctx.eval_lut(&gin198, xor2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout164 + gout187;
    Ciphertext gout199 = ctx.eval_lut(&gin199, and2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout164 + gout187;
    Ciphertext gout200 = ctx.eval_lut(&gin200, xor2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout173 + gout189;
    Ciphertext gout201 = ctx.eval_lut(&gin201, and2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout188 + gout62;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout188 + gout62;
    Ciphertext gout203 = ctx.eval_lut(&gin203, xnor2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout190 + gout186;
    Ciphertext gout204 = ctx.eval_lut(&gin204, nand2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout190 + gout186;
    Ciphertext gout205 = ctx.eval_lut(&gin205, xor2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout8 + gout191;
    Ciphertext gout206 = ctx.eval_lut(&gin206, and2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout13 + gout194;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout183 + gout199;
    Ciphertext gout208 = ctx.eval_lut(&gin208, or2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout63 + gout197;
    Ciphertext gout209 = ctx.eval_lut(&gin209, nand2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * ct_window22 + gout196;
    Ciphertext gout210 = ctx.eval_lut(&gin210, nand2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout130 + gout198;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout51 + gout196;
    Ciphertext gout212 = ctx.eval_lut(&gin212, xnor2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout179 + gout195;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout130 + gout198;
    Ciphertext gout214 = ctx.eval_lut(&gin214, xor2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout175 + gout200;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout175 + gout200;
    Ciphertext gout216 = ctx.eval_lut(&gin216, xor2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout203 + gout192;
    Ciphertext gout217 = ctx.eval_lut(&gin217, xnor2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout57 + gout202;
    Ciphertext gout218 = ctx.eval_lut(&gin218, nand2);
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout185 + gout204;
    Ciphertext gout219 = ctx.eval_lut(&gin219, nand2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout203 + (1 - gout192);
    Ciphertext gout220 = ctx.eval_lut(&gin220, or2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout24 + gout206;
    Ciphertext gout221 = ctx.eval_lut(&gin221, xor2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout24 + gout206;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout207 + gout26;
    Ciphertext gout223 = ctx.eval_lut(&gin223, nand2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout207 + gout26;
    Ciphertext gout224 = ctx.eval_lut(&gin224, xor2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout209 + gout41;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout209 + gout41;
    Ciphertext gout226 = ctx.eval_lut(&gin226, xor2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout213 + gout212;
    Ciphertext gout227 = ctx.eval_lut(&gin227, xnor2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout213 + gout212;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout208 + gout214;
    Ciphertext gout229 = ctx.eval_lut(&gin229, nand2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout208 + gout214;
    Ciphertext gout230 = ctx.eval_lut(&gin230, xnor2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout216 + gout201;
    Ciphertext gout231 = ctx.eval_lut(&gin231, xor2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout216 + gout201;
    Ciphertext gout232 = ctx.eval_lut(&gin232, nand2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout218 + gout72;
    Ciphertext gout233 = ctx.eval_lut(&gin233, nand2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout219 + gout217;
    Ciphertext gout234 = ctx.eval_lut(&gin234, nand2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout219 + gout217;
    Ciphertext gout235 = ctx.eval_lut(&gin235, xor2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout218 + gout72;
    Ciphertext gout236 = ctx.eval_lut(&gin236, xnor2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * (1 - gout230) + gout205;
    Ciphertext gout237 = ctx.eval_lut(&gin237, and2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout230 + gout205;
    Ciphertext gout238 = ctx.eval_lut(&gin238, xnor2);
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout220 + gout234;
    Ciphertext gout239 = ctx.eval_lut(&gin239, nand2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout28 + gout222;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout45 + gout225;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout227 + gout159;
    Ciphertext gout242 = ctx.eval_lut(&gin242, xnor2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * (1 - gout227) + gout159;
    Ciphertext gout243 = ctx.eval_lut(&gin243, and2);
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout211 + gout229;
    Ciphertext gout244 = ctx.eval_lut(&gin244, nand2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * ct_window23 + gout226;
    Ciphertext gout245 = ctx.eval_lut(&gin245, nand2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout37 + gout226;
    Ciphertext gout246 = ctx.eval_lut(&gin246, xnor2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout210 + gout228;
    Ciphertext gout247 = ctx.eval_lut(&gin247, nand2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout68 + gout233;
    Ciphertext gout248 = ctx.eval_lut(&gin248, nand2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout25 + gout223;
    Ciphertext gout249 = ctx.eval_lut(&gin249, nand2);
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout215 + gout232;
    Ciphertext gout250 = ctx.eval_lut(&gin250, nand2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout236 + (1 - gout221);
    Ciphertext gout251 = ctx.eval_lut(&gin251, or2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout236 + gout221;
    Ciphertext gout252 = ctx.eval_lut(&gin252, xnor2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout250 + gout238;
    Ciphertext gout253 = ctx.eval_lut(&gin253, xor2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout250 + gout238;
    Ciphertext gout254 = ctx.eval_lut(&gin254, and2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout43 + gout240;
    Ciphertext gout255 = ctx.eval_lut(&gin255, xnor2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * ct_window63 + gout240;
    Ciphertext gout256 = ctx.eval_lut(&gin256, and2);
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * ct_window15 + gout241;
    Ciphertext gout257 = ctx.eval_lut(&gin257, nand2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout56 + gout241;
    Ciphertext gout258 = ctx.eval_lut(&gin258, xnor2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout244 + gout242;
    Ciphertext gout259 = ctx.eval_lut(&gin259, xor2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * gout34 + gout249;
    Ciphertext gout260 = ctx.eval_lut(&gin260, xnor2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout244 + gout242;
    Ciphertext gout261 = ctx.eval_lut(&gin261, and2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout46 + gout248;
    Ciphertext gout262 = ctx.eval_lut(&gin262, xnor2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout247 + gout246;
    Ciphertext gout263 = ctx.eval_lut(&gin263, xnor2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * ct_window47 + gout248;
    Ciphertext gout264 = ctx.eval_lut(&gin264, and2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout247 + gout246;
    Ciphertext gout265 = ctx.eval_lut(&gin265, nand2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * ct_window39 + gout249;
    Ciphertext gout266 = ctx.eval_lut(&gin266, nand2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout239 + gout252;
    Ciphertext gout267 = ctx.eval_lut(&gin267, nand2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout239 + gout252;
    Ciphertext gout268 = ctx.eval_lut(&gin268, xor2);
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout237 + gout254;
    Ciphertext gout269 = ctx.eval_lut(&gin269, or2);
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout235 + gout259;
    Ciphertext gout270 = ctx.eval_lut(&gin270, xor2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout264 + gout256;
    Ciphertext gout271 = ctx.eval_lut(&gin271, xor2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout264 + gout256;
    Ciphertext gout272 = ctx.eval_lut(&gin272, nand2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * (1 - gout263) + gout193;
    Ciphertext gout273 = ctx.eval_lut(&gin273, and2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout263 + gout193;
    Ciphertext gout274 = ctx.eval_lut(&gin274, xnor2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout245 + gout265;
    Ciphertext gout275 = ctx.eval_lut(&gin275, nand2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * gout243 + gout261;
    Ciphertext gout276 = ctx.eval_lut(&gin276, or2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout235 + gout259;
    Ciphertext gout277 = ctx.eval_lut(&gin277, nand2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout262 + gout255;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout262 + gout255;
    Ciphertext gout279 = ctx.eval_lut(&gin279, xor2);
    std::cerr << "\rINV280   ";
    Ciphertext gout280 = 1 - gout266;

    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout251 + gout267;
    Ciphertext gout281 = ctx.eval_lut(&gin281, nand2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout269 + gout270;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout269 + gout270;
    Ciphertext gout283 = ctx.eval_lut(&gin283, xor2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout275 + gout258;
    Ciphertext gout284 = ctx.eval_lut(&gin284, xnor2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout275 + gout258;
    Ciphertext gout285 = ctx.eval_lut(&gin285, nand2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout276 + gout274;
    Ciphertext gout286 = ctx.eval_lut(&gin286, and2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout276 + gout274;
    Ciphertext gout287 = ctx.eval_lut(&gin287, xnor2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout281 + gout279;
    Ciphertext gout288 = ctx.eval_lut(&gin288, xor2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout281 + gout279;
    Ciphertext gout289 = ctx.eval_lut(&gin289, nand2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout277 + gout282;
    Ciphertext gout290 = ctx.eval_lut(&gin290, nand2);
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout257 + gout285;
    Ciphertext gout291 = ctx.eval_lut(&gin291, nand2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout284 + gout224;
    Ciphertext gout292 = ctx.eval_lut(&gin292, xnor2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * (1 - gout284) + gout224;
    Ciphertext gout293 = ctx.eval_lut(&gin293, and2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout273 + gout286;
    Ciphertext gout294 = ctx.eval_lut(&gin294, or2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * (1 - gout287) + gout268;
    Ciphertext gout295 = ctx.eval_lut(&gin295, and2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout278 + gout289;
    Ciphertext gout296 = ctx.eval_lut(&gin296, nand2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout287 + gout268;
    Ciphertext gout297 = ctx.eval_lut(&gin297, xnor2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout290 + gout297;
    Ciphertext gout298 = ctx.eval_lut(&gin298, xor2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout290 + gout297;
    Ciphertext gout299 = ctx.eval_lut(&gin299, and2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout291 + gout260;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout291 + gout260;
    Ciphertext gout301 = ctx.eval_lut(&gin301, xor2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout294 + gout292;
    Ciphertext gout302 = ctx.eval_lut(&gin302, and2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout294 + gout292;
    Ciphertext gout303 = ctx.eval_lut(&gin303, xor2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout296 + gout271;
    Ciphertext gout304 = ctx.eval_lut(&gin304, nand2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout296 + gout271;
    Ciphertext gout305 = ctx.eval_lut(&gin305, xor2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout295 + gout299;
    Ciphertext gout306 = ctx.eval_lut(&gin306, or2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout266 + gout300;
    Ciphertext gout307 = ctx.eval_lut(&gin307, nand2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout288 + gout303;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout293 + gout302;
    Ciphertext gout309 = ctx.eval_lut(&gin309, or2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout288 + gout303;
    Ciphertext gout310 = ctx.eval_lut(&gin310, xor2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout272 + gout304;
    Ciphertext gout311 = ctx.eval_lut(&gin311, nand2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout306 + gout310;
    Ciphertext gout312 = ctx.eval_lut(&gin312, nand2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout306 + gout310;
    Ciphertext gout313 = ctx.eval_lut(&gin313, xor2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout309 + gout301;
    Ciphertext gout314 = ctx.eval_lut(&gin314, xor2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout314 + gout305;
    Ciphertext gout315 = ctx.eval_lut(&gin315, nand2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout314 + gout305;
    Ciphertext gout316 = ctx.eval_lut(&gin316, xor2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout309 + gout301;
    Ciphertext gout317 = ctx.eval_lut(&gin317, and2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout308 + gout312;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rIMUX319  ";
    Ciphertext gin319A = 2 * gout266 + gout317;
    Ciphertext gout319A = ctx.eval_lut(&gin319A, and2);
    Ciphertext gin319B = 2 * gout307 + (1 - gout317);
    Ciphertext gout319B = ctx.eval_lut(&gin319B, and2);
    Ciphertext gout319 = gout319A + gout319B;
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout280 + gout317;
    Ciphertext gout320 = ctx.eval_lut(&gin320, nand2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout318 + gout316;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout318 + gout316;
    Ciphertext gout322 = ctx.eval_lut(&gin322, xor2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout319 + gout311;
    Ciphertext gout323 = ctx.eval_lut(&gin323, xor2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout319 + gout311;
    Ciphertext gout324 = ctx.eval_lut(&gin324, nand2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout315 + gout321;
    Ciphertext gout325 = ctx.eval_lut(&gin325, nand2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout320 + gout324;
    Ciphertext gout326 = ctx.eval_lut(&gin326, and2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout325 + gout323;
    Ciphertext gout327 = ctx.eval_lut(&gin327, nand2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout325 + gout323;
    Ciphertext gout328 = ctx.eval_lut(&gin328, xor2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout327 + gout326;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout231)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout253)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout283)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout298)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout313)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout322)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout328)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout329)); /* out7 */
    return test_out;
}

static void
BM_image_gaussian_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_image_gaussian_boolean(ctx);
    }
}

BENCHMARK(BM_image_gaussian_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
