#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_strrev_boolean(FHEContext& ctx, uint64_t my_string = 0)
{
    Ciphertext ct_my_string0 = ctx.encrypt_public((my_string >> 0) & 1);
    Ciphertext ct_my_string1 = ctx.encrypt_public((my_string >> 1) & 1);
    Ciphertext ct_my_string2 = ctx.encrypt_public((my_string >> 2) & 1);
    Ciphertext ct_my_string3 = ctx.encrypt_public((my_string >> 3) & 1);
    Ciphertext ct_my_string4 = ctx.encrypt_public((my_string >> 4) & 1);
    Ciphertext ct_my_string5 = ctx.encrypt_public((my_string >> 5) & 1);
    Ciphertext ct_my_string6 = ctx.encrypt_public((my_string >> 6) & 1);
    Ciphertext ct_my_string7 = ctx.encrypt_public((my_string >> 7) & 1);
    Ciphertext ct_my_string8 = ctx.encrypt_public((my_string >> 8) & 1);
    Ciphertext ct_my_string9 = ctx.encrypt_public((my_string >> 9) & 1);
    Ciphertext ct_my_string10 = ctx.encrypt_public((my_string >> 10) & 1);
    Ciphertext ct_my_string11 = ctx.encrypt_public((my_string >> 11) & 1);
    Ciphertext ct_my_string12 = ctx.encrypt_public((my_string >> 12) & 1);
    Ciphertext ct_my_string13 = ctx.encrypt_public((my_string >> 13) & 1);
    Ciphertext ct_my_string14 = ctx.encrypt_public((my_string >> 14) & 1);
    Ciphertext ct_my_string15 = ctx.encrypt_public((my_string >> 15) & 1);
    Ciphertext ct_my_string16 = ctx.encrypt_public((my_string >> 16) & 1);
    Ciphertext ct_my_string17 = ctx.encrypt_public((my_string >> 17) & 1);
    Ciphertext ct_my_string18 = ctx.encrypt_public((my_string >> 18) & 1);
    Ciphertext ct_my_string19 = ctx.encrypt_public((my_string >> 19) & 1);
    Ciphertext ct_my_string20 = ctx.encrypt_public((my_string >> 20) & 1);
    Ciphertext ct_my_string21 = ctx.encrypt_public((my_string >> 21) & 1);
    Ciphertext ct_my_string22 = ctx.encrypt_public((my_string >> 22) & 1);
    Ciphertext ct_my_string23 = ctx.encrypt_public((my_string >> 23) & 1);
    Ciphertext ct_my_string24 = ctx.encrypt_public((my_string >> 24) & 1);
    Ciphertext ct_my_string25 = ctx.encrypt_public((my_string >> 25) & 1);
    Ciphertext ct_my_string26 = ctx.encrypt_public((my_string >> 26) & 1);
    Ciphertext ct_my_string27 = ctx.encrypt_public((my_string >> 27) & 1);
    Ciphertext ct_my_string28 = ctx.encrypt_public((my_string >> 28) & 1);
    Ciphertext ct_my_string29 = ctx.encrypt_public((my_string >> 29) & 1);
    Ciphertext ct_my_string30 = ctx.encrypt_public((my_string >> 30) & 1);
    Ciphertext ct_my_string31 = ctx.encrypt_public((my_string >> 31) & 1);
    Ciphertext ct_my_string32 = ctx.encrypt_public((my_string >> 32) & 1);
    Ciphertext ct_my_string33 = ctx.encrypt_public((my_string >> 33) & 1);
    Ciphertext ct_my_string34 = ctx.encrypt_public((my_string >> 34) & 1);
    Ciphertext ct_my_string35 = ctx.encrypt_public((my_string >> 35) & 1);
    Ciphertext ct_my_string36 = ctx.encrypt_public((my_string >> 36) & 1);
    Ciphertext ct_my_string37 = ctx.encrypt_public((my_string >> 37) & 1);
    Ciphertext ct_my_string38 = ctx.encrypt_public((my_string >> 38) & 1);
    Ciphertext ct_my_string39 = ctx.encrypt_public((my_string >> 39) & 1);
    Ciphertext ct_my_string40 = ctx.encrypt_public((my_string >> 40) & 1);
    Ciphertext ct_my_string41 = ctx.encrypt_public((my_string >> 41) & 1);
    Ciphertext ct_my_string42 = ctx.encrypt_public((my_string >> 42) & 1);
    Ciphertext ct_my_string43 = ctx.encrypt_public((my_string >> 43) & 1);
    Ciphertext ct_my_string44 = ctx.encrypt_public((my_string >> 44) & 1);
    Ciphertext ct_my_string45 = ctx.encrypt_public((my_string >> 45) & 1);
    Ciphertext ct_my_string46 = ctx.encrypt_public((my_string >> 46) & 1);
    Ciphertext ct_my_string47 = ctx.encrypt_public((my_string >> 47) & 1);
    Ciphertext ct_my_string48 = ctx.encrypt_public((my_string >> 48) & 1);
    Ciphertext ct_my_string49 = ctx.encrypt_public((my_string >> 49) & 1);
    Ciphertext ct_my_string50 = ctx.encrypt_public((my_string >> 50) & 1);
    Ciphertext ct_my_string51 = ctx.encrypt_public((my_string >> 51) & 1);
    Ciphertext ct_my_string52 = ctx.encrypt_public((my_string >> 52) & 1);
    Ciphertext ct_my_string53 = ctx.encrypt_public((my_string >> 53) & 1);
    Ciphertext ct_my_string54 = ctx.encrypt_public((my_string >> 54) & 1);
    Ciphertext ct_my_string55 = ctx.encrypt_public((my_string >> 55) & 1);
    Ciphertext ct_my_string56 = ctx.encrypt_public((my_string >> 56) & 1);
    Ciphertext ct_my_string57 = ctx.encrypt_public((my_string >> 57) & 1);
    Ciphertext ct_my_string58 = ctx.encrypt_public((my_string >> 58) & 1);
    Ciphertext ct_my_string59 = ctx.encrypt_public((my_string >> 59) & 1);
    Ciphertext ct_my_string60 = ctx.encrypt_public((my_string >> 60) & 1);
    Ciphertext ct_my_string61 = ctx.encrypt_public((my_string >> 61) & 1);
    Ciphertext ct_my_string62 = ctx.encrypt_public((my_string >> 62) & 1);
    Ciphertext ct_my_string63 = ctx.encrypt_public((my_string >> 63) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_my_string53 + ct_my_string54;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_my_string10 + ct_my_string11;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_my_string62 + ct_my_string63;
    Ciphertext gout2 = ctx.eval_lut(&gin2, nor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_my_string56 + ct_my_string57;
    Ciphertext gout3 = ctx.eval_lut(&gin3, nor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_my_string58 + ct_my_string59;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nor2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_my_string38 + ct_my_string39;
    Ciphertext gout5 = ctx.eval_lut(&gin5, nor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_my_string60 + ct_my_string61;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_my_string36 + ct_my_string37;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_my_string44 + ct_my_string45;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nor2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_my_string40 + ct_my_string41;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_my_string34 + ct_my_string35;
    Ciphertext gout10 = ctx.eval_lut(&gin10, nor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_my_string19 + ct_my_string20;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_my_string32 + ct_my_string33;
    Ciphertext gout12 = ctx.eval_lut(&gin12, nor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_my_string46 + ct_my_string47;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nor2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_my_string17 + ct_my_string18;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nor2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_my_string48 + ct_my_string51;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_my_string42 + ct_my_string43;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_my_string49 + ct_my_string50;
    Ciphertext gout17 = ctx.eval_lut(&gin17, nor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_my_string28 + ct_my_string29;
    Ciphertext gout18 = ctx.eval_lut(&gin18, nor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_my_string21 + ct_my_string22;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_my_string30 + ct_my_string31;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nor2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_my_string2 + ct_my_string3;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_my_string26 + ct_my_string27;
    Ciphertext gout22 = ctx.eval_lut(&gin22, nor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_my_string23 + ct_my_string16;
    Ciphertext gout23 = ctx.eval_lut(&gin23, nor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_my_string24 + ct_my_string25;
    Ciphertext gout24 = ctx.eval_lut(&gin24, nor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_my_string4 + ct_my_string5;
    Ciphertext gout25 = ctx.eval_lut(&gin25, nor2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_my_string12 + ct_my_string13;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nor2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_my_string0 + ct_my_string1;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nor2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_my_string8 + ct_my_string9;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nor2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_my_string6 + ct_my_string7;
    Ciphertext gout29 = ctx.eval_lut(&gin29, nor2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_my_string52 + ct_my_string55;
    Ciphertext gout30 = ctx.eval_lut(&gin30, nor2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_my_string14 + ct_my_string15;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nor2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * gout17 + gout15;
    Ciphertext gout32 = ctx.eval_lut(&gin32, and2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * gout6 + gout2;
    Ciphertext gout33 = ctx.eval_lut(&gin33, and2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * gout7 + gout5;
    Ciphertext gout34 = ctx.eval_lut(&gin34, and2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * gout3 + gout4;
    Ciphertext gout35 = ctx.eval_lut(&gin35, and2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * gout12 + gout10;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * gout9 + gout16;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * gout8 + gout13;
    Ciphertext gout38 = ctx.eval_lut(&gin38, and2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * gout0 + gout30;
    Ciphertext gout39 = ctx.eval_lut(&gin39, and2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * gout11 + gout19;
    Ciphertext gout40 = ctx.eval_lut(&gin40, and2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * gout23 + gout14;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * gout24 + gout22;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * gout25 + gout29;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * gout18 + gout20;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * gout27 + gout21;
    Ciphertext gout45 = ctx.eval_lut(&gin45, and2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * gout28 + gout1;
    Ciphertext gout46 = ctx.eval_lut(&gin46, and2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * gout26 + gout31;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * gout33 + gout35;
    Ciphertext gout48 = ctx.eval_lut(&gin48, nand2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * gout39 + gout32;
    Ciphertext gout49 = ctx.eval_lut(&gin49, nand2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * gout34 + gout36;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * gout38 + gout37;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * gout40 + gout41;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * gout44 + gout42;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * gout47 + gout46;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * gout43 + gout45;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * (1 - gout49) + gout48;
    Ciphertext gout56 = ctx.eval_lut(&gin56, or2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * gout51 + gout56;
    Ciphertext gout57 = ctx.eval_lut(&gin57, nand2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * gout49 + gout48;
    Ciphertext gout58 = ctx.eval_lut(&gin58, and2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * gout50 + gout51;
    Ciphertext gout59 = ctx.eval_lut(&gin59, and2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * gout52 + gout53;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * gout55 + gout54;
    Ciphertext gout61 = ctx.eval_lut(&gin61, and2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * gout50 + gout57;
    Ciphertext gout62 = ctx.eval_lut(&gin62, nand2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * (1 - gout59) + gout58;
    Ciphertext gout63 = ctx.eval_lut(&gin63, or2);
    std::cerr << "\rINV64   ";
    Ciphertext gout64 = 1 - gout59;

    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * gout58 + gout59;
    Ciphertext gout65 = ctx.eval_lut(&gin65, and2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * gout59 + gout60;
    Ciphertext gout66 = ctx.eval_lut(&gin66, and2);
    std::cerr << "\rINV67   ";
    Ciphertext gout67 = 1 - gout61;

    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * gout60 + gout61;
    Ciphertext gout68 = ctx.eval_lut(&gin68, nand2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * gout63 + gout60;
    Ciphertext gout69 = ctx.eval_lut(&gin69, nand2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * gout53 + gout62;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_my_string49 + gout65;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_my_string53 + gout65;
    Ciphertext gout72 = ctx.eval_lut(&gin72, nand2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * gout65 + gout68;
    Ciphertext gout73 = ctx.eval_lut(&gin73, or2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * (1 - gout66) + gout61;
    Ciphertext gout74 = ctx.eval_lut(&gin74, and2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * gout65 + (1 - gout68);
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * gout64 + gout68;
    Ciphertext gout76 = ctx.eval_lut(&gin76, or2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * gout69 + gout61;
    Ciphertext gout77 = ctx.eval_lut(&gin77, and2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * gout52 + gout70;
    Ciphertext gout78 = ctx.eval_lut(&gin78, nand2);
    std::cerr << "\rIMUX79  ";
    Ciphertext gin79A = 2 * ct_my_string5 + gout75;
    Ciphertext gout79A = ctx.eval_lut(&gin79A, and2);
    Ciphertext gin79B = 2 * ct_my_string61 + (1 - gout75);
    Ciphertext gout79B = ctx.eval_lut(&gin79B, and2);
    Ciphertext gout79 = gout79A + gout79B;
    std::cerr << "\rINV80   ";
    Ciphertext gout80 = 1 - gout73;

    std::cerr << "\rIMUX81  ";
    Ciphertext gin81A = 2 * ct_my_string0 + gout75;
    Ciphertext gout81A = ctx.eval_lut(&gin81A, and2);
    Ciphertext gin81B = 2 * ct_my_string56 + (1 - gout75);
    Ciphertext gout81B = ctx.eval_lut(&gin81B, and2);
    Ciphertext gout81 = gout81A + gout81B;
    std::cerr << "\rIMUX82  ";
    Ciphertext gin82A = 2 * ct_my_string3 + gout75;
    Ciphertext gout82A = ctx.eval_lut(&gin82A, and2);
    Ciphertext gin82B = 2 * ct_my_string59 + (1 - gout75);
    Ciphertext gout82B = ctx.eval_lut(&gin82B, and2);
    Ciphertext gout82 = gout82A + gout82B;
    std::cerr << "\rINV83   ";
    Ciphertext gout83 = 1 - gout75;

    std::cerr << "\rIMUX84  ";
    Ciphertext gin84A = 2 * ct_my_string7 + gout75;
    Ciphertext gout84A = ctx.eval_lut(&gin84A, and2);
    Ciphertext gin84B = 2 * ct_my_string63 + (1 - gout75);
    Ciphertext gout84B = ctx.eval_lut(&gin84B, and2);
    Ciphertext gout84 = gout84A + gout84B;
    std::cerr << "\rIMUX85  ";
    Ciphertext gin85A = 2 * ct_my_string2 + gout75;
    Ciphertext gout85A = ctx.eval_lut(&gin85A, and2);
    Ciphertext gin85B = 2 * ct_my_string58 + (1 - gout75);
    Ciphertext gout85B = ctx.eval_lut(&gin85B, and2);
    Ciphertext gout85 = gout85A + gout85B;
    std::cerr << "\rIMUX86  ";
    Ciphertext gin86A = 2 * ct_my_string6 + gout75;
    Ciphertext gout86A = ctx.eval_lut(&gin86A, and2);
    Ciphertext gin86B = 2 * ct_my_string62 + (1 - gout75);
    Ciphertext gout86B = ctx.eval_lut(&gin86B, and2);
    Ciphertext gout86 = gout86A + gout86B;
    std::cerr << "\rIMUX87  ";
    Ciphertext gin87A = 2 * ct_my_string1 + gout75;
    Ciphertext gout87A = ctx.eval_lut(&gin87A, and2);
    Ciphertext gin87B = 2 * ct_my_string57 + (1 - gout75);
    Ciphertext gout87B = ctx.eval_lut(&gin87B, and2);
    Ciphertext gout87 = gout87A + gout87B;
    std::cerr << "\rIMUX88  ";
    Ciphertext gin88A = 2 * ct_my_string4 + gout75;
    Ciphertext gout88A = ctx.eval_lut(&gin88A, and2);
    Ciphertext gin88B = 2 * ct_my_string60 + (1 - gout75);
    Ciphertext gout88B = ctx.eval_lut(&gin88B, and2);
    Ciphertext gout88 = gout88A + gout88B;
    std::cerr << "\rINV89   ";
    Ciphertext gout89 = 1 - gout77;

    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout54 + gout78;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout67 + gout79;
    Ciphertext gout91 = ctx.eval_lut(&gin91, nand2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout67 + gout85;
    Ciphertext gout92 = ctx.eval_lut(&gin92, nand2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout67 + gout84;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout67 + gout82;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout67 + gout81;
    Ciphertext gout95 = ctx.eval_lut(&gin95, nand2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout67 + gout87;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * gout67 + gout86;
    Ciphertext gout97 = ctx.eval_lut(&gin97, nand2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout67 + gout88;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout55 + gout90;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout77 + (1 - gout90);
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout89 + (1 - gout99);
    Ciphertext gout101 = ctx.eval_lut(&gin101, and2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout89 + gout99;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout73 + gout100;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout77 + gout99;
    Ciphertext gout104 = ctx.eval_lut(&gin104, and2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * gout100 + gout66;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * ct_my_string13 + gout103;
    Ciphertext gout106 = ctx.eval_lut(&gin106, nand2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * ct_my_string15 + gout103;
    Ciphertext gout107 = ctx.eval_lut(&gin107, nand2);
    std::cerr << "\rIMUX108  ";
    Ciphertext gin108A = 2 * ct_my_string5 + gout103;
    Ciphertext gout108A = ctx.eval_lut(&gin108A, and2);
    Ciphertext gin108B = 2 * ct_my_string13 + (1 - gout103);
    Ciphertext gout108B = ctx.eval_lut(&gin108B, and2);
    Ciphertext gout108 = gout108A + gout108B;
    std::cerr << "\rIMUX109  ";
    Ciphertext gin109A = 2 * ct_my_string0 + gout103;
    Ciphertext gout109A = ctx.eval_lut(&gin109A, and2);
    Ciphertext gin109B = 2 * ct_my_string8 + (1 - gout103);
    Ciphertext gout109B = ctx.eval_lut(&gin109B, and2);
    Ciphertext gout109 = gout109A + gout109B;
    std::cerr << "\rIMUX110  ";
    Ciphertext gin110A = 2 * ct_my_string4 + gout103;
    Ciphertext gout110A = ctx.eval_lut(&gin110A, and2);
    Ciphertext gin110B = 2 * ct_my_string12 + (1 - gout103);
    Ciphertext gout110B = ctx.eval_lut(&gin110B, and2);
    Ciphertext gout110 = gout110A + gout110B;
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * ct_my_string12 + gout103;
    Ciphertext gout111 = ctx.eval_lut(&gin111, nand2);
    std::cerr << "\rIMUX112  ";
    Ciphertext gin112A = 2 * ct_my_string2 + gout103;
    Ciphertext gout112A = ctx.eval_lut(&gin112A, and2);
    Ciphertext gin112B = 2 * ct_my_string10 + (1 - gout103);
    Ciphertext gout112B = ctx.eval_lut(&gin112B, and2);
    Ciphertext gout112 = gout112A + gout112B;
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * gout102 + gout73;
    Ciphertext gout113 = ctx.eval_lut(&gin113, and2);
    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * ct_my_string9 + gout103;
    Ciphertext gout114 = ctx.eval_lut(&gin114, nand2);
    std::cerr << "\rIMUX115  ";
    Ciphertext gin115A = 2 * ct_my_string6 + gout103;
    Ciphertext gout115A = ctx.eval_lut(&gin115A, and2);
    Ciphertext gin115B = 2 * ct_my_string14 + (1 - gout103);
    Ciphertext gout115B = ctx.eval_lut(&gin115B, and2);
    Ciphertext gout115 = gout115A + gout115B;
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * gout73 + gout104;
    Ciphertext gout116 = ctx.eval_lut(&gin116, and2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * ct_my_string8 + gout103;
    Ciphertext gout117 = ctx.eval_lut(&gin117, nand2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout102 + gout80;
    Ciphertext gout118 = ctx.eval_lut(&gin118, and2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * ct_my_string45 + gout105;
    Ciphertext gout119 = ctx.eval_lut(&gin119, nand2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * ct_my_string40 + gout105;
    Ciphertext gout120 = ctx.eval_lut(&gin120, nand2);
    std::cerr << "\rIMUX121  ";
    Ciphertext gin121A = 2 * ct_my_string6 + gout105;
    Ciphertext gout121A = ctx.eval_lut(&gin121A, and2);
    Ciphertext gin121B = 2 * ct_my_string46 + (1 - gout105);
    Ciphertext gout121B = ctx.eval_lut(&gin121B, and2);
    Ciphertext gout121 = gout121A + gout121B;
    std::cerr << "\rIMUX122  ";
    Ciphertext gin122A = 2 * ct_my_string4 + gout105;
    Ciphertext gout122A = ctx.eval_lut(&gin122A, and2);
    Ciphertext gin122B = 2 * ct_my_string44 + (1 - gout105);
    Ciphertext gout122B = ctx.eval_lut(&gin122B, and2);
    Ciphertext gout122 = gout122A + gout122B;
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * ct_my_string10 + gout103;
    Ciphertext gout123 = ctx.eval_lut(&gin123, nand2);
    std::cerr << "\rIMUX124  ";
    Ciphertext gin124A = 2 * ct_my_string1 + gout103;
    Ciphertext gout124A = ctx.eval_lut(&gin124A, and2);
    Ciphertext gin124B = 2 * ct_my_string9 + (1 - gout103);
    Ciphertext gout124B = ctx.eval_lut(&gin124B, and2);
    Ciphertext gout124 = gout124A + gout124B;
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * gout73 + gout101;
    Ciphertext gout125 = ctx.eval_lut(&gin125, and2);
    std::cerr << "\rIMUX126  ";
    Ciphertext gin126A = 2 * ct_my_string0 + gout105;
    Ciphertext gout126A = ctx.eval_lut(&gin126A, and2);
    Ciphertext gin126B = 2 * ct_my_string40 + (1 - gout105);
    Ciphertext gout126B = ctx.eval_lut(&gin126B, and2);
    Ciphertext gout126 = gout126A + gout126B;
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * gout114 + gout96;
    Ciphertext gout127 = ctx.eval_lut(&gin127, and2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * ct_my_string47 + gout105;
    Ciphertext gout128 = ctx.eval_lut(&gin128, nand2);
    std::cerr << "\rIMUX129  ";
    Ciphertext gin129A = 2 * ct_my_string1 + gout105;
    Ciphertext gout129A = ctx.eval_lut(&gin129A, and2);
    Ciphertext gin129B = 2 * ct_my_string41 + (1 - gout105);
    Ciphertext gout129B = ctx.eval_lut(&gin129B, and2);
    Ciphertext gout129 = gout129A + gout129B;
    std::cerr << "\rIMUX130  ";
    Ciphertext gin130A = 2 * ct_my_string7 + gout105;
    Ciphertext gout130A = ctx.eval_lut(&gin130A, and2);
    Ciphertext gin130B = 2 * ct_my_string47 + (1 - gout105);
    Ciphertext gout130B = ctx.eval_lut(&gin130B, and2);
    Ciphertext gout130 = gout130A + gout130B;
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * ct_my_string46 + gout105;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * ct_my_string14 + gout103;
    Ciphertext gout132 = ctx.eval_lut(&gin132, nand2);
    std::cerr << "\rIMUX133  ";
    Ciphertext gin133A = 2 * ct_my_string3 + gout103;
    Ciphertext gout133A = ctx.eval_lut(&gin133A, and2);
    Ciphertext gin133B = 2 * ct_my_string11 + (1 - gout103);
    Ciphertext gout133B = ctx.eval_lut(&gin133B, and2);
    Ciphertext gout133 = gout133A + gout133B;
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * gout68 + (1 - gout104);
    Ciphertext gout134 = ctx.eval_lut(&gin134, and2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * ct_my_string41 + gout105;
    Ciphertext gout135 = ctx.eval_lut(&gin135, nand2);
    std::cerr << "\rIMUX136  ";
    Ciphertext gin136A = 2 * ct_my_string2 + gout105;
    Ciphertext gout136A = ctx.eval_lut(&gin136A, and2);
    Ciphertext gin136B = 2 * ct_my_string42 + (1 - gout105);
    Ciphertext gout136B = ctx.eval_lut(&gin136B, and2);
    Ciphertext gout136 = gout136A + gout136B;
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_my_string43 + gout105;
    Ciphertext gout137 = ctx.eval_lut(&gin137, nand2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * gout80 + gout104;
    Ciphertext gout138 = ctx.eval_lut(&gin138, and2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_my_string44 + gout105;
    Ciphertext gout139 = ctx.eval_lut(&gin139, nand2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * ct_my_string11 + gout103;
    Ciphertext gout140 = ctx.eval_lut(&gin140, nand2);
    std::cerr << "\rIMUX141  ";
    Ciphertext gin141A = 2 * ct_my_string7 + gout103;
    Ciphertext gout141A = ctx.eval_lut(&gin141A, and2);
    Ciphertext gin141B = 2 * ct_my_string15 + (1 - gout103);
    Ciphertext gout141B = ctx.eval_lut(&gin141B, and2);
    Ciphertext gout141 = gout141A + gout141B;
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_my_string42 + gout105;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rIMUX143  ";
    Ciphertext gin143A = 2 * ct_my_string5 + gout105;
    Ciphertext gout143A = ctx.eval_lut(&gin143A, and2);
    Ciphertext gin143B = 2 * ct_my_string45 + (1 - gout105);
    Ciphertext gout143B = ctx.eval_lut(&gin143B, and2);
    Ciphertext gout143 = gout143A + gout143B;
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * gout101 + gout74;
    Ciphertext gout144 = ctx.eval_lut(&gin144, and2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * gout111 + gout98;
    Ciphertext gout145 = ctx.eval_lut(&gin145, and2);
    std::cerr << "\rIMUX146  ";
    Ciphertext gin146A = 2 * ct_my_string3 + gout105;
    Ciphertext gout146A = ctx.eval_lut(&gin146A, and2);
    Ciphertext gin146B = 2 * ct_my_string43 + (1 - gout105);
    Ciphertext gout146B = ctx.eval_lut(&gin146B, and2);
    Ciphertext gout146 = gout146A + gout146B;
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_my_string5 + gout113;
    Ciphertext gout147 = ctx.eval_lut(&gin147, nand2);
    std::cerr << "\rIMUX148  ";
    Ciphertext gin148A = 2 * ct_my_string6 + gout116;
    Ciphertext gout148A = ctx.eval_lut(&gin148A, and2);
    Ciphertext gin148B = 2 * ct_my_string22 + (1 - gout116);
    Ciphertext gout148B = ctx.eval_lut(&gin148B, and2);
    Ciphertext gout148 = gout148A + gout148B;
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * gout117 + gout95;
    Ciphertext gout149 = ctx.eval_lut(&gin149, and2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * gout107 + gout93;
    Ciphertext gout150 = ctx.eval_lut(&gin150, and2);
    std::cerr << "\rIMUX151  ";
    Ciphertext gin151A = 2 * ct_my_string3 + gout116;
    Ciphertext gout151A = ctx.eval_lut(&gin151A, and2);
    Ciphertext gin151B = 2 * ct_my_string19 + (1 - gout116);
    Ciphertext gout151B = ctx.eval_lut(&gin151B, and2);
    Ciphertext gout151 = gout151A + gout151B;
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_my_string3 + gout113;
    Ciphertext gout152 = ctx.eval_lut(&gin152, nand2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * ct_my_string6 + gout113;
    Ciphertext gout153 = ctx.eval_lut(&gin153, nand2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * ct_my_string18 + gout116;
    Ciphertext gout154 = ctx.eval_lut(&gin154, nand2);
    std::cerr << "\rIMUX155  ";
    Ciphertext gin155A = 2 * ct_my_string5 + gout116;
    Ciphertext gout155A = ctx.eval_lut(&gin155A, and2);
    Ciphertext gin155B = 2 * ct_my_string21 + (1 - gout116);
    Ciphertext gout155B = ctx.eval_lut(&gin155B, and2);
    Ciphertext gout155 = gout155A + gout155B;
    std::cerr << "\rIMUX156  ";
    Ciphertext gin156A = 2 * ct_my_string4 + gout116;
    Ciphertext gout156A = ctx.eval_lut(&gin156A, and2);
    Ciphertext gin156B = 2 * ct_my_string20 + (1 - gout116);
    Ciphertext gout156B = ctx.eval_lut(&gin156B, and2);
    Ciphertext gout156 = gout156A + gout156B;
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * ct_my_string39 + gout118;
    Ciphertext gout157 = ctx.eval_lut(&gin157, nand2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * ct_my_string17 + gout116;
    Ciphertext gout158 = ctx.eval_lut(&gin158, nand2);
    std::cerr << "\rIMUX159  ";
    Ciphertext gin159A = 2 * ct_my_string4 + gout118;
    Ciphertext gout159A = ctx.eval_lut(&gin159A, and2);
    Ciphertext gin159B = 2 * ct_my_string36 + (1 - gout118);
    Ciphertext gout159B = ctx.eval_lut(&gin159B, and2);
    Ciphertext gout159 = gout159A + gout159B;
    std::cerr << "\rIMUX160  ";
    Ciphertext gin160A = 2 * ct_my_string5 + gout118;
    Ciphertext gout160A = ctx.eval_lut(&gin160A, and2);
    Ciphertext gin160B = 2 * ct_my_string37 + (1 - gout118);
    Ciphertext gout160B = ctx.eval_lut(&gin160B, and2);
    Ciphertext gout160 = gout160A + gout160B;
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * ct_my_string38 + gout118;
    Ciphertext gout161 = ctx.eval_lut(&gin161, nand2);
    std::cerr << "\rIMUX162  ";
    Ciphertext gin162A = 2 * ct_my_string0 + gout116;
    Ciphertext gout162A = ctx.eval_lut(&gin162A, and2);
    Ciphertext gin162B = 2 * ct_my_string16 + (1 - gout116);
    Ciphertext gout162B = ctx.eval_lut(&gin162B, and2);
    Ciphertext gout162 = gout162A + gout162B;
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_my_string60 + gout125;
    Ciphertext gout163 = ctx.eval_lut(&gin163, nand2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_my_string16 + gout116;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rIMUX165  ";
    Ciphertext gin165A = 2 * ct_my_string2 + gout116;
    Ciphertext gout165A = ctx.eval_lut(&gin165A, and2);
    Ciphertext gin165B = 2 * ct_my_string18 + (1 - gout116);
    Ciphertext gout165B = ctx.eval_lut(&gin165B, and2);
    Ciphertext gout165 = gout165A + gout165B;
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * gout106 + gout91;
    Ciphertext gout166 = ctx.eval_lut(&gin166, and2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_my_string22 + gout116;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * ct_my_string4 + gout113;
    Ciphertext gout168 = ctx.eval_lut(&gin168, nand2);
    std::cerr << "\rIMUX169  ";
    Ciphertext gin169A = 2 * ct_my_string1 + gout116;
    Ciphertext gout169A = ctx.eval_lut(&gin169A, and2);
    Ciphertext gin169B = 2 * ct_my_string17 + (1 - gout116);
    Ciphertext gout169B = ctx.eval_lut(&gin169B, and2);
    Ciphertext gout169 = gout169A + gout169B;
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * ct_my_string0 + gout113;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * ct_my_string57 + gout125;
    Ciphertext gout171 = ctx.eval_lut(&gin171, nand2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * ct_my_string7 + gout113;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * ct_my_string35 + gout118;
    Ciphertext gout173 = ctx.eval_lut(&gin173, nand2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * ct_my_string63 + gout125;
    Ciphertext gout174 = ctx.eval_lut(&gin174, nand2);
    std::cerr << "\rIMUX175  ";
    Ciphertext gin175A = 2 * ct_my_string2 + gout118;
    Ciphertext gout175A = ctx.eval_lut(&gin175A, and2);
    Ciphertext gin175B = 2 * ct_my_string34 + (1 - gout118);
    Ciphertext gout175B = ctx.eval_lut(&gin175B, and2);
    Ciphertext gout175 = gout175A + gout175B;
    std::cerr << "\rIMUX176  ";
    Ciphertext gin176A = 2 * ct_my_string1 + gout118;
    Ciphertext gout176A = ctx.eval_lut(&gin176A, and2);
    Ciphertext gin176B = 2 * ct_my_string33 + (1 - gout118);
    Ciphertext gout176B = ctx.eval_lut(&gin176B, and2);
    Ciphertext gout176 = gout176A + gout176B;
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * ct_my_string1 + gout113;
    Ciphertext gout177 = ctx.eval_lut(&gin177, nand2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * ct_my_string36 + gout118;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * ct_my_string59 + gout125;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rIMUX180  ";
    Ciphertext gin180A = 2 * ct_my_string6 + gout118;
    Ciphertext gout180A = ctx.eval_lut(&gin180A, and2);
    Ciphertext gin180B = 2 * ct_my_string38 + (1 - gout118);
    Ciphertext gout180B = ctx.eval_lut(&gin180B, and2);
    Ciphertext gout180 = gout180A + gout180B;
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * ct_my_string56 + gout125;
    Ciphertext gout181 = ctx.eval_lut(&gin181, nand2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * ct_my_string19 + gout116;
    Ciphertext gout182 = ctx.eval_lut(&gin182, nand2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * ct_my_string34 + gout118;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout81 + gout134;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rIMUX185  ";
    Ciphertext gin185A = 2 * ct_my_string0 + gout118;
    Ciphertext gout185A = ctx.eval_lut(&gin185A, and2);
    Ciphertext gin185B = 2 * ct_my_string32 + (1 - gout118);
    Ciphertext gout185B = ctx.eval_lut(&gin185B, and2);
    Ciphertext gout185 = gout185A + gout185B;
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * ct_my_string23 + gout116;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * ct_my_string58 + gout125;
    Ciphertext gout187 = ctx.eval_lut(&gin187, nand2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * ct_my_string20 + gout116;
    Ciphertext gout188 = ctx.eval_lut(&gin188, nand2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * ct_my_string32 + gout118;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * gout123 + gout92;
    Ciphertext gout190 = ctx.eval_lut(&gin190, and2);
    std::cerr << "\rIMUX191  ";
    Ciphertext gin191A = 2 * ct_my_string7 + gout116;
    Ciphertext gout191A = ctx.eval_lut(&gin191A, and2);
    Ciphertext gin191B = 2 * ct_my_string23 + (1 - gout116);
    Ciphertext gout191B = ctx.eval_lut(&gin191B, and2);
    Ciphertext gout191 = gout191A + gout191B;
    std::cerr << "\rIMUX192  ";
    Ciphertext gin192A = 2 * ct_my_string0 + gout138;
    Ciphertext gout192A = ctx.eval_lut(&gin192A, and2);
    Ciphertext gin192B = 2 * ct_my_string48 + (1 - gout138);
    Ciphertext gout192B = ctx.eval_lut(&gin192B, and2);
    Ciphertext gout192 = gout192A + gout192B;
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * ct_my_string21 + gout116;
    Ciphertext gout193 = ctx.eval_lut(&gin193, nand2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * ct_my_string33 + gout118;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * ct_my_string51 + gout138;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout86 + gout134;
    Ciphertext gout196 = ctx.eval_lut(&gin196, nand2);
    std::cerr << "\rIMUX197  ";
    Ciphertext gin197A = 2 * ct_my_string7 + gout118;
    Ciphertext gout197A = ctx.eval_lut(&gin197A, and2);
    Ciphertext gin197B = 2 * ct_my_string39 + (1 - gout118);
    Ciphertext gout197B = ctx.eval_lut(&gin197B, and2);
    Ciphertext gout197 = gout197A + gout197B;
    std::cerr << "\rIMUX198  ";
    Ciphertext gin198A = 2 * ct_my_string3 + gout118;
    Ciphertext gout198A = ctx.eval_lut(&gin198A, and2);
    Ciphertext gin198B = 2 * ct_my_string35 + (1 - gout118);
    Ciphertext gout198B = ctx.eval_lut(&gin198B, and2);
    Ciphertext gout198 = gout198A + gout198B;
    std::cerr << "\rIMUX199  ";
    Ciphertext gin199A = 2 * ct_my_string7 + gout144;
    Ciphertext gout199A = ctx.eval_lut(&gin199A, and2);
    Ciphertext gin199B = 2 * ct_my_string31 + (1 - gout144);
    Ciphertext gout199B = ctx.eval_lut(&gin199B, and2);
    Ciphertext gout199 = gout199A + gout199B;
    std::cerr << "\rIMUX200  ";
    Ciphertext gin200A = 2 * gout110 + gout138;
    Ciphertext gout200A = ctx.eval_lut(&gin200A, and2);
    Ciphertext gin200B = 2 * gout122 + (1 - gout138);
    Ciphertext gout200B = ctx.eval_lut(&gin200B, and2);
    Ciphertext gout200 = gout200A + gout200B;
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout119 + gout106;
    Ciphertext gout201 = ctx.eval_lut(&gin201, and2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout138 + gout136;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rIMUX203  ";
    Ciphertext gin203A = 2 * ct_my_string5 + gout138;
    Ciphertext gout203A = ctx.eval_lut(&gin203A, and2);
    Ciphertext gin203B = 2 * ct_my_string53 + (1 - gout138);
    Ciphertext gout203B = ctx.eval_lut(&gin203B, and2);
    Ciphertext gout203 = gout203A + gout203B;
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout139 + gout111;
    Ciphertext gout204 = ctx.eval_lut(&gin204, and2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * ct_my_string37 + gout118;
    Ciphertext gout205 = ctx.eval_lut(&gin205, nand2);
    std::cerr << "\rIMUX206  ";
    Ciphertext gin206A = 2 * ct_my_string2 + gout144;
    Ciphertext gout206A = ctx.eval_lut(&gin206A, and2);
    Ciphertext gin206B = 2 * ct_my_string26 + (1 - gout144);
    Ciphertext gout206B = ctx.eval_lut(&gin206B, and2);
    Ciphertext gout206 = gout206A + gout206B;
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * ct_my_string2 + gout113;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout120 + gout117;
    Ciphertext gout208 = ctx.eval_lut(&gin208, and2);
    std::cerr << "\rIMUX209  ";
    Ciphertext gin209A = 2 * ct_my_string6 + gout138;
    Ciphertext gout209A = ctx.eval_lut(&gin209A, and2);
    Ciphertext gin209B = 2 * ct_my_string54 + (1 - gout138);
    Ciphertext gout209B = ctx.eval_lut(&gin209B, and2);
    Ciphertext gout209 = gout209A + gout209B;
    std::cerr << "\rIMUX210  ";
    Ciphertext gin210A = 2 * ct_my_string2 + gout138;
    Ciphertext gout210A = ctx.eval_lut(&gin210A, and2);
    Ciphertext gin210B = 2 * ct_my_string50 + (1 - gout138);
    Ciphertext gout210B = ctx.eval_lut(&gin210B, and2);
    Ciphertext gout210 = gout210A + gout210B;
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout137 + gout140;
    Ciphertext gout211 = ctx.eval_lut(&gin211, and2);
    std::cerr << "\rIMUX212  ";
    Ciphertext gin212A = 2 * ct_my_string3 + gout144;
    Ciphertext gout212A = ctx.eval_lut(&gin212A, and2);
    Ciphertext gin212B = 2 * ct_my_string27 + (1 - gout144);
    Ciphertext gout212B = ctx.eval_lut(&gin212B, and2);
    Ciphertext gout212 = gout212A + gout212B;
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * ct_my_string61 + gout125;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout87 + gout134;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rIMUX215  ";
    Ciphertext gin215A = 2 * ct_my_string0 + gout144;
    Ciphertext gout215A = ctx.eval_lut(&gin215A, and2);
    Ciphertext gin215B = 2 * ct_my_string24 + (1 - gout144);
    Ciphertext gout215B = ctx.eval_lut(&gin215B, and2);
    Ciphertext gout215 = gout215A + gout215B;
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * ct_my_string55 + gout138;
    Ciphertext gout216 = ctx.eval_lut(&gin216, nand2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * ct_my_string62 + gout125;
    Ciphertext gout217 = ctx.eval_lut(&gin217, nand2);
    std::cerr << "\rIMUX218  ";
    Ciphertext gin218A = 2 * gout112 + gout138;
    Ciphertext gout218A = ctx.eval_lut(&gin218A, and2);
    Ciphertext gin218B = 2 * gout136 + (1 - gout138);
    Ciphertext gout218B = ctx.eval_lut(&gin218B, and2);
    Ciphertext gout218 = gout218A + gout218B;
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout144 + gout148;
    Ciphertext gout219 = ctx.eval_lut(&gin219, nand2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout84 + gout134;
    Ciphertext gout220 = ctx.eval_lut(&gin220, nand2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * ct_my_string25 + gout144;
    Ciphertext gout221 = ctx.eval_lut(&gin221, nand2);
    std::cerr << "\rIMUX222  ";
    Ciphertext gin222A = 2 * gout115 + gout144;
    Ciphertext gout222A = ctx.eval_lut(&gin222A, and2);
    Ciphertext gin222B = 2 * gout148 + (1 - gout144);
    Ciphertext gout222B = ctx.eval_lut(&gin222B, and2);
    Ciphertext gout222 = gout222A + gout222B;
    std::cerr << "\rIMUX223  ";
    Ciphertext gin223A = 2 * ct_my_string1 + gout138;
    Ciphertext gout223A = ctx.eval_lut(&gin223A, and2);
    Ciphertext gin223B = 2 * ct_my_string49 + (1 - gout138);
    Ciphertext gout223B = ctx.eval_lut(&gin223B, and2);
    Ciphertext gout223 = gout223A + gout223B;
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * ct_my_string48 + gout138;
    Ciphertext gout224 = ctx.eval_lut(&gin224, nand2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout138 + gout121;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rIMUX226  ";
    Ciphertext gin226A = 2 * ct_my_string5 + gout144;
    Ciphertext gout226A = ctx.eval_lut(&gin226A, and2);
    Ciphertext gin226B = 2 * ct_my_string29 + (1 - gout144);
    Ciphertext gout226B = ctx.eval_lut(&gin226B, and2);
    Ciphertext gout226 = gout226A + gout226B;
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout144 + gout162;
    Ciphertext gout227 = ctx.eval_lut(&gin227, nand2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout128 + gout107;
    Ciphertext gout228 = ctx.eval_lut(&gin228, and2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * ct_my_string52 + gout138;
    Ciphertext gout229 = ctx.eval_lut(&gin229, nand2);
    std::cerr << "\rIMUX230  ";
    Ciphertext gin230A = 2 * gout124 + gout138;
    Ciphertext gout230A = ctx.eval_lut(&gin230A, and2);
    Ciphertext gin230B = 2 * gout129 + (1 - gout138);
    Ciphertext gout230B = ctx.eval_lut(&gin230B, and2);
    Ciphertext gout230 = gout230A + gout230B;
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * ct_my_string50 + gout138;
    Ciphertext gout231 = ctx.eval_lut(&gin231, nand2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * ct_my_string28 + gout144;
    Ciphertext gout232 = ctx.eval_lut(&gin232, nand2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout138 + gout130;
    Ciphertext gout233 = ctx.eval_lut(&gin233, nand2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout132 + gout97;
    Ciphertext gout234 = ctx.eval_lut(&gin234, and2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * ct_my_string53 + gout138;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout138 + gout146;
    Ciphertext gout236 = ctx.eval_lut(&gin236, nand2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * ct_my_string27 + gout144;
    Ciphertext gout237 = ctx.eval_lut(&gin237, nand2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout138 + gout129;
    Ciphertext gout238 = ctx.eval_lut(&gin238, nand2);
    std::cerr << "\rIMUX239  ";
    Ciphertext gin239A = 2 * gout112 + gout144;
    Ciphertext gout239A = ctx.eval_lut(&gin239A, and2);
    Ciphertext gin239B = 2 * gout165 + (1 - gout144);
    Ciphertext gout239B = ctx.eval_lut(&gin239B, and2);
    Ciphertext gout239 = gout239A + gout239B;
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * ct_my_string31 + gout144;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rIMUX241  ";
    Ciphertext gin241A = 2 * ct_my_string4 + gout138;
    Ciphertext gout241A = ctx.eval_lut(&gin241A, and2);
    Ciphertext gin241B = 2 * ct_my_string52 + (1 - gout138);
    Ciphertext gout241B = ctx.eval_lut(&gin241B, and2);
    Ciphertext gout241 = gout241A + gout241B;
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * ct_my_string49 + gout138;
    Ciphertext gout242 = ctx.eval_lut(&gin242, nand2);
    std::cerr << "\rIMUX243  ";
    Ciphertext gin243A = 2 * gout141 + gout144;
    Ciphertext gout243A = ctx.eval_lut(&gin243A, and2);
    Ciphertext gin243B = 2 * gout191 + (1 - gout144);
    Ciphertext gout243B = ctx.eval_lut(&gin243B, and2);
    Ciphertext gout243 = gout243A + gout243B;
    std::cerr << "\rIMUX244  ";
    Ciphertext gin244A = 2 * gout110 + gout144;
    Ciphertext gout244A = ctx.eval_lut(&gin244A, and2);
    Ciphertext gin244B = 2 * gout156 + (1 - gout144);
    Ciphertext gout244B = ctx.eval_lut(&gin244B, and2);
    Ciphertext gout244 = gout244A + gout244B;
    std::cerr << "\rIMUX245  ";
    Ciphertext gin245A = 2 * ct_my_string7 + gout138;
    Ciphertext gout245A = ctx.eval_lut(&gin245A, and2);
    Ciphertext gin245B = 2 * ct_my_string55 + (1 - gout138);
    Ciphertext gout245B = ctx.eval_lut(&gin245B, and2);
    Ciphertext gout245 = gout245A + gout245B;
    std::cerr << "\rIMUX246  ";
    Ciphertext gin246A = 2 * gout133 + gout144;
    Ciphertext gout246A = ctx.eval_lut(&gin246A, and2);
    Ciphertext gin246B = 2 * gout151 + (1 - gout144);
    Ciphertext gout246B = ctx.eval_lut(&gin246B, and2);
    Ciphertext gout246 = gout246A + gout246B;
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout142 + gout123;
    Ciphertext gout247 = ctx.eval_lut(&gin247, and2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout144 + gout169;
    Ciphertext gout248 = ctx.eval_lut(&gin248, nand2);
    std::cerr << "\rIMUX249  ";
    Ciphertext gin249A = 2 * gout133 + gout138;
    Ciphertext gout249A = ctx.eval_lut(&gin249A, and2);
    Ciphertext gin249B = 2 * gout146 + (1 - gout138);
    Ciphertext gout249B = ctx.eval_lut(&gin249B, and2);
    Ciphertext gout249 = gout249A + gout249B;
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout88 + gout134;
    Ciphertext gout250 = ctx.eval_lut(&gin250, nand2);
    std::cerr << "\rIMUX251  ";
    Ciphertext gin251A = 2 * gout109 + gout138;
    Ciphertext gout251A = ctx.eval_lut(&gin251A, and2);
    Ciphertext gin251B = 2 * gout126 + (1 - gout138);
    Ciphertext gout251B = ctx.eval_lut(&gin251B, and2);
    Ciphertext gout251 = gout251A + gout251B;
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * ct_my_string54 + gout138;
    Ciphertext gout252 = ctx.eval_lut(&gin252, nand2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout138 + gout126;
    Ciphertext gout253 = ctx.eval_lut(&gin253, nand2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout138 + gout122;
    Ciphertext gout254 = ctx.eval_lut(&gin254, nand2);
    std::cerr << "\rIMUX255  ";
    Ciphertext gin255A = 2 * ct_my_string6 + gout144;
    Ciphertext gout255A = ctx.eval_lut(&gin255A, and2);
    Ciphertext gin255B = 2 * ct_my_string30 + (1 - gout144);
    Ciphertext gout255B = ctx.eval_lut(&gin255B, and2);
    Ciphertext gout255 = gout255A + gout255B;
    std::cerr << "\rIMUX256  ";
    Ciphertext gin256A = 2 * ct_my_string3 + gout138;
    Ciphertext gout256A = ctx.eval_lut(&gin256A, and2);
    Ciphertext gin256B = 2 * ct_my_string51 + (1 - gout138);
    Ciphertext gout256B = ctx.eval_lut(&gin256B, and2);
    Ciphertext gout256 = gout256A + gout256B;
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * gout131 + gout132;
    Ciphertext gout257 = ctx.eval_lut(&gin257, and2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout79 + gout134;
    Ciphertext gout258 = ctx.eval_lut(&gin258, nand2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * ct_my_string26 + gout144;
    Ciphertext gout259 = ctx.eval_lut(&gin259, nand2);
    std::cerr << "\rIMUX260  ";
    Ciphertext gin260A = 2 * gout115 + gout138;
    Ciphertext gout260A = ctx.eval_lut(&gin260A, and2);
    Ciphertext gin260B = 2 * gout121 + (1 - gout138);
    Ciphertext gout260B = ctx.eval_lut(&gin260B, and2);
    Ciphertext gout260 = gout260A + gout260B;
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout82 + gout134;
    Ciphertext gout261 = ctx.eval_lut(&gin261, nand2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout138 + gout143;
    Ciphertext gout262 = ctx.eval_lut(&gin262, nand2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * ct_my_string30 + gout144;
    Ciphertext gout263 = ctx.eval_lut(&gin263, nand2);
    std::cerr << "\rIMUX264  ";
    Ciphertext gin264A = 2 * gout141 + gout138;
    Ciphertext gout264A = ctx.eval_lut(&gin264A, and2);
    Ciphertext gin264B = 2 * gout130 + (1 - gout138);
    Ciphertext gout264B = ctx.eval_lut(&gin264B, and2);
    Ciphertext gout264 = gout264A + gout264B;
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout85 + gout134;
    Ciphertext gout265 = ctx.eval_lut(&gin265, nand2);
    std::cerr << "\rIMUX266  ";
    Ciphertext gin266A = 2 * gout108 + gout138;
    Ciphertext gout266A = ctx.eval_lut(&gin266A, and2);
    Ciphertext gin266B = 2 * gout143 + (1 - gout138);
    Ciphertext gout266B = ctx.eval_lut(&gin266B, and2);
    Ciphertext gout266 = gout266A + gout266B;
    std::cerr << "\rIMUX267  ";
    Ciphertext gin267A = 2 * gout109 + gout144;
    Ciphertext gout267A = ctx.eval_lut(&gin267A, and2);
    Ciphertext gin267B = 2 * gout162 + (1 - gout144);
    Ciphertext gout267B = ctx.eval_lut(&gin267B, and2);
    Ciphertext gout267 = gout267A + gout267B;
    std::cerr << "\rIMUX268  ";
    Ciphertext gin268A = 2 * gout124 + gout144;
    Ciphertext gout268A = ctx.eval_lut(&gin268A, and2);
    Ciphertext gin268B = 2 * gout169 + (1 - gout144);
    Ciphertext gout268B = ctx.eval_lut(&gin268B, and2);
    Ciphertext gout268 = gout268A + gout268B;
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout135 + gout114;
    Ciphertext gout269 = ctx.eval_lut(&gin269, and2);
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * ct_my_string29 + gout144;
    Ciphertext gout270 = ctx.eval_lut(&gin270, nand2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout144 + gout156;
    Ciphertext gout271 = ctx.eval_lut(&gin271, nand2);
    std::cerr << "\rIMUX272  ";
    Ciphertext gin272A = 2 * ct_my_string1 + gout144;
    Ciphertext gout272A = ctx.eval_lut(&gin272A, and2);
    Ciphertext gin272B = 2 * ct_my_string25 + (1 - gout144);
    Ciphertext gout272B = ctx.eval_lut(&gin272B, and2);
    Ciphertext gout272 = gout272A + gout272B;
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout140 + gout94;
    Ciphertext gout273 = ctx.eval_lut(&gin273, and2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout144 + gout155;
    Ciphertext gout274 = ctx.eval_lut(&gin274, nand2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout144 + gout191;
    Ciphertext gout275 = ctx.eval_lut(&gin275, nand2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * ct_my_string24 + gout144;
    Ciphertext gout276 = ctx.eval_lut(&gin276, nand2);
    std::cerr << "\rIMUX277  ";
    Ciphertext gin277A = 2 * ct_my_string4 + gout144;
    Ciphertext gout277A = ctx.eval_lut(&gin277A, and2);
    Ciphertext gin277B = 2 * ct_my_string28 + (1 - gout144);
    Ciphertext gout277B = ctx.eval_lut(&gin277B, and2);
    Ciphertext gout277 = gout277A + gout277B;
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout144 + gout151;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rIMUX279  ";
    Ciphertext gin279A = 2 * gout108 + gout144;
    Ciphertext gout279A = ctx.eval_lut(&gin279A, and2);
    Ciphertext gin279B = 2 * gout155 + (1 - gout144);
    Ciphertext gout279B = ctx.eval_lut(&gin279B, and2);
    Ciphertext gout279 = gout279A + gout279B;
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout144 + gout165;
    Ciphertext gout280 = ctx.eval_lut(&gin280, nand2);
    std::cerr << "\rIMUX281  ";
    Ciphertext gin281A = 2 * gout124 + gout105;
    Ciphertext gout281A = ctx.eval_lut(&gin281A, and2);
    Ciphertext gin281B = 2 * gout176 + (1 - gout105);
    Ciphertext gout281B = ctx.eval_lut(&gin281B, and2);
    Ciphertext gout281 = gout281A + gout281B;
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout105 + gout176;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rIMUX283  ";
    Ciphertext gin283A = 2 * gout109 + gout105;
    Ciphertext gout283A = ctx.eval_lut(&gin283A, and2);
    Ciphertext gin283B = 2 * gout185 + (1 - gout105);
    Ciphertext gout283B = ctx.eval_lut(&gin283B, and2);
    Ciphertext gout283 = gout283A + gout283B;
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout157 + gout186;
    Ciphertext gout284 = ctx.eval_lut(&gin284, and2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout202 + gout190;
    Ciphertext gout285 = ctx.eval_lut(&gin285, and2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout158 + gout194;
    Ciphertext gout286 = ctx.eval_lut(&gin286, and2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout105 + gout160;
    Ciphertext gout287 = ctx.eval_lut(&gin287, nand2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout75 + gout200;
    Ciphertext gout288 = ctx.eval_lut(&gin288, nand2);
    std::cerr << "\rIMUX289  ";
    Ciphertext gin289A = 2 * gout112 + gout75;
    Ciphertext gout289A = ctx.eval_lut(&gin289A, and2);
    Ciphertext gin289B = 2 * gout210 + (1 - gout75);
    Ciphertext gout289B = ctx.eval_lut(&gin289B, and2);
    Ciphertext gout289 = gout289A + gout289B;
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout167 + gout196;
    Ciphertext gout290 = ctx.eval_lut(&gin290, and2);
    std::cerr << "\rIMUX291  ";
    Ciphertext gin291A = 2 * gout110 + gout105;
    Ciphertext gout291A = ctx.eval_lut(&gin291A, and2);
    Ciphertext gin291B = 2 * gout159 + (1 - gout105);
    Ciphertext gout291B = ctx.eval_lut(&gin291B, and2);
    Ciphertext gout291 = gout291A + gout291B;
    std::cerr << "\rIMUX292  ";
    Ciphertext gin292A = 2 * gout108 + gout105;
    Ciphertext gout292A = ctx.eval_lut(&gin292A, and2);
    Ciphertext gin292B = 2 * gout160 + (1 - gout105);
    Ciphertext gout292B = ctx.eval_lut(&gin292B, and2);
    Ciphertext gout292 = gout292A + gout292B;
    std::cerr << "\rIMUX293  ";
    Ciphertext gin293A = 2 * gout115 + gout105;
    Ciphertext gout293A = ctx.eval_lut(&gin293A, and2);
    Ciphertext gin293B = 2 * gout180 + (1 - gout105);
    Ciphertext gout293B = ctx.eval_lut(&gin293B, and2);
    Ciphertext gout293 = gout293A + gout293B;
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout195 + gout182;
    Ciphertext gout294 = ctx.eval_lut(&gin294, and2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout138 + gout281;
    Ciphertext gout295 = ctx.eval_lut(&gin295, nand2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout105 + gout198;
    Ciphertext gout296 = ctx.eval_lut(&gin296, nand2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout164 + gout184;
    Ciphertext gout297 = ctx.eval_lut(&gin297, and2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout179 + gout211;
    Ciphertext gout298 = ctx.eval_lut(&gin298, and2);
    std::cerr << "\rIMUX299  ";
    Ciphertext gin299A = 2 * gout112 + gout118;
    Ciphertext gout299A = ctx.eval_lut(&gin299A, and2);
    Ciphertext gin299B = 2 * gout206 + (1 - gout118);
    Ciphertext gout299B = ctx.eval_lut(&gin299B, and2);
    Ciphertext gout299 = gout299A + gout299B;
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout118 + gout212;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout75 + gout210;
    Ciphertext gout301 = ctx.eval_lut(&gin301, nand2);
    std::cerr << "\rIMUX302  ";
    Ciphertext gin302A = 2 * gout141 + gout118;
    Ciphertext gout302A = ctx.eval_lut(&gin302A, and2);
    Ciphertext gin302B = 2 * gout199 + (1 - gout118);
    Ciphertext gout302B = ctx.eval_lut(&gin302B, and2);
    Ciphertext gout302 = gout302A + gout302B;
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout118 + gout206;
    Ciphertext gout303 = ctx.eval_lut(&gin303, nand2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout105 + gout159;
    Ciphertext gout304 = ctx.eval_lut(&gin304, nand2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout174 + gout216;
    Ciphertext gout305 = ctx.eval_lut(&gin305, and2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout186 + gout220;
    Ciphertext gout306 = ctx.eval_lut(&gin306, and2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout75 + gout218;
    Ciphertext gout307 = ctx.eval_lut(&gin307, nand2);
    std::cerr << "\rIMUX308  ";
    Ciphertext gin308A = 2 * gout222 + gout138;
    Ciphertext gout308A = ctx.eval_lut(&gin308A, and2);
    Ciphertext gin308B = 2 * gout293 + (1 - gout138);
    Ciphertext gout308B = ctx.eval_lut(&gin308B, and2);
    Ciphertext gout308 = gout308A + gout308B;
    std::cerr << "\rIMUX309  ";
    Ciphertext gin309A = 2 * gout141 + gout105;
    Ciphertext gout309A = ctx.eval_lut(&gin309A, and2);
    Ciphertext gin309B = 2 * gout197 + (1 - gout105);
    Ciphertext gout309B = ctx.eval_lut(&gin309B, and2);
    Ciphertext gout309 = gout309A + gout309B;
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout75 + gout192;
    Ciphertext gout310 = ctx.eval_lut(&gin310, nand2);
    std::cerr << "\rIMUX311  ";
    Ciphertext gin311A = 2 * gout239 + gout105;
    Ciphertext gout311A = ctx.eval_lut(&gin311A, and2);
    Ciphertext gin311B = 2 * gout299 + (1 - gout105);
    Ciphertext gout311B = ctx.eval_lut(&gin311B, and2);
    Ciphertext gout311 = gout311A + gout311B;
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout242 + gout221;
    Ciphertext gout312 = ctx.eval_lut(&gin312, and2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout118 + gout215;
    Ciphertext gout313 = ctx.eval_lut(&gin313, nand2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout75 + gout245;
    Ciphertext gout314 = ctx.eval_lut(&gin314, nand2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout188 + gout250;
    Ciphertext gout315 = ctx.eval_lut(&gin315, and2);
    std::cerr << "\rIMUX316  ";
    Ciphertext gin316A = 2 * gout239 + gout75;
    Ciphertext gout316A = ctx.eval_lut(&gin316A, and2);
    Ciphertext gin316B = 2 * gout218 + (1 - gout75);
    Ciphertext gout316B = ctx.eval_lut(&gin316B, and2);
    Ciphertext gout316 = gout316A + gout316B;
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout225 + gout234;
    Ciphertext gout317 = ctx.eval_lut(&gin317, and2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout75 + gout209;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout233 + gout150;
    Ciphertext gout319 = ctx.eval_lut(&gin319, and2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout75 + gout249;
    Ciphertext gout320 = ctx.eval_lut(&gin320, nand2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout105 + gout175;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rIMUX322  ";
    Ciphertext gin322A = 2 * gout243 + gout138;
    Ciphertext gout322A = ctx.eval_lut(&gin322A, and2);
    Ciphertext gin322B = 2 * gout309 + (1 - gout138);
    Ciphertext gout322B = ctx.eval_lut(&gin322B, and2);
    Ciphertext gout322 = gout322A + gout322B;
    std::cerr << "\rIMUX323  ";
    Ciphertext gin323A = 2 * gout109 + gout118;
    Ciphertext gout323A = ctx.eval_lut(&gin323A, and2);
    Ciphertext gin323B = 2 * gout215 + (1 - gout118);
    Ciphertext gout323B = ctx.eval_lut(&gin323B, and2);
    Ciphertext gout323 = gout323A + gout323B;
    std::cerr << "\rIMUX324  ";
    Ciphertext gin324A = 2 * gout112 + gout105;
    Ciphertext gout324A = ctx.eval_lut(&gin324A, and2);
    Ciphertext gin324B = 2 * gout175 + (1 - gout105);
    Ciphertext gout324B = ctx.eval_lut(&gin324B, and2);
    Ciphertext gout324 = gout324A + gout324B;
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout173 + gout237;
    Ciphertext gout325 = ctx.eval_lut(&gin325, and2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout105 + gout180;
    Ciphertext gout326 = ctx.eval_lut(&gin326, nand2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout161 + gout217;
    Ciphertext gout327 = ctx.eval_lut(&gin327, and2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout213 + gout201;
    Ciphertext gout328 = ctx.eval_lut(&gin328, and2);
    std::cerr << "\rIMUX329  ";
    Ciphertext gin329A = 2 * gout108 + gout75;
    Ciphertext gout329A = ctx.eval_lut(&gin329A, and2);
    Ciphertext gin329B = 2 * gout203 + (1 - gout75);
    Ciphertext gout329B = ctx.eval_lut(&gin329B, and2);
    Ciphertext gout329 = gout329A + gout329B;
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout183 + gout259;
    Ciphertext gout330 = ctx.eval_lut(&gin330, and2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout178 + gout232;
    Ciphertext gout331 = ctx.eval_lut(&gin331, and2);
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout181 + gout208;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rIMUX333  ";
    Ciphertext gin333A = 2 * gout109 + gout75;
    Ciphertext gout333A = ctx.eval_lut(&gin333A, and2);
    Ciphertext gin333B = 2 * gout192 + (1 - gout75);
    Ciphertext gout333B = ctx.eval_lut(&gin333B, and2);
    Ciphertext gout333 = gout333A + gout333B;
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout238 + gout127;
    Ciphertext gout334 = ctx.eval_lut(&gin334, and2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout263 + gout252;
    Ciphertext gout335 = ctx.eval_lut(&gin335, and2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout154 + gout265;
    Ciphertext gout336 = ctx.eval_lut(&gin336, and2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout105 + gout185;
    Ciphertext gout337 = ctx.eval_lut(&gin337, nand2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout75 + gout264;
    Ciphertext gout338 = ctx.eval_lut(&gin338, nand2);
    std::cerr << "\rIMUX339  ";
    Ciphertext gin339A = 2 * gout110 + gout75;
    Ciphertext gout339A = ctx.eval_lut(&gin339A, and2);
    Ciphertext gin339B = 2 * gout241 + (1 - gout75);
    Ciphertext gout339B = ctx.eval_lut(&gin339B, and2);
    Ciphertext gout339 = gout339A + gout339B;
    std::cerr << "\rIMUX340  ";
    Ciphertext gin340A = 2 * gout115 + gout75;
    Ciphertext gout340A = ctx.eval_lut(&gin340A, and2);
    Ciphertext gin340B = 2 * gout209 + (1 - gout75);
    Ciphertext gout340B = ctx.eval_lut(&gin340B, and2);
    Ciphertext gout340 = gout340A + gout340B;
    std::cerr << "\rIMUX341  ";
    Ciphertext gin341A = 2 * gout108 + gout118;
    Ciphertext gout341A = ctx.eval_lut(&gin341A, and2);
    Ciphertext gin341B = 2 * gout226 + (1 - gout118);
    Ciphertext gout341B = ctx.eval_lut(&gin341B, and2);
    Ciphertext gout341 = gout341A + gout341B;
    std::cerr << "\rIMUX342  ";
    Ciphertext gin342A = 2 * gout133 + gout118;
    Ciphertext gout342A = ctx.eval_lut(&gin342A, and2);
    Ciphertext gin342B = 2 * gout212 + (1 - gout118);
    Ciphertext gout342B = ctx.eval_lut(&gin342B, and2);
    Ciphertext gout342 = gout342A + gout342B;
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout182 + gout261;
    Ciphertext gout343 = ctx.eval_lut(&gin343, and2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout219 + gout290;
    Ciphertext gout344 = ctx.eval_lut(&gin344, and2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout163 + gout204;
    Ciphertext gout345 = ctx.eval_lut(&gin345, and2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout105 + gout197;
    Ciphertext gout346 = ctx.eval_lut(&gin346, nand2);
    std::cerr << "\rIMUX347  ";
    Ciphertext gin347A = 2 * gout133 + gout105;
    Ciphertext gout347A = ctx.eval_lut(&gin347A, and2);
    Ciphertext gin347B = 2 * gout198 + (1 - gout105);
    Ciphertext gout347B = ctx.eval_lut(&gin347B, and2);
    Ciphertext gout347 = gout347A + gout347B;
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout118 + gout255;
    Ciphertext gout348 = ctx.eval_lut(&gin348, nand2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout240 + gout228;
    Ciphertext gout349 = ctx.eval_lut(&gin349, and2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * gout262 + gout166;
    Ciphertext gout350 = ctx.eval_lut(&gin350, and2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout118 + gout199;
    Ciphertext gout351 = ctx.eval_lut(&gin351, nand2);
    std::cerr << "\rIMUX352  ";
    Ciphertext gin352A = 2 * gout246 + gout75;
    Ciphertext gout352A = ctx.eval_lut(&gin352A, and2);
    Ciphertext gin352B = 2 * gout249 + (1 - gout75);
    Ciphertext gout352B = ctx.eval_lut(&gin352B, and2);
    Ciphertext gout352 = gout352A + gout352B;
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout269 + gout171;
    Ciphertext gout353 = ctx.eval_lut(&gin353, and2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout296 + gout278;
    Ciphertext gout354 = ctx.eval_lut(&gin354, and2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout227 + gout297;
    Ciphertext gout355 = ctx.eval_lut(&gin355, and2);
    std::cerr << "\rIMUX356  ";
    Ciphertext gin356A = 2 * gout268 + gout75;
    Ciphertext gout356A = ctx.eval_lut(&gin356A, and2);
    Ciphertext gin356B = 2 * gout230 + (1 - gout75);
    Ciphertext gout356B = ctx.eval_lut(&gin356B, and2);
    Ciphertext gout356 = gout356A + gout356B;
    std::cerr << "\rIMUX357  ";
    Ciphertext gin357A = 2 * gout243 + gout75;
    Ciphertext gout357A = ctx.eval_lut(&gin357A, and2);
    Ciphertext gin357B = 2 * gout264 + (1 - gout75);
    Ciphertext gout357B = ctx.eval_lut(&gin357B, and2);
    Ciphertext gout357 = gout357A + gout357B;
    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout229 + gout188;
    Ciphertext gout358 = ctx.eval_lut(&gin358, and2);
    std::cerr << "\rIMUX359  ";
    Ciphertext gin359A = 2 * gout124 + gout75;
    Ciphertext gout359A = ctx.eval_lut(&gin359A, and2);
    Ciphertext gin359B = 2 * gout223 + (1 - gout75);
    Ciphertext gout359B = ctx.eval_lut(&gin359B, and2);
    Ciphertext gout359 = gout359A + gout359B;
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout75 + gout251;
    Ciphertext gout360 = ctx.eval_lut(&gin360, nand2);
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout187 + gout247;
    Ciphertext gout361 = ctx.eval_lut(&gin361, and2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout236 + gout273;
    Ciphertext gout362 = ctx.eval_lut(&gin362, and2);
    std::cerr << "\rIMUX363  ";
    Ciphertext gin363A = 2 * gout115 + gout118;
    Ciphertext gout363A = ctx.eval_lut(&gin363A, and2);
    Ciphertext gin363B = 2 * gout255 + (1 - gout118);
    Ciphertext gout363B = ctx.eval_lut(&gin363B, and2);
    Ciphertext gout363 = gout363A + gout363B;
    std::cerr << "\rIMUX364  ";
    Ciphertext gin364A = 2 * gout110 + gout118;
    Ciphertext gout364A = ctx.eval_lut(&gin364A, and2);
    Ciphertext gin364B = 2 * gout277 + (1 - gout118);
    Ciphertext gout364B = ctx.eval_lut(&gin364B, and2);
    Ciphertext gout364 = gout364A + gout364B;
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout253 + gout149;
    Ciphertext gout365 = ctx.eval_lut(&gin365, and2);
    std::cerr << "\rLUT366   ";
    Ciphertext gin366 = 2 * gout75 + gout230;
    Ciphertext gout366 = ctx.eval_lut(&gin366, nand2);
    std::cerr << "\rIMUX367  ";
    Ciphertext gin367A = 2 * gout279 + gout105;
    Ciphertext gout367A = ctx.eval_lut(&gin367A, and2);
    Ciphertext gin367B = 2 * gout341 + (1 - gout105);
    Ciphertext gout367B = ctx.eval_lut(&gin367B, and2);
    Ciphertext gout367 = gout367A + gout367B;
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout189 + gout276;
    Ciphertext gout368 = ctx.eval_lut(&gin368, and2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout158 + gout214;
    Ciphertext gout369 = ctx.eval_lut(&gin369, and2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * gout235 + gout193;
    Ciphertext gout370 = ctx.eval_lut(&gin370, and2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout224 + gout164;
    Ciphertext gout371 = ctx.eval_lut(&gin371, and2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout118 + gout277;
    Ciphertext gout372 = ctx.eval_lut(&gin372, nand2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout254 + gout145;
    Ciphertext gout373 = ctx.eval_lut(&gin373, and2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout275 + gout306;
    Ciphertext gout374 = ctx.eval_lut(&gin374, and2);
    std::cerr << "\rIMUX375  ";
    Ciphertext gin375A = 2 * gout244 + gout75;
    Ciphertext gout375A = ctx.eval_lut(&gin375A, and2);
    Ciphertext gin375B = 2 * gout200 + (1 - gout75);
    Ciphertext gout375B = ctx.eval_lut(&gin375B, and2);
    Ciphertext gout375 = gout375A + gout375B;
    std::cerr << "\rLUT376   ";
    Ciphertext gin376 = 2 * gout193 + gout258;
    Ciphertext gout376 = ctx.eval_lut(&gin376, and2);
    std::cerr << "\rLUT377   ";
    Ciphertext gin377 = 2 * gout118 + gout226;
    Ciphertext gout377 = ctx.eval_lut(&gin377, nand2);
    std::cerr << "\rLUT378   ";
    Ciphertext gin378 = 2 * gout231 + gout154;
    Ciphertext gout378 = ctx.eval_lut(&gin378, and2);
    std::cerr << "\rIMUX379  ";
    Ciphertext gin379A = 2 * gout267 + gout75;
    Ciphertext gout379A = ctx.eval_lut(&gin379A, and2);
    Ciphertext gin379B = 2 * gout251 + (1 - gout75);
    Ciphertext gout379B = ctx.eval_lut(&gin379B, and2);
    Ciphertext gout379 = gout379A + gout379B;
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout167 + gout257;
    Ciphertext gout380 = ctx.eval_lut(&gin380, and2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout75 + gout266;
    Ciphertext gout381 = ctx.eval_lut(&gin381, nand2);
    std::cerr << "\rIMUX382  ";
    Ciphertext gin382A = 2 * gout141 + gout75;
    Ciphertext gout382A = ctx.eval_lut(&gin382A, and2);
    Ciphertext gin382B = 2 * gout245 + (1 - gout75);
    Ciphertext gout382B = ctx.eval_lut(&gin382B, and2);
    Ciphertext gout382 = gout382A + gout382B;
    std::cerr << "\rIMUX383  ";
    Ciphertext gin383A = 2 * gout267 + gout105;
    Ciphertext gout383A = ctx.eval_lut(&gin383A, and2);
    Ciphertext gin383B = 2 * gout323 + (1 - gout105);
    Ciphertext gout383B = ctx.eval_lut(&gin383B, and2);
    Ciphertext gout383 = gout383A + gout383B;
    std::cerr << "\rIMUX384  ";
    Ciphertext gin384A = 2 * gout243 + gout105;
    Ciphertext gout384A = ctx.eval_lut(&gin384A, and2);
    Ciphertext gin384B = 2 * gout302 + (1 - gout105);
    Ciphertext gout384B = ctx.eval_lut(&gin384B, and2);
    Ciphertext gout384 = gout384A + gout384B;
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout75 + gout241;
    Ciphertext gout385 = ctx.eval_lut(&gin385, nand2);
    std::cerr << "\rIMUX386  ";
    Ciphertext gin386A = 2 * gout124 + gout118;
    Ciphertext gout386A = ctx.eval_lut(&gin386A, and2);
    Ciphertext gin386B = 2 * gout272 + (1 - gout118);
    Ciphertext gout386B = ctx.eval_lut(&gin386B, and2);
    Ciphertext gout386 = gout386A + gout386B;
    std::cerr << "\rIMUX387  ";
    Ciphertext gin387A = 2 * gout279 + gout75;
    Ciphertext gout387A = ctx.eval_lut(&gin387A, and2);
    Ciphertext gin387B = 2 * gout266 + (1 - gout75);
    Ciphertext gout387B = ctx.eval_lut(&gin387B, and2);
    Ciphertext gout387 = gout387A + gout387B;
    std::cerr << "\rIMUX388  ";
    Ciphertext gin388A = 2 * gout244 + gout138;
    Ciphertext gout388A = ctx.eval_lut(&gin388A, and2);
    Ciphertext gin388B = 2 * gout291 + (1 - gout138);
    Ciphertext gout388B = ctx.eval_lut(&gin388B, and2);
    Ciphertext gout388 = gout388A + gout388B;
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout75 + gout256;
    Ciphertext gout389 = ctx.eval_lut(&gin389, nand2);
    std::cerr << "\rLUT390   ";
    Ciphertext gin390 = 2 * gout280 + gout336;
    Ciphertext gout390 = ctx.eval_lut(&gin390, and2);
    std::cerr << "\rIMUX391  ";
    Ciphertext gin391A = 2 * gout222 + gout75;
    Ciphertext gout391A = ctx.eval_lut(&gin391A, and2);
    Ciphertext gin391B = 2 * gout260 + (1 - gout75);
    Ciphertext gout391B = ctx.eval_lut(&gin391B, and2);
    Ciphertext gout391 = gout391A + gout391B;
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * gout75 + gout260;
    Ciphertext gout392 = ctx.eval_lut(&gin392, nand2);
    std::cerr << "\rIMUX393  ";
    Ciphertext gin393A = 2 * gout133 + gout75;
    Ciphertext gout393A = ctx.eval_lut(&gin393A, and2);
    Ciphertext gin393B = 2 * gout256 + (1 - gout75);
    Ciphertext gout393B = ctx.eval_lut(&gin393B, and2);
    Ciphertext gout393 = gout393A + gout393B;
    std::cerr << "\rIMUX394  ";
    Ciphertext gin394A = 2 * gout267 + gout138;
    Ciphertext gout394A = ctx.eval_lut(&gin394A, and2);
    Ciphertext gin394B = 2 * gout283 + (1 - gout138);
    Ciphertext gout394B = ctx.eval_lut(&gin394B, and2);
    Ciphertext gout394 = gout394A + gout394B;
    std::cerr << "\rIMUX395  ";
    Ciphertext gin395A = 2 * gout279 + gout138;
    Ciphertext gout395A = ctx.eval_lut(&gin395A, and2);
    Ciphertext gin395B = 2 * gout292 + (1 - gout138);
    Ciphertext gout395B = ctx.eval_lut(&gin395B, and2);
    Ciphertext gout395 = gout395A + gout395B;
    std::cerr << "\rLUT396   ";
    Ciphertext gin396 = 2 * gout278 + gout343;
    Ciphertext gout396 = ctx.eval_lut(&gin396, and2);
    std::cerr << "\rLUT397   ";
    Ciphertext gin397 = 2 * gout271 + gout315;
    Ciphertext gout397 = ctx.eval_lut(&gin397, and2);
    std::cerr << "\rIMUX398  ";
    Ciphertext gin398A = 2 * gout268 + gout138;
    Ciphertext gout398A = ctx.eval_lut(&gin398A, and2);
    Ciphertext gin398B = 2 * gout281 + (1 - gout138);
    Ciphertext gout398B = ctx.eval_lut(&gin398B, and2);
    Ciphertext gout398 = gout398A + gout398B;
    std::cerr << "\rLUT399   ";
    Ciphertext gin399 = 2 * gout205 + gout270;
    Ciphertext gout399 = ctx.eval_lut(&gin399, and2);
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout118 + gout272;
    Ciphertext gout400 = ctx.eval_lut(&gin400, nand2);
    std::cerr << "\rLUT401   ";
    Ciphertext gin401 = 2 * gout321 + gout280;
    Ciphertext gout401 = ctx.eval_lut(&gin401, and2);
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * gout105 + gout299;
    Ciphertext gout402 = ctx.eval_lut(&gin402, nand2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout303 + gout301;
    Ciphertext gout403 = ctx.eval_lut(&gin403, and2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * gout138 + gout283;
    Ciphertext gout404 = ctx.eval_lut(&gin404, nand2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout105 + gout323;
    Ciphertext gout405 = ctx.eval_lut(&gin405, nand2);
    std::cerr << "\rLUT406   ";
    Ciphertext gin406 = 2 * gout319 + gout314;
    Ciphertext gout406 = ctx.eval_lut(&gin406, and2);
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout227 + gout310;
    Ciphertext gout407 = ctx.eval_lut(&gin407, and2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout284 + gout305;
    Ciphertext gout408 = ctx.eval_lut(&gin408, and2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout286 + gout312;
    Ciphertext gout409 = ctx.eval_lut(&gin409, and2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout105 + gout341;
    Ciphertext gout410 = ctx.eval_lut(&gin410, nand2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout71 + gout334;
    Ciphertext gout411 = ctx.eval_lut(&gin411, and2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout138 + gout291;
    Ciphertext gout412 = ctx.eval_lut(&gin412, nand2);
    std::cerr << "\rLUT413   ";
    Ciphertext gin413 = 2 * gout138 + gout293;
    Ciphertext gout413 = ctx.eval_lut(&gin413, nand2);
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout138 + gout292;
    Ciphertext gout414 = ctx.eval_lut(&gin414, nand2);
    std::cerr << "\rLUT415   ";
    Ciphertext gin415 = 2 * gout331 + gout358;
    Ciphertext gout415 = ctx.eval_lut(&gin415, and2);
    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout350 + gout72;
    Ciphertext gout416 = ctx.eval_lut(&gin416, and2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout138 + gout324;
    Ciphertext gout417 = ctx.eval_lut(&gin417, nand2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout138 + gout309;
    Ciphertext gout418 = ctx.eval_lut(&gin418, nand2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout105 + gout302;
    Ciphertext gout419 = ctx.eval_lut(&gin419, nand2);
    std::cerr << "\rIMUX420  ";
    Ciphertext gin420A = 2 * gout239 + gout138;
    Ciphertext gout420A = ctx.eval_lut(&gin420A, and2);
    Ciphertext gin420B = 2 * gout324 + (1 - gout138);
    Ciphertext gout420B = ctx.eval_lut(&gin420B, and2);
    Ciphertext gout420 = gout420A + gout420B;
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout325 + gout294;
    Ciphertext gout421 = ctx.eval_lut(&gin421, and2);
    std::cerr << "\rLUT422   ";
    Ciphertext gin422 = 2 * gout346 + gout351;
    Ciphertext gout422 = ctx.eval_lut(&gin422, and2);
    std::cerr << "\rIMUX423  ";
    Ciphertext gin423A = 2 * gout246 + gout105;
    Ciphertext gout423A = ctx.eval_lut(&gin423A, and2);
    Ciphertext gin423B = 2 * gout342 + (1 - gout105);
    Ciphertext gout423B = ctx.eval_lut(&gin423B, and2);
    Ciphertext gout423 = gout423A + gout423B;
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * gout105 + gout386;
    Ciphertext gout424 = ctx.eval_lut(&gin424, nand2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * gout362 + gout354;
    Ciphertext gout425 = ctx.eval_lut(&gin425, and2);
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout327 + gout335;
    Ciphertext gout426 = ctx.eval_lut(&gin426, and2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout360 + gout355;
    Ciphertext gout427 = ctx.eval_lut(&gin427, and2);
    std::cerr << "\rIMUX428  ";
    Ciphertext gin428A = 2 * gout384 + gout75;
    Ciphertext gout428A = ctx.eval_lut(&gin428A, and2);
    Ciphertext gin428B = 2 * gout322 + (1 - gout75);
    Ciphertext gout428B = ctx.eval_lut(&gin428B, and2);
    Ciphertext gout428 = gout428A + gout428B;
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout377 + gout274;
    Ciphertext gout429 = ctx.eval_lut(&gin429, and2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout326 + gout318;
    Ciphertext gout430 = ctx.eval_lut(&gin430, and2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout248 + gout369;
    Ciphertext gout431 = ctx.eval_lut(&gin431, and2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout338 + gout374;
    Ciphertext gout432 = ctx.eval_lut(&gin432, and2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout368 + gout371;
    Ciphertext gout433 = ctx.eval_lut(&gin433, and2);
    std::cerr << "\rLUT434   ";
    Ciphertext gin434 = 2 * gout274 + gout376;
    Ciphertext gout434 = ctx.eval_lut(&gin434, and2);
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout372 + gout373;
    Ciphertext gout435 = ctx.eval_lut(&gin435, and2);
    std::cerr << "\rIMUX436  ";
    Ciphertext gin436A = 2 * gout222 + gout105;
    Ciphertext gout436A = ctx.eval_lut(&gin436A, and2);
    Ciphertext gin436B = 2 * gout363 + (1 - gout105);
    Ciphertext gout436B = ctx.eval_lut(&gin436B, and2);
    Ciphertext gout436 = gout436A + gout436B;
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout275 + gout406;
    Ciphertext gout437 = ctx.eval_lut(&gin437, and2);
    std::cerr << "\rIMUX438  ";
    Ciphertext gin438A = 2 * gout244 + gout105;
    Ciphertext gout438A = ctx.eval_lut(&gin438A, and2);
    Ciphertext gin438B = 2 * gout364 + (1 - gout105);
    Ciphertext gout438B = ctx.eval_lut(&gin438B, and2);
    Ciphertext gout438 = gout438A + gout438B;
    std::cerr << "\rIMUX439  ";
    Ciphertext gin439A = 2 * gout246 + gout138;
    Ciphertext gout439A = ctx.eval_lut(&gin439A, and2);
    Ciphertext gin439B = 2 * gout347 + (1 - gout138);
    Ciphertext gout439B = ctx.eval_lut(&gin439B, and2);
    Ciphertext gout439 = gout439A + gout439B;
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout348 + gout317;
    Ciphertext gout440 = ctx.eval_lut(&gin440, and2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout330 + gout378;
    Ciphertext gout441 = ctx.eval_lut(&gin441, and2);
    std::cerr << "\rLUT442   ";
    Ciphertext gin442 = 2 * gout313 + gout365;
    Ciphertext gout442 = ctx.eval_lut(&gin442, and2);
    std::cerr << "\rLUT443   ";
    Ciphertext gin443 = 2 * gout138 + gout347;
    Ciphertext gout443 = ctx.eval_lut(&gin443, nand2);
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout399 + gout370;
    Ciphertext gout444 = ctx.eval_lut(&gin444, and2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout105 + gout363;
    Ciphertext gout445 = ctx.eval_lut(&gin445, nand2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout105 + gout342;
    Ciphertext gout446 = ctx.eval_lut(&gin446, nand2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout349 + gout408;
    Ciphertext gout447 = ctx.eval_lut(&gin447, nand2);
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * gout105 + gout364;
    Ciphertext gout448 = ctx.eval_lut(&gin448, nand2);
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout353 + gout409;
    Ciphertext gout449 = ctx.eval_lut(&gin449, nand2);
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout300 + gout389;
    Ciphertext gout450 = ctx.eval_lut(&gin450, and2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout304 + gout385;
    Ciphertext gout451 = ctx.eval_lut(&gin451, and2);
    std::cerr << "\rIMUX452  ";
    Ciphertext gin452A = 2 * gout268 + gout105;
    Ciphertext gout452A = ctx.eval_lut(&gin452A, and2);
    Ciphertext gin452B = 2 * gout386 + (1 - gout105);
    Ciphertext gout452B = ctx.eval_lut(&gin452B, and2);
    Ciphertext gout452 = gout452A + gout452B;
    std::cerr << "\rIMUX453  ";
    Ciphertext gin453A = 2 * gout383 + gout75;
    Ciphertext gout453A = ctx.eval_lut(&gin453A, and2);
    Ciphertext gin453B = 2 * gout394 + (1 - gout75);
    Ciphertext gout453B = ctx.eval_lut(&gin453B, and2);
    Ciphertext gout453 = gout453A + gout453B;
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout307 + gout390;
    Ciphertext gout454 = ctx.eval_lut(&gin454, and2);
    std::cerr << "\rLUT455   ";
    Ciphertext gin455 = 2 * gout320 + gout396;
    Ciphertext gout455 = ctx.eval_lut(&gin455, and2);
    std::cerr << "\rIMUX456  ";
    Ciphertext gin456A = 2 * gout367 + gout75;
    Ciphertext gout456A = ctx.eval_lut(&gin456A, and2);
    Ciphertext gin456B = 2 * gout395 + (1 - gout75);
    Ciphertext gout456B = ctx.eval_lut(&gin456B, and2);
    Ciphertext gout456 = gout456A + gout456B;
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout400 + gout282;
    Ciphertext gout457 = ctx.eval_lut(&gin457, and2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout288 + gout397;
    Ciphertext gout458 = ctx.eval_lut(&gin458, and2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout285 + gout401;
    Ciphertext gout459 = ctx.eval_lut(&gin459, and2);
    std::cerr << "\rIMUX460  ";
    Ciphertext gin460A = 2 * gout384 + gout83;
    Ciphertext gout460A = ctx.eval_lut(&gin460A, and2);
    Ciphertext gin460B = 2 * gout322 + (1 - gout83);
    Ciphertext gout460B = ctx.eval_lut(&gin460B, and2);
    Ciphertext gout460 = gout460A + gout460B;
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout392 + gout344;
    Ciphertext gout461 = ctx.eval_lut(&gin461, and2);
    std::cerr << "\rIMUX462  ";
    Ciphertext gin462A = 2 * gout383 + gout83;
    Ciphertext gout462A = ctx.eval_lut(&gin462A, and2);
    Ciphertext gin462B = 2 * gout394 + (1 - gout83);
    Ciphertext gout462B = ctx.eval_lut(&gin462B, and2);
    Ciphertext gout462 = gout462A + gout462B;
    std::cerr << "\rIMUX463  ";
    Ciphertext gin463A = 2 * gout367 + gout83;
    Ciphertext gout463A = ctx.eval_lut(&gin463A, and2);
    Ciphertext gin463B = 2 * gout395 + (1 - gout83);
    Ciphertext gout463B = ctx.eval_lut(&gin463B, and2);
    Ciphertext gout463 = gout463A + gout463B;
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout345 + gout415;
    Ciphertext gout464 = ctx.eval_lut(&gin464, nand2);
    std::cerr << "\rIMUX465  ";
    Ciphertext gin465A = 2 * gout311 + gout83;
    Ciphertext gout465A = ctx.eval_lut(&gin465A, and2);
    Ciphertext gin465B = 2 * gout420 + (1 - gout83);
    Ciphertext gout465B = ctx.eval_lut(&gin465B, and2);
    Ciphertext gout465 = gout465A + gout465B;
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout380 + gout426;
    Ciphertext gout466 = ctx.eval_lut(&gin466, nand2);
    std::cerr << "\rLUT467   ";
    Ciphertext gin467 = 2 * gout332 + gout433;
    Ciphertext gout467 = ctx.eval_lut(&gin467, nand2);
    std::cerr << "\rIMUX468  ";
    Ciphertext gin468A = 2 * gout436 + gout83;
    Ciphertext gout468A = ctx.eval_lut(&gin468A, and2);
    Ciphertext gin468B = 2 * gout308 + (1 - gout83);
    Ciphertext gout468B = ctx.eval_lut(&gin468B, and2);
    Ciphertext gout468 = gout468A + gout468B;
    std::cerr << "\rIMUX469  ";
    Ciphertext gin469A = 2 * gout423 + gout75;
    Ciphertext gout469A = ctx.eval_lut(&gin469A, and2);
    Ciphertext gin469B = 2 * gout439 + (1 - gout75);
    Ciphertext gout469B = ctx.eval_lut(&gin469B, and2);
    Ciphertext gout469 = gout469A + gout469B;
    std::cerr << "\rLUT470   ";
    Ciphertext gin470 = 2 * gout287 + gout429;
    Ciphertext gout470 = ctx.eval_lut(&gin470, and2);
    std::cerr << "\rIMUX471  ";
    Ciphertext gin471A = 2 * gout311 + gout75;
    Ciphertext gout471A = ctx.eval_lut(&gin471A, and2);
    Ciphertext gin471B = 2 * gout420 + (1 - gout75);
    Ciphertext gout471B = ctx.eval_lut(&gin471B, and2);
    Ciphertext gout471 = gout471A + gout471B;
    std::cerr << "\rIMUX472  ";
    Ciphertext gin472A = 2 * gout436 + gout75;
    Ciphertext gout472A = ctx.eval_lut(&gin472A, and2);
    Ciphertext gin472B = 2 * gout308 + (1 - gout75);
    Ciphertext gout472B = ctx.eval_lut(&gin472B, and2);
    Ciphertext gout472 = gout472A + gout472B;
    std::cerr << "\rLUT473   ";
    Ciphertext gin473 = 2 * gout219 + gout430;
    Ciphertext gout473 = ctx.eval_lut(&gin473, and2);
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout366 + gout431;
    Ciphertext gout474 = ctx.eval_lut(&gin474, and2);
    std::cerr << "\rLUT475   ";
    Ciphertext gin475 = 2 * gout298 + gout421;
    Ciphertext gout475 = ctx.eval_lut(&gin475, nand2);
    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout418 + gout432;
    Ciphertext gout476 = ctx.eval_lut(&gin476, and2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout450 + gout425;
    Ciphertext gout477 = ctx.eval_lut(&gin477, nand2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout422 + gout437;
    Ciphertext gout478 = ctx.eval_lut(&gin478, nand2);
    std::cerr << "\rIMUX479  ";
    Ciphertext gin479A = 2 * gout423 + gout83;
    Ciphertext gout479A = ctx.eval_lut(&gin479A, and2);
    Ciphertext gin479B = 2 * gout439 + (1 - gout83);
    Ciphertext gout479B = ctx.eval_lut(&gin479B, and2);
    Ciphertext gout479 = gout479A + gout479B;
    std::cerr << "\rLUT480   ";
    Ciphertext gin480 = 2 * gout404 + gout427;
    Ciphertext gout480 = ctx.eval_lut(&gin480, and2);
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout417 + gout454;
    Ciphertext gout481 = ctx.eval_lut(&gin481, and2);
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout61 + gout447;
    Ciphertext gout482 = ctx.eval_lut(&gin482, nand2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout328 + gout444;
    Ciphertext gout483 = ctx.eval_lut(&gin483, nand2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout61 + gout449;
    Ciphertext gout484 = ctx.eval_lut(&gin484, nand2);
    std::cerr << "\rLUT485   ";
    Ciphertext gin485 = 2 * gout271 + gout451;
    Ciphertext gout485 = ctx.eval_lut(&gin485, and2);
    std::cerr << "\rLUT486   ";
    Ciphertext gin486 = 2 * gout457 + gout248;
    Ciphertext gout486 = ctx.eval_lut(&gin486, and2);
    std::cerr << "\rLUT487   ";
    Ciphertext gin487 = 2 * gout443 + gout455;
    Ciphertext gout487 = ctx.eval_lut(&gin487, and2);
    std::cerr << "\rIMUX488  ";
    Ciphertext gin488A = 2 * gout438 + gout75;
    Ciphertext gout488A = ctx.eval_lut(&gin488A, and2);
    Ciphertext gin488B = 2 * gout388 + (1 - gout75);
    Ciphertext gout488B = ctx.eval_lut(&gin488B, and2);
    Ciphertext gout488 = gout488A + gout488B;
    std::cerr << "\rIMUX489  ";
    Ciphertext gin489A = 2 * gout452 + gout75;
    Ciphertext gout489A = ctx.eval_lut(&gin489A, and2);
    Ciphertext gin489B = 2 * gout398 + (1 - gout75);
    Ciphertext gout489B = ctx.eval_lut(&gin489B, and2);
    Ciphertext gout489 = gout489A + gout489B;
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout442 + gout407;
    Ciphertext gout490 = ctx.eval_lut(&gin490, and2);
    std::cerr << "\rIMUX491  ";
    Ciphertext gin491A = 2 * gout452 + gout83;
    Ciphertext gout491A = ctx.eval_lut(&gin491A, and2);
    Ciphertext gin491B = 2 * gout398 + (1 - gout83);
    Ciphertext gout491B = ctx.eval_lut(&gin491B, and2);
    Ciphertext gout491 = gout491A + gout491B;
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout361 + gout441;
    Ciphertext gout492 = ctx.eval_lut(&gin492, nand2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout381 + gout434;
    Ciphertext gout493 = ctx.eval_lut(&gin493, and2);
    std::cerr << "\rIMUX494  ";
    Ciphertext gin494A = 2 * gout438 + gout83;
    Ciphertext gout494A = ctx.eval_lut(&gin494A, and2);
    Ciphertext gin494B = 2 * gout388 + (1 - gout83);
    Ciphertext gout494B = ctx.eval_lut(&gin494B, and2);
    Ciphertext gout494 = gout494A + gout494B;
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * gout412 + gout458;
    Ciphertext gout495 = ctx.eval_lut(&gin495, and2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout403 + gout459;
    Ciphertext gout496 = ctx.eval_lut(&gin496, nand2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout413 + gout461;
    Ciphertext gout497 = ctx.eval_lut(&gin497, and2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout61 + gout464;
    Ciphertext gout498 = ctx.eval_lut(&gin498, nand2);
    std::cerr << "\rIMUX499  ";
    Ciphertext gin499A = 2 * gout133 + gout68;
    Ciphertext gout499A = ctx.eval_lut(&gin499A, and2);
    Ciphertext gin499B = 2 * gout477 + (1 - gout68);
    Ciphertext gout499B = ctx.eval_lut(&gin499B, and2);
    Ciphertext gout499 = gout499A + gout499B;
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * gout61 + gout466;
    Ciphertext gout500 = ctx.eval_lut(&gin500, nand2);
    std::cerr << "\rIMUX501  ";
    Ciphertext gin501A = 2 * gout141 + gout68;
    Ciphertext gout501A = ctx.eval_lut(&gin501A, and2);
    Ciphertext gin501B = 2 * gout478 + (1 - gout68);
    Ciphertext gout501B = ctx.eval_lut(&gin501B, and2);
    Ciphertext gout501 = gout501A + gout501B;
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout405 + gout480;
    Ciphertext gout502 = ctx.eval_lut(&gin502, nand2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout416 + gout470;
    Ciphertext gout503 = ctx.eval_lut(&gin503, nand2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * gout440 + gout473;
    Ciphertext gout504 = ctx.eval_lut(&gin504, nand2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * gout402 + gout481;
    Ciphertext gout505 = ctx.eval_lut(&gin505, nand2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * gout411 + gout486;
    Ciphertext gout506 = ctx.eval_lut(&gin506, nand2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout295 + gout474;
    Ciphertext gout507 = ctx.eval_lut(&gin507, and2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout172 + gout482;
    Ciphertext gout508 = ctx.eval_lut(&gin508, nand2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout419 + gout476;
    Ciphertext gout509 = ctx.eval_lut(&gin509, nand2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout61 + gout467;
    Ciphertext gout510 = ctx.eval_lut(&gin510, nand2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * gout177 + gout484;
    Ciphertext gout511 = ctx.eval_lut(&gin511, nand2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout61 + gout475;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * gout61 + gout492;
    Ciphertext gout513 = ctx.eval_lut(&gin513, nand2);
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * gout337 + gout490;
    Ciphertext gout514 = ctx.eval_lut(&gin514, nand2);
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout61 + gout483;
    Ciphertext gout515 = ctx.eval_lut(&gin515, nand2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * gout414 + gout493;
    Ciphertext gout516 = ctx.eval_lut(&gin516, and2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout435 + gout485;
    Ciphertext gout517 = ctx.eval_lut(&gin517, nand2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout448 + gout495;
    Ciphertext gout518 = ctx.eval_lut(&gin518, nand2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout446 + gout487;
    Ciphertext gout519 = ctx.eval_lut(&gin519, nand2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * gout445 + gout497;
    Ciphertext gout520 = ctx.eval_lut(&gin520, nand2);
    std::cerr << "\rIMUX521  ";
    Ciphertext gin521A = 2 * gout112 + gout68;
    Ciphertext gout521A = ctx.eval_lut(&gin521A, and2);
    Ciphertext gin521B = 2 * gout496 + (1 - gout68);
    Ciphertext gout521B = ctx.eval_lut(&gin521B, and2);
    Ciphertext gout521 = gout521A + gout521B;
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * gout168 + gout498;
    Ciphertext gout522 = ctx.eval_lut(&gin522, nand2);
    std::cerr << "\rLUT523   ";
    Ciphertext gin523 = 2 * gout153 + gout500;
    Ciphertext gout523 = ctx.eval_lut(&gin523, nand2);
    std::cerr << "\rIMUX524  ";
    Ciphertext gin524A = 2 * gout267 + gout76;
    Ciphertext gout524A = ctx.eval_lut(&gin524A, and2);
    Ciphertext gin524B = 2 * gout502 + (1 - gout76);
    Ciphertext gout524B = ctx.eval_lut(&gin524B, and2);
    Ciphertext gout524 = gout524A + gout524B;
    std::cerr << "\rIMUX525  ";
    Ciphertext gin525A = 2 * gout108 + gout68;
    Ciphertext gout525A = ctx.eval_lut(&gin525A, and2);
    Ciphertext gin525B = 2 * gout503 + (1 - gout68);
    Ciphertext gout525B = ctx.eval_lut(&gin525B, and2);
    Ciphertext gout525 = gout525A + gout525B;
    std::cerr << "\rLUT526   ";
    Ciphertext gin526 = 2 * gout170 + gout510;
    Ciphertext gout526 = ctx.eval_lut(&gin526, nand2);
    std::cerr << "\rIMUX527  ";
    Ciphertext gin527A = 2 * gout239 + gout76;
    Ciphertext gout527A = ctx.eval_lut(&gin527A, and2);
    Ciphertext gin527B = 2 * gout505 + (1 - gout76);
    Ciphertext gout527B = ctx.eval_lut(&gin527B, and2);
    Ciphertext gout527 = gout527A + gout527B;
    std::cerr << "\rLUT528   ";
    Ciphertext gin528 = 2 * gout152 + gout512;
    Ciphertext gout528 = ctx.eval_lut(&gin528, nand2);
    std::cerr << "\rIMUX529  ";
    Ciphertext gin529A = 2 * gout115 + gout68;
    Ciphertext gout529A = ctx.eval_lut(&gin529A, and2);
    Ciphertext gin529B = 2 * gout504 + (1 - gout68);
    Ciphertext gout529B = ctx.eval_lut(&gin529B, and2);
    Ciphertext gout529 = gout529A + gout529B;
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * gout147 + gout515;
    Ciphertext gout530 = ctx.eval_lut(&gin530, nand2);
    std::cerr << "\rIMUX531  ";
    Ciphertext gin531A = 2 * gout124 + gout68;
    Ciphertext gout531A = ctx.eval_lut(&gin531A, and2);
    Ciphertext gin531B = 2 * gout506 + (1 - gout68);
    Ciphertext gout531B = ctx.eval_lut(&gin531B, and2);
    Ciphertext gout531 = gout531A + gout531B;
    std::cerr << "\rLUT532   ";
    Ciphertext gin532 = 2 * gout424 + gout507;
    Ciphertext gout532 = ctx.eval_lut(&gin532, nand2);
    std::cerr << "\rIMUX533  ";
    Ciphertext gin533A = 2 * gout243 + gout76;
    Ciphertext gout533A = ctx.eval_lut(&gin533A, and2);
    Ciphertext gin533B = 2 * gout509 + (1 - gout76);
    Ciphertext gout533B = ctx.eval_lut(&gin533B, and2);
    Ciphertext gout533 = gout533A + gout533B;
    std::cerr << "\rIMUX534  ";
    Ciphertext gin534A = 2 * gout110 + gout68;
    Ciphertext gout534A = ctx.eval_lut(&gin534A, and2);
    Ciphertext gin534B = 2 * gout517 + (1 - gout68);
    Ciphertext gout534B = ctx.eval_lut(&gin534B, and2);
    Ciphertext gout534 = gout534A + gout534B;
    std::cerr << "\rIMUX535  ";
    Ciphertext gin535A = 2 * gout244 + gout76;
    Ciphertext gout535A = ctx.eval_lut(&gin535A, and2);
    Ciphertext gin535B = 2 * gout518 + (1 - gout76);
    Ciphertext gout535B = ctx.eval_lut(&gin535B, and2);
    Ciphertext gout535 = gout535A + gout535B;
    std::cerr << "\rIMUX536  ";
    Ciphertext gin536A = 2 * gout109 + gout68;
    Ciphertext gout536A = ctx.eval_lut(&gin536A, and2);
    Ciphertext gin536B = 2 * gout514 + (1 - gout68);
    Ciphertext gout536B = ctx.eval_lut(&gin536B, and2);
    Ciphertext gout536 = gout536A + gout536B;
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * gout207 + gout513;
    Ciphertext gout537 = ctx.eval_lut(&gin537, nand2);
    std::cerr << "\rIMUX538  ";
    Ciphertext gin538A = 2 * gout222 + gout76;
    Ciphertext gout538A = ctx.eval_lut(&gin538A, and2);
    Ciphertext gin538B = 2 * gout520 + (1 - gout76);
    Ciphertext gout538B = ctx.eval_lut(&gin538B, and2);
    Ciphertext gout538 = gout538A + gout538B;
    std::cerr << "\rIMUX539  ";
    Ciphertext gin539A = 2 * gout246 + gout76;
    Ciphertext gout539A = ctx.eval_lut(&gin539A, and2);
    Ciphertext gin539B = 2 * gout519 + (1 - gout76);
    Ciphertext gout539B = ctx.eval_lut(&gin539B, and2);
    Ciphertext gout539 = gout539A + gout539B;
    std::cerr << "\rLUT540   ";
    Ciphertext gin540 = 2 * gout410 + gout516;
    Ciphertext gout540 = ctx.eval_lut(&gin540, nand2);
    std::cerr << "\rIMUX541  ";
    Ciphertext gin541A = 2 * gout268 + gout76;
    Ciphertext gout541A = ctx.eval_lut(&gin541A, and2);
    Ciphertext gin541B = 2 * gout532 + (1 - gout76);
    Ciphertext gout541B = ctx.eval_lut(&gin541B, and2);
    Ciphertext gout541 = gout541A + gout541B;
    std::cerr << "\rIMUX542  ";
    Ciphertext gin542A = 2 * gout279 + gout76;
    Ciphertext gout542A = ctx.eval_lut(&gin542A, and2);
    Ciphertext gin542B = 2 * gout540 + (1 - gout76);
    Ciphertext gout542B = ctx.eval_lut(&gin542B, and2);
    Ciphertext gout542 = gout542A + gout542B;

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout526)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout511)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout537)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout528)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout522)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout530)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout523)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout508)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout536)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout531)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout521)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout499)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout534)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout525)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout529)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout501)); /* out15 */
    test_out.push_back(ctx.decrypt(&gout524)); /* out16 */
    test_out.push_back(ctx.decrypt(&gout541)); /* out17 */
    test_out.push_back(ctx.decrypt(&gout527)); /* out18 */
    test_out.push_back(ctx.decrypt(&gout539)); /* out19 */
    test_out.push_back(ctx.decrypt(&gout535)); /* out20 */
    test_out.push_back(ctx.decrypt(&gout542)); /* out21 */
    test_out.push_back(ctx.decrypt(&gout538)); /* out22 */
    test_out.push_back(ctx.decrypt(&gout533)); /* out23 */
    test_out.push_back(ctx.decrypt(&gout462)); /* out24 */
    test_out.push_back(ctx.decrypt(&gout491)); /* out25 */
    test_out.push_back(ctx.decrypt(&gout465)); /* out26 */
    test_out.push_back(ctx.decrypt(&gout479)); /* out27 */
    test_out.push_back(ctx.decrypt(&gout494)); /* out28 */
    test_out.push_back(ctx.decrypt(&gout463)); /* out29 */
    test_out.push_back(ctx.decrypt(&gout468)); /* out30 */
    test_out.push_back(ctx.decrypt(&gout460)); /* out31 */
    test_out.push_back(ctx.decrypt(&gout453)); /* out32 */
    test_out.push_back(ctx.decrypt(&gout489)); /* out33 */
    test_out.push_back(ctx.decrypt(&gout471)); /* out34 */
    test_out.push_back(ctx.decrypt(&gout469)); /* out35 */
    test_out.push_back(ctx.decrypt(&gout488)); /* out36 */
    test_out.push_back(ctx.decrypt(&gout456)); /* out37 */
    test_out.push_back(ctx.decrypt(&gout472)); /* out38 */
    test_out.push_back(ctx.decrypt(&gout428)); /* out39 */
    test_out.push_back(ctx.decrypt(&gout379)); /* out40 */
    test_out.push_back(ctx.decrypt(&gout356)); /* out41 */
    test_out.push_back(ctx.decrypt(&gout316)); /* out42 */
    test_out.push_back(ctx.decrypt(&gout352)); /* out43 */
    test_out.push_back(ctx.decrypt(&gout375)); /* out44 */
    test_out.push_back(ctx.decrypt(&gout387)); /* out45 */
    test_out.push_back(ctx.decrypt(&gout391)); /* out46 */
    test_out.push_back(ctx.decrypt(&gout357)); /* out47 */
    test_out.push_back(ctx.decrypt(&gout333)); /* out48 */
    test_out.push_back(ctx.decrypt(&gout359)); /* out49 */
    test_out.push_back(ctx.decrypt(&gout289)); /* out50 */
    test_out.push_back(ctx.decrypt(&gout393)); /* out51 */
    test_out.push_back(ctx.decrypt(&gout339)); /* out52 */
    test_out.push_back(ctx.decrypt(&gout329)); /* out53 */
    test_out.push_back(ctx.decrypt(&gout340)); /* out54 */
    test_out.push_back(ctx.decrypt(&gout382)); /* out55 */
    test_out.push_back(ctx.decrypt(&gout81)); /* out56 */
    test_out.push_back(ctx.decrypt(&gout87)); /* out57 */
    test_out.push_back(ctx.decrypt(&gout85)); /* out58 */
    test_out.push_back(ctx.decrypt(&gout82)); /* out59 */
    test_out.push_back(ctx.decrypt(&gout88)); /* out60 */
    test_out.push_back(ctx.decrypt(&gout79)); /* out61 */
    test_out.push_back(ctx.decrypt(&gout86)); /* out62 */
    test_out.push_back(ctx.decrypt(&gout84)); /* out63 */
    return test_out;
}

static void
BM_strrev_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_strrev_boolean(ctx);
    }
}

BENCHMARK(BM_strrev_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
