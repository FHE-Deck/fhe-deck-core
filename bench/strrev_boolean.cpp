#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_strrev_boolean(FHEContext& ctx, uint64_t my_string = 0)
{
    Ciphertext ct_my_string0 = ctx.encrypt_public((my_string >> 0) & 1);
    Ciphertext ct_my_string1 = ctx.encrypt_public((my_string >> 1) & 1);
    Ciphertext ct_my_string2 = ctx.encrypt_public((my_string >> 2) & 1);
    Ciphertext ct_my_string3 = ctx.encrypt_public((my_string >> 3) & 1);
    Ciphertext ct_my_string4 = ctx.encrypt_public((my_string >> 4) & 1);
    Ciphertext ct_my_string5 = ctx.encrypt_public((my_string >> 5) & 1);
    Ciphertext ct_my_string6 = ctx.encrypt_public((my_string >> 6) & 1);
    Ciphertext ct_my_string7 = ctx.encrypt_public((my_string >> 7) & 1);
    Ciphertext ct_my_string8 = ctx.encrypt_public((my_string >> 8) & 1);
    Ciphertext ct_my_string9 = ctx.encrypt_public((my_string >> 9) & 1);
    Ciphertext ct_my_string10 = ctx.encrypt_public((my_string >> 10) & 1);
    Ciphertext ct_my_string11 = ctx.encrypt_public((my_string >> 11) & 1);
    Ciphertext ct_my_string12 = ctx.encrypt_public((my_string >> 12) & 1);
    Ciphertext ct_my_string13 = ctx.encrypt_public((my_string >> 13) & 1);
    Ciphertext ct_my_string14 = ctx.encrypt_public((my_string >> 14) & 1);
    Ciphertext ct_my_string15 = ctx.encrypt_public((my_string >> 15) & 1);
    Ciphertext ct_my_string16 = ctx.encrypt_public((my_string >> 16) & 1);
    Ciphertext ct_my_string17 = ctx.encrypt_public((my_string >> 17) & 1);
    Ciphertext ct_my_string18 = ctx.encrypt_public((my_string >> 18) & 1);
    Ciphertext ct_my_string19 = ctx.encrypt_public((my_string >> 19) & 1);
    Ciphertext ct_my_string20 = ctx.encrypt_public((my_string >> 20) & 1);
    Ciphertext ct_my_string21 = ctx.encrypt_public((my_string >> 21) & 1);
    Ciphertext ct_my_string22 = ctx.encrypt_public((my_string >> 22) & 1);
    Ciphertext ct_my_string23 = ctx.encrypt_public((my_string >> 23) & 1);
    Ciphertext ct_my_string24 = ctx.encrypt_public((my_string >> 24) & 1);
    Ciphertext ct_my_string25 = ctx.encrypt_public((my_string >> 25) & 1);
    Ciphertext ct_my_string26 = ctx.encrypt_public((my_string >> 26) & 1);
    Ciphertext ct_my_string27 = ctx.encrypt_public((my_string >> 27) & 1);
    Ciphertext ct_my_string28 = ctx.encrypt_public((my_string >> 28) & 1);
    Ciphertext ct_my_string29 = ctx.encrypt_public((my_string >> 29) & 1);
    Ciphertext ct_my_string30 = ctx.encrypt_public((my_string >> 30) & 1);
    Ciphertext ct_my_string31 = ctx.encrypt_public((my_string >> 31) & 1);
    Ciphertext ct_my_string32 = ctx.encrypt_public((my_string >> 32) & 1);
    Ciphertext ct_my_string33 = ctx.encrypt_public((my_string >> 33) & 1);
    Ciphertext ct_my_string34 = ctx.encrypt_public((my_string >> 34) & 1);
    Ciphertext ct_my_string35 = ctx.encrypt_public((my_string >> 35) & 1);
    Ciphertext ct_my_string36 = ctx.encrypt_public((my_string >> 36) & 1);
    Ciphertext ct_my_string37 = ctx.encrypt_public((my_string >> 37) & 1);
    Ciphertext ct_my_string38 = ctx.encrypt_public((my_string >> 38) & 1);
    Ciphertext ct_my_string39 = ctx.encrypt_public((my_string >> 39) & 1);
    Ciphertext ct_my_string40 = ctx.encrypt_public((my_string >> 40) & 1);
    Ciphertext ct_my_string41 = ctx.encrypt_public((my_string >> 41) & 1);
    Ciphertext ct_my_string42 = ctx.encrypt_public((my_string >> 42) & 1);
    Ciphertext ct_my_string43 = ctx.encrypt_public((my_string >> 43) & 1);
    Ciphertext ct_my_string44 = ctx.encrypt_public((my_string >> 44) & 1);
    Ciphertext ct_my_string45 = ctx.encrypt_public((my_string >> 45) & 1);
    Ciphertext ct_my_string46 = ctx.encrypt_public((my_string >> 46) & 1);
    Ciphertext ct_my_string47 = ctx.encrypt_public((my_string >> 47) & 1);
    Ciphertext ct_my_string48 = ctx.encrypt_public((my_string >> 48) & 1);
    Ciphertext ct_my_string49 = ctx.encrypt_public((my_string >> 49) & 1);
    Ciphertext ct_my_string50 = ctx.encrypt_public((my_string >> 50) & 1);
    Ciphertext ct_my_string51 = ctx.encrypt_public((my_string >> 51) & 1);
    Ciphertext ct_my_string52 = ctx.encrypt_public((my_string >> 52) & 1);
    Ciphertext ct_my_string53 = ctx.encrypt_public((my_string >> 53) & 1);
    Ciphertext ct_my_string54 = ctx.encrypt_public((my_string >> 54) & 1);
    Ciphertext ct_my_string55 = ctx.encrypt_public((my_string >> 55) & 1);
    Ciphertext ct_my_string56 = ctx.encrypt_public((my_string >> 56) & 1);
    Ciphertext ct_my_string57 = ctx.encrypt_public((my_string >> 57) & 1);
    Ciphertext ct_my_string58 = ctx.encrypt_public((my_string >> 58) & 1);
    Ciphertext ct_my_string59 = ctx.encrypt_public((my_string >> 59) & 1);
    Ciphertext ct_my_string60 = ctx.encrypt_public((my_string >> 60) & 1);
    Ciphertext ct_my_string61 = ctx.encrypt_public((my_string >> 61) & 1);
    Ciphertext ct_my_string62 = ctx.encrypt_public((my_string >> 62) & 1);
    Ciphertext ct_my_string63 = ctx.encrypt_public((my_string >> 63) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_my_string56 + ct_my_string57;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_my_string62 + ct_my_string63;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_my_string60 + ct_my_string61;
    Ciphertext gout2 = ctx.eval_lut(&gin2, nor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_my_string34 + ct_my_string35;
    Ciphertext gout3 = ctx.eval_lut(&gin3, nor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_my_string53 + ct_my_string54;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nor2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_my_string32 + ct_my_string33;
    Ciphertext gout5 = ctx.eval_lut(&gin5, nor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_my_string58 + ct_my_string59;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_my_string42 + ct_my_string43;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_my_string38 + ct_my_string39;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nor2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_my_string36 + ct_my_string37;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_my_string44 + ct_my_string45;
    Ciphertext gout10 = ctx.eval_lut(&gin10, nor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_my_string40 + ct_my_string41;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_my_string19 + ct_my_string20;
    Ciphertext gout12 = ctx.eval_lut(&gin12, nor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_my_string46 + ct_my_string47;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nor2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_my_string17 + ct_my_string18;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nor2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_my_string48 + ct_my_string51;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_my_string23 + ct_my_string16;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_my_string24 + ct_my_string25;
    Ciphertext gout17 = ctx.eval_lut(&gin17, nor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_my_string49 + ct_my_string50;
    Ciphertext gout18 = ctx.eval_lut(&gin18, nor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_my_string21 + ct_my_string22;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_my_string30 + ct_my_string31;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nor2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_my_string28 + ct_my_string29;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_my_string2 + ct_my_string3;
    Ciphertext gout22 = ctx.eval_lut(&gin22, nor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_my_string6 + ct_my_string7;
    Ciphertext gout23 = ctx.eval_lut(&gin23, nor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_my_string26 + ct_my_string27;
    Ciphertext gout24 = ctx.eval_lut(&gin24, nor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * gout18 + gout15;
    Ciphertext gout25 = ctx.eval_lut(&gin25, and2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_my_string4 + ct_my_string5;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nor2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_my_string12 + ct_my_string13;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nor2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_my_string0 + ct_my_string1;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nor2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_my_string10 + ct_my_string11;
    Ciphertext gout29 = ctx.eval_lut(&gin29, nor2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_my_string8 + ct_my_string9;
    Ciphertext gout30 = ctx.eval_lut(&gin30, nor2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_my_string14 + ct_my_string15;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nor2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * gout0 + gout6;
    Ciphertext gout32 = ctx.eval_lut(&gin32, and2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_my_string52 + ct_my_string55;
    Ciphertext gout33 = ctx.eval_lut(&gin33, nor2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * gout9 + gout8;
    Ciphertext gout34 = ctx.eval_lut(&gin34, and2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * gout10 + gout13;
    Ciphertext gout35 = ctx.eval_lut(&gin35, and2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * gout2 + gout1;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * gout5 + gout3;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * gout11 + gout7;
    Ciphertext gout38 = ctx.eval_lut(&gin38, and2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * gout4 + gout33;
    Ciphertext gout39 = ctx.eval_lut(&gin39, and2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * gout16 + gout14;
    Ciphertext gout40 = ctx.eval_lut(&gin40, and2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * gout12 + gout19;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * gout26 + gout23;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * gout17 + gout24;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * gout27 + gout31;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * gout21 + gout20;
    Ciphertext gout45 = ctx.eval_lut(&gin45, and2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * gout28 + gout22;
    Ciphertext gout46 = ctx.eval_lut(&gin46, and2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * gout30 + gout29;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * gout39 + gout25;
    Ciphertext gout48 = ctx.eval_lut(&gin48, nand2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * gout36 + gout32;
    Ciphertext gout49 = ctx.eval_lut(&gin49, nand2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * gout34 + gout37;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * (1 - gout48) + gout49;
    Ciphertext gout51 = ctx.eval_lut(&gin51, or2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * gout35 + gout38;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * gout41 + gout40;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * gout45 + gout43;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * gout42 + gout46;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * gout44 + gout47;
    Ciphertext gout56 = ctx.eval_lut(&gin56, nand2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * gout48 + gout49;
    Ciphertext gout57 = ctx.eval_lut(&gin57, and2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * gout50 + gout52;
    Ciphertext gout58 = ctx.eval_lut(&gin58, and2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * (1 - gout58) + gout57;
    Ciphertext gout59 = ctx.eval_lut(&gin59, or2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * gout53 + gout54;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * gout52 + gout51;
    Ciphertext gout61 = ctx.eval_lut(&gin61, nand2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * gout55 + gout56;
    Ciphertext gout62 = ctx.eval_lut(&gin62, and2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * gout57 + gout58;
    Ciphertext gout63 = ctx.eval_lut(&gin63, and2);
    std::cerr << "\rINV64   ";
    Ciphertext gout64 = 1 - gout58;

    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * gout59 + gout60;
    Ciphertext gout65 = ctx.eval_lut(&gin65, nand2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * gout58 + gout60;
    Ciphertext gout66 = ctx.eval_lut(&gin66, and2);
    std::cerr << "\rINV67   ";
    Ciphertext gout67 = 1 - gout62;

    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * gout50 + gout61;
    Ciphertext gout68 = ctx.eval_lut(&gin68, nand2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * gout60 + gout62;
    Ciphertext gout69 = ctx.eval_lut(&gin69, nand2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_my_string49 + gout63;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * gout63 + gout69;
    Ciphertext gout71 = ctx.eval_lut(&gin71, or2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_my_string53 + gout63;
    Ciphertext gout72 = ctx.eval_lut(&gin72, nand2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * gout65 + gout62;
    Ciphertext gout73 = ctx.eval_lut(&gin73, and2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * gout54 + gout68;
    Ciphertext gout74 = ctx.eval_lut(&gin74, nand2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * (1 - gout66) + gout62;
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * gout63 + (1 - gout69);
    Ciphertext gout76 = ctx.eval_lut(&gin76, and2);
    std::cerr << "\rIMUX77  ";
    Ciphertext gin77A = 2 * ct_my_string6 + gout76;
    Ciphertext gout77A = ctx.eval_lut(&gin77A, and2);
    Ciphertext gin77B = 2 * ct_my_string62 + (1 - gout76);
    Ciphertext gout77B = ctx.eval_lut(&gin77B, and2);
    Ciphertext gout77 = gout77A + gout77B;
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * gout64 + gout69;
    Ciphertext gout78 = ctx.eval_lut(&gin78, or2);
    std::cerr << "\rINV79   ";
    Ciphertext gout79 = 1 - gout71;

    std::cerr << "\rIMUX80  ";
    Ciphertext gin80A = 2 * ct_my_string2 + gout76;
    Ciphertext gout80A = ctx.eval_lut(&gin80A, and2);
    Ciphertext gin80B = 2 * ct_my_string58 + (1 - gout76);
    Ciphertext gout80B = ctx.eval_lut(&gin80B, and2);
    Ciphertext gout80 = gout80A + gout80B;
    std::cerr << "\rINV81   ";
    Ciphertext gout81 = 1 - gout73;

    std::cerr << "\rIMUX82  ";
    Ciphertext gin82A = 2 * ct_my_string0 + gout76;
    Ciphertext gout82A = ctx.eval_lut(&gin82A, and2);
    Ciphertext gin82B = 2 * ct_my_string56 + (1 - gout76);
    Ciphertext gout82B = ctx.eval_lut(&gin82B, and2);
    Ciphertext gout82 = gout82A + gout82B;
    std::cerr << "\rIMUX83  ";
    Ciphertext gin83A = 2 * ct_my_string3 + gout76;
    Ciphertext gout83A = ctx.eval_lut(&gin83A, and2);
    Ciphertext gin83B = 2 * ct_my_string59 + (1 - gout76);
    Ciphertext gout83B = ctx.eval_lut(&gin83B, and2);
    Ciphertext gout83 = gout83A + gout83B;
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * gout53 + gout74;
    Ciphertext gout84 = ctx.eval_lut(&gin84, nand2);
    std::cerr << "\rIMUX85  ";
    Ciphertext gin85A = 2 * ct_my_string7 + gout76;
    Ciphertext gout85A = ctx.eval_lut(&gin85A, and2);
    Ciphertext gin85B = 2 * ct_my_string63 + (1 - gout76);
    Ciphertext gout85B = ctx.eval_lut(&gin85B, and2);
    Ciphertext gout85 = gout85A + gout85B;
    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * gout67 + gout80;
    Ciphertext gout86 = ctx.eval_lut(&gin86, nand2);
    std::cerr << "\rINV87   ";
    Ciphertext gout87 = 1 - gout76;

    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout67 + gout77;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rIMUX89  ";
    Ciphertext gin89A = 2 * ct_my_string5 + gout76;
    Ciphertext gout89A = ctx.eval_lut(&gin89A, and2);
    Ciphertext gin89B = 2 * ct_my_string61 + (1 - gout76);
    Ciphertext gout89B = ctx.eval_lut(&gin89B, and2);
    Ciphertext gout89 = gout89A + gout89B;
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout67 + gout83;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rIMUX91  ";
    Ciphertext gin91A = 2 * ct_my_string1 + gout76;
    Ciphertext gout91A = ctx.eval_lut(&gin91A, and2);
    Ciphertext gin91B = 2 * ct_my_string57 + (1 - gout76);
    Ciphertext gout91B = ctx.eval_lut(&gin91B, and2);
    Ciphertext gout91 = gout91A + gout91B;
    std::cerr << "\rIMUX92  ";
    Ciphertext gin92A = 2 * ct_my_string4 + gout76;
    Ciphertext gout92A = ctx.eval_lut(&gin92A, and2);
    Ciphertext gin92B = 2 * ct_my_string60 + (1 - gout76);
    Ciphertext gout92B = ctx.eval_lut(&gin92B, and2);
    Ciphertext gout92 = gout92A + gout92B;
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout67 + gout82;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout67 + gout85;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout56 + gout84;
    Ciphertext gout95 = ctx.eval_lut(&gin95, nand2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout67 + gout89;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * gout67 + gout91;
    Ciphertext gout97 = ctx.eval_lut(&gin97, nand2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout67 + gout92;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout55 + gout95;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout81 + (1 - gout99);
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout73 + (1 - gout95);
    Ciphertext gout101 = ctx.eval_lut(&gin101, and2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout73 + gout99;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout101 + gout66;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * ct_my_string46 + gout103;
    Ciphertext gout104 = ctx.eval_lut(&gin104, nand2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * ct_my_string45 + gout103;
    Ciphertext gout105 = ctx.eval_lut(&gin105, nand2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * gout71 + gout101;
    Ciphertext gout106 = ctx.eval_lut(&gin106, and2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * gout79 + gout102;
    Ciphertext gout107 = ctx.eval_lut(&gin107, and2);
    std::cerr << "\rIMUX108  ";
    Ciphertext gin108A = 2 * ct_my_string1 + gout103;
    Ciphertext gout108A = ctx.eval_lut(&gin108A, and2);
    Ciphertext gin108B = 2 * ct_my_string41 + (1 - gout103);
    Ciphertext gout108B = ctx.eval_lut(&gin108B, and2);
    Ciphertext gout108 = gout108A + gout108B;
    std::cerr << "\rIMUX109  ";
    Ciphertext gin109A = 2 * ct_my_string4 + gout103;
    Ciphertext gout109A = ctx.eval_lut(&gin109A, and2);
    Ciphertext gin109B = 2 * ct_my_string44 + (1 - gout103);
    Ciphertext gout109B = ctx.eval_lut(&gin109B, and2);
    Ciphertext gout109 = gout109A + gout109B;
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * ct_my_string15 + gout106;
    Ciphertext gout110 = ctx.eval_lut(&gin110, nand2);
    std::cerr << "\rIMUX111  ";
    Ciphertext gin111A = 2 * ct_my_string6 + gout106;
    Ciphertext gout111A = ctx.eval_lut(&gin111A, and2);
    Ciphertext gin111B = 2 * ct_my_string14 + (1 - gout106);
    Ciphertext gout111B = ctx.eval_lut(&gin111B, and2);
    Ciphertext gout111 = gout111A + gout111B;
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * ct_my_string13 + gout106;
    Ciphertext gout112 = ctx.eval_lut(&gin112, nand2);
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * ct_my_string12 + gout106;
    Ciphertext gout113 = ctx.eval_lut(&gin113, nand2);
    std::cerr << "\rIMUX114  ";
    Ciphertext gin114A = 2 * ct_my_string6 + gout107;
    Ciphertext gout114A = ctx.eval_lut(&gin114A, and2);
    Ciphertext gin114B = 2 * ct_my_string54 + (1 - gout107);
    Ciphertext gout114B = ctx.eval_lut(&gin114B, and2);
    Ciphertext gout114 = gout114A + gout114B;
    std::cerr << "\rIMUX115  ";
    Ciphertext gin115A = 2 * ct_my_string2 + gout106;
    Ciphertext gout115A = ctx.eval_lut(&gin115A, and2);
    Ciphertext gin115B = 2 * ct_my_string10 + (1 - gout106);
    Ciphertext gout115B = ctx.eval_lut(&gin115B, and2);
    Ciphertext gout115 = gout115A + gout115B;
    std::cerr << "\rIMUX116  ";
    Ciphertext gin116A = 2 * ct_my_string3 + gout103;
    Ciphertext gout116A = ctx.eval_lut(&gin116A, and2);
    Ciphertext gin116B = 2 * ct_my_string43 + (1 - gout103);
    Ciphertext gout116B = ctx.eval_lut(&gin116B, and2);
    Ciphertext gout116 = gout116A + gout116B;
    std::cerr << "\rIMUX117  ";
    Ciphertext gin117A = 2 * ct_my_string7 + gout103;
    Ciphertext gout117A = ctx.eval_lut(&gin117A, and2);
    Ciphertext gin117B = 2 * ct_my_string47 + (1 - gout103);
    Ciphertext gout117B = ctx.eval_lut(&gin117B, and2);
    Ciphertext gout117 = gout117A + gout117B;
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout71 + gout102;
    Ciphertext gout118 = ctx.eval_lut(&gin118, and2);
    std::cerr << "\rIMUX119  ";
    Ciphertext gin119A = 2 * ct_my_string6 + gout118;
    Ciphertext gout119A = ctx.eval_lut(&gin119A, and2);
    Ciphertext gin119B = 2 * ct_my_string22 + (1 - gout118);
    Ciphertext gout119B = ctx.eval_lut(&gin119B, and2);
    Ciphertext gout119 = gout119A + gout119B;
    std::cerr << "\rIMUX120  ";
    Ciphertext gin120A = 2 * ct_my_string1 + gout106;
    Ciphertext gout120A = ctx.eval_lut(&gin120A, and2);
    Ciphertext gin120B = 2 * ct_my_string9 + (1 - gout106);
    Ciphertext gout120B = ctx.eval_lut(&gin120B, and2);
    Ciphertext gout120 = gout120A + gout120B;
    std::cerr << "\rIMUX121  ";
    Ciphertext gin121A = 2 * ct_my_string3 + gout107;
    Ciphertext gout121A = ctx.eval_lut(&gin121A, and2);
    Ciphertext gin121B = 2 * ct_my_string51 + (1 - gout107);
    Ciphertext gout121B = ctx.eval_lut(&gin121B, and2);
    Ciphertext gout121 = gout121A + gout121B;
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * gout81 + gout99;
    Ciphertext gout122 = ctx.eval_lut(&gin122, and2);
    std::cerr << "\rIMUX123  ";
    Ciphertext gin123A = 2 * ct_my_string3 + gout118;
    Ciphertext gout123A = ctx.eval_lut(&gin123A, and2);
    Ciphertext gin123B = 2 * ct_my_string19 + (1 - gout118);
    Ciphertext gout123B = ctx.eval_lut(&gin123B, and2);
    Ciphertext gout123 = gout123A + gout123B;
    std::cerr << "\rIMUX124  ";
    Ciphertext gin124A = 2 * ct_my_string0 + gout118;
    Ciphertext gout124A = ctx.eval_lut(&gin124A, and2);
    Ciphertext gin124B = 2 * ct_my_string16 + (1 - gout118);
    Ciphertext gout124B = ctx.eval_lut(&gin124B, and2);
    Ciphertext gout124 = gout124A + gout124B;
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * ct_my_string42 + gout103;
    Ciphertext gout125 = ctx.eval_lut(&gin125, nand2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * ct_my_string49 + gout107;
    Ciphertext gout126 = ctx.eval_lut(&gin126, nand2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * ct_my_string55 + gout107;
    Ciphertext gout127 = ctx.eval_lut(&gin127, nand2);
    std::cerr << "\rIMUX128  ";
    Ciphertext gin128A = 2 * ct_my_string2 + gout118;
    Ciphertext gout128A = ctx.eval_lut(&gin128A, and2);
    Ciphertext gin128B = 2 * ct_my_string18 + (1 - gout118);
    Ciphertext gout128B = ctx.eval_lut(&gin128B, and2);
    Ciphertext gout128 = gout128A + gout128B;
    std::cerr << "\rIMUX129  ";
    Ciphertext gin129A = 2 * ct_my_string5 + gout103;
    Ciphertext gout129A = ctx.eval_lut(&gin129A, and2);
    Ciphertext gin129B = 2 * ct_my_string45 + (1 - gout103);
    Ciphertext gout129B = ctx.eval_lut(&gin129B, and2);
    Ciphertext gout129 = gout129A + gout129B;
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * ct_my_string14 + gout106;
    Ciphertext gout130 = ctx.eval_lut(&gin130, nand2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * gout107 + gout129;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * gout100 + gout75;
    Ciphertext gout132 = ctx.eval_lut(&gin132, and2);
    std::cerr << "\rIMUX133  ";
    Ciphertext gin133A = 2 * gout111 + gout132;
    Ciphertext gout133A = ctx.eval_lut(&gin133A, and2);
    Ciphertext gin133B = 2 * gout119 + (1 - gout132);
    Ciphertext gout133B = ctx.eval_lut(&gin133B, and2);
    Ciphertext gout133 = gout133A + gout133B;
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * ct_my_string25 + gout132;
    Ciphertext gout134 = ctx.eval_lut(&gin134, nand2);
    std::cerr << "\rIMUX135  ";
    Ciphertext gin135A = 2 * ct_my_string1 + gout107;
    Ciphertext gout135A = ctx.eval_lut(&gin135A, and2);
    Ciphertext gin135B = 2 * ct_my_string49 + (1 - gout107);
    Ciphertext gout135B = ctx.eval_lut(&gin135B, and2);
    Ciphertext gout135 = gout135A + gout135B;
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * gout126 + gout134;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_my_string16 + gout118;
    Ciphertext gout137 = ctx.eval_lut(&gin137, nand2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * ct_my_string18 + gout118;
    Ciphertext gout138 = ctx.eval_lut(&gin138, nand2);
    std::cerr << "\rIMUX139  ";
    Ciphertext gin139A = 2 * ct_my_string4 + gout118;
    Ciphertext gout139A = ctx.eval_lut(&gin139A, and2);
    Ciphertext gin139B = 2 * ct_my_string20 + (1 - gout118);
    Ciphertext gout139B = ctx.eval_lut(&gin139B, and2);
    Ciphertext gout139 = gout139A + gout139B;
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * gout105 + gout112;
    Ciphertext gout140 = ctx.eval_lut(&gin140, and2);
    std::cerr << "\rIMUX141  ";
    Ciphertext gin141A = 2 * ct_my_string5 + gout132;
    Ciphertext gout141A = ctx.eval_lut(&gin141A, and2);
    Ciphertext gin141B = 2 * ct_my_string29 + (1 - gout132);
    Ciphertext gout141B = ctx.eval_lut(&gin141B, and2);
    Ciphertext gout141 = gout141A + gout141B;
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * gout113 + gout98;
    Ciphertext gout142 = ctx.eval_lut(&gin142, and2);
    std::cerr << "\rIMUX143  ";
    Ciphertext gin143A = 2 * ct_my_string1 + gout118;
    Ciphertext gout143A = ctx.eval_lut(&gin143A, and2);
    Ciphertext gin143B = 2 * ct_my_string17 + (1 - gout118);
    Ciphertext gout143B = ctx.eval_lut(&gin143B, and2);
    Ciphertext gout143 = gout143A + gout143B;
    std::cerr << "\rIMUX144  ";
    Ciphertext gin144A = 2 * ct_my_string5 + gout107;
    Ciphertext gout144A = ctx.eval_lut(&gin144A, and2);
    Ciphertext gin144B = 2 * ct_my_string53 + (1 - gout107);
    Ciphertext gout144B = ctx.eval_lut(&gin144B, and2);
    Ciphertext gout144 = gout144A + gout144B;
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * gout112 + gout96;
    Ciphertext gout145 = ctx.eval_lut(&gin145, and2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * gout132 + gout124;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_my_string22 + gout118;
    Ciphertext gout147 = ctx.eval_lut(&gin147, nand2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * ct_my_string41 + gout103;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rIMUX149  ";
    Ciphertext gin149A = 2 * ct_my_string2 + gout103;
    Ciphertext gout149A = ctx.eval_lut(&gin149A, and2);
    Ciphertext gin149B = 2 * ct_my_string42 + (1 - gout103);
    Ciphertext gout149B = ctx.eval_lut(&gin149B, and2);
    Ciphertext gout149 = gout149A + gout149B;
    std::cerr << "\rIMUX150  ";
    Ciphertext gin150A = 2 * ct_my_string5 + gout118;
    Ciphertext gout150A = ctx.eval_lut(&gin150A, and2);
    Ciphertext gin150B = 2 * ct_my_string21 + (1 - gout118);
    Ciphertext gout150B = ctx.eval_lut(&gin150B, and2);
    Ciphertext gout150 = gout150A + gout150B;
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_my_string54 + gout107;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_my_string43 + gout103;
    Ciphertext gout152 = ctx.eval_lut(&gin152, nand2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * ct_my_string50 + gout107;
    Ciphertext gout153 = ctx.eval_lut(&gin153, nand2);
    std::cerr << "\rIMUX154  ";
    Ciphertext gin154A = 2 * ct_my_string5 + gout106;
    Ciphertext gout154A = ctx.eval_lut(&gin154A, and2);
    Ciphertext gin154B = 2 * ct_my_string13 + (1 - gout106);
    Ciphertext gout154B = ctx.eval_lut(&gin154B, and2);
    Ciphertext gout154 = gout154A + gout154B;
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * ct_my_string40 + gout103;
    Ciphertext gout155 = ctx.eval_lut(&gin155, nand2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * ct_my_string23 + gout118;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rIMUX157  ";
    Ciphertext gin157A = 2 * ct_my_string0 + gout106;
    Ciphertext gout157A = ctx.eval_lut(&gin157A, and2);
    Ciphertext gin157B = 2 * ct_my_string8 + (1 - gout106);
    Ciphertext gout157B = ctx.eval_lut(&gin157B, and2);
    Ciphertext gout157 = gout157A + gout157B;
    std::cerr << "\rIMUX158  ";
    Ciphertext gin158A = 2 * gout120 + gout107;
    Ciphertext gout158A = ctx.eval_lut(&gin158A, and2);
    Ciphertext gin158B = 2 * gout108 + (1 - gout107);
    Ciphertext gout158B = ctx.eval_lut(&gin158B, and2);
    Ciphertext gout158 = gout158A + gout158B;
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * ct_my_string20 + gout118;
    Ciphertext gout159 = ctx.eval_lut(&gin159, nand2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * ct_my_string29 + gout132;
    Ciphertext gout160 = ctx.eval_lut(&gin160, nand2);
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * gout107 + gout117;
    Ciphertext gout161 = ctx.eval_lut(&gin161, nand2);
    std::cerr << "\rIMUX162  ";
    Ciphertext gin162A = 2 * ct_my_string1 + gout132;
    Ciphertext gout162A = ctx.eval_lut(&gin162A, and2);
    Ciphertext gin162B = 2 * ct_my_string25 + (1 - gout132);
    Ciphertext gout162B = ctx.eval_lut(&gin162B, and2);
    Ciphertext gout162 = gout162A + gout162B;
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_my_string44 + gout103;
    Ciphertext gout163 = ctx.eval_lut(&gin163, nand2);
    std::cerr << "\rIMUX164  ";
    Ciphertext gin164A = 2 * gout111 + gout76;
    Ciphertext gout164A = ctx.eval_lut(&gin164A, and2);
    Ciphertext gin164B = 2 * gout114 + (1 - gout76);
    Ciphertext gout164B = ctx.eval_lut(&gin164B, and2);
    Ciphertext gout164 = gout164A + gout164B;
    std::cerr << "\rIMUX165  ";
    Ciphertext gin165A = 2 * ct_my_string6 + gout103;
    Ciphertext gout165A = ctx.eval_lut(&gin165A, and2);
    Ciphertext gin165B = 2 * ct_my_string46 + (1 - gout103);
    Ciphertext gout165B = ctx.eval_lut(&gin165B, and2);
    Ciphertext gout165 = gout165A + gout165B;
    std::cerr << "\rIMUX166  ";
    Ciphertext gin166A = 2 * ct_my_string4 + gout106;
    Ciphertext gout166A = ctx.eval_lut(&gin166A, and2);
    Ciphertext gin166B = 2 * ct_my_string12 + (1 - gout106);
    Ciphertext gout166B = ctx.eval_lut(&gin166B, and2);
    Ciphertext gout166 = gout166A + gout166B;
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_my_string9 + gout106;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * gout130 + gout88;
    Ciphertext gout168 = ctx.eval_lut(&gin168, and2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * ct_my_string53 + gout107;
    Ciphertext gout169 = ctx.eval_lut(&gin169, nand2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * gout107 + gout109;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * ct_my_string17 + gout118;
    Ciphertext gout171 = ctx.eval_lut(&gin171, nand2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * gout122 + gout79;
    Ciphertext gout172 = ctx.eval_lut(&gin172, and2);
    std::cerr << "\rIMUX173  ";
    Ciphertext gin173A = 2 * ct_my_string4 + gout172;
    Ciphertext gout173A = ctx.eval_lut(&gin173A, and2);
    Ciphertext gin173B = 2 * ct_my_string36 + (1 - gout172);
    Ciphertext gout173B = ctx.eval_lut(&gin173B, and2);
    Ciphertext gout173 = gout173A + gout173B;
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * gout107 + gout116;
    Ciphertext gout174 = ctx.eval_lut(&gin174, nand2);
    std::cerr << "\rIMUX175  ";
    Ciphertext gin175A = 2 * ct_my_string0 + gout107;
    Ciphertext gout175A = ctx.eval_lut(&gin175A, and2);
    Ciphertext gin175B = 2 * ct_my_string48 + (1 - gout107);
    Ciphertext gout175B = ctx.eval_lut(&gin175B, and2);
    Ciphertext gout175 = gout175A + gout175B;
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * ct_my_string8 + gout106;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * gout110 + gout94;
    Ciphertext gout177 = ctx.eval_lut(&gin177, and2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * ct_my_string27 + gout132;
    Ciphertext gout178 = ctx.eval_lut(&gin178, nand2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * gout104 + gout130;
    Ciphertext gout179 = ctx.eval_lut(&gin179, and2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * gout172 + gout162;
    Ciphertext gout180 = ctx.eval_lut(&gin180, nand2);
    std::cerr << "\rIMUX181  ";
    Ciphertext gin181A = 2 * ct_my_string1 + gout172;
    Ciphertext gout181A = ctx.eval_lut(&gin181A, and2);
    Ciphertext gin181B = 2 * ct_my_string33 + (1 - gout172);
    Ciphertext gout181B = ctx.eval_lut(&gin181B, and2);
    Ciphertext gout181 = gout181A + gout181B;
    std::cerr << "\rIMUX182  ";
    Ciphertext gin182A = 2 * ct_my_string5 + gout172;
    Ciphertext gout182A = ctx.eval_lut(&gin182A, and2);
    Ciphertext gin182B = 2 * ct_my_string37 + (1 - gout172);
    Ciphertext gout182B = ctx.eval_lut(&gin182B, and2);
    Ciphertext gout182 = gout182A + gout182B;
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * gout107 + gout108;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * ct_my_string21 + gout118;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * ct_my_string26 + gout132;
    Ciphertext gout185 = ctx.eval_lut(&gin185, nand2);
    std::cerr << "\rIMUX186  ";
    Ciphertext gin186A = 2 * ct_my_string6 + gout132;
    Ciphertext gout186A = ctx.eval_lut(&gin186A, and2);
    Ciphertext gin186B = 2 * ct_my_string30 + (1 - gout132);
    Ciphertext gout186B = ctx.eval_lut(&gin186B, and2);
    Ciphertext gout186 = gout186A + gout186B;
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * ct_my_string11 + gout106;
    Ciphertext gout187 = ctx.eval_lut(&gin187, nand2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * ct_my_string38 + gout172;
    Ciphertext gout188 = ctx.eval_lut(&gin188, nand2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * ct_my_string51 + gout107;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rIMUX190  ";
    Ciphertext gin190A = 2 * gout115 + gout132;
    Ciphertext gout190A = ctx.eval_lut(&gin190A, and2);
    Ciphertext gin190B = 2 * gout128 + (1 - gout132);
    Ciphertext gout190B = ctx.eval_lut(&gin190B, and2);
    Ciphertext gout190 = gout190A + gout190B;
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * ct_my_string24 + gout132;
    Ciphertext gout191 = ctx.eval_lut(&gin191, nand2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * ct_my_string10 + gout106;
    Ciphertext gout192 = ctx.eval_lut(&gin192, nand2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * ct_my_string31 + gout132;
    Ciphertext gout193 = ctx.eval_lut(&gin193, nand2);
    std::cerr << "\rIMUX194  ";
    Ciphertext gin194A = 2 * gout154 + gout172;
    Ciphertext gout194A = ctx.eval_lut(&gin194A, and2);
    Ciphertext gin194B = 2 * gout141 + (1 - gout172);
    Ciphertext gout194B = ctx.eval_lut(&gin194B, and2);
    Ciphertext gout194 = gout194A + gout194B;
    std::cerr << "\rIMUX195  ";
    Ciphertext gin195A = 2 * ct_my_string4 + gout107;
    Ciphertext gout195A = ctx.eval_lut(&gin195A, and2);
    Ciphertext gin195B = 2 * ct_my_string52 + (1 - gout107);
    Ciphertext gout195B = ctx.eval_lut(&gin195B, and2);
    Ciphertext gout195 = gout195A + gout195B;
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * gout167 + gout97;
    Ciphertext gout196 = ctx.eval_lut(&gin196, and2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * gout71 + gout100;
    Ciphertext gout197 = ctx.eval_lut(&gin197, and2);
    std::cerr << "\rIMUX198  ";
    Ciphertext gin198A = 2 * ct_my_string0 + gout103;
    Ciphertext gout198A = ctx.eval_lut(&gin198A, and2);
    Ciphertext gin198B = 2 * ct_my_string40 + (1 - gout103);
    Ciphertext gout198B = ctx.eval_lut(&gin198B, and2);
    Ciphertext gout198 = gout198A + gout198B;
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * ct_my_string60 + gout197;
    Ciphertext gout199 = ctx.eval_lut(&gin199, nand2);
    std::cerr << "\rIMUX200  ";
    Ciphertext gin200A = 2 * gout166 + gout132;
    Ciphertext gout200A = ctx.eval_lut(&gin200A, and2);
    Ciphertext gin200B = 2 * gout139 + (1 - gout132);
    Ciphertext gout200B = ctx.eval_lut(&gin200B, and2);
    Ciphertext gout200 = gout200A + gout200B;
    std::cerr << "\rIMUX201  ";
    Ciphertext gin201A = 2 * ct_my_string7 + gout107;
    Ciphertext gout201A = ctx.eval_lut(&gin201A, and2);
    Ciphertext gin201B = 2 * ct_my_string55 + (1 - gout107);
    Ciphertext gout201B = ctx.eval_lut(&gin201B, and2);
    Ciphertext gout201 = gout201A + gout201B;
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * ct_my_string19 + gout118;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout125 + gout192;
    Ciphertext gout203 = ctx.eval_lut(&gin203, and2);
    std::cerr << "\rIMUX204  ";
    Ciphertext gin204A = 2 * gout157 + gout76;
    Ciphertext gout204A = ctx.eval_lut(&gin204A, and2);
    Ciphertext gin204B = 2 * gout175 + (1 - gout76);
    Ciphertext gout204B = ctx.eval_lut(&gin204B, and2);
    Ciphertext gout204 = gout204A + gout204B;
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout183 + gout196;
    Ciphertext gout205 = ctx.eval_lut(&gin205, and2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout76 + gout201;
    Ciphertext gout206 = ctx.eval_lut(&gin206, nand2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout76 + gout121;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout132 + gout139;
    Ciphertext gout208 = ctx.eval_lut(&gin208, nand2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * ct_my_string48 + gout107;
    Ciphertext gout209 = ctx.eval_lut(&gin209, nand2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * ct_my_string52 + gout107;
    Ciphertext gout210 = ctx.eval_lut(&gin210, nand2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * ct_my_string47 + gout103;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rIMUX212  ";
    Ciphertext gin212A = 2 * gout157 + gout107;
    Ciphertext gout212A = ctx.eval_lut(&gin212A, and2);
    Ciphertext gin212B = 2 * gout198 + (1 - gout107);
    Ciphertext gout212B = ctx.eval_lut(&gin212B, and2);
    Ciphertext gout212 = gout212A + gout212B;
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * ct_my_string28 + gout132;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rIMUX214  ";
    Ciphertext gin214A = 2 * ct_my_string2 + gout107;
    Ciphertext gout214A = ctx.eval_lut(&gin214A, and2);
    Ciphertext gin214B = 2 * ct_my_string50 + (1 - gout107);
    Ciphertext gout214B = ctx.eval_lut(&gin214B, and2);
    Ciphertext gout214 = gout214A + gout214B;
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * ct_my_string57 + gout197;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout76 + gout114;
    Ciphertext gout216 = ctx.eval_lut(&gin216, nand2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout161 + gout177;
    Ciphertext gout217 = ctx.eval_lut(&gin217, and2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout172 + gout186;
    Ciphertext gout218 = ctx.eval_lut(&gin218, nand2);
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout107 + gout198;
    Ciphertext gout219 = ctx.eval_lut(&gin219, nand2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * ct_my_string63 + gout197;
    Ciphertext gout220 = ctx.eval_lut(&gin220, nand2);
    std::cerr << "\rIMUX221  ";
    Ciphertext gin221A = 2 * ct_my_string2 + gout172;
    Ciphertext gout221A = ctx.eval_lut(&gin221A, and2);
    Ciphertext gin221B = 2 * ct_my_string34 + (1 - gout172);
    Ciphertext gout221B = ctx.eval_lut(&gin221B, and2);
    Ciphertext gout221 = gout221A + gout221B;
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout176 + gout93;
    Ciphertext gout222 = ctx.eval_lut(&gin222, and2);
    std::cerr << "\rIMUX223  ";
    Ciphertext gin223A = 2 * ct_my_string6 + gout172;
    Ciphertext gout223A = ctx.eval_lut(&gin223A, and2);
    Ciphertext gin223B = 2 * ct_my_string38 + (1 - gout172);
    Ciphertext gout223B = ctx.eval_lut(&gin223B, and2);
    Ciphertext gout223 = gout223A + gout223B;
    std::cerr << "\rIMUX224  ";
    Ciphertext gin224A = 2 * gout111 + gout107;
    Ciphertext gout224A = ctx.eval_lut(&gin224A, and2);
    Ciphertext gin224B = 2 * gout165 + (1 - gout107);
    Ciphertext gout224B = ctx.eval_lut(&gin224B, and2);
    Ciphertext gout224 = gout224A + gout224B;
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * ct_my_string30 + gout132;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * ct_my_string59 + gout197;
    Ciphertext gout226 = ctx.eval_lut(&gin226, nand2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * ct_my_string56 + gout197;
    Ciphertext gout227 = ctx.eval_lut(&gin227, nand2);
    std::cerr << "\rIMUX228  ";
    Ciphertext gin228A = 2 * gout154 + gout107;
    Ciphertext gout228A = ctx.eval_lut(&gin228A, and2);
    Ciphertext gin228B = 2 * gout129 + (1 - gout107);
    Ciphertext gout228B = ctx.eval_lut(&gin228B, and2);
    Ciphertext gout228 = gout228A + gout228B;
    std::cerr << "\rIMUX229  ";
    Ciphertext gin229A = 2 * gout120 + gout103;
    Ciphertext gout229A = ctx.eval_lut(&gin229A, and2);
    Ciphertext gin229B = 2 * gout181 + (1 - gout103);
    Ciphertext gout229B = ctx.eval_lut(&gin229B, and2);
    Ciphertext gout229 = gout229A + gout229B;
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout172 + gout141;
    Ciphertext gout230 = ctx.eval_lut(&gin230, nand2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout148 + gout167;
    Ciphertext gout231 = ctx.eval_lut(&gin231, and2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout76 + gout224;
    Ciphertext gout232 = ctx.eval_lut(&gin232, nand2);
    std::cerr << "\rIMUX233  ";
    Ciphertext gin233A = 2 * ct_my_string0 + gout172;
    Ciphertext gout233A = ctx.eval_lut(&gin233A, and2);
    Ciphertext gin233B = 2 * ct_my_string32 + (1 - gout172);
    Ciphertext gout233B = ctx.eval_lut(&gin233B, and2);
    Ciphertext gout233 = gout233A + gout233B;
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * ct_my_string35 + gout172;
    Ciphertext gout234 = ctx.eval_lut(&gin234, nand2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * ct_my_string32 + gout172;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout234 + gout178;
    Ciphertext gout236 = ctx.eval_lut(&gin236, and2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout192 + gout86;
    Ciphertext gout237 = ctx.eval_lut(&gin237, and2);
    std::cerr << "\rIMUX238  ";
    Ciphertext gin238A = 2 * gout133 + gout76;
    Ciphertext gout238A = ctx.eval_lut(&gin238A, and2);
    Ciphertext gin238B = 2 * gout224 + (1 - gout76);
    Ciphertext gout238B = ctx.eval_lut(&gin238B, and2);
    Ciphertext gout238 = gout238A + gout238B;
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout103 + gout181;
    Ciphertext gout239 = ctx.eval_lut(&gin239, nand2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * ct_my_string33 + gout172;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout187 + gout90;
    Ciphertext gout241 = ctx.eval_lut(&gin241, and2);
    std::cerr << "\rIMUX242  ";
    Ciphertext gin242A = 2 * gout154 + gout103;
    Ciphertext gout242A = ctx.eval_lut(&gin242A, and2);
    Ciphertext gin242B = 2 * gout182 + (1 - gout103);
    Ciphertext gout242B = ctx.eval_lut(&gin242B, and2);
    Ciphertext gout242 = gout242A + gout242B;
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * gout217 + gout206;
    Ciphertext gout243 = ctx.eval_lut(&gin243, and2);
    std::cerr << "\rIMUX244  ";
    Ciphertext gin244A = 2 * ct_my_string3 + gout106;
    Ciphertext gout244A = ctx.eval_lut(&gin244A, and2);
    Ciphertext gin244B = 2 * ct_my_string11 + (1 - gout106);
    Ciphertext gout244B = ctx.eval_lut(&gin244B, and2);
    Ciphertext gout244 = gout244A + gout244B;
    std::cerr << "\rIMUX245  ";
    Ciphertext gin245A = 2 * ct_my_string7 + gout106;
    Ciphertext gout245A = ctx.eval_lut(&gin245A, and2);
    Ciphertext gin245B = 2 * ct_my_string15 + (1 - gout106);
    Ciphertext gout245B = ctx.eval_lut(&gin245B, and2);
    Ciphertext gout245 = gout245A + gout245B;
    std::cerr << "\rIMUX246  ";
    Ciphertext gin246A = 2 * gout245 + gout107;
    Ciphertext gout246A = ctx.eval_lut(&gin246A, and2);
    Ciphertext gin246B = 2 * gout117 + (1 - gout107);
    Ciphertext gout246B = ctx.eval_lut(&gin246B, and2);
    Ciphertext gout246 = gout246A + gout246B;
    std::cerr << "\rIMUX247  ";
    Ciphertext gin247A = 2 * ct_my_string7 + gout172;
    Ciphertext gout247A = ctx.eval_lut(&gin247A, and2);
    Ciphertext gin247B = 2 * ct_my_string39 + (1 - gout172);
    Ciphertext gout247B = ctx.eval_lut(&gin247B, and2);
    Ciphertext gout247 = gout247A + gout247B;
    std::cerr << "\rIMUX248  ";
    Ciphertext gin248A = 2 * gout120 + gout172;
    Ciphertext gout248A = ctx.eval_lut(&gin248A, and2);
    Ciphertext gin248B = 2 * gout162 + (1 - gout172);
    Ciphertext gout248B = ctx.eval_lut(&gin248B, and2);
    Ciphertext gout248 = gout248A + gout248B;
    std::cerr << "\rIMUX249  ";
    Ciphertext gin249A = 2 * ct_my_string3 + gout172;
    Ciphertext gout249A = ctx.eval_lut(&gin249A, and2);
    Ciphertext gin249B = 2 * ct_my_string35 + (1 - gout172);
    Ciphertext gout249B = ctx.eval_lut(&gin249B, and2);
    Ciphertext gout249 = gout249A + gout249B;
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout131 + gout145;
    Ciphertext gout250 = ctx.eval_lut(&gin250, and2);
    std::cerr << "\rIMUX251  ";
    Ciphertext gin251A = 2 * gout157 + gout103;
    Ciphertext gout251A = ctx.eval_lut(&gin251A, and2);
    Ciphertext gin251B = 2 * gout233 + (1 - gout103);
    Ciphertext gout251B = ctx.eval_lut(&gin251B, and2);
    Ciphertext gout251 = gout251A + gout251B;
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout132 + gout123;
    Ciphertext gout252 = ctx.eval_lut(&gin252, nand2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout69 + (1 - gout102);
    Ciphertext gout253 = ctx.eval_lut(&gin253, and2);
    std::cerr << "\rIMUX254  ";
    Ciphertext gin254A = 2 * gout157 + gout132;
    Ciphertext gout254A = ctx.eval_lut(&gin254A, and2);
    Ciphertext gin254B = 2 * gout124 + (1 - gout132);
    Ciphertext gout254B = ctx.eval_lut(&gin254B, and2);
    Ciphertext gout254 = gout254A + gout254B;
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * ct_my_string36 + gout172;
    Ciphertext gout255 = ctx.eval_lut(&gin255, nand2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * ct_my_string34 + gout172;
    Ciphertext gout256 = ctx.eval_lut(&gin256, nand2);
    std::cerr << "\rIMUX257  ";
    Ciphertext gin257A = 2 * gout120 + gout76;
    Ciphertext gout257A = ctx.eval_lut(&gin257A, and2);
    Ciphertext gin257B = 2 * gout135 + (1 - gout76);
    Ciphertext gout257B = ctx.eval_lut(&gin257B, and2);
    Ciphertext gout257 = gout257A + gout257B;
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout132 + gout128;
    Ciphertext gout258 = ctx.eval_lut(&gin258, nand2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout225 + gout151;
    Ciphertext gout259 = ctx.eval_lut(&gin259, and2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * gout231 + gout215;
    Ciphertext gout260 = ctx.eval_lut(&gin260, and2);
    std::cerr << "\rIMUX261  ";
    Ciphertext gin261A = 2 * ct_my_string7 + gout132;
    Ciphertext gout261A = ctx.eval_lut(&gin261A, and2);
    Ciphertext gin261B = 2 * ct_my_string31 + (1 - gout132);
    Ciphertext gout261B = ctx.eval_lut(&gin261B, and2);
    Ciphertext gout261 = gout261A + gout261B;
    std::cerr << "\rIMUX262  ";
    Ciphertext gin262A = 2 * gout166 + gout107;
    Ciphertext gout262A = ctx.eval_lut(&gin262A, and2);
    Ciphertext gin262B = 2 * gout109 + (1 - gout107);
    Ciphertext gout262B = ctx.eval_lut(&gin262B, and2);
    Ciphertext gout262 = gout262A + gout262B;
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout107 + gout149;
    Ciphertext gout263 = ctx.eval_lut(&gin263, nand2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout210 + gout159;
    Ciphertext gout264 = ctx.eval_lut(&gin264, and2);
    std::cerr << "\rIMUX265  ";
    Ciphertext gin265A = 2 * gout166 + gout76;
    Ciphertext gout265A = ctx.eval_lut(&gin265A, and2);
    Ciphertext gin265B = 2 * gout195 + (1 - gout76);
    Ciphertext gout265B = ctx.eval_lut(&gin265B, and2);
    Ciphertext gout265 = gout265A + gout265B;
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout147 + gout179;
    Ciphertext gout266 = ctx.eval_lut(&gin266, and2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout163 + gout113;
    Ciphertext gout267 = ctx.eval_lut(&gin267, and2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * ct_my_string37 + gout172;
    Ciphertext gout268 = ctx.eval_lut(&gin268, nand2);
    std::cerr << "\rIMUX269  ";
    Ciphertext gin269A = 2 * ct_my_string2 + gout132;
    Ciphertext gout269A = ctx.eval_lut(&gin269A, and2);
    Ciphertext gin269B = 2 * ct_my_string26 + (1 - gout132);
    Ciphertext gout269B = ctx.eval_lut(&gin269B, and2);
    Ciphertext gout269 = gout269A + gout269B;
    std::cerr << "\rIMUX270  ";
    Ciphertext gin270A = 2 * ct_my_string7 + gout118;
    Ciphertext gout270A = ctx.eval_lut(&gin270A, and2);
    Ciphertext gin270B = 2 * ct_my_string23 + (1 - gout118);
    Ciphertext gout270B = ctx.eval_lut(&gin270B, and2);
    Ciphertext gout270 = gout270A + gout270B;
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout174 + gout241;
    Ciphertext gout271 = ctx.eval_lut(&gin271, and2);
    std::cerr << "\rIMUX272  ";
    Ciphertext gin272A = 2 * gout111 + gout172;
    Ciphertext gout272A = ctx.eval_lut(&gin272A, and2);
    Ciphertext gin272B = 2 * gout186 + (1 - gout172);
    Ciphertext gout272B = ctx.eval_lut(&gin272B, and2);
    Ciphertext gout272 = gout272A + gout272B;
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout169 + gout184;
    Ciphertext gout273 = ctx.eval_lut(&gin273, and2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout76 + gout246;
    Ciphertext gout274 = ctx.eval_lut(&gin274, nand2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout152 + gout187;
    Ciphertext gout275 = ctx.eval_lut(&gin275, and2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * gout171 + gout240;
    Ciphertext gout276 = ctx.eval_lut(&gin276, and2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout122 + gout71;
    Ciphertext gout277 = ctx.eval_lut(&gin277, and2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout219 + gout222;
    Ciphertext gout278 = ctx.eval_lut(&gin278, and2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout76 + gout158;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout76 + gout262;
    Ciphertext gout280 = ctx.eval_lut(&gin280, nand2);
    std::cerr << "\rIMUX281  ";
    Ciphertext gin281A = 2 * gout115 + gout76;
    Ciphertext gout281A = ctx.eval_lut(&gin281A, and2);
    Ciphertext gin281B = 2 * gout214 + (1 - gout76);
    Ciphertext gout281B = ctx.eval_lut(&gin281B, and2);
    Ciphertext gout281 = gout281A + gout281B;
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout103 + gout247;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rIMUX283  ";
    Ciphertext gin283A = 2 * gout245 + gout132;
    Ciphertext gout283A = ctx.eval_lut(&gin283A, and2);
    Ciphertext gin283B = 2 * gout270 + (1 - gout132);
    Ciphertext gout283B = ctx.eval_lut(&gin283B, and2);
    Ciphertext gout283 = gout283A + gout283B;
    std::cerr << "\rIMUX284  ";
    Ciphertext gin284A = 2 * gout111 + gout103;
    Ciphertext gout284A = ctx.eval_lut(&gin284A, and2);
    Ciphertext gin284B = 2 * gout223 + (1 - gout103);
    Ciphertext gout284B = ctx.eval_lut(&gin284B, and2);
    Ciphertext gout284 = gout284A + gout284B;
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout70 + gout205;
    Ciphertext gout285 = ctx.eval_lut(&gin285, and2);
    std::cerr << "\rIMUX286  ";
    Ciphertext gin286A = 2 * ct_my_string4 + gout132;
    Ciphertext gout286A = ctx.eval_lut(&gin286A, and2);
    Ciphertext gin286B = 2 * ct_my_string28 + (1 - gout132);
    Ciphertext gout286B = ctx.eval_lut(&gin286B, and2);
    Ciphertext gout286 = gout286A + gout286B;
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout76 + gout175;
    Ciphertext gout287 = ctx.eval_lut(&gin287, nand2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout209 + gout137;
    Ciphertext gout288 = ctx.eval_lut(&gin288, and2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout172 + gout286;
    Ciphertext gout289 = ctx.eval_lut(&gin289, nand2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout132 + gout143;
    Ciphertext gout290 = ctx.eval_lut(&gin290, nand2);
    std::cerr << "\rIMUX291  ";
    Ciphertext gin291A = 2 * gout166 + gout103;
    Ciphertext gout291A = ctx.eval_lut(&gin291A, and2);
    Ciphertext gin291B = 2 * gout173 + (1 - gout103);
    Ciphertext gout291B = ctx.eval_lut(&gin291B, and2);
    Ciphertext gout291 = gout291A + gout291B;
    std::cerr << "\rIMUX292  ";
    Ciphertext gin292A = 2 * ct_my_string0 + gout132;
    Ciphertext gout292A = ctx.eval_lut(&gin292A, and2);
    Ciphertext gin292B = 2 * ct_my_string24 + (1 - gout132);
    Ciphertext gout292B = ctx.eval_lut(&gin292B, and2);
    Ciphertext gout292 = gout292A + gout292B;
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * ct_my_string61 + gout197;
    Ciphertext gout293 = ctx.eval_lut(&gin293, nand2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout91 + gout253;
    Ciphertext gout294 = ctx.eval_lut(&gin294, nand2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout170 + gout142;
    Ciphertext gout295 = ctx.eval_lut(&gin295, and2);
    std::cerr << "\rIMUX296  ";
    Ciphertext gin296A = 2 * gout200 + gout76;
    Ciphertext gout296A = ctx.eval_lut(&gin296A, and2);
    Ciphertext gin296B = 2 * gout262 + (1 - gout76);
    Ciphertext gout296B = ctx.eval_lut(&gin296B, and2);
    Ciphertext gout296 = gout296A + gout296B;
    std::cerr << "\rIMUX297  ";
    Ciphertext gin297A = 2 * gout120 + gout132;
    Ciphertext gout297A = ctx.eval_lut(&gin297A, and2);
    Ciphertext gin297B = 2 * gout143 + (1 - gout132);
    Ciphertext gout297B = ctx.eval_lut(&gin297B, and2);
    Ciphertext gout297 = gout297A + gout297B;
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * ct_my_string62 + gout197;
    Ciphertext gout298 = ctx.eval_lut(&gin298, nand2);
    std::cerr << "\rIMUX299  ";
    Ciphertext gin299A = 2 * gout244 + gout132;
    Ciphertext gout299A = ctx.eval_lut(&gin299A, and2);
    Ciphertext gin299B = 2 * gout123 + (1 - gout132);
    Ciphertext gout299B = ctx.eval_lut(&gin299B, and2);
    Ciphertext gout299 = gout299A + gout299B;
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * ct_my_string3 + gout277;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rIMUX301  ";
    Ciphertext gin301A = 2 * gout115 + gout107;
    Ciphertext gout301A = ctx.eval_lut(&gin301A, and2);
    Ciphertext gin301B = 2 * gout149 + (1 - gout107);
    Ciphertext gout301B = ctx.eval_lut(&gin301B, and2);
    Ciphertext gout301 = gout301A + gout301B;
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout132 + gout119;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout189 + gout202;
    Ciphertext gout303 = ctx.eval_lut(&gin303, and2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout107 + gout165;
    Ciphertext gout304 = ctx.eval_lut(&gin304, nand2);
    std::cerr << "\rIMUX305  ";
    Ciphertext gin305A = 2 * gout154 + gout132;
    Ciphertext gout305A = ctx.eval_lut(&gin305A, and2);
    Ciphertext gin305B = 2 * gout150 + (1 - gout132);
    Ciphertext gout305B = ctx.eval_lut(&gin305B, and2);
    Ciphertext gout305 = gout305A + gout305B;
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * ct_my_string58 + gout197;
    Ciphertext gout306 = ctx.eval_lut(&gin306, nand2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout103 + gout248;
    Ciphertext gout307 = ctx.eval_lut(&gin307, nand2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout103 + gout249;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout250 + gout72;
    Ciphertext gout309 = ctx.eval_lut(&gin309, and2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * ct_my_string6 + gout277;
    Ciphertext gout310 = ctx.eval_lut(&gin310, nand2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout211 + gout110;
    Ciphertext gout311 = ctx.eval_lut(&gin311, and2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout226 + gout275;
    Ciphertext gout312 = ctx.eval_lut(&gin312, and2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout263 + gout237;
    Ciphertext gout313 = ctx.eval_lut(&gin313, and2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * ct_my_string39 + gout172;
    Ciphertext gout314 = ctx.eval_lut(&gin314, nand2);
    std::cerr << "\rIMUX315  ";
    Ciphertext gin315A = 2 * gout115 + gout172;
    Ciphertext gout315A = ctx.eval_lut(&gin315A, and2);
    Ciphertext gin315B = 2 * gout269 + (1 - gout172);
    Ciphertext gout315B = ctx.eval_lut(&gin315B, and2);
    Ciphertext gout315 = gout315A + gout315B;
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout132 + gout150;
    Ciphertext gout316 = ctx.eval_lut(&gin316, nand2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout107 + gout291;
    Ciphertext gout317 = ctx.eval_lut(&gin317, nand2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * ct_my_string2 + gout277;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout153 + gout138;
    Ciphertext gout319 = ctx.eval_lut(&gin319, and2);
    std::cerr << "\rIMUX320  ";
    Ciphertext gin320A = 2 * gout154 + gout76;
    Ciphertext gout320A = ctx.eval_lut(&gin320A, and2);
    Ciphertext gin320B = 2 * gout144 + (1 - gout76);
    Ciphertext gout320B = ctx.eval_lut(&gin320B, and2);
    Ciphertext gout320 = gout320A + gout320B;
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout103 + gout315;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rIMUX322  ";
    Ciphertext gin322A = 2 * gout254 + gout76;
    Ciphertext gout322A = ctx.eval_lut(&gin322A, and2);
    Ciphertext gin322B = 2 * gout212 + (1 - gout76);
    Ciphertext gout322B = ctx.eval_lut(&gin322B, and2);
    Ciphertext gout322 = gout322A + gout322B;
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout155 + gout176;
    Ciphertext gout323 = ctx.eval_lut(&gin323, and2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout107 + gout242;
    Ciphertext gout324 = ctx.eval_lut(&gin324, nand2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout76 + gout214;
    Ciphertext gout325 = ctx.eval_lut(&gin325, nand2);
    std::cerr << "\rIMUX326  ";
    Ciphertext gin326A = 2 * ct_my_string3 + gout132;
    Ciphertext gout326A = ctx.eval_lut(&gin326A, and2);
    Ciphertext gin326B = 2 * ct_my_string27 + (1 - gout132);
    Ciphertext gout326B = ctx.eval_lut(&gin326B, and2);
    Ciphertext gout326 = gout326A + gout326B;
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout76 + gout212;
    Ciphertext gout327 = ctx.eval_lut(&gin327, nand2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * ct_my_string5 + gout277;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout235 + gout191;
    Ciphertext gout329 = ctx.eval_lut(&gin329, and2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout103 + gout173;
    Ciphertext gout330 = ctx.eval_lut(&gin330, nand2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout171 + gout294;
    Ciphertext gout331 = ctx.eval_lut(&gin331, and2);
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout220 + gout127;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout236 + gout303;
    Ciphertext gout333 = ctx.eval_lut(&gin333, and2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout103 + gout182;
    Ciphertext gout334 = ctx.eval_lut(&gin334, nand2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout103 + gout194;
    Ciphertext gout335 = ctx.eval_lut(&gin335, nand2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout76 + gout301;
    Ciphertext gout336 = ctx.eval_lut(&gin336, nand2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout76 + gout195;
    Ciphertext gout337 = ctx.eval_lut(&gin337, nand2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout172 + gout292;
    Ciphertext gout338 = ctx.eval_lut(&gin338, nand2);
    std::cerr << "\rIMUX339  ";
    Ciphertext gin339A = 2 * gout245 + gout103;
    Ciphertext gout339A = ctx.eval_lut(&gin339A, and2);
    Ciphertext gin339B = 2 * gout247 + (1 - gout103);
    Ciphertext gout339B = ctx.eval_lut(&gin339B, and2);
    Ciphertext gout339 = gout339A + gout339B;
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout85 + gout253;
    Ciphertext gout340 = ctx.eval_lut(&gin340, nand2);
    std::cerr << "\rIMUX341  ";
    Ciphertext gin341A = 2 * gout244 + gout107;
    Ciphertext gout341A = ctx.eval_lut(&gin341A, and2);
    Ciphertext gin341B = 2 * gout116 + (1 - gout107);
    Ciphertext gout341B = ctx.eval_lut(&gin341B, and2);
    Ciphertext gout341 = gout341A + gout341B;
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout92 + gout253;
    Ciphertext gout342 = ctx.eval_lut(&gin342, nand2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout304 + gout168;
    Ciphertext gout343 = ctx.eval_lut(&gin343, and2);
    std::cerr << "\rIMUX344  ";
    Ciphertext gin344A = 2 * gout305 + gout107;
    Ciphertext gout344A = ctx.eval_lut(&gin344A, and2);
    Ciphertext gin344B = 2 * gout242 + (1 - gout107);
    Ciphertext gout344B = ctx.eval_lut(&gin344B, and2);
    Ciphertext gout344 = gout344A + gout344B;
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout76 + gout228;
    Ciphertext gout345 = ctx.eval_lut(&gin345, nand2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * ct_my_string0 + gout277;
    Ciphertext gout346 = ctx.eval_lut(&gin346, nand2);
    std::cerr << "\rIMUX347  ";
    Ciphertext gin347A = 2 * gout245 + gout172;
    Ciphertext gout347A = ctx.eval_lut(&gin347A, and2);
    Ciphertext gin347B = 2 * gout261 + (1 - gout172);
    Ciphertext gout347B = ctx.eval_lut(&gin347B, and2);
    Ciphertext gout347 = gout347A + gout347B;
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout76 + gout341;
    Ciphertext gout348 = ctx.eval_lut(&gin348, nand2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * ct_my_string7 + gout277;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * gout172 + gout269;
    Ciphertext gout350 = ctx.eval_lut(&gin350, nand2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout103 + gout221;
    Ciphertext gout351 = ctx.eval_lut(&gin351, nand2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout329 + gout288;
    Ciphertext gout352 = ctx.eval_lut(&gin352, and2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout107 + gout229;
    Ciphertext gout353 = ctx.eval_lut(&gin353, nand2);
    std::cerr << "\rIMUX354  ";
    Ciphertext gin354A = 2 * gout245 + gout76;
    Ciphertext gout354A = ctx.eval_lut(&gin354A, and2);
    Ciphertext gin354B = 2 * gout201 + (1 - gout76);
    Ciphertext gout354B = ctx.eval_lut(&gin354B, and2);
    Ciphertext gout354 = gout354A + gout354B;
    std::cerr << "\rIMUX355  ";
    Ciphertext gin355A = 2 * gout157 + gout172;
    Ciphertext gout355A = ctx.eval_lut(&gin355A, and2);
    Ciphertext gin355B = 2 * gout292 + (1 - gout172);
    Ciphertext gout355B = ctx.eval_lut(&gin355B, and2);
    Ciphertext gout355 = gout355A + gout355B;
    std::cerr << "\rLUT356   ";
    Ciphertext gin356 = 2 * gout289 + gout295;
    Ciphertext gout356 = ctx.eval_lut(&gin356, and2);
    std::cerr << "\rIMUX357  ";
    Ciphertext gin357A = 2 * gout115 + gout103;
    Ciphertext gout357A = ctx.eval_lut(&gin357A, and2);
    Ciphertext gin357B = 2 * gout221 + (1 - gout103);
    Ciphertext gout357B = ctx.eval_lut(&gin357B, and2);
    Ciphertext gout357 = gout357A + gout357B;
    std::cerr << "\rIMUX358  ";
    Ciphertext gin358A = 2 * gout305 + gout76;
    Ciphertext gout358A = ctx.eval_lut(&gin358A, and2);
    Ciphertext gin358B = 2 * gout228 + (1 - gout76);
    Ciphertext gout358B = ctx.eval_lut(&gin358B, and2);
    Ciphertext gout358 = gout358A + gout358B;
    std::cerr << "\rIMUX359  ";
    Ciphertext gin359A = 2 * gout133 + gout103;
    Ciphertext gout359A = ctx.eval_lut(&gin359A, and2);
    Ciphertext gin359B = 2 * gout272 + (1 - gout103);
    Ciphertext gout359B = ctx.eval_lut(&gin359B, and2);
    Ciphertext gout359 = gout359A + gout359B;
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout103 + gout355;
    Ciphertext gout360 = ctx.eval_lut(&gin360, nand2);
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout180 + gout239;
    Ciphertext gout361 = ctx.eval_lut(&gin361, and2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * ct_my_string1 + gout277;
    Ciphertext gout362 = ctx.eval_lut(&gin362, nand2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout308 + gout252;
    Ciphertext gout363 = ctx.eval_lut(&gin363, and2);
    std::cerr << "\rLUT364   ";
    Ciphertext gin364 = 2 * gout103 + gout223;
    Ciphertext gout364 = ctx.eval_lut(&gin364, nand2);
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout89 + gout253;
    Ciphertext gout365 = ctx.eval_lut(&gin365, nand2);
    std::cerr << "\rLUT366   ";
    Ciphertext gin366 = 2 * gout188 + gout298;
    Ciphertext gout366 = ctx.eval_lut(&gin366, and2);
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout293 + gout140;
    Ciphertext gout367 = ctx.eval_lut(&gin367, and2);
    std::cerr << "\rIMUX368  ";
    Ciphertext gin368A = 2 * gout283 + gout76;
    Ciphertext gout368A = ctx.eval_lut(&gin368A, and2);
    Ciphertext gin368B = 2 * gout246 + (1 - gout76);
    Ciphertext gout368B = ctx.eval_lut(&gin368B, and2);
    Ciphertext gout368 = gout368A + gout368B;
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout80 + gout253;
    Ciphertext gout369 = ctx.eval_lut(&gin369, nand2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * gout82 + gout253;
    Ciphertext gout370 = ctx.eval_lut(&gin370, nand2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout159 + gout342;
    Ciphertext gout371 = ctx.eval_lut(&gin371, and2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout230 + gout316;
    Ciphertext gout372 = ctx.eval_lut(&gin372, and2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout255 + gout213;
    Ciphertext gout373 = ctx.eval_lut(&gin373, and2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout146 + gout287;
    Ciphertext gout374 = ctx.eval_lut(&gin374, and2);
    std::cerr << "\rLUT375   ";
    Ciphertext gin375 = 2 * gout227 + gout323;
    Ciphertext gout375 = ctx.eval_lut(&gin375, and2);
    std::cerr << "\rIMUX376  ";
    Ciphertext gin376A = 2 * gout254 + gout103;
    Ciphertext gout376A = ctx.eval_lut(&gin376A, and2);
    Ciphertext gin376B = 2 * gout355 + (1 - gout103);
    Ciphertext gout376B = ctx.eval_lut(&gin376B, and2);
    Ciphertext gout376 = gout376A + gout376B;
    std::cerr << "\rLUT377   ";
    Ciphertext gin377 = 2 * gout338 + gout278;
    Ciphertext gout377 = ctx.eval_lut(&gin377, and2);
    std::cerr << "\rIMUX378  ";
    Ciphertext gin378A = 2 * gout190 + gout76;
    Ciphertext gout378A = ctx.eval_lut(&gin378A, and2);
    Ciphertext gin378B = 2 * gout301 + (1 - gout76);
    Ciphertext gout378B = ctx.eval_lut(&gin378B, and2);
    Ciphertext gout378 = gout378A + gout378B;
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout138 + gout369;
    Ciphertext gout379 = ctx.eval_lut(&gin379, and2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout268 + gout160;
    Ciphertext gout380 = ctx.eval_lut(&gin380, and2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout103 + gout233;
    Ciphertext gout381 = ctx.eval_lut(&gin381, nand2);
    std::cerr << "\rLUT382   ";
    Ciphertext gin382 = 2 * gout380 + gout273;
    Ciphertext gout382 = ctx.eval_lut(&gin382, and2);
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * ct_my_string4 + gout277;
    Ciphertext gout383 = ctx.eval_lut(&gin383, nand2);
    std::cerr << "\rIMUX384  ";
    Ciphertext gin384A = 2 * gout283 + gout103;
    Ciphertext gout384A = ctx.eval_lut(&gin384A, and2);
    Ciphertext gin384B = 2 * gout347 + (1 - gout103);
    Ciphertext gout384B = ctx.eval_lut(&gin384B, and2);
    Ciphertext gout384 = gout384A + gout384B;
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout103 + gout272;
    Ciphertext gout385 = ctx.eval_lut(&gin385, nand2);
    std::cerr << "\rLUT386   ";
    Ciphertext gin386 = 2 * gout290 + gout331;
    Ciphertext gout386 = ctx.eval_lut(&gin386, and2);
    std::cerr << "\rIMUX387  ";
    Ciphertext gin387A = 2 * gout254 + gout107;
    Ciphertext gout387A = ctx.eval_lut(&gin387A, and2);
    Ciphertext gin387B = 2 * gout251 + (1 - gout107);
    Ciphertext gout387B = ctx.eval_lut(&gin387B, and2);
    Ciphertext gout387 = gout387A + gout387B;
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * gout107 + gout251;
    Ciphertext gout388 = ctx.eval_lut(&gin388, nand2);
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout276 + gout136;
    Ciphertext gout389 = ctx.eval_lut(&gin389, and2);
    std::cerr << "\rIMUX390  ";
    Ciphertext gin390A = 2 * gout200 + gout107;
    Ciphertext gout390A = ctx.eval_lut(&gin390A, and2);
    Ciphertext gin390B = 2 * gout291 + (1 - gout107);
    Ciphertext gout390B = ctx.eval_lut(&gin390B, and2);
    Ciphertext gout390 = gout390A + gout390B;
    std::cerr << "\rLUT391   ";
    Ciphertext gin391 = 2 * gout373 + gout264;
    Ciphertext gout391 = ctx.eval_lut(&gin391, and2);
    std::cerr << "\rIMUX392  ";
    Ciphertext gin392A = 2 * gout244 + gout76;
    Ciphertext gout392A = ctx.eval_lut(&gin392A, and2);
    Ciphertext gin392B = 2 * gout121 + (1 - gout76);
    Ciphertext gout392B = ctx.eval_lut(&gin392B, and2);
    Ciphertext gout392 = gout392A + gout392B;
    std::cerr << "\rLUT393   ";
    Ciphertext gin393 = 2 * gout334 + gout372;
    Ciphertext gout393 = ctx.eval_lut(&gin393, and2);
    std::cerr << "\rLUT394   ";
    Ciphertext gin394 = 2 * gout199 + gout267;
    Ciphertext gout394 = ctx.eval_lut(&gin394, and2);
    std::cerr << "\rLUT395   ";
    Ciphertext gin395 = 2 * gout77 + gout253;
    Ciphertext gout395 = ctx.eval_lut(&gin395, nand2);
    std::cerr << "\rIMUX396  ";
    Ciphertext gin396A = 2 * gout244 + gout103;
    Ciphertext gout396A = ctx.eval_lut(&gin396A, and2);
    Ciphertext gin396B = 2 * gout249 + (1 - gout103);
    Ciphertext gout396B = ctx.eval_lut(&gin396B, and2);
    Ciphertext gout396 = gout396A + gout396B;
    std::cerr << "\rLUT397   ";
    Ciphertext gin397 = 2 * gout83 + gout253;
    Ciphertext gout397 = ctx.eval_lut(&gin397, nand2);
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout394 + gout391;
    Ciphertext gout398 = ctx.eval_lut(&gin398, nand2);
    std::cerr << "\rLUT399   ";
    Ciphertext gin399 = 2 * gout218 + gout343;
    Ciphertext gout399 = ctx.eval_lut(&gin399, and2);
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout256 + gout185;
    Ciphertext gout400 = ctx.eval_lut(&gin400, and2);
    std::cerr << "\rIMUX401  ";
    Ciphertext gin401A = 2 * gout297 + gout103;
    Ciphertext gout401A = ctx.eval_lut(&gin401A, and2);
    Ciphertext gin401B = 2 * gout248 + (1 - gout103);
    Ciphertext gout401B = ctx.eval_lut(&gin401B, and2);
    Ciphertext gout401 = gout401A + gout401B;
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * gout193 + gout311;
    Ciphertext gout402 = ctx.eval_lut(&gin402, and2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout172 + gout261;
    Ciphertext gout403 = ctx.eval_lut(&gin403, nand2);
    std::cerr << "\rIMUX404  ";
    Ciphertext gin404A = 2 * gout299 + gout76;
    Ciphertext gout404A = ctx.eval_lut(&gin404A, and2);
    Ciphertext gin404B = 2 * gout341 + (1 - gout76);
    Ciphertext gout404B = ctx.eval_lut(&gin404B, and2);
    Ciphertext gout404 = gout404A + gout404B;
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout361 + gout290;
    Ciphertext gout405 = ctx.eval_lut(&gin405, and2);
    std::cerr << "\rIMUX406  ";
    Ciphertext gin406A = 2 * gout376 + gout76;
    Ciphertext gout406A = ctx.eval_lut(&gin406A, and2);
    Ciphertext gin406B = 2 * gout387 + (1 - gout76);
    Ciphertext gout406B = ctx.eval_lut(&gin406B, and2);
    Ciphertext gout406 = gout406A + gout406B;
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout312 + gout333;
    Ciphertext gout407 = ctx.eval_lut(&gin407, nand2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout132 + gout270;
    Ciphertext gout408 = ctx.eval_lut(&gin408, nand2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout107 + gout284;
    Ciphertext gout409 = ctx.eval_lut(&gin409, nand2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout314 + gout156;
    Ciphertext gout410 = ctx.eval_lut(&gin410, and2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout306 + gout203;
    Ciphertext gout411 = ctx.eval_lut(&gin411, and2);
    std::cerr << "\rIMUX412  ";
    Ciphertext gin412A = 2 * gout133 + gout107;
    Ciphertext gout412A = ctx.eval_lut(&gin412A, and2);
    Ciphertext gin412B = 2 * gout284 + (1 - gout107);
    Ciphertext gout412B = ctx.eval_lut(&gin412B, and2);
    Ciphertext gout412 = gout412A + gout412B;
    std::cerr << "\rIMUX413  ";
    Ciphertext gin413A = 2 * gout244 + gout172;
    Ciphertext gout413A = ctx.eval_lut(&gin413A, and2);
    Ciphertext gin413B = 2 * gout326 + (1 - gout172);
    Ciphertext gout413B = ctx.eval_lut(&gin413B, and2);
    Ciphertext gout413 = gout413A + gout413B;
    std::cerr << "\rIMUX414  ";
    Ciphertext gin414A = 2 * gout166 + gout172;
    Ciphertext gout414A = ctx.eval_lut(&gin414A, and2);
    Ciphertext gin414B = 2 * gout286 + (1 - gout172);
    Ciphertext gout414B = ctx.eval_lut(&gin414B, and2);
    Ciphertext gout414 = gout414A + gout414B;
    std::cerr << "\rIMUX415  ";
    Ciphertext gin415A = 2 * gout305 + gout103;
    Ciphertext gout415A = ctx.eval_lut(&gin415A, and2);
    Ciphertext gin415B = 2 * gout194 + (1 - gout103);
    Ciphertext gout415B = ctx.eval_lut(&gin415B, and2);
    Ciphertext gout415 = gout415A + gout415B;
    std::cerr << "\rIMUX416  ";
    Ciphertext gin416A = 2 * gout190 + gout103;
    Ciphertext gout416A = ctx.eval_lut(&gin416A, and2);
    Ciphertext gin416B = 2 * gout315 + (1 - gout103);
    Ciphertext gout416B = ctx.eval_lut(&gin416B, and2);
    Ciphertext gout416 = gout416A + gout416B;
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout208 + gout371;
    Ciphertext gout417 = ctx.eval_lut(&gin417, and2);
    std::cerr << "\rIMUX418  ";
    Ciphertext gin418A = 2 * gout297 + gout107;
    Ciphertext gout418A = ctx.eval_lut(&gin418A, and2);
    Ciphertext gin418B = 2 * gout229 + (1 - gout107);
    Ciphertext gout418B = ctx.eval_lut(&gin418B, and2);
    Ciphertext gout418 = gout418A + gout418B;
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout330 + gout337;
    Ciphertext gout419 = ctx.eval_lut(&gin419, and2);
    std::cerr << "\rLUT420   ";
    Ciphertext gin420 = 2 * gout147 + gout395;
    Ciphertext gout420 = ctx.eval_lut(&gin420, and2);
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout202 + gout397;
    Ciphertext gout421 = ctx.eval_lut(&gin421, and2);
    std::cerr << "\rIMUX422  ";
    Ciphertext gin422A = 2 * gout297 + gout76;
    Ciphertext gout422A = ctx.eval_lut(&gin422A, and2);
    Ciphertext gin422B = 2 * gout158 + (1 - gout76);
    Ciphertext gout422B = ctx.eval_lut(&gin422B, and2);
    Ciphertext gout422 = gout422A + gout422B;
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * gout366 + gout259;
    Ciphertext gout423 = ctx.eval_lut(&gin423, and2);
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * gout279 + gout386;
    Ciphertext gout424 = ctx.eval_lut(&gin424, and2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * gout351 + gout258;
    Ciphertext gout425 = ctx.eval_lut(&gin425, and2);
    std::cerr << "\rIMUX426  ";
    Ciphertext gin426A = 2 * gout283 + gout107;
    Ciphertext gout426A = ctx.eval_lut(&gin426A, and2);
    Ciphertext gin426B = 2 * gout339 + (1 - gout107);
    Ciphertext gout426B = ctx.eval_lut(&gin426B, and2);
    Ciphertext gout426 = gout426A + gout426B;
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout258 + gout379;
    Ciphertext gout427 = ctx.eval_lut(&gin427, and2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout107 + gout357;
    Ciphertext gout428 = ctx.eval_lut(&gin428, nand2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout107 + gout339;
    Ciphertext gout429 = ctx.eval_lut(&gin429, nand2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout172 + gout326;
    Ciphertext gout430 = ctx.eval_lut(&gin430, nand2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout208 + gout419;
    Ciphertext gout431 = ctx.eval_lut(&gin431, and2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout103 + gout347;
    Ciphertext gout432 = ctx.eval_lut(&gin432, nand2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout375 + gout352;
    Ciphertext gout433 = ctx.eval_lut(&gin433, nand2);
    std::cerr << "\rLUT434   ";
    Ciphertext gin434 = 2 * gout271 + gout363;
    Ciphertext gout434 = ctx.eval_lut(&gin434, and2);
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout252 + gout421;
    Ciphertext gout435 = ctx.eval_lut(&gin435, and2);
    std::cerr << "\rLUT436   ";
    Ciphertext gin436 = 2 * gout282 + gout403;
    Ciphertext gout436 = ctx.eval_lut(&gin436, and2);
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout156 + gout340;
    Ciphertext gout437 = ctx.eval_lut(&gin437, and2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout285 + gout405;
    Ciphertext gout438 = ctx.eval_lut(&gin438, nand2);
    std::cerr << "\rLUT439   ";
    Ciphertext gin439 = 2 * gout353 + gout424;
    Ciphertext gout439 = ctx.eval_lut(&gin439, and2);
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout377 + gout374;
    Ciphertext gout440 = ctx.eval_lut(&gin440, and2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout184 + gout365;
    Ciphertext gout441 = ctx.eval_lut(&gin441, and2);
    std::cerr << "\rIMUX442  ";
    Ciphertext gin442A = 2 * gout299 + gout103;
    Ciphertext gout442A = ctx.eval_lut(&gin442A, and2);
    Ciphertext gin442B = 2 * gout413 + (1 - gout103);
    Ciphertext gout442B = ctx.eval_lut(&gin442B, and2);
    Ciphertext gout442 = gout442A + gout442B;
    std::cerr << "\rIMUX443  ";
    Ciphertext gin443A = 2 * gout401 + gout87;
    Ciphertext gout443A = ctx.eval_lut(&gin443A, and2);
    Ciphertext gin443B = 2 * gout418 + (1 - gout87);
    Ciphertext gout443B = ctx.eval_lut(&gin443B, and2);
    Ciphertext gout443 = gout443A + gout443B;
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout367 + gout382;
    Ciphertext gout444 = ctx.eval_lut(&gin444, nand2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout62 + gout407;
    Ciphertext gout445 = ctx.eval_lut(&gin445, nand2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout137 + gout370;
    Ciphertext gout446 = ctx.eval_lut(&gin446, and2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout364 + gout216;
    Ciphertext gout447 = ctx.eval_lut(&gin447, and2);
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * gout350 + gout325;
    Ciphertext gout448 = ctx.eval_lut(&gin448, and2);
    std::cerr << "\rIMUX449  ";
    Ciphertext gin449A = 2 * gout376 + gout87;
    Ciphertext gout449A = ctx.eval_lut(&gin449A, and2);
    Ciphertext gin449B = 2 * gout387 + (1 - gout87);
    Ciphertext gout449B = ctx.eval_lut(&gin449B, and2);
    Ciphertext gout449 = gout449A + gout449B;
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout280 + gout417;
    Ciphertext gout450 = ctx.eval_lut(&gin450, and2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout260 + gout389;
    Ciphertext gout451 = ctx.eval_lut(&gin451, nand2);
    std::cerr << "\rLUT452   ";
    Ciphertext gin452 = 2 * gout302 + gout420;
    Ciphertext gout452 = ctx.eval_lut(&gin452, and2);
    std::cerr << "\rIMUX453  ";
    Ciphertext gin453A = 2 * gout190 + gout107;
    Ciphertext gout453A = ctx.eval_lut(&gin453A, and2);
    Ciphertext gin453B = 2 * gout357 + (1 - gout107);
    Ciphertext gout453B = ctx.eval_lut(&gin453B, and2);
    Ciphertext gout453 = gout453A + gout453B;
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout381 + gout440;
    Ciphertext gout454 = ctx.eval_lut(&gin454, nand2);
    std::cerr << "\rIMUX455  ";
    Ciphertext gin455A = 2 * gout415 + gout87;
    Ciphertext gout455A = ctx.eval_lut(&gin455A, and2);
    Ciphertext gin455B = 2 * gout344 + (1 - gout87);
    Ciphertext gout455B = ctx.eval_lut(&gin455B, and2);
    Ciphertext gout455 = gout455A + gout455B;
    std::cerr << "\rIMUX456  ";
    Ciphertext gin456A = 2 * gout416 + gout87;
    Ciphertext gout456A = ctx.eval_lut(&gin456A, and2);
    Ciphertext gin456B = 2 * gout453 + (1 - gout87);
    Ciphertext gout456B = ctx.eval_lut(&gin456B, and2);
    Ciphertext gout456 = gout456A + gout456B;
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout62 + gout398;
    Ciphertext gout457 = ctx.eval_lut(&gin457, nand2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout309 + gout393;
    Ciphertext gout458 = ctx.eval_lut(&gin458, nand2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout62 + gout444;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout408 + gout243;
    Ciphertext gout460 = ctx.eval_lut(&gin460, and2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout336 + gout427;
    Ciphertext gout461 = ctx.eval_lut(&gin461, and2);
    std::cerr << "\rIMUX462  ";
    Ciphertext gin462A = 2 * gout200 + gout103;
    Ciphertext gout462A = ctx.eval_lut(&gin462A, and2);
    Ciphertext gin462B = 2 * gout414 + (1 - gout103);
    Ciphertext gout462B = ctx.eval_lut(&gin462B, and2);
    Ciphertext gout462 = gout462A + gout462B;
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout62 + gout451;
    Ciphertext gout463 = ctx.eval_lut(&gin463, nand2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout313 + gout425;
    Ciphertext gout464 = ctx.eval_lut(&gin464, and2);
    std::cerr << "\rLUT465   ";
    Ciphertext gin465 = 2 * gout400 + gout319;
    Ciphertext gout465 = ctx.eval_lut(&gin465, and2);
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout356 + gout431;
    Ciphertext gout466 = ctx.eval_lut(&gin466, nand2);
    std::cerr << "\rLUT467   ";
    Ciphertext gin467 = 2 * gout62 + gout433;
    Ciphertext gout467 = ctx.eval_lut(&gin467, nand2);
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout232 + gout452;
    Ciphertext gout468 = ctx.eval_lut(&gin468, and2);
    std::cerr << "\rLUT469   ";
    Ciphertext gin469 = 2 * gout348 + gout435;
    Ciphertext gout469 = ctx.eval_lut(&gin469, and2);
    std::cerr << "\rLUT470   ";
    Ciphertext gin470 = 2 * gout107 + gout396;
    Ciphertext gout470 = ctx.eval_lut(&gin470, nand2);
    std::cerr << "\rLUT471   ";
    Ciphertext gin471 = 2 * gout383 + gout457;
    Ciphertext gout471 = ctx.eval_lut(&gin471, nand2);
    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout410 + gout332;
    Ciphertext gout472 = ctx.eval_lut(&gin472, and2);
    std::cerr << "\rIMUX473  ";
    Ciphertext gin473A = 2 * gout359 + gout87;
    Ciphertext gout473A = ctx.eval_lut(&gin473A, and2);
    Ciphertext gin473B = 2 * gout412 + (1 - gout87);
    Ciphertext gout473B = ctx.eval_lut(&gin473B, and2);
    Ciphertext gout473 = gout473A + gout473B;
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout408 + gout437;
    Ciphertext gout474 = ctx.eval_lut(&gin474, and2);
    std::cerr << "\rLUT475   ";
    Ciphertext gin475 = 2 * gout103 + gout413;
    Ciphertext gout475 = ctx.eval_lut(&gin475, nand2);
    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout470 + gout469;
    Ciphertext gout476 = ctx.eval_lut(&gin476, and2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout266 + gout423;
    Ciphertext gout477 = ctx.eval_lut(&gin477, nand2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout328 + gout459;
    Ciphertext gout478 = ctx.eval_lut(&gin478, nand2);
    std::cerr << "\rLUT479   ";
    Ciphertext gin479 = 2 * gout307 + gout439;
    Ciphertext gout479 = ctx.eval_lut(&gin479, nand2);
    std::cerr << "\rIMUX480  ";
    Ciphertext gin480A = 2 * gout401 + gout76;
    Ciphertext gout480A = ctx.eval_lut(&gin480A, and2);
    Ciphertext gin480B = 2 * gout418 + (1 - gout76);
    Ciphertext gout480B = ctx.eval_lut(&gin480B, and2);
    Ciphertext gout480 = gout480A + gout480B;
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout300 + gout445;
    Ciphertext gout481 = ctx.eval_lut(&gin481, nand2);
    std::cerr << "\rIMUX482  ";
    Ciphertext gin482A = 2 * gout299 + gout107;
    Ciphertext gout482A = ctx.eval_lut(&gin482A, and2);
    Ciphertext gin482B = 2 * gout396 + (1 - gout107);
    Ciphertext gout482B = ctx.eval_lut(&gin482B, and2);
    Ciphertext gout482 = gout482A + gout482B;
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout430 + gout207;
    Ciphertext gout483 = ctx.eval_lut(&gin483, and2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout146 + gout446;
    Ciphertext gout484 = ctx.eval_lut(&gin484, and2);
    std::cerr << "\rIMUX485  ";
    Ciphertext gin485A = 2 * gout416 + gout76;
    Ciphertext gout485A = ctx.eval_lut(&gin485A, and2);
    Ciphertext gin485B = 2 * gout453 + (1 - gout76);
    Ciphertext gout485B = ctx.eval_lut(&gin485B, and2);
    Ciphertext gout485 = gout485A + gout485B;
    std::cerr << "\rIMUX486  ";
    Ciphertext gin486A = 2 * gout359 + gout76;
    Ciphertext gout486A = ctx.eval_lut(&gin486A, and2);
    Ciphertext gin486B = 2 * gout412 + (1 - gout76);
    Ciphertext gout486B = ctx.eval_lut(&gin486B, and2);
    Ciphertext gout486 = gout486A + gout486B;
    std::cerr << "\rIMUX487  ";
    Ciphertext gin487A = 2 * gout384 + gout87;
    Ciphertext gout487A = ctx.eval_lut(&gin487A, and2);
    Ciphertext gin487B = 2 * gout426 + (1 - gout87);
    Ciphertext gout487B = ctx.eval_lut(&gin487B, and2);
    Ciphertext gout487 = gout487A + gout487B;
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * gout103 + gout414;
    Ciphertext gout488 = ctx.eval_lut(&gin488, nand2);
    std::cerr << "\rIMUX489  ";
    Ciphertext gin489A = 2 * gout120 + gout69;
    Ciphertext gout489A = ctx.eval_lut(&gin489A, and2);
    Ciphertext gin489B = 2 * gout438 + (1 - gout69);
    Ciphertext gout489B = ctx.eval_lut(&gin489B, and2);
    Ciphertext gout489 = gout489A + gout489B;
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout483 + gout434;
    Ciphertext gout490 = ctx.eval_lut(&gin490, nand2);
    std::cerr << "\rLUT491   ";
    Ciphertext gin491 = 2 * gout346 + gout467;
    Ciphertext gout491 = ctx.eval_lut(&gin491, nand2);
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout428 + gout461;
    Ciphertext gout492 = ctx.eval_lut(&gin492, and2);
    std::cerr << "\rIMUX493  ";
    Ciphertext gin493A = 2 * gout415 + gout76;
    Ciphertext gout493A = ctx.eval_lut(&gin493A, and2);
    Ciphertext gin493B = 2 * gout344 + (1 - gout76);
    Ciphertext gout493B = ctx.eval_lut(&gin493B, and2);
    Ciphertext gout493 = gout493A + gout493B;
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * gout411 + gout465;
    Ciphertext gout494 = ctx.eval_lut(&gin494, nand2);
    std::cerr << "\rIMUX495  ";
    Ciphertext gin495A = 2 * gout442 + gout76;
    Ciphertext gout495A = ctx.eval_lut(&gin495A, and2);
    Ciphertext gin495B = 2 * gout482 + (1 - gout76);
    Ciphertext gout495B = ctx.eval_lut(&gin495B, and2);
    Ciphertext gout495 = gout495A + gout495B;
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout327 + gout484;
    Ciphertext gout496 = ctx.eval_lut(&gin496, and2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout362 + gout463;
    Ciphertext gout497 = ctx.eval_lut(&gin497, nand2);
    std::cerr << "\rIMUX498  ";
    Ciphertext gin498A = 2 * gout384 + gout76;
    Ciphertext gout498A = ctx.eval_lut(&gin498A, and2);
    Ciphertext gin498B = 2 * gout426 + (1 - gout76);
    Ciphertext gout498B = ctx.eval_lut(&gin498B, and2);
    Ciphertext gout498 = gout498A + gout498B;
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * gout302 + gout447;
    Ciphertext gout499 = ctx.eval_lut(&gin499, and2);
    std::cerr << "\rIMUX500  ";
    Ciphertext gin500A = 2 * gout157 + gout69;
    Ciphertext gout500A = ctx.eval_lut(&gin500A, and2);
    Ciphertext gin500B = 2 * gout454 + (1 - gout69);
    Ciphertext gout500B = ctx.eval_lut(&gin500B, and2);
    Ciphertext gout500 = gout500A + gout500B;
    std::cerr << "\rIMUX501  ";
    Ciphertext gin501A = 2 * gout244 + gout69;
    Ciphertext gout501A = ctx.eval_lut(&gin501A, and2);
    Ciphertext gin501B = 2 * gout490 + (1 - gout69);
    Ciphertext gout501B = ctx.eval_lut(&gin501B, and2);
    Ciphertext gout501 = gout501A + gout501B;
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout317 + gout450;
    Ciphertext gout502 = ctx.eval_lut(&gin502, and2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout62 + gout477;
    Ciphertext gout503 = ctx.eval_lut(&gin503, nand2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * gout321 + gout492;
    Ciphertext gout504 = ctx.eval_lut(&gin504, nand2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * gout274 + gout474;
    Ciphertext gout505 = ctx.eval_lut(&gin505, and2);
    std::cerr << "\rIMUX506  ";
    Ciphertext gin506A = 2 * gout154 + gout69;
    Ciphertext gout506A = ctx.eval_lut(&gin506A, and2);
    Ciphertext gin506B = 2 * gout458 + (1 - gout69);
    Ciphertext gout506B = ctx.eval_lut(&gin506B, and2);
    Ciphertext gout506 = gout506A + gout506B;
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout436 + gout460;
    Ciphertext gout507 = ctx.eval_lut(&gin507, nand2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout316 + gout441;
    Ciphertext gout508 = ctx.eval_lut(&gin508, and2);
    std::cerr << "\rIMUX509  ";
    Ciphertext gin509A = 2 * gout462 + gout76;
    Ciphertext gout509A = ctx.eval_lut(&gin509A, and2);
    Ciphertext gin509B = 2 * gout390 + (1 - gout76);
    Ciphertext gout509B = ctx.eval_lut(&gin509B, and2);
    Ciphertext gout509 = gout509A + gout509B;
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout448 + gout464;
    Ciphertext gout510 = ctx.eval_lut(&gin510, nand2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * gout399 + gout499;
    Ciphertext gout511 = ctx.eval_lut(&gin511, nand2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout488 + gout502;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rIMUX513  ";
    Ciphertext gin513A = 2 * gout166 + gout69;
    Ciphertext gout513A = ctx.eval_lut(&gin513A, and2);
    Ciphertext gin513B = 2 * gout466 + (1 - gout69);
    Ciphertext gout513B = ctx.eval_lut(&gin513B, and2);
    Ciphertext gout513 = gout513A + gout513B;
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * gout409 + gout468;
    Ciphertext gout514 = ctx.eval_lut(&gin514, and2);
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout402 + gout472;
    Ciphertext gout515 = ctx.eval_lut(&gin515, nand2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * gout475 + gout476;
    Ciphertext gout516 = ctx.eval_lut(&gin516, nand2);
    std::cerr << "\rIMUX517  ";
    Ciphertext gin517A = 2 * gout297 + gout78;
    Ciphertext gout517A = ctx.eval_lut(&gin517A, and2);
    Ciphertext gin517B = 2 * gout479 + (1 - gout78);
    Ciphertext gout517B = ctx.eval_lut(&gin517B, and2);
    Ciphertext gout517 = gout517A + gout517B;
    std::cerr << "\rIMUX518  ";
    Ciphertext gin518A = 2 * gout115 + gout69;
    Ciphertext gout518A = ctx.eval_lut(&gin518A, and2);
    Ciphertext gin518B = 2 * gout510 + (1 - gout69);
    Ciphertext gout518B = ctx.eval_lut(&gin518B, and2);
    Ciphertext gout518 = gout518A + gout518B;
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout388 + gout496;
    Ciphertext gout519 = ctx.eval_lut(&gin519, and2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * gout62 + gout494;
    Ciphertext gout520 = ctx.eval_lut(&gin520, nand2);
    std::cerr << "\rIMUX521  ";
    Ciphertext gin521A = 2 * gout462 + gout87;
    Ciphertext gout521A = ctx.eval_lut(&gin521A, and2);
    Ciphertext gin521B = 2 * gout390 + (1 - gout87);
    Ciphertext gout521B = ctx.eval_lut(&gin521B, and2);
    Ciphertext gout521 = gout521A + gout521B;
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * gout62 + gout515;
    Ciphertext gout522 = ctx.eval_lut(&gin522, nand2);
    std::cerr << "\rIMUX523  ";
    Ciphertext gin523A = 2 * gout442 + gout87;
    Ciphertext gout523A = ctx.eval_lut(&gin523A, and2);
    Ciphertext gin523B = 2 * gout482 + (1 - gout87);
    Ciphertext gout523B = ctx.eval_lut(&gin523B, and2);
    Ciphertext gout523 = gout523A + gout523B;
    std::cerr << "\rIMUX524  ";
    Ciphertext gin524A = 2 * gout299 + gout78;
    Ciphertext gout524A = ctx.eval_lut(&gin524A, and2);
    Ciphertext gin524B = 2 * gout516 + (1 - gout78);
    Ciphertext gout524B = ctx.eval_lut(&gin524B, and2);
    Ciphertext gout524 = gout524A + gout524B;
    std::cerr << "\rLUT525   ";
    Ciphertext gin525 = 2 * gout429 + gout505;
    Ciphertext gout525 = ctx.eval_lut(&gin525, and2);
    std::cerr << "\rLUT526   ";
    Ciphertext gin526 = 2 * gout310 + gout503;
    Ciphertext gout526 = ctx.eval_lut(&gin526, nand2);
    std::cerr << "\rLUT527   ";
    Ciphertext gin527 = 2 * gout345 + gout508;
    Ciphertext gout527 = ctx.eval_lut(&gin527, and2);
    std::cerr << "\rIMUX528  ";
    Ciphertext gin528A = 2 * gout245 + gout69;
    Ciphertext gout528A = ctx.eval_lut(&gin528A, and2);
    Ciphertext gin528B = 2 * gout507 + (1 - gout69);
    Ciphertext gout528B = ctx.eval_lut(&gin528B, and2);
    Ciphertext gout528 = gout528A + gout528B;
    std::cerr << "\rIMUX529  ";
    Ciphertext gin529A = 2 * gout190 + gout78;
    Ciphertext gout529A = ctx.eval_lut(&gin529A, and2);
    Ciphertext gin529B = 2 * gout504 + (1 - gout78);
    Ciphertext gout529B = ctx.eval_lut(&gin529B, and2);
    Ciphertext gout529 = gout529A + gout529B;
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * gout360 + gout519;
    Ciphertext gout530 = ctx.eval_lut(&gin530, nand2);
    std::cerr << "\rIMUX531  ";
    Ciphertext gin531A = 2 * gout111 + gout69;
    Ciphertext gout531A = ctx.eval_lut(&gin531A, and2);
    Ciphertext gin531B = 2 * gout511 + (1 - gout69);
    Ciphertext gout531B = ctx.eval_lut(&gin531B, and2);
    Ciphertext gout531 = gout531A + gout531B;
    std::cerr << "\rIMUX532  ";
    Ciphertext gin532A = 2 * gout200 + gout78;
    Ciphertext gout532A = ctx.eval_lut(&gin532A, and2);
    Ciphertext gin532B = 2 * gout512 + (1 - gout78);
    Ciphertext gout532B = ctx.eval_lut(&gin532B, and2);
    Ciphertext gout532 = gout532A + gout532B;
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout349 + gout522;
    Ciphertext gout533 = ctx.eval_lut(&gin533, nand2);
    std::cerr << "\rLUT534   ";
    Ciphertext gin534 = 2 * gout432 + gout525;
    Ciphertext gout534 = ctx.eval_lut(&gin534, nand2);
    std::cerr << "\rLUT535   ";
    Ciphertext gin535 = 2 * gout385 + gout514;
    Ciphertext gout535 = ctx.eval_lut(&gin535, nand2);
    std::cerr << "\rLUT536   ";
    Ciphertext gin536 = 2 * gout324 + gout527;
    Ciphertext gout536 = ctx.eval_lut(&gin536, and2);
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * gout318 + gout520;
    Ciphertext gout537 = ctx.eval_lut(&gin537, nand2);
    std::cerr << "\rIMUX538  ";
    Ciphertext gin538A = 2 * gout133 + gout78;
    Ciphertext gout538A = ctx.eval_lut(&gin538A, and2);
    Ciphertext gin538B = 2 * gout535 + (1 - gout78);
    Ciphertext gout538B = ctx.eval_lut(&gin538B, and2);
    Ciphertext gout538 = gout538A + gout538B;
    std::cerr << "\rIMUX539  ";
    Ciphertext gin539A = 2 * gout254 + gout78;
    Ciphertext gout539A = ctx.eval_lut(&gin539A, and2);
    Ciphertext gin539B = 2 * gout530 + (1 - gout78);
    Ciphertext gout539B = ctx.eval_lut(&gin539B, and2);
    Ciphertext gout539 = gout539A + gout539B;
    std::cerr << "\rIMUX540  ";
    Ciphertext gin540A = 2 * gout283 + gout78;
    Ciphertext gout540A = ctx.eval_lut(&gin540A, and2);
    Ciphertext gin540B = 2 * gout534 + (1 - gout78);
    Ciphertext gout540B = ctx.eval_lut(&gin540B, and2);
    Ciphertext gout540 = gout540A + gout540B;
    std::cerr << "\rLUT541   ";
    Ciphertext gin541 = 2 * gout335 + gout536;
    Ciphertext gout541 = ctx.eval_lut(&gin541, nand2);
    std::cerr << "\rIMUX542  ";
    Ciphertext gin542A = 2 * gout305 + gout78;
    Ciphertext gout542A = ctx.eval_lut(&gin542A, and2);
    Ciphertext gin542B = 2 * gout541 + (1 - gout78);
    Ciphertext gout542B = ctx.eval_lut(&gin542B, and2);
    Ciphertext gout542 = gout542A + gout542B;

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout491)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout497)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout537)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout481)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout471)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout478)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout526)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout533)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout500)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout489)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout518)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout501)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout513)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout506)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout531)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout528)); /* out15 */
    test_out.push_back(ctx.decrypt(&gout539)); /* out16 */
    test_out.push_back(ctx.decrypt(&gout517)); /* out17 */
    test_out.push_back(ctx.decrypt(&gout529)); /* out18 */
    test_out.push_back(ctx.decrypt(&gout524)); /* out19 */
    test_out.push_back(ctx.decrypt(&gout532)); /* out20 */
    test_out.push_back(ctx.decrypt(&gout542)); /* out21 */
    test_out.push_back(ctx.decrypt(&gout538)); /* out22 */
    test_out.push_back(ctx.decrypt(&gout540)); /* out23 */
    test_out.push_back(ctx.decrypt(&gout449)); /* out24 */
    test_out.push_back(ctx.decrypt(&gout443)); /* out25 */
    test_out.push_back(ctx.decrypt(&gout456)); /* out26 */
    test_out.push_back(ctx.decrypt(&gout523)); /* out27 */
    test_out.push_back(ctx.decrypt(&gout521)); /* out28 */
    test_out.push_back(ctx.decrypt(&gout455)); /* out29 */
    test_out.push_back(ctx.decrypt(&gout473)); /* out30 */
    test_out.push_back(ctx.decrypt(&gout487)); /* out31 */
    test_out.push_back(ctx.decrypt(&gout406)); /* out32 */
    test_out.push_back(ctx.decrypt(&gout480)); /* out33 */
    test_out.push_back(ctx.decrypt(&gout485)); /* out34 */
    test_out.push_back(ctx.decrypt(&gout495)); /* out35 */
    test_out.push_back(ctx.decrypt(&gout509)); /* out36 */
    test_out.push_back(ctx.decrypt(&gout493)); /* out37 */
    test_out.push_back(ctx.decrypt(&gout486)); /* out38 */
    test_out.push_back(ctx.decrypt(&gout498)); /* out39 */
    test_out.push_back(ctx.decrypt(&gout322)); /* out40 */
    test_out.push_back(ctx.decrypt(&gout422)); /* out41 */
    test_out.push_back(ctx.decrypt(&gout378)); /* out42 */
    test_out.push_back(ctx.decrypt(&gout404)); /* out43 */
    test_out.push_back(ctx.decrypt(&gout296)); /* out44 */
    test_out.push_back(ctx.decrypt(&gout358)); /* out45 */
    test_out.push_back(ctx.decrypt(&gout238)); /* out46 */
    test_out.push_back(ctx.decrypt(&gout368)); /* out47 */
    test_out.push_back(ctx.decrypt(&gout204)); /* out48 */
    test_out.push_back(ctx.decrypt(&gout257)); /* out49 */
    test_out.push_back(ctx.decrypt(&gout281)); /* out50 */
    test_out.push_back(ctx.decrypt(&gout392)); /* out51 */
    test_out.push_back(ctx.decrypt(&gout265)); /* out52 */
    test_out.push_back(ctx.decrypt(&gout320)); /* out53 */
    test_out.push_back(ctx.decrypt(&gout164)); /* out54 */
    test_out.push_back(ctx.decrypt(&gout354)); /* out55 */
    test_out.push_back(ctx.decrypt(&gout82)); /* out56 */
    test_out.push_back(ctx.decrypt(&gout91)); /* out57 */
    test_out.push_back(ctx.decrypt(&gout80)); /* out58 */
    test_out.push_back(ctx.decrypt(&gout83)); /* out59 */
    test_out.push_back(ctx.decrypt(&gout92)); /* out60 */
    test_out.push_back(ctx.decrypt(&gout89)); /* out61 */
    test_out.push_back(ctx.decrypt(&gout77)); /* out62 */
    test_out.push_back(ctx.decrypt(&gout85)); /* out63 */
    return test_out;
}

static void
BM_strrev_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_strrev_boolean(ctx);
    }
}

BENCHMARK(BM_strrev_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
