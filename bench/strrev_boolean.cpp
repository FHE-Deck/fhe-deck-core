#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_strrev_boolean(FHEContext& ctx, uint64_t my_string = 0)
{
    Ciphertext ct_my_string0 = ctx.encrypt_public((my_string >> 0) & 1);
    Ciphertext ct_my_string1 = ctx.encrypt_public((my_string >> 1) & 1);
    Ciphertext ct_my_string2 = ctx.encrypt_public((my_string >> 2) & 1);
    Ciphertext ct_my_string3 = ctx.encrypt_public((my_string >> 3) & 1);
    Ciphertext ct_my_string4 = ctx.encrypt_public((my_string >> 4) & 1);
    Ciphertext ct_my_string5 = ctx.encrypt_public((my_string >> 5) & 1);
    Ciphertext ct_my_string6 = ctx.encrypt_public((my_string >> 6) & 1);
    Ciphertext ct_my_string7 = ctx.encrypt_public((my_string >> 7) & 1);
    Ciphertext ct_my_string8 = ctx.encrypt_public((my_string >> 8) & 1);
    Ciphertext ct_my_string9 = ctx.encrypt_public((my_string >> 9) & 1);
    Ciphertext ct_my_string10 = ctx.encrypt_public((my_string >> 10) & 1);
    Ciphertext ct_my_string11 = ctx.encrypt_public((my_string >> 11) & 1);
    Ciphertext ct_my_string12 = ctx.encrypt_public((my_string >> 12) & 1);
    Ciphertext ct_my_string13 = ctx.encrypt_public((my_string >> 13) & 1);
    Ciphertext ct_my_string14 = ctx.encrypt_public((my_string >> 14) & 1);
    Ciphertext ct_my_string15 = ctx.encrypt_public((my_string >> 15) & 1);
    Ciphertext ct_my_string16 = ctx.encrypt_public((my_string >> 16) & 1);
    Ciphertext ct_my_string17 = ctx.encrypt_public((my_string >> 17) & 1);
    Ciphertext ct_my_string18 = ctx.encrypt_public((my_string >> 18) & 1);
    Ciphertext ct_my_string19 = ctx.encrypt_public((my_string >> 19) & 1);
    Ciphertext ct_my_string20 = ctx.encrypt_public((my_string >> 20) & 1);
    Ciphertext ct_my_string21 = ctx.encrypt_public((my_string >> 21) & 1);
    Ciphertext ct_my_string22 = ctx.encrypt_public((my_string >> 22) & 1);
    Ciphertext ct_my_string23 = ctx.encrypt_public((my_string >> 23) & 1);
    Ciphertext ct_my_string24 = ctx.encrypt_public((my_string >> 24) & 1);
    Ciphertext ct_my_string25 = ctx.encrypt_public((my_string >> 25) & 1);
    Ciphertext ct_my_string26 = ctx.encrypt_public((my_string >> 26) & 1);
    Ciphertext ct_my_string27 = ctx.encrypt_public((my_string >> 27) & 1);
    Ciphertext ct_my_string28 = ctx.encrypt_public((my_string >> 28) & 1);
    Ciphertext ct_my_string29 = ctx.encrypt_public((my_string >> 29) & 1);
    Ciphertext ct_my_string30 = ctx.encrypt_public((my_string >> 30) & 1);
    Ciphertext ct_my_string31 = ctx.encrypt_public((my_string >> 31) & 1);
    Ciphertext ct_my_string32 = ctx.encrypt_public((my_string >> 32) & 1);
    Ciphertext ct_my_string33 = ctx.encrypt_public((my_string >> 33) & 1);
    Ciphertext ct_my_string34 = ctx.encrypt_public((my_string >> 34) & 1);
    Ciphertext ct_my_string35 = ctx.encrypt_public((my_string >> 35) & 1);
    Ciphertext ct_my_string36 = ctx.encrypt_public((my_string >> 36) & 1);
    Ciphertext ct_my_string37 = ctx.encrypt_public((my_string >> 37) & 1);
    Ciphertext ct_my_string38 = ctx.encrypt_public((my_string >> 38) & 1);
    Ciphertext ct_my_string39 = ctx.encrypt_public((my_string >> 39) & 1);
    Ciphertext ct_my_string40 = ctx.encrypt_public((my_string >> 40) & 1);
    Ciphertext ct_my_string41 = ctx.encrypt_public((my_string >> 41) & 1);
    Ciphertext ct_my_string42 = ctx.encrypt_public((my_string >> 42) & 1);
    Ciphertext ct_my_string43 = ctx.encrypt_public((my_string >> 43) & 1);
    Ciphertext ct_my_string44 = ctx.encrypt_public((my_string >> 44) & 1);
    Ciphertext ct_my_string45 = ctx.encrypt_public((my_string >> 45) & 1);
    Ciphertext ct_my_string46 = ctx.encrypt_public((my_string >> 46) & 1);
    Ciphertext ct_my_string47 = ctx.encrypt_public((my_string >> 47) & 1);
    Ciphertext ct_my_string48 = ctx.encrypt_public((my_string >> 48) & 1);
    Ciphertext ct_my_string49 = ctx.encrypt_public((my_string >> 49) & 1);
    Ciphertext ct_my_string50 = ctx.encrypt_public((my_string >> 50) & 1);
    Ciphertext ct_my_string51 = ctx.encrypt_public((my_string >> 51) & 1);
    Ciphertext ct_my_string52 = ctx.encrypt_public((my_string >> 52) & 1);
    Ciphertext ct_my_string53 = ctx.encrypt_public((my_string >> 53) & 1);
    Ciphertext ct_my_string54 = ctx.encrypt_public((my_string >> 54) & 1);
    Ciphertext ct_my_string55 = ctx.encrypt_public((my_string >> 55) & 1);
    Ciphertext ct_my_string56 = ctx.encrypt_public((my_string >> 56) & 1);
    Ciphertext ct_my_string57 = ctx.encrypt_public((my_string >> 57) & 1);
    Ciphertext ct_my_string58 = ctx.encrypt_public((my_string >> 58) & 1);
    Ciphertext ct_my_string59 = ctx.encrypt_public((my_string >> 59) & 1);
    Ciphertext ct_my_string60 = ctx.encrypt_public((my_string >> 60) & 1);
    Ciphertext ct_my_string61 = ctx.encrypt_public((my_string >> 61) & 1);
    Ciphertext ct_my_string62 = ctx.encrypt_public((my_string >> 62) & 1);
    Ciphertext ct_my_string63 = ctx.encrypt_public((my_string >> 63) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_my_string28 + ct_my_string29;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_my_string0 + ct_my_string1;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_my_string2 + ct_my_string3;
    Ciphertext gout2 = ctx.eval_lut(&gin2, nor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_my_string10 + ct_my_string11;
    Ciphertext gout3 = ctx.eval_lut(&gin3, nor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_my_string6 + ct_my_string7;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nor2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_my_string52 + ct_my_string55;
    Ciphertext gout5 = ctx.eval_lut(&gin5, nor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_my_string4 + ct_my_string5;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_my_string12 + ct_my_string13;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_my_string53 + ct_my_string54;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nor2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_my_string8 + ct_my_string9;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_my_string14 + ct_my_string15;
    Ciphertext gout10 = ctx.eval_lut(&gin10, nor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_my_string56 + ct_my_string57;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_my_string62 + ct_my_string63;
    Ciphertext gout12 = ctx.eval_lut(&gin12, nor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_my_string60 + ct_my_string61;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nor2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_my_string36 + ct_my_string37;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nor2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_my_string58 + ct_my_string59;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_my_string32 + ct_my_string33;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_my_string40 + ct_my_string41;
    Ciphertext gout17 = ctx.eval_lut(&gin17, nor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_my_string38 + ct_my_string39;
    Ciphertext gout18 = ctx.eval_lut(&gin18, nor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_my_string46 + ct_my_string47;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_my_string44 + ct_my_string45;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nor2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_my_string48 + ct_my_string51;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_my_string34 + ct_my_string35;
    Ciphertext gout22 = ctx.eval_lut(&gin22, nor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_my_string42 + ct_my_string43;
    Ciphertext gout23 = ctx.eval_lut(&gin23, nor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_my_string19 + ct_my_string20;
    Ciphertext gout24 = ctx.eval_lut(&gin24, nor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_my_string23 + ct_my_string16;
    Ciphertext gout25 = ctx.eval_lut(&gin25, nor2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_my_string21 + ct_my_string22;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nor2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_my_string17 + ct_my_string18;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nor2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_my_string26 + ct_my_string27;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nor2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_my_string24 + ct_my_string25;
    Ciphertext gout29 = ctx.eval_lut(&gin29, nor2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_my_string49 + ct_my_string50;
    Ciphertext gout30 = ctx.eval_lut(&gin30, nor2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_my_string30 + ct_my_string31;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nor2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * gout8 + gout5;
    Ciphertext gout32 = ctx.eval_lut(&gin32, and2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * gout6 + gout4;
    Ciphertext gout33 = ctx.eval_lut(&gin33, and2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * gout7 + gout10;
    Ciphertext gout34 = ctx.eval_lut(&gin34, and2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * gout1 + gout2;
    Ciphertext gout35 = ctx.eval_lut(&gin35, and2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * gout9 + gout3;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * gout30 + gout21;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * gout11 + gout15;
    Ciphertext gout38 = ctx.eval_lut(&gin38, and2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * gout16 + gout22;
    Ciphertext gout39 = ctx.eval_lut(&gin39, and2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * gout14 + gout18;
    Ciphertext gout40 = ctx.eval_lut(&gin40, and2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * gout20 + gout19;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * gout13 + gout12;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * gout17 + gout23;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * gout24 + gout26;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * gout0 + gout31;
    Ciphertext gout45 = ctx.eval_lut(&gin45, and2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * gout25 + gout27;
    Ciphertext gout46 = ctx.eval_lut(&gin46, and2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * gout29 + gout28;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * gout33 + gout35;
    Ciphertext gout48 = ctx.eval_lut(&gin48, nand2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * gout34 + gout36;
    Ciphertext gout49 = ctx.eval_lut(&gin49, nand2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * gout32 + gout37;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * gout42 + gout38;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * gout40 + gout39;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * gout41 + gout43;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * gout44 + gout46;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * gout45 + gout47;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * gout48 + gout49;
    Ciphertext gout56 = ctx.eval_lut(&gin56, and2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * (1 - gout50) + gout51;
    Ciphertext gout57 = ctx.eval_lut(&gin57, or2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * gout50 + gout51;
    Ciphertext gout58 = ctx.eval_lut(&gin58, and2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * gout52 + gout53;
    Ciphertext gout59 = ctx.eval_lut(&gin59, and2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * gout54 + gout55;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rINV61   ";
    Ciphertext gout61 = 1 - gout56;

    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * gout53 + gout57;
    Ciphertext gout62 = ctx.eval_lut(&gin62, nand2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * gout60 + gout56;
    Ciphertext gout63 = ctx.eval_lut(&gin63, nand2);
    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * (1 - gout59) + gout58;
    Ciphertext gout64 = ctx.eval_lut(&gin64, or2);
    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * gout58 + gout59;
    Ciphertext gout65 = ctx.eval_lut(&gin65, and2);
    std::cerr << "\rINV66   ";
    Ciphertext gout66 = 1 - gout59;

    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * gout59 + gout60;
    Ciphertext gout67 = ctx.eval_lut(&gin67, and2);
    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * ct_my_string49 + gout65;
    Ciphertext gout68 = ctx.eval_lut(&gin68, nand2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * gout65 + gout63;
    Ciphertext gout69 = ctx.eval_lut(&gin69, or2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_my_string53 + gout65;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * gout52 + gout62;
    Ciphertext gout71 = ctx.eval_lut(&gin71, nand2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * (1 - gout67) + gout56;
    Ciphertext gout72 = ctx.eval_lut(&gin72, and2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * gout65 + (1 - gout63);
    Ciphertext gout73 = ctx.eval_lut(&gin73, and2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * gout66 + gout63;
    Ciphertext gout74 = ctx.eval_lut(&gin74, or2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * gout64 + gout60;
    Ciphertext gout75 = ctx.eval_lut(&gin75, nand2);
    std::cerr << "\rINV76   ";
    Ciphertext gout76 = 1 - gout69;

    std::cerr << "\rIMUX77  ";
    Ciphertext gin77A = 2 * ct_my_string4 + gout73;
    Ciphertext gout77A = ctx.eval_lut(&gin77A, and2);
    Ciphertext gin77B = 2 * ct_my_string60 + (1 - gout73);
    Ciphertext gout77B = ctx.eval_lut(&gin77B, and2);
    Ciphertext gout77 = gout77A + gout77B;
    std::cerr << "\rINV78   ";
    Ciphertext gout78 = 1 - gout73;

    std::cerr << "\rIMUX79  ";
    Ciphertext gin79A = 2 * ct_my_string2 + gout73;
    Ciphertext gout79A = ctx.eval_lut(&gin79A, and2);
    Ciphertext gin79B = 2 * ct_my_string58 + (1 - gout73);
    Ciphertext gout79B = ctx.eval_lut(&gin79B, and2);
    Ciphertext gout79 = gout79A + gout79B;
    std::cerr << "\rIMUX80  ";
    Ciphertext gin80A = 2 * ct_my_string0 + gout73;
    Ciphertext gout80A = ctx.eval_lut(&gin80A, and2);
    Ciphertext gin80B = 2 * ct_my_string56 + (1 - gout73);
    Ciphertext gout80B = ctx.eval_lut(&gin80B, and2);
    Ciphertext gout80 = gout80A + gout80B;
    std::cerr << "\rIMUX81  ";
    Ciphertext gin81A = 2 * ct_my_string7 + gout73;
    Ciphertext gout81A = ctx.eval_lut(&gin81A, and2);
    Ciphertext gin81B = 2 * ct_my_string63 + (1 - gout73);
    Ciphertext gout81B = ctx.eval_lut(&gin81B, and2);
    Ciphertext gout81 = gout81A + gout81B;
    std::cerr << "\rIMUX82  ";
    Ciphertext gin82A = 2 * ct_my_string3 + gout73;
    Ciphertext gout82A = ctx.eval_lut(&gin82A, and2);
    Ciphertext gin82B = 2 * ct_my_string59 + (1 - gout73);
    Ciphertext gout82B = ctx.eval_lut(&gin82B, and2);
    Ciphertext gout82 = gout82A + gout82B;
    std::cerr << "\rIMUX83  ";
    Ciphertext gin83A = 2 * ct_my_string6 + gout73;
    Ciphertext gout83A = ctx.eval_lut(&gin83A, and2);
    Ciphertext gin83B = 2 * ct_my_string62 + (1 - gout73);
    Ciphertext gout83B = ctx.eval_lut(&gin83B, and2);
    Ciphertext gout83 = gout83A + gout83B;
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * gout55 + gout71;
    Ciphertext gout84 = ctx.eval_lut(&gin84, nand2);
    std::cerr << "\rIMUX85  ";
    Ciphertext gin85A = 2 * ct_my_string1 + gout73;
    Ciphertext gout85A = ctx.eval_lut(&gin85A, and2);
    Ciphertext gin85B = 2 * ct_my_string57 + (1 - gout73);
    Ciphertext gout85B = ctx.eval_lut(&gin85B, and2);
    Ciphertext gout85 = gout85A + gout85B;
    std::cerr << "\rIMUX86  ";
    Ciphertext gin86A = 2 * ct_my_string5 + gout73;
    Ciphertext gout86A = ctx.eval_lut(&gin86A, and2);
    Ciphertext gin86B = 2 * ct_my_string61 + (1 - gout73);
    Ciphertext gout86B = ctx.eval_lut(&gin86B, and2);
    Ciphertext gout86 = gout86A + gout86B;
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * gout75 + gout56;
    Ciphertext gout87 = ctx.eval_lut(&gin87, and2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout61 + gout79;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * gout61 + gout77;
    Ciphertext gout89 = ctx.eval_lut(&gin89, nand2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout61 + gout83;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout61 + gout81;
    Ciphertext gout91 = ctx.eval_lut(&gin91, nand2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout61 + gout82;
    Ciphertext gout92 = ctx.eval_lut(&gin92, nand2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout61 + gout80;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout54 + gout84;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout61 + gout86;
    Ciphertext gout95 = ctx.eval_lut(&gin95, nand2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout61 + gout85;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rINV97   ";
    Ciphertext gout97 = 1 - gout87;

    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout49 + gout94;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout48 + gout98;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout87 + (1 - gout98);
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout97 + (1 - gout99);
    Ciphertext gout101 = ctx.eval_lut(&gin101, and2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout87 + gout99;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout100 + gout67;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout97 + gout99;
    Ciphertext gout104 = ctx.eval_lut(&gin104, and2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * gout69 + gout100;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * gout69 + gout101;
    Ciphertext gout106 = ctx.eval_lut(&gin106, and2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * ct_my_string41 + gout103;
    Ciphertext gout107 = ctx.eval_lut(&gin107, nand2);
    std::cerr << "\rIMUX108  ";
    Ciphertext gin108A = 2 * ct_my_string4 + gout103;
    Ciphertext gout108A = ctx.eval_lut(&gin108A, and2);
    Ciphertext gin108B = 2 * ct_my_string44 + (1 - gout103);
    Ciphertext gout108B = ctx.eval_lut(&gin108B, and2);
    Ciphertext gout108 = gout108A + gout108B;
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * ct_my_string44 + gout103;
    Ciphertext gout109 = ctx.eval_lut(&gin109, nand2);
    std::cerr << "\rIMUX110  ";
    Ciphertext gin110A = 2 * ct_my_string0 + gout103;
    Ciphertext gout110A = ctx.eval_lut(&gin110A, and2);
    Ciphertext gin110B = 2 * ct_my_string40 + (1 - gout103);
    Ciphertext gout110B = ctx.eval_lut(&gin110B, and2);
    Ciphertext gout110 = gout110A + gout110B;
    std::cerr << "\rIMUX111  ";
    Ciphertext gin111A = 2 * ct_my_string1 + gout103;
    Ciphertext gout111A = ctx.eval_lut(&gin111A, and2);
    Ciphertext gin111B = 2 * ct_my_string41 + (1 - gout103);
    Ciphertext gout111B = ctx.eval_lut(&gin111B, and2);
    Ciphertext gout111 = gout111A + gout111B;
    std::cerr << "\rIMUX112  ";
    Ciphertext gin112A = 2 * ct_my_string2 + gout103;
    Ciphertext gout112A = ctx.eval_lut(&gin112A, and2);
    Ciphertext gin112B = 2 * ct_my_string42 + (1 - gout103);
    Ciphertext gout112B = ctx.eval_lut(&gin112B, and2);
    Ciphertext gout112 = gout112A + gout112B;
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * gout63 + (1 - gout102);
    Ciphertext gout113 = ctx.eval_lut(&gin113, and2);
    std::cerr << "\rIMUX114  ";
    Ciphertext gin114A = 2 * ct_my_string6 + gout103;
    Ciphertext gout114A = ctx.eval_lut(&gin114A, and2);
    Ciphertext gin114B = 2 * ct_my_string46 + (1 - gout103);
    Ciphertext gout114B = ctx.eval_lut(&gin114B, and2);
    Ciphertext gout114 = gout114A + gout114B;
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * ct_my_string43 + gout103;
    Ciphertext gout115 = ctx.eval_lut(&gin115, nand2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * ct_my_string40 + gout103;
    Ciphertext gout116 = ctx.eval_lut(&gin116, nand2);
    std::cerr << "\rIMUX117  ";
    Ciphertext gin117A = 2 * ct_my_string7 + gout103;
    Ciphertext gout117A = ctx.eval_lut(&gin117A, and2);
    Ciphertext gin117B = 2 * ct_my_string47 + (1 - gout103);
    Ciphertext gout117B = ctx.eval_lut(&gin117B, and2);
    Ciphertext gout117 = gout117A + gout117B;
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * gout76 + gout102;
    Ciphertext gout118 = ctx.eval_lut(&gin118, and2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * gout69 + gout102;
    Ciphertext gout119 = ctx.eval_lut(&gin119, and2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * gout104 + gout69;
    Ciphertext gout120 = ctx.eval_lut(&gin120, and2);
    std::cerr << "\rIMUX121  ";
    Ciphertext gin121A = 2 * ct_my_string3 + gout103;
    Ciphertext gout121A = ctx.eval_lut(&gin121A, and2);
    Ciphertext gin121B = 2 * ct_my_string43 + (1 - gout103);
    Ciphertext gout121B = ctx.eval_lut(&gin121B, and2);
    Ciphertext gout121 = gout121A + gout121B;
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * ct_my_string46 + gout103;
    Ciphertext gout122 = ctx.eval_lut(&gin122, nand2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * gout104 + gout76;
    Ciphertext gout123 = ctx.eval_lut(&gin123, and2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * ct_my_string14 + gout105;
    Ciphertext gout124 = ctx.eval_lut(&gin124, nand2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * ct_my_string47 + gout103;
    Ciphertext gout125 = ctx.eval_lut(&gin125, nand2);
    std::cerr << "\rIMUX126  ";
    Ciphertext gin126A = 2 * ct_my_string0 + gout105;
    Ciphertext gout126A = ctx.eval_lut(&gin126A, and2);
    Ciphertext gin126B = 2 * ct_my_string8 + (1 - gout105);
    Ciphertext gout126B = ctx.eval_lut(&gin126B, and2);
    Ciphertext gout126 = gout126A + gout126B;
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * ct_my_string12 + gout105;
    Ciphertext gout127 = ctx.eval_lut(&gin127, nand2);
    std::cerr << "\rIMUX128  ";
    Ciphertext gin128A = 2 * ct_my_string5 + gout105;
    Ciphertext gout128A = ctx.eval_lut(&gin128A, and2);
    Ciphertext gin128B = 2 * ct_my_string13 + (1 - gout105);
    Ciphertext gout128B = ctx.eval_lut(&gin128B, and2);
    Ciphertext gout128 = gout128A + gout128B;
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * ct_my_string9 + gout105;
    Ciphertext gout129 = ctx.eval_lut(&gin129, nand2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * ct_my_string11 + gout105;
    Ciphertext gout130 = ctx.eval_lut(&gin130, nand2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * gout101 + gout72;
    Ciphertext gout131 = ctx.eval_lut(&gin131, and2);
    std::cerr << "\rIMUX132  ";
    Ciphertext gin132A = 2 * ct_my_string6 + gout105;
    Ciphertext gout132A = ctx.eval_lut(&gin132A, and2);
    Ciphertext gin132B = 2 * ct_my_string14 + (1 - gout105);
    Ciphertext gout132B = ctx.eval_lut(&gin132B, and2);
    Ciphertext gout132 = gout132A + gout132B;
    std::cerr << "\rIMUX133  ";
    Ciphertext gin133A = 2 * ct_my_string3 + gout105;
    Ciphertext gout133A = ctx.eval_lut(&gin133A, and2);
    Ciphertext gin133B = 2 * ct_my_string11 + (1 - gout105);
    Ciphertext gout133B = ctx.eval_lut(&gin133B, and2);
    Ciphertext gout133 = gout133A + gout133B;
    std::cerr << "\rIMUX134  ";
    Ciphertext gin134A = 2 * ct_my_string5 + gout103;
    Ciphertext gout134A = ctx.eval_lut(&gin134A, and2);
    Ciphertext gin134B = 2 * ct_my_string45 + (1 - gout103);
    Ciphertext gout134B = ctx.eval_lut(&gin134B, and2);
    Ciphertext gout134 = gout134A + gout134B;
    std::cerr << "\rIMUX135  ";
    Ciphertext gin135A = 2 * ct_my_string4 + gout105;
    Ciphertext gout135A = ctx.eval_lut(&gin135A, and2);
    Ciphertext gin135B = 2 * ct_my_string12 + (1 - gout105);
    Ciphertext gout135B = ctx.eval_lut(&gin135B, and2);
    Ciphertext gout135 = gout135A + gout135B;
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * ct_my_string13 + gout105;
    Ciphertext gout136 = ctx.eval_lut(&gin136, nand2);
    std::cerr << "\rIMUX137  ";
    Ciphertext gin137A = 2 * ct_my_string2 + gout105;
    Ciphertext gout137A = ctx.eval_lut(&gin137A, and2);
    Ciphertext gin137B = 2 * ct_my_string10 + (1 - gout105);
    Ciphertext gout137B = ctx.eval_lut(&gin137B, and2);
    Ciphertext gout137 = gout137A + gout137B;
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * ct_my_string10 + gout105;
    Ciphertext gout138 = ctx.eval_lut(&gin138, nand2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_my_string8 + gout105;
    Ciphertext gout139 = ctx.eval_lut(&gin139, nand2);
    std::cerr << "\rIMUX140  ";
    Ciphertext gin140A = 2 * ct_my_string1 + gout105;
    Ciphertext gout140A = ctx.eval_lut(&gin140A, and2);
    Ciphertext gin140B = 2 * ct_my_string9 + (1 - gout105);
    Ciphertext gout140B = ctx.eval_lut(&gin140B, and2);
    Ciphertext gout140 = gout140A + gout140B;
    std::cerr << "\rIMUX141  ";
    Ciphertext gin141A = 2 * ct_my_string7 + gout105;
    Ciphertext gout141A = ctx.eval_lut(&gin141A, and2);
    Ciphertext gin141B = 2 * ct_my_string15 + (1 - gout105);
    Ciphertext gout141B = ctx.eval_lut(&gin141B, and2);
    Ciphertext gout141 = gout141A + gout141B;
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_my_string42 + gout103;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * ct_my_string45 + gout103;
    Ciphertext gout143 = ctx.eval_lut(&gin143, nand2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * ct_my_string15 + gout105;
    Ciphertext gout144 = ctx.eval_lut(&gin144, nand2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * ct_my_string57 + gout106;
    Ciphertext gout145 = ctx.eval_lut(&gin145, nand2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * ct_my_string61 + gout106;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_my_string60 + gout106;
    Ciphertext gout147 = ctx.eval_lut(&gin147, nand2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * ct_my_string62 + gout106;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * gout86 + gout113;
    Ciphertext gout149 = ctx.eval_lut(&gin149, nand2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * gout79 + gout113;
    Ciphertext gout150 = ctx.eval_lut(&gin150, nand2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_my_string53 + gout118;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * gout118 + gout117;
    Ciphertext gout152 = ctx.eval_lut(&gin152, nand2);
    std::cerr << "\rIMUX153  ";
    Ciphertext gin153A = 2 * ct_my_string6 + gout119;
    Ciphertext gout153A = ctx.eval_lut(&gin153A, and2);
    Ciphertext gin153B = 2 * ct_my_string22 + (1 - gout119);
    Ciphertext gout153B = ctx.eval_lut(&gin153B, and2);
    Ciphertext gout153 = gout153A + gout153B;
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * gout118 + gout110;
    Ciphertext gout154 = ctx.eval_lut(&gin154, nand2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * gout118 + gout108;
    Ciphertext gout155 = ctx.eval_lut(&gin155, nand2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * gout85 + gout113;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * gout81 + gout113;
    Ciphertext gout157 = ctx.eval_lut(&gin157, nand2);
    std::cerr << "\rIMUX158  ";
    Ciphertext gin158A = 2 * ct_my_string3 + gout119;
    Ciphertext gout158A = ctx.eval_lut(&gin158A, and2);
    Ciphertext gin158B = 2 * ct_my_string19 + (1 - gout119);
    Ciphertext gout158B = ctx.eval_lut(&gin158B, and2);
    Ciphertext gout158 = gout158A + gout158B;
    std::cerr << "\rIMUX159  ";
    Ciphertext gin159A = 2 * ct_my_string0 + gout119;
    Ciphertext gout159A = ctx.eval_lut(&gin159A, and2);
    Ciphertext gin159B = 2 * ct_my_string16 + (1 - gout119);
    Ciphertext gout159B = ctx.eval_lut(&gin159B, and2);
    Ciphertext gout159 = gout159A + gout159B;
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * gout118 + gout114;
    Ciphertext gout160 = ctx.eval_lut(&gin160, nand2);
    std::cerr << "\rIMUX161  ";
    Ciphertext gin161A = 2 * ct_my_string1 + gout118;
    Ciphertext gout161A = ctx.eval_lut(&gin161A, and2);
    Ciphertext gin161B = 2 * ct_my_string49 + (1 - gout118);
    Ciphertext gout161B = ctx.eval_lut(&gin161B, and2);
    Ciphertext gout161 = gout161A + gout161B;
    std::cerr << "\rIMUX162  ";
    Ciphertext gin162A = 2 * ct_my_string4 + gout119;
    Ciphertext gout162A = ctx.eval_lut(&gin162A, and2);
    Ciphertext gin162B = 2 * ct_my_string20 + (1 - gout119);
    Ciphertext gout162B = ctx.eval_lut(&gin162B, and2);
    Ciphertext gout162 = gout162A + gout162B;
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * gout77 + gout113;
    Ciphertext gout163 = ctx.eval_lut(&gin163, nand2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_my_string48 + gout118;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rIMUX165  ";
    Ciphertext gin165A = 2 * ct_my_string1 + gout119;
    Ciphertext gout165A = ctx.eval_lut(&gin165A, and2);
    Ciphertext gin165B = 2 * ct_my_string17 + (1 - gout119);
    Ciphertext gout165B = ctx.eval_lut(&gin165B, and2);
    Ciphertext gout165 = gout165A + gout165B;
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * ct_my_string54 + gout118;
    Ciphertext gout166 = ctx.eval_lut(&gin166, nand2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_my_string2 + gout120;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * ct_my_string0 + gout120;
    Ciphertext gout168 = ctx.eval_lut(&gin168, nand2);
    std::cerr << "\rIMUX169  ";
    Ciphertext gin169A = 2 * ct_my_string5 + gout119;
    Ciphertext gout169A = ctx.eval_lut(&gin169A, and2);
    Ciphertext gin169B = 2 * ct_my_string21 + (1 - gout119);
    Ciphertext gout169B = ctx.eval_lut(&gin169B, and2);
    Ciphertext gout169 = gout169A + gout169B;
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * ct_my_string50 + gout118;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * ct_my_string7 + gout120;
    Ciphertext gout171 = ctx.eval_lut(&gin171, nand2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * ct_my_string5 + gout120;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rIMUX173  ";
    Ciphertext gin173A = 2 * ct_my_string2 + gout123;
    Ciphertext gout173A = ctx.eval_lut(&gin173A, and2);
    Ciphertext gin173B = 2 * ct_my_string34 + (1 - gout123);
    Ciphertext gout173B = ctx.eval_lut(&gin173B, and2);
    Ciphertext gout173 = gout173A + gout173B;
    std::cerr << "\rIMUX174  ";
    Ciphertext gin174A = 2 * ct_my_string4 + gout123;
    Ciphertext gout174A = ctx.eval_lut(&gin174A, and2);
    Ciphertext gin174B = 2 * ct_my_string36 + (1 - gout123);
    Ciphertext gout174B = ctx.eval_lut(&gin174B, and2);
    Ciphertext gout174 = gout174A + gout174B;
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * gout118 + gout111;
    Ciphertext gout175 = ctx.eval_lut(&gin175, nand2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * ct_my_string38 + gout123;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * ct_my_string1 + gout120;
    Ciphertext gout177 = ctx.eval_lut(&gin177, nand2);
    std::cerr << "\rIMUX178  ";
    Ciphertext gin178A = 2 * ct_my_string4 + gout118;
    Ciphertext gout178A = ctx.eval_lut(&gin178A, and2);
    Ciphertext gin178B = 2 * ct_my_string52 + (1 - gout118);
    Ciphertext gout178B = ctx.eval_lut(&gin178B, and2);
    Ciphertext gout178 = gout178A + gout178B;
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * ct_my_string49 + gout118;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * ct_my_string19 + gout119;
    Ciphertext gout180 = ctx.eval_lut(&gin180, nand2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * ct_my_string16 + gout119;
    Ciphertext gout181 = ctx.eval_lut(&gin181, nand2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * ct_my_string56 + gout106;
    Ciphertext gout182 = ctx.eval_lut(&gin182, nand2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * gout131 + gout165;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * gout127 + gout89;
    Ciphertext gout184 = ctx.eval_lut(&gin184, and2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * gout109 + gout127;
    Ciphertext gout185 = ctx.eval_lut(&gin185, and2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * ct_my_string52 + gout118;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * ct_my_string22 + gout119;
    Ciphertext gout187 = ctx.eval_lut(&gin187, nand2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout107 + gout129;
    Ciphertext gout188 = ctx.eval_lut(&gin188, and2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * ct_my_string4 + gout120;
    Ciphertext gout189 = ctx.eval_lut(&gin189, nand2);
    std::cerr << "\rIMUX190  ";
    Ciphertext gin190A = 2 * ct_my_string6 + gout118;
    Ciphertext gout190A = ctx.eval_lut(&gin190A, and2);
    Ciphertext gin190B = 2 * ct_my_string54 + (1 - gout118);
    Ciphertext gout190B = ctx.eval_lut(&gin190B, and2);
    Ciphertext gout190 = gout190A + gout190B;
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * gout131 + gout162;
    Ciphertext gout191 = ctx.eval_lut(&gin191, nand2);
    std::cerr << "\rIMUX192  ";
    Ciphertext gin192A = 2 * ct_my_string2 + gout118;
    Ciphertext gout192A = ctx.eval_lut(&gin192A, and2);
    Ciphertext gin192B = 2 * ct_my_string50 + (1 - gout118);
    Ciphertext gout192B = ctx.eval_lut(&gin192B, and2);
    Ciphertext gout192 = gout192A + gout192B;
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * gout115 + gout130;
    Ciphertext gout193 = ctx.eval_lut(&gin193, and2);
    std::cerr << "\rIMUX194  ";
    Ciphertext gin194A = 2 * ct_my_string1 + gout131;
    Ciphertext gout194A = ctx.eval_lut(&gin194A, and2);
    Ciphertext gin194B = 2 * ct_my_string25 + (1 - gout131);
    Ciphertext gout194B = ctx.eval_lut(&gin194B, and2);
    Ciphertext gout194 = gout194A + gout194B;
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * ct_my_string28 + gout131;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * ct_my_string20 + gout119;
    Ciphertext gout196 = ctx.eval_lut(&gin196, nand2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * ct_my_string35 + gout123;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * ct_my_string32 + gout123;
    Ciphertext gout198 = ctx.eval_lut(&gin198, nand2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * gout131 + gout169;
    Ciphertext gout199 = ctx.eval_lut(&gin199, nand2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * gout129 + gout96;
    Ciphertext gout200 = ctx.eval_lut(&gin200, and2);
    std::cerr << "\rIMUX201  ";
    Ciphertext gin201A = 2 * ct_my_string6 + gout131;
    Ciphertext gout201A = ctx.eval_lut(&gin201A, and2);
    Ciphertext gin201B = 2 * ct_my_string30 + (1 - gout131);
    Ciphertext gout201B = ctx.eval_lut(&gin201B, and2);
    Ciphertext gout201 = gout201A + gout201B;
    std::cerr << "\rIMUX202  ";
    Ciphertext gin202A = 2 * ct_my_string3 + gout118;
    Ciphertext gout202A = ctx.eval_lut(&gin202A, and2);
    Ciphertext gin202B = 2 * ct_my_string51 + (1 - gout118);
    Ciphertext gout202B = ctx.eval_lut(&gin202B, and2);
    Ciphertext gout202 = gout202A + gout202B;
    std::cerr << "\rIMUX203  ";
    Ciphertext gin203A = 2 * ct_my_string7 + gout123;
    Ciphertext gout203A = ctx.eval_lut(&gin203A, and2);
    Ciphertext gin203B = 2 * ct_my_string39 + (1 - gout123);
    Ciphertext gout203B = ctx.eval_lut(&gin203B, and2);
    Ciphertext gout203 = gout203A + gout203B;
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * ct_my_string6 + gout120;
    Ciphertext gout204 = ctx.eval_lut(&gin204, nand2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * ct_my_string18 + gout119;
    Ciphertext gout205 = ctx.eval_lut(&gin205, nand2);
    std::cerr << "\rIMUX206  ";
    Ciphertext gin206A = 2 * ct_my_string6 + gout123;
    Ciphertext gout206A = ctx.eval_lut(&gin206A, and2);
    Ciphertext gin206B = 2 * ct_my_string38 + (1 - gout123);
    Ciphertext gout206B = ctx.eval_lut(&gin206B, and2);
    Ciphertext gout206 = gout206A + gout206B;
    std::cerr << "\rIMUX207  ";
    Ciphertext gin207A = 2 * ct_my_string2 + gout119;
    Ciphertext gout207A = ctx.eval_lut(&gin207A, and2);
    Ciphertext gin207B = 2 * ct_my_string18 + (1 - gout119);
    Ciphertext gout207B = ctx.eval_lut(&gin207B, and2);
    Ciphertext gout207 = gout207A + gout207B;
    std::cerr << "\rIMUX208  ";
    Ciphertext gin208A = 2 * gout132 + gout118;
    Ciphertext gout208A = ctx.eval_lut(&gin208A, and2);
    Ciphertext gin208B = 2 * gout114 + (1 - gout118);
    Ciphertext gout208B = ctx.eval_lut(&gin208B, and2);
    Ciphertext gout208 = gout208A + gout208B;
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * ct_my_string58 + gout106;
    Ciphertext gout209 = ctx.eval_lut(&gin209, nand2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout82 + gout113;
    Ciphertext gout210 = ctx.eval_lut(&gin210, nand2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * ct_my_string30 + gout131;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rIMUX212  ";
    Ciphertext gin212A = 2 * gout126 + gout131;
    Ciphertext gout212A = ctx.eval_lut(&gin212A, and2);
    Ciphertext gin212B = 2 * gout159 + (1 - gout131);
    Ciphertext gout212B = ctx.eval_lut(&gin212B, and2);
    Ciphertext gout212 = gout212A + gout212B;
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * ct_my_string36 + gout123;
    Ciphertext gout213 = ctx.eval_lut(&gin213, nand2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout118 + gout134;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * ct_my_string59 + gout106;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout118 + gout112;
    Ciphertext gout216 = ctx.eval_lut(&gin216, nand2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * ct_my_string37 + gout123;
    Ciphertext gout217 = ctx.eval_lut(&gin217, nand2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * ct_my_string39 + gout123;
    Ciphertext gout218 = ctx.eval_lut(&gin218, nand2);
    std::cerr << "\rIMUX219  ";
    Ciphertext gin219A = 2 * ct_my_string2 + gout131;
    Ciphertext gout219A = ctx.eval_lut(&gin219A, and2);
    Ciphertext gin219B = 2 * ct_my_string26 + (1 - gout131);
    Ciphertext gout219B = ctx.eval_lut(&gin219B, and2);
    Ciphertext gout219 = gout219A + gout219B;
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout124 + gout90;
    Ciphertext gout220 = ctx.eval_lut(&gin220, and2);
    std::cerr << "\rIMUX221  ";
    Ciphertext gin221A = 2 * ct_my_string0 + gout123;
    Ciphertext gout221A = ctx.eval_lut(&gin221A, and2);
    Ciphertext gin221B = 2 * ct_my_string32 + (1 - gout123);
    Ciphertext gout221B = ctx.eval_lut(&gin221B, and2);
    Ciphertext gout221 = gout221A + gout221B;
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * ct_my_string17 + gout119;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * ct_my_string23 + gout119;
    Ciphertext gout223 = ctx.eval_lut(&gin223, nand2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * ct_my_string29 + gout131;
    Ciphertext gout224 = ctx.eval_lut(&gin224, nand2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout118 + gout121;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rIMUX226  ";
    Ciphertext gin226A = 2 * ct_my_string5 + gout123;
    Ciphertext gout226A = ctx.eval_lut(&gin226A, and2);
    Ciphertext gin226B = 2 * ct_my_string37 + (1 - gout123);
    Ciphertext gout226B = ctx.eval_lut(&gin226B, and2);
    Ciphertext gout226 = gout226A + gout226B;
    std::cerr << "\rIMUX227  ";
    Ciphertext gin227A = 2 * gout135 + gout73;
    Ciphertext gout227A = ctx.eval_lut(&gin227A, and2);
    Ciphertext gin227B = 2 * gout178 + (1 - gout73);
    Ciphertext gout227B = ctx.eval_lut(&gin227B, and2);
    Ciphertext gout227 = gout227A + gout227B;
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * ct_my_string33 + gout123;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout130 + gout92;
    Ciphertext gout229 = ctx.eval_lut(&gin229, and2);
    std::cerr << "\rIMUX230  ";
    Ciphertext gin230A = 2 * ct_my_string0 + gout118;
    Ciphertext gout230A = ctx.eval_lut(&gin230A, and2);
    Ciphertext gin230B = 2 * ct_my_string48 + (1 - gout118);
    Ciphertext gout230B = ctx.eval_lut(&gin230B, and2);
    Ciphertext gout230 = gout230A + gout230B;
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * ct_my_string31 + gout131;
    Ciphertext gout231 = ctx.eval_lut(&gin231, nand2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout122 + gout124;
    Ciphertext gout232 = ctx.eval_lut(&gin232, and2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * ct_my_string3 + gout120;
    Ciphertext gout233 = ctx.eval_lut(&gin233, nand2);
    std::cerr << "\rIMUX234  ";
    Ciphertext gin234A = 2 * ct_my_string1 + gout123;
    Ciphertext gout234A = ctx.eval_lut(&gin234A, and2);
    Ciphertext gin234B = 2 * ct_my_string33 + (1 - gout123);
    Ciphertext gout234B = ctx.eval_lut(&gin234B, and2);
    Ciphertext gout234 = gout234A + gout234B;
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * ct_my_string55 + gout118;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * ct_my_string21 + gout119;
    Ciphertext gout236 = ctx.eval_lut(&gin236, nand2);
    std::cerr << "\rIMUX237  ";
    Ciphertext gin237A = 2 * gout135 + gout131;
    Ciphertext gout237A = ctx.eval_lut(&gin237A, and2);
    Ciphertext gin237B = 2 * gout162 + (1 - gout131);
    Ciphertext gout237B = ctx.eval_lut(&gin237B, and2);
    Ciphertext gout237 = gout237A + gout237B;
    std::cerr << "\rIMUX238  ";
    Ciphertext gin238A = 2 * ct_my_string7 + gout118;
    Ciphertext gout238A = ctx.eval_lut(&gin238A, and2);
    Ciphertext gin238B = 2 * ct_my_string55 + (1 - gout118);
    Ciphertext gout238B = ctx.eval_lut(&gin238B, and2);
    Ciphertext gout238 = gout238A + gout238B;
    std::cerr << "\rIMUX239  ";
    Ciphertext gin239A = 2 * gout133 + gout131;
    Ciphertext gout239A = ctx.eval_lut(&gin239A, and2);
    Ciphertext gin239B = 2 * gout158 + (1 - gout131);
    Ciphertext gout239B = ctx.eval_lut(&gin239B, and2);
    Ciphertext gout239 = gout239A + gout239B;
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * ct_my_string51 + gout118;
    Ciphertext gout240 = ctx.eval_lut(&gin240, nand2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout83 + gout113;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * ct_my_string24 + gout131;
    Ciphertext gout242 = ctx.eval_lut(&gin242, nand2);
    std::cerr << "\rLUT243   ";
    Ciphertext gin243 = 2 * gout131 + gout153;
    Ciphertext gout243 = ctx.eval_lut(&gin243, nand2);
    std::cerr << "\rIMUX244  ";
    Ciphertext gin244A = 2 * gout132 + gout131;
    Ciphertext gout244A = ctx.eval_lut(&gin244A, and2);
    Ciphertext gin244B = 2 * gout153 + (1 - gout131);
    Ciphertext gout244B = ctx.eval_lut(&gin244B, and2);
    Ciphertext gout244 = gout244A + gout244B;
    std::cerr << "\rIMUX245  ";
    Ciphertext gin245A = 2 * gout141 + gout118;
    Ciphertext gout245A = ctx.eval_lut(&gin245A, and2);
    Ciphertext gin245B = 2 * gout117 + (1 - gout118);
    Ciphertext gout245B = ctx.eval_lut(&gin245B, and2);
    Ciphertext gout245 = gout245A + gout245B;
    std::cerr << "\rIMUX246  ";
    Ciphertext gin246A = 2 * ct_my_string3 + gout123;
    Ciphertext gout246A = ctx.eval_lut(&gin246A, and2);
    Ciphertext gin246B = 2 * ct_my_string35 + (1 - gout123);
    Ciphertext gout246B = ctx.eval_lut(&gin246B, and2);
    Ciphertext gout246 = gout246A + gout246B;
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * ct_my_string25 + gout131;
    Ciphertext gout247 = ctx.eval_lut(&gin247, nand2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout131 + gout158;
    Ciphertext gout248 = ctx.eval_lut(&gin248, nand2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout131 + gout159;
    Ciphertext gout249 = ctx.eval_lut(&gin249, nand2);
    std::cerr << "\rIMUX250  ";
    Ciphertext gin250A = 2 * gout126 + gout118;
    Ciphertext gout250A = ctx.eval_lut(&gin250A, and2);
    Ciphertext gin250B = 2 * gout110 + (1 - gout118);
    Ciphertext gout250B = ctx.eval_lut(&gin250B, and2);
    Ciphertext gout250 = gout250A + gout250B;
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * ct_my_string34 + gout123;
    Ciphertext gout251 = ctx.eval_lut(&gin251, nand2);
    std::cerr << "\rIMUX252  ";
    Ciphertext gin252A = 2 * ct_my_string5 + gout118;
    Ciphertext gout252A = ctx.eval_lut(&gin252A, and2);
    Ciphertext gin252B = 2 * ct_my_string53 + (1 - gout118);
    Ciphertext gout252B = ctx.eval_lut(&gin252B, and2);
    Ciphertext gout252 = gout252A + gout252B;
    std::cerr << "\rIMUX253  ";
    Ciphertext gin253A = 2 * ct_my_string7 + gout131;
    Ciphertext gout253A = ctx.eval_lut(&gin253A, and2);
    Ciphertext gin253B = 2 * ct_my_string31 + (1 - gout131);
    Ciphertext gout253B = ctx.eval_lut(&gin253B, and2);
    Ciphertext gout253 = gout253A + gout253B;
    std::cerr << "\rIMUX254  ";
    Ciphertext gin254A = 2 * gout135 + gout118;
    Ciphertext gout254A = ctx.eval_lut(&gin254A, and2);
    Ciphertext gin254B = 2 * gout108 + (1 - gout118);
    Ciphertext gout254B = ctx.eval_lut(&gin254B, and2);
    Ciphertext gout254 = gout254A + gout254B;
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout80 + gout113;
    Ciphertext gout255 = ctx.eval_lut(&gin255, nand2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout143 + gout136;
    Ciphertext gout256 = ctx.eval_lut(&gin256, and2);
    std::cerr << "\rIMUX257  ";
    Ciphertext gin257A = 2 * gout140 + gout118;
    Ciphertext gout257A = ctx.eval_lut(&gin257A, and2);
    Ciphertext gin257B = 2 * gout111 + (1 - gout118);
    Ciphertext gout257B = ctx.eval_lut(&gin257B, and2);
    Ciphertext gout257 = gout257A + gout257B;
    std::cerr << "\rIMUX258  ";
    Ciphertext gin258A = 2 * ct_my_string3 + gout131;
    Ciphertext gout258A = ctx.eval_lut(&gin258A, and2);
    Ciphertext gin258B = 2 * ct_my_string27 + (1 - gout131);
    Ciphertext gout258B = ctx.eval_lut(&gin258B, and2);
    Ciphertext gout258 = gout258A + gout258B;
    std::cerr << "\rIMUX259  ";
    Ciphertext gin259A = 2 * ct_my_string7 + gout119;
    Ciphertext gout259A = ctx.eval_lut(&gin259A, and2);
    Ciphertext gin259B = 2 * ct_my_string23 + (1 - gout119);
    Ciphertext gout259B = ctx.eval_lut(&gin259B, and2);
    Ciphertext gout259 = gout259A + gout259B;
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * ct_my_string63 + gout106;
    Ciphertext gout260 = ctx.eval_lut(&gin260, nand2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * ct_my_string27 + gout131;
    Ciphertext gout261 = ctx.eval_lut(&gin261, nand2);
    std::cerr << "\rIMUX262  ";
    Ciphertext gin262A = 2 * ct_my_string4 + gout131;
    Ciphertext gout262A = ctx.eval_lut(&gin262A, and2);
    Ciphertext gin262B = 2 * ct_my_string28 + (1 - gout131);
    Ciphertext gout262B = ctx.eval_lut(&gin262B, and2);
    Ciphertext gout262 = gout262A + gout262B;
    std::cerr << "\rIMUX263  ";
    Ciphertext gin263A = 2 * gout137 + gout131;
    Ciphertext gout263A = ctx.eval_lut(&gin263A, and2);
    Ciphertext gin263B = 2 * gout207 + (1 - gout131);
    Ciphertext gout263B = ctx.eval_lut(&gin263B, and2);
    Ciphertext gout263 = gout263A + gout263B;
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout144 + gout91;
    Ciphertext gout264 = ctx.eval_lut(&gin264, and2);
    std::cerr << "\rIMUX265  ";
    Ciphertext gin265A = 2 * ct_my_string0 + gout131;
    Ciphertext gout265A = ctx.eval_lut(&gin265A, and2);
    Ciphertext gin265B = 2 * ct_my_string24 + (1 - gout131);
    Ciphertext gout265B = ctx.eval_lut(&gin265B, and2);
    Ciphertext gout265 = gout265A + gout265B;
    std::cerr << "\rIMUX266  ";
    Ciphertext gin266A = 2 * gout128 + gout131;
    Ciphertext gout266A = ctx.eval_lut(&gin266A, and2);
    Ciphertext gin266B = 2 * gout169 + (1 - gout131);
    Ciphertext gout266B = ctx.eval_lut(&gin266B, and2);
    Ciphertext gout266 = gout266A + gout266B;
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * gout136 + gout95;
    Ciphertext gout267 = ctx.eval_lut(&gin267, and2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout125 + gout144;
    Ciphertext gout268 = ctx.eval_lut(&gin268, and2);
    std::cerr << "\rIMUX269  ";
    Ciphertext gin269A = 2 * ct_my_string5 + gout131;
    Ciphertext gout269A = ctx.eval_lut(&gin269A, and2);
    Ciphertext gin269B = 2 * ct_my_string29 + (1 - gout131);
    Ciphertext gout269B = ctx.eval_lut(&gin269B, and2);
    Ciphertext gout269 = gout269A + gout269B;
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout116 + gout139;
    Ciphertext gout270 = ctx.eval_lut(&gin270, and2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * ct_my_string26 + gout131;
    Ciphertext gout271 = ctx.eval_lut(&gin271, nand2);
    std::cerr << "\rIMUX272  ";
    Ciphertext gin272A = 2 * gout137 + gout73;
    Ciphertext gout272A = ctx.eval_lut(&gin272A, and2);
    Ciphertext gin272B = 2 * gout192 + (1 - gout73);
    Ciphertext gout272B = ctx.eval_lut(&gin272B, and2);
    Ciphertext gout272 = gout272A + gout272B;
    std::cerr << "\rIMUX273  ";
    Ciphertext gin273A = 2 * gout128 + gout118;
    Ciphertext gout273A = ctx.eval_lut(&gin273A, and2);
    Ciphertext gin273B = 2 * gout134 + (1 - gout118);
    Ciphertext gout273B = ctx.eval_lut(&gin273B, and2);
    Ciphertext gout273 = gout273A + gout273B;
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout142 + gout138;
    Ciphertext gout274 = ctx.eval_lut(&gin274, and2);
    std::cerr << "\rIMUX275  ";
    Ciphertext gin275A = 2 * gout133 + gout118;
    Ciphertext gout275A = ctx.eval_lut(&gin275A, and2);
    Ciphertext gin275B = 2 * gout121 + (1 - gout118);
    Ciphertext gout275B = ctx.eval_lut(&gin275B, and2);
    Ciphertext gout275 = gout275A + gout275B;
    std::cerr << "\rIMUX276  ";
    Ciphertext gin276A = 2 * gout140 + gout131;
    Ciphertext gout276A = ctx.eval_lut(&gin276A, and2);
    Ciphertext gin276B = 2 * gout165 + (1 - gout131);
    Ciphertext gout276B = ctx.eval_lut(&gin276B, and2);
    Ciphertext gout276 = gout276A + gout276B;
    std::cerr << "\rIMUX277  ";
    Ciphertext gin277A = 2 * gout137 + gout123;
    Ciphertext gout277A = ctx.eval_lut(&gin277A, and2);
    Ciphertext gin277B = 2 * gout219 + (1 - gout123);
    Ciphertext gout277B = ctx.eval_lut(&gin277B, and2);
    Ciphertext gout277 = gout277A + gout277B;
    std::cerr << "\rIMUX278  ";
    Ciphertext gin278A = 2 * gout141 + gout73;
    Ciphertext gout278A = ctx.eval_lut(&gin278A, and2);
    Ciphertext gin278B = 2 * gout238 + (1 - gout73);
    Ciphertext gout278B = ctx.eval_lut(&gin278B, and2);
    Ciphertext gout278 = gout278A + gout278B;
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout139 + gout93;
    Ciphertext gout279 = ctx.eval_lut(&gin279, and2);
    std::cerr << "\rIMUX280  ";
    Ciphertext gin280A = 2 * gout137 + gout118;
    Ciphertext gout280A = ctx.eval_lut(&gin280A, and2);
    Ciphertext gin280B = 2 * gout112 + (1 - gout118);
    Ciphertext gout280B = ctx.eval_lut(&gin280B, and2);
    Ciphertext gout280 = gout280A + gout280B;
    std::cerr << "\rIMUX281  ";
    Ciphertext gin281A = 2 * gout137 + gout103;
    Ciphertext gout281A = ctx.eval_lut(&gin281A, and2);
    Ciphertext gin281B = 2 * gout173 + (1 - gout103);
    Ciphertext gout281B = ctx.eval_lut(&gin281B, and2);
    Ciphertext gout281 = gout281A + gout281B;
    std::cerr << "\rIMUX282  ";
    Ciphertext gin282A = 2 * gout140 + gout123;
    Ciphertext gout282A = ctx.eval_lut(&gin282A, and2);
    Ciphertext gin282B = 2 * gout194 + (1 - gout123);
    Ciphertext gout282B = ctx.eval_lut(&gin282B, and2);
    Ciphertext gout282 = gout282A + gout282B;
    std::cerr << "\rIMUX283  ";
    Ciphertext gin283A = 2 * gout141 + gout103;
    Ciphertext gout283A = ctx.eval_lut(&gin283A, and2);
    Ciphertext gin283B = 2 * gout203 + (1 - gout103);
    Ciphertext gout283B = ctx.eval_lut(&gin283B, and2);
    Ciphertext gout283 = gout283A + gout283B;
    std::cerr << "\rIMUX284  ";
    Ciphertext gin284A = 2 * gout135 + gout103;
    Ciphertext gout284A = ctx.eval_lut(&gin284A, and2);
    Ciphertext gin284B = 2 * gout174 + (1 - gout103);
    Ciphertext gout284B = ctx.eval_lut(&gin284B, and2);
    Ciphertext gout284 = gout284A + gout284B;
    std::cerr << "\rIMUX285  ";
    Ciphertext gin285A = 2 * gout140 + gout73;
    Ciphertext gout285A = ctx.eval_lut(&gin285A, and2);
    Ciphertext gin285B = 2 * gout161 + (1 - gout73);
    Ciphertext gout285B = ctx.eval_lut(&gin285B, and2);
    Ciphertext gout285 = gout285A + gout285B;
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout138 + gout88;
    Ciphertext gout286 = ctx.eval_lut(&gin286, and2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout188 + gout145;
    Ciphertext gout287 = ctx.eval_lut(&gin287, and2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout205 + gout150;
    Ciphertext gout288 = ctx.eval_lut(&gin288, and2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout123 + gout194;
    Ciphertext gout289 = ctx.eval_lut(&gin289, nand2);
    std::cerr << "\rIMUX290  ";
    Ciphertext gin290A = 2 * gout132 + gout73;
    Ciphertext gout290A = ctx.eval_lut(&gin290A, and2);
    Ciphertext gin290B = 2 * gout190 + (1 - gout73);
    Ciphertext gout290B = ctx.eval_lut(&gin290B, and2);
    Ciphertext gout290 = gout290A + gout290B;
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout180 + gout210;
    Ciphertext gout291 = ctx.eval_lut(&gin291, and2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout164 + gout181;
    Ciphertext gout292 = ctx.eval_lut(&gin292, and2);
    std::cerr << "\rIMUX293  ";
    Ciphertext gin293A = 2 * gout128 + gout103;
    Ciphertext gout293A = ctx.eval_lut(&gin293A, and2);
    Ciphertext gin293B = 2 * gout226 + (1 - gout103);
    Ciphertext gout293B = ctx.eval_lut(&gin293B, and2);
    Ciphertext gout293 = gout293A + gout293B;
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout155 + gout184;
    Ciphertext gout294 = ctx.eval_lut(&gin294, and2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout73 + gout202;
    Ciphertext gout295 = ctx.eval_lut(&gin295, nand2);
    std::cerr << "\rLUT296   ";
    Ciphertext gin296 = 2 * gout73 + gout190;
    Ciphertext gout296 = ctx.eval_lut(&gin296, nand2);
    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout73 + gout192;
    Ciphertext gout297 = ctx.eval_lut(&gin297, nand2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout186 + gout196;
    Ciphertext gout298 = ctx.eval_lut(&gin298, and2);
    std::cerr << "\rIMUX299  ";
    Ciphertext gin299A = 2 * gout141 + gout123;
    Ciphertext gout299A = ctx.eval_lut(&gin299A, and2);
    Ciphertext gin299B = 2 * gout253 + (1 - gout123);
    Ciphertext gout299B = ctx.eval_lut(&gin299B, and2);
    Ciphertext gout299 = gout299A + gout299B;
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout218 + gout223;
    Ciphertext gout300 = ctx.eval_lut(&gin300, and2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout187 + gout232;
    Ciphertext gout301 = ctx.eval_lut(&gin301, and2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout103 + gout173;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rIMUX303  ";
    Ciphertext gin303A = 2 * gout132 + gout123;
    Ciphertext gout303A = ctx.eval_lut(&gin303A, and2);
    Ciphertext gin303B = 2 * gout201 + (1 - gout123);
    Ciphertext gout303B = ctx.eval_lut(&gin303B, and2);
    Ciphertext gout303 = gout303A + gout303B;
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout225 + gout229;
    Ciphertext gout304 = ctx.eval_lut(&gin304, and2);
    std::cerr << "\rIMUX305  ";
    Ciphertext gin305A = 2 * gout133 + gout123;
    Ciphertext gout305A = ctx.eval_lut(&gin305A, and2);
    Ciphertext gin305B = 2 * gout258 + (1 - gout123);
    Ciphertext gout305B = ctx.eval_lut(&gin305B, and2);
    Ciphertext gout305 = gout305A + gout305B;
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout151 + gout236;
    Ciphertext gout306 = ctx.eval_lut(&gin306, and2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout222 + gout228;
    Ciphertext gout307 = ctx.eval_lut(&gin307, and2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout73 + gout254;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout176 + gout148;
    Ciphertext gout309 = ctx.eval_lut(&gin309, and2);
    std::cerr << "\rIMUX310  ";
    Ciphertext gin310A = 2 * gout132 + gout103;
    Ciphertext gout310A = ctx.eval_lut(&gin310A, and2);
    Ciphertext gin310B = 2 * gout206 + (1 - gout103);
    Ciphertext gout310B = ctx.eval_lut(&gin310B, and2);
    Ciphertext gout310 = gout310A + gout310B;
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout187 + gout241;
    Ciphertext gout311 = ctx.eval_lut(&gin311, and2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout73 + gout230;
    Ciphertext gout312 = ctx.eval_lut(&gin312, nand2);
    std::cerr << "\rIMUX313  ";
    Ciphertext gin313A = 2 * gout237 + gout73;
    Ciphertext gout313A = ctx.eval_lut(&gin313A, and2);
    Ciphertext gin313B = 2 * gout254 + (1 - gout73);
    Ciphertext gout313B = ctx.eval_lut(&gin313B, and2);
    Ciphertext gout313 = gout313A + gout313B;
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout179 + gout247;
    Ciphertext gout314 = ctx.eval_lut(&gin314, and2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout196 + gout163;
    Ciphertext gout315 = ctx.eval_lut(&gin315, and2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout123 + gout265;
    Ciphertext gout316 = ctx.eval_lut(&gin316, nand2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout73 + gout238;
    Ciphertext gout317 = ctx.eval_lut(&gin317, nand2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout175 + gout200;
    Ciphertext gout318 = ctx.eval_lut(&gin318, and2);
    std::cerr << "\rIMUX319  ";
    Ciphertext gin319A = 2 * gout126 + gout73;
    Ciphertext gout319A = ctx.eval_lut(&gin319A, and2);
    Ciphertext gin319B = 2 * gout230 + (1 - gout73);
    Ciphertext gout319B = ctx.eval_lut(&gin319B, and2);
    Ciphertext gout319 = gout319A + gout319B;
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout181 + gout255;
    Ciphertext gout320 = ctx.eval_lut(&gin320, and2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout215 + gout193;
    Ciphertext gout321 = ctx.eval_lut(&gin321, and2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout160 + gout220;
    Ciphertext gout322 = ctx.eval_lut(&gin322, and2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout170 + gout205;
    Ciphertext gout323 = ctx.eval_lut(&gin323, and2);
    std::cerr << "\rIMUX324  ";
    Ciphertext gin324A = 2 * gout212 + gout73;
    Ciphertext gout324A = ctx.eval_lut(&gin324A, and2);
    Ciphertext gin324B = 2 * gout250 + (1 - gout73);
    Ciphertext gout324B = ctx.eval_lut(&gin324B, and2);
    Ciphertext gout324 = gout324A + gout324B;
    std::cerr << "\rIMUX325  ";
    Ciphertext gin325A = 2 * gout133 + gout73;
    Ciphertext gout325A = ctx.eval_lut(&gin325A, and2);
    Ciphertext gin325B = 2 * gout202 + (1 - gout73);
    Ciphertext gout325B = ctx.eval_lut(&gin325B, and2);
    Ciphertext gout325 = gout325A + gout325B;
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout123 + gout219;
    Ciphertext gout326 = ctx.eval_lut(&gin326, nand2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout198 + gout242;
    Ciphertext gout327 = ctx.eval_lut(&gin327, and2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout103 + gout174;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout222 + gout156;
    Ciphertext gout329 = ctx.eval_lut(&gin329, and2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout260 + gout235;
    Ciphertext gout330 = ctx.eval_lut(&gin330, and2);
    std::cerr << "\rIMUX331  ";
    Ciphertext gin331A = 2 * gout266 + gout118;
    Ciphertext gout331A = ctx.eval_lut(&gin331A, and2);
    Ciphertext gin331B = 2 * gout293 + (1 - gout118);
    Ciphertext gout331B = ctx.eval_lut(&gin331B, and2);
    Ciphertext gout331 = gout331A + gout331B;
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * gout147 + gout185;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout123 + gout201;
    Ciphertext gout333 = ctx.eval_lut(&gin333, nand2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout223 + gout157;
    Ciphertext gout334 = ctx.eval_lut(&gin334, and2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout73 + gout178;
    Ciphertext gout335 = ctx.eval_lut(&gin335, nand2);
    std::cerr << "\rIMUX336  ";
    Ciphertext gin336A = 2 * gout128 + gout73;
    Ciphertext gout336A = ctx.eval_lut(&gin336A, and2);
    Ciphertext gin336B = 2 * gout252 + (1 - gout73);
    Ciphertext gout336B = ctx.eval_lut(&gin336B, and2);
    Ciphertext gout336 = gout336A + gout336B;
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout214 + gout267;
    Ciphertext gout337 = ctx.eval_lut(&gin337, and2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout211 + gout166;
    Ciphertext gout338 = ctx.eval_lut(&gin338, and2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout213 + gout195;
    Ciphertext gout339 = ctx.eval_lut(&gin339, and2);
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout182 + gout270;
    Ciphertext gout340 = ctx.eval_lut(&gin340, and2);
    std::cerr << "\rIMUX341  ";
    Ciphertext gin341A = 2 * gout140 + gout103;
    Ciphertext gout341A = ctx.eval_lut(&gin341A, and2);
    Ciphertext gin341B = 2 * gout234 + (1 - gout103);
    Ciphertext gout341B = ctx.eval_lut(&gin341B, and2);
    Ciphertext gout341 = gout341A + gout341B;
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout197 + gout261;
    Ciphertext gout342 = ctx.eval_lut(&gin342, and2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout73 + gout273;
    Ciphertext gout343 = ctx.eval_lut(&gin343, nand2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout152 + gout264;
    Ciphertext gout344 = ctx.eval_lut(&gin344, and2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout103 + gout203;
    Ciphertext gout345 = ctx.eval_lut(&gin345, nand2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout73 + gout208;
    Ciphertext gout346 = ctx.eval_lut(&gin346, nand2);
    std::cerr << "\rIMUX347  ";
    Ciphertext gin347A = 2 * gout244 + gout73;
    Ciphertext gout347A = ctx.eval_lut(&gin347A, and2);
    Ciphertext gin347B = 2 * gout208 + (1 - gout73);
    Ciphertext gout347B = ctx.eval_lut(&gin347B, and2);
    Ciphertext gout347 = gout347A + gout347B;
    std::cerr << "\rIMUX348  ";
    Ciphertext gin348A = 2 * gout141 + gout131;
    Ciphertext gout348A = ctx.eval_lut(&gin348A, and2);
    Ciphertext gin348B = 2 * gout259 + (1 - gout131);
    Ciphertext gout348B = ctx.eval_lut(&gin348B, and2);
    Ciphertext gout348 = gout348A + gout348B;
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout73 + gout275;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rIMUX350  ";
    Ciphertext gin350A = 2 * gout126 + gout123;
    Ciphertext gout350A = ctx.eval_lut(&gin350A, and2);
    Ciphertext gin350B = 2 * gout265 + (1 - gout123);
    Ciphertext gout350B = ctx.eval_lut(&gin350B, and2);
    Ciphertext gout350 = gout350A + gout350B;
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout240 + gout180;
    Ciphertext gout351 = ctx.eval_lut(&gin351, and2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout103 + gout206;
    Ciphertext gout352 = ctx.eval_lut(&gin352, nand2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout131 + gout207;
    Ciphertext gout353 = ctx.eval_lut(&gin353, nand2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout248 + gout291;
    Ciphertext gout354 = ctx.eval_lut(&gin354, and2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout103 + gout246;
    Ciphertext gout355 = ctx.eval_lut(&gin355, nand2);
    std::cerr << "\rIMUX356  ";
    Ciphertext gin356A = 2 * gout126 + gout103;
    Ciphertext gout356A = ctx.eval_lut(&gin356A, and2);
    Ciphertext gin356B = 2 * gout221 + (1 - gout103);
    Ciphertext gout356B = ctx.eval_lut(&gin356B, and2);
    Ciphertext gout356 = gout356A + gout356B;
    std::cerr << "\rLUT357   ";
    Ciphertext gin357 = 2 * gout231 + gout268;
    Ciphertext gout357 = ctx.eval_lut(&gin357, and2);
    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout146 + gout256;
    Ciphertext gout358 = ctx.eval_lut(&gin358, and2);
    std::cerr << "\rIMUX359  ";
    Ciphertext gin359A = 2 * gout239 + gout73;
    Ciphertext gout359A = ctx.eval_lut(&gin359A, and2);
    Ciphertext gin359B = 2 * gout275 + (1 - gout73);
    Ciphertext gout359B = ctx.eval_lut(&gin359B, and2);
    Ciphertext gout359 = gout359A + gout359B;
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout251 + gout271;
    Ciphertext gout360 = ctx.eval_lut(&gin360, and2);
    std::cerr << "\rIMUX361  ";
    Ciphertext gin361A = 2 * gout276 + gout73;
    Ciphertext gout361A = ctx.eval_lut(&gin361A, and2);
    Ciphertext gin361B = 2 * gout257 + (1 - gout73);
    Ciphertext gout361B = ctx.eval_lut(&gin361B, and2);
    Ciphertext gout361 = gout361A + gout361B;
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout73 + gout250;
    Ciphertext gout362 = ctx.eval_lut(&gin362, nand2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout217 + gout224;
    Ciphertext gout363 = ctx.eval_lut(&gin363, and2);
    std::cerr << "\rLUT364   ";
    Ciphertext gin364 = 2 * gout103 + gout221;
    Ciphertext gout364 = ctx.eval_lut(&gin364, nand2);
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout209 + gout274;
    Ciphertext gout365 = ctx.eval_lut(&gin365, and2);
    std::cerr << "\rIMUX366  ";
    Ciphertext gin366A = 2 * gout135 + gout123;
    Ciphertext gout366A = ctx.eval_lut(&gin366A, and2);
    Ciphertext gin366B = 2 * gout262 + (1 - gout123);
    Ciphertext gout366B = ctx.eval_lut(&gin366B, and2);
    Ciphertext gout366 = gout366A + gout366B;
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout103 + gout226;
    Ciphertext gout367 = ctx.eval_lut(&gin367, nand2);
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout73 + gout245;
    Ciphertext gout368 = ctx.eval_lut(&gin368, nand2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout154 + gout279;
    Ciphertext gout369 = ctx.eval_lut(&gin369, and2);
    std::cerr << "\rIMUX370  ";
    Ciphertext gin370A = 2 * gout128 + gout123;
    Ciphertext gout370A = ctx.eval_lut(&gin370A, and2);
    Ciphertext gin370B = 2 * gout269 + (1 - gout123);
    Ciphertext gout370B = ctx.eval_lut(&gin370B, and2);
    Ciphertext gout370 = gout370A + gout370B;
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout73 + gout257;
    Ciphertext gout371 = ctx.eval_lut(&gin371, nand2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout236 + gout149;
    Ciphertext gout372 = ctx.eval_lut(&gin372, and2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout103 + gout234;
    Ciphertext gout373 = ctx.eval_lut(&gin373, nand2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout123 + gout262;
    Ciphertext gout374 = ctx.eval_lut(&gin374, nand2);
    std::cerr << "\rIMUX375  ";
    Ciphertext gin375A = 2 * gout237 + gout118;
    Ciphertext gout375A = ctx.eval_lut(&gin375A, and2);
    Ciphertext gin375B = 2 * gout284 + (1 - gout118);
    Ciphertext gout375B = ctx.eval_lut(&gin375B, and2);
    Ciphertext gout375 = gout375A + gout375B;
    std::cerr << "\rIMUX376  ";
    Ciphertext gin376A = 2 * gout263 + gout118;
    Ciphertext gout376A = ctx.eval_lut(&gin376A, and2);
    Ciphertext gin376B = 2 * gout281 + (1 - gout118);
    Ciphertext gout376B = ctx.eval_lut(&gin376B, and2);
    Ciphertext gout376 = gout376A + gout376B;
    std::cerr << "\rIMUX377  ";
    Ciphertext gin377A = 2 * gout133 + gout103;
    Ciphertext gout377A = ctx.eval_lut(&gin377A, and2);
    Ciphertext gin377B = 2 * gout246 + (1 - gout103);
    Ciphertext gout377B = ctx.eval_lut(&gin377B, and2);
    Ciphertext gout377 = gout377A + gout377B;
    std::cerr << "\rLUT378   ";
    Ciphertext gin378 = 2 * gout123 + gout269;
    Ciphertext gout378 = ctx.eval_lut(&gin378, nand2);
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout123 + gout253;
    Ciphertext gout379 = ctx.eval_lut(&gin379, nand2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout123 + gout258;
    Ciphertext gout380 = ctx.eval_lut(&gin380, nand2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout131 + gout259;
    Ciphertext gout381 = ctx.eval_lut(&gin381, nand2);
    std::cerr << "\rIMUX382  ";
    Ciphertext gin382A = 2 * gout348 + gout118;
    Ciphertext gout382A = ctx.eval_lut(&gin382A, and2);
    Ciphertext gin382B = 2 * gout283 + (1 - gout118);
    Ciphertext gout382B = ctx.eval_lut(&gin382B, and2);
    Ciphertext gout382 = gout382A + gout382B;
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * gout73 + gout280;
    Ciphertext gout383 = ctx.eval_lut(&gin383, nand2);
    std::cerr << "\rIMUX384  ";
    Ciphertext gin384A = 2 * gout263 + gout73;
    Ciphertext gout384A = ctx.eval_lut(&gin384A, and2);
    Ciphertext gin384B = 2 * gout280 + (1 - gout73);
    Ciphertext gout384B = ctx.eval_lut(&gin384B, and2);
    Ciphertext gout384 = gout384A + gout384B;
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout216 + gout286;
    Ciphertext gout385 = ctx.eval_lut(&gin385, and2);
    std::cerr << "\rIMUX386  ";
    Ciphertext gin386A = 2 * gout276 + gout103;
    Ciphertext gout386A = ctx.eval_lut(&gin386A, and2);
    Ciphertext gin386B = 2 * gout282 + (1 - gout103);
    Ciphertext gout386B = ctx.eval_lut(&gin386B, and2);
    Ciphertext gout386 = gout386A + gout386B;
    std::cerr << "\rLUT387   ";
    Ciphertext gin387 = 2 * gout118 + gout281;
    Ciphertext gout387 = ctx.eval_lut(&gin387, nand2);
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * gout103 + gout277;
    Ciphertext gout388 = ctx.eval_lut(&gin388, nand2);
    std::cerr << "\rIMUX389  ";
    Ciphertext gin389A = 2 * gout266 + gout73;
    Ciphertext gout389A = ctx.eval_lut(&gin389A, and2);
    Ciphertext gin389B = 2 * gout273 + (1 - gout73);
    Ciphertext gout389B = ctx.eval_lut(&gin389B, and2);
    Ciphertext gout389 = gout389A + gout389B;
    std::cerr << "\rIMUX390  ";
    Ciphertext gin390A = 2 * gout263 + gout103;
    Ciphertext gout390A = ctx.eval_lut(&gin390A, and2);
    Ciphertext gin390B = 2 * gout277 + (1 - gout103);
    Ciphertext gout390B = ctx.eval_lut(&gin390B, and2);
    Ciphertext gout390 = gout390A + gout390B;
    std::cerr << "\rIMUX391  ";
    Ciphertext gin391A = 2 * gout276 + gout118;
    Ciphertext gout391A = ctx.eval_lut(&gin391A, and2);
    Ciphertext gin391B = 2 * gout341 + (1 - gout118);
    Ciphertext gout391B = ctx.eval_lut(&gin391B, and2);
    Ciphertext gout391 = gout391A + gout391B;
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * gout118 + gout283;
    Ciphertext gout392 = ctx.eval_lut(&gin392, nand2);
    std::cerr << "\rLUT393   ";
    Ciphertext gin393 = 2 * gout118 + gout284;
    Ciphertext gout393 = ctx.eval_lut(&gin393, nand2);
    std::cerr << "\rLUT394   ";
    Ciphertext gin394 = 2 * gout103 + gout282;
    Ciphertext gout394 = ctx.eval_lut(&gin394, nand2);
    std::cerr << "\rLUT395   ";
    Ciphertext gin395 = 2 * gout103 + gout299;
    Ciphertext gout395 = ctx.eval_lut(&gin395, nand2);
    std::cerr << "\rIMUX396  ";
    Ciphertext gin396A = 2 * gout244 + gout103;
    Ciphertext gout396A = ctx.eval_lut(&gin396A, and2);
    Ciphertext gin396B = 2 * gout303 + (1 - gout103);
    Ciphertext gout396B = ctx.eval_lut(&gin396B, and2);
    Ciphertext gout396 = gout396A + gout396B;
    std::cerr << "\rIMUX397  ";
    Ciphertext gin397A = 2 * gout239 + gout103;
    Ciphertext gout397A = ctx.eval_lut(&gin397A, and2);
    Ciphertext gin397B = 2 * gout305 + (1 - gout103);
    Ciphertext gout397B = ctx.eval_lut(&gin397B, and2);
    Ciphertext gout397 = gout397A + gout397B;
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout249 + gout312;
    Ciphertext gout398 = ctx.eval_lut(&gin398, and2);
    std::cerr << "\rIMUX399  ";
    Ciphertext gin399A = 2 * gout244 + gout118;
    Ciphertext gout399A = ctx.eval_lut(&gin399A, and2);
    Ciphertext gin399B = 2 * gout310 + (1 - gout118);
    Ciphertext gout399B = ctx.eval_lut(&gin399B, and2);
    Ciphertext gout399 = gout399A + gout399B;
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout326 + gout297;
    Ciphertext gout400 = ctx.eval_lut(&gin400, and2);
    std::cerr << "\rLUT401   ";
    Ciphertext gin401 = 2 * gout243 + gout311;
    Ciphertext gout401 = ctx.eval_lut(&gin401, and2);
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * gout118 + gout341;
    Ciphertext gout402 = ctx.eval_lut(&gin402, nand2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout344 + gout317;
    Ciphertext gout403 = ctx.eval_lut(&gin403, and2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * gout328 + gout335;
    Ciphertext gout404 = ctx.eval_lut(&gin404, and2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout249 + gout320;
    Ciphertext gout405 = ctx.eval_lut(&gin405, and2);
    std::cerr << "\rLUT406   ";
    Ciphertext gin406 = 2 * gout118 + gout293;
    Ciphertext gout406 = ctx.eval_lut(&gin406, nand2);
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout118 + gout310;
    Ciphertext gout407 = ctx.eval_lut(&gin407, nand2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout300 + gout330;
    Ciphertext gout408 = ctx.eval_lut(&gin408, and2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout103 + gout303;
    Ciphertext gout409 = ctx.eval_lut(&gin409, nand2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout103 + gout305;
    Ciphertext gout410 = ctx.eval_lut(&gin410, nand2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout307 + gout314;
    Ciphertext gout411 = ctx.eval_lut(&gin411, and2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout339 + gout298;
    Ciphertext gout412 = ctx.eval_lut(&gin412, and2);
    std::cerr << "\rLUT413   ";
    Ciphertext gin413 = 2 * gout68 + gout318;
    Ciphertext gout413 = ctx.eval_lut(&gin413, and2);
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout333 + gout322;
    Ciphertext gout414 = ctx.eval_lut(&gin414, and2);
    std::cerr << "\rIMUX415  ";
    Ciphertext gin415A = 2 * gout212 + gout118;
    Ciphertext gout415A = ctx.eval_lut(&gin415A, and2);
    Ciphertext gin415B = 2 * gout356 + (1 - gout118);
    Ciphertext gout415B = ctx.eval_lut(&gin415B, and2);
    Ciphertext gout415 = gout415A + gout415B;
    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout103 + gout366;
    Ciphertext gout416 = ctx.eval_lut(&gin416, nand2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout327 + gout292;
    Ciphertext gout417 = ctx.eval_lut(&gin417, and2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout374 + gout294;
    Ciphertext gout418 = ctx.eval_lut(&gin418, and2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout289 + gout373;
    Ciphertext gout419 = ctx.eval_lut(&gin419, and2);
    std::cerr << "\rLUT420   ";
    Ciphertext gin420 = 2 * gout342 + gout351;
    Ciphertext gout420 = ctx.eval_lut(&gin420, and2);
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout349 + gout354;
    Ciphertext gout421 = ctx.eval_lut(&gin421, and2);
    std::cerr << "\rIMUX422  ";
    Ciphertext gin422A = 2 * gout237 + gout103;
    Ciphertext gout422A = ctx.eval_lut(&gin422A, and2);
    Ciphertext gin422B = 2 * gout366 + (1 - gout103);
    Ciphertext gout422B = ctx.eval_lut(&gin422B, and2);
    Ciphertext gout422 = gout422A + gout422B;
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * gout191 + gout315;
    Ciphertext gout423 = ctx.eval_lut(&gin423, and2);
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * gout309 + gout338;
    Ciphertext gout424 = ctx.eval_lut(&gin424, and2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * gout337 + gout70;
    Ciphertext gout425 = ctx.eval_lut(&gin425, and2);
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout302 + gout353;
    Ciphertext gout426 = ctx.eval_lut(&gin426, and2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout353 + gout288;
    Ciphertext gout427 = ctx.eval_lut(&gin427, and2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout183 + gout329;
    Ciphertext gout428 = ctx.eval_lut(&gin428, and2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout199 + gout372;
    Ciphertext gout429 = ctx.eval_lut(&gin429, and2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout103 + gout350;
    Ciphertext gout430 = ctx.eval_lut(&gin430, nand2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout346 + gout401;
    Ciphertext gout431 = ctx.eval_lut(&gin431, and2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout345 + gout379;
    Ciphertext gout432 = ctx.eval_lut(&gin432, and2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout381 + gout403;
    Ciphertext gout433 = ctx.eval_lut(&gin433, and2);
    std::cerr << "\rIMUX434  ";
    Ciphertext gin434A = 2 * gout239 + gout118;
    Ciphertext gout434A = ctx.eval_lut(&gin434A, and2);
    Ciphertext gin434B = 2 * gout377 + (1 - gout118);
    Ciphertext gout434B = ctx.eval_lut(&gin434B, and2);
    Ciphertext gout434 = gout434A + gout434B;
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout355 + gout248;
    Ciphertext gout435 = ctx.eval_lut(&gin435, and2);
    std::cerr << "\rIMUX436  ";
    Ciphertext gin436A = 2 * gout348 + gout73;
    Ciphertext gout436A = ctx.eval_lut(&gin436A, and2);
    Ciphertext gin436B = 2 * gout245 + (1 - gout73);
    Ciphertext gout436B = ctx.eval_lut(&gin436B, and2);
    Ciphertext gout436 = gout436A + gout436B;
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout362 + gout405;
    Ciphertext gout437 = ctx.eval_lut(&gin437, and2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout378 + gout199;
    Ciphertext gout438 = ctx.eval_lut(&gin438, and2);
    std::cerr << "\rIMUX439  ";
    Ciphertext gin439A = 2 * gout212 + gout103;
    Ciphertext gout439A = ctx.eval_lut(&gin439A, and2);
    Ciphertext gin439B = 2 * gout350 + (1 - gout103);
    Ciphertext gout439B = ctx.eval_lut(&gin439B, and2);
    Ciphertext gout439 = gout439A + gout439B;
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout316 + gout369;
    Ciphertext gout440 = ctx.eval_lut(&gin440, and2);
    std::cerr << "\rIMUX441  ";
    Ciphertext gin441A = 2 * gout348 + gout103;
    Ciphertext gout441A = ctx.eval_lut(&gin441A, and2);
    Ciphertext gin441B = 2 * gout299 + (1 - gout103);
    Ciphertext gout441B = ctx.eval_lut(&gin441B, and2);
    Ciphertext gout441 = gout441A + gout441B;
    std::cerr << "\rIMUX442  ";
    Ciphertext gin442A = 2 * gout266 + gout103;
    Ciphertext gout442A = ctx.eval_lut(&gin442A, and2);
    Ciphertext gin442B = 2 * gout370 + (1 - gout103);
    Ciphertext gout442B = ctx.eval_lut(&gin442B, and2);
    Ciphertext gout442 = gout442A + gout442B;
    std::cerr << "\rLUT443   ";
    Ciphertext gin443 = 2 * gout352 + gout296;
    Ciphertext gout443 = ctx.eval_lut(&gin443, and2);
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout363 + gout306;
    Ciphertext gout444 = ctx.eval_lut(&gin444, and2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout118 + gout356;
    Ciphertext gout445 = ctx.eval_lut(&gin445, nand2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout103 + gout370;
    Ciphertext gout446 = ctx.eval_lut(&gin446, nand2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout380 + gout295;
    Ciphertext gout447 = ctx.eval_lut(&gin447, and2);
    std::cerr << "\rIMUX448  ";
    Ciphertext gin448A = 2 * gout390 + gout78;
    Ciphertext gout448A = ctx.eval_lut(&gin448A, and2);
    Ciphertext gin448B = 2 * gout376 + (1 - gout78);
    Ciphertext gout448B = ctx.eval_lut(&gin448B, and2);
    Ciphertext gout448 = gout448A + gout448B;
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout360 + gout323;
    Ciphertext gout449 = ctx.eval_lut(&gin449, and2);
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout118 + gout377;
    Ciphertext gout450 = ctx.eval_lut(&gin450, nand2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout381 + gout334;
    Ciphertext gout451 = ctx.eval_lut(&gin451, and2);
    std::cerr << "\rIMUX452  ";
    Ciphertext gin452A = 2 * gout390 + gout73;
    Ciphertext gout452A = ctx.eval_lut(&gin452A, and2);
    Ciphertext gin452B = 2 * gout376 + (1 - gout73);
    Ciphertext gout452B = ctx.eval_lut(&gin452B, and2);
    Ciphertext gout452 = gout452A + gout452B;
    std::cerr << "\rLUT453   ";
    Ciphertext gin453 = 2 * gout385 + gout426;
    Ciphertext gout453 = ctx.eval_lut(&gin453, and2);
    std::cerr << "\rIMUX454  ";
    Ciphertext gin454A = 2 * gout386 + gout78;
    Ciphertext gout454A = ctx.eval_lut(&gin454A, and2);
    Ciphertext gin454B = 2 * gout391 + (1 - gout78);
    Ciphertext gout454B = ctx.eval_lut(&gin454B, and2);
    Ciphertext gout454 = gout454A + gout454B;
    std::cerr << "\rIMUX455  ";
    Ciphertext gin455A = 2 * gout386 + gout73;
    Ciphertext gout455A = ctx.eval_lut(&gin455A, and2);
    Ciphertext gin455B = 2 * gout391 + (1 - gout73);
    Ciphertext gout455B = ctx.eval_lut(&gin455B, and2);
    Ciphertext gout455 = gout455A + gout455B;
    std::cerr << "\rIMUX456  ";
    Ciphertext gin456A = 2 * gout396 + gout78;
    Ciphertext gout456A = ctx.eval_lut(&gin456A, and2);
    Ciphertext gin456B = 2 * gout399 + (1 - gout78);
    Ciphertext gout456B = ctx.eval_lut(&gin456B, and2);
    Ciphertext gout456 = gout456A + gout456B;
    std::cerr << "\rIMUX457  ";
    Ciphertext gin457A = 2 * gout396 + gout73;
    Ciphertext gout457A = ctx.eval_lut(&gin457A, and2);
    Ciphertext gin457B = 2 * gout399 + (1 - gout73);
    Ciphertext gout457B = ctx.eval_lut(&gin457B, and2);
    Ciphertext gout457 = gout457A + gout457B;
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout287 + gout411;
    Ciphertext gout458 = ctx.eval_lut(&gin458, nand2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout357 + gout408;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout191 + gout404;
    Ciphertext gout460 = ctx.eval_lut(&gin460, and2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout332 + gout412;
    Ciphertext gout461 = ctx.eval_lut(&gin461, nand2);
    std::cerr << "\rIMUX462  ";
    Ciphertext gin462A = 2 * gout422 + gout73;
    Ciphertext gout462A = ctx.eval_lut(&gin462A, and2);
    Ciphertext gin462B = 2 * gout375 + (1 - gout73);
    Ciphertext gout462B = ctx.eval_lut(&gin462B, and2);
    Ciphertext gout462 = gout462A + gout462B;
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout371 + gout428;
    Ciphertext gout463 = ctx.eval_lut(&gin463, and2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout343 + gout429;
    Ciphertext gout464 = ctx.eval_lut(&gin464, and2);
    std::cerr << "\rIMUX465  ";
    Ciphertext gin465A = 2 * gout442 + gout73;
    Ciphertext gout465A = ctx.eval_lut(&gin465A, and2);
    Ciphertext gin465B = 2 * gout331 + (1 - gout73);
    Ciphertext gout465B = ctx.eval_lut(&gin465B, and2);
    Ciphertext gout465 = gout465A + gout465B;
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout308 + gout423;
    Ciphertext gout466 = ctx.eval_lut(&gin466, and2);
    std::cerr << "\rIMUX467  ";
    Ciphertext gin467A = 2 * gout422 + gout78;
    Ciphertext gout467A = ctx.eval_lut(&gin467A, and2);
    Ciphertext gin467B = 2 * gout375 + (1 - gout78);
    Ciphertext gout467B = ctx.eval_lut(&gin467B, and2);
    Ciphertext gout467 = gout467A + gout467B;
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout301 + gout424;
    Ciphertext gout468 = ctx.eval_lut(&gin468, nand2);
    std::cerr << "\rLUT469   ";
    Ciphertext gin469 = 2 * gout445 + gout437;
    Ciphertext gout469 = ctx.eval_lut(&gin469, and2);
    std::cerr << "\rIMUX470  ";
    Ciphertext gin470A = 2 * gout397 + gout78;
    Ciphertext gout470A = ctx.eval_lut(&gin470A, and2);
    Ciphertext gin470B = 2 * gout434 + (1 - gout78);
    Ciphertext gout470B = ctx.eval_lut(&gin470B, and2);
    Ciphertext gout470 = gout470A + gout470B;
    std::cerr << "\rLUT471   ";
    Ciphertext gin471 = 2 * gout383 + gout427;
    Ciphertext gout471 = ctx.eval_lut(&gin471, and2);
    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout358 + gout444;
    Ciphertext gout472 = ctx.eval_lut(&gin472, nand2);
    std::cerr << "\rLUT473   ";
    Ciphertext gin473 = 2 * gout340 + gout417;
    Ciphertext gout473 = ctx.eval_lut(&gin473, nand2);
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout321 + gout420;
    Ciphertext gout474 = ctx.eval_lut(&gin474, nand2);
    std::cerr << "\rIMUX475  ";
    Ciphertext gin475A = 2 * gout397 + gout73;
    Ciphertext gout475A = ctx.eval_lut(&gin475A, and2);
    Ciphertext gin475B = 2 * gout434 + (1 - gout73);
    Ciphertext gout475B = ctx.eval_lut(&gin475B, and2);
    Ciphertext gout475 = gout475A + gout475B;
    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout440 + gout398;
    Ciphertext gout476 = ctx.eval_lut(&gin476, and2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout367 + gout438;
    Ciphertext gout477 = ctx.eval_lut(&gin477, and2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout243 + gout443;
    Ciphertext gout478 = ctx.eval_lut(&gin478, and2);
    std::cerr << "\rIMUX479  ";
    Ciphertext gin479A = 2 * gout439 + gout78;
    Ciphertext gout479A = ctx.eval_lut(&gin479A, and2);
    Ciphertext gin479B = 2 * gout415 + (1 - gout78);
    Ciphertext gout479B = ctx.eval_lut(&gin479B, and2);
    Ciphertext gout479 = gout479A + gout479B;
    std::cerr << "\rIMUX480  ";
    Ciphertext gin480A = 2 * gout441 + gout78;
    Ciphertext gout480A = ctx.eval_lut(&gin480A, and2);
    Ciphertext gin480B = 2 * gout382 + (1 - gout78);
    Ciphertext gout480B = ctx.eval_lut(&gin480B, and2);
    Ciphertext gout480 = gout480A + gout480B;
    std::cerr << "\rIMUX481  ";
    Ciphertext gin481A = 2 * gout442 + gout78;
    Ciphertext gout481A = ctx.eval_lut(&gin481A, and2);
    Ciphertext gin481B = 2 * gout331 + (1 - gout78);
    Ciphertext gout481B = ctx.eval_lut(&gin481B, and2);
    Ciphertext gout481 = gout481A + gout481B;
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout368 + gout451;
    Ciphertext gout482 = ctx.eval_lut(&gin482, and2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout419 + gout183;
    Ciphertext gout483 = ctx.eval_lut(&gin483, and2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout407 + gout431;
    Ciphertext gout484 = ctx.eval_lut(&gin484, and2);
    std::cerr << "\rLUT485   ";
    Ciphertext gin485 = 2 * gout393 + gout466;
    Ciphertext gout485 = ctx.eval_lut(&gin485, and2);
    std::cerr << "\rLUT486   ";
    Ciphertext gin486 = 2 * gout304 + gout435;
    Ciphertext gout486 = ctx.eval_lut(&gin486, and2);
    std::cerr << "\rLUT487   ";
    Ciphertext gin487 = 2 * gout432 + gout433;
    Ciphertext gout487 = ctx.eval_lut(&gin487, nand2);
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * gout365 + gout449;
    Ciphertext gout488 = ctx.eval_lut(&gin488, nand2);
    std::cerr << "\rIMUX489  ";
    Ciphertext gin489A = 2 * gout439 + gout73;
    Ciphertext gout489A = ctx.eval_lut(&gin489A, and2);
    Ciphertext gin489B = 2 * gout415 + (1 - gout73);
    Ciphertext gout489B = ctx.eval_lut(&gin489B, and2);
    Ciphertext gout489 = gout489A + gout489B;
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout400 + gout453;
    Ciphertext gout490 = ctx.eval_lut(&gin490, nand2);
    std::cerr << "\rIMUX491  ";
    Ciphertext gin491A = 2 * gout441 + gout73;
    Ciphertext gout491A = ctx.eval_lut(&gin491A, and2);
    Ciphertext gin491B = 2 * gout382 + (1 - gout73);
    Ciphertext gout491B = ctx.eval_lut(&gin491B, and2);
    Ciphertext gout491 = gout491A + gout491B;
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout450 + gout421;
    Ciphertext gout492 = ctx.eval_lut(&gin492, and2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout56 + gout459;
    Ciphertext gout493 = ctx.eval_lut(&gin493, nand2);
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * gout56 + gout458;
    Ciphertext gout494 = ctx.eval_lut(&gin494, nand2);
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * gout56 + gout461;
    Ciphertext gout495 = ctx.eval_lut(&gin495, nand2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout418 + gout460;
    Ciphertext gout496 = ctx.eval_lut(&gin496, nand2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout387 + gout471;
    Ciphertext gout497 = ctx.eval_lut(&gin497, and2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout56 + gout474;
    Ciphertext gout498 = ctx.eval_lut(&gin498, nand2);
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * gout56 + gout468;
    Ciphertext gout499 = ctx.eval_lut(&gin499, nand2);
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * gout402 + gout463;
    Ciphertext gout500 = ctx.eval_lut(&gin500, and2);
    std::cerr << "\rLUT501   ";
    Ciphertext gin501 = 2 * gout392 + gout482;
    Ciphertext gout501 = ctx.eval_lut(&gin501, and2);
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout409 + gout484;
    Ciphertext gout502 = ctx.eval_lut(&gin502, nand2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout406 + gout464;
    Ciphertext gout503 = ctx.eval_lut(&gin503, and2);
    std::cerr << "\rIMUX504  ";
    Ciphertext gin504A = 2 * gout141 + gout63;
    Ciphertext gout504A = ctx.eval_lut(&gin504A, and2);
    Ciphertext gin504B = 2 * gout487 + (1 - gout63);
    Ciphertext gout504B = ctx.eval_lut(&gin504B, and2);
    Ciphertext gout504 = gout504A + gout504B;
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * gout447 + gout486;
    Ciphertext gout505 = ctx.eval_lut(&gin505, nand2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * gout430 + gout469;
    Ciphertext gout506 = ctx.eval_lut(&gin506, nand2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout364 + gout476;
    Ciphertext gout507 = ctx.eval_lut(&gin507, nand2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout425 + gout477;
    Ciphertext gout508 = ctx.eval_lut(&gin508, nand2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout414 + gout478;
    Ciphertext gout509 = ctx.eval_lut(&gin509, nand2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout56 + gout472;
    Ciphertext gout510 = ctx.eval_lut(&gin510, nand2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * gout413 + gout483;
    Ciphertext gout511 = ctx.eval_lut(&gin511, nand2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout56 + gout473;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * gout416 + gout485;
    Ciphertext gout513 = ctx.eval_lut(&gin513, nand2);
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * gout56 + gout488;
    Ciphertext gout514 = ctx.eval_lut(&gin514, nand2);
    std::cerr << "\rIMUX515  ";
    Ciphertext gin515A = 2 * gout137 + gout63;
    Ciphertext gout515A = ctx.eval_lut(&gin515A, and2);
    Ciphertext gin515B = 2 * gout490 + (1 - gout63);
    Ciphertext gout515B = ctx.eval_lut(&gin515B, and2);
    Ciphertext gout515 = gout515A + gout515B;
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * gout410 + gout492;
    Ciphertext gout516 = ctx.eval_lut(&gin516, nand2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout171 + gout493;
    Ciphertext gout517 = ctx.eval_lut(&gin517, nand2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout177 + gout494;
    Ciphertext gout518 = ctx.eval_lut(&gin518, nand2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout189 + gout495;
    Ciphertext gout519 = ctx.eval_lut(&gin519, nand2);
    std::cerr << "\rIMUX520  ";
    Ciphertext gin520A = 2 * gout135 + gout63;
    Ciphertext gout520A = ctx.eval_lut(&gin520A, and2);
    Ciphertext gin520B = 2 * gout496 + (1 - gout63);
    Ciphertext gout520B = ctx.eval_lut(&gin520B, and2);
    Ciphertext gout520 = gout520A + gout520B;
    std::cerr << "\rLUT521   ";
    Ciphertext gin521 = 2 * gout204 + gout499;
    Ciphertext gout521 = ctx.eval_lut(&gin521, nand2);
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * gout388 + gout497;
    Ciphertext gout522 = ctx.eval_lut(&gin522, nand2);
    std::cerr << "\rLUT523   ";
    Ciphertext gin523 = 2 * gout233 + gout498;
    Ciphertext gout523 = ctx.eval_lut(&gin523, nand2);
    std::cerr << "\rLUT524   ";
    Ciphertext gin524 = 2 * gout446 + gout503;
    Ciphertext gout524 = ctx.eval_lut(&gin524, nand2);
    std::cerr << "\rIMUX525  ";
    Ciphertext gin525A = 2 * gout133 + gout63;
    Ciphertext gout525A = ctx.eval_lut(&gin525A, and2);
    Ciphertext gin525B = 2 * gout505 + (1 - gout63);
    Ciphertext gout525B = ctx.eval_lut(&gin525B, and2);
    Ciphertext gout525 = gout525A + gout525B;
    std::cerr << "\rIMUX526  ";
    Ciphertext gin526A = 2 * gout212 + gout74;
    Ciphertext gout526A = ctx.eval_lut(&gin526A, and2);
    Ciphertext gin526B = 2 * gout506 + (1 - gout74);
    Ciphertext gout526B = ctx.eval_lut(&gin526B, and2);
    Ciphertext gout526 = gout526A + gout526B;
    std::cerr << "\rIMUX527  ";
    Ciphertext gin527A = 2 * gout126 + gout63;
    Ciphertext gout527A = ctx.eval_lut(&gin527A, and2);
    Ciphertext gin527B = 2 * gout507 + (1 - gout63);
    Ciphertext gout527B = ctx.eval_lut(&gin527B, and2);
    Ciphertext gout527 = gout527A + gout527B;
    std::cerr << "\rIMUX528  ";
    Ciphertext gin528A = 2 * gout128 + gout63;
    Ciphertext gout528A = ctx.eval_lut(&gin528A, and2);
    Ciphertext gin528B = 2 * gout508 + (1 - gout63);
    Ciphertext gout528B = ctx.eval_lut(&gin528B, and2);
    Ciphertext gout528 = gout528A + gout528B;
    std::cerr << "\rLUT529   ";
    Ciphertext gin529 = 2 * gout168 + gout512;
    Ciphertext gout529 = ctx.eval_lut(&gin529, nand2);
    std::cerr << "\rIMUX530  ";
    Ciphertext gin530A = 2 * gout140 + gout63;
    Ciphertext gout530A = ctx.eval_lut(&gin530A, and2);
    Ciphertext gin530B = 2 * gout511 + (1 - gout63);
    Ciphertext gout530B = ctx.eval_lut(&gin530B, and2);
    Ciphertext gout530 = gout530A + gout530B;
    std::cerr << "\rIMUX531  ";
    Ciphertext gin531A = 2 * gout132 + gout63;
    Ciphertext gout531A = ctx.eval_lut(&gin531A, and2);
    Ciphertext gin531B = 2 * gout509 + (1 - gout63);
    Ciphertext gout531B = ctx.eval_lut(&gin531B, and2);
    Ciphertext gout531 = gout531A + gout531B;
    std::cerr << "\rLUT532   ";
    Ciphertext gin532 = 2 * gout172 + gout510;
    Ciphertext gout532 = ctx.eval_lut(&gin532, nand2);
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout395 + gout501;
    Ciphertext gout533 = ctx.eval_lut(&gin533, nand2);
    std::cerr << "\rLUT534   ";
    Ciphertext gin534 = 2 * gout394 + gout500;
    Ciphertext gout534 = ctx.eval_lut(&gin534, nand2);
    std::cerr << "\rIMUX535  ";
    Ciphertext gin535A = 2 * gout237 + gout74;
    Ciphertext gout535A = ctx.eval_lut(&gin535A, and2);
    Ciphertext gin535B = 2 * gout513 + (1 - gout74);
    Ciphertext gout535B = ctx.eval_lut(&gin535B, and2);
    Ciphertext gout535 = gout535A + gout535B;
    std::cerr << "\rIMUX536  ";
    Ciphertext gin536A = 2 * gout244 + gout74;
    Ciphertext gout536A = ctx.eval_lut(&gin536A, and2);
    Ciphertext gin536B = 2 * gout502 + (1 - gout74);
    Ciphertext gout536B = ctx.eval_lut(&gin536B, and2);
    Ciphertext gout536 = gout536A + gout536B;
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * gout167 + gout514;
    Ciphertext gout537 = ctx.eval_lut(&gin537, nand2);
    std::cerr << "\rIMUX538  ";
    Ciphertext gin538A = 2 * gout239 + gout74;
    Ciphertext gout538A = ctx.eval_lut(&gin538A, and2);
    Ciphertext gin538B = 2 * gout516 + (1 - gout74);
    Ciphertext gout538B = ctx.eval_lut(&gin538B, and2);
    Ciphertext gout538 = gout538A + gout538B;
    std::cerr << "\rIMUX539  ";
    Ciphertext gin539A = 2 * gout263 + gout74;
    Ciphertext gout539A = ctx.eval_lut(&gin539A, and2);
    Ciphertext gin539B = 2 * gout522 + (1 - gout74);
    Ciphertext gout539B = ctx.eval_lut(&gin539B, and2);
    Ciphertext gout539 = gout539A + gout539B;
    std::cerr << "\rIMUX540  ";
    Ciphertext gin540A = 2 * gout266 + gout74;
    Ciphertext gout540A = ctx.eval_lut(&gin540A, and2);
    Ciphertext gin540B = 2 * gout524 + (1 - gout74);
    Ciphertext gout540B = ctx.eval_lut(&gin540B, and2);
    Ciphertext gout540 = gout540A + gout540B;
    std::cerr << "\rIMUX541  ";
    Ciphertext gin541A = 2 * gout276 + gout74;
    Ciphertext gout541A = ctx.eval_lut(&gin541A, and2);
    Ciphertext gin541B = 2 * gout534 + (1 - gout74);
    Ciphertext gout541B = ctx.eval_lut(&gin541B, and2);
    Ciphertext gout541 = gout541A + gout541B;
    std::cerr << "\rIMUX542  ";
    Ciphertext gin542A = 2 * gout348 + gout74;
    Ciphertext gout542A = ctx.eval_lut(&gin542A, and2);
    Ciphertext gin542B = 2 * gout533 + (1 - gout74);
    Ciphertext gout542B = ctx.eval_lut(&gin542B, and2);
    Ciphertext gout542 = gout542A + gout542B;

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout529)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout518)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout537)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout523)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout519)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout532)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout521)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout517)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout527)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout530)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout515)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout525)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout520)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout528)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout531)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout504)); /* out15 */
    test_out.push_back(ctx.decrypt(&gout526)); /* out16 */
    test_out.push_back(ctx.decrypt(&gout541)); /* out17 */
    test_out.push_back(ctx.decrypt(&gout539)); /* out18 */
    test_out.push_back(ctx.decrypt(&gout538)); /* out19 */
    test_out.push_back(ctx.decrypt(&gout535)); /* out20 */
    test_out.push_back(ctx.decrypt(&gout540)); /* out21 */
    test_out.push_back(ctx.decrypt(&gout536)); /* out22 */
    test_out.push_back(ctx.decrypt(&gout542)); /* out23 */
    test_out.push_back(ctx.decrypt(&gout479)); /* out24 */
    test_out.push_back(ctx.decrypt(&gout454)); /* out25 */
    test_out.push_back(ctx.decrypt(&gout448)); /* out26 */
    test_out.push_back(ctx.decrypt(&gout470)); /* out27 */
    test_out.push_back(ctx.decrypt(&gout467)); /* out28 */
    test_out.push_back(ctx.decrypt(&gout481)); /* out29 */
    test_out.push_back(ctx.decrypt(&gout456)); /* out30 */
    test_out.push_back(ctx.decrypt(&gout480)); /* out31 */
    test_out.push_back(ctx.decrypt(&gout489)); /* out32 */
    test_out.push_back(ctx.decrypt(&gout455)); /* out33 */
    test_out.push_back(ctx.decrypt(&gout452)); /* out34 */
    test_out.push_back(ctx.decrypt(&gout475)); /* out35 */
    test_out.push_back(ctx.decrypt(&gout462)); /* out36 */
    test_out.push_back(ctx.decrypt(&gout465)); /* out37 */
    test_out.push_back(ctx.decrypt(&gout457)); /* out38 */
    test_out.push_back(ctx.decrypt(&gout491)); /* out39 */
    test_out.push_back(ctx.decrypt(&gout324)); /* out40 */
    test_out.push_back(ctx.decrypt(&gout361)); /* out41 */
    test_out.push_back(ctx.decrypt(&gout384)); /* out42 */
    test_out.push_back(ctx.decrypt(&gout359)); /* out43 */
    test_out.push_back(ctx.decrypt(&gout313)); /* out44 */
    test_out.push_back(ctx.decrypt(&gout389)); /* out45 */
    test_out.push_back(ctx.decrypt(&gout347)); /* out46 */
    test_out.push_back(ctx.decrypt(&gout436)); /* out47 */
    test_out.push_back(ctx.decrypt(&gout319)); /* out48 */
    test_out.push_back(ctx.decrypt(&gout285)); /* out49 */
    test_out.push_back(ctx.decrypt(&gout272)); /* out50 */
    test_out.push_back(ctx.decrypt(&gout325)); /* out51 */
    test_out.push_back(ctx.decrypt(&gout227)); /* out52 */
    test_out.push_back(ctx.decrypt(&gout336)); /* out53 */
    test_out.push_back(ctx.decrypt(&gout290)); /* out54 */
    test_out.push_back(ctx.decrypt(&gout278)); /* out55 */
    test_out.push_back(ctx.decrypt(&gout80)); /* out56 */
    test_out.push_back(ctx.decrypt(&gout85)); /* out57 */
    test_out.push_back(ctx.decrypt(&gout79)); /* out58 */
    test_out.push_back(ctx.decrypt(&gout82)); /* out59 */
    test_out.push_back(ctx.decrypt(&gout77)); /* out60 */
    test_out.push_back(ctx.decrypt(&gout86)); /* out61 */
    test_out.push_back(ctx.decrypt(&gout83)); /* out62 */
    test_out.push_back(ctx.decrypt(&gout81)); /* out63 */
    return test_out;
}

static void
BM_strrev_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_strrev_boolean(ctx);
    }
}

BENCHMARK(BM_strrev_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
