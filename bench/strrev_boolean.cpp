#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_strrev_boolean(FHEContext& ctx, uint64_t my_string = 0)
{
    Ciphertext ct_my_string0 = ctx.encrypt_public((my_string >> 0) & 1);
    Ciphertext ct_my_string1 = ctx.encrypt_public((my_string >> 1) & 1);
    Ciphertext ct_my_string2 = ctx.encrypt_public((my_string >> 2) & 1);
    Ciphertext ct_my_string3 = ctx.encrypt_public((my_string >> 3) & 1);
    Ciphertext ct_my_string4 = ctx.encrypt_public((my_string >> 4) & 1);
    Ciphertext ct_my_string5 = ctx.encrypt_public((my_string >> 5) & 1);
    Ciphertext ct_my_string6 = ctx.encrypt_public((my_string >> 6) & 1);
    Ciphertext ct_my_string7 = ctx.encrypt_public((my_string >> 7) & 1);
    Ciphertext ct_my_string8 = ctx.encrypt_public((my_string >> 8) & 1);
    Ciphertext ct_my_string9 = ctx.encrypt_public((my_string >> 9) & 1);
    Ciphertext ct_my_string10 = ctx.encrypt_public((my_string >> 10) & 1);
    Ciphertext ct_my_string11 = ctx.encrypt_public((my_string >> 11) & 1);
    Ciphertext ct_my_string12 = ctx.encrypt_public((my_string >> 12) & 1);
    Ciphertext ct_my_string13 = ctx.encrypt_public((my_string >> 13) & 1);
    Ciphertext ct_my_string14 = ctx.encrypt_public((my_string >> 14) & 1);
    Ciphertext ct_my_string15 = ctx.encrypt_public((my_string >> 15) & 1);
    Ciphertext ct_my_string16 = ctx.encrypt_public((my_string >> 16) & 1);
    Ciphertext ct_my_string17 = ctx.encrypt_public((my_string >> 17) & 1);
    Ciphertext ct_my_string18 = ctx.encrypt_public((my_string >> 18) & 1);
    Ciphertext ct_my_string19 = ctx.encrypt_public((my_string >> 19) & 1);
    Ciphertext ct_my_string20 = ctx.encrypt_public((my_string >> 20) & 1);
    Ciphertext ct_my_string21 = ctx.encrypt_public((my_string >> 21) & 1);
    Ciphertext ct_my_string22 = ctx.encrypt_public((my_string >> 22) & 1);
    Ciphertext ct_my_string23 = ctx.encrypt_public((my_string >> 23) & 1);
    Ciphertext ct_my_string24 = ctx.encrypt_public((my_string >> 24) & 1);
    Ciphertext ct_my_string25 = ctx.encrypt_public((my_string >> 25) & 1);
    Ciphertext ct_my_string26 = ctx.encrypt_public((my_string >> 26) & 1);
    Ciphertext ct_my_string27 = ctx.encrypt_public((my_string >> 27) & 1);
    Ciphertext ct_my_string28 = ctx.encrypt_public((my_string >> 28) & 1);
    Ciphertext ct_my_string29 = ctx.encrypt_public((my_string >> 29) & 1);
    Ciphertext ct_my_string30 = ctx.encrypt_public((my_string >> 30) & 1);
    Ciphertext ct_my_string31 = ctx.encrypt_public((my_string >> 31) & 1);
    Ciphertext ct_my_string32 = ctx.encrypt_public((my_string >> 32) & 1);
    Ciphertext ct_my_string33 = ctx.encrypt_public((my_string >> 33) & 1);
    Ciphertext ct_my_string34 = ctx.encrypt_public((my_string >> 34) & 1);
    Ciphertext ct_my_string35 = ctx.encrypt_public((my_string >> 35) & 1);
    Ciphertext ct_my_string36 = ctx.encrypt_public((my_string >> 36) & 1);
    Ciphertext ct_my_string37 = ctx.encrypt_public((my_string >> 37) & 1);
    Ciphertext ct_my_string38 = ctx.encrypt_public((my_string >> 38) & 1);
    Ciphertext ct_my_string39 = ctx.encrypt_public((my_string >> 39) & 1);
    Ciphertext ct_my_string40 = ctx.encrypt_public((my_string >> 40) & 1);
    Ciphertext ct_my_string41 = ctx.encrypt_public((my_string >> 41) & 1);
    Ciphertext ct_my_string42 = ctx.encrypt_public((my_string >> 42) & 1);
    Ciphertext ct_my_string43 = ctx.encrypt_public((my_string >> 43) & 1);
    Ciphertext ct_my_string44 = ctx.encrypt_public((my_string >> 44) & 1);
    Ciphertext ct_my_string45 = ctx.encrypt_public((my_string >> 45) & 1);
    Ciphertext ct_my_string46 = ctx.encrypt_public((my_string >> 46) & 1);
    Ciphertext ct_my_string47 = ctx.encrypt_public((my_string >> 47) & 1);
    Ciphertext ct_my_string48 = ctx.encrypt_public((my_string >> 48) & 1);
    Ciphertext ct_my_string49 = ctx.encrypt_public((my_string >> 49) & 1);
    Ciphertext ct_my_string50 = ctx.encrypt_public((my_string >> 50) & 1);
    Ciphertext ct_my_string51 = ctx.encrypt_public((my_string >> 51) & 1);
    Ciphertext ct_my_string52 = ctx.encrypt_public((my_string >> 52) & 1);
    Ciphertext ct_my_string53 = ctx.encrypt_public((my_string >> 53) & 1);
    Ciphertext ct_my_string54 = ctx.encrypt_public((my_string >> 54) & 1);
    Ciphertext ct_my_string55 = ctx.encrypt_public((my_string >> 55) & 1);
    Ciphertext ct_my_string56 = ctx.encrypt_public((my_string >> 56) & 1);
    Ciphertext ct_my_string57 = ctx.encrypt_public((my_string >> 57) & 1);
    Ciphertext ct_my_string58 = ctx.encrypt_public((my_string >> 58) & 1);
    Ciphertext ct_my_string59 = ctx.encrypt_public((my_string >> 59) & 1);
    Ciphertext ct_my_string60 = ctx.encrypt_public((my_string >> 60) & 1);
    Ciphertext ct_my_string61 = ctx.encrypt_public((my_string >> 61) & 1);
    Ciphertext ct_my_string62 = ctx.encrypt_public((my_string >> 62) & 1);
    Ciphertext ct_my_string63 = ctx.encrypt_public((my_string >> 63) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_my_string62 + ct_my_string63;
    Ciphertext gout0 = ctx.eval_lut(&gin0, nor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_my_string60 + ct_my_string61;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nor2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_my_string56 + ct_my_string57;
    Ciphertext gout2 = ctx.eval_lut(&gin2, nor2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_my_string58 + ct_my_string59;
    Ciphertext gout3 = ctx.eval_lut(&gin3, nor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_my_string32 + ct_my_string33;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nor2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_my_string38 + ct_my_string39;
    Ciphertext gout5 = ctx.eval_lut(&gin5, nor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_my_string46 + ct_my_string47;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nor2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_my_string36 + ct_my_string37;
    Ciphertext gout7 = ctx.eval_lut(&gin7, nor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_my_string40 + ct_my_string41;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nor2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_my_string19 + ct_my_string20;
    Ciphertext gout9 = ctx.eval_lut(&gin9, nor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_my_string34 + ct_my_string35;
    Ciphertext gout10 = ctx.eval_lut(&gin10, nor2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_my_string42 + ct_my_string43;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nor2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_my_string44 + ct_my_string45;
    Ciphertext gout12 = ctx.eval_lut(&gin12, nor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_my_string17 + ct_my_string18;
    Ciphertext gout13 = ctx.eval_lut(&gin13, nor2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_my_string26 + ct_my_string27;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nor2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_my_string48 + ct_my_string51;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nor2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_my_string49 + ct_my_string50;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nor2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_my_string21 + ct_my_string22;
    Ciphertext gout17 = ctx.eval_lut(&gin17, nor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_my_string28 + ct_my_string29;
    Ciphertext gout18 = ctx.eval_lut(&gin18, nor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_my_string52 + ct_my_string55;
    Ciphertext gout19 = ctx.eval_lut(&gin19, nor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_my_string0 + ct_my_string1;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nor2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_my_string23 + ct_my_string16;
    Ciphertext gout21 = ctx.eval_lut(&gin21, nor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_my_string24 + ct_my_string25;
    Ciphertext gout22 = ctx.eval_lut(&gin22, nor2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_my_string6 + ct_my_string7;
    Ciphertext gout23 = ctx.eval_lut(&gin23, nor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_my_string30 + ct_my_string31;
    Ciphertext gout24 = ctx.eval_lut(&gin24, nor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_my_string4 + ct_my_string5;
    Ciphertext gout25 = ctx.eval_lut(&gin25, nor2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_my_string12 + ct_my_string13;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nor2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_my_string8 + ct_my_string9;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nor2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_my_string2 + ct_my_string3;
    Ciphertext gout28 = ctx.eval_lut(&gin28, nor2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_my_string10 + ct_my_string11;
    Ciphertext gout29 = ctx.eval_lut(&gin29, nor2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_my_string14 + ct_my_string15;
    Ciphertext gout30 = ctx.eval_lut(&gin30, nor2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_my_string53 + ct_my_string54;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nor2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * gout4 + gout10;
    Ciphertext gout32 = ctx.eval_lut(&gin32, and2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * gout2 + gout3;
    Ciphertext gout33 = ctx.eval_lut(&gin33, and2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * gout1 + gout0;
    Ciphertext gout34 = ctx.eval_lut(&gin34, and2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * gout7 + gout5;
    Ciphertext gout35 = ctx.eval_lut(&gin35, and2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * gout12 + gout6;
    Ciphertext gout36 = ctx.eval_lut(&gin36, and2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * gout31 + gout19;
    Ciphertext gout37 = ctx.eval_lut(&gin37, and2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * gout8 + gout11;
    Ciphertext gout38 = ctx.eval_lut(&gin38, and2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * gout9 + gout17;
    Ciphertext gout39 = ctx.eval_lut(&gin39, and2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * gout18 + gout24;
    Ciphertext gout40 = ctx.eval_lut(&gin40, and2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * gout21 + gout13;
    Ciphertext gout41 = ctx.eval_lut(&gin41, and2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * gout22 + gout14;
    Ciphertext gout42 = ctx.eval_lut(&gin42, and2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * gout16 + gout15;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * gout20 + gout28;
    Ciphertext gout44 = ctx.eval_lut(&gin44, and2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * gout27 + gout29;
    Ciphertext gout45 = ctx.eval_lut(&gin45, and2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * gout25 + gout23;
    Ciphertext gout46 = ctx.eval_lut(&gin46, and2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * gout26 + gout30;
    Ciphertext gout47 = ctx.eval_lut(&gin47, and2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * gout34 + gout33;
    Ciphertext gout48 = ctx.eval_lut(&gin48, nand2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * gout37 + gout43;
    Ciphertext gout49 = ctx.eval_lut(&gin49, nand2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * gout35 + gout32;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * gout36 + gout38;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * gout39 + gout41;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * gout46 + gout44;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * gout40 + gout42;
    Ciphertext gout54 = ctx.eval_lut(&gin54, nand2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * gout47 + gout45;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * (1 - gout49) + gout48;
    Ciphertext gout56 = ctx.eval_lut(&gin56, or2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * gout49 + gout48;
    Ciphertext gout57 = ctx.eval_lut(&gin57, and2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * gout50 + gout51;
    Ciphertext gout58 = ctx.eval_lut(&gin58, and2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * gout52 + gout54;
    Ciphertext gout59 = ctx.eval_lut(&gin59, and2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * gout53 + gout55;
    Ciphertext gout60 = ctx.eval_lut(&gin60, and2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * gout51 + gout56;
    Ciphertext gout61 = ctx.eval_lut(&gin61, nand2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * gout59 + gout60;
    Ciphertext gout62 = ctx.eval_lut(&gin62, nand2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * (1 - gout58) + gout57;
    Ciphertext gout63 = ctx.eval_lut(&gin63, or2);
    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * gout57 + gout58;
    Ciphertext gout64 = ctx.eval_lut(&gin64, and2);
    std::cerr << "\rINV65   ";
    Ciphertext gout65 = 1 - gout58;

    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * gout58 + gout59;
    Ciphertext gout66 = ctx.eval_lut(&gin66, and2);
    std::cerr << "\rINV67   ";
    Ciphertext gout67 = 1 - gout60;

    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * gout64 + gout62;
    Ciphertext gout68 = ctx.eval_lut(&gin68, or2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * ct_my_string49 + gout64;
    Ciphertext gout69 = ctx.eval_lut(&gin69, nand2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * gout50 + gout61;
    Ciphertext gout70 = ctx.eval_lut(&gin70, nand2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * gout65 + gout62;
    Ciphertext gout71 = ctx.eval_lut(&gin71, or2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * gout64 + (1 - gout62);
    Ciphertext gout72 = ctx.eval_lut(&gin72, and2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * ct_my_string53 + gout64;
    Ciphertext gout73 = ctx.eval_lut(&gin73, nand2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * gout63 + gout59;
    Ciphertext gout74 = ctx.eval_lut(&gin74, nand2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * (1 - gout66) + gout60;
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rINV76   ";
    Ciphertext gout76 = 1 - gout68;

    std::cerr << "\rIMUX77  ";
    Ciphertext gin77A = 2 * ct_my_string2 + gout72;
    Ciphertext gout77A = ctx.eval_lut(&gin77A, and2);
    Ciphertext gin77B = 2 * ct_my_string58 + (1 - gout72);
    Ciphertext gout77B = ctx.eval_lut(&gin77B, and2);
    Ciphertext gout77 = gout77A + gout77B;
    std::cerr << "\rIMUX78  ";
    Ciphertext gin78A = 2 * ct_my_string0 + gout72;
    Ciphertext gout78A = ctx.eval_lut(&gin78A, and2);
    Ciphertext gin78B = 2 * ct_my_string56 + (1 - gout72);
    Ciphertext gout78B = ctx.eval_lut(&gin78B, and2);
    Ciphertext gout78 = gout78A + gout78B;
    std::cerr << "\rINV79   ";
    Ciphertext gout79 = 1 - gout72;

    std::cerr << "\rIMUX80  ";
    Ciphertext gin80A = 2 * ct_my_string4 + gout72;
    Ciphertext gout80A = ctx.eval_lut(&gin80A, and2);
    Ciphertext gin80B = 2 * ct_my_string60 + (1 - gout72);
    Ciphertext gout80B = ctx.eval_lut(&gin80B, and2);
    Ciphertext gout80 = gout80A + gout80B;
    std::cerr << "\rIMUX81  ";
    Ciphertext gin81A = 2 * ct_my_string5 + gout72;
    Ciphertext gout81A = ctx.eval_lut(&gin81A, and2);
    Ciphertext gin81B = 2 * ct_my_string61 + (1 - gout72);
    Ciphertext gout81B = ctx.eval_lut(&gin81B, and2);
    Ciphertext gout81 = gout81A + gout81B;
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * gout54 + gout70;
    Ciphertext gout82 = ctx.eval_lut(&gin82, nand2);
    std::cerr << "\rIMUX83  ";
    Ciphertext gin83A = 2 * ct_my_string6 + gout72;
    Ciphertext gout83A = ctx.eval_lut(&gin83A, and2);
    Ciphertext gin83B = 2 * ct_my_string62 + (1 - gout72);
    Ciphertext gout83B = ctx.eval_lut(&gin83B, and2);
    Ciphertext gout83 = gout83A + gout83B;
    std::cerr << "\rIMUX84  ";
    Ciphertext gin84A = 2 * ct_my_string1 + gout72;
    Ciphertext gout84A = ctx.eval_lut(&gin84A, and2);
    Ciphertext gin84B = 2 * ct_my_string57 + (1 - gout72);
    Ciphertext gout84B = ctx.eval_lut(&gin84B, and2);
    Ciphertext gout84 = gout84A + gout84B;
    std::cerr << "\rIMUX85  ";
    Ciphertext gin85A = 2 * ct_my_string3 + gout72;
    Ciphertext gout85A = ctx.eval_lut(&gin85A, and2);
    Ciphertext gin85B = 2 * ct_my_string59 + (1 - gout72);
    Ciphertext gout85B = ctx.eval_lut(&gin85B, and2);
    Ciphertext gout85 = gout85A + gout85B;
    std::cerr << "\rIMUX86  ";
    Ciphertext gin86A = 2 * ct_my_string7 + gout72;
    Ciphertext gout86A = ctx.eval_lut(&gin86A, and2);
    Ciphertext gin86B = 2 * ct_my_string63 + (1 - gout72);
    Ciphertext gout86B = ctx.eval_lut(&gin86B, and2);
    Ciphertext gout86 = gout86A + gout86B;
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * gout74 + gout60;
    Ciphertext gout87 = ctx.eval_lut(&gin87, and2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * gout67 + gout77;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * gout67 + gout78;
    Ciphertext gout89 = ctx.eval_lut(&gin89, nand2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * gout67 + gout81;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * gout52 + gout82;
    Ciphertext gout91 = ctx.eval_lut(&gin91, nand2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * gout67 + gout80;
    Ciphertext gout92 = ctx.eval_lut(&gin92, nand2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * gout67 + gout83;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * gout67 + gout85;
    Ciphertext gout94 = ctx.eval_lut(&gin94, nand2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * gout67 + gout86;
    Ciphertext gout95 = ctx.eval_lut(&gin95, nand2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * gout67 + gout84;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rINV97   ";
    Ciphertext gout97 = 1 - gout87;

    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * gout55 + gout91;
    Ciphertext gout98 = ctx.eval_lut(&gin98, nand2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * gout53 + gout98;
    Ciphertext gout99 = ctx.eval_lut(&gin99, and2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * gout87 + (1 - gout98);
    Ciphertext gout100 = ctx.eval_lut(&gin100, and2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * gout97 + (1 - gout99);
    Ciphertext gout101 = ctx.eval_lut(&gin101, and2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * gout87 + gout99;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * gout68 + gout100;
    Ciphertext gout103 = ctx.eval_lut(&gin103, and2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * gout97 + gout99;
    Ciphertext gout104 = ctx.eval_lut(&gin104, and2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * gout100 + gout66;
    Ciphertext gout105 = ctx.eval_lut(&gin105, and2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * ct_my_string14 + gout103;
    Ciphertext gout106 = ctx.eval_lut(&gin106, nand2);
    std::cerr << "\rIMUX107  ";
    Ciphertext gin107A = 2 * ct_my_string1 + gout103;
    Ciphertext gout107A = ctx.eval_lut(&gin107A, and2);
    Ciphertext gin107B = 2 * ct_my_string9 + (1 - gout103);
    Ciphertext gout107B = ctx.eval_lut(&gin107B, and2);
    Ciphertext gout107 = gout107A + gout107B;
    std::cerr << "\rIMUX108  ";
    Ciphertext gin108A = 2 * ct_my_string5 + gout103;
    Ciphertext gout108A = ctx.eval_lut(&gin108A, and2);
    Ciphertext gin108B = 2 * ct_my_string13 + (1 - gout103);
    Ciphertext gout108B = ctx.eval_lut(&gin108B, and2);
    Ciphertext gout108 = gout108A + gout108B;
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * gout68 + gout101;
    Ciphertext gout109 = ctx.eval_lut(&gin109, and2);
    std::cerr << "\rIMUX110  ";
    Ciphertext gin110A = 2 * ct_my_string6 + gout103;
    Ciphertext gout110A = ctx.eval_lut(&gin110A, and2);
    Ciphertext gin110B = 2 * ct_my_string14 + (1 - gout103);
    Ciphertext gout110B = ctx.eval_lut(&gin110B, and2);
    Ciphertext gout110 = gout110A + gout110B;
    std::cerr << "\rIMUX111  ";
    Ciphertext gin111A = 2 * ct_my_string4 + gout105;
    Ciphertext gout111A = ctx.eval_lut(&gin111A, and2);
    Ciphertext gin111B = 2 * ct_my_string44 + (1 - gout105);
    Ciphertext gout111B = ctx.eval_lut(&gin111B, and2);
    Ciphertext gout111 = gout111A + gout111B;
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * ct_my_string42 + gout105;
    Ciphertext gout112 = ctx.eval_lut(&gin112, nand2);
    std::cerr << "\rIMUX113  ";
    Ciphertext gin113A = 2 * ct_my_string4 + gout103;
    Ciphertext gout113A = ctx.eval_lut(&gin113A, and2);
    Ciphertext gin113B = 2 * ct_my_string12 + (1 - gout103);
    Ciphertext gout113B = ctx.eval_lut(&gin113B, and2);
    Ciphertext gout113 = gout113A + gout113B;
    std::cerr << "\rIMUX114  ";
    Ciphertext gin114A = 2 * ct_my_string6 + gout105;
    Ciphertext gout114A = ctx.eval_lut(&gin114A, and2);
    Ciphertext gin114B = 2 * ct_my_string46 + (1 - gout105);
    Ciphertext gout114B = ctx.eval_lut(&gin114B, and2);
    Ciphertext gout114 = gout114A + gout114B;
    std::cerr << "\rIMUX115  ";
    Ciphertext gin115A = 2 * ct_my_string2 + gout105;
    Ciphertext gout115A = ctx.eval_lut(&gin115A, and2);
    Ciphertext gin115B = 2 * ct_my_string42 + (1 - gout105);
    Ciphertext gout115B = ctx.eval_lut(&gin115B, and2);
    Ciphertext gout115 = gout115A + gout115B;
    std::cerr << "\rIMUX116  ";
    Ciphertext gin116A = 2 * ct_my_string7 + gout105;
    Ciphertext gout116A = ctx.eval_lut(&gin116A, and2);
    Ciphertext gin116B = 2 * ct_my_string47 + (1 - gout105);
    Ciphertext gout116B = ctx.eval_lut(&gin116B, and2);
    Ciphertext gout116 = gout116A + gout116B;
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * gout101 + gout75;
    Ciphertext gout117 = ctx.eval_lut(&gin117, and2);
    std::cerr << "\rIMUX118  ";
    Ciphertext gin118A = 2 * ct_my_string3 + gout105;
    Ciphertext gout118A = ctx.eval_lut(&gin118A, and2);
    Ciphertext gin118B = 2 * ct_my_string43 + (1 - gout105);
    Ciphertext gout118B = ctx.eval_lut(&gin118B, and2);
    Ciphertext gout118 = gout118A + gout118B;
    std::cerr << "\rIMUX119  ";
    Ciphertext gin119A = 2 * ct_my_string2 + gout103;
    Ciphertext gout119A = ctx.eval_lut(&gin119A, and2);
    Ciphertext gin119B = 2 * ct_my_string10 + (1 - gout103);
    Ciphertext gout119B = ctx.eval_lut(&gin119B, and2);
    Ciphertext gout119 = gout119A + gout119B;
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * ct_my_string41 + gout105;
    Ciphertext gout120 = ctx.eval_lut(&gin120, nand2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * ct_my_string44 + gout105;
    Ciphertext gout121 = ctx.eval_lut(&gin121, nand2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * ct_my_string45 + gout105;
    Ciphertext gout122 = ctx.eval_lut(&gin122, nand2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * ct_my_string47 + gout105;
    Ciphertext gout123 = ctx.eval_lut(&gin123, nand2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * ct_my_string13 + gout103;
    Ciphertext gout124 = ctx.eval_lut(&gin124, nand2);
    std::cerr << "\rIMUX125  ";
    Ciphertext gin125A = 2 * ct_my_string1 + gout105;
    Ciphertext gout125A = ctx.eval_lut(&gin125A, and2);
    Ciphertext gin125B = 2 * ct_my_string41 + (1 - gout105);
    Ciphertext gout125B = ctx.eval_lut(&gin125B, and2);
    Ciphertext gout125 = gout125A + gout125B;
    std::cerr << "\rIMUX126  ";
    Ciphertext gin126A = 2 * ct_my_string3 + gout103;
    Ciphertext gout126A = ctx.eval_lut(&gin126A, and2);
    Ciphertext gin126B = 2 * ct_my_string11 + (1 - gout103);
    Ciphertext gout126B = ctx.eval_lut(&gin126B, and2);
    Ciphertext gout126 = gout126A + gout126B;
    std::cerr << "\rIMUX127  ";
    Ciphertext gin127A = 2 * ct_my_string0 + gout103;
    Ciphertext gout127A = ctx.eval_lut(&gin127A, and2);
    Ciphertext gin127B = 2 * ct_my_string8 + (1 - gout103);
    Ciphertext gout127B = ctx.eval_lut(&gin127B, and2);
    Ciphertext gout127 = gout127A + gout127B;
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * ct_my_string15 + gout103;
    Ciphertext gout128 = ctx.eval_lut(&gin128, nand2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * ct_my_string40 + gout105;
    Ciphertext gout129 = ctx.eval_lut(&gin129, nand2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * gout76 + gout102;
    Ciphertext gout130 = ctx.eval_lut(&gin130, and2);
    std::cerr << "\rIMUX131  ";
    Ciphertext gin131A = 2 * ct_my_string7 + gout103;
    Ciphertext gout131A = ctx.eval_lut(&gin131A, and2);
    Ciphertext gin131B = 2 * ct_my_string15 + (1 - gout103);
    Ciphertext gout131B = ctx.eval_lut(&gin131B, and2);
    Ciphertext gout131 = gout131A + gout131B;
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * ct_my_string9 + gout103;
    Ciphertext gout132 = ctx.eval_lut(&gin132, nand2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * gout68 + gout102;
    Ciphertext gout133 = ctx.eval_lut(&gin133, and2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * ct_my_string11 + gout103;
    Ciphertext gout134 = ctx.eval_lut(&gin134, nand2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * ct_my_string10 + gout103;
    Ciphertext gout135 = ctx.eval_lut(&gin135, nand2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * gout104 + gout68;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_my_string46 + gout105;
    Ciphertext gout137 = ctx.eval_lut(&gin137, nand2);
    std::cerr << "\rIMUX138  ";
    Ciphertext gin138A = 2 * ct_my_string0 + gout105;
    Ciphertext gout138A = ctx.eval_lut(&gin138A, and2);
    Ciphertext gin138B = 2 * ct_my_string40 + (1 - gout105);
    Ciphertext gout138B = ctx.eval_lut(&gin138B, and2);
    Ciphertext gout138 = gout138A + gout138B;
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_my_string12 + gout103;
    Ciphertext gout139 = ctx.eval_lut(&gin139, nand2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * ct_my_string43 + gout105;
    Ciphertext gout140 = ctx.eval_lut(&gin140, nand2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * gout62 + (1 - gout102);
    Ciphertext gout141 = ctx.eval_lut(&gin141, and2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_my_string8 + gout103;
    Ciphertext gout142 = ctx.eval_lut(&gin142, nand2);
    std::cerr << "\rIMUX143  ";
    Ciphertext gin143A = 2 * ct_my_string5 + gout105;
    Ciphertext gout143A = ctx.eval_lut(&gin143A, and2);
    Ciphertext gin143B = 2 * ct_my_string45 + (1 - gout105);
    Ciphertext gout143B = ctx.eval_lut(&gin143B, and2);
    Ciphertext gout143 = gout143A + gout143B;
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * gout104 + gout76;
    Ciphertext gout144 = ctx.eval_lut(&gin144, and2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * ct_my_string60 + gout109;
    Ciphertext gout145 = ctx.eval_lut(&gin145, nand2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * ct_my_string58 + gout109;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_my_string62 + gout109;
    Ciphertext gout147 = ctx.eval_lut(&gin147, nand2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * gout106 + gout93;
    Ciphertext gout148 = ctx.eval_lut(&gin148, and2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * ct_my_string59 + gout109;
    Ciphertext gout149 = ctx.eval_lut(&gin149, nand2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * ct_my_string56 + gout109;
    Ciphertext gout150 = ctx.eval_lut(&gin150, nand2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_my_string30 + gout117;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rIMUX152  ";
    Ciphertext gin152A = 2 * ct_my_string4 + gout117;
    Ciphertext gout152A = ctx.eval_lut(&gin152A, and2);
    Ciphertext gin152B = 2 * ct_my_string28 + (1 - gout117);
    Ciphertext gout152B = ctx.eval_lut(&gin152B, and2);
    Ciphertext gout152 = gout152A + gout152B;
    std::cerr << "\rIMUX153  ";
    Ciphertext gin153A = 2 * ct_my_string6 + gout117;
    Ciphertext gout153A = ctx.eval_lut(&gin153A, and2);
    Ciphertext gin153B = 2 * ct_my_string30 + (1 - gout117);
    Ciphertext gout153B = ctx.eval_lut(&gin153B, and2);
    Ciphertext gout153 = gout153A + gout153B;
    std::cerr << "\rIMUX154  ";
    Ciphertext gin154A = 2 * ct_my_string2 + gout117;
    Ciphertext gout154A = ctx.eval_lut(&gin154A, and2);
    Ciphertext gin154B = 2 * ct_my_string26 + (1 - gout117);
    Ciphertext gout154B = ctx.eval_lut(&gin154B, and2);
    Ciphertext gout154 = gout154A + gout154B;
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * ct_my_string57 + gout109;
    Ciphertext gout155 = ctx.eval_lut(&gin155, nand2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * ct_my_string28 + gout117;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rIMUX157  ";
    Ciphertext gin157A = 2 * ct_my_string7 + gout130;
    Ciphertext gout157A = ctx.eval_lut(&gin157A, and2);
    Ciphertext gin157B = 2 * ct_my_string55 + (1 - gout130);
    Ciphertext gout157B = ctx.eval_lut(&gin157B, and2);
    Ciphertext gout157 = gout157A + gout157B;
    std::cerr << "\rIMUX158  ";
    Ciphertext gin158A = 2 * ct_my_string1 + gout117;
    Ciphertext gout158A = ctx.eval_lut(&gin158A, and2);
    Ciphertext gin158B = 2 * ct_my_string25 + (1 - gout117);
    Ciphertext gout158B = ctx.eval_lut(&gin158B, and2);
    Ciphertext gout158 = gout158A + gout158B;
    std::cerr << "\rIMUX159  ";
    Ciphertext gin159A = 2 * ct_my_string4 + gout130;
    Ciphertext gout159A = ctx.eval_lut(&gin159A, and2);
    Ciphertext gin159B = 2 * ct_my_string52 + (1 - gout130);
    Ciphertext gout159B = ctx.eval_lut(&gin159B, and2);
    Ciphertext gout159 = gout159A + gout159B;
    std::cerr << "\rIMUX160  ";
    Ciphertext gin160A = 2 * gout131 + gout130;
    Ciphertext gout160A = ctx.eval_lut(&gin160A, and2);
    Ciphertext gin160B = 2 * gout116 + (1 - gout130);
    Ciphertext gout160B = ctx.eval_lut(&gin160B, and2);
    Ciphertext gout160 = gout160A + gout160B;
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * ct_my_string53 + gout130;
    Ciphertext gout161 = ctx.eval_lut(&gin161, nand2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * ct_my_string61 + gout109;
    Ciphertext gout162 = ctx.eval_lut(&gin162, nand2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_my_string31 + gout117;
    Ciphertext gout163 = ctx.eval_lut(&gin163, nand2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_my_string3 + gout136;
    Ciphertext gout164 = ctx.eval_lut(&gin164, nand2);
    std::cerr << "\rIMUX165  ";
    Ciphertext gin165A = 2 * gout126 + gout130;
    Ciphertext gout165A = ctx.eval_lut(&gin165A, and2);
    Ciphertext gin165B = 2 * gout118 + (1 - gout130);
    Ciphertext gout165B = ctx.eval_lut(&gin165B, and2);
    Ciphertext gout165 = gout165A + gout165B;
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * ct_my_string63 + gout109;
    Ciphertext gout166 = ctx.eval_lut(&gin166, nand2);
    std::cerr << "\rIMUX167  ";
    Ciphertext gin167A = 2 * ct_my_string5 + gout130;
    Ciphertext gout167A = ctx.eval_lut(&gin167A, and2);
    Ciphertext gin167B = 2 * ct_my_string53 + (1 - gout130);
    Ciphertext gout167B = ctx.eval_lut(&gin167B, and2);
    Ciphertext gout167 = gout167A + gout167B;
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * ct_my_string55 + gout130;
    Ciphertext gout168 = ctx.eval_lut(&gin168, nand2);
    std::cerr << "\rIMUX169  ";
    Ciphertext gin169A = 2 * gout131 + gout72;
    Ciphertext gout169A = ctx.eval_lut(&gin169A, and2);
    Ciphertext gin169B = 2 * gout157 + (1 - gout72);
    Ciphertext gout169B = ctx.eval_lut(&gin169B, and2);
    Ciphertext gout169 = gout169A + gout169B;
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * ct_my_string21 + gout133;
    Ciphertext gout170 = ctx.eval_lut(&gin170, nand2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * gout130 + gout138;
    Ciphertext gout171 = ctx.eval_lut(&gin171, nand2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * ct_my_string51 + gout130;
    Ciphertext gout172 = ctx.eval_lut(&gin172, nand2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * ct_my_string1 + gout136;
    Ciphertext gout173 = ctx.eval_lut(&gin173, nand2);
    std::cerr << "\rIMUX174  ";
    Ciphertext gin174A = 2 * gout113 + gout130;
    Ciphertext gout174A = ctx.eval_lut(&gin174A, and2);
    Ciphertext gin174B = 2 * gout111 + (1 - gout130);
    Ciphertext gout174B = ctx.eval_lut(&gin174B, and2);
    Ciphertext gout174 = gout174A + gout174B;
    std::cerr << "\rIMUX175  ";
    Ciphertext gin175A = 2 * gout107 + gout130;
    Ciphertext gout175A = ctx.eval_lut(&gin175A, and2);
    Ciphertext gin175B = 2 * gout125 + (1 - gout130);
    Ciphertext gout175B = ctx.eval_lut(&gin175B, and2);
    Ciphertext gout175 = gout175A + gout175B;
    std::cerr << "\rIMUX176  ";
    Ciphertext gin176A = 2 * ct_my_string0 + gout117;
    Ciphertext gout176A = ctx.eval_lut(&gin176A, and2);
    Ciphertext gin176B = 2 * ct_my_string24 + (1 - gout117);
    Ciphertext gout176B = ctx.eval_lut(&gin176B, and2);
    Ciphertext gout176 = gout176A + gout176B;
    std::cerr << "\rIMUX177  ";
    Ciphertext gin177A = 2 * ct_my_string6 + gout130;
    Ciphertext gout177A = ctx.eval_lut(&gin177A, and2);
    Ciphertext gin177B = 2 * ct_my_string54 + (1 - gout130);
    Ciphertext gout177B = ctx.eval_lut(&gin177B, and2);
    Ciphertext gout177 = gout177A + gout177B;
    std::cerr << "\rIMUX178  ";
    Ciphertext gin178A = 2 * ct_my_string2 + gout130;
    Ciphertext gout178A = ctx.eval_lut(&gin178A, and2);
    Ciphertext gin178B = 2 * ct_my_string50 + (1 - gout130);
    Ciphertext gout178B = ctx.eval_lut(&gin178B, and2);
    Ciphertext gout178 = gout178A + gout178B;
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * ct_my_string49 + gout130;
    Ciphertext gout179 = ctx.eval_lut(&gin179, nand2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * ct_my_string25 + gout117;
    Ciphertext gout180 = ctx.eval_lut(&gin180, nand2);
    std::cerr << "\rIMUX181  ";
    Ciphertext gin181A = 2 * ct_my_string0 + gout133;
    Ciphertext gout181A = ctx.eval_lut(&gin181A, and2);
    Ciphertext gin181B = 2 * ct_my_string16 + (1 - gout133);
    Ciphertext gout181B = ctx.eval_lut(&gin181B, and2);
    Ciphertext gout181 = gout181A + gout181B;
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * ct_my_string19 + gout133;
    Ciphertext gout182 = ctx.eval_lut(&gin182, nand2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * ct_my_string16 + gout133;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * ct_my_string6 + gout136;
    Ciphertext gout184 = ctx.eval_lut(&gin184, nand2);
    std::cerr << "\rIMUX185  ";
    Ciphertext gin185A = 2 * ct_my_string7 + gout133;
    Ciphertext gout185A = ctx.eval_lut(&gin185A, and2);
    Ciphertext gin185B = 2 * ct_my_string23 + (1 - gout133);
    Ciphertext gout185B = ctx.eval_lut(&gin185B, and2);
    Ciphertext gout185 = gout185A + gout185B;
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * ct_my_string22 + gout133;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * gout135 + gout88;
    Ciphertext gout187 = ctx.eval_lut(&gin187, and2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * gout122 + gout124;
    Ciphertext gout188 = ctx.eval_lut(&gin188, and2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * gout123 + gout128;
    Ciphertext gout189 = ctx.eval_lut(&gin189, and2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * ct_my_string4 + gout136;
    Ciphertext gout190 = ctx.eval_lut(&gin190, nand2);
    std::cerr << "\rIMUX191  ";
    Ciphertext gin191A = 2 * gout119 + gout130;
    Ciphertext gout191A = ctx.eval_lut(&gin191A, and2);
    Ciphertext gin191B = 2 * gout115 + (1 - gout130);
    Ciphertext gout191B = ctx.eval_lut(&gin191B, and2);
    Ciphertext gout191 = gout191A + gout191B;
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * ct_my_string2 + gout136;
    Ciphertext gout192 = ctx.eval_lut(&gin192, nand2);
    std::cerr << "\rIMUX193  ";
    Ciphertext gin193A = 2 * ct_my_string3 + gout133;
    Ciphertext gout193A = ctx.eval_lut(&gin193A, and2);
    Ciphertext gin193B = 2 * ct_my_string19 + (1 - gout133);
    Ciphertext gout193B = ctx.eval_lut(&gin193B, and2);
    Ciphertext gout193 = gout193A + gout193B;
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * ct_my_string29 + gout117;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * ct_my_string23 + gout133;
    Ciphertext gout195 = ctx.eval_lut(&gin195, nand2);
    std::cerr << "\rIMUX196  ";
    Ciphertext gin196A = 2 * ct_my_string5 + gout117;
    Ciphertext gout196A = ctx.eval_lut(&gin196A, and2);
    Ciphertext gin196B = 2 * ct_my_string29 + (1 - gout117);
    Ciphertext gout196B = ctx.eval_lut(&gin196B, and2);
    Ciphertext gout196 = gout196A + gout196B;
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * ct_my_string17 + gout133;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * ct_my_string27 + gout117;
    Ciphertext gout198 = ctx.eval_lut(&gin198, nand2);
    std::cerr << "\rIMUX199  ";
    Ciphertext gin199A = 2 * gout110 + gout130;
    Ciphertext gout199A = ctx.eval_lut(&gin199A, and2);
    Ciphertext gin199B = 2 * gout114 + (1 - gout130);
    Ciphertext gout199B = ctx.eval_lut(&gin199B, and2);
    Ciphertext gout199 = gout199A + gout199B;
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * ct_my_string20 + gout133;
    Ciphertext gout200 = ctx.eval_lut(&gin200, nand2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout124 + gout90;
    Ciphertext gout201 = ctx.eval_lut(&gin201, and2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * ct_my_string5 + gout136;
    Ciphertext gout202 = ctx.eval_lut(&gin202, nand2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout142 + gout89;
    Ciphertext gout203 = ctx.eval_lut(&gin203, and2);
    std::cerr << "\rIMUX204  ";
    Ciphertext gin204A = 2 * ct_my_string7 + gout117;
    Ciphertext gout204A = ctx.eval_lut(&gin204A, and2);
    Ciphertext gin204B = 2 * ct_my_string31 + (1 - gout117);
    Ciphertext gout204B = ctx.eval_lut(&gin204B, and2);
    Ciphertext gout204 = gout204A + gout204B;
    std::cerr << "\rIMUX205  ";
    Ciphertext gin205A = 2 * ct_my_string1 + gout133;
    Ciphertext gout205A = ctx.eval_lut(&gin205A, and2);
    Ciphertext gin205B = 2 * ct_my_string17 + (1 - gout133);
    Ciphertext gout205B = ctx.eval_lut(&gin205B, and2);
    Ciphertext gout205 = gout205A + gout205B;
    std::cerr << "\rIMUX206  ";
    Ciphertext gin206A = 2 * ct_my_string4 + gout133;
    Ciphertext gout206A = ctx.eval_lut(&gin206A, and2);
    Ciphertext gin206B = 2 * ct_my_string20 + (1 - gout133);
    Ciphertext gout206B = ctx.eval_lut(&gin206B, and2);
    Ciphertext gout206 = gout206A + gout206B;
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * ct_my_string33 + gout144;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * ct_my_string24 + gout117;
    Ciphertext gout208 = ctx.eval_lut(&gin208, nand2);
    std::cerr << "\rIMUX209  ";
    Ciphertext gin209A = 2 * gout127 + gout130;
    Ciphertext gout209A = ctx.eval_lut(&gin209A, and2);
    Ciphertext gin209B = 2 * gout138 + (1 - gout130);
    Ciphertext gout209B = ctx.eval_lut(&gin209B, and2);
    Ciphertext gout209 = gout209A + gout209B;
    std::cerr << "\rIMUX210  ";
    Ciphertext gin210A = 2 * ct_my_string0 + gout130;
    Ciphertext gout210A = ctx.eval_lut(&gin210A, and2);
    Ciphertext gin210B = 2 * ct_my_string48 + (1 - gout130);
    Ciphertext gout210B = ctx.eval_lut(&gin210B, and2);
    Ciphertext gout210 = gout210A + gout210B;
    std::cerr << "\rIMUX211  ";
    Ciphertext gin211A = 2 * ct_my_string1 + gout144;
    Ciphertext gout211A = ctx.eval_lut(&gin211A, and2);
    Ciphertext gin211B = 2 * ct_my_string33 + (1 - gout144);
    Ciphertext gout211B = ctx.eval_lut(&gin211B, and2);
    Ciphertext gout211 = gout211A + gout211B;
    std::cerr << "\rIMUX212  ";
    Ciphertext gin212A = 2 * ct_my_string5 + gout133;
    Ciphertext gout212A = ctx.eval_lut(&gin212A, and2);
    Ciphertext gin212B = 2 * ct_my_string21 + (1 - gout133);
    Ciphertext gout212B = ctx.eval_lut(&gin212B, and2);
    Ciphertext gout212 = gout212A + gout212B;
    std::cerr << "\rIMUX213  ";
    Ciphertext gin213A = 2 * ct_my_string1 + gout130;
    Ciphertext gout213A = ctx.eval_lut(&gin213A, and2);
    Ciphertext gin213B = 2 * ct_my_string49 + (1 - gout130);
    Ciphertext gout213B = ctx.eval_lut(&gin213B, and2);
    Ciphertext gout213 = gout213A + gout213B;
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * ct_my_string48 + gout130;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout130 + gout116;
    Ciphertext gout215 = ctx.eval_lut(&gin215, nand2);
    std::cerr << "\rIMUX216  ";
    Ciphertext gin216A = 2 * ct_my_string6 + gout133;
    Ciphertext gout216A = ctx.eval_lut(&gin216A, and2);
    Ciphertext gin216B = 2 * ct_my_string22 + (1 - gout133);
    Ciphertext gout216B = ctx.eval_lut(&gin216B, and2);
    Ciphertext gout216 = gout216A + gout216B;
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout112 + gout135;
    Ciphertext gout217 = ctx.eval_lut(&gin217, and2);
    std::cerr << "\rIMUX218  ";
    Ciphertext gin218A = 2 * ct_my_string2 + gout144;
    Ciphertext gout218A = ctx.eval_lut(&gin218A, and2);
    Ciphertext gin218B = 2 * ct_my_string34 + (1 - gout144);
    Ciphertext gout218B = ctx.eval_lut(&gin218B, and2);
    Ciphertext gout218 = gout218A + gout218B;
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * ct_my_string54 + gout130;
    Ciphertext gout219 = ctx.eval_lut(&gin219, nand2);
    std::cerr << "\rIMUX220  ";
    Ciphertext gin220A = 2 * ct_my_string3 + gout130;
    Ciphertext gout220A = ctx.eval_lut(&gin220A, and2);
    Ciphertext gin220B = 2 * ct_my_string51 + (1 - gout130);
    Ciphertext gout220B = ctx.eval_lut(&gin220B, and2);
    Ciphertext gout220 = gout220A + gout220B;
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * ct_my_string52 + gout130;
    Ciphertext gout221 = ctx.eval_lut(&gin221, nand2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * ct_my_string0 + gout136;
    Ciphertext gout222 = ctx.eval_lut(&gin222, nand2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout132 + gout96;
    Ciphertext gout223 = ctx.eval_lut(&gin223, and2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout120 + gout132;
    Ciphertext gout224 = ctx.eval_lut(&gin224, and2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * ct_my_string50 + gout130;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rIMUX226  ";
    Ciphertext gin226A = 2 * ct_my_string2 + gout133;
    Ciphertext gout226A = ctx.eval_lut(&gin226A, and2);
    Ciphertext gin226B = 2 * ct_my_string18 + (1 - gout133);
    Ciphertext gout226B = ctx.eval_lut(&gin226B, and2);
    Ciphertext gout226 = gout226A + gout226B;
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout130 + gout143;
    Ciphertext gout227 = ctx.eval_lut(&gin227, nand2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout77 + gout141;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * ct_my_string32 + gout144;
    Ciphertext gout229 = ctx.eval_lut(&gin229, nand2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * ct_my_string7 + gout136;
    Ciphertext gout230 = ctx.eval_lut(&gin230, nand2);
    std::cerr << "\rIMUX231  ";
    Ciphertext gin231A = 2 * gout108 + gout130;
    Ciphertext gout231A = ctx.eval_lut(&gin231A, and2);
    Ciphertext gin231B = 2 * gout143 + (1 - gout130);
    Ciphertext gout231B = ctx.eval_lut(&gin231B, and2);
    Ciphertext gout231 = gout231A + gout231B;
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout144 + gout196;
    Ciphertext gout232 = ctx.eval_lut(&gin232, nand2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout139 + gout92;
    Ciphertext gout233 = ctx.eval_lut(&gin233, and2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * ct_my_string38 + gout144;
    Ciphertext gout234 = ctx.eval_lut(&gin234, nand2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout144 + gout158;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout121 + gout139;
    Ciphertext gout236 = ctx.eval_lut(&gin236, and2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * ct_my_string37 + gout144;
    Ciphertext gout237 = ctx.eval_lut(&gin237, nand2);
    std::cerr << "\rIMUX238  ";
    Ciphertext gin238A = 2 * gout119 + gout144;
    Ciphertext gout238A = ctx.eval_lut(&gin238A, and2);
    Ciphertext gin238B = 2 * gout154 + (1 - gout144);
    Ciphertext gout238B = ctx.eval_lut(&gin238B, and2);
    Ciphertext gout238 = gout238A + gout238B;
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout144 + gout176;
    Ciphertext gout239 = ctx.eval_lut(&gin239, nand2);
    std::cerr << "\rIMUX240  ";
    Ciphertext gin240A = 2 * gout131 + gout144;
    Ciphertext gout240A = ctx.eval_lut(&gin240A, and2);
    Ciphertext gin240B = 2 * gout204 + (1 - gout144);
    Ciphertext gout240B = ctx.eval_lut(&gin240B, and2);
    Ciphertext gout240 = gout240A + gout240B;
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout83 + gout141;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout85 + gout141;
    Ciphertext gout242 = ctx.eval_lut(&gin242, nand2);
    std::cerr << "\rIMUX243  ";
    Ciphertext gin243A = 2 * ct_my_string7 + gout144;
    Ciphertext gout243A = ctx.eval_lut(&gin243A, and2);
    Ciphertext gin243B = 2 * ct_my_string39 + (1 - gout144);
    Ciphertext gout243B = ctx.eval_lut(&gin243B, and2);
    Ciphertext gout243 = gout243A + gout243B;
    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * gout81 + gout141;
    Ciphertext gout244 = ctx.eval_lut(&gin244, nand2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * gout144 + gout154;
    Ciphertext gout245 = ctx.eval_lut(&gin245, nand2);
    std::cerr << "\rIMUX246  ";
    Ciphertext gin246A = 2 * ct_my_string6 + gout144;
    Ciphertext gout246A = ctx.eval_lut(&gin246A, and2);
    Ciphertext gin246B = 2 * ct_my_string38 + (1 - gout144);
    Ciphertext gout246B = ctx.eval_lut(&gin246B, and2);
    Ciphertext gout246 = gout246A + gout246B;
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * ct_my_string35 + gout144;
    Ciphertext gout247 = ctx.eval_lut(&gin247, nand2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout137 + gout106;
    Ciphertext gout248 = ctx.eval_lut(&gin248, and2);
    std::cerr << "\rIMUX249  ";
    Ciphertext gin249A = 2 * ct_my_string3 + gout117;
    Ciphertext gout249A = ctx.eval_lut(&gin249A, and2);
    Ciphertext gin249B = 2 * ct_my_string27 + (1 - gout117);
    Ciphertext gout249B = ctx.eval_lut(&gin249B, and2);
    Ciphertext gout249 = gout249A + gout249B;
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * ct_my_string26 + gout117;
    Ciphertext gout250 = ctx.eval_lut(&gin250, nand2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout130 + gout125;
    Ciphertext gout251 = ctx.eval_lut(&gin251, nand2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * ct_my_string36 + gout144;
    Ciphertext gout252 = ctx.eval_lut(&gin252, nand2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout130 + gout118;
    Ciphertext gout253 = ctx.eval_lut(&gin253, nand2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * ct_my_string18 + gout133;
    Ciphertext gout254 = ctx.eval_lut(&gin254, nand2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout128 + gout95;
    Ciphertext gout255 = ctx.eval_lut(&gin255, and2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout130 + gout115;
    Ciphertext gout256 = ctx.eval_lut(&gin256, nand2);
    std::cerr << "\rIMUX257  ";
    Ciphertext gin257A = 2 * ct_my_string5 + gout144;
    Ciphertext gout257A = ctx.eval_lut(&gin257A, and2);
    Ciphertext gin257B = 2 * ct_my_string37 + (1 - gout144);
    Ciphertext gout257B = ctx.eval_lut(&gin257B, and2);
    Ciphertext gout257 = gout257A + gout257B;
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * ct_my_string34 + gout144;
    Ciphertext gout258 = ctx.eval_lut(&gin258, nand2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout130 + gout111;
    Ciphertext gout259 = ctx.eval_lut(&gin259, nand2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * ct_my_string39 + gout144;
    Ciphertext gout260 = ctx.eval_lut(&gin260, nand2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout134 + gout94;
    Ciphertext gout261 = ctx.eval_lut(&gin261, and2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout86 + gout141;
    Ciphertext gout262 = ctx.eval_lut(&gin262, nand2);
    std::cerr << "\rIMUX263  ";
    Ciphertext gin263A = 2 * gout108 + gout144;
    Ciphertext gout263A = ctx.eval_lut(&gin263A, and2);
    Ciphertext gin263B = 2 * gout196 + (1 - gout144);
    Ciphertext gout263B = ctx.eval_lut(&gin263B, and2);
    Ciphertext gout263 = gout263A + gout263B;
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout84 + gout141;
    Ciphertext gout264 = ctx.eval_lut(&gin264, nand2);
    std::cerr << "\rIMUX265  ";
    Ciphertext gin265A = 2 * ct_my_string0 + gout144;
    Ciphertext gout265A = ctx.eval_lut(&gin265A, and2);
    Ciphertext gin265B = 2 * ct_my_string32 + (1 - gout144);
    Ciphertext gout265B = ctx.eval_lut(&gin265B, and2);
    Ciphertext gout265 = gout265A + gout265B;
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout130 + gout114;
    Ciphertext gout266 = ctx.eval_lut(&gin266, nand2);
    std::cerr << "\rIMUX267  ";
    Ciphertext gin267A = 2 * gout127 + gout144;
    Ciphertext gout267A = ctx.eval_lut(&gin267A, and2);
    Ciphertext gin267B = 2 * gout176 + (1 - gout144);
    Ciphertext gout267B = ctx.eval_lut(&gin267B, and2);
    Ciphertext gout267 = gout267A + gout267B;
    std::cerr << "\rIMUX268  ";
    Ciphertext gin268A = 2 * gout107 + gout144;
    Ciphertext gout268A = ctx.eval_lut(&gin268A, and2);
    Ciphertext gin268B = 2 * gout158 + (1 - gout144);
    Ciphertext gout268B = ctx.eval_lut(&gin268B, and2);
    Ciphertext gout268 = gout268A + gout268B;
    std::cerr << "\rIMUX269  ";
    Ciphertext gin269A = 2 * ct_my_string3 + gout144;
    Ciphertext gout269A = ctx.eval_lut(&gin269A, and2);
    Ciphertext gin269B = 2 * ct_my_string35 + (1 - gout144);
    Ciphertext gout269B = ctx.eval_lut(&gin269B, and2);
    Ciphertext gout269 = gout269A + gout269B;
    std::cerr << "\rLUT270   ";
    Ciphertext gin270 = 2 * gout80 + gout141;
    Ciphertext gout270 = ctx.eval_lut(&gin270, nand2);
    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout140 + gout134;
    Ciphertext gout271 = ctx.eval_lut(&gin271, and2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout144 + gout204;
    Ciphertext gout272 = ctx.eval_lut(&gin272, nand2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout78 + gout141;
    Ciphertext gout273 = ctx.eval_lut(&gin273, nand2);
    std::cerr << "\rIMUX274  ";
    Ciphertext gin274A = 2 * ct_my_string4 + gout144;
    Ciphertext gout274A = ctx.eval_lut(&gin274A, and2);
    Ciphertext gin274B = 2 * ct_my_string36 + (1 - gout144);
    Ciphertext gout274B = ctx.eval_lut(&gin274B, and2);
    Ciphertext gout274 = gout274A + gout274B;
    std::cerr << "\rIMUX275  ";
    Ciphertext gin275A = 2 * gout110 + gout144;
    Ciphertext gout275A = ctx.eval_lut(&gin275A, and2);
    Ciphertext gin275B = 2 * gout153 + (1 - gout144);
    Ciphertext gout275B = ctx.eval_lut(&gin275B, and2);
    Ciphertext gout275 = gout275A + gout275B;
    std::cerr << "\rIMUX276  ";
    Ciphertext gin276A = 2 * gout113 + gout144;
    Ciphertext gout276A = ctx.eval_lut(&gin276A, and2);
    Ciphertext gin276B = 2 * gout152 + (1 - gout144);
    Ciphertext gout276B = ctx.eval_lut(&gin276B, and2);
    Ciphertext gout276 = gout276A + gout276B;
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout129 + gout142;
    Ciphertext gout277 = ctx.eval_lut(&gin277, and2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout144 + gout153;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout144 + gout152;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout72 + gout160;
    Ciphertext gout280 = ctx.eval_lut(&gin280, nand2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout72 + gout165;
    Ciphertext gout281 = ctx.eval_lut(&gin281, nand2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout72 + gout174;
    Ciphertext gout282 = ctx.eval_lut(&gin282, nand2);
    std::cerr << "\rIMUX283  ";
    Ciphertext gin283A = 2 * gout119 + gout72;
    Ciphertext gout283A = ctx.eval_lut(&gin283A, and2);
    Ciphertext gin283B = 2 * gout178 + (1 - gout72);
    Ciphertext gout283B = ctx.eval_lut(&gin283B, and2);
    Ciphertext gout283 = gout283A + gout283B;
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout72 + gout157;
    Ciphertext gout284 = ctx.eval_lut(&gin284, nand2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout166 + gout168;
    Ciphertext gout285 = ctx.eval_lut(&gin285, and2);
    std::cerr << "\rIMUX286  ";
    Ciphertext gin286A = 2 * gout110 + gout72;
    Ciphertext gout286A = ctx.eval_lut(&gin286A, and2);
    Ciphertext gin286B = 2 * gout177 + (1 - gout72);
    Ciphertext gout286B = ctx.eval_lut(&gin286B, and2);
    Ciphertext gout286 = gout286A + gout286B;
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout179 + gout180;
    Ciphertext gout287 = ctx.eval_lut(&gin287, and2);
    std::cerr << "\rIMUX288  ";
    Ciphertext gin288A = 2 * gout108 + gout117;
    Ciphertext gout288A = ctx.eval_lut(&gin288A, and2);
    Ciphertext gin288B = 2 * gout212 + (1 - gout117);
    Ciphertext gout288B = ctx.eval_lut(&gin288B, and2);
    Ciphertext gout288 = gout288A + gout288B;
    std::cerr << "\rIMUX289  ";
    Ciphertext gin289A = 2 * gout127 + gout117;
    Ciphertext gout289A = ctx.eval_lut(&gin289A, and2);
    Ciphertext gin289B = 2 * gout181 + (1 - gout117);
    Ciphertext gout289B = ctx.eval_lut(&gin289B, and2);
    Ciphertext gout289 = gout289A + gout289B;
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout72 + gout159;
    Ciphertext gout290 = ctx.eval_lut(&gin290, nand2);
    std::cerr << "\rIMUX291  ";
    Ciphertext gin291A = 2 * gout107 + gout105;
    Ciphertext gout291A = ctx.eval_lut(&gin291A, and2);
    Ciphertext gin291B = 2 * gout211 + (1 - gout105);
    Ciphertext gout291B = ctx.eval_lut(&gin291B, and2);
    Ciphertext gout291 = gout291A + gout291B;
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout172 + gout182;
    Ciphertext gout292 = ctx.eval_lut(&gin292, and2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout72 + gout210;
    Ciphertext gout293 = ctx.eval_lut(&gin293, nand2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout72 + gout231;
    Ciphertext gout294 = ctx.eval_lut(&gin294, nand2);
    std::cerr << "\rIMUX295  ";
    Ciphertext gin295A = 2 * gout113 + gout117;
    Ciphertext gout295A = ctx.eval_lut(&gin295A, and2);
    Ciphertext gin295B = 2 * gout206 + (1 - gout117);
    Ciphertext gout295B = ctx.eval_lut(&gin295B, and2);
    Ciphertext gout295 = gout295A + gout295B;
    std::cerr << "\rIMUX296  ";
    Ciphertext gin296A = 2 * gout126 + gout117;
    Ciphertext gout296A = ctx.eval_lut(&gin296A, and2);
    Ciphertext gin296B = 2 * gout193 + (1 - gout117);
    Ciphertext gout296B = ctx.eval_lut(&gin296B, and2);
    Ciphertext gout296 = gout296A + gout296B;
    std::cerr << "\rIMUX297  ";
    Ciphertext gin297A = 2 * gout110 + gout117;
    Ciphertext gout297A = ctx.eval_lut(&gin297A, and2);
    Ciphertext gin297B = 2 * gout216 + (1 - gout117);
    Ciphertext gout297B = ctx.eval_lut(&gin297B, and2);
    Ciphertext gout297 = gout297A + gout297B;
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout224 + gout155;
    Ciphertext gout298 = ctx.eval_lut(&gin298, and2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout105 + gout240;
    Ciphertext gout299 = ctx.eval_lut(&gin299, nand2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout117 + gout193;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rIMUX301  ";
    Ciphertext gin301A = 2 * gout107 + gout72;
    Ciphertext gout301A = ctx.eval_lut(&gin301A, and2);
    Ciphertext gin301B = 2 * gout213 + (1 - gout72);
    Ciphertext gout301B = ctx.eval_lut(&gin301B, and2);
    Ciphertext gout301 = gout301A + gout301B;
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout117 + gout226;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout227 + gout201;
    Ciphertext gout303 = ctx.eval_lut(&gin303, and2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout117 + gout205;
    Ciphertext gout304 = ctx.eval_lut(&gin304, nand2);
    std::cerr << "\rIMUX305  ";
    Ciphertext gin305A = 2 * gout113 + gout72;
    Ciphertext gout305A = ctx.eval_lut(&gin305A, and2);
    Ciphertext gin305B = 2 * gout159 + (1 - gout72);
    Ciphertext gout305B = ctx.eval_lut(&gin305B, and2);
    Ciphertext gout305 = gout305A + gout305B;
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout214 + gout183;
    Ciphertext gout306 = ctx.eval_lut(&gin306, and2);
    std::cerr << "\rIMUX307  ";
    Ciphertext gin307A = 2 * gout119 + gout117;
    Ciphertext gout307A = ctx.eval_lut(&gin307A, and2);
    Ciphertext gin307B = 2 * gout226 + (1 - gout117);
    Ciphertext gout307B = ctx.eval_lut(&gin307B, and2);
    Ciphertext gout307 = gout307A + gout307B;
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout117 + gout181;
    Ciphertext gout308 = ctx.eval_lut(&gin308, nand2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout72 + gout175;
    Ciphertext gout309 = ctx.eval_lut(&gin309, nand2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout72 + gout209;
    Ciphertext gout310 = ctx.eval_lut(&gin310, nand2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout162 + gout188;
    Ciphertext gout311 = ctx.eval_lut(&gin311, and2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout163 + gout189;
    Ciphertext gout312 = ctx.eval_lut(&gin312, and2);
    std::cerr << "\rIMUX313  ";
    Ciphertext gin313A = 2 * gout126 + gout144;
    Ciphertext gout313A = ctx.eval_lut(&gin313A, and2);
    Ciphertext gin313B = 2 * gout249 + (1 - gout144);
    Ciphertext gout313B = ctx.eval_lut(&gin313B, and2);
    Ciphertext gout313 = gout313A + gout313B;
    std::cerr << "\rIMUX314  ";
    Ciphertext gin314A = 2 * gout108 + gout72;
    Ciphertext gout314A = ctx.eval_lut(&gin314A, and2);
    Ciphertext gin314B = 2 * gout167 + (1 - gout72);
    Ciphertext gout314B = ctx.eval_lut(&gin314B, and2);
    Ciphertext gout314 = gout314A + gout314B;
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout72 + gout199;
    Ciphertext gout315 = ctx.eval_lut(&gin315, nand2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout252 + gout156;
    Ciphertext gout316 = ctx.eval_lut(&gin316, and2);
    std::cerr << "\rIMUX317  ";
    Ciphertext gin317A = 2 * gout131 + gout117;
    Ciphertext gout317A = ctx.eval_lut(&gin317A, and2);
    Ciphertext gin317B = 2 * gout185 + (1 - gout117);
    Ciphertext gout317B = ctx.eval_lut(&gin317B, and2);
    Ciphertext gout317 = gout317A + gout317B;
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout151 + gout219;
    Ciphertext gout318 = ctx.eval_lut(&gin318, and2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout170 + gout244;
    Ciphertext gout319 = ctx.eval_lut(&gin319, and2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout237 + gout194;
    Ciphertext gout320 = ctx.eval_lut(&gin320, and2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout260 + gout195;
    Ciphertext gout321 = ctx.eval_lut(&gin321, and2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout182 + gout242;
    Ciphertext gout322 = ctx.eval_lut(&gin322, and2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout146 + gout217;
    Ciphertext gout323 = ctx.eval_lut(&gin323, and2);
    std::cerr << "\rIMUX324  ";
    Ciphertext gin324A = 2 * gout108 + gout105;
    Ciphertext gout324A = ctx.eval_lut(&gin324A, and2);
    Ciphertext gin324B = 2 * gout257 + (1 - gout105);
    Ciphertext gout324B = ctx.eval_lut(&gin324B, and2);
    Ciphertext gout324 = gout324A + gout324B;
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout171 + gout203;
    Ciphertext gout325 = ctx.eval_lut(&gin325, and2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * gout161 + gout170;
    Ciphertext gout326 = ctx.eval_lut(&gin326, and2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * gout256 + gout187;
    Ciphertext gout327 = ctx.eval_lut(&gin327, and2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout72 + gout177;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout72 + gout178;
    Ciphertext gout329 = ctx.eval_lut(&gin329, nand2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout117 + gout185;
    Ciphertext gout330 = ctx.eval_lut(&gin330, nand2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout197 + gout264;
    Ciphertext gout331 = ctx.eval_lut(&gin331, and2);
    std::cerr << "\rIMUX332  ";
    Ciphertext gin332A = 2 * gout288 + gout72;
    Ciphertext gout332A = ctx.eval_lut(&gin332A, and2);
    Ciphertext gin332B = 2 * gout231 + (1 - gout72);
    Ciphertext gout332B = ctx.eval_lut(&gin332B, and2);
    Ciphertext gout332 = gout332A + gout332B;
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout149 + gout271;
    Ciphertext gout333 = ctx.eval_lut(&gin333, and2);
    std::cerr << "\rIMUX334  ";
    Ciphertext gin334A = 2 * gout289 + gout105;
    Ciphertext gout334A = ctx.eval_lut(&gin334A, and2);
    Ciphertext gin334B = 2 * gout267 + (1 - gout105);
    Ciphertext gout334B = ctx.eval_lut(&gin334B, and2);
    Ciphertext gout334 = gout334A + gout334B;
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * gout72 + gout191;
    Ciphertext gout335 = ctx.eval_lut(&gin335, nand2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout105 + gout263;
    Ciphertext gout336 = ctx.eval_lut(&gin336, nand2);
    std::cerr << "\rIMUX337  ";
    Ciphertext gin337A = 2 * gout288 + gout105;
    Ciphertext gout337A = ctx.eval_lut(&gin337A, and2);
    Ciphertext gin337B = 2 * gout263 + (1 - gout105);
    Ciphertext gout337B = ctx.eval_lut(&gin337B, and2);
    Ciphertext gout337 = gout337A + gout337B;
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * gout221 + gout200;
    Ciphertext gout338 = ctx.eval_lut(&gin338, and2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout186 + gout248;
    Ciphertext gout339 = ctx.eval_lut(&gin339, and2);
    std::cerr << "\rIMUX340  ";
    Ciphertext gin340A = 2 * gout110 + gout105;
    Ciphertext gout340A = ctx.eval_lut(&gin340A, and2);
    Ciphertext gin340B = 2 * gout246 + (1 - gout105);
    Ciphertext gout340B = ctx.eval_lut(&gin340B, and2);
    Ciphertext gout340 = gout340A + gout340B;
    std::cerr << "\rLUT341   ";
    Ciphertext gin341 = 2 * gout186 + gout241;
    Ciphertext gout341 = ctx.eval_lut(&gin341, and2);
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout225 + gout254;
    Ciphertext gout342 = ctx.eval_lut(&gin342, and2);
    std::cerr << "\rIMUX343  ";
    Ciphertext gin343A = 2 * gout113 + gout105;
    Ciphertext gout343A = ctx.eval_lut(&gin343A, and2);
    Ciphertext gin343B = 2 * gout274 + (1 - gout105);
    Ciphertext gout343B = ctx.eval_lut(&gin343B, and2);
    Ciphertext gout343 = gout343A + gout343B;
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout259 + gout233;
    Ciphertext gout344 = ctx.eval_lut(&gin344, and2);
    std::cerr << "\rIMUX345  ";
    Ciphertext gin345A = 2 * gout107 + gout117;
    Ciphertext gout345A = ctx.eval_lut(&gin345A, and2);
    Ciphertext gin345B = 2 * gout205 + (1 - gout117);
    Ciphertext gout345B = ctx.eval_lut(&gin345B, and2);
    Ciphertext gout345 = gout345A + gout345B;
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * gout197 + gout207;
    Ciphertext gout346 = ctx.eval_lut(&gin346, and2);
    std::cerr << "\rLUT347   ";
    Ciphertext gin347 = 2 * gout200 + gout270;
    Ciphertext gout347 = ctx.eval_lut(&gin347, and2);
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout72 + gout220;
    Ciphertext gout348 = ctx.eval_lut(&gin348, nand2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout117 + gout206;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rIMUX350  ";
    Ciphertext gin350A = 2 * gout127 + gout72;
    Ciphertext gout350A = ctx.eval_lut(&gin350A, and2);
    Ciphertext gin350B = 2 * gout210 + (1 - gout72);
    Ciphertext gout350B = ctx.eval_lut(&gin350B, and2);
    Ciphertext gout350 = gout350A + gout350B;
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout229 + gout208;
    Ciphertext gout351 = ctx.eval_lut(&gin351, and2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout234 + gout147;
    Ciphertext gout352 = ctx.eval_lut(&gin352, and2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout183 + gout273;
    Ciphertext gout353 = ctx.eval_lut(&gin353, and2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout105 + gout211;
    Ciphertext gout354 = ctx.eval_lut(&gin354, nand2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout266 + gout148;
    Ciphertext gout355 = ctx.eval_lut(&gin355, and2);
    std::cerr << "\rLUT356   ";
    Ciphertext gin356 = 2 * gout117 + gout212;
    Ciphertext gout356 = ctx.eval_lut(&gin356, nand2);
    std::cerr << "\rLUT357   ";
    Ciphertext gin357 = 2 * gout150 + gout277;
    Ciphertext gout357 = ctx.eval_lut(&gin357, and2);
    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout253 + gout261;
    Ciphertext gout358 = ctx.eval_lut(&gin358, and2);
    std::cerr << "\rLUT359   ";
    Ciphertext gin359 = 2 * gout215 + gout255;
    Ciphertext gout359 = ctx.eval_lut(&gin359, and2);
    std::cerr << "\rIMUX360  ";
    Ciphertext gin360A = 2 * gout126 + gout72;
    Ciphertext gout360A = ctx.eval_lut(&gin360A, and2);
    Ciphertext gin360B = 2 * gout220 + (1 - gout72);
    Ciphertext gout360B = ctx.eval_lut(&gin360B, and2);
    Ciphertext gout360 = gout360A + gout360B;
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout117 + gout216;
    Ciphertext gout361 = ctx.eval_lut(&gin361, nand2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout105 + gout218;
    Ciphertext gout362 = ctx.eval_lut(&gin362, nand2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout105 + gout238;
    Ciphertext gout363 = ctx.eval_lut(&gin363, nand2);
    std::cerr << "\rIMUX364  ";
    Ciphertext gin364A = 2 * gout119 + gout105;
    Ciphertext gout364A = ctx.eval_lut(&gin364A, and2);
    Ciphertext gin364B = 2 * gout218 + (1 - gout105);
    Ciphertext gout364B = ctx.eval_lut(&gin364B, and2);
    Ciphertext gout364 = gout364A + gout364B;
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout105 + gout276;
    Ciphertext gout365 = ctx.eval_lut(&gin365, nand2);
    std::cerr << "\rIMUX366  ";
    Ciphertext gin366A = 2 * gout131 + gout105;
    Ciphertext gout366A = ctx.eval_lut(&gin366A, and2);
    Ciphertext gin366B = 2 * gout243 + (1 - gout105);
    Ciphertext gout366B = ctx.eval_lut(&gin366B, and2);
    Ciphertext gout366 = gout366A + gout366B;
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout195 + gout262;
    Ciphertext gout367 = ctx.eval_lut(&gin367, and2);
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout105 + gout275;
    Ciphertext gout368 = ctx.eval_lut(&gin368, nand2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout247 + gout198;
    Ciphertext gout369 = ctx.eval_lut(&gin369, and2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * gout145 + gout236;
    Ciphertext gout370 = ctx.eval_lut(&gin370, and2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout105 + gout243;
    Ciphertext gout371 = ctx.eval_lut(&gin371, nand2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout251 + gout223;
    Ciphertext gout372 = ctx.eval_lut(&gin372, and2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout105 + gout246;
    Ciphertext gout373 = ctx.eval_lut(&gin373, nand2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout279 + gout344;
    Ciphertext gout374 = ctx.eval_lut(&gin374, and2);
    std::cerr << "\rLUT375   ";
    Ciphertext gin375 = 2 * gout105 + gout268;
    Ciphertext gout375 = ctx.eval_lut(&gin375, nand2);
    std::cerr << "\rLUT376   ";
    Ciphertext gin376 = 2 * gout105 + gout269;
    Ciphertext gout376 = ctx.eval_lut(&gin376, nand2);
    std::cerr << "\rIMUX377  ";
    Ciphertext gin377A = 2 * gout127 + gout105;
    Ciphertext gout377A = ctx.eval_lut(&gin377A, and2);
    Ciphertext gin377B = 2 * gout265 + (1 - gout105);
    Ciphertext gout377B = ctx.eval_lut(&gin377B, and2);
    Ciphertext gout377 = gout377A + gout377B;
    std::cerr << "\rIMUX378  ";
    Ciphertext gin378A = 2 * gout295 + gout105;
    Ciphertext gout378A = ctx.eval_lut(&gin378A, and2);
    Ciphertext gin378B = 2 * gout276 + (1 - gout105);
    Ciphertext gout378B = ctx.eval_lut(&gin378B, and2);
    Ciphertext gout378 = gout378A + gout378B;
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout144 + gout249;
    Ciphertext gout379 = ctx.eval_lut(&gin379, nand2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout254 + gout228;
    Ciphertext gout380 = ctx.eval_lut(&gin380, and2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout105 + gout257;
    Ciphertext gout381 = ctx.eval_lut(&gin381, nand2);
    std::cerr << "\rLUT382   ";
    Ciphertext gin382 = 2 * gout258 + gout250;
    Ciphertext gout382 = ctx.eval_lut(&gin382, and2);
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * gout105 + gout265;
    Ciphertext gout383 = ctx.eval_lut(&gin383, nand2);
    std::cerr << "\rLUT384   ";
    Ciphertext gin384 = 2 * gout105 + gout267;
    Ciphertext gout384 = ctx.eval_lut(&gin384, nand2);
    std::cerr << "\rIMUX385  ";
    Ciphertext gin385A = 2 * gout126 + gout105;
    Ciphertext gout385A = ctx.eval_lut(&gin385A, and2);
    Ciphertext gin385B = 2 * gout269 + (1 - gout105);
    Ciphertext gout385B = ctx.eval_lut(&gin385B, and2);
    Ciphertext gout385 = gout385A + gout385B;
    std::cerr << "\rIMUX386  ";
    Ciphertext gin386A = 2 * gout297 + gout105;
    Ciphertext gout386A = ctx.eval_lut(&gin386A, and2);
    Ciphertext gin386B = 2 * gout275 + (1 - gout105);
    Ciphertext gout386B = ctx.eval_lut(&gin386B, and2);
    Ciphertext gout386 = gout386A + gout386B;
    std::cerr << "\rLUT387   ";
    Ciphertext gin387 = 2 * gout105 + gout274;
    Ciphertext gout387 = ctx.eval_lut(&gin387, nand2);
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * gout105 + gout313;
    Ciphertext gout388 = ctx.eval_lut(&gin388, nand2);
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout321 + gout285;
    Ciphertext gout389 = ctx.eval_lut(&gin389, and2);
    std::cerr << "\rIMUX390  ";
    Ciphertext gin390A = 2 * gout307 + gout72;
    Ciphertext gout390A = ctx.eval_lut(&gin390A, and2);
    Ciphertext gin390B = 2 * gout191 + (1 - gout72);
    Ciphertext gout390B = ctx.eval_lut(&gin390B, and2);
    Ciphertext gout390 = gout390A + gout390B;
    std::cerr << "\rIMUX391  ";
    Ciphertext gin391A = 2 * gout317 + gout105;
    Ciphertext gout391A = ctx.eval_lut(&gin391A, and2);
    Ciphertext gin391B = 2 * gout240 + (1 - gout105);
    Ciphertext gout391B = ctx.eval_lut(&gin391B, and2);
    Ciphertext gout391 = gout391A + gout391B;
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * gout303 + gout73;
    Ciphertext gout392 = ctx.eval_lut(&gin392, and2);
    std::cerr << "\rIMUX393  ";
    Ciphertext gin393A = 2 * gout289 + gout72;
    Ciphertext gout393A = ctx.eval_lut(&gin393A, and2);
    Ciphertext gin393B = 2 * gout209 + (1 - gout72);
    Ciphertext gout393B = ctx.eval_lut(&gin393B, and2);
    Ciphertext gout393 = gout393A + gout393B;
    std::cerr << "\rIMUX394  ";
    Ciphertext gin394A = 2 * gout288 + gout130;
    Ciphertext gout394A = ctx.eval_lut(&gin394A, and2);
    Ciphertext gin394B = 2 * gout324 + (1 - gout130);
    Ciphertext gout394B = ctx.eval_lut(&gin394B, and2);
    Ciphertext gout394 = gout394A + gout394B;
    std::cerr << "\rIMUX395  ";
    Ciphertext gin395A = 2 * gout296 + gout72;
    Ciphertext gout395A = ctx.eval_lut(&gin395A, and2);
    Ciphertext gin395B = 2 * gout165 + (1 - gout72);
    Ciphertext gout395B = ctx.eval_lut(&gin395B, and2);
    Ciphertext gout395 = gout395A + gout395B;
    std::cerr << "\rIMUX396  ";
    Ciphertext gin396A = 2 * gout307 + gout105;
    Ciphertext gout396A = ctx.eval_lut(&gin396A, and2);
    Ciphertext gin396B = 2 * gout238 + (1 - gout105);
    Ciphertext gout396B = ctx.eval_lut(&gin396B, and2);
    Ciphertext gout396 = gout396A + gout396B;
    std::cerr << "\rIMUX397  ";
    Ciphertext gin397A = 2 * gout297 + gout130;
    Ciphertext gout397A = ctx.eval_lut(&gin397A, and2);
    Ciphertext gin397B = 2 * gout340 + (1 - gout130);
    Ciphertext gout397B = ctx.eval_lut(&gin397B, and2);
    Ciphertext gout397 = gout397A + gout397B;
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout245 + gout329;
    Ciphertext gout398 = ctx.eval_lut(&gin398, and2);
    std::cerr << "\rIMUX399  ";
    Ciphertext gin399A = 2 * gout297 + gout72;
    Ciphertext gout399A = ctx.eval_lut(&gin399A, and2);
    Ciphertext gin399B = 2 * gout199 + (1 - gout72);
    Ciphertext gout399B = ctx.eval_lut(&gin399B, and2);
    Ciphertext gout399 = gout399A + gout399B;
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout304 + gout331;
    Ciphertext gout400 = ctx.eval_lut(&gin400, and2);
    std::cerr << "\rIMUX401  ";
    Ciphertext gin401A = 2 * gout337 + gout79;
    Ciphertext gout401A = ctx.eval_lut(&gin401A, and2);
    Ciphertext gin401B = 2 * gout394 + (1 - gout79);
    Ciphertext gout401B = ctx.eval_lut(&gin401B, and2);
    Ciphertext gout401 = gout401A + gout401B;
    std::cerr << "\rIMUX402  ";
    Ciphertext gin402A = 2 * gout295 + gout72;
    Ciphertext gout402A = ctx.eval_lut(&gin402A, and2);
    Ciphertext gin402B = 2 * gout174 + (1 - gout72);
    Ciphertext gout402B = ctx.eval_lut(&gin402B, and2);
    Ciphertext gout402 = gout402A + gout402B;
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * gout130 + gout291;
    Ciphertext gout403 = ctx.eval_lut(&gin403, nand2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * gout356 + gout319;
    Ciphertext gout404 = ctx.eval_lut(&gin404, and2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout300 + gout322;
    Ciphertext gout405 = ctx.eval_lut(&gin405, and2);
    std::cerr << "\rIMUX406  ";
    Ciphertext gin406A = 2 * gout296 + gout105;
    Ciphertext gout406A = ctx.eval_lut(&gin406A, and2);
    Ciphertext gin406B = 2 * gout313 + (1 - gout105);
    Ciphertext gout406B = ctx.eval_lut(&gin406B, and2);
    Ciphertext gout406 = gout406A + gout406B;
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout232 + gout356;
    Ciphertext gout407 = ctx.eval_lut(&gin407, and2);
    std::cerr << "\rIMUX408  ";
    Ciphertext gin408A = 2 * gout295 + gout130;
    Ciphertext gout408A = ctx.eval_lut(&gin408A, and2);
    Ciphertext gin408B = 2 * gout343 + (1 - gout130);
    Ciphertext gout408B = ctx.eval_lut(&gin408B, and2);
    Ciphertext gout408 = gout408A + gout408B;
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout361 + gout341;
    Ciphertext gout409 = ctx.eval_lut(&gin409, and2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * gout308 + gout293;
    Ciphertext gout410 = ctx.eval_lut(&gin410, and2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout359 + gout284;
    Ciphertext gout411 = ctx.eval_lut(&gin411, and2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout320 + gout326;
    Ciphertext gout412 = ctx.eval_lut(&gin412, and2);
    std::cerr << "\rIMUX413  ";
    Ciphertext gin413A = 2 * gout317 + gout130;
    Ciphertext gout413A = ctx.eval_lut(&gin413A, and2);
    Ciphertext gin413B = 2 * gout366 + (1 - gout130);
    Ciphertext gout413B = ctx.eval_lut(&gin413B, and2);
    Ciphertext gout413 = gout413A + gout413B;
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout239 + gout325;
    Ciphertext gout414 = ctx.eval_lut(&gin414, and2);
    std::cerr << "\rIMUX415  ";
    Ciphertext gin415A = 2 * gout317 + gout72;
    Ciphertext gout415A = ctx.eval_lut(&gin415A, and2);
    Ciphertext gin415B = 2 * gout160 + (1 - gout72);
    Ciphertext gout415B = ctx.eval_lut(&gin415B, and2);
    Ciphertext gout415 = gout415A + gout415B;
    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout346 + gout287;
    Ciphertext gout416 = ctx.eval_lut(&gin416, and2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout235 + gout354;
    Ciphertext gout417 = ctx.eval_lut(&gin417, and2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout130 + gout324;
    Ciphertext gout418 = ctx.eval_lut(&gin418, nand2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout330 + gout367;
    Ciphertext gout419 = ctx.eval_lut(&gin419, and2);
    std::cerr << "\rIMUX420  ";
    Ciphertext gin420A = 2 * gout337 + gout72;
    Ciphertext gout420A = ctx.eval_lut(&gin420A, and2);
    Ciphertext gin420B = 2 * gout394 + (1 - gout72);
    Ciphertext gout420B = ctx.eval_lut(&gin420B, and2);
    Ciphertext gout420 = gout420A + gout420B;
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout349 + gout347;
    Ciphertext gout421 = ctx.eval_lut(&gin421, and2);
    std::cerr << "\rLUT422   ";
    Ciphertext gin422 = 2 * gout308 + gout353;
    Ciphertext gout422 = ctx.eval_lut(&gin422, and2);
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * gout278 + gout355;
    Ciphertext gout423 = ctx.eval_lut(&gin423, and2);
    std::cerr << "\rIMUX424  ";
    Ciphertext gin424A = 2 * gout289 + gout130;
    Ciphertext gout424A = ctx.eval_lut(&gin424A, and2);
    Ciphertext gin424B = 2 * gout377 + (1 - gout130);
    Ciphertext gout424B = ctx.eval_lut(&gin424B, and2);
    Ciphertext gout424 = gout424A + gout424B;
    std::cerr << "\rIMUX425  ";
    Ciphertext gin425A = 2 * gout345 + gout105;
    Ciphertext gout425A = ctx.eval_lut(&gin425A, and2);
    Ciphertext gin425B = 2 * gout268 + (1 - gout105);
    Ciphertext gout425B = ctx.eval_lut(&gin425B, and2);
    Ciphertext gout425 = gout425A + gout425B;
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout316 + gout338;
    Ciphertext gout426 = ctx.eval_lut(&gin426, and2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout352 + gout318;
    Ciphertext gout427 = ctx.eval_lut(&gin427, and2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout130 + gout343;
    Ciphertext gout428 = ctx.eval_lut(&gin428, nand2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout130 + gout340;
    Ciphertext gout429 = ctx.eval_lut(&gin429, nand2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * gout371 + gout272;
    Ciphertext gout430 = ctx.eval_lut(&gin430, and2);
    std::cerr << "\rIMUX431  ";
    Ciphertext gin431A = 2 * gout345 + gout130;
    Ciphertext gout431A = ctx.eval_lut(&gin431A, and2);
    Ciphertext gin431B = 2 * gout291 + (1 - gout130);
    Ciphertext gout431B = ctx.eval_lut(&gin431B, and2);
    Ciphertext gout431 = gout431A + gout431B;
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout351 + gout306;
    Ciphertext gout432 = ctx.eval_lut(&gin432, and2);
    std::cerr << "\rIMUX433  ";
    Ciphertext gin433A = 2 * gout345 + gout72;
    Ciphertext gout433A = ctx.eval_lut(&gin433A, and2);
    Ciphertext gin433B = 2 * gout175 + (1 - gout72);
    Ciphertext gout433B = ctx.eval_lut(&gin433B, and2);
    Ciphertext gout433 = gout433A + gout433B;
    std::cerr << "\rIMUX434  ";
    Ciphertext gin434A = 2 * gout378 + gout79;
    Ciphertext gout434A = ctx.eval_lut(&gin434A, and2);
    Ciphertext gin434B = 2 * gout408 + (1 - gout79);
    Ciphertext gout434B = ctx.eval_lut(&gin434B, and2);
    Ciphertext gout434 = gout434A + gout434B;
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout369 + gout292;
    Ciphertext gout435 = ctx.eval_lut(&gin435, and2);
    std::cerr << "\rLUT436   ";
    Ciphertext gin436 = 2 * gout130 + gout364;
    Ciphertext gout436 = ctx.eval_lut(&gin436, nand2);
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout362 + gout302;
    Ciphertext gout437 = ctx.eval_lut(&gin437, and2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout302 + gout380;
    Ciphertext gout438 = ctx.eval_lut(&gin438, and2);
    std::cerr << "\rLUT439   ";
    Ciphertext gin439 = 2 * gout373 + gout328;
    Ciphertext gout439 = ctx.eval_lut(&gin439, and2);
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout69 + gout372;
    Ciphertext gout440 = ctx.eval_lut(&gin440, and2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout130 + gout366;
    Ciphertext gout441 = ctx.eval_lut(&gin441, nand2);
    std::cerr << "\rIMUX442  ";
    Ciphertext gin442A = 2 * gout386 + gout72;
    Ciphertext gout442A = ctx.eval_lut(&gin442A, and2);
    Ciphertext gin442B = 2 * gout397 + (1 - gout72);
    Ciphertext gout442B = ctx.eval_lut(&gin442B, and2);
    Ciphertext gout442 = gout442A + gout442B;
    std::cerr << "\rIMUX443  ";
    Ciphertext gin443A = 2 * gout307 + gout130;
    Ciphertext gout443A = ctx.eval_lut(&gin443A, and2);
    Ciphertext gin443B = 2 * gout364 + (1 - gout130);
    Ciphertext gout443B = ctx.eval_lut(&gin443B, and2);
    Ciphertext gout443 = gout443A + gout443B;
    std::cerr << "\rIMUX444  ";
    Ciphertext gin444A = 2 * gout378 + gout72;
    Ciphertext gout444A = ctx.eval_lut(&gin444A, and2);
    Ciphertext gin444B = 2 * gout408 + (1 - gout72);
    Ciphertext gout444B = ctx.eval_lut(&gin444B, and2);
    Ciphertext gout444 = gout444A + gout444B;
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout376 + gout300;
    Ciphertext gout445 = ctx.eval_lut(&gin445, and2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout381 + gout407;
    Ciphertext gout446 = ctx.eval_lut(&gin446, and2);
    std::cerr << "\rIMUX447  ";
    Ciphertext gin447A = 2 * gout386 + gout79;
    Ciphertext gout447A = ctx.eval_lut(&gin447A, and2);
    Ciphertext gin447B = 2 * gout397 + (1 - gout79);
    Ciphertext gout447B = ctx.eval_lut(&gin447B, and2);
    Ciphertext gout447 = gout447A + gout447B;
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * gout130 + gout377;
    Ciphertext gout448 = ctx.eval_lut(&gin448, nand2);
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout387 + gout290;
    Ciphertext gout449 = ctx.eval_lut(&gin449, and2);
    std::cerr << "\rIMUX450  ";
    Ciphertext gin450A = 2 * gout296 + gout130;
    Ciphertext gout450A = ctx.eval_lut(&gin450A, and2);
    Ciphertext gin450B = 2 * gout385 + (1 - gout130);
    Ciphertext gout450B = ctx.eval_lut(&gin450B, and2);
    Ciphertext gout450 = gout450A + gout450B;
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout379 + gout348;
    Ciphertext gout451 = ctx.eval_lut(&gin451, and2);
    std::cerr << "\rLUT452   ";
    Ciphertext gin452 = 2 * gout130 + gout385;
    Ciphertext gout452 = ctx.eval_lut(&gin452, nand2);
    std::cerr << "\rLUT453   ";
    Ciphertext gin453 = 2 * gout382 + gout342;
    Ciphertext gout453 = ctx.eval_lut(&gin453, and2);
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout312 + gout389;
    Ciphertext gout454 = ctx.eval_lut(&gin454, nand2);
    std::cerr << "\rLUT455   ";
    Ciphertext gin455 = 2 * gout309 + gout400;
    Ciphertext gout455 = ctx.eval_lut(&gin455, and2);
    std::cerr << "\rLUT456   ";
    Ciphertext gin456 = 2 * gout294 + gout404;
    Ciphertext gout456 = ctx.eval_lut(&gin456, and2);
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout315 + gout409;
    Ciphertext gout457 = ctx.eval_lut(&gin457, and2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout281 + gout405;
    Ciphertext gout458 = ctx.eval_lut(&gin458, and2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout311 + gout412;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout330 + gout411;
    Ciphertext gout460 = ctx.eval_lut(&gin460, and2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout298 + gout416;
    Ciphertext gout461 = ctx.eval_lut(&gin461, nand2);
    std::cerr << "\rLUT462   ";
    Ciphertext gin462 = 2 * gout310 + gout422;
    Ciphertext gout462 = ctx.eval_lut(&gin462, and2);
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout418 + gout456;
    Ciphertext gout463 = ctx.eval_lut(&gin463, and2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout414 + gout410;
    Ciphertext gout464 = ctx.eval_lut(&gin464, and2);
    std::cerr << "\rIMUX465  ";
    Ciphertext gin465A = 2 * gout425 + gout72;
    Ciphertext gout465A = ctx.eval_lut(&gin465A, and2);
    Ciphertext gin465B = 2 * gout431 + (1 - gout72);
    Ciphertext gout465B = ctx.eval_lut(&gin465B, and2);
    Ciphertext gout465 = gout465A + gout465B;
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout282 + gout421;
    Ciphertext gout466 = ctx.eval_lut(&gin466, and2);
    std::cerr << "\rIMUX467  ";
    Ciphertext gin467A = 2 * gout391 + gout79;
    Ciphertext gout467A = ctx.eval_lut(&gin467A, and2);
    Ciphertext gin467B = 2 * gout413 + (1 - gout79);
    Ciphertext gout467B = ctx.eval_lut(&gin467B, and2);
    Ciphertext gout467 = gout467A + gout467B;
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout335 + gout438;
    Ciphertext gout468 = ctx.eval_lut(&gin468, and2);
    std::cerr << "\rIMUX469  ";
    Ciphertext gin469A = 2 * gout396 + gout72;
    Ciphertext gout469A = ctx.eval_lut(&gin469A, and2);
    Ciphertext gin469B = 2 * gout443 + (1 - gout72);
    Ciphertext gout469B = ctx.eval_lut(&gin469B, and2);
    Ciphertext gout469 = gout469A + gout469B;
    std::cerr << "\rIMUX470  ";
    Ciphertext gin470A = 2 * gout391 + gout72;
    Ciphertext gout470A = ctx.eval_lut(&gin470A, and2);
    Ciphertext gin470B = 2 * gout413 + (1 - gout72);
    Ciphertext gout470B = ctx.eval_lut(&gin470B, and2);
    Ciphertext gout470 = gout470A + gout470B;
    std::cerr << "\rLUT471   ";
    Ciphertext gin471 = 2 * gout370 + gout426;
    Ciphertext gout471 = ctx.eval_lut(&gin471, nand2);
    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout429 + gout457;
    Ciphertext gout472 = ctx.eval_lut(&gin472, and2);
    std::cerr << "\rIMUX473  ";
    Ciphertext gin473A = 2 * gout334 + gout72;
    Ciphertext gout473A = ctx.eval_lut(&gin473A, and2);
    Ciphertext gin473B = 2 * gout424 + (1 - gout72);
    Ciphertext gout473B = ctx.eval_lut(&gin473B, and2);
    Ciphertext gout473 = gout473A + gout473B;
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout357 + gout432;
    Ciphertext gout474 = ctx.eval_lut(&gin474, nand2);
    std::cerr << "\rLUT475   ";
    Ciphertext gin475 = 2 * gout448 + gout462;
    Ciphertext gout475 = ctx.eval_lut(&gin475, and2);
    std::cerr << "\rIMUX476  ";
    Ciphertext gin476A = 2 * gout406 + gout79;
    Ciphertext gout476A = ctx.eval_lut(&gin476A, and2);
    Ciphertext gin476B = 2 * gout450 + (1 - gout79);
    Ciphertext gout476B = ctx.eval_lut(&gin476B, and2);
    Ciphertext gout476 = gout476A + gout476B;
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout333 + gout435;
    Ciphertext gout477 = ctx.eval_lut(&gin477, nand2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout392 + gout446;
    Ciphertext gout478 = ctx.eval_lut(&gin478, nand2);
    std::cerr << "\rIMUX479  ";
    Ciphertext gin479A = 2 * gout334 + gout79;
    Ciphertext gout479A = ctx.eval_lut(&gin479A, and2);
    Ciphertext gin479B = 2 * gout424 + (1 - gout79);
    Ciphertext gout479B = ctx.eval_lut(&gin479B, and2);
    Ciphertext gout479 = gout479A + gout479B;
    std::cerr << "\rLUT480   ";
    Ciphertext gin480 = 2 * gout339 + gout427;
    Ciphertext gout480 = ctx.eval_lut(&gin480, nand2);
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout327 + gout437;
    Ciphertext gout481 = ctx.eval_lut(&gin481, and2);
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout417 + gout304;
    Ciphertext gout482 = ctx.eval_lut(&gin482, and2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout280 + gout419;
    Ciphertext gout483 = ctx.eval_lut(&gin483, and2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout358 + gout445;
    Ciphertext gout484 = ctx.eval_lut(&gin484, and2);
    std::cerr << "\rLUT485   ";
    Ciphertext gin485 = 2 * gout430 + gout460;
    Ciphertext gout485 = ctx.eval_lut(&gin485, nand2);
    std::cerr << "\rIMUX486  ";
    Ciphertext gin486A = 2 * gout406 + gout72;
    Ciphertext gout486A = ctx.eval_lut(&gin486A, and2);
    Ciphertext gin486B = 2 * gout450 + (1 - gout72);
    Ciphertext gout486B = ctx.eval_lut(&gin486B, and2);
    Ciphertext gout486 = gout486A + gout486B;
    std::cerr << "\rIMUX487  ";
    Ciphertext gin487A = 2 * gout425 + gout79;
    Ciphertext gout487A = ctx.eval_lut(&gin487A, and2);
    Ciphertext gin487B = 2 * gout431 + (1 - gout79);
    Ciphertext gout487B = ctx.eval_lut(&gin487B, and2);
    Ciphertext gout487 = gout487A + gout487B;
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * gout361 + gout439;
    Ciphertext gout488 = ctx.eval_lut(&gin488, and2);
    std::cerr << "\rLUT489   ";
    Ciphertext gin489 = 2 * gout323 + gout453;
    Ciphertext gout489 = ctx.eval_lut(&gin489, nand2);
    std::cerr << "\rIMUX490  ";
    Ciphertext gin490A = 2 * gout396 + gout79;
    Ciphertext gout490A = ctx.eval_lut(&gin490A, and2);
    Ciphertext gin490B = 2 * gout443 + (1 - gout79);
    Ciphertext gout490B = ctx.eval_lut(&gin490B, and2);
    Ciphertext gout490 = gout490A + gout490B;
    std::cerr << "\rLUT491   ";
    Ciphertext gin491 = 2 * gout452 + gout458;
    Ciphertext gout491 = ctx.eval_lut(&gin491, and2);
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout349 + gout449;
    Ciphertext gout492 = ctx.eval_lut(&gin492, and2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout60 + gout454;
    Ciphertext gout493 = ctx.eval_lut(&gin493, nand2);
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * gout60 + gout459;
    Ciphertext gout494 = ctx.eval_lut(&gin494, nand2);
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * gout403 + gout455;
    Ciphertext gout495 = ctx.eval_lut(&gin495, and2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout60 + gout461;
    Ciphertext gout496 = ctx.eval_lut(&gin496, nand2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout60 + gout471;
    Ciphertext gout497 = ctx.eval_lut(&gin497, nand2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout383 + gout464;
    Ciphertext gout498 = ctx.eval_lut(&gin498, nand2);
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * gout428 + gout466;
    Ciphertext gout499 = ctx.eval_lut(&gin499, and2);
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * gout60 + gout480;
    Ciphertext gout500 = ctx.eval_lut(&gin500, nand2);
    std::cerr << "\rLUT501   ";
    Ciphertext gin501 = 2 * gout436 + gout468;
    Ciphertext gout501 = ctx.eval_lut(&gin501, and2);
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout336 + gout463;
    Ciphertext gout502 = ctx.eval_lut(&gin502, nand2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout60 + gout477;
    Ciphertext gout503 = ctx.eval_lut(&gin503, nand2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * gout398 + gout481;
    Ciphertext gout504 = ctx.eval_lut(&gin504, nand2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * gout441 + gout483;
    Ciphertext gout505 = ctx.eval_lut(&gin505, and2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * gout368 + gout472;
    Ciphertext gout506 = ctx.eval_lut(&gin506, nand2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout451 + gout484;
    Ciphertext gout507 = ctx.eval_lut(&gin507, nand2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout60 + gout474;
    Ciphertext gout508 = ctx.eval_lut(&gin508, nand2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout384 + gout475;
    Ciphertext gout509 = ctx.eval_lut(&gin509, nand2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout440 + gout482;
    Ciphertext gout510 = ctx.eval_lut(&gin510, nand2);
    std::cerr << "\rIMUX511  ";
    Ciphertext gin511A = 2 * gout108 + gout62;
    Ciphertext gout511A = ctx.eval_lut(&gin511A, and2);
    Ciphertext gin511B = 2 * gout478 + (1 - gout62);
    Ciphertext gout511B = ctx.eval_lut(&gin511B, and2);
    Ciphertext gout511 = gout511A + gout511B;
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout423 + gout488;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * gout374 + gout492;
    Ciphertext gout513 = ctx.eval_lut(&gin513, nand2);
    std::cerr << "\rIMUX514  ";
    Ciphertext gin514A = 2 * gout131 + gout62;
    Ciphertext gout514A = ctx.eval_lut(&gin514A, and2);
    Ciphertext gin514B = 2 * gout485 + (1 - gout62);
    Ciphertext gout514B = ctx.eval_lut(&gin514B, and2);
    Ciphertext gout514 = gout514A + gout514B;
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout60 + gout489;
    Ciphertext gout515 = ctx.eval_lut(&gin515, nand2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * gout388 + gout491;
    Ciphertext gout516 = ctx.eval_lut(&gin516, nand2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout230 + gout493;
    Ciphertext gout517 = ctx.eval_lut(&gin517, nand2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout375 + gout495;
    Ciphertext gout518 = ctx.eval_lut(&gin518, nand2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout202 + gout494;
    Ciphertext gout519 = ctx.eval_lut(&gin519, nand2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * gout173 + gout496;
    Ciphertext gout520 = ctx.eval_lut(&gin520, nand2);
    std::cerr << "\rLUT521   ";
    Ciphertext gin521 = 2 * gout190 + gout497;
    Ciphertext gout521 = ctx.eval_lut(&gin521, nand2);
    std::cerr << "\rIMUX522  ";
    Ciphertext gin522A = 2 * gout127 + gout62;
    Ciphertext gout522A = ctx.eval_lut(&gin522A, and2);
    Ciphertext gin522B = 2 * gout498 + (1 - gout62);
    Ciphertext gout522B = ctx.eval_lut(&gin522B, and2);
    Ciphertext gout522 = gout522A + gout522B;
    std::cerr << "\rLUT523   ";
    Ciphertext gin523 = 2 * gout184 + gout500;
    Ciphertext gout523 = ctx.eval_lut(&gin523, nand2);
    std::cerr << "\rLUT524   ";
    Ciphertext gin524 = 2 * gout363 + gout501;
    Ciphertext gout524 = ctx.eval_lut(&gin524, nand2);
    std::cerr << "\rLUT525   ";
    Ciphertext gin525 = 2 * gout365 + gout499;
    Ciphertext gout525 = ctx.eval_lut(&gin525, nand2);
    std::cerr << "\rIMUX526  ";
    Ciphertext gin526A = 2 * gout289 + gout71;
    Ciphertext gout526A = ctx.eval_lut(&gin526A, and2);
    Ciphertext gin526B = 2 * gout509 + (1 - gout71);
    Ciphertext gout526B = ctx.eval_lut(&gin526B, and2);
    Ciphertext gout526 = gout526A + gout526B;
    std::cerr << "\rIMUX527  ";
    Ciphertext gin527A = 2 * gout107 + gout62;
    Ciphertext gout527A = ctx.eval_lut(&gin527A, and2);
    Ciphertext gin527B = 2 * gout510 + (1 - gout62);
    Ciphertext gout527B = ctx.eval_lut(&gin527B, and2);
    Ciphertext gout527 = gout527A + gout527B;
    std::cerr << "\rLUT528   ";
    Ciphertext gin528 = 2 * gout164 + gout503;
    Ciphertext gout528 = ctx.eval_lut(&gin528, nand2);
    std::cerr << "\rIMUX529  ";
    Ciphertext gin529A = 2 * gout119 + gout62;
    Ciphertext gout529A = ctx.eval_lut(&gin529A, and2);
    Ciphertext gin529B = 2 * gout504 + (1 - gout62);
    Ciphertext gout529B = ctx.eval_lut(&gin529B, and2);
    Ciphertext gout529 = gout529A + gout529B;
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * gout222 + gout508;
    Ciphertext gout530 = ctx.eval_lut(&gin530, nand2);
    std::cerr << "\rIMUX531  ";
    Ciphertext gin531A = 2 * gout288 + gout71;
    Ciphertext gout531A = ctx.eval_lut(&gin531A, and2);
    Ciphertext gin531B = 2 * gout502 + (1 - gout71);
    Ciphertext gout531B = ctx.eval_lut(&gin531B, and2);
    Ciphertext gout531 = gout531A + gout531B;
    std::cerr << "\rIMUX532  ";
    Ciphertext gin532A = 2 * gout110 + gout62;
    Ciphertext gout532A = ctx.eval_lut(&gin532A, and2);
    Ciphertext gin532B = 2 * gout512 + (1 - gout62);
    Ciphertext gout532B = ctx.eval_lut(&gin532B, and2);
    Ciphertext gout532 = gout532A + gout532B;
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout299 + gout505;
    Ciphertext gout533 = ctx.eval_lut(&gin533, nand2);
    std::cerr << "\rIMUX534  ";
    Ciphertext gin534A = 2 * gout297 + gout71;
    Ciphertext gout534A = ctx.eval_lut(&gin534A, and2);
    Ciphertext gin534B = 2 * gout506 + (1 - gout71);
    Ciphertext gout534B = ctx.eval_lut(&gin534B, and2);
    Ciphertext gout534 = gout534A + gout534B;
    std::cerr << "\rLUT535   ";
    Ciphertext gin535 = 2 * gout192 + gout515;
    Ciphertext gout535 = ctx.eval_lut(&gin535, nand2);
    std::cerr << "\rIMUX536  ";
    Ciphertext gin536A = 2 * gout126 + gout62;
    Ciphertext gout536A = ctx.eval_lut(&gin536A, and2);
    Ciphertext gin536B = 2 * gout507 + (1 - gout62);
    Ciphertext gout536B = ctx.eval_lut(&gin536B, and2);
    Ciphertext gout536 = gout536A + gout536B;
    std::cerr << "\rIMUX537  ";
    Ciphertext gin537A = 2 * gout113 + gout62;
    Ciphertext gout537A = ctx.eval_lut(&gin537A, and2);
    Ciphertext gin537B = 2 * gout513 + (1 - gout62);
    Ciphertext gout537B = ctx.eval_lut(&gin537B, and2);
    Ciphertext gout537 = gout537A + gout537B;
    std::cerr << "\rIMUX538  ";
    Ciphertext gin538A = 2 * gout296 + gout71;
    Ciphertext gout538A = ctx.eval_lut(&gin538A, and2);
    Ciphertext gin538B = 2 * gout516 + (1 - gout71);
    Ciphertext gout538B = ctx.eval_lut(&gin538B, and2);
    Ciphertext gout538 = gout538A + gout538B;
    std::cerr << "\rIMUX539  ";
    Ciphertext gin539A = 2 * gout345 + gout71;
    Ciphertext gout539A = ctx.eval_lut(&gin539A, and2);
    Ciphertext gin539B = 2 * gout518 + (1 - gout71);
    Ciphertext gout539B = ctx.eval_lut(&gin539B, and2);
    Ciphertext gout539 = gout539A + gout539B;
    std::cerr << "\rIMUX540  ";
    Ciphertext gin540A = 2 * gout295 + gout71;
    Ciphertext gout540A = ctx.eval_lut(&gin540A, and2);
    Ciphertext gin540B = 2 * gout525 + (1 - gout71);
    Ciphertext gout540B = ctx.eval_lut(&gin540B, and2);
    Ciphertext gout540 = gout540A + gout540B;
    std::cerr << "\rIMUX541  ";
    Ciphertext gin541A = 2 * gout307 + gout71;
    Ciphertext gout541A = ctx.eval_lut(&gin541A, and2);
    Ciphertext gin541B = 2 * gout524 + (1 - gout71);
    Ciphertext gout541B = ctx.eval_lut(&gin541B, and2);
    Ciphertext gout541 = gout541A + gout541B;
    std::cerr << "\rIMUX542  ";
    Ciphertext gin542A = 2 * gout317 + gout71;
    Ciphertext gout542A = ctx.eval_lut(&gin542A, and2);
    Ciphertext gin542B = 2 * gout533 + (1 - gout71);
    Ciphertext gout542B = ctx.eval_lut(&gin542B, and2);
    Ciphertext gout542 = gout542A + gout542B;

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout530)); /* out0 */
    test_out.push_back(ctx.decrypt(&gout520)); /* out1 */
    test_out.push_back(ctx.decrypt(&gout535)); /* out2 */
    test_out.push_back(ctx.decrypt(&gout528)); /* out3 */
    test_out.push_back(ctx.decrypt(&gout521)); /* out4 */
    test_out.push_back(ctx.decrypt(&gout519)); /* out5 */
    test_out.push_back(ctx.decrypt(&gout523)); /* out6 */
    test_out.push_back(ctx.decrypt(&gout517)); /* out7 */
    test_out.push_back(ctx.decrypt(&gout522)); /* out8 */
    test_out.push_back(ctx.decrypt(&gout527)); /* out9 */
    test_out.push_back(ctx.decrypt(&gout529)); /* out10 */
    test_out.push_back(ctx.decrypt(&gout536)); /* out11 */
    test_out.push_back(ctx.decrypt(&gout537)); /* out12 */
    test_out.push_back(ctx.decrypt(&gout511)); /* out13 */
    test_out.push_back(ctx.decrypt(&gout532)); /* out14 */
    test_out.push_back(ctx.decrypt(&gout514)); /* out15 */
    test_out.push_back(ctx.decrypt(&gout526)); /* out16 */
    test_out.push_back(ctx.decrypt(&gout539)); /* out17 */
    test_out.push_back(ctx.decrypt(&gout541)); /* out18 */
    test_out.push_back(ctx.decrypt(&gout538)); /* out19 */
    test_out.push_back(ctx.decrypt(&gout540)); /* out20 */
    test_out.push_back(ctx.decrypt(&gout531)); /* out21 */
    test_out.push_back(ctx.decrypt(&gout534)); /* out22 */
    test_out.push_back(ctx.decrypt(&gout542)); /* out23 */
    test_out.push_back(ctx.decrypt(&gout479)); /* out24 */
    test_out.push_back(ctx.decrypt(&gout487)); /* out25 */
    test_out.push_back(ctx.decrypt(&gout490)); /* out26 */
    test_out.push_back(ctx.decrypt(&gout476)); /* out27 */
    test_out.push_back(ctx.decrypt(&gout434)); /* out28 */
    test_out.push_back(ctx.decrypt(&gout401)); /* out29 */
    test_out.push_back(ctx.decrypt(&gout447)); /* out30 */
    test_out.push_back(ctx.decrypt(&gout467)); /* out31 */
    test_out.push_back(ctx.decrypt(&gout473)); /* out32 */
    test_out.push_back(ctx.decrypt(&gout465)); /* out33 */
    test_out.push_back(ctx.decrypt(&gout469)); /* out34 */
    test_out.push_back(ctx.decrypt(&gout486)); /* out35 */
    test_out.push_back(ctx.decrypt(&gout444)); /* out36 */
    test_out.push_back(ctx.decrypt(&gout420)); /* out37 */
    test_out.push_back(ctx.decrypt(&gout442)); /* out38 */
    test_out.push_back(ctx.decrypt(&gout470)); /* out39 */
    test_out.push_back(ctx.decrypt(&gout393)); /* out40 */
    test_out.push_back(ctx.decrypt(&gout433)); /* out41 */
    test_out.push_back(ctx.decrypt(&gout390)); /* out42 */
    test_out.push_back(ctx.decrypt(&gout395)); /* out43 */
    test_out.push_back(ctx.decrypt(&gout402)); /* out44 */
    test_out.push_back(ctx.decrypt(&gout332)); /* out45 */
    test_out.push_back(ctx.decrypt(&gout399)); /* out46 */
    test_out.push_back(ctx.decrypt(&gout415)); /* out47 */
    test_out.push_back(ctx.decrypt(&gout350)); /* out48 */
    test_out.push_back(ctx.decrypt(&gout301)); /* out49 */
    test_out.push_back(ctx.decrypt(&gout283)); /* out50 */
    test_out.push_back(ctx.decrypt(&gout360)); /* out51 */
    test_out.push_back(ctx.decrypt(&gout305)); /* out52 */
    test_out.push_back(ctx.decrypt(&gout314)); /* out53 */
    test_out.push_back(ctx.decrypt(&gout286)); /* out54 */
    test_out.push_back(ctx.decrypt(&gout169)); /* out55 */
    test_out.push_back(ctx.decrypt(&gout78)); /* out56 */
    test_out.push_back(ctx.decrypt(&gout84)); /* out57 */
    test_out.push_back(ctx.decrypt(&gout77)); /* out58 */
    test_out.push_back(ctx.decrypt(&gout85)); /* out59 */
    test_out.push_back(ctx.decrypt(&gout80)); /* out60 */
    test_out.push_back(ctx.decrypt(&gout81)); /* out61 */
    test_out.push_back(ctx.decrypt(&gout83)); /* out62 */
    test_out.push_back(ctx.decrypt(&gout86)); /* out63 */
    return test_out;
}

static void
BM_strrev_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_strrev_boolean(ctx);
    }
}

BENCHMARK(BM_strrev_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
