#include <benchmark/benchmark.h>
#include "fhe_context.h"
using namespace fhe_deck;


std::vector<long>
test_sum3d_boolean(FHEContext& ctx, std::vector<uint64_t> out = { 0 })
{
    Ciphertext ct_out0 = ctx.encrypt_public((out[0] >> 0) & 1);
    Ciphertext ct_out1 = ctx.encrypt_public((out[0] >> 1) & 1);
    Ciphertext ct_out2 = ctx.encrypt_public((out[0] >> 2) & 1);
    Ciphertext ct_out3 = ctx.encrypt_public((out[0] >> 3) & 1);
    Ciphertext ct_out4 = ctx.encrypt_public((out[0] >> 4) & 1);
    Ciphertext ct_out5 = ctx.encrypt_public((out[0] >> 5) & 1);
    Ciphertext ct_out6 = ctx.encrypt_public((out[0] >> 6) & 1);
    Ciphertext ct_out7 = ctx.encrypt_public((out[0] >> 7) & 1);
    Ciphertext ct_out8 = ctx.encrypt_public((out[0] >> 8) & 1);
    Ciphertext ct_out9 = ctx.encrypt_public((out[0] >> 9) & 1);
    Ciphertext ct_out10 = ctx.encrypt_public((out[0] >> 10) & 1);
    Ciphertext ct_out11 = ctx.encrypt_public((out[0] >> 11) & 1);
    Ciphertext ct_out12 = ctx.encrypt_public((out[0] >> 12) & 1);
    Ciphertext ct_out13 = ctx.encrypt_public((out[0] >> 13) & 1);
    Ciphertext ct_out14 = ctx.encrypt_public((out[0] >> 14) & 1);
    Ciphertext ct_out15 = ctx.encrypt_public((out[0] >> 15) & 1);
    Ciphertext ct_out16 = ctx.encrypt_public((out[0] >> 16) & 1);
    Ciphertext ct_out17 = ctx.encrypt_public((out[0] >> 17) & 1);
    Ciphertext ct_out18 = ctx.encrypt_public((out[0] >> 18) & 1);
    Ciphertext ct_out19 = ctx.encrypt_public((out[0] >> 19) & 1);
    Ciphertext ct_out20 = ctx.encrypt_public((out[0] >> 20) & 1);
    Ciphertext ct_out21 = ctx.encrypt_public((out[0] >> 21) & 1);
    Ciphertext ct_out22 = ctx.encrypt_public((out[0] >> 22) & 1);
    Ciphertext ct_out23 = ctx.encrypt_public((out[0] >> 23) & 1);
    Ciphertext ct_out24 = ctx.encrypt_public((out[0] >> 24) & 1);
    Ciphertext ct_out25 = ctx.encrypt_public((out[0] >> 25) & 1);
    Ciphertext ct_out26 = ctx.encrypt_public((out[0] >> 26) & 1);
    Ciphertext ct_out27 = ctx.encrypt_public((out[0] >> 27) & 1);
    Ciphertext ct_out28 = ctx.encrypt_public((out[0] >> 28) & 1);
    Ciphertext ct_out29 = ctx.encrypt_public((out[0] >> 29) & 1);
    Ciphertext ct_out30 = ctx.encrypt_public((out[0] >> 30) & 1);
    Ciphertext ct_out31 = ctx.encrypt_public((out[0] >> 31) & 1);
    Ciphertext ct_out32 = ctx.encrypt_public((out[0] >> 32) & 1);
    Ciphertext ct_out33 = ctx.encrypt_public((out[0] >> 33) & 1);
    Ciphertext ct_out34 = ctx.encrypt_public((out[0] >> 34) & 1);
    Ciphertext ct_out35 = ctx.encrypt_public((out[0] >> 35) & 1);
    Ciphertext ct_out36 = ctx.encrypt_public((out[0] >> 36) & 1);
    Ciphertext ct_out37 = ctx.encrypt_public((out[0] >> 37) & 1);
    Ciphertext ct_out38 = ctx.encrypt_public((out[0] >> 38) & 1);
    Ciphertext ct_out39 = ctx.encrypt_public((out[0] >> 39) & 1);
    Ciphertext ct_out40 = ctx.encrypt_public((out[0] >> 40) & 1);
    Ciphertext ct_out41 = ctx.encrypt_public((out[0] >> 41) & 1);
    Ciphertext ct_out42 = ctx.encrypt_public((out[0] >> 42) & 1);
    Ciphertext ct_out43 = ctx.encrypt_public((out[0] >> 43) & 1);
    Ciphertext ct_out44 = ctx.encrypt_public((out[0] >> 44) & 1);
    Ciphertext ct_out45 = ctx.encrypt_public((out[0] >> 45) & 1);
    Ciphertext ct_out46 = ctx.encrypt_public((out[0] >> 46) & 1);
    Ciphertext ct_out47 = ctx.encrypt_public((out[0] >> 47) & 1);
    Ciphertext ct_out48 = ctx.encrypt_public((out[0] >> 48) & 1);
    Ciphertext ct_out49 = ctx.encrypt_public((out[0] >> 49) & 1);
    Ciphertext ct_out50 = ctx.encrypt_public((out[0] >> 50) & 1);
    Ciphertext ct_out51 = ctx.encrypt_public((out[0] >> 51) & 1);
    Ciphertext ct_out52 = ctx.encrypt_public((out[0] >> 52) & 1);
    Ciphertext ct_out53 = ctx.encrypt_public((out[0] >> 53) & 1);
    Ciphertext ct_out54 = ctx.encrypt_public((out[0] >> 54) & 1);
    Ciphertext ct_out55 = ctx.encrypt_public((out[0] >> 55) & 1);
    Ciphertext ct_out56 = ctx.encrypt_public((out[0] >> 56) & 1);
    Ciphertext ct_out57 = ctx.encrypt_public((out[0] >> 57) & 1);
    Ciphertext ct_out58 = ctx.encrypt_public((out[0] >> 58) & 1);
    Ciphertext ct_out59 = ctx.encrypt_public((out[0] >> 59) & 1);
    Ciphertext ct_out60 = ctx.encrypt_public((out[0] >> 60) & 1);
    Ciphertext ct_out61 = ctx.encrypt_public((out[0] >> 61) & 1);
    Ciphertext ct_out62 = ctx.encrypt_public((out[0] >> 62) & 1);
    Ciphertext ct_out63 = ctx.encrypt_public((out[0] >> 63) & 1);
    Ciphertext ct_out64 = ctx.encrypt_public((out[1] >> 0) & 1);
    Ciphertext ct_out65 = ctx.encrypt_public((out[1] >> 1) & 1);
    Ciphertext ct_out66 = ctx.encrypt_public((out[1] >> 2) & 1);
    Ciphertext ct_out67 = ctx.encrypt_public((out[1] >> 3) & 1);
    Ciphertext ct_out68 = ctx.encrypt_public((out[1] >> 4) & 1);
    Ciphertext ct_out69 = ctx.encrypt_public((out[1] >> 5) & 1);
    Ciphertext ct_out70 = ctx.encrypt_public((out[1] >> 6) & 1);
    Ciphertext ct_out71 = ctx.encrypt_public((out[1] >> 7) & 1);
    Ciphertext ct_out72 = ctx.encrypt_public((out[1] >> 8) & 1);
    Ciphertext ct_out73 = ctx.encrypt_public((out[1] >> 9) & 1);
    Ciphertext ct_out74 = ctx.encrypt_public((out[1] >> 10) & 1);
    Ciphertext ct_out75 = ctx.encrypt_public((out[1] >> 11) & 1);
    Ciphertext ct_out76 = ctx.encrypt_public((out[1] >> 12) & 1);
    Ciphertext ct_out77 = ctx.encrypt_public((out[1] >> 13) & 1);
    Ciphertext ct_out78 = ctx.encrypt_public((out[1] >> 14) & 1);
    Ciphertext ct_out79 = ctx.encrypt_public((out[1] >> 15) & 1);
    Ciphertext ct_out80 = ctx.encrypt_public((out[1] >> 16) & 1);
    Ciphertext ct_out81 = ctx.encrypt_public((out[1] >> 17) & 1);
    Ciphertext ct_out82 = ctx.encrypt_public((out[1] >> 18) & 1);
    Ciphertext ct_out83 = ctx.encrypt_public((out[1] >> 19) & 1);
    Ciphertext ct_out84 = ctx.encrypt_public((out[1] >> 20) & 1);
    Ciphertext ct_out85 = ctx.encrypt_public((out[1] >> 21) & 1);
    Ciphertext ct_out86 = ctx.encrypt_public((out[1] >> 22) & 1);
    Ciphertext ct_out87 = ctx.encrypt_public((out[1] >> 23) & 1);
    Ciphertext ct_out88 = ctx.encrypt_public((out[1] >> 24) & 1);
    Ciphertext ct_out89 = ctx.encrypt_public((out[1] >> 25) & 1);
    Ciphertext ct_out90 = ctx.encrypt_public((out[1] >> 26) & 1);
    Ciphertext ct_out91 = ctx.encrypt_public((out[1] >> 27) & 1);
    Ciphertext ct_out92 = ctx.encrypt_public((out[1] >> 28) & 1);
    Ciphertext ct_out93 = ctx.encrypt_public((out[1] >> 29) & 1);
    Ciphertext ct_out94 = ctx.encrypt_public((out[1] >> 30) & 1);
    Ciphertext ct_out95 = ctx.encrypt_public((out[1] >> 31) & 1);
    Ciphertext ct_out96 = ctx.encrypt_public((out[1] >> 32) & 1);
    Ciphertext ct_out97 = ctx.encrypt_public((out[1] >> 33) & 1);
    Ciphertext ct_out98 = ctx.encrypt_public((out[1] >> 34) & 1);
    Ciphertext ct_out99 = ctx.encrypt_public((out[1] >> 35) & 1);
    Ciphertext ct_out100 = ctx.encrypt_public((out[1] >> 36) & 1);
    Ciphertext ct_out101 = ctx.encrypt_public((out[1] >> 37) & 1);
    Ciphertext ct_out102 = ctx.encrypt_public((out[1] >> 38) & 1);
    Ciphertext ct_out103 = ctx.encrypt_public((out[1] >> 39) & 1);
    Ciphertext ct_out104 = ctx.encrypt_public((out[1] >> 40) & 1);
    Ciphertext ct_out105 = ctx.encrypt_public((out[1] >> 41) & 1);
    Ciphertext ct_out106 = ctx.encrypt_public((out[1] >> 42) & 1);
    Ciphertext ct_out107 = ctx.encrypt_public((out[1] >> 43) & 1);
    Ciphertext ct_out108 = ctx.encrypt_public((out[1] >> 44) & 1);
    Ciphertext ct_out109 = ctx.encrypt_public((out[1] >> 45) & 1);
    Ciphertext ct_out110 = ctx.encrypt_public((out[1] >> 46) & 1);
    Ciphertext ct_out111 = ctx.encrypt_public((out[1] >> 47) & 1);
    Ciphertext ct_out112 = ctx.encrypt_public((out[1] >> 48) & 1);
    Ciphertext ct_out113 = ctx.encrypt_public((out[1] >> 49) & 1);
    Ciphertext ct_out114 = ctx.encrypt_public((out[1] >> 50) & 1);
    Ciphertext ct_out115 = ctx.encrypt_public((out[1] >> 51) & 1);
    Ciphertext ct_out116 = ctx.encrypt_public((out[1] >> 52) & 1);
    Ciphertext ct_out117 = ctx.encrypt_public((out[1] >> 53) & 1);
    Ciphertext ct_out118 = ctx.encrypt_public((out[1] >> 54) & 1);
    Ciphertext ct_out119 = ctx.encrypt_public((out[1] >> 55) & 1);
    Ciphertext ct_out120 = ctx.encrypt_public((out[1] >> 56) & 1);
    Ciphertext ct_out121 = ctx.encrypt_public((out[1] >> 57) & 1);
    Ciphertext ct_out122 = ctx.encrypt_public((out[1] >> 58) & 1);
    Ciphertext ct_out123 = ctx.encrypt_public((out[1] >> 59) & 1);
    Ciphertext ct_out124 = ctx.encrypt_public((out[1] >> 60) & 1);
    Ciphertext ct_out125 = ctx.encrypt_public((out[1] >> 61) & 1);
    Ciphertext ct_out126 = ctx.encrypt_public((out[1] >> 62) & 1);
    Ciphertext ct_out127 = ctx.encrypt_public((out[1] >> 63) & 1);
    Ciphertext ct_out128 = ctx.encrypt_public((out[2] >> 0) & 1);
    Ciphertext ct_out129 = ctx.encrypt_public((out[2] >> 1) & 1);
    Ciphertext ct_out130 = ctx.encrypt_public((out[2] >> 2) & 1);
    Ciphertext ct_out131 = ctx.encrypt_public((out[2] >> 3) & 1);
    Ciphertext ct_out132 = ctx.encrypt_public((out[2] >> 4) & 1);
    Ciphertext ct_out133 = ctx.encrypt_public((out[2] >> 5) & 1);
    Ciphertext ct_out134 = ctx.encrypt_public((out[2] >> 6) & 1);
    Ciphertext ct_out135 = ctx.encrypt_public((out[2] >> 7) & 1);
    Ciphertext ct_out136 = ctx.encrypt_public((out[2] >> 8) & 1);
    Ciphertext ct_out137 = ctx.encrypt_public((out[2] >> 9) & 1);
    Ciphertext ct_out138 = ctx.encrypt_public((out[2] >> 10) & 1);
    Ciphertext ct_out139 = ctx.encrypt_public((out[2] >> 11) & 1);
    Ciphertext ct_out140 = ctx.encrypt_public((out[2] >> 12) & 1);
    Ciphertext ct_out141 = ctx.encrypt_public((out[2] >> 13) & 1);
    Ciphertext ct_out142 = ctx.encrypt_public((out[2] >> 14) & 1);
    Ciphertext ct_out143 = ctx.encrypt_public((out[2] >> 15) & 1);
    Ciphertext ct_out144 = ctx.encrypt_public((out[2] >> 16) & 1);
    Ciphertext ct_out145 = ctx.encrypt_public((out[2] >> 17) & 1);
    Ciphertext ct_out146 = ctx.encrypt_public((out[2] >> 18) & 1);
    Ciphertext ct_out147 = ctx.encrypt_public((out[2] >> 19) & 1);
    Ciphertext ct_out148 = ctx.encrypt_public((out[2] >> 20) & 1);
    Ciphertext ct_out149 = ctx.encrypt_public((out[2] >> 21) & 1);
    Ciphertext ct_out150 = ctx.encrypt_public((out[2] >> 22) & 1);
    Ciphertext ct_out151 = ctx.encrypt_public((out[2] >> 23) & 1);
    Ciphertext ct_out152 = ctx.encrypt_public((out[2] >> 24) & 1);
    Ciphertext ct_out153 = ctx.encrypt_public((out[2] >> 25) & 1);
    Ciphertext ct_out154 = ctx.encrypt_public((out[2] >> 26) & 1);
    Ciphertext ct_out155 = ctx.encrypt_public((out[2] >> 27) & 1);
    Ciphertext ct_out156 = ctx.encrypt_public((out[2] >> 28) & 1);
    Ciphertext ct_out157 = ctx.encrypt_public((out[2] >> 29) & 1);
    Ciphertext ct_out158 = ctx.encrypt_public((out[2] >> 30) & 1);
    Ciphertext ct_out159 = ctx.encrypt_public((out[2] >> 31) & 1);
    Ciphertext ct_out160 = ctx.encrypt_public((out[2] >> 32) & 1);
    Ciphertext ct_out161 = ctx.encrypt_public((out[2] >> 33) & 1);
    Ciphertext ct_out162 = ctx.encrypt_public((out[2] >> 34) & 1);
    Ciphertext ct_out163 = ctx.encrypt_public((out[2] >> 35) & 1);
    Ciphertext ct_out164 = ctx.encrypt_public((out[2] >> 36) & 1);
    Ciphertext ct_out165 = ctx.encrypt_public((out[2] >> 37) & 1);
    Ciphertext ct_out166 = ctx.encrypt_public((out[2] >> 38) & 1);
    Ciphertext ct_out167 = ctx.encrypt_public((out[2] >> 39) & 1);
    Ciphertext ct_out168 = ctx.encrypt_public((out[2] >> 40) & 1);
    Ciphertext ct_out169 = ctx.encrypt_public((out[2] >> 41) & 1);
    Ciphertext ct_out170 = ctx.encrypt_public((out[2] >> 42) & 1);
    Ciphertext ct_out171 = ctx.encrypt_public((out[2] >> 43) & 1);
    Ciphertext ct_out172 = ctx.encrypt_public((out[2] >> 44) & 1);
    Ciphertext ct_out173 = ctx.encrypt_public((out[2] >> 45) & 1);
    Ciphertext ct_out174 = ctx.encrypt_public((out[2] >> 46) & 1);
    Ciphertext ct_out175 = ctx.encrypt_public((out[2] >> 47) & 1);
    Ciphertext ct_out176 = ctx.encrypt_public((out[2] >> 48) & 1);
    Ciphertext ct_out177 = ctx.encrypt_public((out[2] >> 49) & 1);
    Ciphertext ct_out178 = ctx.encrypt_public((out[2] >> 50) & 1);
    Ciphertext ct_out179 = ctx.encrypt_public((out[2] >> 51) & 1);
    Ciphertext ct_out180 = ctx.encrypt_public((out[2] >> 52) & 1);
    Ciphertext ct_out181 = ctx.encrypt_public((out[2] >> 53) & 1);
    Ciphertext ct_out182 = ctx.encrypt_public((out[2] >> 54) & 1);
    Ciphertext ct_out183 = ctx.encrypt_public((out[2] >> 55) & 1);
    Ciphertext ct_out184 = ctx.encrypt_public((out[2] >> 56) & 1);
    Ciphertext ct_out185 = ctx.encrypt_public((out[2] >> 57) & 1);
    Ciphertext ct_out186 = ctx.encrypt_public((out[2] >> 58) & 1);
    Ciphertext ct_out187 = ctx.encrypt_public((out[2] >> 59) & 1);
    Ciphertext ct_out188 = ctx.encrypt_public((out[2] >> 60) & 1);
    Ciphertext ct_out189 = ctx.encrypt_public((out[2] >> 61) & 1);
    Ciphertext ct_out190 = ctx.encrypt_public((out[2] >> 62) & 1);
    Ciphertext ct_out191 = ctx.encrypt_public((out[2] >> 63) & 1);

    auto fand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly and2 = ctx.genrate_lut(fand2);

    auto fnand2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nand2 = ctx.genrate_lut(fnand2);

    auto for2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly or2 = ctx.genrate_lut(for2);

    auto fnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly nor2 = ctx.genrate_lut(fnor2);

    auto fxor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 0;
            case 1:  return 1;
            case 2:  return 1;
            case 3:  return 0;
            default: assert(0);
        }
    };
    RotationPoly xor2 = ctx.genrate_lut(fxor2);

    auto fxnor2 = [](long i, long t) -> long {
        switch (i) {
            case 0:  return 1;
            case 1:  return 0;
            case 2:  return 0;
            case 3:  return 1;
            default: assert(0);
        }
    };
    RotationPoly xnor2 = ctx.genrate_lut(fxnor2);

    std::cerr << "\rLUT0   ";
    Ciphertext gin0 = 2 * ct_out138 + ct_out42;
    Ciphertext gout0 = ctx.eval_lut(&gin0, xor2);
    std::cerr << "\rLUT1   ";
    Ciphertext gin1 = 2 * ct_out167 + ct_out71;
    Ciphertext gout1 = ctx.eval_lut(&gin1, nand2);
    std::cerr << "\rLUT2   ";
    Ciphertext gin2 = 2 * ct_out143 + ct_out47;
    Ciphertext gout2 = ctx.eval_lut(&gin2, or2);
    std::cerr << "\rLUT3   ";
    Ciphertext gin3 = 2 * ct_out120 + ct_out24;
    Ciphertext gout3 = ctx.eval_lut(&gin3, xor2);
    std::cerr << "\rLUT4   ";
    Ciphertext gin4 = 2 * ct_out102 + ct_out6;
    Ciphertext gout4 = ctx.eval_lut(&gin4, nand2);
    std::cerr << "\rLUT5   ";
    Ciphertext gin5 = 2 * ct_out154 + ct_out58;
    Ciphertext gout5 = ctx.eval_lut(&gin5, xor2);
    std::cerr << "\rLUT6   ";
    Ciphertext gin6 = 2 * ct_out118 + ct_out22;
    Ciphertext gout6 = ctx.eval_lut(&gin6, nand2);
    std::cerr << "\rLUT7   ";
    Ciphertext gin7 = 2 * ct_out133 + ct_out37;
    Ciphertext gout7 = ctx.eval_lut(&gin7, xor2);
    std::cerr << "\rLUT8   ";
    Ciphertext gin8 = 2 * ct_out149 + ct_out53;
    Ciphertext gout8 = ctx.eval_lut(&gin8, nand2);
    std::cerr << "\rLUT9   ";
    Ciphertext gin9 = 2 * ct_out152 + ct_out56;
    Ciphertext gout9 = ctx.eval_lut(&gin9, xor2);
    std::cerr << "\rLUT10   ";
    Ciphertext gin10 = 2 * ct_out109 + ct_out13;
    Ciphertext gout10 = ctx.eval_lut(&gin10, nand2);
    std::cerr << "\rLUT11   ";
    Ciphertext gin11 = 2 * ct_out175 + ct_out79;
    Ciphertext gout11 = ctx.eval_lut(&gin11, nand2);
    std::cerr << "\rLUT12   ";
    Ciphertext gin12 = 2 * ct_out97 + ct_out1;
    Ciphertext gout12 = ctx.eval_lut(&gin12, xor2);
    std::cerr << "\rLUT13   ";
    Ciphertext gin13 = 2 * ct_out153 + ct_out57;
    Ciphertext gout13 = ctx.eval_lut(&gin13, xor2);
    std::cerr << "\rLUT14   ";
    Ciphertext gin14 = 2 * ct_out110 + ct_out14;
    Ciphertext gout14 = ctx.eval_lut(&gin14, nand2);
    std::cerr << "\rLUT15   ";
    Ciphertext gin15 = 2 * ct_out140 + ct_out44;
    Ciphertext gout15 = ctx.eval_lut(&gin15, nand2);
    std::cerr << "\rLUT16   ";
    Ciphertext gin16 = 2 * ct_out141 + ct_out45;
    Ciphertext gout16 = ctx.eval_lut(&gin16, nand2);
    std::cerr << "\rLUT17   ";
    Ciphertext gin17 = 2 * ct_out119 + ct_out23;
    Ciphertext gout17 = ctx.eval_lut(&gin17, xor2);
    std::cerr << "\rLUT18   ";
    Ciphertext gin18 = 2 * ct_out171 + ct_out75;
    Ciphertext gout18 = ctx.eval_lut(&gin18, xor2);
    std::cerr << "\rLUT19   ";
    Ciphertext gin19 = 2 * ct_out126 + ct_out30;
    Ciphertext gout19 = ctx.eval_lut(&gin19, xor2);
    std::cerr << "\rLUT20   ";
    Ciphertext gin20 = 2 * ct_out122 + ct_out26;
    Ciphertext gout20 = ctx.eval_lut(&gin20, nand2);
    std::cerr << "\rLUT21   ";
    Ciphertext gin21 = 2 * ct_out156 + ct_out60;
    Ciphertext gout21 = ctx.eval_lut(&gin21, xor2);
    std::cerr << "\rLUT22   ";
    Ciphertext gin22 = 2 * ct_out156 + ct_out60;
    Ciphertext gout22 = ctx.eval_lut(&gin22, nand2);
    std::cerr << "\rLUT23   ";
    Ciphertext gin23 = 2 * ct_out125 + ct_out29;
    Ciphertext gout23 = ctx.eval_lut(&gin23, xor2);
    std::cerr << "\rLUT24   ";
    Ciphertext gin24 = 2 * ct_out179 + ct_out83;
    Ciphertext gout24 = ctx.eval_lut(&gin24, xor2);
    std::cerr << "\rLUT25   ";
    Ciphertext gin25 = 2 * ct_out157 + ct_out61;
    Ciphertext gout25 = ctx.eval_lut(&gin25, xor2);
    std::cerr << "\rLUT26   ";
    Ciphertext gin26 = 2 * ct_out159 + ct_out63;
    Ciphertext gout26 = ctx.eval_lut(&gin26, nand2);
    std::cerr << "\rLUT27   ";
    Ciphertext gin27 = 2 * ct_out125 + ct_out29;
    Ciphertext gout27 = ctx.eval_lut(&gin27, nand2);
    std::cerr << "\rLUT28   ";
    Ciphertext gin28 = 2 * ct_out150 + ct_out54;
    Ciphertext gout28 = ctx.eval_lut(&gin28, xor2);
    std::cerr << "\rLUT29   ";
    Ciphertext gin29 = 2 * ct_out111 + ct_out15;
    Ciphertext gout29 = ctx.eval_lut(&gin29, or2);
    std::cerr << "\rLUT30   ";
    Ciphertext gin30 = 2 * ct_out130 + ct_out34;
    Ciphertext gout30 = ctx.eval_lut(&gin30, xor2);
    std::cerr << "\rLUT31   ";
    Ciphertext gin31 = 2 * ct_out146 + ct_out50;
    Ciphertext gout31 = ctx.eval_lut(&gin31, nand2);
    std::cerr << "\rLUT32   ";
    Ciphertext gin32 = 2 * ct_out130 + ct_out34;
    Ciphertext gout32 = ctx.eval_lut(&gin32, nand2);
    std::cerr << "\rLUT33   ";
    Ciphertext gin33 = 2 * ct_out191 + ct_out95;
    Ciphertext gout33 = ctx.eval_lut(&gin33, nand2);
    std::cerr << "\rLUT34   ";
    Ciphertext gin34 = 2 * ct_out127 + ct_out31;
    Ciphertext gout34 = ctx.eval_lut(&gin34, xor2);
    std::cerr << "\rLUT35   ";
    Ciphertext gin35 = 2 * ct_out105 + ct_out9;
    Ciphertext gout35 = ctx.eval_lut(&gin35, xor2);
    std::cerr << "\rLUT36   ";
    Ciphertext gin36 = 2 * ct_out151 + ct_out55;
    Ciphertext gout36 = ctx.eval_lut(&gin36, nand2);
    std::cerr << "\rLUT37   ";
    Ciphertext gin37 = 2 * ct_out146 + ct_out50;
    Ciphertext gout37 = ctx.eval_lut(&gin37, xor2);
    std::cerr << "\rLUT38   ";
    Ciphertext gin38 = 2 * ct_out111 + ct_out15;
    Ciphertext gout38 = ctx.eval_lut(&gin38, xor2);
    std::cerr << "\rLUT39   ";
    Ciphertext gin39 = 2 * ct_out138 + ct_out42;
    Ciphertext gout39 = ctx.eval_lut(&gin39, nand2);
    std::cerr << "\rLUT40   ";
    Ciphertext gin40 = 2 * ct_out121 + ct_out25;
    Ciphertext gout40 = ctx.eval_lut(&gin40, xor2);
    std::cerr << "\rLUT41   ";
    Ciphertext gin41 = 2 * ct_out99 + ct_out3;
    Ciphertext gout41 = ctx.eval_lut(&gin41, xor2);
    std::cerr << "\rLUT42   ";
    Ciphertext gin42 = 2 * ct_out160 + ct_out64;
    Ciphertext gout42 = ctx.eval_lut(&gin42, xor2);
    std::cerr << "\rLUT43   ";
    Ciphertext gin43 = 2 * ct_out120 + ct_out24;
    Ciphertext gout43 = ctx.eval_lut(&gin43, and2);
    std::cerr << "\rLUT44   ";
    Ciphertext gin44 = 2 * ct_out151 + ct_out55;
    Ciphertext gout44 = ctx.eval_lut(&gin44, or2);
    std::cerr << "\rLUT45   ";
    Ciphertext gin45 = 2 * ct_out142 + ct_out46;
    Ciphertext gout45 = ctx.eval_lut(&gin45, nand2);
    std::cerr << "\rLUT46   ";
    Ciphertext gin46 = 2 * ct_out145 + ct_out49;
    Ciphertext gout46 = ctx.eval_lut(&gin46, nand2);
    std::cerr << "\rLUT47   ";
    Ciphertext gin47 = 2 * ct_out158 + ct_out62;
    Ciphertext gout47 = ctx.eval_lut(&gin47, xor2);
    std::cerr << "\rLUT48   ";
    Ciphertext gin48 = 2 * ct_out161 + ct_out65;
    Ciphertext gout48 = ctx.eval_lut(&gin48, xor2);
    std::cerr << "\rLUT49   ";
    Ciphertext gin49 = 2 * ct_out180 + ct_out84;
    Ciphertext gout49 = ctx.eval_lut(&gin49, xor2);
    std::cerr << "\rLUT50   ";
    Ciphertext gin50 = 2 * ct_out133 + ct_out37;
    Ciphertext gout50 = ctx.eval_lut(&gin50, nand2);
    std::cerr << "\rLUT51   ";
    Ciphertext gin51 = 2 * ct_out172 + ct_out76;
    Ciphertext gout51 = ctx.eval_lut(&gin51, nand2);
    std::cerr << "\rLUT52   ";
    Ciphertext gin52 = 2 * ct_out103 + ct_out7;
    Ciphertext gout52 = ctx.eval_lut(&gin52, nand2);
    std::cerr << "\rLUT53   ";
    Ciphertext gin53 = 2 * ct_out183 + ct_out87;
    Ciphertext gout53 = ctx.eval_lut(&gin53, nand2);
    std::cerr << "\rLUT54   ";
    Ciphertext gin54 = 2 * ct_out152 + ct_out56;
    Ciphertext gout54 = ctx.eval_lut(&gin54, and2);
    std::cerr << "\rLUT55   ";
    Ciphertext gin55 = 2 * ct_out148 + ct_out52;
    Ciphertext gout55 = ctx.eval_lut(&gin55, nand2);
    std::cerr << "\rLUT56   ";
    Ciphertext gin56 = 2 * ct_out149 + ct_out53;
    Ciphertext gout56 = ctx.eval_lut(&gin56, xor2);
    std::cerr << "\rLUT57   ";
    Ciphertext gin57 = 2 * ct_out119 + ct_out23;
    Ciphertext gout57 = ctx.eval_lut(&gin57, nand2);
    std::cerr << "\rLUT58   ";
    Ciphertext gin58 = 2 * ct_out188 + ct_out92;
    Ciphertext gout58 = ctx.eval_lut(&gin58, xor2);
    std::cerr << "\rLUT59   ";
    Ciphertext gin59 = 2 * ct_out172 + ct_out76;
    Ciphertext gout59 = ctx.eval_lut(&gin59, xor2);
    std::cerr << "\rLUT60   ";
    Ciphertext gin60 = 2 * ct_out103 + ct_out7;
    Ciphertext gout60 = ctx.eval_lut(&gin60, xor2);
    std::cerr << "\rLUT61   ";
    Ciphertext gin61 = 2 * ct_out104 + ct_out8;
    Ciphertext gout61 = ctx.eval_lut(&gin61, xor2);
    std::cerr << "\rLUT62   ";
    Ciphertext gin62 = 2 * ct_out181 + ct_out85;
    Ciphertext gout62 = ctx.eval_lut(&gin62, xor2);
    std::cerr << "\rLUT63   ";
    Ciphertext gin63 = 2 * ct_out165 + ct_out69;
    Ciphertext gout63 = ctx.eval_lut(&gin63, xor2);
    std::cerr << "\rLUT64   ";
    Ciphertext gin64 = 2 * ct_out107 + ct_out11;
    Ciphertext gout64 = ctx.eval_lut(&gin64, nand2);
    std::cerr << "\rLUT65   ";
    Ciphertext gin65 = 2 * ct_out162 + ct_out66;
    Ciphertext gout65 = ctx.eval_lut(&gin65, xor2);
    std::cerr << "\rLUT66   ";
    Ciphertext gin66 = 2 * ct_out100 + ct_out4;
    Ciphertext gout66 = ctx.eval_lut(&gin66, nand2);
    std::cerr << "\rLUT67   ";
    Ciphertext gin67 = 2 * ct_out97 + ct_out1;
    Ciphertext gout67 = ctx.eval_lut(&gin67, nand2);
    std::cerr << "\rLUT68   ";
    Ciphertext gin68 = 2 * ct_out123 + ct_out27;
    Ciphertext gout68 = ctx.eval_lut(&gin68, xor2);
    std::cerr << "\rLUT69   ";
    Ciphertext gin69 = 2 * ct_out127 + ct_out31;
    Ciphertext gout69 = ctx.eval_lut(&gin69, nand2);
    std::cerr << "\rLUT70   ";
    Ciphertext gin70 = 2 * ct_out178 + ct_out82;
    Ciphertext gout70 = ctx.eval_lut(&gin70, xor2);
    std::cerr << "\rLUT71   ";
    Ciphertext gin71 = 2 * ct_out116 + ct_out20;
    Ciphertext gout71 = ctx.eval_lut(&gin71, xor2);
    std::cerr << "\rLUT72   ";
    Ciphertext gin72 = 2 * ct_out111 + ct_out15;
    Ciphertext gout72 = ctx.eval_lut(&gin72, nand2);
    std::cerr << "\rLUT73   ";
    Ciphertext gin73 = 2 * ct_out134 + ct_out38;
    Ciphertext gout73 = ctx.eval_lut(&gin73, nand2);
    std::cerr << "\rLUT74   ";
    Ciphertext gin74 = 2 * ct_out135 + ct_out39;
    Ciphertext gout74 = ctx.eval_lut(&gin74, xor2);
    std::cerr << "\rLUT75   ";
    Ciphertext gin75 = 2 * ct_out136 + ct_out40;
    Ciphertext gout75 = ctx.eval_lut(&gin75, and2);
    std::cerr << "\rLUT76   ";
    Ciphertext gin76 = 2 * ct_out174 + ct_out78;
    Ciphertext gout76 = ctx.eval_lut(&gin76, nand2);
    std::cerr << "\rLUT77   ";
    Ciphertext gin77 = 2 * ct_out129 + ct_out33;
    Ciphertext gout77 = ctx.eval_lut(&gin77, nand2);
    std::cerr << "\rLUT78   ";
    Ciphertext gin78 = 2 * ct_out150 + ct_out54;
    Ciphertext gout78 = ctx.eval_lut(&gin78, nand2);
    std::cerr << "\rLUT79   ";
    Ciphertext gin79 = 2 * ct_out173 + ct_out77;
    Ciphertext gout79 = ctx.eval_lut(&gin79, xor2);
    std::cerr << "\rLUT80   ";
    Ciphertext gin80 = 2 * ct_out112 + ct_out16;
    Ciphertext gout80 = ctx.eval_lut(&gin80, xor2);
    std::cerr << "\rLUT81   ";
    Ciphertext gin81 = 2 * ct_out164 + ct_out68;
    Ciphertext gout81 = ctx.eval_lut(&gin81, nand2);
    std::cerr << "\rLUT82   ";
    Ciphertext gin82 = 2 * ct_out189 + ct_out93;
    Ciphertext gout82 = ctx.eval_lut(&gin82, nand2);
    std::cerr << "\rLUT83   ";
    Ciphertext gin83 = 2 * ct_out177 + ct_out81;
    Ciphertext gout83 = ctx.eval_lut(&gin83, xor2);
    std::cerr << "\rLUT84   ";
    Ciphertext gin84 = 2 * ct_out184 + ct_out88;
    Ciphertext gout84 = ctx.eval_lut(&gin84, and2);
    std::cerr << "\rLUT85   ";
    Ciphertext gin85 = 2 * ct_out144 + ct_out48;
    Ciphertext gout85 = ctx.eval_lut(&gin85, xor2);
    std::cerr << "\rLUT86   ";
    Ciphertext gin86 = 2 * ct_out129 + ct_out33;
    Ciphertext gout86 = ctx.eval_lut(&gin86, xor2);
    std::cerr << "\rLUT87   ";
    Ciphertext gin87 = 2 * ct_out187 + ct_out91;
    Ciphertext gout87 = ctx.eval_lut(&gin87, xor2);
    std::cerr << "\rLUT88   ";
    Ciphertext gin88 = 2 * ct_out187 + ct_out91;
    Ciphertext gout88 = ctx.eval_lut(&gin88, nand2);
    std::cerr << "\rLUT89   ";
    Ciphertext gin89 = 2 * ct_out142 + ct_out46;
    Ciphertext gout89 = ctx.eval_lut(&gin89, xor2);
    std::cerr << "\rLUT90   ";
    Ciphertext gin90 = 2 * ct_out158 + ct_out62;
    Ciphertext gout90 = ctx.eval_lut(&gin90, nand2);
    std::cerr << "\rLUT91   ";
    Ciphertext gin91 = 2 * ct_out164 + ct_out68;
    Ciphertext gout91 = ctx.eval_lut(&gin91, xor2);
    std::cerr << "\rLUT92   ";
    Ciphertext gin92 = 2 * ct_out161 + ct_out65;
    Ciphertext gout92 = ctx.eval_lut(&gin92, nand2);
    std::cerr << "\rLUT93   ";
    Ciphertext gin93 = 2 * ct_out180 + ct_out84;
    Ciphertext gout93 = ctx.eval_lut(&gin93, nand2);
    std::cerr << "\rLUT94   ";
    Ciphertext gin94 = 2 * ct_out143 + ct_out47;
    Ciphertext gout94 = ctx.eval_lut(&gin94, xor2);
    std::cerr << "\rLUT95   ";
    Ciphertext gin95 = 2 * ct_out163 + ct_out67;
    Ciphertext gout95 = ctx.eval_lut(&gin95, xor2);
    std::cerr << "\rLUT96   ";
    Ciphertext gin96 = 2 * ct_out115 + ct_out19;
    Ciphertext gout96 = ctx.eval_lut(&gin96, nand2);
    std::cerr << "\rLUT97   ";
    Ciphertext gin97 = 2 * ct_out99 + ct_out3;
    Ciphertext gout97 = ctx.eval_lut(&gin97, nand2);
    std::cerr << "\rLUT98   ";
    Ciphertext gin98 = 2 * ct_out96 + ct_out0;
    Ciphertext gout98 = ctx.eval_lut(&gin98, xor2);
    std::cerr << "\rLUT99   ";
    Ciphertext gin99 = 2 * ct_out167 + ct_out71;
    Ciphertext gout99 = ctx.eval_lut(&gin99, xor2);
    std::cerr << "\rLUT100   ";
    Ciphertext gin100 = 2 * ct_out154 + ct_out58;
    Ciphertext gout100 = ctx.eval_lut(&gin100, nand2);
    std::cerr << "\rLUT101   ";
    Ciphertext gin101 = 2 * ct_out115 + ct_out19;
    Ciphertext gout101 = ctx.eval_lut(&gin101, xor2);
    std::cerr << "\rLUT102   ";
    Ciphertext gin102 = 2 * ct_out160 + ct_out64;
    Ciphertext gout102 = ctx.eval_lut(&gin102, and2);
    std::cerr << "\rLUT103   ";
    Ciphertext gin103 = 2 * ct_out109 + ct_out13;
    Ciphertext gout103 = ctx.eval_lut(&gin103, xor2);
    std::cerr << "\rLUT104   ";
    Ciphertext gin104 = 2 * ct_out119 + ct_out23;
    Ciphertext gout104 = ctx.eval_lut(&gin104, or2);
    std::cerr << "\rLUT105   ";
    Ciphertext gin105 = 2 * ct_out137 + ct_out41;
    Ciphertext gout105 = ctx.eval_lut(&gin105, nand2);
    std::cerr << "\rLUT106   ";
    Ciphertext gin106 = 2 * ct_out169 + ct_out73;
    Ciphertext gout106 = ctx.eval_lut(&gin106, nand2);
    std::cerr << "\rLUT107   ";
    Ciphertext gin107 = 2 * ct_out106 + ct_out10;
    Ciphertext gout107 = ctx.eval_lut(&gin107, xor2);
    std::cerr << "\rLUT108   ";
    Ciphertext gin108 = 2 * ct_out140 + ct_out44;
    Ciphertext gout108 = ctx.eval_lut(&gin108, xor2);
    std::cerr << "\rLUT109   ";
    Ciphertext gin109 = 2 * ct_out185 + ct_out89;
    Ciphertext gout109 = ctx.eval_lut(&gin109, xor2);
    std::cerr << "\rLUT110   ";
    Ciphertext gin110 = 2 * ct_out153 + ct_out57;
    Ciphertext gout110 = ctx.eval_lut(&gin110, nand2);
    std::cerr << "\rLUT111   ";
    Ciphertext gin111 = 2 * ct_out165 + ct_out69;
    Ciphertext gout111 = ctx.eval_lut(&gin111, nand2);
    std::cerr << "\rLUT112   ";
    Ciphertext gin112 = 2 * ct_out107 + ct_out11;
    Ciphertext gout112 = ctx.eval_lut(&gin112, xor2);
    std::cerr << "\rLUT113   ";
    Ciphertext gin113 = 2 * ct_out166 + ct_out70;
    Ciphertext gout113 = ctx.eval_lut(&gin113, nand2);
    std::cerr << "\rLUT114   ";
    Ciphertext gin114 = 2 * ct_out175 + ct_out79;
    Ciphertext gout114 = ctx.eval_lut(&gin114, xor2);
    std::cerr << "\rLUT115   ";
    Ciphertext gin115 = 2 * ct_out100 + ct_out4;
    Ciphertext gout115 = ctx.eval_lut(&gin115, xor2);
    std::cerr << "\rLUT116   ";
    Ciphertext gin116 = 2 * ct_out169 + ct_out73;
    Ciphertext gout116 = ctx.eval_lut(&gin116, xor2);
    std::cerr << "\rLUT117   ";
    Ciphertext gin117 = 2 * ct_out188 + ct_out92;
    Ciphertext gout117 = ctx.eval_lut(&gin117, nand2);
    std::cerr << "\rLUT118   ";
    Ciphertext gin118 = 2 * ct_out122 + ct_out26;
    Ciphertext gout118 = ctx.eval_lut(&gin118, xor2);
    std::cerr << "\rLUT119   ";
    Ciphertext gin119 = 2 * ct_out113 + ct_out17;
    Ciphertext gout119 = ctx.eval_lut(&gin119, nand2);
    std::cerr << "\rLUT120   ";
    Ciphertext gin120 = 2 * ct_out104 + ct_out8;
    Ciphertext gout120 = ctx.eval_lut(&gin120, and2);
    std::cerr << "\rLUT121   ";
    Ciphertext gin121 = 2 * ct_out131 + ct_out35;
    Ciphertext gout121 = ctx.eval_lut(&gin121, xor2);
    std::cerr << "\rLUT122   ";
    Ciphertext gin122 = 2 * ct_out181 + ct_out85;
    Ciphertext gout122 = ctx.eval_lut(&gin122, nand2);
    std::cerr << "\rLUT123   ";
    Ciphertext gin123 = 2 * ct_out171 + ct_out75;
    Ciphertext gout123 = ctx.eval_lut(&gin123, nand2);
    std::cerr << "\rLUT124   ";
    Ciphertext gin124 = 2 * ct_out166 + ct_out70;
    Ciphertext gout124 = ctx.eval_lut(&gin124, xor2);
    std::cerr << "\rLUT125   ";
    Ciphertext gin125 = 2 * ct_out182 + ct_out86;
    Ciphertext gout125 = ctx.eval_lut(&gin125, xor2);
    std::cerr << "\rLUT126   ";
    Ciphertext gin126 = 2 * ct_out127 + ct_out31;
    Ciphertext gout126 = ctx.eval_lut(&gin126, or2);
    std::cerr << "\rLUT127   ";
    Ciphertext gin127 = 2 * ct_out162 + ct_out66;
    Ciphertext gout127 = ctx.eval_lut(&gin127, nand2);
    std::cerr << "\rLUT128   ";
    Ciphertext gin128 = 2 * ct_out157 + ct_out61;
    Ciphertext gout128 = ctx.eval_lut(&gin128, nand2);
    std::cerr << "\rLUT129   ";
    Ciphertext gin129 = 2 * ct_out179 + ct_out83;
    Ciphertext gout129 = ctx.eval_lut(&gin129, nand2);
    std::cerr << "\rLUT130   ";
    Ciphertext gin130 = 2 * ct_out136 + ct_out40;
    Ciphertext gout130 = ctx.eval_lut(&gin130, xor2);
    std::cerr << "\rLUT131   ";
    Ciphertext gin131 = 2 * ct_out173 + ct_out77;
    Ciphertext gout131 = ctx.eval_lut(&gin131, nand2);
    std::cerr << "\rLUT132   ";
    Ciphertext gin132 = 2 * ct_out174 + ct_out78;
    Ciphertext gout132 = ctx.eval_lut(&gin132, xor2);
    std::cerr << "\rLUT133   ";
    Ciphertext gin133 = 2 * ct_out170 + ct_out74;
    Ciphertext gout133 = ctx.eval_lut(&gin133, nand2);
    std::cerr << "\rLUT134   ";
    Ciphertext gin134 = 2 * ct_out167 + ct_out71;
    Ciphertext gout134 = ctx.eval_lut(&gin134, or2);
    std::cerr << "\rLUT135   ";
    Ciphertext gin135 = 2 * ct_out105 + ct_out9;
    Ciphertext gout135 = ctx.eval_lut(&gin135, nand2);
    std::cerr << "\rLUT136   ";
    Ciphertext gin136 = 2 * ct_out112 + ct_out16;
    Ciphertext gout136 = ctx.eval_lut(&gin136, and2);
    std::cerr << "\rLUT137   ";
    Ciphertext gin137 = 2 * ct_out189 + ct_out93;
    Ciphertext gout137 = ctx.eval_lut(&gin137, xor2);
    std::cerr << "\rLUT138   ";
    Ciphertext gin138 = 2 * ct_out168 + ct_out72;
    Ciphertext gout138 = ctx.eval_lut(&gin138, xor2);
    std::cerr << "\rLUT139   ";
    Ciphertext gin139 = 2 * ct_out101 + ct_out5;
    Ciphertext gout139 = ctx.eval_lut(&gin139, xor2);
    std::cerr << "\rLUT140   ";
    Ciphertext gin140 = 2 * ct_out186 + ct_out90;
    Ciphertext gout140 = ctx.eval_lut(&gin140, xor2);
    std::cerr << "\rLUT141   ";
    Ciphertext gin141 = 2 * ct_out190 + ct_out94;
    Ciphertext gout141 = ctx.eval_lut(&gin141, xor2);
    std::cerr << "\rLUT142   ";
    Ciphertext gin142 = 2 * ct_out170 + ct_out74;
    Ciphertext gout142 = ctx.eval_lut(&gin142, xor2);
    std::cerr << "\rLUT143   ";
    Ciphertext gin143 = 2 * ct_out184 + ct_out88;
    Ciphertext gout143 = ctx.eval_lut(&gin143, xor2);
    std::cerr << "\rLUT144   ";
    Ciphertext gin144 = 2 * ct_out186 + ct_out90;
    Ciphertext gout144 = ctx.eval_lut(&gin144, nand2);
    std::cerr << "\rLUT145   ";
    Ciphertext gin145 = 2 * ct_out108 + ct_out12;
    Ciphertext gout145 = ctx.eval_lut(&gin145, xor2);
    std::cerr << "\rLUT146   ";
    Ciphertext gin146 = 2 * ct_out124 + ct_out28;
    Ciphertext gout146 = ctx.eval_lut(&gin146, nand2);
    std::cerr << "\rLUT147   ";
    Ciphertext gin147 = 2 * ct_out145 + ct_out49;
    Ciphertext gout147 = ctx.eval_lut(&gin147, xor2);
    std::cerr << "\rLUT148   ";
    Ciphertext gin148 = 2 * ct_out108 + ct_out12;
    Ciphertext gout148 = ctx.eval_lut(&gin148, nand2);
    std::cerr << "\rLUT149   ";
    Ciphertext gin149 = 2 * ct_out118 + ct_out22;
    Ciphertext gout149 = ctx.eval_lut(&gin149, xor2);
    std::cerr << "\rLUT150   ";
    Ciphertext gin150 = 2 * ct_out139 + ct_out43;
    Ciphertext gout150 = ctx.eval_lut(&gin150, xor2);
    std::cerr << "\rLUT151   ";
    Ciphertext gin151 = 2 * ct_out155 + ct_out59;
    Ciphertext gout151 = ctx.eval_lut(&gin151, nand2);
    std::cerr << "\rLUT152   ";
    Ciphertext gin152 = 2 * ct_out98 + ct_out2;
    Ciphertext gout152 = ctx.eval_lut(&gin152, xor2);
    std::cerr << "\rLUT153   ";
    Ciphertext gin153 = 2 * ct_out114 + ct_out18;
    Ciphertext gout153 = ctx.eval_lut(&gin153, nand2);
    std::cerr << "\rLUT154   ";
    Ciphertext gin154 = 2 * ct_out102 + ct_out6;
    Ciphertext gout154 = ctx.eval_lut(&gin154, xor2);
    std::cerr << "\rLUT155   ";
    Ciphertext gin155 = 2 * ct_out98 + ct_out2;
    Ciphertext gout155 = ctx.eval_lut(&gin155, nand2);
    std::cerr << "\rLUT156   ";
    Ciphertext gin156 = 2 * ct_out177 + ct_out81;
    Ciphertext gout156 = ctx.eval_lut(&gin156, nand2);
    std::cerr << "\rLUT157   ";
    Ciphertext gin157 = 2 * ct_out132 + ct_out36;
    Ciphertext gout157 = ctx.eval_lut(&gin157, xor2);
    std::cerr << "\rLUT158   ";
    Ciphertext gin158 = 2 * ct_out124 + ct_out28;
    Ciphertext gout158 = ctx.eval_lut(&gin158, xor2);
    std::cerr << "\rLUT159   ";
    Ciphertext gin159 = 2 * ct_out144 + ct_out48;
    Ciphertext gout159 = ctx.eval_lut(&gin159, and2);
    std::cerr << "\rLUT160   ";
    Ciphertext gin160 = 2 * ct_out132 + ct_out36;
    Ciphertext gout160 = ctx.eval_lut(&gin160, nand2);
    std::cerr << "\rLUT161   ";
    Ciphertext gin161 = 2 * ct_out175 + ct_out79;
    Ciphertext gout161 = ctx.eval_lut(&gin161, or2);
    std::cerr << "\rLUT162   ";
    Ciphertext gin162 = 2 * ct_out151 + ct_out55;
    Ciphertext gout162 = ctx.eval_lut(&gin162, xor2);
    std::cerr << "\rLUT163   ";
    Ciphertext gin163 = 2 * ct_out155 + ct_out59;
    Ciphertext gout163 = ctx.eval_lut(&gin163, xor2);
    std::cerr << "\rLUT164   ";
    Ciphertext gin164 = 2 * ct_out114 + ct_out18;
    Ciphertext gout164 = ctx.eval_lut(&gin164, xor2);
    std::cerr << "\rLUT165   ";
    Ciphertext gin165 = 2 * ct_out183 + ct_out87;
    Ciphertext gout165 = ctx.eval_lut(&gin165, or2);
    std::cerr << "\rLUT166   ";
    Ciphertext gin166 = 2 * ct_out148 + ct_out52;
    Ciphertext gout166 = ctx.eval_lut(&gin166, xor2);
    std::cerr << "\rLUT167   ";
    Ciphertext gin167 = 2 * ct_out117 + ct_out21;
    Ciphertext gout167 = ctx.eval_lut(&gin167, nand2);
    std::cerr << "\rLUT168   ";
    Ciphertext gin168 = 2 * ct_out185 + ct_out89;
    Ciphertext gout168 = ctx.eval_lut(&gin168, nand2);
    std::cerr << "\rLUT169   ";
    Ciphertext gin169 = 2 * ct_out137 + ct_out41;
    Ciphertext gout169 = ctx.eval_lut(&gin169, xor2);
    std::cerr << "\rLUT170   ";
    Ciphertext gin170 = 2 * ct_out96 + ct_out0;
    Ciphertext gout170 = ctx.eval_lut(&gin170, and2);
    std::cerr << "\rLUT171   ";
    Ciphertext gin171 = 2 * ct_out183 + ct_out87;
    Ciphertext gout171 = ctx.eval_lut(&gin171, xor2);
    std::cerr << "\rLUT172   ";
    Ciphertext gin172 = 2 * ct_out168 + ct_out72;
    Ciphertext gout172 = ctx.eval_lut(&gin172, and2);
    std::cerr << "\rLUT173   ";
    Ciphertext gin173 = 2 * ct_out182 + ct_out86;
    Ciphertext gout173 = ctx.eval_lut(&gin173, nand2);
    std::cerr << "\rLUT174   ";
    Ciphertext gin174 = 2 * ct_out159 + ct_out63;
    Ciphertext gout174 = ctx.eval_lut(&gin174, xor2);
    std::cerr << "\rLUT175   ";
    Ciphertext gin175 = 2 * ct_out110 + ct_out14;
    Ciphertext gout175 = ctx.eval_lut(&gin175, xor2);
    std::cerr << "\rLUT176   ";
    Ciphertext gin176 = 2 * ct_out126 + ct_out30;
    Ciphertext gout176 = ctx.eval_lut(&gin176, nand2);
    std::cerr << "\rLUT177   ";
    Ciphertext gin177 = 2 * ct_out106 + ct_out10;
    Ciphertext gout177 = ctx.eval_lut(&gin177, nand2);
    std::cerr << "\rLUT178   ";
    Ciphertext gin178 = 2 * ct_out113 + ct_out17;
    Ciphertext gout178 = ctx.eval_lut(&gin178, xor2);
    std::cerr << "\rLUT179   ";
    Ciphertext gin179 = 2 * ct_out128 + ct_out32;
    Ciphertext gout179 = ctx.eval_lut(&gin179, xor2);
    std::cerr << "\rLUT180   ";
    Ciphertext gin180 = 2 * ct_out141 + ct_out45;
    Ciphertext gout180 = ctx.eval_lut(&gin180, xor2);
    std::cerr << "\rLUT181   ";
    Ciphertext gin181 = 2 * ct_out123 + ct_out27;
    Ciphertext gout181 = ctx.eval_lut(&gin181, nand2);
    std::cerr << "\rLUT182   ";
    Ciphertext gin182 = 2 * ct_out128 + ct_out32;
    Ciphertext gout182 = ctx.eval_lut(&gin182, and2);
    std::cerr << "\rLUT183   ";
    Ciphertext gin183 = 2 * ct_out135 + ct_out39;
    Ciphertext gout183 = ctx.eval_lut(&gin183, nand2);
    std::cerr << "\rLUT184   ";
    Ciphertext gin184 = 2 * ct_out134 + ct_out38;
    Ciphertext gout184 = ctx.eval_lut(&gin184, xor2);
    std::cerr << "\rLUT185   ";
    Ciphertext gin185 = 2 * ct_out178 + ct_out82;
    Ciphertext gout185 = ctx.eval_lut(&gin185, nand2);
    std::cerr << "\rLUT186   ";
    Ciphertext gin186 = 2 * ct_out116 + ct_out20;
    Ciphertext gout186 = ctx.eval_lut(&gin186, nand2);
    std::cerr << "\rLUT187   ";
    Ciphertext gin187 = 2 * ct_out147 + ct_out51;
    Ciphertext gout187 = ctx.eval_lut(&gin187, nand2);
    std::cerr << "\rLUT188   ";
    Ciphertext gin188 = 2 * ct_out135 + ct_out39;
    Ciphertext gout188 = ctx.eval_lut(&gin188, or2);
    std::cerr << "\rLUT189   ";
    Ciphertext gin189 = 2 * ct_out176 + ct_out80;
    Ciphertext gout189 = ctx.eval_lut(&gin189, xor2);
    std::cerr << "\rLUT190   ";
    Ciphertext gin190 = 2 * ct_out131 + ct_out35;
    Ciphertext gout190 = ctx.eval_lut(&gin190, nand2);
    std::cerr << "\rLUT191   ";
    Ciphertext gin191 = 2 * ct_out176 + ct_out80;
    Ciphertext gout191 = ctx.eval_lut(&gin191, and2);
    std::cerr << "\rLUT192   ";
    Ciphertext gin192 = 2 * ct_out101 + ct_out5;
    Ciphertext gout192 = ctx.eval_lut(&gin192, nand2);
    std::cerr << "\rLUT193   ";
    Ciphertext gin193 = 2 * ct_out147 + ct_out51;
    Ciphertext gout193 = ctx.eval_lut(&gin193, xor2);
    std::cerr << "\rLUT194   ";
    Ciphertext gin194 = 2 * ct_out143 + ct_out47;
    Ciphertext gout194 = ctx.eval_lut(&gin194, nand2);
    std::cerr << "\rLUT195   ";
    Ciphertext gin195 = 2 * ct_out191 + ct_out95;
    Ciphertext gout195 = ctx.eval_lut(&gin195, xor2);
    std::cerr << "\rLUT196   ";
    Ciphertext gin196 = 2 * ct_out117 + ct_out21;
    Ciphertext gout196 = ctx.eval_lut(&gin196, xor2);
    std::cerr << "\rLUT197   ";
    Ciphertext gin197 = 2 * ct_out121 + ct_out25;
    Ciphertext gout197 = ctx.eval_lut(&gin197, nand2);
    std::cerr << "\rLUT198   ";
    Ciphertext gin198 = 2 * ct_out163 + ct_out67;
    Ciphertext gout198 = ctx.eval_lut(&gin198, nand2);
    std::cerr << "\rLUT199   ";
    Ciphertext gin199 = 2 * ct_out190 + ct_out94;
    Ciphertext gout199 = ctx.eval_lut(&gin199, nand2);
    std::cerr << "\rLUT200   ";
    Ciphertext gin200 = 2 * ct_out139 + ct_out43;
    Ciphertext gout200 = ctx.eval_lut(&gin200, nand2);
    std::cerr << "\rLUT201   ";
    Ciphertext gin201 = 2 * gout43 + gout40;
    Ciphertext gout201 = ctx.eval_lut(&gin201, xor2);
    std::cerr << "\rLUT202   ";
    Ciphertext gin202 = 2 * gout85 + gout9;
    Ciphertext gout202 = ctx.eval_lut(&gin202, and2);
    std::cerr << "\rLUT203   ";
    Ciphertext gin203 = 2 * gout102 + gout48;
    Ciphertext gout203 = ctx.eval_lut(&gin203, nand2);
    std::cerr << "\rLUT204   ";
    Ciphertext gin204 = 2 * gout84 + gout109;
    Ciphertext gout204 = ctx.eval_lut(&gin204, xor2);
    std::cerr << "\rLUT205   ";
    Ciphertext gin205 = 2 * gout98 + gout61;
    Ciphertext gout205 = ctx.eval_lut(&gin205, and2);
    std::cerr << "\rLUT206   ";
    Ciphertext gin206 = 2 * gout179 + gout130;
    Ciphertext gout206 = ctx.eval_lut(&gin206, xor2);
    std::cerr << "\rLUT207   ";
    Ciphertext gin207 = 2 * gout120 + gout35;
    Ciphertext gout207 = ctx.eval_lut(&gin207, nand2);
    std::cerr << "\rLUT208   ";
    Ciphertext gin208 = 2 * gout42 + gout138;
    Ciphertext gout208 = ctx.eval_lut(&gin208, and2);
    std::cerr << "\rLUT209   ";
    Ciphertext gin209 = 2 * gout189 + gout143;
    Ciphertext gout209 = ctx.eval_lut(&gin209, xor2);
    std::cerr << "\rLUT210   ";
    Ciphertext gin210 = 2 * gout159 + gout147;
    Ciphertext gout210 = ctx.eval_lut(&gin210, xnor2);
    std::cerr << "\rLUT211   ";
    Ciphertext gin211 = 2 * gout191 + gout83;
    Ciphertext gout211 = ctx.eval_lut(&gin211, nand2);
    std::cerr << "\rLUT212   ";
    Ciphertext gin212 = 2 * gout84 + gout109;
    Ciphertext gout212 = ctx.eval_lut(&gin212, nand2);
    std::cerr << "\rLUT213   ";
    Ciphertext gin213 = 2 * gout75 + gout169;
    Ciphertext gout213 = ctx.eval_lut(&gin213, xor2);
    std::cerr << "\rLUT214   ";
    Ciphertext gin214 = 2 * gout54 + gout13;
    Ciphertext gout214 = ctx.eval_lut(&gin214, nand2);
    std::cerr << "\rLUT215   ";
    Ciphertext gin215 = 2 * gout42 + gout138;
    Ciphertext gout215 = ctx.eval_lut(&gin215, xor2);
    std::cerr << "\rLUT216   ";
    Ciphertext gin216 = 2 * gout136 + gout178;
    Ciphertext gout216 = ctx.eval_lut(&gin216, xnor2);
    std::cerr << "\rLUT217   ";
    Ciphertext gin217 = 2 * gout43 + gout40;
    Ciphertext gout217 = ctx.eval_lut(&gin217, nand2);
    std::cerr << "\rLUT218   ";
    Ciphertext gin218 = 2 * gout80 + gout3;
    Ciphertext gout218 = ctx.eval_lut(&gin218, xor2);
    std::cerr << "\rLUT219   ";
    Ciphertext gin219 = 2 * gout85 + gout9;
    Ciphertext gout219 = ctx.eval_lut(&gin219, xor2);
    std::cerr << "\rLUT220   ";
    Ciphertext gin220 = 2 * gout170 + gout12;
    Ciphertext gout220 = ctx.eval_lut(&gin220, xnor2);
    std::cerr << "\rLUT221   ";
    Ciphertext gin221 = 2 * gout54 + gout13;
    Ciphertext gout221 = ctx.eval_lut(&gin221, xor2);
    std::cerr << "\rLUT222   ";
    Ciphertext gin222 = 2 * gout98 + gout61;
    Ciphertext gout222 = ctx.eval_lut(&gin222, xor2);
    std::cerr << "\rLUT223   ";
    Ciphertext gin223 = 2 * gout120 + gout35;
    Ciphertext gout223 = ctx.eval_lut(&gin223, xor2);
    std::cerr << "\rLUT224   ";
    Ciphertext gin224 = 2 * gout80 + gout3;
    Ciphertext gout224 = ctx.eval_lut(&gin224, and2);
    std::cerr << "\rLUT225   ";
    Ciphertext gin225 = 2 * gout182 + gout86;
    Ciphertext gout225 = ctx.eval_lut(&gin225, nand2);
    std::cerr << "\rLUT226   ";
    Ciphertext gin226 = 2 * gout159 + gout147;
    Ciphertext gout226 = ctx.eval_lut(&gin226, nand2);
    std::cerr << "\rLUT227   ";
    Ciphertext gin227 = 2 * gout102 + gout48;
    Ciphertext gout227 = ctx.eval_lut(&gin227, xor2);
    std::cerr << "\rLUT228   ";
    Ciphertext gin228 = 2 * gout172 + gout116;
    Ciphertext gout228 = ctx.eval_lut(&gin228, nand2);
    std::cerr << "\rLUT229   ";
    Ciphertext gin229 = 2 * gout172 + gout116;
    Ciphertext gout229 = ctx.eval_lut(&gin229, xor2);
    std::cerr << "\rLUT230   ";
    Ciphertext gin230 = 2 * gout170 + gout12;
    Ciphertext gout230 = ctx.eval_lut(&gin230, nand2);
    std::cerr << "\rLUT231   ";
    Ciphertext gin231 = 2 * gout179 + gout130;
    Ciphertext gout231 = ctx.eval_lut(&gin231, and2);
    std::cerr << "\rLUT232   ";
    Ciphertext gin232 = 2 * gout189 + gout143;
    Ciphertext gout232 = ctx.eval_lut(&gin232, and2);
    std::cerr << "\rLUT233   ";
    Ciphertext gin233 = 2 * gout182 + gout86;
    Ciphertext gout233 = ctx.eval_lut(&gin233, xnor2);
    std::cerr << "\rLUT234   ";
    Ciphertext gin234 = 2 * gout191 + gout83;
    Ciphertext gout234 = ctx.eval_lut(&gin234, xor2);
    std::cerr << "\rLUT235   ";
    Ciphertext gin235 = 2 * gout75 + gout169;
    Ciphertext gout235 = ctx.eval_lut(&gin235, nand2);
    std::cerr << "\rLUT236   ";
    Ciphertext gin236 = 2 * gout136 + gout178;
    Ciphertext gout236 = ctx.eval_lut(&gin236, nand2);
    std::cerr << "\rLUT237   ";
    Ciphertext gin237 = 2 * gout92 + gout203;
    Ciphertext gout237 = ctx.eval_lut(&gin237, nand2);
    std::cerr << "\rLUT238   ";
    Ciphertext gin238 = 2 * gout168 + gout212;
    Ciphertext gout238 = ctx.eval_lut(&gin238, nand2);
    std::cerr << "\rLUT239   ";
    Ciphertext gin239 = 2 * gout215 + gout209;
    Ciphertext gout239 = ctx.eval_lut(&gin239, xor2);
    std::cerr << "\rLUT240   ";
    Ciphertext gin240 = 2 * gout216 + gout201;
    Ciphertext gout240 = ctx.eval_lut(&gin240, xnor2);
    std::cerr << "\rLUT241   ";
    Ciphertext gin241 = 2 * gout135 + gout207;
    Ciphertext gout241 = ctx.eval_lut(&gin241, nand2);
    std::cerr << "\rLUT242   ";
    Ciphertext gin242 = 2 * gout197 + gout217;
    Ciphertext gout242 = ctx.eval_lut(&gin242, nand2);
    std::cerr << "\rINV243   ";
    Ciphertext gout243 = 1 - gout208;

    std::cerr << "\rLUT244   ";
    Ciphertext gin244 = 2 * (1 - gout216) + gout201;
    Ciphertext gout244 = ctx.eval_lut(&gin244, and2);
    std::cerr << "\rLUT245   ";
    Ciphertext gin245 = 2 * gout222 + gout218;
    Ciphertext gout245 = ctx.eval_lut(&gin245, xor2);
    std::cerr << "\rLUT246   ";
    Ciphertext gin246 = 2 * gout206 + gout219;
    Ciphertext gout246 = ctx.eval_lut(&gin246, xor2);
    std::cerr << "\rLUT247   ";
    Ciphertext gin247 = 2 * gout210 + gout221;
    Ciphertext gout247 = ctx.eval_lut(&gin247, xnor2);
    std::cerr << "\rLUT248   ";
    Ciphertext gin248 = 2 * gout46 + gout226;
    Ciphertext gout248 = ctx.eval_lut(&gin248, nand2);
    std::cerr << "\rLUT249   ";
    Ciphertext gin249 = 2 * gout110 + gout214;
    Ciphertext gout249 = ctx.eval_lut(&gin249, nand2);
    std::cerr << "\rLUT250   ";
    Ciphertext gin250 = 2 * gout220 + gout223;
    Ciphertext gout250 = ctx.eval_lut(&gin250, xnor2);
    std::cerr << "\rLUT251   ";
    Ciphertext gin251 = 2 * gout222 + gout218;
    Ciphertext gout251 = ctx.eval_lut(&gin251, and2);
    std::cerr << "\rLUT252   ";
    Ciphertext gin252 = 2 * gout224 + gout240;
    Ciphertext gout252 = ctx.eval_lut(&gin252, xor2);
    std::cerr << "\rLUT253   ";
    Ciphertext gin253 = 2 * gout206 + gout219;
    Ciphertext gout253 = ctx.eval_lut(&gin253, and2);
    std::cerr << "\rLUT254   ";
    Ciphertext gin254 = 2 * gout227 + gout229;
    Ciphertext gout254 = ctx.eval_lut(&gin254, nand2);
    std::cerr << "\rLUT255   ";
    Ciphertext gin255 = 2 * gout106 + gout228;
    Ciphertext gout255 = ctx.eval_lut(&gin255, nand2);
    std::cerr << "\rLUT256   ";
    Ciphertext gin256 = 2 * gout67 + gout230;
    Ciphertext gout256 = ctx.eval_lut(&gin256, nand2);
    std::cerr << "\rLUT257   ";
    Ciphertext gin257 = 2 * (1 - gout220) + gout223;
    Ciphertext gout257 = ctx.eval_lut(&gin257, and2);
    std::cerr << "\rLUT258   ";
    Ciphertext gin258 = 2 * gout215 + gout209;
    Ciphertext gout258 = ctx.eval_lut(&gin258, and2);
    std::cerr << "\rLUT259   ";
    Ciphertext gin259 = 2 * gout156 + gout211;
    Ciphertext gout259 = ctx.eval_lut(&gin259, nand2);
    std::cerr << "\rLUT260   ";
    Ciphertext gin260 = 2 * (1 - gout233) + gout213;
    Ciphertext gout260 = ctx.eval_lut(&gin260, and2);
    std::cerr << "\rLUT261   ";
    Ciphertext gin261 = 2 * gout234 + gout204;
    Ciphertext gout261 = ctx.eval_lut(&gin261, xor2);
    std::cerr << "\rLUT262   ";
    Ciphertext gin262 = 2 * gout233 + gout213;
    Ciphertext gout262 = ctx.eval_lut(&gin262, xnor2);
    std::cerr << "\rLUT263   ";
    Ciphertext gin263 = 2 * gout119 + gout236;
    Ciphertext gout263 = ctx.eval_lut(&gin263, nand2);
    std::cerr << "\rLUT264   ";
    Ciphertext gin264 = 2 * gout224 + gout240;
    Ciphertext gout264 = ctx.eval_lut(&gin264, and2);
    std::cerr << "\rLUT265   ";
    Ciphertext gin265 = 2 * gout77 + gout225;
    Ciphertext gout265 = ctx.eval_lut(&gin265, nand2);
    std::cerr << "\rLUT266   ";
    Ciphertext gin266 = 2 * gout234 + gout204;
    Ciphertext gout266 = ctx.eval_lut(&gin266, nand2);
    std::cerr << "\rLUT267   ";
    Ciphertext gin267 = 2 * (1 - gout210) + gout221;
    Ciphertext gout267 = ctx.eval_lut(&gin267, and2);
    std::cerr << "\rLUT268   ";
    Ciphertext gin268 = 2 * gout105 + gout235;
    Ciphertext gout268 = ctx.eval_lut(&gin268, nand2);
    std::cerr << "\rLUT269   ";
    Ciphertext gin269 = 2 * gout227 + gout229;
    Ciphertext gout269 = ctx.eval_lut(&gin269, xor2);
    std::cerr << "\rINV270   ";
    Ciphertext gout270 = 1 - gout239;

    std::cerr << "\rLUT271   ";
    Ciphertext gin271 = 2 * gout242 + gout118;
    Ciphertext gout271 = ctx.eval_lut(&gin271, nand2);
    std::cerr << "\rLUT272   ";
    Ciphertext gin272 = 2 * gout237 + gout65;
    Ciphertext gout272 = ctx.eval_lut(&gin272, xnor2);
    std::cerr << "\rLUT273   ";
    Ciphertext gin273 = 2 * gout237 + gout65;
    Ciphertext gout273 = ctx.eval_lut(&gin273, nand2);
    std::cerr << "\rLUT274   ";
    Ciphertext gin274 = 2 * gout245 + gout246;
    Ciphertext gout274 = ctx.eval_lut(&gin274, and2);
    std::cerr << "\rLUT275   ";
    Ciphertext gin275 = 2 * gout245 + gout246;
    Ciphertext gout275 = ctx.eval_lut(&gin275, xor2);
    std::cerr << "\rLUT276   ";
    Ciphertext gin276 = 2 * gout238 + gout140;
    Ciphertext gout276 = ctx.eval_lut(&gin276, xor2);
    std::cerr << "\rLUT277   ";
    Ciphertext gin277 = 2 * gout205 + gout250;
    Ciphertext gout277 = ctx.eval_lut(&gin277, xnor2);
    std::cerr << "\rLUT278   ";
    Ciphertext gin278 = 2 * gout249 + gout5;
    Ciphertext gout278 = ctx.eval_lut(&gin278, nand2);
    std::cerr << "\rLUT279   ";
    Ciphertext gin279 = 2 * gout241 + gout107;
    Ciphertext gout279 = ctx.eval_lut(&gin279, nand2);
    std::cerr << "\rLUT280   ";
    Ciphertext gin280 = 2 * gout242 + gout118;
    Ciphertext gout280 = ctx.eval_lut(&gin280, xor2);
    std::cerr << "\rLUT281   ";
    Ciphertext gin281 = 2 * gout255 + gout142;
    Ciphertext gout281 = ctx.eval_lut(&gin281, nand2);
    std::cerr << "\rLUT282   ";
    Ciphertext gin282 = 2 * gout202 + gout247;
    Ciphertext gout282 = ctx.eval_lut(&gin282, and2);
    std::cerr << "\rLUT283   ";
    Ciphertext gin283 = 2 * gout255 + gout142;
    Ciphertext gout283 = ctx.eval_lut(&gin283, xor2);
    std::cerr << "\rLUT284   ";
    Ciphertext gin284 = 2 * gout238 + gout140;
    Ciphertext gout284 = ctx.eval_lut(&gin284, nand2);
    std::cerr << "\rLUT285   ";
    Ciphertext gin285 = 2 * gout202 + gout247;
    Ciphertext gout285 = ctx.eval_lut(&gin285, xor2);
    std::cerr << "\rLUT286   ";
    Ciphertext gin286 = 2 * gout205 + gout250;
    Ciphertext gout286 = ctx.eval_lut(&gin286, and2);
    std::cerr << "\rLUT287   ";
    Ciphertext gin287 = 2 * gout256 + gout152;
    Ciphertext gout287 = ctx.eval_lut(&gin287, nand2);
    std::cerr << "\rLUT288   ";
    Ciphertext gin288 = 2 * gout256 + gout152;
    Ciphertext gout288 = ctx.eval_lut(&gin288, xnor2);
    std::cerr << "\rLUT289   ";
    Ciphertext gin289 = 2 * gout241 + gout107;
    Ciphertext gout289 = ctx.eval_lut(&gin289, xor2);
    std::cerr << "\rLUT290   ";
    Ciphertext gin290 = 2 * gout259 + gout70;
    Ciphertext gout290 = ctx.eval_lut(&gin290, nand2);
    std::cerr << "\rLUT291   ";
    Ciphertext gin291 = 2 * gout231 + gout262;
    Ciphertext gout291 = ctx.eval_lut(&gin291, and2);
    std::cerr << "\rLUT292   ";
    Ciphertext gin292 = 2 * gout232 + gout261;
    Ciphertext gout292 = ctx.eval_lut(&gin292, xor2);
    std::cerr << "\rLUT293   ";
    Ciphertext gin293 = 2 * gout231 + gout262;
    Ciphertext gout293 = ctx.eval_lut(&gin293, xnor2);
    std::cerr << "\rLUT294   ";
    Ciphertext gin294 = 2 * gout263 + gout164;
    Ciphertext gout294 = ctx.eval_lut(&gin294, nand2);
    std::cerr << "\rLUT295   ";
    Ciphertext gin295 = 2 * gout249 + gout5;
    Ciphertext gout295 = ctx.eval_lut(&gin295, xor2);
    std::cerr << "\rINV296   ";
    Ciphertext gout296 = 1 - gout258;

    std::cerr << "\rLUT297   ";
    Ciphertext gin297 = 2 * gout265 + gout30;
    Ciphertext gout297 = ctx.eval_lut(&gin297, xnor2);
    std::cerr << "\rLUT298   ";
    Ciphertext gin298 = 2 * gout232 + gout261;
    Ciphertext gout298 = ctx.eval_lut(&gin298, nand2);
    std::cerr << "\rLUT299   ";
    Ciphertext gin299 = 2 * gout248 + gout37;
    Ciphertext gout299 = ctx.eval_lut(&gin299, nand2);
    std::cerr << "\rLUT300   ";
    Ciphertext gin300 = 2 * gout265 + gout30;
    Ciphertext gout300 = ctx.eval_lut(&gin300, nand2);
    std::cerr << "\rLUT301   ";
    Ciphertext gin301 = 2 * gout248 + gout37;
    Ciphertext gout301 = ctx.eval_lut(&gin301, xnor2);
    std::cerr << "\rLUT302   ";
    Ciphertext gin302 = 2 * gout208 + gout269;
    Ciphertext gout302 = ctx.eval_lut(&gin302, nand2);
    std::cerr << "\rLUT303   ";
    Ciphertext gin303 = 2 * gout259 + gout70;
    Ciphertext gout303 = ctx.eval_lut(&gin303, xnor2);
    std::cerr << "\rLUT304   ";
    Ciphertext gin304 = 2 * gout243 + gout269;
    Ciphertext gout304 = ctx.eval_lut(&gin304, xnor2);
    std::cerr << "\rLUT305   ";
    Ciphertext gin305 = 2 * gout244 + gout264;
    Ciphertext gout305 = ctx.eval_lut(&gin305, or2);
    std::cerr << "\rLUT306   ";
    Ciphertext gin306 = 2 * gout263 + gout164;
    Ciphertext gout306 = ctx.eval_lut(&gin306, xnor2);
    std::cerr << "\rLUT307   ";
    Ciphertext gin307 = 2 * gout267 + gout282;
    Ciphertext gout307 = ctx.eval_lut(&gin307, or2);
    std::cerr << "\rLUT308   ";
    Ciphertext gin308 = 2 * gout268 + gout0;
    Ciphertext gout308 = ctx.eval_lut(&gin308, xor2);
    std::cerr << "\rLUT309   ";
    Ciphertext gin309 = 2 * gout268 + gout0;
    Ciphertext gout309 = ctx.eval_lut(&gin309, nand2);
    std::cerr << "\rLUT310   ";
    Ciphertext gin310 = 2 * gout239 + gout275;
    Ciphertext gout310 = ctx.eval_lut(&gin310, and2);
    std::cerr << "\rLUT311   ";
    Ciphertext gin311 = 2 * gout127 + gout273;
    Ciphertext gout311 = ctx.eval_lut(&gin311, nand2);
    std::cerr << "\rLUT312   ";
    Ciphertext gin312 = 2 * gout177 + gout279;
    Ciphertext gout312 = ctx.eval_lut(&gin312, nand2);
    std::cerr << "\rLUT313   ";
    Ciphertext gin313 = 2 * gout20 + gout271;
    Ciphertext gout313 = ctx.eval_lut(&gin313, nand2);
    std::cerr << "\rLUT314   ";
    Ciphertext gin314 = 2 * gout257 + gout286;
    Ciphertext gout314 = ctx.eval_lut(&gin314, or2);
    std::cerr << "\rLUT315   ";
    Ciphertext gin315 = 2 * gout133 + gout281;
    Ciphertext gout315 = ctx.eval_lut(&gin315, nand2);
    std::cerr << "\rLUT316   ";
    Ciphertext gin316 = 2 * gout270 + gout275;
    Ciphertext gout316 = ctx.eval_lut(&gin316, xnor2);
    std::cerr << "\rLUT317   ";
    Ciphertext gin317 = 2 * gout277 + gout252;
    Ciphertext gout317 = ctx.eval_lut(&gin317, xnor2);
    std::cerr << "\rLUT318   ";
    Ciphertext gin318 = 2 * gout100 + gout278;
    Ciphertext gout318 = ctx.eval_lut(&gin318, nand2);
    std::cerr << "\rLUT319   ";
    Ciphertext gin319 = 2 * gout293 + gout285;
    Ciphertext gout319 = ctx.eval_lut(&gin319, xnor2);
    std::cerr << "\rLUT320   ";
    Ciphertext gin320 = 2 * gout301 + gout295;
    Ciphertext gout320 = ctx.eval_lut(&gin320, xnor2);
    std::cerr << "\rLUT321   ";
    Ciphertext gin321 = 2 * gout144 + gout284;
    Ciphertext gout321 = ctx.eval_lut(&gin321, nand2);
    std::cerr << "\rLUT322   ";
    Ciphertext gin322 = 2 * gout272 + gout283;
    Ciphertext gout322 = ctx.eval_lut(&gin322, xnor2);
    std::cerr << "\rLUT323   ";
    Ciphertext gin323 = 2 * gout153 + gout294;
    Ciphertext gout323 = ctx.eval_lut(&gin323, nand2);
    std::cerr << "\rLUT324   ";
    Ciphertext gin324 = 2 * gout185 + gout290;
    Ciphertext gout324 = ctx.eval_lut(&gin324, nand2);
    std::cerr << "\rLUT325   ";
    Ciphertext gin325 = 2 * gout260 + gout291;
    Ciphertext gout325 = ctx.eval_lut(&gin325, or2);
    std::cerr << "\rLUT326   ";
    Ciphertext gin326 = 2 * (1 - gout277) + gout252;
    Ciphertext gout326 = ctx.eval_lut(&gin326, and2);
    std::cerr << "\rLUT327   ";
    Ciphertext gin327 = 2 * (1 - gout293) + gout285;
    Ciphertext gout327 = ctx.eval_lut(&gin327, and2);
    std::cerr << "\rLUT328   ";
    Ciphertext gin328 = 2 * gout155 + gout287;
    Ciphertext gout328 = ctx.eval_lut(&gin328, nand2);
    std::cerr << "\rLUT329   ";
    Ciphertext gin329 = 2 * gout272 + (1 - gout283);
    Ciphertext gout329 = ctx.eval_lut(&gin329, or2);
    std::cerr << "\rLUT330   ";
    Ciphertext gin330 = 2 * gout254 + gout302;
    Ciphertext gout330 = ctx.eval_lut(&gin330, nand2);
    std::cerr << "\rLUT331   ";
    Ciphertext gin331 = 2 * gout303 + gout276;
    Ciphertext gout331 = ctx.eval_lut(&gin331, xnor2);
    std::cerr << "\rLUT332   ";
    Ciphertext gin332 = 2 * (1 - gout301) + gout295;
    Ciphertext gout332 = ctx.eval_lut(&gin332, and2);
    std::cerr << "\rLUT333   ";
    Ciphertext gin333 = 2 * gout32 + gout300;
    Ciphertext gout333 = ctx.eval_lut(&gin333, nand2);
    std::cerr << "\rLUT334   ";
    Ciphertext gin334 = 2 * gout31 + gout299;
    Ciphertext gout334 = ctx.eval_lut(&gin334, nand2);
    std::cerr << "\rLUT335   ";
    Ciphertext gin335 = 2 * (1 - gout288) + gout289;
    Ciphertext gout335 = ctx.eval_lut(&gin335, and2);
    std::cerr << "\rLUT336   ";
    Ciphertext gin336 = 2 * gout306 + gout280;
    Ciphertext gout336 = ctx.eval_lut(&gin336, xnor2);
    std::cerr << "\rLUT337   ";
    Ciphertext gin337 = 2 * gout288 + gout289;
    Ciphertext gout337 = ctx.eval_lut(&gin337, xnor2);
    std::cerr << "\rLUT338   ";
    Ciphertext gin338 = 2 * (1 - gout306) + gout280;
    Ciphertext gout338 = ctx.eval_lut(&gin338, and2);
    std::cerr << "\rLUT339   ";
    Ciphertext gin339 = 2 * gout303 + (1 - gout276);
    Ciphertext gout339 = ctx.eval_lut(&gin339, or2);
    std::cerr << "\rLUT340   ";
    Ciphertext gin340 = 2 * gout307 + gout320;
    Ciphertext gout340 = ctx.eval_lut(&gin340, and2);
    std::cerr << "\rLUT341   ";
    Ciphertext gin341 = 2 * gout266 + gout298;
    Ciphertext gout341 = ctx.eval_lut(&gin341, nand2);
    std::cerr << "\rLUT342   ";
    Ciphertext gin342 = 2 * gout39 + gout309;
    Ciphertext gout342 = ctx.eval_lut(&gin342, nand2);
    std::cerr << "\rLUT343   ";
    Ciphertext gin343 = 2 * gout304 + gout292;
    Ciphertext gout343 = ctx.eval_lut(&gin343, xor2);
    std::cerr << "\rLUT344   ";
    Ciphertext gin344 = 2 * gout304 + gout292;
    Ciphertext gout344 = ctx.eval_lut(&gin344, nand2);
    std::cerr << "\rLUT345   ";
    Ciphertext gin345 = 2 * gout297 + gout308;
    Ciphertext gout345 = ctx.eval_lut(&gin345, xnor2);
    std::cerr << "\rLUT346   ";
    Ciphertext gin346 = 2 * (1 - gout297) + gout308;
    Ciphertext gout346 = ctx.eval_lut(&gin346, and2);
    std::cerr << "\rLUT347   ";
    Ciphertext gin347 = 2 * gout307 + gout320;
    Ciphertext gout347 = ctx.eval_lut(&gin347, xor2);
    std::cerr << "\rLUT348   ";
    Ciphertext gin348 = 2 * gout315 + gout18;
    Ciphertext gout348 = ctx.eval_lut(&gin348, nand2);
    std::cerr << "\rLUT349   ";
    Ciphertext gin349 = 2 * gout311 + gout95;
    Ciphertext gout349 = ctx.eval_lut(&gin349, nand2);
    std::cerr << "\rLUT350   ";
    Ciphertext gin350 = 2 * gout251 + gout317;
    Ciphertext gout350 = ctx.eval_lut(&gin350, xnor2);
    std::cerr << "\rLUT351   ";
    Ciphertext gin351 = 2 * gout253 + gout319;
    Ciphertext gout351 = ctx.eval_lut(&gin351, xor2);
    std::cerr << "\rLUT352   ";
    Ciphertext gin352 = 2 * gout312 + gout112;
    Ciphertext gout352 = ctx.eval_lut(&gin352, nand2);
    std::cerr << "\rLUT353   ";
    Ciphertext gin353 = 2 * gout311 + gout95;
    Ciphertext gout353 = ctx.eval_lut(&gin353, xnor2);
    std::cerr << "\rLUT354   ";
    Ciphertext gin354 = 2 * gout321 + gout87;
    Ciphertext gout354 = ctx.eval_lut(&gin354, nand2);
    std::cerr << "\rLUT355   ";
    Ciphertext gin355 = 2 * gout323 + gout101;
    Ciphertext gout355 = ctx.eval_lut(&gin355, xnor2);
    std::cerr << "\rLUT356   ";
    Ciphertext gin356 = 2 * gout312 + gout112;
    Ciphertext gout356 = ctx.eval_lut(&gin356, xor2);
    std::cerr << "\rLUT357   ";
    Ciphertext gin357 = 2 * gout313 + gout68;
    Ciphertext gout357 = ctx.eval_lut(&gin357, nand2);
    std::cerr << "\rLUT358   ";
    Ciphertext gin358 = 2 * gout253 + gout319;
    Ciphertext gout358 = ctx.eval_lut(&gin358, and2);
    std::cerr << "\rLUT359   ";
    Ciphertext gin359 = 2 * gout313 + gout68;
    Ciphertext gout359 = ctx.eval_lut(&gin359, xor2);
    std::cerr << "\rLUT360   ";
    Ciphertext gin360 = 2 * gout251 + gout317;
    Ciphertext gout360 = ctx.eval_lut(&gin360, and2);
    std::cerr << "\rLUT361   ";
    Ciphertext gin361 = 2 * gout328 + gout41;
    Ciphertext gout361 = ctx.eval_lut(&gin361, xnor2);
    std::cerr << "\rLUT362   ";
    Ciphertext gin362 = 2 * gout323 + gout101;
    Ciphertext gout362 = ctx.eval_lut(&gin362, nand2);
    std::cerr << "\rLUT363   ";
    Ciphertext gin363 = 2 * gout318 + gout163;
    Ciphertext gout363 = ctx.eval_lut(&gin363, xor2);
    std::cerr << "\rLUT364   ";
    Ciphertext gin364 = 2 * gout315 + gout18;
    Ciphertext gout364 = ctx.eval_lut(&gin364, xor2);
    std::cerr << "\rLUT365   ";
    Ciphertext gin365 = 2 * gout330 + gout322;
    Ciphertext gout365 = ctx.eval_lut(&gin365, nand2);
    std::cerr << "\rLUT366   ";
    Ciphertext gin366 = 2 * gout324 + gout24;
    Ciphertext gout366 = ctx.eval_lut(&gin366, nand2);
    std::cerr << "\rLUT367   ";
    Ciphertext gin367 = 2 * gout334 + gout193;
    Ciphertext gout367 = ctx.eval_lut(&gin367, nand2);
    std::cerr << "\rLUT368   ";
    Ciphertext gin368 = 2 * gout333 + gout121;
    Ciphertext gout368 = ctx.eval_lut(&gin368, nand2);
    std::cerr << "\rLUT369   ";
    Ciphertext gin369 = 2 * gout314 + gout337;
    Ciphertext gout369 = ctx.eval_lut(&gin369, and2);
    std::cerr << "\rLUT370   ";
    Ciphertext gin370 = 2 * gout334 + gout193;
    Ciphertext gout370 = ctx.eval_lut(&gin370, xnor2);
    std::cerr << "\rLUT371   ";
    Ciphertext gin371 = 2 * gout305 + gout336;
    Ciphertext gout371 = ctx.eval_lut(&gin371, xor2);
    std::cerr << "\rLUT372   ";
    Ciphertext gin372 = 2 * gout314 + gout337;
    Ciphertext gout372 = ctx.eval_lut(&gin372, xnor2);
    std::cerr << "\rLUT373   ";
    Ciphertext gin373 = 2 * gout305 + gout336;
    Ciphertext gout373 = ctx.eval_lut(&gin373, and2);
    std::cerr << "\rLUT374   ";
    Ciphertext gin374 = 2 * gout321 + gout87;
    Ciphertext gout374 = ctx.eval_lut(&gin374, xor2);
    std::cerr << "\rLUT375   ";
    Ciphertext gin375 = 2 * gout318 + gout163;
    Ciphertext gout375 = ctx.eval_lut(&gin375, nand2);
    std::cerr << "\rLUT376   ";
    Ciphertext gin376 = 2 * gout328 + gout41;
    Ciphertext gout376 = ctx.eval_lut(&gin376, nand2);
    std::cerr << "\rLUT377   ";
    Ciphertext gin377 = 2 * gout330 + gout322;
    Ciphertext gout377 = ctx.eval_lut(&gin377, xnor2);
    std::cerr << "\rLUT378   ";
    Ciphertext gin378 = 2 * gout341 + gout331;
    Ciphertext gout378 = ctx.eval_lut(&gin378, nand2);
    std::cerr << "\rLUT379   ";
    Ciphertext gin379 = 2 * gout333 + gout121;
    Ciphertext gout379 = ctx.eval_lut(&gin379, xnor2);
    std::cerr << "\rLUT380   ";
    Ciphertext gin380 = 2 * gout332 + gout340;
    Ciphertext gout380 = ctx.eval_lut(&gin380, or2);
    std::cerr << "\rLUT381   ";
    Ciphertext gin381 = 2 * gout324 + gout24;
    Ciphertext gout381 = ctx.eval_lut(&gin381, xnor2);
    std::cerr << "\rLUT382   ";
    Ciphertext gin382 = 2 * gout342 + gout150;
    Ciphertext gout382 = ctx.eval_lut(&gin382, nand2);
    std::cerr << "\rLUT383   ";
    Ciphertext gin383 = 2 * gout258 + gout343;
    Ciphertext gout383 = ctx.eval_lut(&gin383, nand2);
    std::cerr << "\rLUT384   ";
    Ciphertext gin384 = 2 * gout325 + gout345;
    Ciphertext gout384 = ctx.eval_lut(&gin384, xnor2);
    std::cerr << "\rLUT385   ";
    Ciphertext gin385 = 2 * gout325 + gout345;
    Ciphertext gout385 = ctx.eval_lut(&gin385, and2);
    std::cerr << "\rLUT386   ";
    Ciphertext gin386 = 2 * gout341 + gout331;
    Ciphertext gout386 = ctx.eval_lut(&gin386, xor2);
    std::cerr << "\rLUT387   ";
    Ciphertext gin387 = 2 * gout342 + gout150;
    Ciphertext gout387 = ctx.eval_lut(&gin387, xor2);
    std::cerr << "\rLUT388   ";
    Ciphertext gin388 = 2 * gout296 + gout343;
    Ciphertext gout388 = ctx.eval_lut(&gin388, xnor2);
    std::cerr << "\rLUT389   ";
    Ciphertext gin389 = 2 * gout198 + gout349;
    Ciphertext gout389 = ctx.eval_lut(&gin389, nand2);
    std::cerr << "\rLUT390   ";
    Ciphertext gin390 = 2 * gout123 + gout348;
    Ciphertext gout390 = ctx.eval_lut(&gin390, nand2);
    std::cerr << "\rLUT391   ";
    Ciphertext gin391 = 2 * gout350 + gout351;
    Ciphertext gout391 = ctx.eval_lut(&gin391, xnor2);
    std::cerr << "\rLUT392   ";
    Ciphertext gin392 = 2 * (1 - gout350) + gout351;
    Ciphertext gout392 = ctx.eval_lut(&gin392, and2);
    std::cerr << "\rLUT393   ";
    Ciphertext gin393 = 2 * gout64 + gout352;
    Ciphertext gout393 = ctx.eval_lut(&gin393, nand2);
    std::cerr << "\rLUT394   ";
    Ciphertext gin394 = 2 * gout88 + gout354;
    Ciphertext gout394 = ctx.eval_lut(&gin394, nand2);
    std::cerr << "\rLUT395   ";
    Ciphertext gin395 = 2 * gout96 + gout362;
    Ciphertext gout395 = ctx.eval_lut(&gin395, nand2);
    std::cerr << "\rLUT396   ";
    Ciphertext gin396 = 2 * (1 - gout355) + gout359;
    Ciphertext gout396 = ctx.eval_lut(&gin396, and2);
    std::cerr << "\rLUT397   ";
    Ciphertext gin397 = 2 * gout353 + gout364;
    Ciphertext gout397 = ctx.eval_lut(&gin397, xnor2);
    std::cerr << "\rLUT398   ";
    Ciphertext gin398 = 2 * gout355 + gout359;
    Ciphertext gout398 = ctx.eval_lut(&gin398, xnor2);
    std::cerr << "\rLUT399   ";
    Ciphertext gin399 = 2 * gout129 + gout366;
    Ciphertext gout399 = ctx.eval_lut(&gin399, nand2);
    std::cerr << "\rLUT400   ";
    Ciphertext gin400 = 2 * gout181 + gout357;
    Ciphertext gout400 = ctx.eval_lut(&gin400, nand2);
    std::cerr << "\rLUT401   ";
    Ciphertext gin401 = 2 * gout370 + gout363;
    Ciphertext gout401 = ctx.eval_lut(&gin401, xnor2);
    std::cerr << "\rLUT402   ";
    Ciphertext gin402 = 2 * gout327 + gout358;
    Ciphertext gout402 = ctx.eval_lut(&gin402, or2);
    std::cerr << "\rLUT403   ";
    Ciphertext gin403 = 2 * (1 - gout361) + gout356;
    Ciphertext gout403 = ctx.eval_lut(&gin403, and2);
    std::cerr << "\rLUT404   ";
    Ciphertext gin404 = 2 * gout361 + gout356;
    Ciphertext gout404 = ctx.eval_lut(&gin404, xnor2);
    std::cerr << "\rLUT405   ";
    Ciphertext gin405 = 2 * gout326 + gout360;
    Ciphertext gout405 = ctx.eval_lut(&gin405, or2);
    std::cerr << "\rLUT406   ";
    Ciphertext gin406 = 2 * gout353 + (1 - gout364);
    Ciphertext gout406 = ctx.eval_lut(&gin406, or2);
    std::cerr << "\rLUT407   ";
    Ciphertext gin407 = 2 * gout187 + gout367;
    Ciphertext gout407 = ctx.eval_lut(&gin407, nand2);
    std::cerr << "\rLUT408   ";
    Ciphertext gin408 = 2 * gout190 + gout368;
    Ciphertext gout408 = ctx.eval_lut(&gin408, nand2);
    std::cerr << "\rLUT409   ";
    Ciphertext gin409 = 2 * gout372 + gout371;
    Ciphertext gout409 = ctx.eval_lut(&gin409, xnor2);
    std::cerr << "\rLUT410   ";
    Ciphertext gin410 = 2 * (1 - gout372) + gout371;
    Ciphertext gout410 = ctx.eval_lut(&gin410, and2);
    std::cerr << "\rLUT411   ";
    Ciphertext gin411 = 2 * gout381 + gout374;
    Ciphertext gout411 = ctx.eval_lut(&gin411, xnor2);
    std::cerr << "\rLUT412   ";
    Ciphertext gin412 = 2 * gout381 + (1 - gout374);
    Ciphertext gout412 = ctx.eval_lut(&gin412, or2);
    std::cerr << "\rLUT413   ";
    Ciphertext gin413 = 2 * (1 - gout370) + gout363;
    Ciphertext gout413 = ctx.eval_lut(&gin413, and2);
    std::cerr << "\rLUT414   ";
    Ciphertext gin414 = 2 * gout151 + gout375;
    Ciphertext gout414 = ctx.eval_lut(&gin414, nand2);
    std::cerr << "\rLUT415   ";
    Ciphertext gin415 = 2 * gout97 + gout376;
    Ciphertext gout415 = ctx.eval_lut(&gin415, nand2);
    std::cerr << "\rLUT416   ";
    Ciphertext gin416 = 2 * gout380 + gout401;
    Ciphertext gout416 = ctx.eval_lut(&gin416, and2);
    std::cerr << "\rLUT417   ";
    Ciphertext gin417 = 2 * gout329 + gout365;
    Ciphertext gout417 = ctx.eval_lut(&gin417, nand2);
    std::cerr << "\rLUT418   ";
    Ciphertext gin418 = 2 * gout380 + gout401;
    Ciphertext gout418 = ctx.eval_lut(&gin418, xor2);
    std::cerr << "\rLUT419   ";
    Ciphertext gin419 = 2 * gout338 + gout373;
    Ciphertext gout419 = ctx.eval_lut(&gin419, or2);
    std::cerr << "\rLUT420   ";
    Ciphertext gin420 = 2 * gout335 + gout369;
    Ciphertext gout420 = ctx.eval_lut(&gin420, or2);
    std::cerr << "\rLUT421   ";
    Ciphertext gin421 = 2 * gout200 + gout382;
    Ciphertext gout421 = ctx.eval_lut(&gin421, nand2);
    std::cerr << "\rLUT422   ";
    Ciphertext gin422 = 2 * gout339 + gout378;
    Ciphertext gout422 = ctx.eval_lut(&gin422, nand2);
    std::cerr << "\rLUT423   ";
    Ciphertext gin423 = 2 * gout344 + gout383;
    Ciphertext gout423 = ctx.eval_lut(&gin423, nand2);
    std::cerr << "\rLUT424   ";
    Ciphertext gin424 = 2 * (1 - gout384) + gout347;
    Ciphertext gout424 = ctx.eval_lut(&gin424, and2);
    std::cerr << "\rLUT425   ";
    Ciphertext gin425 = 2 * gout384 + gout347;
    Ciphertext gout425 = ctx.eval_lut(&gin425, xnor2);
    std::cerr << "\rLUT426   ";
    Ciphertext gin426 = 2 * gout346 + gout385;
    Ciphertext gout426 = ctx.eval_lut(&gin426, or2);
    std::cerr << "\rLUT427   ";
    Ciphertext gin427 = 2 * gout377 + gout386;
    Ciphertext gout427 = ctx.eval_lut(&gin427, xnor2);
    std::cerr << "\rLUT428   ";
    Ciphertext gin428 = 2 * gout377 + (1 - gout386);
    Ciphertext gout428 = ctx.eval_lut(&gin428, or2);
    std::cerr << "\rLUT429   ";
    Ciphertext gin429 = 2 * gout379 + gout387;
    Ciphertext gout429 = ctx.eval_lut(&gin429, xnor2);
    std::cerr << "\rLUT430   ";
    Ciphertext gin430 = 2 * (1 - gout379) + gout387;
    Ciphertext gout430 = ctx.eval_lut(&gin430, and2);
    std::cerr << "\rLUT431   ";
    Ciphertext gin431 = 2 * gout389 + gout91;
    Ciphertext gout431 = ctx.eval_lut(&gin431, nand2);
    std::cerr << "\rLUT432   ";
    Ciphertext gin432 = 2 * gout390 + gout59;
    Ciphertext gout432 = ctx.eval_lut(&gin432, nand2);
    std::cerr << "\rLUT433   ";
    Ciphertext gin433 = 2 * gout274 + gout391;
    Ciphertext gout433 = ctx.eval_lut(&gin433, xor2);
    std::cerr << "\rLUT434   ";
    Ciphertext gin434 = 2 * gout274 + gout391;
    Ciphertext gout434 = ctx.eval_lut(&gin434, and2);
    std::cerr << "\rLUT435   ";
    Ciphertext gin435 = 2 * gout389 + gout91;
    Ciphertext gout435 = ctx.eval_lut(&gin435, xnor2);
    std::cerr << "\rLUT436   ";
    Ciphertext gin436 = 2 * gout393 + gout145;
    Ciphertext gout436 = ctx.eval_lut(&gin436, nand2);
    std::cerr << "\rLUT437   ";
    Ciphertext gin437 = 2 * gout390 + gout59;
    Ciphertext gout437 = ctx.eval_lut(&gin437, xor2);
    std::cerr << "\rLUT438   ";
    Ciphertext gin438 = 2 * gout394 + gout58;
    Ciphertext gout438 = ctx.eval_lut(&gin438, nand2);
    std::cerr << "\rLUT439   ";
    Ciphertext gin439 = 2 * gout395 + gout71;
    Ciphertext gout439 = ctx.eval_lut(&gin439, nand2);
    std::cerr << "\rLUT440   ";
    Ciphertext gin440 = 2 * gout393 + gout145;
    Ciphertext gout440 = ctx.eval_lut(&gin440, xor2);
    std::cerr << "\rLUT441   ";
    Ciphertext gin441 = 2 * gout399 + gout49;
    Ciphertext gout441 = ctx.eval_lut(&gin441, nand2);
    std::cerr << "\rLUT442   ";
    Ciphertext gin442 = 2 * gout399 + gout49;
    Ciphertext gout442 = ctx.eval_lut(&gin442, xnor2);
    std::cerr << "\rLUT443   ";
    Ciphertext gin443 = 2 * gout394 + gout58;
    Ciphertext gout443 = ctx.eval_lut(&gin443, xor2);
    std::cerr << "\rLUT444   ";
    Ciphertext gin444 = 2 * gout395 + gout71;
    Ciphertext gout444 = ctx.eval_lut(&gin444, xnor2);
    std::cerr << "\rLUT445   ";
    Ciphertext gin445 = 2 * gout414 + gout21;
    Ciphertext gout445 = ctx.eval_lut(&gin445, xor2);
    std::cerr << "\rLUT446   ";
    Ciphertext gin446 = 2 * gout400 + gout158;
    Ciphertext gout446 = ctx.eval_lut(&gin446, nand2);
    std::cerr << "\rLUT447   ";
    Ciphertext gin447 = 2 * gout413 + gout416;
    Ciphertext gout447 = ctx.eval_lut(&gin447, or2);
    std::cerr << "\rLUT448   ";
    Ciphertext gin448 = 2 * gout400 + gout158;
    Ciphertext gout448 = ctx.eval_lut(&gin448, xor2);
    std::cerr << "\rLUT449   ";
    Ciphertext gin449 = 2 * gout408 + gout157;
    Ciphertext gout449 = ctx.eval_lut(&gin449, nand2);
    std::cerr << "\rLUT450   ";
    Ciphertext gin450 = 2 * gout417 + gout397;
    Ciphertext gout450 = ctx.eval_lut(&gin450, nand2);
    std::cerr << "\rLUT451   ";
    Ciphertext gin451 = 2 * gout407 + gout166;
    Ciphertext gout451 = ctx.eval_lut(&gin451, xnor2);
    std::cerr << "\rLUT452   ";
    Ciphertext gin452 = 2 * gout405 + gout409;
    Ciphertext gout452 = ctx.eval_lut(&gin452, xnor2);
    std::cerr << "\rLUT453   ";
    Ciphertext gin453 = 2 * gout407 + gout166;
    Ciphertext gout453 = ctx.eval_lut(&gin453, nand2);
    std::cerr << "\rLUT454   ";
    Ciphertext gin454 = 2 * gout405 + gout409;
    Ciphertext gout454 = ctx.eval_lut(&gin454, and2);
    std::cerr << "\rLUT455   ";
    Ciphertext gin455 = 2 * gout415 + gout115;
    Ciphertext gout455 = ctx.eval_lut(&gin455, xnor2);
    std::cerr << "\rLUT456   ";
    Ciphertext gin456 = 2 * gout408 + gout157;
    Ciphertext gout456 = ctx.eval_lut(&gin456, xnor2);
    std::cerr << "\rLUT457   ";
    Ciphertext gin457 = 2 * gout417 + gout397;
    Ciphertext gout457 = ctx.eval_lut(&gin457, xnor2);
    std::cerr << "\rLUT458   ";
    Ciphertext gin458 = 2 * gout415 + gout115;
    Ciphertext gout458 = ctx.eval_lut(&gin458, nand2);
    std::cerr << "\rLUT459   ";
    Ciphertext gin459 = 2 * gout414 + gout21;
    Ciphertext gout459 = ctx.eval_lut(&gin459, nand2);
    std::cerr << "\rLUT460   ";
    Ciphertext gin460 = 2 * gout421 + gout108;
    Ciphertext gout460 = ctx.eval_lut(&gin460, nand2);
    std::cerr << "\rLUT461   ";
    Ciphertext gin461 = 2 * gout420 + gout404;
    Ciphertext gout461 = ctx.eval_lut(&gin461, and2);
    std::cerr << "\rLUT462   ";
    Ciphertext gin462 = 2 * gout388 + gout433;
    Ciphertext gout462 = ctx.eval_lut(&gin462, nand2);
    std::cerr << "\rLUT463   ";
    Ciphertext gin463 = 2 * gout402 + gout425;
    Ciphertext gout463 = ctx.eval_lut(&gin463, xor2);
    std::cerr << "\rLUT464   ";
    Ciphertext gin464 = 2 * gout419 + gout398;
    Ciphertext gout464 = ctx.eval_lut(&gin464, xor2);
    std::cerr << "\rLUT465   ";
    Ciphertext gin465 = 2 * gout420 + gout404;
    Ciphertext gout465 = ctx.eval_lut(&gin465, xnor2);
    std::cerr << "\rLUT466   ";
    Ciphertext gin466 = 2 * gout419 + gout398;
    Ciphertext gout466 = ctx.eval_lut(&gin466, and2);
    std::cerr << "\rLUT467   ";
    Ciphertext gin467 = 2 * gout388 + gout433;
    Ciphertext gout467 = ctx.eval_lut(&gin467, xor2);
    std::cerr << "\rLUT468   ";
    Ciphertext gin468 = 2 * gout422 + gout411;
    Ciphertext gout468 = ctx.eval_lut(&gin468, xor2);
    std::cerr << "\rLUT469   ";
    Ciphertext gin469 = 2 * gout422 + gout411;
    Ciphertext gout469 = ctx.eval_lut(&gin469, nand2);
    std::cerr << "\rLUT470   ";
    Ciphertext gin470 = 2 * gout421 + gout108;
    Ciphertext gout470 = ctx.eval_lut(&gin470, xor2);
    std::cerr << "\rLUT471   ";
    Ciphertext gin471 = 2 * gout423 + gout427;
    Ciphertext gout471 = ctx.eval_lut(&gin471, xor2);
    std::cerr << "\rLUT472   ";
    Ciphertext gin472 = 2 * gout423 + gout427;
    Ciphertext gout472 = ctx.eval_lut(&gin472, nand2);
    std::cerr << "\rLUT473   ";
    Ciphertext gin473 = 2 * gout402 + gout425;
    Ciphertext gout473 = ctx.eval_lut(&gin473, and2);
    std::cerr << "\rLUT474   ";
    Ciphertext gin474 = 2 * gout426 + gout429;
    Ciphertext gout474 = ctx.eval_lut(&gin474, xnor2);
    std::cerr << "\rLUT475   ";
    Ciphertext gin475 = 2 * gout426 + gout429;
    Ciphertext gout475 = ctx.eval_lut(&gin475, and2);
    std::cerr << "\rLUT476   ";
    Ciphertext gin476 = 2 * gout51 + gout432;
    Ciphertext gout476 = ctx.eval_lut(&gin476, nand2);
    std::cerr << "\rLUT477   ";
    Ciphertext gin477 = 2 * gout81 + gout431;
    Ciphertext gout477 = ctx.eval_lut(&gin477, nand2);
    std::cerr << "\rLUT478   ";
    Ciphertext gin478 = 2 * gout392 + gout434;
    Ciphertext gout478 = ctx.eval_lut(&gin478, or2);
    std::cerr << "\rLUT479   ";
    Ciphertext gin479 = 2 * gout148 + gout436;
    Ciphertext gout479 = ctx.eval_lut(&gin479, nand2);
    std::cerr << "\rLUT480   ";
    Ciphertext gin480 = 2 * gout435 + gout437;
    Ciphertext gout480 = ctx.eval_lut(&gin480, xnor2);
    std::cerr << "\rLUT481   ";
    Ciphertext gin481 = 2 * gout435 + (1 - gout437);
    Ciphertext gout481 = ctx.eval_lut(&gin481, or2);
    std::cerr << "\rLUT482   ";
    Ciphertext gin482 = 2 * gout117 + gout438;
    Ciphertext gout482 = ctx.eval_lut(&gin482, nand2);
    std::cerr << "\rLUT483   ";
    Ciphertext gin483 = 2 * gout93 + gout441;
    Ciphertext gout483 = ctx.eval_lut(&gin483, nand2);
    std::cerr << "\rLUT484   ";
    Ciphertext gin484 = 2 * gout406 + gout450;
    Ciphertext gout484 = ctx.eval_lut(&gin484, nand2);
    std::cerr << "\rLUT485   ";
    Ciphertext gin485 = 2 * (1 - gout451) + gout445;
    Ciphertext gout485 = ctx.eval_lut(&gin485, and2);
    std::cerr << "\rLUT486   ";
    Ciphertext gin486 = 2 * gout186 + gout439;
    Ciphertext gout486 = ctx.eval_lut(&gin486, nand2);
    std::cerr << "\rLUT487   ";
    Ciphertext gin487 = 2 * gout410 + gout454;
    Ciphertext gout487 = ctx.eval_lut(&gin487, or2);
    std::cerr << "\rLUT488   ";
    Ciphertext gin488 = 2 * (1 - gout444) + gout448;
    Ciphertext gout488 = ctx.eval_lut(&gin488, and2);
    std::cerr << "\rLUT489   ";
    Ciphertext gin489 = 2 * (1 - gout455) + gout440;
    Ciphertext gout489 = ctx.eval_lut(&gin489, and2);
    std::cerr << "\rLUT490   ";
    Ciphertext gin490 = 2 * gout444 + gout448;
    Ciphertext gout490 = ctx.eval_lut(&gin490, xnor2);
    std::cerr << "\rLUT491   ";
    Ciphertext gin491 = 2 * gout442 + (1 - gout443);
    Ciphertext gout491 = ctx.eval_lut(&gin491, or2);
    std::cerr << "\rLUT492   ";
    Ciphertext gin492 = 2 * gout442 + gout443;
    Ciphertext gout492 = ctx.eval_lut(&gin492, xnor2);
    std::cerr << "\rLUT493   ";
    Ciphertext gin493 = 2 * gout451 + gout445;
    Ciphertext gout493 = ctx.eval_lut(&gin493, xnor2);
    std::cerr << "\rLUT494   ";
    Ciphertext gin494 = 2 * gout455 + gout440;
    Ciphertext gout494 = ctx.eval_lut(&gin494, xnor2);
    std::cerr << "\rLUT495   ";
    Ciphertext gin495 = 2 * gout160 + gout449;
    Ciphertext gout495 = ctx.eval_lut(&gin495, nand2);
    std::cerr << "\rLUT496   ";
    Ciphertext gin496 = 2 * gout146 + gout446;
    Ciphertext gout496 = ctx.eval_lut(&gin496, nand2);
    std::cerr << "\rLUT497   ";
    Ciphertext gin497 = 2 * gout55 + gout453;
    Ciphertext gout497 = ctx.eval_lut(&gin497, nand2);
    std::cerr << "\rLUT498   ";
    Ciphertext gin498 = 2 * gout396 + gout466;
    Ciphertext gout498 = ctx.eval_lut(&gin498, or2);
    std::cerr << "\rLUT499   ";
    Ciphertext gin499 = 2 * gout403 + gout461;
    Ciphertext gout499 = ctx.eval_lut(&gin499, or2);
    std::cerr << "\rLUT500   ";
    Ciphertext gin500 = 2 * (1 - gout456) + gout470;
    Ciphertext gout500 = ctx.eval_lut(&gin500, and2);
    std::cerr << "\rLUT501   ";
    Ciphertext gin501 = 2 * gout412 + gout469;
    Ciphertext gout501 = ctx.eval_lut(&gin501, nand2);
    std::cerr << "\rLUT502   ";
    Ciphertext gin502 = 2 * gout310 + gout467;
    Ciphertext gout502 = ctx.eval_lut(&gin502, nand2);
    std::cerr << "\rLUT503   ";
    Ciphertext gin503 = 2 * gout452 + gout463;
    Ciphertext gout503 = ctx.eval_lut(&gin503, xnor2);
    std::cerr << "\rLUT504   ";
    Ciphertext gin504 = 2 * gout465 + gout464;
    Ciphertext gout504 = ctx.eval_lut(&gin504, xnor2);
    std::cerr << "\rLUT505   ";
    Ciphertext gin505 = 2 * (1 - gout465) + gout464;
    Ciphertext gout505 = ctx.eval_lut(&gin505, and2);
    std::cerr << "\rLUT506   ";
    Ciphertext gin506 = 2 * gout310 + gout467;
    Ciphertext gout506 = ctx.eval_lut(&gin506, xor2);
    std::cerr << "\rLUT507   ";
    Ciphertext gin507 = 2 * gout15 + gout460;
    Ciphertext gout507 = ctx.eval_lut(&gin507, nand2);
    std::cerr << "\rLUT508   ";
    Ciphertext gin508 = 2 * gout457 + gout468;
    Ciphertext gout508 = ctx.eval_lut(&gin508, xnor2);
    std::cerr << "\rLUT509   ";
    Ciphertext gin509 = 2 * gout22 + gout459;
    Ciphertext gout509 = ctx.eval_lut(&gin509, nand2);
    std::cerr << "\rLUT510   ";
    Ciphertext gin510 = 2 * gout457 + (1 - gout468);
    Ciphertext gout510 = ctx.eval_lut(&gin510, or2);
    std::cerr << "\rLUT511   ";
    Ciphertext gin511 = 2 * gout456 + gout470;
    Ciphertext gout511 = ctx.eval_lut(&gin511, xnor2);
    std::cerr << "\rLUT512   ";
    Ciphertext gin512 = 2 * gout66 + gout458;
    Ciphertext gout512 = ctx.eval_lut(&gin512, nand2);
    std::cerr << "\rLUT513   ";
    Ciphertext gin513 = 2 * gout430 + gout475;
    Ciphertext gout513 = ctx.eval_lut(&gin513, or2);
    std::cerr << "\rLUT514   ";
    Ciphertext gin514 = 2 * (1 - gout452) + gout463;
    Ciphertext gout514 = ctx.eval_lut(&gin514, and2);
    std::cerr << "\rLUT515   ";
    Ciphertext gin515 = 2 * gout424 + gout473;
    Ciphertext gout515 = ctx.eval_lut(&gin515, or2);
    std::cerr << "\rLUT516   ";
    Ciphertext gin516 = 2 * (1 - gout474) + gout418;
    Ciphertext gout516 = ctx.eval_lut(&gin516, and2);
    std::cerr << "\rLUT517   ";
    Ciphertext gin517 = 2 * gout474 + gout418;
    Ciphertext gout517 = ctx.eval_lut(&gin517, xnor2);
    std::cerr << "\rLUT518   ";
    Ciphertext gin518 = 2 * gout428 + gout472;
    Ciphertext gout518 = ctx.eval_lut(&gin518, nand2);
    std::cerr << "\rLUT519   ";
    Ciphertext gin519 = 2 * gout476 + gout79;
    Ciphertext gout519 = ctx.eval_lut(&gin519, nand2);
    std::cerr << "\rLUT520   ";
    Ciphertext gin520 = 2 * gout477 + gout63;
    Ciphertext gout520 = ctx.eval_lut(&gin520, xnor2);
    std::cerr << "\rLUT521   ";
    Ciphertext gin521 = 2 * gout477 + gout63;
    Ciphertext gout521 = ctx.eval_lut(&gin521, nand2);
    std::cerr << "\rLUT522   ";
    Ciphertext gin522 = 2 * gout476 + gout79;
    Ciphertext gout522 = ctx.eval_lut(&gin522, xor2);
    std::cerr << "\rLUT523   ";
    Ciphertext gin523 = 2 * gout482 + gout137;
    Ciphertext gout523 = ctx.eval_lut(&gin523, xor2);
    std::cerr << "\rLUT524   ";
    Ciphertext gin524 = 2 * gout482 + gout137;
    Ciphertext gout524 = ctx.eval_lut(&gin524, nand2);
    std::cerr << "\rLUT525   ";
    Ciphertext gin525 = 2 * gout479 + gout103;
    Ciphertext gout525 = ctx.eval_lut(&gin525, xor2);
    std::cerr << "\rLUT526   ";
    Ciphertext gin526 = 2 * gout479 + gout103;
    Ciphertext gout526 = ctx.eval_lut(&gin526, nand2);
    std::cerr << "\rLUT527   ";
    Ciphertext gin527 = 2 * gout483 + gout62;
    Ciphertext gout527 = ctx.eval_lut(&gin527, xnor2);
    std::cerr << "\rLUT528   ";
    Ciphertext gin528 = 2 * gout483 + gout62;
    Ciphertext gout528 = ctx.eval_lut(&gin528, nand2);
    std::cerr << "\rLUT529   ";
    Ciphertext gin529 = 2 * gout486 + gout196;
    Ciphertext gout529 = ctx.eval_lut(&gin529, nand2);
    std::cerr << "\rLUT530   ";
    Ciphertext gin530 = 2 * gout484 + gout480;
    Ciphertext gout530 = ctx.eval_lut(&gin530, xnor2);
    std::cerr << "\rLUT531   ";
    Ciphertext gin531 = 2 * gout484 + gout480;
    Ciphertext gout531 = ctx.eval_lut(&gin531, nand2);
    std::cerr << "\rLUT532   ";
    Ciphertext gin532 = 2 * gout486 + gout196;
    Ciphertext gout532 = ctx.eval_lut(&gin532, xnor2);
    std::cerr << "\rLUT533   ";
    Ciphertext gin533 = 2 * gout495 + gout7;
    Ciphertext gout533 = ctx.eval_lut(&gin533, nand2);
    std::cerr << "\rLUT534   ";
    Ciphertext gin534 = 2 * gout497 + gout56;
    Ciphertext gout534 = ctx.eval_lut(&gin534, xnor2);
    std::cerr << "\rLUT535   ";
    Ciphertext gin535 = 2 * gout497 + gout56;
    Ciphertext gout535 = ctx.eval_lut(&gin535, nand2);
    std::cerr << "\rLUT536   ";
    Ciphertext gin536 = 2 * gout498 + gout490;
    Ciphertext gout536 = ctx.eval_lut(&gin536, xor2);
    std::cerr << "\rLUT537   ";
    Ciphertext gin537 = 2 * gout496 + gout23;
    Ciphertext gout537 = ctx.eval_lut(&gin537, xor2);
    std::cerr << "\rLUT538   ";
    Ciphertext gin538 = 2 * gout496 + gout23;
    Ciphertext gout538 = ctx.eval_lut(&gin538, nand2);
    std::cerr << "\rLUT539   ";
    Ciphertext gin539 = 2 * gout447 + gout493;
    Ciphertext gout539 = ctx.eval_lut(&gin539, xor2);
    std::cerr << "\rLUT540   ";
    Ciphertext gin540 = 2 * gout447 + gout493;
    Ciphertext gout540 = ctx.eval_lut(&gin540, and2);
    std::cerr << "\rLUT541   ";
    Ciphertext gin541 = 2 * gout495 + gout7;
    Ciphertext gout541 = ctx.eval_lut(&gin541, xnor2);
    std::cerr << "\rLUT542   ";
    Ciphertext gin542 = 2 * gout462 + gout502;
    Ciphertext gout542 = ctx.eval_lut(&gin542, nand2);
    std::cerr << "\rLUT543   ";
    Ciphertext gin543 = 2 * gout478 + gout503;
    Ciphertext gout543 = ctx.eval_lut(&gin543, xor2);
    std::cerr << "\rLUT544   ";
    Ciphertext gin544 = 2 * gout487 + gout504;
    Ciphertext gout544 = ctx.eval_lut(&gin544, xnor2);
    std::cerr << "\rLUT545   ";
    Ciphertext gin545 = 2 * gout499 + gout494;
    Ciphertext gout545 = ctx.eval_lut(&gin545, xnor2);
    std::cerr << "\rLUT546   ";
    Ciphertext gin546 = 2 * gout487 + gout504;
    Ciphertext gout546 = ctx.eval_lut(&gin546, and2);
    std::cerr << "\rLUT547   ";
    Ciphertext gin547 = 2 * gout498 + gout490;
    Ciphertext gout547 = ctx.eval_lut(&gin547, and2);
    std::cerr << "\rLUT548   ";
    Ciphertext gin548 = 2 * gout499 + gout494;
    Ciphertext gout548 = ctx.eval_lut(&gin548, and2);
    std::cerr << "\rLUT549   ";
    Ciphertext gin549 = 2 * gout507 + gout180;
    Ciphertext gout549 = ctx.eval_lut(&gin549, nand2);
    std::cerr << "\rLUT550   ";
    Ciphertext gin550 = 2 * gout507 + gout180;
    Ciphertext gout550 = ctx.eval_lut(&gin550, xor2);
    std::cerr << "\rLUT551   ";
    Ciphertext gin551 = 2 * gout509 + gout25;
    Ciphertext gout551 = ctx.eval_lut(&gin551, nand2);
    std::cerr << "\rLUT552   ";
    Ciphertext gin552 = 2 * gout501 + gout492;
    Ciphertext gout552 = ctx.eval_lut(&gin552, nand2);
    std::cerr << "\rLUT553   ";
    Ciphertext gin553 = 2 * gout513 + gout511;
    Ciphertext gout553 = ctx.eval_lut(&gin553, xnor2);
    std::cerr << "\rLUT554   ";
    Ciphertext gin554 = 2 * gout501 + gout492;
    Ciphertext gout554 = ctx.eval_lut(&gin554, xor2);
    std::cerr << "\rLUT555   ";
    Ciphertext gin555 = 2 * gout512 + gout139;
    Ciphertext gout555 = ctx.eval_lut(&gin555, nand2);
    std::cerr << "\rLUT556   ";
    Ciphertext gin556 = 2 * gout478 + gout503;
    Ciphertext gout556 = ctx.eval_lut(&gin556, and2);
    std::cerr << "\rLUT557   ";
    Ciphertext gin557 = 2 * gout518 + gout508;
    Ciphertext gout557 = ctx.eval_lut(&gin557, nand2);
    std::cerr << "\rLUT558   ";
    Ciphertext gin558 = 2 * gout509 + gout25;
    Ciphertext gout558 = ctx.eval_lut(&gin558, xor2);
    std::cerr << "\rLUT559   ";
    Ciphertext gin559 = 2 * gout515 + gout517;
    Ciphertext gout559 = ctx.eval_lut(&gin559, xor2);
    std::cerr << "\rLUT560   ";
    Ciphertext gin560 = 2 * gout515 + gout517;
    Ciphertext gout560 = ctx.eval_lut(&gin560, and2);
    std::cerr << "\rLUT561   ";
    Ciphertext gin561 = 2 * gout512 + gout139;
    Ciphertext gout561 = ctx.eval_lut(&gin561, xnor2);
    std::cerr << "\rLUT562   ";
    Ciphertext gin562 = 2 * gout513 + gout511;
    Ciphertext gout562 = ctx.eval_lut(&gin562, and2);
    std::cerr << "\rLUT563   ";
    Ciphertext gin563 = 2 * gout518 + gout508;
    Ciphertext gout563 = ctx.eval_lut(&gin563, xor2);
    std::cerr << "\rLUT564   ";
    Ciphertext gin564 = 2 * gout131 + gout519;
    Ciphertext gout564 = ctx.eval_lut(&gin564, nand2);
    std::cerr << "\rLUT565   ";
    Ciphertext gin565 = 2 * gout520 + gout522;
    Ciphertext gout565 = ctx.eval_lut(&gin565, xnor2);
    std::cerr << "\rLUT566   ";
    Ciphertext gin566 = 2 * gout111 + gout521;
    Ciphertext gout566 = ctx.eval_lut(&gin566, nand2);
    std::cerr << "\rLUT567   ";
    Ciphertext gin567 = 2 * gout520 + (1 - gout522);
    Ciphertext gout567 = ctx.eval_lut(&gin567, or2);
    std::cerr << "\rLUT568   ";
    Ciphertext gin568 = 2 * gout10 + gout526;
    Ciphertext gout568 = ctx.eval_lut(&gin568, nand2);
    std::cerr << "\rLUT569   ";
    Ciphertext gin569 = 2 * gout122 + gout528;
    Ciphertext gout569 = ctx.eval_lut(&gin569, nand2);
    std::cerr << "\rLUT570   ";
    Ciphertext gin570 = 2 * gout82 + gout524;
    Ciphertext gout570 = ctx.eval_lut(&gin570, nand2);
    std::cerr << "\rLUT571   ";
    Ciphertext gin571 = 2 * gout167 + gout529;
    Ciphertext gout571 = ctx.eval_lut(&gin571, nand2);
    std::cerr << "\rLUT572   ";
    Ciphertext gin572 = 2 * gout481 + gout531;
    Ciphertext gout572 = ctx.eval_lut(&gin572, nand2);
    std::cerr << "\rLUT573   ";
    Ciphertext gin573 = 2 * gout527 + gout523;
    Ciphertext gout573 = ctx.eval_lut(&gin573, xnor2);
    std::cerr << "\rLUT574   ";
    Ciphertext gin574 = 2 * gout527 + (1 - gout523);
    Ciphertext gout574 = ctx.eval_lut(&gin574, or2);
    std::cerr << "\rLUT575   ";
    Ciphertext gin575 = 2 * gout50 + gout533;
    Ciphertext gout575 = ctx.eval_lut(&gin575, nand2);
    std::cerr << "\rLUT576   ";
    Ciphertext gin576 = 2 * gout27 + gout538;
    Ciphertext gout576 = ctx.eval_lut(&gin576, nand2);
    std::cerr << "\rLUT577   ";
    Ciphertext gin577 = 2 * gout8 + gout535;
    Ciphertext gout577 = ctx.eval_lut(&gin577, nand2);
    std::cerr << "\rLUT578   ";
    Ciphertext gin578 = 2 * gout488 + gout547;
    Ciphertext gout578 = ctx.eval_lut(&gin578, or2);
    std::cerr << "\rLUT579   ";
    Ciphertext gin579 = 2 * (1 - gout532) + gout537;
    Ciphertext gout579 = ctx.eval_lut(&gin579, and2);
    std::cerr << "\rLUT580   ";
    Ciphertext gin580 = 2 * gout128 + gout551;
    Ciphertext gout580 = ctx.eval_lut(&gin580, nand2);
    std::cerr << "\rLUT581   ";
    Ciphertext gin581 = 2 * gout485 + gout540;
    Ciphertext gout581 = ctx.eval_lut(&gin581, or2);
    std::cerr << "\rLUT582   ";
    Ciphertext gin582 = 2 * gout505 + gout546;
    Ciphertext gout582 = ctx.eval_lut(&gin582, or2);
    std::cerr << "\rLUT583   ";
    Ciphertext gin583 = 2 * gout471 + gout543;
    Ciphertext gout583 = ctx.eval_lut(&gin583, xor2);
    std::cerr << "\rLUT584   ";
    Ciphertext gin584 = 2 * (1 - gout541) + gout550;
    Ciphertext gout584 = ctx.eval_lut(&gin584, and2);
    std::cerr << "\rLUT585   ";
    Ciphertext gin585 = 2 * (1 - gout545) + gout536;
    Ciphertext gout585 = ctx.eval_lut(&gin585, and2);
    std::cerr << "\rLUT586   ";
    Ciphertext gin586 = 2 * gout489 + gout548;
    Ciphertext gout586 = ctx.eval_lut(&gin586, or2);
    std::cerr << "\rLUT587   ";
    Ciphertext gin587 = 2 * gout16 + gout549;
    Ciphertext gout587 = ctx.eval_lut(&gin587, nand2);
    std::cerr << "\rLUT588   ";
    Ciphertext gin588 = 2 * gout541 + gout550;
    Ciphertext gout588 = ctx.eval_lut(&gin588, xnor2);
    std::cerr << "\rLUT589   ";
    Ciphertext gin589 = 2 * gout514 + gout556;
    Ciphertext gout589 = ctx.eval_lut(&gin589, or2);
    std::cerr << "\rLUT590   ";
    Ciphertext gin590 = 2 * gout530 + (1 - gout554);
    Ciphertext gout590 = ctx.eval_lut(&gin590, or2);
    std::cerr << "\rLUT591   ";
    Ciphertext gin591 = 2 * gout532 + gout537;
    Ciphertext gout591 = ctx.eval_lut(&gin591, xnor2);
    std::cerr << "\rLUT592   ";
    Ciphertext gin592 = 2 * gout530 + gout554;
    Ciphertext gout592 = ctx.eval_lut(&gin592, xnor2);
    std::cerr << "\rLUT593   ";
    Ciphertext gin593 = 2 * gout471 + gout543;
    Ciphertext gout593 = ctx.eval_lut(&gin593, nand2);
    std::cerr << "\rLUT594   ";
    Ciphertext gin594 = 2 * gout491 + gout552;
    Ciphertext gout594 = ctx.eval_lut(&gin594, nand2);
    std::cerr << "\rLUT595   ";
    Ciphertext gin595 = 2 * gout561 + gout525;
    Ciphertext gout595 = ctx.eval_lut(&gin595, xnor2);
    std::cerr << "\rLUT596   ";
    Ciphertext gin596 = 2 * gout545 + gout536;
    Ciphertext gout596 = ctx.eval_lut(&gin596, xnor2);
    std::cerr << "\rLUT597   ";
    Ciphertext gin597 = 2 * (1 - gout561) + gout525;
    Ciphertext gout597 = ctx.eval_lut(&gin597, and2);
    std::cerr << "\rLUT598   ";
    Ciphertext gin598 = 2 * gout534 + gout558;
    Ciphertext gout598 = ctx.eval_lut(&gin598, xnor2);
    std::cerr << "\rLUT599   ";
    Ciphertext gin599 = 2 * gout192 + gout555;
    Ciphertext gout599 = ctx.eval_lut(&gin599, nand2);
    std::cerr << "\rLUT600   ";
    Ciphertext gin600 = 2 * gout553 + gout539;
    Ciphertext gout600 = ctx.eval_lut(&gin600, xnor2);
    std::cerr << "\rLUT601   ";
    Ciphertext gin601 = 2 * (1 - gout544) + gout559;
    Ciphertext gout601 = ctx.eval_lut(&gin601, and2);
    std::cerr << "\rLUT602   ";
    Ciphertext gin602 = 2 * (1 - gout553) + gout539;
    Ciphertext gout602 = ctx.eval_lut(&gin602, and2);
    std::cerr << "\rLUT603   ";
    Ciphertext gin603 = 2 * gout500 + gout562;
    Ciphertext gout603 = ctx.eval_lut(&gin603, or2);
    std::cerr << "\rLUT604   ";
    Ciphertext gin604 = 2 * gout510 + gout557;
    Ciphertext gout604 = ctx.eval_lut(&gin604, nand2);
    std::cerr << "\rLUT605   ";
    Ciphertext gin605 = 2 * (1 - gout534) + gout558;
    Ciphertext gout605 = ctx.eval_lut(&gin605, and2);
    std::cerr << "\rLUT606   ";
    Ciphertext gin606 = 2 * gout544 + gout559;
    Ciphertext gout606 = ctx.eval_lut(&gin606, xnor2);
    std::cerr << "\rLUT607   ";
    Ciphertext gin607 = 2 * gout516 + gout560;
    Ciphertext gout607 = ctx.eval_lut(&gin607, or2);
    std::cerr << "\rLUT608   ";
    Ciphertext gin608 = 2 * gout564 + gout132;
    Ciphertext gout608 = ctx.eval_lut(&gin608, nand2);
    std::cerr << "\rLUT609   ";
    Ciphertext gin609 = 2 * gout566 + gout124;
    Ciphertext gout609 = ctx.eval_lut(&gin609, nand2);
    std::cerr << "\rLUT610   ";
    Ciphertext gin610 = 2 * gout566 + gout124;
    Ciphertext gout610 = ctx.eval_lut(&gin610, xnor2);
    std::cerr << "\rLUT611   ";
    Ciphertext gin611 = 2 * gout564 + gout132;
    Ciphertext gout611 = ctx.eval_lut(&gin611, xor2);
    std::cerr << "\rLUT612   ";
    Ciphertext gin612 = 2 * gout568 + gout175;
    Ciphertext gout612 = ctx.eval_lut(&gin612, nand2);
    std::cerr << "\rLUT613   ";
    Ciphertext gin613 = 2 * gout568 + gout175;
    Ciphertext gout613 = ctx.eval_lut(&gin613, xor2);
    std::cerr << "\rLUT614   ";
    Ciphertext gin614 = 2 * gout570 + gout141;
    Ciphertext gout614 = ctx.eval_lut(&gin614, xor2);
    std::cerr << "\rLUT615   ";
    Ciphertext gin615 = 2 * gout570 + gout141;
    Ciphertext gout615 = ctx.eval_lut(&gin615, nand2);
    std::cerr << "\rLUT616   ";
    Ciphertext gin616 = 2 * gout572 + gout565;
    Ciphertext gout616 = ctx.eval_lut(&gin616, xnor2);
    std::cerr << "\rLUT617   ";
    Ciphertext gin617 = 2 * gout571 + gout149;
    Ciphertext gout617 = ctx.eval_lut(&gin617, nand2);
    std::cerr << "\rLUT618   ";
    Ciphertext gin618 = 2 * gout572 + gout565;
    Ciphertext gout618 = ctx.eval_lut(&gin618, nand2);
    std::cerr << "\rLUT619   ";
    Ciphertext gin619 = 2 * gout569 + gout125;
    Ciphertext gout619 = ctx.eval_lut(&gin619, xnor2);
    std::cerr << "\rLUT620   ";
    Ciphertext gin620 = 2 * gout569 + gout125;
    Ciphertext gout620 = ctx.eval_lut(&gin620, nand2);
    std::cerr << "\rLUT621   ";
    Ciphertext gin621 = 2 * gout571 + gout149;
    Ciphertext gout621 = ctx.eval_lut(&gin621, xnor2);
    std::cerr << "\rLUT622   ";
    Ciphertext gin622 = 2 * gout576 + gout19;
    Ciphertext gout622 = ctx.eval_lut(&gin622, xor2);
    std::cerr << "\rLUT623   ";
    Ciphertext gin623 = 2 * gout576 + gout19;
    Ciphertext gout623 = ctx.eval_lut(&gin623, nand2);
    std::cerr << "\rLUT624   ";
    Ciphertext gin624 = 2 * gout575 + gout184;
    Ciphertext gout624 = ctx.eval_lut(&gin624, xnor2);
    std::cerr << "\rLUT625   ";
    Ciphertext gin625 = 2 * gout577 + gout28;
    Ciphertext gout625 = ctx.eval_lut(&gin625, nand2);
    std::cerr << "\rLUT626   ";
    Ciphertext gin626 = 2 * gout575 + gout184;
    Ciphertext gout626 = ctx.eval_lut(&gin626, nand2);
    std::cerr << "\rLUT627   ";
    Ciphertext gin627 = 2 * gout542 + gout583;
    Ciphertext gout627 = ctx.eval_lut(&gin627, xor2);
    std::cerr << "\rLUT628   ";
    Ciphertext gin628 = 2 * gout577 + gout28;
    Ciphertext gout628 = ctx.eval_lut(&gin628, xnor2);
    std::cerr << "\rLUT629   ";
    Ciphertext gin629 = 2 * gout586 + gout595;
    Ciphertext gout629 = ctx.eval_lut(&gin629, and2);
    std::cerr << "\rLUT630   ";
    Ciphertext gin630 = 2 * gout582 + gout596;
    Ciphertext gout630 = ctx.eval_lut(&gin630, and2);
    std::cerr << "\rLUT631   ";
    Ciphertext gin631 = 2 * gout587 + gout89;
    Ciphertext gout631 = ctx.eval_lut(&gin631, xor2);
    std::cerr << "\rLUT632   ";
    Ciphertext gin632 = 2 * gout580 + gout47;
    Ciphertext gout632 = ctx.eval_lut(&gin632, nand2);
    std::cerr << "\rLUT633   ";
    Ciphertext gin633 = 2 * gout587 + gout89;
    Ciphertext gout633 = ctx.eval_lut(&gin633, nand2);
    std::cerr << "\rLUT634   ";
    Ciphertext gin634 = 2 * gout580 + gout47;
    Ciphertext gout634 = ctx.eval_lut(&gin634, xor2);
    std::cerr << "\rLUT635   ";
    Ciphertext gin635 = 2 * gout578 + gout591;
    Ciphertext gout635 = ctx.eval_lut(&gin635, xor2);
    std::cerr << "\rLUT636   ";
    Ciphertext gin636 = 2 * gout594 + gout573;
    Ciphertext gout636 = ctx.eval_lut(&gin636, nand2);
    std::cerr << "\rLUT637   ";
    Ciphertext gin637 = 2 * gout542 + gout583;
    Ciphertext gout637 = ctx.eval_lut(&gin637, nand2);
    std::cerr << "\rLUT638   ";
    Ciphertext gin638 = 2 * gout594 + gout573;
    Ciphertext gout638 = ctx.eval_lut(&gin638, xor2);
    std::cerr << "\rLUT639   ";
    Ciphertext gin639 = 2 * gout586 + gout595;
    Ciphertext gout639 = ctx.eval_lut(&gin639, xnor2);
    std::cerr << "\rLUT640   ";
    Ciphertext gin640 = 2 * gout582 + gout596;
    Ciphertext gout640 = ctx.eval_lut(&gin640, xnor2);
    std::cerr << "\rLUT641   ";
    Ciphertext gin641 = 2 * gout581 + gout598;
    Ciphertext gout641 = ctx.eval_lut(&gin641, xor2);
    std::cerr << "\rLUT642   ";
    Ciphertext gin642 = 2 * gout578 + gout591;
    Ciphertext gout642 = ctx.eval_lut(&gin642, and2);
    std::cerr << "\rLUT643   ";
    Ciphertext gin643 = 2 * gout599 + gout154;
    Ciphertext gout643 = ctx.eval_lut(&gin643, nand2);
    std::cerr << "\rLUT644   ";
    Ciphertext gin644 = 2 * gout589 + gout606;
    Ciphertext gout644 = ctx.eval_lut(&gin644, and2);
    std::cerr << "\rLUT645   ";
    Ciphertext gin645 = 2 * gout603 + gout588;
    Ciphertext gout645 = ctx.eval_lut(&gin645, and2);
    std::cerr << "\rLUT646   ";
    Ciphertext gin646 = 2 * gout603 + gout588;
    Ciphertext gout646 = ctx.eval_lut(&gin646, xnor2);
    std::cerr << "\rLUT647   ";
    Ciphertext gin647 = 2 * gout581 + gout598;
    Ciphertext gout647 = ctx.eval_lut(&gin647, and2);
    std::cerr << "\rLUT648   ";
    Ciphertext gin648 = 2 * gout604 + gout592;
    Ciphertext gout648 = ctx.eval_lut(&gin648, nand2);
    std::cerr << "\rLUT649   ";
    Ciphertext gin649 = 2 * gout599 + gout154;
    Ciphertext gout649 = ctx.eval_lut(&gin649, xnor2);
    std::cerr << "\rLUT650   ";
    Ciphertext gin650 = 2 * gout589 + gout606;
    Ciphertext gout650 = ctx.eval_lut(&gin650, xor2);
    std::cerr << "\rLUT651   ";
    Ciphertext gin651 = 2 * gout604 + gout592;
    Ciphertext gout651 = ctx.eval_lut(&gin651, xor2);
    std::cerr << "\rLUT652   ";
    Ciphertext gin652 = 2 * gout607 + gout600;
    Ciphertext gout652 = ctx.eval_lut(&gin652, xor2);
    std::cerr << "\rLUT653   ";
    Ciphertext gin653 = 2 * gout607 + gout600;
    Ciphertext gout653 = ctx.eval_lut(&gin653, and2);
    std::cerr << "\rLUT654   ";
    Ciphertext gin654 = 2 * gout113 + gout609;
    Ciphertext gout654 = ctx.eval_lut(&gin654, nand2);
    std::cerr << "\rLUT655   ";
    Ciphertext gin655 = 2 * gout76 + gout608;
    Ciphertext gout655 = ctx.eval_lut(&gin655, and2);
    std::cerr << "\rLUT656   ";
    Ciphertext gin656 = 2 * gout199 + gout615;
    Ciphertext gout656 = ctx.eval_lut(&gin656, nand2);
    std::cerr << "\rLUT657   ";
    Ciphertext gin657 = 2 * gout610 + (1 - gout611);
    Ciphertext gout657 = ctx.eval_lut(&gin657, or2);
    std::cerr << "\rLUT658   ";
    Ciphertext gin658 = 2 * gout619 + gout614;
    Ciphertext gout658 = ctx.eval_lut(&gin658, xnor2);
    std::cerr << "\rLUT659   ";
    Ciphertext gin659 = 2 * gout610 + gout611;
    Ciphertext gout659 = ctx.eval_lut(&gin659, xnor2);
    std::cerr << "\rLUT660   ";
    Ciphertext gin660 = 2 * gout619 + (1 - gout614);
    Ciphertext gout660 = ctx.eval_lut(&gin660, or2);
    std::cerr << "\rLUT661   ";
    Ciphertext gin661 = 2 * gout173 + gout620;
    Ciphertext gout661 = ctx.eval_lut(&gin661, and2);
    std::cerr << "\rLUT662   ";
    Ciphertext gin662 = 2 * gout6 + gout617;
    Ciphertext gout662 = ctx.eval_lut(&gin662, nand2);
    std::cerr << "\rLUT663   ";
    Ciphertext gin663 = 2 * gout567 + gout618;
    Ciphertext gout663 = ctx.eval_lut(&gin663, nand2);
    std::cerr << "\rLUT664   ";
    Ciphertext gin664 = 2 * gout14 + gout612;
    Ciphertext gout664 = ctx.eval_lut(&gin664, and2);
    std::cerr << "\rLUT665   ";
    Ciphertext gin665 = 2 * gout621 + gout622;
    Ciphertext gout665 = ctx.eval_lut(&gin665, xnor2);
    std::cerr << "\rLUT666   ";
    Ciphertext gin666 = 2 * (1 - gout621) + gout622;
    Ciphertext gout666 = ctx.eval_lut(&gin666, and2);
    std::cerr << "\rLUT667   ";
    Ciphertext gin667 = 2 * gout90 + gout632;
    Ciphertext gout667 = ctx.eval_lut(&gin667, nand2);
    std::cerr << "\rLUT668   ";
    Ciphertext gin668 = 2 * gout176 + gout623;
    Ciphertext gout668 = ctx.eval_lut(&gin668, and2);
    std::cerr << "\rLUT669   ";
    Ciphertext gin669 = 2 * gout597 + gout629;
    Ciphertext gout669 = ctx.eval_lut(&gin669, or2);
    std::cerr << "\rLUT670   ";
    Ciphertext gin670 = 2 * gout78 + gout625;
    Ciphertext gout670 = ctx.eval_lut(&gin670, and2);
    std::cerr << "\rLUT671   ";
    Ciphertext gin671 = 2 * gout585 + gout630;
    Ciphertext gout671 = ctx.eval_lut(&gin671, or2);
    std::cerr << "\rLUT672   ";
    Ciphertext gin672 = 2 * gout624 + gout631;
    Ciphertext gout672 = ctx.eval_lut(&gin672, xnor2);
    std::cerr << "\rLUT673   ";
    Ciphertext gin673 = 2 * (1 - gout624) + gout631;
    Ciphertext gout673 = ctx.eval_lut(&gin673, and2);
    std::cerr << "\rLUT674   ";
    Ciphertext gin674 = 2 * gout639 + gout635;
    Ciphertext gout674 = ctx.eval_lut(&gin674, xnor2);
    std::cerr << "\rLUT675   ";
    Ciphertext gin675 = 2 * gout628 + gout634;
    Ciphertext gout675 = ctx.eval_lut(&gin675, xnor2);
    std::cerr << "\rLUT676   ";
    Ciphertext gin676 = 2 * gout574 + gout636;
    Ciphertext gout676 = ctx.eval_lut(&gin676, nand2);
    std::cerr << "\rLUT677   ";
    Ciphertext gin677 = 2 * (1 - gout628) + gout634;
    Ciphertext gout677 = ctx.eval_lut(&gin677, and2);
    std::cerr << "\rLUT678   ";
    Ciphertext gin678 = 2 * gout73 + gout626;
    Ciphertext gout678 = ctx.eval_lut(&gin678, nand2);
    std::cerr << "\rLUT679   ";
    Ciphertext gin679 = 2 * gout616 + (1 - gout638);
    Ciphertext gout679 = ctx.eval_lut(&gin679, or2);
    std::cerr << "\rLUT680   ";
    Ciphertext gin680 = 2 * gout593 + gout637;
    Ciphertext gout680 = ctx.eval_lut(&gin680, nand2);
    std::cerr << "\rLUT681   ";
    Ciphertext gin681 = 2 * gout616 + gout638;
    Ciphertext gout681 = ctx.eval_lut(&gin681, xnor2);
    std::cerr << "\rLUT682   ";
    Ciphertext gin682 = 2 * gout579 + gout642;
    Ciphertext gout682 = ctx.eval_lut(&gin682, or2);
    std::cerr << "\rLUT683   ";
    Ciphertext gin683 = 2 * (1 - gout639) + gout635;
    Ciphertext gout683 = ctx.eval_lut(&gin683, and2);
    std::cerr << "\rLUT684   ";
    Ciphertext gin684 = 2 * gout584 + gout645;
    Ciphertext gout684 = ctx.eval_lut(&gin684, or2);
    std::cerr << "\rLUT685   ";
    Ciphertext gin685 = 2 * gout563 + gout650;
    Ciphertext gout685 = ctx.eval_lut(&gin685, nand2);
    std::cerr << "\rLUT686   ";
    Ciphertext gin686 = 2 * gout605 + gout647;
    Ciphertext gout686 = ctx.eval_lut(&gin686, or2);
    std::cerr << "\rLUT687   ";
    Ciphertext gin687 = 2 * gout649 + gout613;
    Ciphertext gout687 = ctx.eval_lut(&gin687, xnor2);
    std::cerr << "\rLUT688   ";
    Ciphertext gin688 = 2 * gout45 + gout633;
    Ciphertext gout688 = ctx.eval_lut(&gin688, and2);
    std::cerr << "\rLUT689   ";
    Ciphertext gin689 = 2 * gout646 + gout641;
    Ciphertext gout689 = ctx.eval_lut(&gin689, xnor2);
    std::cerr << "\rLUT690   ";
    Ciphertext gin690 = 2 * (1 - gout646) + gout641;
    Ciphertext gout690 = ctx.eval_lut(&gin690, and2);
    std::cerr << "\rLUT691   ";
    Ciphertext gin691 = 2 * gout4 + gout643;
    Ciphertext gout691 = ctx.eval_lut(&gin691, nand2);
    std::cerr << "\rLUT692   ";
    Ciphertext gin692 = 2 * gout601 + gout644;
    Ciphertext gout692 = ctx.eval_lut(&gin692, or2);
    std::cerr << "\rLUT693   ";
    Ciphertext gin693 = 2 * gout563 + gout650;
    Ciphertext gout693 = ctx.eval_lut(&gin693, xor2);
    std::cerr << "\rLUT694   ";
    Ciphertext gin694 = 2 * (1 - gout649) + gout613;
    Ciphertext gout694 = ctx.eval_lut(&gin694, and2);
    std::cerr << "\rLUT695   ";
    Ciphertext gin695 = 2 * gout590 + gout648;
    Ciphertext gout695 = ctx.eval_lut(&gin695, nand2);
    std::cerr << "\rLUT696   ";
    Ciphertext gin696 = 2 * gout640 + gout652;
    Ciphertext gout696 = ctx.eval_lut(&gin696, xnor2);
    std::cerr << "\rLUT697   ";
    Ciphertext gin697 = 2 * (1 - gout640) + gout652;
    Ciphertext gout697 = ctx.eval_lut(&gin697, and2);
    std::cerr << "\rLUT698   ";
    Ciphertext gin698 = 2 * gout602 + gout653;
    Ciphertext gout698 = ctx.eval_lut(&gin698, or2);
    std::cerr << "\rLUT699   ";
    Ciphertext gin699 = 2 * gout654 + gout134;
    Ciphertext gout699 = ctx.eval_lut(&gin699, nand2);
    std::cerr << "\rLUT700   ";
    Ciphertext gin700 = 2 * gout654 + gout99;
    Ciphertext gout700 = ctx.eval_lut(&gin700, xnor2);
    std::cerr << "\rLUT701   ";
    Ciphertext gin701 = 2 * gout655 + gout11;
    Ciphertext gout701 = ctx.eval_lut(&gin701, nand2);
    std::cerr << "\rLUT702   ";
    Ciphertext gin702 = 2 * gout664 + gout72;
    Ciphertext gout702 = ctx.eval_lut(&gin702, nand2);
    std::cerr << "\rLUT703   ";
    Ciphertext gin703 = 2 * gout663 + gout659;
    Ciphertext gout703 = ctx.eval_lut(&gin703, nand2);
    std::cerr << "\rLUT704   ";
    Ciphertext gin704 = 2 * gout663 + gout659;
    Ciphertext gout704 = ctx.eval_lut(&gin704, xnor2);
    std::cerr << "\rLUT705   ";
    Ciphertext gin705 = 2 * gout655 + gout114;
    Ciphertext gout705 = ctx.eval_lut(&gin705, xnor2);
    std::cerr << "\rLUT706   ";
    Ciphertext gin706 = 2 * gout662 + gout17;
    Ciphertext gout706 = ctx.eval_lut(&gin706, xnor2);
    std::cerr << "\rLUT707   ";
    Ciphertext gin707 = 2 * gout662 + gout104;
    Ciphertext gout707 = ctx.eval_lut(&gin707, nand2);
    std::cerr << "\rLUT708   ";
    Ciphertext gin708 = 2 * gout656 + gout195;
    Ciphertext gout708 = ctx.eval_lut(&gin708, xnor2);
    std::cerr << "\rLUT709   ";
    Ciphertext gin709 = 2 * gout656 + gout195;
    Ciphertext gout709 = ctx.eval_lut(&gin709, nand2);
    std::cerr << "\rLUT710   ";
    Ciphertext gin710 = 2 * gout661 + gout53;
    Ciphertext gout710 = ctx.eval_lut(&gin710, nand2);
    std::cerr << "\rLUT711   ";
    Ciphertext gin711 = 2 * gout661 + gout171;
    Ciphertext gout711 = ctx.eval_lut(&gin711, xnor2);
    std::cerr << "\rLUT712   ";
    Ciphertext gin712 = 2 * gout664 + gout38;
    Ciphertext gout712 = ctx.eval_lut(&gin712, xnor2);
    std::cerr << "\rLUT713   ";
    Ciphertext gin713 = 2 * gout667 + gout174;
    Ciphertext gout713 = ctx.eval_lut(&gin713, xnor2);
    std::cerr << "\rLUT714   ";
    Ciphertext gin714 = 2 * gout668 + gout69;
    Ciphertext gout714 = ctx.eval_lut(&gin714, nand2);
    std::cerr << "\rLUT715   ";
    Ciphertext gin715 = 2 * gout668 + gout34;
    Ciphertext gout715 = ctx.eval_lut(&gin715, xnor2);
    std::cerr << "\rLUT716   ";
    Ciphertext gin716 = 2 * gout670 + gout162;
    Ciphertext gout716 = ctx.eval_lut(&gin716, xnor2);
    std::cerr << "\rLUT717   ";
    Ciphertext gin717 = 2 * gout670 + gout36;
    Ciphertext gout717 = ctx.eval_lut(&gin717, nand2);
    std::cerr << "\rLUT718   ";
    Ciphertext gin718 = 2 * gout671 + gout674;
    Ciphertext gout718 = ctx.eval_lut(&gin718, and2);
    std::cerr << "\rLUT719   ";
    Ciphertext gin719 = 2 * gout671 + gout674;
    Ciphertext gout719 = ctx.eval_lut(&gin719, xnor2);
    std::cerr << "\rLUT720   ";
    Ciphertext gin720 = 2 * gout667 + gout174;
    Ciphertext gout720 = ctx.eval_lut(&gin720, nand2);
    std::cerr << "\rLUT721   ";
    Ciphertext gin721 = 2 * gout678 + gout74;
    Ciphertext gout721 = ctx.eval_lut(&gin721, xnor2);
    std::cerr << "\rLUT722   ";
    Ciphertext gin722 = 2 * gout682 + gout665;
    Ciphertext gout722 = ctx.eval_lut(&gin722, and2);
    std::cerr << "\rLUT723   ";
    Ciphertext gin723 = 2 * gout683 + gout718;
    Ciphertext gout723 = ctx.eval_lut(&gin723, or2);
    std::cerr << "\rLUT724   ";
    Ciphertext gin724 = 2 * gout682 + gout665;
    Ciphertext gout724 = ctx.eval_lut(&gin724, xor2);
    std::cerr << "\rLUT725   ";
    Ciphertext gin725 = 2 * gout676 + gout658;
    Ciphertext gout725 = ctx.eval_lut(&gin725, xor2);
    std::cerr << "\rLUT726   ";
    Ciphertext gin726 = 2 * gout676 + gout658;
    Ciphertext gout726 = ctx.eval_lut(&gin726, nand2);
    std::cerr << "\rLUT727   ";
    Ciphertext gin727 = 2 * gout688 + gout194;
    Ciphertext gout727 = ctx.eval_lut(&gin727, nand2);
    std::cerr << "\rLUT728   ";
    Ciphertext gin728 = 2 * gout678 + gout188;
    Ciphertext gout728 = ctx.eval_lut(&gin728, nand2);
    std::cerr << "\rLUT729   ";
    Ciphertext gin729 = 2 * gout680 + gout693;
    Ciphertext gout729 = ctx.eval_lut(&gin729, nand2);
    std::cerr << "\rLUT730   ";
    Ciphertext gin730 = 2 * gout669 + gout687;
    Ciphertext gout730 = ctx.eval_lut(&gin730, xnor2);
    std::cerr << "\rLUT731   ";
    Ciphertext gin731 = 2 * gout691 + gout60;
    Ciphertext gout731 = ctx.eval_lut(&gin731, xnor2);
    std::cerr << "\rLUT732   ";
    Ciphertext gin732 = 2 * gout684 + gout672;
    Ciphertext gout732 = ctx.eval_lut(&gin732, xnor2);
    std::cerr << "\rLUT733   ";
    Ciphertext gin733 = 2 * gout684 + gout672;
    Ciphertext gout733 = ctx.eval_lut(&gin733, and2);
    std::cerr << "\rLUT734   ";
    Ciphertext gin734 = 2 * gout686 + gout675;
    Ciphertext gout734 = ctx.eval_lut(&gin734, xor2);
    std::cerr << "\rLUT735   ";
    Ciphertext gin735 = 2 * gout686 + gout675;
    Ciphertext gout735 = ctx.eval_lut(&gin735, and2);
    std::cerr << "\rLUT736   ";
    Ciphertext gin736 = 2 * gout669 + gout687;
    Ciphertext gout736 = ctx.eval_lut(&gin736, and2);
    std::cerr << "\rLUT737   ";
    Ciphertext gin737 = 2 * gout695 + gout681;
    Ciphertext gout737 = ctx.eval_lut(&gin737, nand2);
    std::cerr << "\rLUT738   ";
    Ciphertext gin738 = 2 * gout695 + gout681;
    Ciphertext gout738 = ctx.eval_lut(&gin738, xor2);
    std::cerr << "\rLUT739   ";
    Ciphertext gin739 = 2 * gout688 + gout94;
    Ciphertext gout739 = ctx.eval_lut(&gin739, xnor2);
    std::cerr << "\rLUT740   ";
    Ciphertext gin740 = 2 * gout691 + gout60;
    Ciphertext gout740 = ctx.eval_lut(&gin740, nand2);
    std::cerr << "\rLUT741   ";
    Ciphertext gin741 = 2 * gout680 + gout693;
    Ciphertext gout741 = ctx.eval_lut(&gin741, xor2);
    std::cerr << "\rLUT742   ";
    Ciphertext gin742 = 2 * gout692 + gout696;
    Ciphertext gout742 = ctx.eval_lut(&gin742, xor2);
    std::cerr << "\rLUT743   ";
    Ciphertext gin743 = 2 * gout692 + gout696;
    Ciphertext gout743 = ctx.eval_lut(&gin743, and2);
    std::cerr << "\rLUT744   ";
    Ciphertext gin744 = 2 * gout698 + gout689;
    Ciphertext gout744 = ctx.eval_lut(&gin744, and2);
    std::cerr << "\rLUT745   ";
    Ciphertext gin745 = 2 * gout698 + gout689;
    Ciphertext gout745 = ctx.eval_lut(&gin745, xor2);
    std::cerr << "\rLUT746   ";
    Ciphertext gin746 = 2 * gout1 + gout699;
    Ciphertext gout746 = ctx.eval_lut(&gin746, nand2);
    std::cerr << "\rLUT747   ";
    Ciphertext gin747 = 2 * gout657 + gout703;
    Ciphertext gout747 = ctx.eval_lut(&gin747, nand2);
    std::cerr << "\rLUT748   ";
    Ciphertext gin748 = 2 * gout29 + gout702;
    Ciphertext gout748 = ctx.eval_lut(&gin748, and2);
    std::cerr << "\rLUT749   ";
    Ciphertext gin749 = 2 * gout700 + (1 - gout705);
    Ciphertext gout749 = ctx.eval_lut(&gin749, or2);
    std::cerr << "\rLUT750   ";
    Ciphertext gin750 = 2 * gout700 + gout705;
    Ciphertext gout750 = ctx.eval_lut(&gin750, xnor2);
    std::cerr << "\rLUT751   ";
    Ciphertext gin751 = 2 * gout161 + gout701;
    Ciphertext gout751 = ctx.eval_lut(&gin751, and2);
    std::cerr << "\rLUT752   ";
    Ciphertext gin752 = 2 * gout57 + gout707;
    Ciphertext gout752 = ctx.eval_lut(&gin752, nand2);
    std::cerr << "\rLUT753   ";
    Ciphertext gin753 = 2 * gout165 + gout710;
    Ciphertext gout753 = ctx.eval_lut(&gin753, and2);
    std::cerr << "\rLUT754   ";
    Ciphertext gin754 = 2 * gout708 + gout711;
    Ciphertext gout754 = ctx.eval_lut(&gin754, xnor2);
    std::cerr << "\rLUT755   ";
    Ciphertext gin755 = 2 * gout33 + gout709;
    Ciphertext gout755 = ctx.eval_lut(&gin755, nand2);
    std::cerr << "\rLUT756   ";
    Ciphertext gin756 = 2 * gout708 + (1 - gout711);
    Ciphertext gout756 = ctx.eval_lut(&gin756, or2);
    std::cerr << "\rLUT757   ";
    Ciphertext gin757 = 2 * gout706 + gout715;
    Ciphertext gout757 = ctx.eval_lut(&gin757, xnor2);
    std::cerr << "\rLUT758   ";
    Ciphertext gin758 = 2 * gout126 + gout714;
    Ciphertext gout758 = ctx.eval_lut(&gin758, and2);
    std::cerr << "\rLUT759   ";
    Ciphertext gin759 = 2 * gout713 + gout716;
    Ciphertext gout759 = ctx.eval_lut(&gin759, xnor2);
    std::cerr << "\rLUT760   ";
    Ciphertext gin760 = 2 * gout660 + gout726;
    Ciphertext gout760 = ctx.eval_lut(&gin760, nand2);
    std::cerr << "\rLUT761   ";
    Ciphertext gin761 = 2 * gout706 + (1 - gout715);
    Ciphertext gout761 = ctx.eval_lut(&gin761, or2);
    std::cerr << "\rLUT762   ";
    Ciphertext gin762 = 2 * gout44 + gout717;
    Ciphertext gout762 = ctx.eval_lut(&gin762, and2);
    std::cerr << "\rLUT763   ";
    Ciphertext gin763 = 2 * gout713 + (1 - gout716);
    Ciphertext gout763 = ctx.eval_lut(&gin763, or2);
    std::cerr << "\rLUT764   ";
    Ciphertext gin764 = 2 * gout730 + gout724;
    Ciphertext gout764 = ctx.eval_lut(&gin764, xnor2);
    std::cerr << "\rLUT765   ";
    Ciphertext gin765 = 2 * gout704 + gout725;
    Ciphertext gout765 = ctx.eval_lut(&gin765, xnor2);
    std::cerr << "\rLUT766   ";
    Ciphertext gin766 = 2 * gout704 + (1 - gout725);
    Ciphertext gout766 = ctx.eval_lut(&gin766, or2);
    std::cerr << "\rLUT767   ";
    Ciphertext gin767 = 2 * gout2 + gout727;
    Ciphertext gout767 = ctx.eval_lut(&gin767, and2);
    std::cerr << "\rLUT768   ";
    Ciphertext gin768 = 2 * gout685 + gout729;
    Ciphertext gout768 = ctx.eval_lut(&gin768, nand2);
    std::cerr << "\rLUT769   ";
    Ciphertext gin769 = 2 * gout731 + (1 - gout712);
    Ciphertext gout769 = ctx.eval_lut(&gin769, or2);
    std::cerr << "\rLUT770   ";
    Ciphertext gin770 = 2 * gout26 + gout720;
    Ciphertext gout770 = ctx.eval_lut(&gin770, nand2);
    std::cerr << "\rLUT771   ";
    Ciphertext gin771 = 2 * gout666 + gout722;
    Ciphertext gout771 = ctx.eval_lut(&gin771, or2);
    std::cerr << "\rLUT772   ";
    Ciphertext gin772 = 2 * gout677 + gout735;
    Ciphertext gout772 = ctx.eval_lut(&gin772, or2);
    std::cerr << "\rLUT773   ";
    Ciphertext gin773 = 2 * gout183 + gout728;
    Ciphertext gout773 = ctx.eval_lut(&gin773, nand2);
    std::cerr << "\rLUT774   ";
    Ciphertext gin774 = 2 * (1 - gout732) + gout734;
    Ciphertext gout774 = ctx.eval_lut(&gin774, and2);
    std::cerr << "\rLUT775   ";
    Ciphertext gin775 = 2 * gout731 + gout712;
    Ciphertext gout775 = ctx.eval_lut(&gin775, xnor2);
    std::cerr << "\rLUT776   ";
    Ciphertext gin776 = 2 * gout721 + gout739;
    Ciphertext gout776 = ctx.eval_lut(&gin776, xnor2);
    std::cerr << "\rLUT777   ";
    Ciphertext gin777 = 2 * gout721 + (1 - gout739);
    Ciphertext gout777 = ctx.eval_lut(&gin777, or2);
    std::cerr << "\rLUT778   ";
    Ciphertext gin778 = 2 * gout694 + gout736;
    Ciphertext gout778 = ctx.eval_lut(&gin778, or2);
    std::cerr << "\rLUT779   ";
    Ciphertext gin779 = 2 * (1 - gout730) + gout724;
    Ciphertext gout779 = ctx.eval_lut(&gin779, and2);
    std::cerr << "\rLUT780   ";
    Ciphertext gin780 = 2 * gout679 + gout737;
    Ciphertext gout780 = ctx.eval_lut(&gin780, nand2);
    std::cerr << "\rLUT781   ";
    Ciphertext gin781 = 2 * gout673 + gout733;
    Ciphertext gout781 = ctx.eval_lut(&gin781, or2);
    std::cerr << "\rLUT782   ";
    Ciphertext gin782 = 2 * gout732 + gout734;
    Ciphertext gout782 = ctx.eval_lut(&gin782, xnor2);
    std::cerr << "\rLUT783   ";
    Ciphertext gin783 = 2 * gout697 + gout743;
    Ciphertext gout783 = ctx.eval_lut(&gin783, or2);
    std::cerr << "\rLUT784   ";
    Ciphertext gin784 = 2 * gout651 + gout742;
    Ciphertext gout784 = ctx.eval_lut(&gin784, xor2);
    std::cerr << "\rLUT785   ";
    Ciphertext gin785 = 2 * gout651 + gout742;
    Ciphertext gout785 = ctx.eval_lut(&gin785, nand2);
    std::cerr << "\rLUT786   ";
    Ciphertext gin786 = 2 * gout52 + gout740;
    Ciphertext gout786 = ctx.eval_lut(&gin786, nand2);
    std::cerr << "\rLUT787   ";
    Ciphertext gin787 = 2 * gout690 + gout744;
    Ciphertext gout787 = ctx.eval_lut(&gin787, or2);
    std::cerr << "\rLUT788   ";
    Ciphertext gin788 = 2 * (1 - gout719) + gout745;
    Ciphertext gout788 = ctx.eval_lut(&gin788, and2);
    std::cerr << "\rLUT789   ";
    Ciphertext gin789 = 2 * gout719 + gout745;
    Ciphertext gout789 = ctx.eval_lut(&gin789, xnor2);
    std::cerr << "\rLUT790   ";
    Ciphertext gin790 = 2 * gout747 + gout750;
    Ciphertext gout790 = ctx.eval_lut(&gin790, nand2);
    std::cerr << "\rLUT791   ";
    Ciphertext gin791 = 2 * gout747 + gout750;
    Ciphertext gout791 = ctx.eval_lut(&gin791, xnor2);
    std::cerr << "\rLUT792   ";
    Ciphertext gin792 = 2 * gout746 + gout751;
    Ciphertext gout792 = ctx.eval_lut(&gin792, xor2);
    std::cerr << "\rLUT793   ";
    Ciphertext gin793 = 2 * gout746 + gout751;
    Ciphertext gout793 = ctx.eval_lut(&gin793, nand2);
    std::cerr << "\rLUT794   ";
    Ciphertext gin794 = 2 * gout755 + gout753;
    Ciphertext gout794 = ctx.eval_lut(&gin794, xor2);
    std::cerr << "\rLUT795   ";
    Ciphertext gin795 = 2 * gout752 + gout758;
    Ciphertext gout795 = ctx.eval_lut(&gin795, nand2);
    std::cerr << "\rLUT796   ";
    Ciphertext gin796 = 2 * gout755 + gout753;
    Ciphertext gout796 = ctx.eval_lut(&gin796, nand2);
    std::cerr << "\rLUT797   ";
    Ciphertext gin797 = 2 * gout760 + gout754;
    Ciphertext gout797 = ctx.eval_lut(&gin797, xor2);
    std::cerr << "\rLUT798   ";
    Ciphertext gin798 = 2 * gout760 + gout754;
    Ciphertext gout798 = ctx.eval_lut(&gin798, nand2);
    std::cerr << "\rLUT799   ";
    Ciphertext gin799 = 2 * gout752 + gout758;
    Ciphertext gout799 = ctx.eval_lut(&gin799, xor2);
    std::cerr << "\rLUT800   ";
    Ciphertext gin800 = 2 * gout723 + gout764;
    Ciphertext gout800 = ctx.eval_lut(&gin800, xnor2);
    std::cerr << "\rLUT801   ";
    Ciphertext gin801 = 2 * gout723 + gout764;
    Ciphertext gout801 = ctx.eval_lut(&gin801, and2);
    std::cerr << "\rLUT802   ";
    Ciphertext gin802 = 2 * gout771 + gout757;
    Ciphertext gout802 = ctx.eval_lut(&gin802, xor2);
    std::cerr << "\rLUT803   ";
    Ciphertext gin803 = 2 * gout772 + gout759;
    Ciphertext gout803 = ctx.eval_lut(&gin803, xor2);
    std::cerr << "\rLUT804   ";
    Ciphertext gin804 = 2 * gout773 + gout767;
    Ciphertext gout804 = ctx.eval_lut(&gin804, xor2);
    std::cerr << "\rLUT805   ";
    Ciphertext gin805 = 2 * gout771 + gout757;
    Ciphertext gout805 = ctx.eval_lut(&gin805, nand2);
    std::cerr << "\rLUT806   ";
    Ciphertext gin806 = 2 * gout778 + gout775;
    Ciphertext gout806 = ctx.eval_lut(&gin806, xnor2);
    std::cerr << "\rLUT807   ";
    Ciphertext gin807 = 2 * gout770 + gout762;
    Ciphertext gout807 = ctx.eval_lut(&gin807, xor2);
    std::cerr << "\rLUT808   ";
    Ciphertext gin808 = 2 * gout770 + gout762;
    Ciphertext gout808 = ctx.eval_lut(&gin808, nand2);
    std::cerr << "\rLUT809   ";
    Ciphertext gin809 = 2 * gout781 + gout776;
    Ciphertext gout809 = ctx.eval_lut(&gin809, xnor2);
    std::cerr << "\rLUT810   ";
    Ciphertext gin810 = 2 * gout781 + gout776;
    Ciphertext gout810 = ctx.eval_lut(&gin810, nand2);
    std::cerr << "\rLUT811   ";
    Ciphertext gin811 = 2 * gout772 + gout759;
    Ciphertext gout811 = ctx.eval_lut(&gin811, nand2);
    std::cerr << "\rLUT812   ";
    Ciphertext gin812 = 2 * gout773 + gout767;
    Ciphertext gout812 = ctx.eval_lut(&gin812, nand2);
    std::cerr << "\rLUT813   ";
    Ciphertext gin813 = 2 * gout780 + gout765;
    Ciphertext gout813 = ctx.eval_lut(&gin813, xor2);
    std::cerr << "\rLUT814   ";
    Ciphertext gin814 = 2 * gout780 + gout765;
    Ciphertext gout814 = ctx.eval_lut(&gin814, nand2);
    std::cerr << "\rLUT815   ";
    Ciphertext gin815 = 2 * gout778 + gout775;
    Ciphertext gout815 = ctx.eval_lut(&gin815, nand2);
    std::cerr << "\rLUT816   ";
    Ciphertext gin816 = 2 * gout786 + gout748;
    Ciphertext gout816 = ctx.eval_lut(&gin816, nand2);
    std::cerr << "\rLUT817   ";
    Ciphertext gin817 = 2 * gout768 + gout784;
    Ciphertext gout817 = ctx.eval_lut(&gin817, nand2);
    std::cerr << "\rLUT818   ";
    Ciphertext gin818 = 2 * gout779 + gout801;
    Ciphertext gout818 = ctx.eval_lut(&gin818, or2);
    std::cerr << "\rLUT819   ";
    Ciphertext gin819 = 2 * gout783 + gout789;
    Ciphertext gout819 = ctx.eval_lut(&gin819, xor2);
    std::cerr << "\rLUT820   ";
    Ciphertext gin820 = 2 * gout783 + gout789;
    Ciphertext gout820 = ctx.eval_lut(&gin820, and2);
    std::cerr << "\rLUT821   ";
    Ciphertext gin821 = 2 * gout787 + gout782;
    Ciphertext gout821 = ctx.eval_lut(&gin821, xor2);
    std::cerr << "\rLUT822   ";
    Ciphertext gin822 = 2 * gout787 + gout782;
    Ciphertext gout822 = ctx.eval_lut(&gin822, and2);
    std::cerr << "\rLUT823   ";
    Ciphertext gin823 = 2 * gout786 + gout748;
    Ciphertext gout823 = ctx.eval_lut(&gin823, xor2);
    std::cerr << "\rLUT824   ";
    Ciphertext gin824 = 2 * gout768 + gout784;
    Ciphertext gout824 = ctx.eval_lut(&gin824, xor2);
    std::cerr << "\rLUT825   ";
    Ciphertext gin825 = 2 * gout749 + gout790;
    Ciphertext gout825 = ctx.eval_lut(&gin825, nand2);
    std::cerr << "\rLUT826   ";
    Ciphertext gin826 = 2 * gout756 + gout798;
    Ciphertext gout826 = ctx.eval_lut(&gin826, nand2);
    std::cerr << "\rLUT827   ";
    Ciphertext gin827 = 2 * gout791 + gout797;
    Ciphertext gout827 = ctx.eval_lut(&gin827, xnor2);
    std::cerr << "\rLUT828   ";
    Ciphertext gin828 = 2 * gout791 + (1 - gout797);
    Ciphertext gout828 = ctx.eval_lut(&gin828, or2);
    std::cerr << "\rLUT829   ";
    Ciphertext gin829 = 2 * gout761 + gout805;
    Ciphertext gout829 = ctx.eval_lut(&gin829, nand2);
    std::cerr << "\rLUT830   ";
    Ciphertext gin830 = 2 * (1 - gout806) + gout802;
    Ciphertext gout830 = ctx.eval_lut(&gin830, and2);
    std::cerr << "\rLUT831   ";
    Ciphertext gin831 = 2 * gout809 + gout803;
    Ciphertext gout831 = ctx.eval_lut(&gin831, xnor2);
    std::cerr << "\rLUT832   ";
    Ciphertext gin832 = 2 * gout763 + gout811;
    Ciphertext gout832 = ctx.eval_lut(&gin832, nand2);
    std::cerr << "\rLUT833   ";
    Ciphertext gin833 = 2 * gout806 + gout802;
    Ciphertext gout833 = ctx.eval_lut(&gin833, xnor2);
    std::cerr << "\rLUT834   ";
    Ciphertext gin834 = 2 * gout785 + gout817;
    Ciphertext gout834 = ctx.eval_lut(&gin834, nand2);
    std::cerr << "\rLUT835   ";
    Ciphertext gin835 = 2 * gout777 + gout810;
    Ciphertext gout835 = ctx.eval_lut(&gin835, nand2);
    std::cerr << "\rLUT836   ";
    Ciphertext gin836 = 2 * (1 - gout809) + gout803;
    Ciphertext gout836 = ctx.eval_lut(&gin836, and2);
    std::cerr << "\rLUT837   ";
    Ciphertext gin837 = 2 * gout766 + gout814;
    Ciphertext gout837 = ctx.eval_lut(&gin837, nand2);
    std::cerr << "\rLUT838   ";
    Ciphertext gin838 = 2 * gout738 + gout819;
    Ciphertext gout838 = ctx.eval_lut(&gin838, xor2);
    std::cerr << "\rLUT839   ";
    Ciphertext gin839 = 2 * gout769 + gout815;
    Ciphertext gout839 = ctx.eval_lut(&gin839, nand2);
    std::cerr << "\rLUT840   ";
    Ciphertext gin840 = 2 * gout738 + gout819;
    Ciphertext gout840 = ctx.eval_lut(&gin840, nand2);
    std::cerr << "\rLUT841   ";
    Ciphertext gin841 = 2 * gout800 + gout821;
    Ciphertext gout841 = ctx.eval_lut(&gin841, xnor2);
    std::cerr << "\rLUT842   ";
    Ciphertext gin842 = 2 * (1 - gout800) + gout821;
    Ciphertext gout842 = ctx.eval_lut(&gin842, and2);
    std::cerr << "\rLUT843   ";
    Ciphertext gin843 = 2 * gout774 + gout822;
    Ciphertext gout843 = ctx.eval_lut(&gin843, or2);
    std::cerr << "\rLUT844   ";
    Ciphertext gin844 = 2 * gout788 + gout820;
    Ciphertext gout844 = ctx.eval_lut(&gin844, or2);
    std::cerr << "\rLUT845   ";
    Ciphertext gin845 = 2 * gout825 + gout792;
    Ciphertext gout845 = ctx.eval_lut(&gin845, nand2);
    std::cerr << "\rLUT846   ";
    Ciphertext gin846 = 2 * gout825 + gout792;
    Ciphertext gout846 = ctx.eval_lut(&gin846, xnor2);
    std::cerr << "\rLUT847   ";
    Ciphertext gin847 = 2 * gout793 + gout845;
    Ciphertext gout847 = ctx.eval_lut(&gin847, nand2);
    std::cerr << "\rLUT848   ";
    Ciphertext gin848 = 2 * gout826 + gout794;
    Ciphertext gout848 = ctx.eval_lut(&gin848, xor2);
    std::cerr << "\rLUT849   ";
    Ciphertext gin849 = 2 * gout826 + gout794;
    Ciphertext gout849 = ctx.eval_lut(&gin849, nand2);
    std::cerr << "\rLUT850   ";
    Ciphertext gin850 = 2 * gout829 + gout799;
    Ciphertext gout850 = ctx.eval_lut(&gin850, xor2);
    std::cerr << "\rLUT851   ";
    Ciphertext gin851 = 2 * gout829 + gout799;
    Ciphertext gout851 = ctx.eval_lut(&gin851, nand2);
    std::cerr << "\rLUT852   ";
    Ciphertext gin852 = 2 * gout835 + gout804;
    Ciphertext gout852 = ctx.eval_lut(&gin852, xnor2);
    std::cerr << "\rLUT853   ";
    Ciphertext gin853 = 2 * gout835 + gout804;
    Ciphertext gout853 = ctx.eval_lut(&gin853, nand2);
    std::cerr << "\rLUT854   ";
    Ciphertext gin854 = 2 * gout818 + gout833;
    Ciphertext gout854 = ctx.eval_lut(&gin854, and2);
    std::cerr << "\rLUT855   ";
    Ciphertext gin855 = 2 * gout832 + gout807;
    Ciphertext gout855 = ctx.eval_lut(&gin855, nand2);
    std::cerr << "\rLUT856   ";
    Ciphertext gin856 = 2 * gout834 + gout838;
    Ciphertext gout856 = ctx.eval_lut(&gin856, nand2);
    std::cerr << "\rLUT857   ";
    Ciphertext gin857 = 2 * gout840 + gout856;
    Ciphertext gout857 = ctx.eval_lut(&gin857, nand2);
    std::cerr << "\rLUT858   ";
    Ciphertext gin858 = 2 * gout837 + gout827;
    Ciphertext gout858 = ctx.eval_lut(&gin858, nand2);
    std::cerr << "\rLUT859   ";
    Ciphertext gin859 = 2 * gout834 + gout838;
    Ciphertext gout859 = ctx.eval_lut(&gin859, xor2);
    std::cerr << "\rLUT860   ";
    Ciphertext gin860 = 2 * gout839 + gout823;
    Ciphertext gout860 = ctx.eval_lut(&gin860, nand2);
    std::cerr << "\rLUT861   ";
    Ciphertext gin861 = 2 * gout832 + gout807;
    Ciphertext gout861 = ctx.eval_lut(&gin861, xor2);
    std::cerr << "\rLUT862   ";
    Ciphertext gin862 = 2 * gout818 + gout833;
    Ciphertext gout862 = ctx.eval_lut(&gin862, xnor2);
    std::cerr << "\rLUT863   ";
    Ciphertext gin863 = 2 * gout844 + gout841;
    Ciphertext gout863 = ctx.eval_lut(&gin863, xor2);
    std::cerr << "\rLUT864   ";
    Ciphertext gin864 = 2 * gout839 + gout823;
    Ciphertext gout864 = ctx.eval_lut(&gin864, xnor2);
    std::cerr << "\rLUT865   ";
    Ciphertext gin865 = 2 * gout843 + gout831;
    Ciphertext gout865 = ctx.eval_lut(&gin865, xor2);
    std::cerr << "\rLUT866   ";
    Ciphertext gin866 = 2 * gout837 + gout827;
    Ciphertext gout866 = ctx.eval_lut(&gin866, xor2);
    std::cerr << "\rLUT867   ";
    Ciphertext gin867 = 2 * gout843 + gout831;
    Ciphertext gout867 = ctx.eval_lut(&gin867, and2);
    std::cerr << "\rLUT868   ";
    Ciphertext gin868 = 2 * gout844 + gout841;
    Ciphertext gout868 = ctx.eval_lut(&gin868, and2);
    std::cerr << "\rLUT869   ";
    Ciphertext gin869 = 2 * gout846 + gout848;
    Ciphertext gout869 = ctx.eval_lut(&gin869, xnor2);
    std::cerr << "\rLUT870   ";
    Ciphertext gin870 = 2 * gout796 + gout849;
    Ciphertext gout870 = ctx.eval_lut(&gin870, nand2);
    std::cerr << "\rLUT871   ";
    Ciphertext gin871 = 2 * gout846 + (1 - gout848);
    Ciphertext gout871 = ctx.eval_lut(&gin871, or2);
    std::cerr << "\rLUT872   ";
    Ciphertext gin872 = 2 * gout795 + gout851;
    Ciphertext gout872 = ctx.eval_lut(&gin872, nand2);
    std::cerr << "\rLUT873   ";
    Ciphertext gin873 = 2 * gout830 + gout854;
    Ciphertext gout873 = ctx.eval_lut(&gin873, or2);
    std::cerr << "\rLUT874   ";
    Ciphertext gin874 = 2 * gout812 + gout853;
    Ciphertext gout874 = ctx.eval_lut(&gin874, nand2);
    std::cerr << "\rLUT875   ";
    Ciphertext gin875 = 2 * gout816 + gout860;
    Ciphertext gout875 = ctx.eval_lut(&gin875, nand2);
    std::cerr << "\rLUT876   ";
    Ciphertext gin876 = 2 * (1 - gout864) + gout850;
    Ciphertext gout876 = ctx.eval_lut(&gin876, and2);
    std::cerr << "\rLUT877   ";
    Ciphertext gin877 = 2 * gout808 + gout855;
    Ciphertext gout877 = ctx.eval_lut(&gin877, nand2);
    std::cerr << "\rLUT878   ";
    Ciphertext gin878 = 2 * gout813 + gout863;
    Ciphertext gout878 = ctx.eval_lut(&gin878, nand2);
    std::cerr << "\rLUT879   ";
    Ciphertext gin879 = 2 * gout862 + gout865;
    Ciphertext gout879 = ctx.eval_lut(&gin879, xnor2);
    std::cerr << "\rLUT880   ";
    Ciphertext gin880 = 2 * (1 - gout852) + gout861;
    Ciphertext gout880 = ctx.eval_lut(&gin880, and2);
    std::cerr << "\rLUT881   ";
    Ciphertext gin881 = 2 * gout813 + gout863;
    Ciphertext gout881 = ctx.eval_lut(&gin881, xor2);
    std::cerr << "\rLUT882   ";
    Ciphertext gin882 = 2 * gout864 + gout850;
    Ciphertext gout882 = ctx.eval_lut(&gin882, xnor2);
    std::cerr << "\rLUT883   ";
    Ciphertext gin883 = 2 * gout852 + gout861;
    Ciphertext gout883 = ctx.eval_lut(&gin883, xnor2);
    std::cerr << "\rLUT884   ";
    Ciphertext gin884 = 2 * gout828 + gout858;
    Ciphertext gout884 = ctx.eval_lut(&gin884, nand2);
    std::cerr << "\rLUT885   ";
    Ciphertext gin885 = 2 * gout842 + gout868;
    Ciphertext gout885 = ctx.eval_lut(&gin885, or2);
    std::cerr << "\rLUT886   ";
    Ciphertext gin886 = 2 * (1 - gout862) + gout865;
    Ciphertext gout886 = ctx.eval_lut(&gin886, and2);
    std::cerr << "\rLUT887   ";
    Ciphertext gin887 = 2 * gout836 + gout867;
    Ciphertext gout887 = ctx.eval_lut(&gin887, or2);
    std::cerr << "\rLUT888   ";
    Ciphertext gin888 = 2 * gout847 + gout870;
    Ciphertext gout888 = ctx.eval_lut(&gin888, nand2);
    std::cerr << "\rLUT889   ";
    Ciphertext gin889 = 2 * gout847 + gout870;
    Ciphertext gout889 = ctx.eval_lut(&gin889, xor2);
    std::cerr << "\rLUT890   ";
    Ciphertext gin890 = 2 * gout874 + gout877;
    Ciphertext gout890 = ctx.eval_lut(&gin890, or2);
    std::cerr << "\rLUT891   ";
    Ciphertext gin891 = 2 * gout874 + gout877;
    Ciphertext gout891 = ctx.eval_lut(&gin891, nand2);
    std::cerr << "\rLUT892   ";
    Ciphertext gin892 = 2 * gout874 + gout877;
    Ciphertext gout892 = ctx.eval_lut(&gin892, xnor2);
    std::cerr << "\rLUT893   ";
    Ciphertext gin893 = 2 * gout875 + gout872;
    Ciphertext gout893 = ctx.eval_lut(&gin893, xor2);
    std::cerr << "\rLUT894   ";
    Ciphertext gin894 = 2 * gout875 + gout872;
    Ciphertext gout894 = ctx.eval_lut(&gin894, nand2);
    std::cerr << "\rLUT895   ";
    Ciphertext gin895 = 2 * gout857 + gout881;
    Ciphertext gout895 = ctx.eval_lut(&gin895, nand2);
    std::cerr << "\rLUT896   ";
    Ciphertext gin896 = 2 * gout885 + gout879;
    Ciphertext gout896 = ctx.eval_lut(&gin896, xor2);
    std::cerr << "\rLUT897   ";
    Ciphertext gin897 = 2 * gout857 + gout881;
    Ciphertext gout897 = ctx.eval_lut(&gin897, xor2);
    std::cerr << "\rLUT898   ";
    Ciphertext gin898 = 2 * gout873 + gout882;
    Ciphertext gout898 = ctx.eval_lut(&gin898, xnor2);
    std::cerr << "\rLUT899   ";
    Ciphertext gin899 = 2 * gout873 + gout882;
    Ciphertext gout899 = ctx.eval_lut(&gin899, and2);
    std::cerr << "\rLUT900   ";
    Ciphertext gin900 = 2 * gout887 + gout883;
    Ciphertext gout900 = ctx.eval_lut(&gin900, xor2);
    std::cerr << "\rLUT901   ";
    Ciphertext gin901 = 2 * gout884 + gout869;
    Ciphertext gout901 = ctx.eval_lut(&gin901, xor2);
    std::cerr << "\rLUT902   ";
    Ciphertext gin902 = 2 * gout884 + gout869;
    Ciphertext gout902 = ctx.eval_lut(&gin902, nand2);
    std::cerr << "\rLUT903   ";
    Ciphertext gin903 = 2 * gout885 + gout879;
    Ciphertext gout903 = ctx.eval_lut(&gin903, and2);
    std::cerr << "\rLUT904   ";
    Ciphertext gin904 = 2 * gout887 + gout883;
    Ciphertext gout904 = ctx.eval_lut(&gin904, and2);
    std::cerr << "\rLUT905   ";
    Ciphertext gin905 = 2 * gout866 + gout896;
    Ciphertext gout905 = ctx.eval_lut(&gin905, nand2);
    std::cerr << "\rLUT906   ";
    Ciphertext gin906 = 2 * gout898 + gout900;
    Ciphertext gout906 = ctx.eval_lut(&gin906, xnor2);
    std::cerr << "\rINV907   ";
    Ciphertext gout907 = 1 - gout893;

    std::cerr << "\rLUT908   ";
    Ciphertext gin908 = 2 * gout866 + gout896;
    Ciphertext gout908 = ctx.eval_lut(&gin908, xor2);
    std::cerr << "\rLUT909   ";
    Ciphertext gin909 = 2 * (1 - gout898) + gout900;
    Ciphertext gout909 = ctx.eval_lut(&gin909, and2);
    std::cerr << "\rLUT910   ";
    Ciphertext gin910 = 2 * gout878 + gout895;
    Ciphertext gout910 = ctx.eval_lut(&gin910, nand2);
    std::cerr << "\rLUT911   ";
    Ciphertext gin911 = 2 * gout876 + gout899;
    Ciphertext gout911 = ctx.eval_lut(&gin911, or2);
    std::cerr << "\rLUT912   ";
    Ciphertext gin912 = 2 * gout871 + gout902;
    Ciphertext gout912 = ctx.eval_lut(&gin912, nand2);
    std::cerr << "\rLUT913   ";
    Ciphertext gin913 = 2 * gout886 + gout903;
    Ciphertext gout913 = ctx.eval_lut(&gin913, or2);
    std::cerr << "\rLUT914   ";
    Ciphertext gin914 = 2 * gout880 + gout904;
    Ciphertext gout914 = ctx.eval_lut(&gin914, or2);
    std::cerr << "\rLUT915   ";
    Ciphertext gin915 = 2 * gout911 + gout907;
    Ciphertext gout915 = ctx.eval_lut(&gin915, xnor2);
    std::cerr << "\rLUT916   ";
    Ciphertext gin916 = 2 * gout912 + gout889;
    Ciphertext gout916 = ctx.eval_lut(&gin916, xor2);
    std::cerr << "\rLUT917   ";
    Ciphertext gin917 = 2 * gout910 + gout908;
    Ciphertext gout917 = ctx.eval_lut(&gin917, xor2);
    std::cerr << "\rLUT918   ";
    Ciphertext gin918 = 2 * gout913 + gout906;
    Ciphertext gout918 = ctx.eval_lut(&gin918, and2);
    std::cerr << "\rLUT919   ";
    Ciphertext gin919 = 2 * gout910 + gout908;
    Ciphertext gout919 = ctx.eval_lut(&gin919, nand2);
    std::cerr << "\rLUT920   ";
    Ciphertext gin920 = 2 * gout913 + gout906;
    Ciphertext gout920 = ctx.eval_lut(&gin920, xor2);
    std::cerr << "\rLUT921   ";
    Ciphertext gin921 = 2 * gout911 + gout893;
    Ciphertext gout921 = ctx.eval_lut(&gin921, nand2);
    std::cerr << "\rLUT922   ";
    Ciphertext gin922 = 2 * gout914 + gout890;
    Ciphertext gout922 = ctx.eval_lut(&gin922, nand2);
    std::cerr << "\rLUT923   ";
    Ciphertext gin923 = 2 * gout912 + gout889;
    Ciphertext gout923 = ctx.eval_lut(&gin923, nand2);
    std::cerr << "\rLUT924   ";
    Ciphertext gin924 = 2 * gout914 + gout892;
    Ciphertext gout924 = ctx.eval_lut(&gin924, xor2);
    std::cerr << "\rLUT925   ";
    Ciphertext gin925 = 2 * gout909 + gout918;
    Ciphertext gout925 = ctx.eval_lut(&gin925, or2);
    std::cerr << "\rLUT926   ";
    Ciphertext gin926 = 2 * gout891 + gout922;
    Ciphertext gout926 = ctx.eval_lut(&gin926, nand2);
    std::cerr << "\rLUT927   ";
    Ciphertext gin927 = 2 * gout905 + gout919;
    Ciphertext gout927 = ctx.eval_lut(&gin927, nand2);
    std::cerr << "\rLUT928   ";
    Ciphertext gin928 = 2 * gout901 + gout920;
    Ciphertext gout928 = ctx.eval_lut(&gin928, nand2);
    std::cerr << "\rLUT929   ";
    Ciphertext gin929 = 2 * gout901 + gout920;
    Ciphertext gout929 = ctx.eval_lut(&gin929, xor2);
    std::cerr << "\rLUT930   ";
    Ciphertext gin930 = 2 * gout894 + gout921;
    Ciphertext gout930 = ctx.eval_lut(&gin930, nand2);
    std::cerr << "\rLUT931   ";
    Ciphertext gin931 = 2 * gout915 + gout924;
    Ciphertext gout931 = ctx.eval_lut(&gin931, xnor2);
    std::cerr << "\rLUT932   ";
    Ciphertext gin932 = 2 * gout888 + gout923;
    Ciphertext gout932 = ctx.eval_lut(&gin932, nand2);
    std::cerr << "\rLUT933   ";
    Ciphertext gin933 = 2 * (1 - gout915) + gout924;
    Ciphertext gout933 = ctx.eval_lut(&gin933, or2);
    std::cerr << "\rLUT934   ";
    Ciphertext gin934 = 2 * (1 - gout924) + gout915;
    Ciphertext gout934 = ctx.eval_lut(&gin934, or2);
    std::cerr << "\rLUT935   ";
    Ciphertext gin935 = 2 * gout930 + gout926;
    Ciphertext gout935 = ctx.eval_lut(&gin935, xor2);
    std::cerr << "\rLUT936   ";
    Ciphertext gin936 = 2 * gout927 + gout929;
    Ciphertext gout936 = ctx.eval_lut(&gin936, xor2);
    std::cerr << "\rLUT937   ";
    Ciphertext gin937 = 2 * gout925 + gout931;
    Ciphertext gout937 = ctx.eval_lut(&gin937, xnor2);
    std::cerr << "\rLUT938   ";
    Ciphertext gin938 = 2 * gout927 + gout929;
    Ciphertext gout938 = ctx.eval_lut(&gin938, nand2);
    std::cerr << "\rLUT939   ";
    Ciphertext gin939 = 2 * gout930 + gout926;
    Ciphertext gout939 = ctx.eval_lut(&gin939, nand2);
    std::cerr << "\rLUT940   ";
    Ciphertext gin940 = 2 * gout925 + gout934;
    Ciphertext gout940 = ctx.eval_lut(&gin940, nand2);
    std::cerr << "\rLUT941   ";
    Ciphertext gin941 = 2 * (1 - gout937) + gout916;
    Ciphertext gout941 = ctx.eval_lut(&gin941, and2);
    std::cerr << "\rLUT942   ";
    Ciphertext gin942 = 2 * gout916 + gout937;
    Ciphertext gout942 = ctx.eval_lut(&gin942, xnor2);
    std::cerr << "\rLUT943   ";
    Ciphertext gin943 = 2 * gout928 + gout938;
    Ciphertext gout943 = ctx.eval_lut(&gin943, nand2);
    std::cerr << "\rLUT944   ";
    Ciphertext gin944 = 2 * gout933 + gout940;
    Ciphertext gout944 = ctx.eval_lut(&gin944, nand2);
    std::cerr << "\rLUT945   ";
    Ciphertext gin945 = 2 * gout943 + gout942;
    Ciphertext gout945 = ctx.eval_lut(&gin945, xor2);
    std::cerr << "\rLUT946   ";
    Ciphertext gin946 = 2 * gout943 + gout942;
    Ciphertext gout946 = ctx.eval_lut(&gin946, and2);
    std::cerr << "\rLUT947   ";
    Ciphertext gin947 = 2 * gout935 + gout944;
    Ciphertext gout947 = ctx.eval_lut(&gin947, nand2);
    std::cerr << "\rLUT948   ";
    Ciphertext gin948 = 2 * gout935 + gout944;
    Ciphertext gout948 = ctx.eval_lut(&gin948, xor2);
    std::cerr << "\rLUT949   ";
    Ciphertext gin949 = 2 * gout941 + gout946;
    Ciphertext gout949 = ctx.eval_lut(&gin949, or2);
    std::cerr << "\rLUT950   ";
    Ciphertext gin950 = 2 * gout939 + gout947;
    Ciphertext gout950 = ctx.eval_lut(&gin950, nand2);
    std::cerr << "\rLUT951   ";
    Ciphertext gin951 = 2 * gout932 + gout948;
    Ciphertext gout951 = ctx.eval_lut(&gin951, nand2);
    std::cerr << "\rLUT952   ";
    Ciphertext gin952 = 2 * gout932 + gout948;
    Ciphertext gout952 = ctx.eval_lut(&gin952, xor2);
    std::cerr << "\rLUT953   ";
    Ciphertext gin953 = 2 * gout949 + gout952;
    Ciphertext gout953 = ctx.eval_lut(&gin953, nand2);
    std::cerr << "\rLUT954   ";
    Ciphertext gin954 = 2 * gout949 + gout952;
    Ciphertext gout954 = ctx.eval_lut(&gin954, xor2);
    std::cerr << "\rLUT955   ";
    Ciphertext gin955 = 2 * gout951 + gout953;
    Ciphertext gout955 = ctx.eval_lut(&gin955, nand2);
    std::cerr << "\rLUT956   ";
    Ciphertext gin956 = 2 * gout950 + gout955;
    Ciphertext gout956 = ctx.eval_lut(&gin956, and2);
    std::cerr << "\rLUT957   ";
    Ciphertext gin957 = 2 * gout950 + gout955;
    Ciphertext gout957 = ctx.eval_lut(&gin957, xor2);

    std::cerr << "\r          \r";
    std::vector<long> test_out;
    test_out.push_back(ctx.decrypt(&gout316)); /* out192 */
    test_out.push_back(ctx.decrypt(&gout506)); /* out193 */
    test_out.push_back(ctx.decrypt(&gout627)); /* out194 */
    test_out.push_back(ctx.decrypt(&gout741)); /* out195 */
    test_out.push_back(ctx.decrypt(&gout824)); /* out196 */
    test_out.push_back(ctx.decrypt(&gout859)); /* out197 */
    test_out.push_back(ctx.decrypt(&gout897)); /* out198 */
    test_out.push_back(ctx.decrypt(&gout917)); /* out199 */
    test_out.push_back(ctx.decrypt(&gout936)); /* out200 */
    test_out.push_back(ctx.decrypt(&gout945)); /* out201 */
    test_out.push_back(ctx.decrypt(&gout954)); /* out202 */
    test_out.push_back(ctx.decrypt(&gout957)); /* out203 */
    test_out.push_back(ctx.decrypt(&gout956)); /* out204 */
    return test_out;
}

static void
BM_sum3d_boolean(benchmark::State& state)
{
    FHEContext ctx;
    ctx.generate_context(tfhe_11_NTT);
    ctx.set_default_message_encoding_type(partial_domain);
    ctx.set_default_plaintext_space(4);

    for (auto _ : state) {
        test_sum3d_boolean(ctx);
    }
}

BENCHMARK(BM_sum3d_boolean)->Unit(benchmark::kSecond);
BENCHMARK_MAIN();
